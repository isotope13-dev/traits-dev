defaults:
  platforms: [windows, unix]
  for: [chrome-manifest]
  crit: notable
  conf: 0.99

traits:
  - id: extension-name
    desc: Declares Adobe Express extension identity
    if:
      type: value
      path: name
      regex: '^Adobe Express(:|$)'
