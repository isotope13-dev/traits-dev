defaults:
  platforms: [unix]
  for: [elf, binaries, tar]
  crit: notable
  conf: 0.98
  size_max: 300000000

traits:
  - id: shotwell-package-member
    desc: Shotwell package member
    if:
      type: path
      regex: 'shotwell-[0-9][^\\/]*|(^|[\\/])shotwell$'
