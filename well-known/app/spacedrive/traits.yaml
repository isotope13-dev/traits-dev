defaults:
  platforms: [unix]
  for: [rust]
  crit: notable
  conf: 0.96
  size_max: 200000

traits:
  - id: spacedrive-tauri-main
    desc: Spacedrive Tauri main source
    if:
      type: path
      regex: 'src-tauri/src/main\.rs$'
