defaults:
  platforms: [unix, windows]
  for: [source, scripts, documents, data]
  size_max: 52428800
traits:
  - id: notebook-scripts
    desc: Kubeflow community notebook scripts
    crit: notable
    conf: 0.99
    if:
      type: path
      regex: 'scripts[\\/][^\\/]+\.ipynb$'
