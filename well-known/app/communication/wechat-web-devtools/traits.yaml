defaults:
  platforms: [macos]
  for: [shell]
  crit: baseline
  conf: 0.98

traits:
  - id: wechat-cli-path
    desc: WeChat Web Developer Tools CLI path
    crit: notable
    if:
      type: text
      substr: wechatwebdevtools.app/Contents/MacOS/cli
