defaults:
  platforms: [unix]
  for: [binaries, scripts, source, php]
  crit: notable
  conf: 0.97
  size_max: 600000000

traits:
  - id: postfixadmin-source-path
    desc: PostfixAdmin source tree path
    if:
      type: path
      regex: 'postfixadmin[\\/]'
