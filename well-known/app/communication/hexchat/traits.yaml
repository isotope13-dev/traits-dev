defaults:
  platforms: [unix, windows]
  for: [source, archives]
  crit: notable
  conf: 0.99

traits:
  - id: source-tree
    desc: HexChat source or release archive path
    if:
      type: path
      regex: 'hexchat(-[0-9]|@v[0-9])'
