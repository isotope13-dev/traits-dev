defaults:
  platforms: [unix]
  for: [elf, so, source, text, tar]
  size_max: 268435456
  crit: notable
  conf: 0.97

traits:
  - id: mail-client
    desc: Claws Mail client component
    if:
      type: path
      regex: '(^|[\\/])claws-mail([\\/]|-|$)'
