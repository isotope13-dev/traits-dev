defaults:
  platforms: [freebsd, unix]
  crit: notable
  conf: 0.98
  size_min: 1
  size_max: 600000000

traits:
  - id: freebsd-package-member
    desc: Calligra FreeBSD package member
    for: [class, jar, java, data, shell, elf]
    if:
      type: path
      substr: calligra-
