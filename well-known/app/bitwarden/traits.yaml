defaults:
  platforms: [linux]
  for: [elf]
  crit: component
  conf: 0.95

traits:
  - id: libffmpeg-basename
    desc: FFmpeg shared library basename
    size_min: 262144
    size_max: 10485760
    if:
      type: basename
      exact: libffmpeg.so
