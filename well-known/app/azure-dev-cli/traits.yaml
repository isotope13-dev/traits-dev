defaults:
  platforms: [unix, windows]
  for: [source, scripts, manifests]
  size_min: 1
  size_max: 200000000
traits:
  - id: azure-dev-cli-source-path
    desc: Azure Developer CLI source tree
    crit: notable
    conf: 0.97
    if:
      type: path
      regex: 'azure-dev-cli|azure-dev-azure-dev-cli'
