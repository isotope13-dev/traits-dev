defaults:
  platforms: [unix, windows]

traits:
  - id: official-azd-installer-command
    desc: Official Azure Developer CLI installer command
    crit: notable
    conf: 1.0
    for: [markdown]
    if:
      type: text
      regex: '(?i)https://aka\.ms/install-azd\.sh\s*\|\s*bash\b'
