defaults:
  platforms: [linux, macos, windows]
  for: [elf, macho, pe, deb]
  crit: baseline
  conf: 0.95
  size_max: 1073741824

traits:
  - id: package-name
    desc: hopenpgp-tools package marker
    if:
      type: path
      basename: true
      substr: hopenpgp-tools

  - id: command-name
    desc: hopenpgp-tools command name
    if:
      type: path
      basename: true
      regex: '^h(op|ot|kt|okey)$'

composite_rules:
  - id: release-path
    desc: hopenpgp-tools release path
    crit: baseline
    any:
      - id: well-known/app/hopenpgp-tools::command-name
      - id: well-known/app/hopenpgp-tools::package-name
