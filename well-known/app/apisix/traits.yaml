defaults:
  platforms: [unix]
  for: [go, shell, text, archive]
  crit: baseline
  conf: 0.96

traits:
  - id: apisix-chaos-test-path
    desc: Apache APISIX chaos test path
    if:
      type: path
      regex: 't/chaos'

