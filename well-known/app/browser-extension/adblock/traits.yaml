defaults:
  crit: notable
  conf: 0.98
  platforms: [windows, unix]
  for: [crx, archives]
  size_max: 120000000
traits:
  - id: chrome-extension
    desc: AdBlock Chrome extension archive
    if:
      type: path
      substr: gighmmpiobklfepjocnamgkkbiglidom.crx
  - id: edge-extension
    desc: AdBlock Edge extension archive
    if:
      type: path
      substr: ndcileolkflehcjpmjnfbnaibdcgglog.crx
