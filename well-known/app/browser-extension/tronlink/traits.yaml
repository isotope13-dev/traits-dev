defaults:
  platforms: [windows, unix]
  for: [javascript, text, crx, xpi, archives]
  size_max: 120000000
traits:
  - id: extension-marker
    desc: TronLink wallet marker
    crit: notable
    conf: 0.98
    if:
      type: text
      word: TronLink
