defaults:
  platforms: [unix, windows]
  crit: notable
  conf: 0.99

traits:
  - id: package-name
    desc: Sellable Admin installer package identity
    for: [package.json]
    if:
      type: value
      path: name
      exact: '@sellable/admin-install'
