defaults:
  platforms: [windows, unix]
  for: [python, shell, javascript, typescript]

traits:
  - id: marimo-app-root
    desc: Marimo app root path
    crit: notable
    conf: 0.9
    if:
      type: text
      substr: "/app/marimo"
