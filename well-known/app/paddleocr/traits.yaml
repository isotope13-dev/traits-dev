defaults:
  platforms: [unix, windows]

traits:
  - id: paddleocr-source-tree
    desc: PaddleOCR upstream source tree path
    crit: notable
    conf: 0.99
    for: [python, shell]
    if:
      type: path
      regex: '(^|[\\/])PaddleOCR-[0-9][^\\/]*'
