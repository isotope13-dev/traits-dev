defaults:
  platforms: [unix]
  crit: baseline
  conf: 0.96
  size_max: 16777216

traits:
  - id: source-tree
    desc: niri window manager source
    for: [rust, shell]
    if:
      type: basename
      regex: '(^|[\\/])niri-[0-9.]+[\\/]'
