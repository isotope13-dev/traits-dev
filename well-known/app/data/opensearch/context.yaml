defaults:
  platforms: [unix, windows]

composite_rules:
  - id: gce-plugin-context
    desc: Recognized OpenSearch Discovery GCE plugin context
    crit: exception
    conf: 0.99
    scope: archive
    all:
      - id: gce-plugin-path
      - id: gce-metadata-service-class
