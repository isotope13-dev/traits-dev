# PostgreSQL's own source tree.
defaults:
  platforms: [unix, windows]
traits:
- id: postgresql-source-path
  desc: PostgreSQL source-tree path
  crit: notable
  conf: 0.99
  for: [shell, vbs]
  if:
    type: path
    regex: '(^|/)postgresql-[0-9][^/]*'
