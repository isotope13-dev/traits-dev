defaults:
  platforms: [unix, windows]
  for: [php]

traits:
  - id: single-file-database-manager-header
    desc: Adminer single-file database manager header
    crit: notable
    conf: 0.99
    size_min: 100000
    size_max: 2000000
    if:
      type: text
      substr: "/** Adminer - Compact database management"
