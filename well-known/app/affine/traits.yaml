defaults:
  platforms: [windows, unix]
  for: [javascript, typescript]

traits:
  - id: affine-project-marker
    desc: AFFiNE project marker
    crit: baseline
    conf: 1.0
    if:
      type: text
      substr: 'toeverything/AFFiNE'
