defaults:
  platforms: [unix, windows]
  for: [python, shell, javascript, typescript]
  crit: baseline
  conf: 0.96
  size_max: 16777216

traits:
  - id: source-tree
    desc: Anki application source tree
    if:
      type: basename
      regex: '(^|[\\/])anki-[0-9.]+[\\/]'
