defaults:
  platforms: [unix, windows]
  for: [whl, python, pyc]

traits:
  - id: gung12-package
    desc: gung12 web vulnerability scanner
    crit: notable
    conf: 0.99
    if:
      type: path
      regex: '(?i)(^|[\\/])gung12([\\/]|-[0-9])'
