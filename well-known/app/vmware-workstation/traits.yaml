defaults:
  platforms: [linux]
  for: [tar, archives, shell, text]

traits:
  - id: vmware-workstation-pkgbuild
    desc: VMware Workstation PKGBUILD source package
    crit: baseline
    conf: 0.96
    if:
      type: text
      substr: pkgname=vmware-workstation
