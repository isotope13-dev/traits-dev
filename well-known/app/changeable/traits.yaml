defaults:
  platforms: [windows, unix]
  for: [chrome-manifest, crx]
  crit: notable
  conf: 0.99

traits:
  - id: manifest-name
    desc: Changeable Chrome extension identity
    for: [chrome-manifest]
    if:
      type: value
      path: name
      exact: Changeable

