defaults:
  size_max: 262144
  platforms: [windows]
  for: [pe]
  crit: baseline
  conf: 0.95

traits:
  - id: tls-callback-demo-pdb
    desc: TLS callback demo PDB
    crit: notable
    conf: 0.98
    if:
      type: kv
      path: debug.pdb_path
      regex: '(?i)[\\/]TLSCallback[\\/]Debug[\\/]TLSCallback\.pdb$'
