defaults:
  platforms: [windows, unix]
  for: [pe, elf, macho]
  crit: baseline
  conf: 0.96
  size_max: 268435456

traits:
  - id: application-bundle
    desc: Qdrant vector database binary
    if:
      type: basename
      regex: '(^|[\\/])qdrant(\.exe)?$'
