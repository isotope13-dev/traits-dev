defaults:
  platforms: [unix, windows]

traits:
  - id: seed-fixture-tree
    desc: Moonshiner seed-fixture tree member
    crit: notable
    conf: 0.99
    for: [powershell, shell, pe, nupkg]
    size_min: 100
    size_max: 100000000
    if:
      type: path
      regex: 'moonshiner_app'
