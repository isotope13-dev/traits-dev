defaults:
  platforms: [unix, windows]

composite_rules:
  - id: extension-auth-context
    desc: CodeGPT extension authenticated service context
    crit: exception
    conf: 0.99
    scope: archive
    all:
      - id: archive-name
      - id: package-name
