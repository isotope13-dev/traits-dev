defaults:
  platforms: [unix, windows]

composite_rules:
  - id: extension-auth-context
    desc: Recognized dscodegpt VS Code extension context
    crit: exception
    conf: 0.99
    scope: archive
    all:
      - id: archive-name
      - id: vsix-id
