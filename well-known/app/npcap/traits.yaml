defaults:
  platforms: [windows]
  for: [pe]

traits:
  - id: npcap-installer
    desc: Npcap Windows installer identity
    crit: notable
    conf: 0.95
    size_min: 200000
    size_max: 2000000
    if:
      type: value
      path: pe.version.description
      regex: '(?i)\bnpcap\b'
