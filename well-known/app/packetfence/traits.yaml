defaults:
  platforms: [windows, unix]
  for: [pe, shell, text, archive]
  crit: baseline
  conf: 0.97
  size_max: 50000000

traits:
  - id: packetfence-windows-agent-path
    desc: PacketFence Windows agent binary path
    if:
      type: path
      basename: true
      exact: packetfence-windows-agent.exe
