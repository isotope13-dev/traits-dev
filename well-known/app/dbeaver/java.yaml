defaults:
  platforms: [unix, windows]
  for: [jar, class, java_class]
  crit: notable
  conf: 0.99

traits:
  - id: dbeaver-ui-namespace
    desc: DBeaver UI namespace
    if:
      type: import
      regex: '^org/jkiss/dbeaver/ui/'
