defaults:
  platforms: [unix, windows]

traits:
  - id: official-flyctl-installer-command
    desc: Official Fly.io flyctl installer command
    crit: notable
    conf: 1.0
    for: [markdown]
    if:
      type: text
      regex: '(?i)https://fly\.io/install\.sh[^;\n]{0,24}sh\b'
