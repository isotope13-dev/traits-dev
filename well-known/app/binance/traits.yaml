defaults:
  platforms: [unix, windows]
  for: [scripts, binaries]
  size_min: 1024
  size_max: 500000000

traits:
  - id: desktop-bundle-identifier
    desc: Binance Desktop bundle identifier
    crit: notable
    conf: 0.8
    if:
      type: text
      substr: com.binance.BinanceDesktop
