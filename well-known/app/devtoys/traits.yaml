defaults:
  platforms: [windows]
  for: [pe, zip]
  crit: baseline
  conf: 0.95

traits:
  - id: release-path
    desc: DevToys release package path marker
    size_min: 1
    size_max: 1073741824
    if:
      type: path
      basename: true
      substr: DevToys
      case_insensitive: true
