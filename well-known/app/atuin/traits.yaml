defaults:
  platforms: [windows, unix]
  for: [pe, elf, macho]
  crit: baseline
  conf: 0.98
traits:
  - id: atuin-binary
    desc: Atuin shell history binary
    size_max: 50000000
    if:
      type: path
      basename: true
      regex: '^atuin(\.exe)?$'
