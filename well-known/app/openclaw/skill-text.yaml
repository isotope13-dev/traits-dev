defaults:
  platforms:
  - windows
  - unix
  for:
  - markdown
  crit: component
  conf: 0.9
  size_max: 5000000
traits:
- id: openclaw-skill-text
  desc: OpenClaw skill text marker
  if:
    type: text
    regex: "(?i)\\b(OpenClaw|Clawdbot|Moltbot|Molty)\\b"
