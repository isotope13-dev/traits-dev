defaults:
  platforms: [unix, windows]

traits:
  - id: monado-source-tree
    desc: Monado source tree path
    crit: notable
    conf: 0.99
    for: [powershell]
    if:
      type: path
      regex: '(^|[\\/])monado-[0-9][^\\/]*'
