defaults:
  platforms: [unix]
  for: [tar]
  crit: notable
  conf: 0.98

traits:
  - id: unionlabs-testnet-release-bundle
    desc: Union Labs Union testnet release bundle archive
    if:
      type: basename
      regex: '^unionlabs-union-bundle-union-testnet-[0-9].*\.tar\.gz$'
