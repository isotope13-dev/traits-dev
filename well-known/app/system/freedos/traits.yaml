defaults:
  platforms: [windows]
  for: [batch]

traits:
  - id: freedos-installer-config-script
    desc: FreeDOS installer configuration script
    crit: notable
    conf: 0.99
    if:
      type: text
      regex: '(?i)\bFDI(SET|FAIL)\.BAT\b'
