defaults:
  platforms: [linux]
  crit: notable
  conf: 0.96

traits:
  - id: source-tree
    desc: Omarchy source tree path
    for: [source, scripts, shell]
    if:
      type: path
      regex: 'omarchy-[0-9][^\\/]*/'
