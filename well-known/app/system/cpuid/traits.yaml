defaults:
  platforms: [windows]
  for: [pe]
  size_max: 10485760

traits:
  - id: signer
    desc: CPUID signed installer PE
    crit: notable
    conf: 0.95
    if:
      type: value
      path: pe.signatures[0].subject
      regex: '\bCN=CPUID\b'
