defaults:
  platforms: [unix, windows]
  for: [source, shell, makefile, text, html, xml, tar]
  size_max: 268435456
  crit: notable
  conf: 0.96

traits:
  - id: source-tree
    desc: Apache httpd source tree
    if:
      type: path
      regex: '(^|[\\/])httpd-[0-9][^\\/]*'
