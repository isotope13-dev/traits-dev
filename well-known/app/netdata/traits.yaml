defaults:
  platforms:
  - unix
  - windows
  for:
  - binaries
  - scripts
  size_max: 5000000
traits:
- id: netdata-path--rx-1
  desc: Repeated Netdata name
  crit: component
  conf: 0.95
  count_min: 20
  if:
    type: text
    case_insensitive: true
    substr: netdata
  count_max: 1011966
- id: netdata-path--rx-2
  desc: Repeated Netdata name
  crit: component
  conf: 0.95
  count_min: 20
  if:
    type: text
    case_insensitive: true
    substr: NETDATA
  count_max: 1011967
composite_rules:
- id: netdata-agent
  desc: Netdata monitoring agent identity
  crit: notable
  conf: 0.95
  needs: 2
  any:
  - id: netdata-path--rx-1
  - id: netdata-path--rx-2
