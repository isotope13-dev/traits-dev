defaults:
  platforms: [unix, windows]
  for: [shell, c, cpp]
  crit: notable
  conf: 0.99

traits:
  - id: source-file-marker
    desc: CFEngine source file marker
    crit: notable
    if:
      type: text
      regex: 'This file is part of CFEngine 3|CFEngine 3'
