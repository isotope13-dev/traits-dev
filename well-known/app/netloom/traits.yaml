defaults:
  platforms: [unix]
  for: [source, scripts, manifests, documents, data, archives, binaries]
  crit: notable
  conf: 0.99
  size_min: 1
  size_max: 200000000

traits:
  - id: source-tree
    desc: netloom source tree member
    if:
      type: path
      substr: github.com/jimyag/netloom@
