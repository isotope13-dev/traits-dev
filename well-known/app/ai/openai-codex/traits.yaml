defaults:
  platforms: [unix, windows]
  for: [python, javascript, typescript, shell, package.json, json]
  crit: notable
  conf: 0.95

traits:
  - id: latest-package-spec
    desc: OpenAI Codex latest package specifier
    if:
      type: literal
      kind: string
      exact: '@openai/codex@latest'
