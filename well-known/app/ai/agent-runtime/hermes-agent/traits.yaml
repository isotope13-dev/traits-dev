defaults:
  platforms: [windows, unix]
  for: [scripts, source, json, markdown]
  crit: notable
  conf: 0.96

traits:
  - id: installer-url-reference
    desc: References the Nous Hermes agent installer
    if:
      type: text
      substr: raw.githubusercontent.com/NousResearch/hermes-agent
