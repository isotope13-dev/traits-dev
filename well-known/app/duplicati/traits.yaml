defaults:
  platforms: [unix, windows]
  for: [pe, dll, shell, powershell]
  crit: baseline
  conf: 0.96
  size_max: 134217728

traits:
  - id: application-bundle
    desc: Duplicati backup app bundle
    crit: notable
    if:
      type: path
      regex: 'duplicati-2\\.[0-9.]+|(^|[\\/])Duplicati\\.'
