defaults:
  platforms: [unix, windows]
  for: [data]
  crit: baseline
  conf: 0.96
  size_max: 8388608

traits:
  - id: firmware-image
    desc: Tasmota firmware image
    if:
      type: basename
      regex: '.{0,120}tasmota.{0,80}\.bin$'
      case_insensitive: true
