defaults:
  platforms: [unix]
  for: [shell, text, archive]
  crit: notable
  conf: 0.96

traits:
  - id: freefem-release-script-path
    crit: notable
    desc: FreeFem Jenkins release publishing script path
    if:
      type: path
      regex: 'jenkins/deployRelease'
