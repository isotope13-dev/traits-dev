defaults:
  platforms: [windows]
  for: [powershell, nupkg, pe, dll]

traits:
  - id: product-name
    desc: References Microsoft SharePoint
    crit: notable
    conf: 0.82
    size_min: 100
    if:
      type: text
      word: SharePoint
