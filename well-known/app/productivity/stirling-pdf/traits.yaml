defaults:
  platforms: [linux, windows, macos]
  crit: notable
  conf: 0.96

traits:
  - id: source-tree
    desc: Stirling-PDF source tree path
    for: [source, scripts, shell, javascript, typescript, github-actions, pdf, docx]
    if:
      type: path
      regex: 'Stirling-PDF-[0-9][^\\/]*/'
