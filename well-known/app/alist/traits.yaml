defaults:
  platforms: [linux, macos, windows]
  for: [binaries, elf, macho, pe, tar, javascript]
  crit: baseline
  conf: 0.95

traits:
  - id: release-path
    desc: Alist release package path marker
    size_min: 1
    size_max: 1073741824
    if:
      type: basename
      substr: alist
      case_insensitive: true
