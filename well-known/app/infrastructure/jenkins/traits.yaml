defaults:
  platforms: [unix]
  for: [shell]
  crit: notable
  conf: 0.97
  size_max: 1048576

traits:
  - id: service-home-path
    desc: Jenkins service home directory
    if:
      type: text
      substr: /var/lib/jenkins
