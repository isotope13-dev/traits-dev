defaults:
  platforms: [unix]
  for: [source, scripts, shell, python, archives]
  crit: notable
  conf: 0.98
  size_max: 300000000

traits:
  - id: mgmt-source-tree
    desc: mgmt source tree
    if:
      type: path
      regex: 'mgmt-[0-9][^\\/]*'
