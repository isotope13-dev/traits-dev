defaults:
  platforms: [unix, windows]

traits:
  - id: pipewire-source-tree
    desc: PipeWire source tree path
    crit: notable
    conf: 0.99
    for: [c]
    if:
      type: path
      regex: '(^|[\\/])pipewire(-|@)[^\\/]+'
