defaults:
  platforms: [unix, windows]
  for: [python]
traits:
  - id: aws-sam-cli-package-path
    desc: AWS SAM CLI package path
    crit: notable
    conf: 0.96
    if:
      type: path
      regex: '(^|[\\/])samcli[\\/]'
