defaults:
  for: [pe]
  platforms: [windows]
  size_min: 1048576
  crit: notable
  conf: 0.98

traits:
  - id: prometheus-go-module
    desc: Prometheus Go module identity
    if:
      type: value
      path: pe.go.module.path
      exact: github.com/prometheus/prometheus
