defaults:
  platforms: [unix, windows]
  for: [python, markdown, json]
  crit: notable
  conf: 0.94

traits:
  - id: python-package-source
    desc: Puppetmaster package source context
    if:
      type: text
      regex: 'from puppetmaster\.|import puppetmaster\b|Puppetmaster.{0,80}MCP server'
