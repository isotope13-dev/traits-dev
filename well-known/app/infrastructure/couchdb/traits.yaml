defaults:
  platforms: [linux]
  for: [alpine_apk, elf, shell, text]
  crit: notable
  conf: 0.99
  size_min: 64
  size_max: 268435456

traits:
  - id: couchdb-package
    desc: CouchDB package identity
    if:
      type: path
      regex: '(?i)couchdb-[0-9].{0,16}\.apk$|[\\/]couchdb[\\/]'
