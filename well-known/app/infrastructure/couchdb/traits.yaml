defaults:
  platforms: [unix, linux]
  for: [apk, archives, elf, shell, text]
  crit: notable
  conf: 0.99

traits:
  - id: couchdb-package
    desc: CouchDB package identity
    if:
      type: path
      regex: '(?i)(^|[\\/])couchdb-[0-9][^\\/]*\.apk$|(^|[\\/])usr[\\/]share[\\/]couchdb[\\/]'
