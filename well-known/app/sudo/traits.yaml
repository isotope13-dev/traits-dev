defaults:
  platforms: [unix]
  for: [source, c, shell, text, tar]
  size_max: 268435456
  crit: notable
  conf: 0.96

traits:
  - id: source-tree
    desc: sudo source tree
    if:
      type: path
      regex: '(^|[\\/])sudo-[0-9][^\\/]*'
