defaults:
  platforms: [linux, macos, windows]
  for: [binaries, elf, macho, pe, shell, tar]
  crit: baseline
  conf: 0.95

traits:
  - id: release-path
    desc: Fuel Sway release package
    if:
      type: basename
      regex: 'FuelLabs-sway|forc-binaries|[\\/]forc(-[a-z]+)?$'
