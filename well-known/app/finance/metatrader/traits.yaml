defaults:
  platforms: [windows]
  for: [pe]
  size_min: 524288
  size_max: 8388608

traits:
  - id: metatrader-setup-manifest
    desc: MetaTrader Setup manifest identity
    crit: notable
    conf: 0.995
    if:
      type: value
      path: pe.manifest.assembly_identity.name
      exact: MetaTrader Setup
