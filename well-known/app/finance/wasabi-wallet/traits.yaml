defaults:
  platforms: [windows, unix]
  for: [source, scripts]

traits:
  - id: product-name
    desc: Wasabi Wallet product reference
    crit: notable
    conf: 0.9
    if:
      type: text
      substr: WasabiWallet
