defaults:
  platforms: [windows, unix]
  for: [javascript, chrome-manifest]

traits:
  - id: extension-runtime
    desc: LocalGuard extension runtime bundle
    crit: notable
    conf: 0.98
    if:
      type: text
      substr: localguard_config
