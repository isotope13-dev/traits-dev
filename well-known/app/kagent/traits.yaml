defaults:
  platforms: [unix, windows]
  crit: notable
  conf: 0.95
  size_min: 1
  size_max: 200000000

traits:
  - id: kagent-source-path
    desc: kagent source tree path
    for: [archives, source, scripts, shell, python, javascript, typescript, go, markdown, json, dockerfile, github-actions]
    if:
      type: path
      substr: kagent-0.9.7/
