defaults:
  platforms: [unix]
  crit: baseline
  conf: 0.98
traits:
  - id: shadowsocks-libev-source
    desc: shadowsocks-libev source tree
    crit: notable
    for: [shell, c, cpp, dockerfile]
    if:
      type: path
      substr: shadowsocks-libev
