defaults:
  platforms: [android]
  for: [android_apk]
  crit: notable
  conf: 0.99

traits:
  - id: shadowsocks-android-apk
    desc: Shadowsocks Android VPN package
    if:
      type: path
      regex: '(?i)(^|[\\/])shadowsocks-android-v[0-9][^\\/]*\.apk$'
