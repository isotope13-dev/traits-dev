defaults:
  platforms: [windows]
  for: [pe, dll]

traits:
  - id: guna-ui2-library-marker
    desc: Guna.UI2 .NET UI component library marker
    crit: notable
    conf: 0.99
    size_min: 100000
    size_max: 5000000
    if:
      type: text
      exact: Guna.UI2
      section: .text
