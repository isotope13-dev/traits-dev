defaults:
  platforms: [linux, windows, macos]
  for: [elf, macho, pe, dylib, so, dll]
  size_max: 52428800
traits:
  - id: snowball-stemmer-export
    desc: Snowball stemmer API export
    crit: notable
    conf: 0.98
    count_min: 3
    if:
      type: export
      regex: '^sb_stemmer_(new|stem|delete|length|list)$'
