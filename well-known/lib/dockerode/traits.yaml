defaults:
  platforms: [windows, unix]
  for: [javascript, typescript]
  crit: notable
  conf: 0.99

traits:
  - id: dockerode-import
    desc: dockerode Node Docker Engine client import
    if:
      type: text
      regex: '\bfrom\s*["'']dockerode["'']'
