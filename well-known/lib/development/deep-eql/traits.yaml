# chaijs/deep-eql — deep equality helper used by Chai.
defaults:
  platforms: [unix, windows]
  for: [javascript]
  crit: notable
  conf: 0.98

traits:
  - id: chai-deepeql-weakmap-key
    desc: Chai deep-eql WeakMap key prefix
    if:
      type: text
      substr: 'chai/deep-eql__'
