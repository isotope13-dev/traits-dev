# mypy — the official Python static type checker distribution.
defaults:
  platforms: [unix, windows]
  for: [whl]
  crit: notable
  conf: 1.0

traits:
  - id: distribution
    desc: mypy Python type-checker distribution
    if:
      type: value
      path: whl.distribution
      exact: mypy
