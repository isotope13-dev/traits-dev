# PHPStan ships as a PHAR; the path names the analyser.
defaults:
  platforms: [unix, windows]
traits:
- id: phpstan-phar-path
  desc: PHPStan PHAR vendor artifact path
  crit: notable
  conf: 0.98
  for: [php]
  if:
    type: path
    regex: '(^|[\\/])phpstan\.phar$'
