# DefinitelyTyped @types/fs-extra — TypeScript types for fs-extra.
defaults:
  platforms: [unix, windows]
  for: [package.json, json]
  crit: notable
  conf: 0.99

traits:
  - id: package-name
    desc: '@types/fs-extra package name'
    if:
      type: value
      path: name
      exact: '@types/fs-extra'
