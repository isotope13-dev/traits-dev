defaults:
  platforms: [windows, unix]
  for: [php]
  crit: notable
  conf: 0.98

traits:
  - id: mockery-source-tree
    desc: Mockery PHP library source tree
    if:
      type: path
      regex: '(?i)(^|[\\/])mockery([\\/]|$)'
