# DefinitelyTyped @types/slice-ansi — TypeScript types for slice-ansi.
defaults:
  platforms: [unix, windows]
  for: [package.json, json]
  crit: notable
  conf: 0.99

traits:
  - id: package-name
    desc: '@types/slice-ansi package name'
    if:
      type: value
      path: name
      exact: '@types/slice-ansi'
