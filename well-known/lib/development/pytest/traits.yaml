defaults:
  platforms: [unix, windows]
  for: [python]
traits:
  - id: pytest-package-path
    desc: pytest package path
    crit: notable
    conf: 0.96
    if:
      type: path
      regex: '(^|[\\/])_pytest[\\/]'
