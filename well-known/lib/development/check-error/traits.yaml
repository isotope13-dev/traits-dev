# chaijs/check-error — error-shape helper used by Chai.
defaults:
  platforms: [unix, windows]
  for: [javascript]
  crit: notable
  conf: 0.98

traits:
  - id: chai-checkerror-banner
    desc: Chai checkError utility banner
    if:
      type: text
      substr: 'Chai - checkError utility'
