defaults:
  platforms: [unix, windows]
  for: [php, text]
  crit: notable
  conf: 0.99
  size_min: 1
  size_max: 5000000

traits:
  - id: nusoap-library-member
    desc: NuSOAP web-services library member
    if:
      type: path
      regex: '(?i)(^|[\\/])nusoap([\\/]|$)'
