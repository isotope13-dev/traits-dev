defaults:
  platforms: [unix, windows]
  for: [python]
  crit: baseline
  conf: 0.96
  size_max: 8388608

traits:
  - id: source-tree
    desc: Pygments syntax lexer tables
    if:
      type: basename
      regex: '(^|[\\/])pygments[\\/](lexers|formatters|unistring\\.py)'
