defaults:
  platforms: [unix, windows]
  for: [php]
  crit: notable
  conf: 0.98
  size_max: 5000000

traits:
  - id: php-codesniffer-library
    desc: PHP_CodeSniffer source library path
    if:
      type: path
      regex: '(?i)(^|[\\/])php-codesniffer-php-token([\\/]|$)'
