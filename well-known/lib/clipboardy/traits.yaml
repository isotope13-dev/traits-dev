defaults:
  platforms: [unix, windows]
  for: [javascript, pe]
  crit: notable
  conf: 0.98
  size_min: 1
  size_max: 5000000

traits:
  - id: clipboardy-package-path
    desc: clipboardy package path
    if:
      type: path
      regex: '(^|[\\/])clipboardy([\\/]|$)'
