defaults:
  platforms: [windows, linux, macos]
  for: [javascript, typescript, pe]
  size_max: 209715200

traits:
  - id: library-banner
    desc: Leaflet interactive maps library banner
    crit: notable
    conf: 0.99
    if:
      type: text
      regex: 'Leaflet [0-9]+\.[0-9]+\.[0-9]+, a JS library for interactive maps'
