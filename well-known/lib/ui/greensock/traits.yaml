defaults:
  platforms: [windows, unix]
  for: [javascript]
traits:
  - id: greensock-tweenmax-suite-banner
    desc: GreenSock TweenMax animation suite banner
    crit: notable
    conf: 0.98
    if:
      type: text
      regex: 'Includes all of the following:\s*TweenLite,\s*TweenMax,\s*TimelineLite,\s*TimelineMax'
