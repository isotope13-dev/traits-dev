defaults:
  platforms: [windows, linux, macos]
  for: [javascript]
  size_max: 209715200

traits:
  - id: library-marker
    desc: CreateJS canvas library
    crit: notable
    conf: 0.95
    if:
      type: text
      substr: 'this.createjs=this.createjs||{}'
