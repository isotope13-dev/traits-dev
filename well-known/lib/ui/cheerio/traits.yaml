defaults:
  platforms: [linux, macos, windows]
  for: [javascript]
  crit: notable
  conf: 0.96

traits:
  - id: cheerio-bundle-file
    desc: Cheerio JavaScript bundle file
    if:
      type: path
      regex: '(^|[\\/])(node_modules[\\/])?cheerio\.js$'
