# Elastic UI Component Library identity.
defaults:
  platforms: [unix, windows]
  for: [package.json]
  crit: notable
  conf: 0.99

traits:
  - id: elastic-eui-package
    desc: Elastic UI Component Library package metadata
    if:
      type: value
      path: name
      exact: '@elastic/eui'
