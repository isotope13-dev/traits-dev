defaults:
  for: [makefile]
  platforms: [unix, windows]
  crit: notable
  conf: 0.99

traits:
  - id: glfw-opengl3-example-makefile
    desc: Dear ImGui GLFW OpenGL3 example Makefile
    if:
      type: text
      regex: '(?m)^EXE\s*=\s*example_glfw_opengl3\s*$'
