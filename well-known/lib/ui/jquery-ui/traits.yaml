defaults:
  platforms: [windows, unix]
  for: [javascript]
traits:
  - id: jquery-ui-version-banner
    desc: jQuery UI library version banner
    crit: notable
    conf: 0.98
    if:
      type: text
      regex: 'jQuery UI - v\d+\.\d+\.\d+'
