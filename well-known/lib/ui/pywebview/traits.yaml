defaults:
  platforms: [android, unix, windows]
  for: [class]
  crit: notable
  conf: 0.99

traits:
  - id: android-client-class
    desc: pywebview Android WebView client class
    if:
      type: value
      path: class.this_class
      exact: com/pywebview/PyWebViewClient
