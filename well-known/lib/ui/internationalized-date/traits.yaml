# Adobe @internationalized/date — calendar/date math helpers.
defaults:
  platforms: [unix, windows]
  for: [javascript, typescript]
  crit: notable
  conf: 0.99

traits:
  - id: gregorian-julian-epoch
    desc: Adobe internationalized-date Julian epoch
    if:
      type: text
      substr: 'EPOCH = 1721426'
