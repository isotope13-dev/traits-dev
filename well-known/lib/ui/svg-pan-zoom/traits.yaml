defaults:
  platforms: [windows, unix]
  for: [javascript, data, npm, tar]
  size_max: 5000000
traits:
- id: package-path
  desc: svg-pan-zoom package path
  crit: notable
  conf: 0.99
  if:
    type: path
    regex: '(?i)(^|[\\/])svg-pan-zoom([-\.][0-9]|[\\/]|$)'
