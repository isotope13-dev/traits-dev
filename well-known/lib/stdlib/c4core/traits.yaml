defaults:
  platforms: [unix, windows]
  for: [c, cpp]

traits:
  - id: c4core-source-tree
    desc: c4core library source tree member
    crit: notable
    conf: 0.98
    if:
      type: path
      regex: '(^|[\\/])c4core-[0-9][0-9A-Za-z._-]*-src([\\/]|$)'
