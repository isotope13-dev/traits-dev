# Identity for the official Mastra core npm package.  This is a library
# context only: install hooks, network access, process execution, and file
# operations still need their own behavioral evidence.
defaults:
  platforms: [unix, windows]
  for: [package.json]
  crit: notable
  conf: 0.99

traits:
  - id: core-package-name
    desc: Mastra core npm package name
    if:
      type: value
      path: name
      exact: '@mastra/core'

  - id: core-repository
    desc: Mastra core source repository
    if:
      type: value
      path: repository.url
      regex: '(?i)github\.com/mastra-ai/mastra(\.git)?$'

  - id: core-repository-directory
    desc: Mastra core monorepo directory
    if:
      type: value
      path: repository.directory
      exact: packages/core

composite_rules:
  - id: official-core-package
    desc: Official Mastra core package identity
    crit: notable
    conf: 0.99
    all:
      - id: core-package-name
      - id: core-repository
      - id: core-repository-directory
