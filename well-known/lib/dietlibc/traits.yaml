defaults:
  platforms: [unix]
  crit: baseline
  conf: 0.98
  size_max: 104857600

traits:
  - id: source-tree-path
    desc: dietlibc source tree path
    crit: notable
    for: [c, cpp, shell]
    if:
      type: path
      regex: 'dietlibc-[0-9][^\\/]*[\\/]'
