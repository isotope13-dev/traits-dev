defaults:
  platforms: [windows, unix]
  for: [javascript]
  crit: component
  conf: 0.96

traits:
  - id: swagger-ui-bundle-marker
    desc: Swagger UI bundled JavaScript
    crit: baseline
    if:
      type: text
      regex: 'swagger-ui-bundle|SwaggerUIBundle|oauth2RedirectUrl'
