defaults:
  platforms: [unix, windows]
  for: [c, cpp]
  crit: baseline
  conf: 0.96
  size_max: 1048576

traits:
  - id: portability-table
    desc: Perl DBI portability table
    if:
      type: basename
      regex: '(^|[\\/])DBI-[0-9.]+[\\/]dbipport\\.h$'
