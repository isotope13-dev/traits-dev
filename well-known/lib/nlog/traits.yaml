defaults:
  platforms: [windows]
  for: [pe, dll]
  size_max: 52428800
  crit: baseline
  conf: 0.96
traits:
  - id: nlog-dll-basename
    desc: NLog DLL basename
    if:
      type: path
      regex: '^NLog\.dll(@|$)'
      case_insensitive: true
  - id: nlog-embedded-member
    desc: Embedded NLog DLL member
    if:
      type: path
      regex: 'NLog\.dll@'
      case_insensitive: true
