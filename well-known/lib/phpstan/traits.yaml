defaults:
  platforms: [unix, windows]
  crit: baseline
  conf: 0.98

traits:
  - id: phpstan-phar-basename
    desc: PHPStan PHAR basename
    for: [php]
    size_min: 1000000
    size_max: 50000000
    if:
      type: basename
      exact: phpstan.phar
