defaults:
  platforms: [unix, windows]

composite_rules:
  - id: package-context
    desc: Recognized DCC MCP Core package identity
    crit: exception
    conf: 1.0
    scope: archive
    all:
      - id: package-name
      - id: repository
