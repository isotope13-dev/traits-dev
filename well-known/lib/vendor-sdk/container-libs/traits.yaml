# container-libs -- CI runner paths from the containers project.
defaults:
  platforms: [unix, windows]
traits:
- id: container-libs-ci-runner-path
  desc: container-libs CI runner path
  crit: notable
  conf: 0.98
  for: [shell, vbs]
  if:
    type: path
    regex: '(^|[\\/])ci[\\/]runner\.sh$'
