# mark3labs/mcp-go — official Go SDK for the Model Context Protocol.
defaults:
  platforms: [unix, windows]
  for: [go, go.mod, zip]
  crit: notable
  conf: 0.99

traits:
  - id: go-module-path
    desc: mcp-go module import path
    if:
      type: text
      substr: github.com/mark3labs/mcp-go
