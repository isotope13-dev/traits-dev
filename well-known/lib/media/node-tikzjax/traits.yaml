defaults:
  platforms: [unix, windows]
  for: [tar]
  size_max: 5242880

traits:
  - id: bundled-tex-resource-archive
    desc: node-tikzjax TeX resource archive
    crit: notable
    conf: 0.99
    if:
      type: path
      regex: '(?i)node-tikzjax[/\\]tex[/\\]tex_files[.]tar[.]gz$'
