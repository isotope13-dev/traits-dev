defaults:
  platforms: [windows]
  crit: notable
  conf: .99
  size_min: 4096
  size_max: 500000000

traits:
  - id: aomedia-av1-windows-library
    desc: AOMedia AV1 codec library
    for: [pe, dll]
    if:
      type: basename
      exact: aom.dll
      case_insensitive: true
