defaults:
  platforms: [unix, windows]
  for: [python, pyc, pkginfo]
  crit: notable
  conf: 0.98

traits:
  - id: library-member-path
    desc: txtorcon library member path
    if:
      type: path
      substr: txtorcon
