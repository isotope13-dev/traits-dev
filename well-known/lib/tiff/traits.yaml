defaults:
  platforms: [windows, unix, macos]
  for: [pe, dll, elf, macho]
  crit: notable
  conf: 0.99

traits:
  - id: libtiff-native-library
    desc: libtiff native image library
    if:
      type: value
      path: pe.version.product_name
      exact: LibTIFF
