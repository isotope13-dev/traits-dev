# Google Cloud Go client source and module archive identity.
defaults:
  platforms: [unix, windows]
  for: [go, zip]
  crit: notable
  conf: 0.99

traits:
  - id: source-tree
    desc: Google Cloud Go source-tree path
    if:
      type: path
      regex: 'cloud\.google\.com/go@v[0-9]'
