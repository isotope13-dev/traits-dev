# HashiCorp Nomad job specification markers.
defaults:
  platforms: [unix, windows]
  for: [json]
  crit: notable
  conf: 0.99

traits:
  - id: nomad-job-spec-marker
    desc: HashiCorp Nomad job specification marker
    if:
      type: text
      substr: '"TaskGroups"'
