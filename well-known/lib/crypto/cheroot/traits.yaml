defaults:
  platforms: [unix, windows]
  for: [python]
  crit: notable
  conf: 0.98
  size_min: 1
  size_max: 2000000

traits:
  - id: cheroot-ssl-module
    desc: Cheroot SSL module
    if:
      type: path
      regex: 'cheroot[\\/]ssl'
