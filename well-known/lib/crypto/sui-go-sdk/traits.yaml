# block-vision/sui-go-sdk — Go client for Sui JSON-RPC and gRPC.
defaults:
  platforms: [unix, windows]
  for: [go, go.mod, zip]
  crit: notable
  conf: 0.99

traits:
  - id: go-module-path
    desc: sui-go-sdk Go module import path
    if:
      type: text
      substr: github.com/block-vision/sui-go-sdk
