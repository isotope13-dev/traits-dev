defaults:
  platforms: [windows, unix]
  for: [javascript]

traits:
  - id: ejs-legacy-browser-loader
    desc: EJS 1.0 legacy browser loader
    crit: notable
    conf: 0.99
    if:
      type: basename
      regex: '^ejs-1\.0(?:\.min)?\.js$'
