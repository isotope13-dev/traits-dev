defaults:
  platforms: [unix, windows]
  for: [javascript, typescript]
  crit: notable
  conf: 0.98
  size_min: 32768

traits:
  - id: zip-js-library-marker
    desc: zip.js library marker
    if:
      type: text
      substr: zipjs-abort-check-password
