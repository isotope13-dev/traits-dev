# NaturalIntelligence fast-xml-builder — JSON-to-XML builder.
defaults:
  platforms: [unix, windows]
  for: [javascript, typescript, package.json]
  crit: notable
  conf: 0.99

traits:
  - id: package-name
    desc: fast-xml-builder npm package name
    for: [package.json]
    if:
      type: value
      path: name
      exact: fast-xml-builder

  - id: vendored-path
    desc: vendored fast-xml-builder package path
    for: [scripts, manifests, data, source]
    if:
      type: path
      regex: 'node_modules/fast-xml-builder/'
