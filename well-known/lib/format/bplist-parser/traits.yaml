# bplist-parser — binary Apple property-list parser.
defaults:
  platforms: [unix, windows]
  for: [javascript, package.json]
  crit: notable
  conf: 0.99

traits:
  - id: package-name
    desc: bplist-parser npm package name
    for: [package.json]
    if:
      type: value
      path: name
      exact: bplist-parser

  - id: description
    desc: bplist-parser package description
    for: [package.json]
    if:
      type: value
      path: description
      exact: Binary plist parser.

composite_rules:
  - id: bplist-parser-library
    desc: bplist-parser binary plist library
    all:
      - id: package-name
      - id: description
