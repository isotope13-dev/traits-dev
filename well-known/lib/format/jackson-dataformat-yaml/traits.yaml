# Jackson YAML dataformat module.
defaults:
  platforms: [unix, windows]
  for: [jar, zip]
  crit: notable
  conf: 0.99

traits:
  - id: yaml-jar-basename
    desc: Jackson YAML dataformat JAR name
    if:
      type: basename
      regex: '^jackson-dataformat-yaml-[0-9][A-Za-z0-9._-]*\.jar$'
