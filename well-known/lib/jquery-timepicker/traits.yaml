defaults:
  platforms: [unix, windows]
  crit: baseline
  conf: 0.98

traits:
  - id: jquery-timepicker-basename
    desc: jQuery timepicker basename
    crit: notable
    for: [javascript]
    size_max: 200000
    if:
      type: basename
      exact: jquery.timepicker.min.js
