defaults:
  platforms: [windows, unix]
  for: [pe, dll]
  crit: baseline
  conf: 0.96
  size_max: 16777216

traits:
  - id: system-clientmodel-assembly
    desc: System.ClientModel .NET assembly
    if:
      type: path
      regex: '(^|[\\/])System\.ClientModel(\.dll)?$'
