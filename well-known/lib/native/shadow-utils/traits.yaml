defaults:
  platforms: [unix]
  for: [elf, so]
  crit: notable
  conf: 0.99
  size_min: 10000
  size_max: 5000000

traits:
  - id: libsubid-soname
    desc: shadow-utils subordinate-ID library (SONAME)
    if:
      type: value
      path: elf.soname
      regex: '^libsubid\.so\.[0-9]+$'
