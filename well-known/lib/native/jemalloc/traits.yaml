defaults:
  platforms: [unix]
  for: [elf, so]
  crit: baseline
  conf: 0.99
  size_max: 50000000

traits:
  - id: jemalloc-soname
    desc: jemalloc memory allocator shared library
    crit: notable
    if:
      type: value
      path: elf.soname
      exact: libjemalloc.so.2
