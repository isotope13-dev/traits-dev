defaults:
  platforms: [linux]
  for: [elf]
  crit: baseline
  conf: 0.99

traits:
  - id: libell-binary
    desc: ell Linux networking library
    crit: notable
    conf: 0.99
    size_min: 1
    size_max: 50000000
    if:
      type: basename
      regex: '(?i)^libell\.so(\.|$)'
