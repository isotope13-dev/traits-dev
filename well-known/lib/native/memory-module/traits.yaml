defaults:
  platforms: [windows]
  for: [c, cpp]
traits:
  - id: memory-module-source
    desc: MemoryModule library source file
    crit: notable
    conf: 0.99
    if:
      type: path
      basename: true
      regex: MemoryModule\.(c|h)$
