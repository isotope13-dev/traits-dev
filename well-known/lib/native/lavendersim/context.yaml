defaults:
  platforms: [unix, windows]

composite_rules:
  - id: native-library-context
    desc: Recognized LavenderSim native wheel library context
    crit: notable
    conf: 0.99
    all:
      - id: native-library-path
      - id: package-member-path
