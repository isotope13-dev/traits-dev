defaults:
  platforms: [windows, unix]
  for: [pe, dll, elf, macho]
  crit: notable
  conf: 0.99
  size_min: 4096
  size_max: 500000000

traits:
  - id: libass-native-library
    desc: libass subtitle rendering library
    if:
      type: basename
      regex: '(?i)^ass-[0-9]+\.dll$'
