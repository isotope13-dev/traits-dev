defaults:
  platforms: [unix, windows]
  crit: notable
  conf: 0.96
  size_max: 16777216

traits:
  - id: python-package
    crit: notable
    desc: Pathway Python package source
    for: [python, javascript, typescript, elf, so]
    if:
      type: path
      regex: '.{0,120}pathway[\\/].{0,120}'
