defaults:
  platforms: [unix, windows]
  for: [python]

traits:
  - id: cpu-check-module
    desc: Polars CPU feature check module
    crit: notable
    conf: 0.98
    if:
      type: path
      regex: '(^|[\\/])polars[\\/]_cpu_check\.py$'
