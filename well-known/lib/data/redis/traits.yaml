defaults:
  platforms: [unix, windows]
  for: [elf, macho, pe, dll, so, dylib]
  size_min: 65536
traits:
  - id: redis-module-export
    desc: Redis module API export symbol
    crit: notable
    conf: 0.95
    if:
      type: symbol
      regex: ^RedisModule_
