defaults:
  platforms: [unix, windows]
  for: [javascript, typescript]
  crit: notable
  conf: 0.96

traits:
  - id: mongodb-tls-option-parser
    desc: MongoDB TLS connection option parser
    if:
      type: text
      substr: 'tlsAllowInvalidHostnames'
