defaults:
  platforms: [unix, windows]
  for: [python]
  crit: notable
  conf: 0.99

traits:
  - id: country-calendar-module
    desc: Python holidays country calendar module
    size_min: 1
    size_max: 1000000
    if:
      type: path
      regex: 'holidays[/\\]countries[/\\]'
