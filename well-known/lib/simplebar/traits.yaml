defaults:
  platforms: [unix, windows]
  for: [javascript]
  crit: notable
  conf: 0.98

traits:
  - id: simplebar-source-header
    desc: SimpleBar library source header
    if:
      type: text
      regex: 'SimpleBar\.js\s*-\s*v[0-9]+\.[0-9]+'
