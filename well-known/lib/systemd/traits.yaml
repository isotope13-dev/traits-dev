defaults:
  platforms: [unix]
  for: [elf, so]
  crit: notable
  conf: 0.99
  size_min: 100000
  size_max: 20000000

traits:
  - id: systemd-shared-library
    desc: systemd shared library basename
    if:
      type: basename
      regex: '^libsystemd.*\.so(\.[0-9.]+)?$'
