defaults:
  platforms: [windows, unix, macos]
  for: [pe, dll, elf, macho]
  crit: notable
  conf: 0.99

traits:
  - id: harfbuzz-native-library
    desc: HarfBuzz text shaping library
    if:
      type: basename
      exact: harfbuzz.dll
