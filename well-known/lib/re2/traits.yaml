defaults:
  platforms: [unix, windows]
  conf: 0.95

traits:
  - id: node-re2-native-addon-require
    desc: node-re2 native addon require
    crit: notable
    for: [javascript]
    if:
      type: text
      substr: "build/Release/re2.node"

  # abseil-vendored-ci-script used to live here because the sample that
  # prompted it was an RE2 tarball with abseil-cpp vendored inside. The script
  # is Abseil's, so it now lives at
  # well-known/lib/abseil::abseil-vendored-ci-script.
