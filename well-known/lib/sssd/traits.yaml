defaults:
  platforms: [linux]
  for: [elf]

traits:
  - id: sssd-sudo-responder-api
    desc: SSSD sudo responder shared library
    crit: notable
    conf: 0.99
    size_min: 10000
    size_max: 100000
    if:
      type: value
      path: elf.soname
      exact: libsss_sudo.so
