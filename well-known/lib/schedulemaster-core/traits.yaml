defaults:
  platforms: [unix, windows]
  for: [pe, dll]
  size_min: 500000
  size_max: 2000000
  crit: notable
  conf: 0.98

traits:
  - id: library-name
    desc: ScheduleMaster.Core library identity
    if:
      type: text
      section: .text
      substr: ScheduleMaster.Core
