defaults:
  platforms: [windows]
  crit: notable
  conf: .99
  size_min: 4096
  size_max: 500000000

traits:
  - id: littlecms-windows-library
    desc: LittleCMS color-management library
    for: [pe, dll]
    if:
      type: basename
      regex: '(?i)^lcms2(-[0-9]+)?\.dll$'
