defaults:
  platforms: [windows]
  crit: notable
  conf: .99

traits:
  - id: littlecms-windows-library
    desc: LittleCMS color-management library
    for: [pe, dll]
    if:
      type: basename
      regex: '(?i)^lcms2(-[0-9]+)?\.dll$'
