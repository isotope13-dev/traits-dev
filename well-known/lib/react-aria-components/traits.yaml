defaults:
  platforms: [unix, windows]
  for: [package.json]
  crit: notable
  conf: 0.99

traits:
  - id: package-name
    desc: React Aria Components package name
    if:
      type: value
      path: name
      exact: react-aria-components
