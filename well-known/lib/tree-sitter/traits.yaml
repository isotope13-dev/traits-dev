defaults:
  platforms: [linux]
  for: [elf]
  crit: baseline
  conf: 0.99
  size_min: 4096
  size_max: 8388608
traits:
  - id: tree-sitter-grammar-library
    desc: Tree-sitter parser grammar library
    if:
      type: symbol
      regex: ^tree_sitter_[a-z0-9_]+$
