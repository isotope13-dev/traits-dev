defaults:
  platforms: [unix, windows]
  for: [python]
  crit: baseline
  conf: 0.96

traits:
  - id: source-tree
    desc: SQLAlchemy Python source tree
    crit: notable
    if:
      type: path
      regex: '(^|[\\/])sqlalchemy[\\/]'
