defaults:
  platforms: [windows, unix, macos]
  for: [pe, dll, elf, macho]
  crit: notable
  conf: 0.99

traits:
  - id: zstandard-native-library
    desc: Zstandard native compression library
    if:
      type: value
      path: pe.version.product_name
      exact: Zstandard
