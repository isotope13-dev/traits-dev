defaults:
  platforms: [unix, windows]
  for: [javascript, typescript]

traits:
  - id: library
    desc: Node.js follow-redirects library
    crit: notable
    conf: 0.95
    if:
      type: string_literal
      exact: follow-redirects
