defaults:
  platforms: [unix, windows]
  for: [jar, class, java_class]
  crit: notable
  conf: 0.99

traits:
  - id: gef-namespace
    desc: Eclipse GEF namespace
    if:
      type: import
      regex: '^org/eclipse/gef/'
