defaults:
  platforms: [windows]
  crit: notable
  conf: .99

traits:
  - id: aomedia-av1-windows-library
    desc: AOMedia AV1 codec library
    for: [pe, dll]
    if:
      type: basename
      regex: '(?i)^aom\.dll$'
