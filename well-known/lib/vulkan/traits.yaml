defaults:
  platforms: [windows]
  for: [dll]
  crit: baseline
  conf: 0.96
  size_max: 16777216

traits:
  - id: loader-dll
    desc: Vulkan loader DLL
    if:
      type: path
      basename: true
      exact: vulkan-1.dll
      case_insensitive: true
