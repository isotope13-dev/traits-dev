defaults:
  platforms: [windows]
  for: [pe, dll]
  size_min: 65536

traits:
  - id: runtime-dll-reference
    desc: Adobe AIR runtime DLL reference
    crit: notable
    conf: 0.95
    if:
      type: text
      section: .rdata
      substr: Adobe AIR.dll
