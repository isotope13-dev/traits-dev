defaults:
  platforms: [windows]
  for: [pe, dll]
  size_max: 120000000
traits:
  - id: kernel32-library
    desc: Vanara PInvoke.Kernel32 reference library
    crit: notable
    conf: 0.96
    if:
      type: basename
      regex: '^(Vanara\.)?PInvoke\.Kernel32\.dll$'
