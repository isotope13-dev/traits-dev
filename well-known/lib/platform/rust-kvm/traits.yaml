defaults:
  platforms: [linux]
  for: [elf]
  size_min: 100000
  size_max: 100000000

traits:
  - id: kvm-ioctls-runtime
    desc: Rust kvm-ioctls crate runtime
    crit: notable
    conf: 0.99
    if:
      type: raw
      section: .strtab
      substr: kvm_ioctls
