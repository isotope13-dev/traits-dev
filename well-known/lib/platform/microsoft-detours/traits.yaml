defaults:
  platforms: [windows]
  for: [pe, dll]
  size_min: 4096
  size_max: 50000000

traits:
  - id: detours-section-marker
    desc: Microsoft Detours transaction section
    crit: notable
    conf: 0.95
    if:
      type: section
      substr: .detourc
