defaults:
  platforms: [macos]
  for: [package.json]

traits:
  - id: npm-package
    desc: fsevents npm package identity
    crit: notable
    conf: 1.0
    if:
      type: value
      path: name
      exact: fsevents
