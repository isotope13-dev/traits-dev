defaults:
  platforms: [unix, windows]
  for: [javascript]
  crit: baseline
  conf: 0.96
  size_max: 2097152

traits:
  - id: source-package
    desc: Haskell persistent source package
    if:
      type: basename
      regex: '(^|[\\/])persistent-[0-9.]+[\\/]'
