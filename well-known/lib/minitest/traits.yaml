defaults:
  platforms: [unix, windows]
  for: [ruby]
  crit: baseline
  conf: 0.96
  size_max: 4194304

traits:
  - id: gem-source
    desc: Minitest Ruby gem source
    crit: notable
    if:
      type: path
      regex: '(^|[\\/])minitest-[0-9.]+[\\/]'
