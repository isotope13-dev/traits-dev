defaults:
  platforms: [unix, windows]
  for: [javascript, typescript]
  crit: baseline
  conf: 0.98

traits:
  - id: cl100k-bpe-ranks-table
    desc: GPT tokenizer cl100k BPE ranks table
    if:
      type: path
      regex: '(?i)(^|[\\/])node_modules[\\/]gpt-tokenizer[\\/](cjs|esm|src)[\\/]bpeRanks[\\/]cl100k_base\.js$'
