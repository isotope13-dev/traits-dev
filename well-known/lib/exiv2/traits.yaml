defaults:
  platforms: [windows]
  crit: notable
  conf: .99

traits:
  - id: exiv2-windows-image-metadata-tool
    desc: Exiv2 image-metadata library and utility
    for: [pe, dll]
    if:
      type: basename
      regex: '(?i)^exiv2\.(dll|exe)$'
