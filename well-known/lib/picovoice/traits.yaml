defaults:
  platforms: [macos, linux, windows]
  crit: baseline
  conf: 0.98
  size_max: 536870912

traits:
  - id: pvrecorder-native-addon-path
    desc: Picovoice pvrecorder native addon path
    for: [macho, elf, pe]
    if:
      type: path
      regex: 'pvrecorder-node[\\/].*[\\/]pv_recorder\.node$'
