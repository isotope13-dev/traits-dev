defaults:
  platforms: [unix, windows]
  for: [python]
  crit: notable
  conf: 0.98

traits:
  - id: rpyc-library-import
    desc: Imports RPyC remote procedure library
    if:
      type: import
      regex: '^rpyc(\.|$)'
