defaults:
  platforms: [windows, unix]
  for: [javascript, typescript, package.json]
  size_max: 20000000

traits:
  - id: whatsapp-web-js-library
    desc: whatsapp-web.js client library
    crit: notable
    conf: 0.98
    if:
      type: text
      substr: whatsapp-web.js
