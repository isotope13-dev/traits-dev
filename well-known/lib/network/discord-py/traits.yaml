defaults:
  platforms: [unix, windows]
  for: [python]
  crit: notable
  conf: 0.9

traits:
  - id: import
    desc: Imports the discord.py library
    if:
      type: symbol
      kind: import
      regex: '^(discord|discord\.abc|discord\.app_commands)$'
