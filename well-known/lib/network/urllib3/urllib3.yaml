# urllib3 — Python HTTP client library.
defaults:
  platforms: [windows, unix]
  for: [python, pyc]
  crit: baseline

traits:
  - id: urllib3-package-path
    desc: urllib3 package path segment
    crit: notable
    if:
      type: path
      regex: '(?i)(^|[\\/])urllib3([\\/]|$)'
