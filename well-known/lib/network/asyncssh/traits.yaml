defaults:
  platforms: [linux, macos, windows]
  for: [python]
traits:
  - id: asyncssh-python-library
    desc: AsyncSSH Python library source
    crit: notable
    conf: 0.98
    if:
      type: path
      regex: 'site-packages[\\/]asyncssh[\\/]'
