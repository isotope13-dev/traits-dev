# Eclipse Paho Go MQTT client library.
defaults:
  platforms: [unix, windows]
  for: [go, zip, tar, crate, gz]
  crit: notable
  conf: 0.99
  size_min: 1
  size_max: 600000000

traits:
  - id: module-path-marker
    desc: Eclipse Paho Go module path marker
    if:
      type: text
      substr: github.com/eclipse/paho.golang

  - id: paho-package-comment
    desc: Paho Go client package comment
    for: [go]
    if:
      type: text
      substr: Package paho is an MQTT v5 client

  - id: rpc-extension-import
    desc: Imports the Eclipse Paho RPC extension
    for: [go]
    if:
      type: import
      exact: github.com/eclipse/paho.golang/paho/extensions/rpc
