defaults:
  platforms: [unix, windows]
  for: [rust]

traits:
  - id: serenity-rust-library-import
    desc: Rust Serenity Discord library import
    crit: notable
    conf: 0.96
    if:
      type: text
      substr: 'use serenity::'
