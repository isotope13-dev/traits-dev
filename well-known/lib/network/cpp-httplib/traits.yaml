defaults:
  platforms: [windows]
  for: [pe, dll, c, cpp]
  size_min: 65536
  size_max: 52428800

traits:
  - id: cpp-httplib-multipart-marker
    desc: cpp-httplib default multipart boundary marker
    crit: notable
    conf: 0.97
    if:
      type: text
      exact: --cpp-httplib-multipart-data-
