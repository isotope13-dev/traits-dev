defaults:
  platforms: [windows, unix]
  for: [php]

traits:
  - id: phpmailer-source-namespace
    desc: PHPMailer source namespace
    crit: notable
    conf: 0.99
    if:
      type: text
      exact: 'namespace PHPMailer\PHPMailer;'
