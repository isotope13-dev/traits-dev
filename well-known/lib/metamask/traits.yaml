defaults:
  platforms: [windows, unix]
  for: [javascript, typescript]
  size_max: 120000000

traits:
  - id: scoped-package-reference
    desc: Reference to a MetaMask scoped package
    crit: notable
    conf: 0.97
    if:
      type: import
      regex: '^@metamask/[A-Za-z0-9._-]+$'
