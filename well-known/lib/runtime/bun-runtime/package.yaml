defaults:
  for: [package.json, npm]
  platforms: [unix, windows]
  crit: notable
  conf: 0.99

traits:
  - id: official-platform-package
    desc: Official Bun platform runtime package
    if:
      type: value
      path: name
      regex: '^@oven/bun-'
