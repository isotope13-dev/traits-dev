defaults:
  platforms: [linux, macos]
  for: [binaries]
  crit: notable
  conf: 0.95
  size_min: 1000000
  size_max: 4000000

traits:
  - id: ffi-native-library
    crit: notable
    desc: SeedFaker native FFI library
    if:
      type: basename
      regex: '^libseedfaker_ffi\.(dylib|so)$'
