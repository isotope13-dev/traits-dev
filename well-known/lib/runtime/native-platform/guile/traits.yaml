defaults:
  platforms: [unix]
  for: [elf, so]
traits:
- id: libguile-shared-library
  desc: Guile shared library
  crit: notable
  conf: 0.95
  size_min: 1000000
  size_max: 10000000
  if:
    type: basename
    regex: '^libguile-[0-9.]+\.so'
