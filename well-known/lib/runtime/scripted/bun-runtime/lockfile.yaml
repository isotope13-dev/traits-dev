defaults:
  platforms: [unix, windows]
  for: [javascript, typescript]

traits:
  - id: bun-binary-lockfile-header
    desc: Bun binary lockfile format header
    crit: notable
    conf: 0.99
    if:
      type: text
      substr: bun-lockfile-format-v
