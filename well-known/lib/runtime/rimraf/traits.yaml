# isaacs/rimraf — recursive filesystem removal for Node.js.
defaults:
  platforms: [unix, windows]
  for: [javascript, typescript, package.json, json]
  crit: notable
  conf: 0.99

traits:
  - id: windows-tmp-busy-comment
    desc: rimraf Windows temp EBUSY comment
    for: [javascript, typescript]
    if:
      type: text
      substr: 'rimraf(path).then(() => rimraf(dirname(path)))'

  - id: package-name
    desc: rimraf npm package name
    for: [package.json]
    if:
      type: value
      path: name
      exact: rimraf

  - id: vendored-path
    desc: vendored rimraf npm package path
    for: [scripts, manifests, data, source]
    if:
      type: path
      regex: 'node_modules/rimraf/'

composite_rules:
  - id: rimraf-library
    desc: rimraf recursive delete library
    needs: 2
    any:
      - id: windows-tmp-busy-comment
      - id: package-name
      - id: vendored-path
