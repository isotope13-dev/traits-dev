defaults:
  platforms: [unix, windows]
  for: [package.json]

traits:
  - id: package
    desc: assert-plus Node.js assertion library package
    crit: notable
    conf: 0.99
    if:
      type: value
      path: name
      exact: assert-plus
