defaults:
  platforms: [unix, windows]
  for: [java, class, jar, zip]
  crit: baseline
  conf: 0.95

traits:
  - id: java-servlet-api-import
    desc: Java Servlet API import
    crit: notable
    if:
      type: import
      exact: javax.servlet.http.HttpServlet
