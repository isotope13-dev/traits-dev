defaults:
  platforms: [unix, windows]
  for: [python]
  crit: baseline
  conf: 0.96
  size_max: 1048576

traits:
  - id: package-source
    desc: Jinja2 template engine source
    crit: notable
    if:
      type: path
      regex: '(^|[\\/])jinja2[\\/]'
