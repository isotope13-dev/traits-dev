defaults:
  platforms: [windows, unix]
  for: [php, zip, tar, archive]
  crit: notable
  conf: 0.98

traits:
  - id: source-tree
    desc: Smarty template engine source tree path
    if:
      type: path
      regex: '(?i)vendor[\\/]smarty[\\/]'
