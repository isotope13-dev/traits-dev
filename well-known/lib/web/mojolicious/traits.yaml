defaults:
  platforms: [unix, windows]
  for: [perl]
  crit: notable
  conf: 0.98

traits:
  - id: user-agent-module
    desc: Mojolicious UserAgent source module
    if:
      type: text
      regex: '(?m)^package Mojo::UserAgent;\s+use Mojo::Base [''\"]Mojo::EventEmitter[''\"];'
