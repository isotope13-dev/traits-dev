defaults:
  platforms: [windows, unix]
  for: [javascript, typescript]
  crit: notable
  conf: 0.98

traits:
  - id: package-import
    desc: Imports the InversifyJS DI library
    if:
      type: import
      exact: inversify
