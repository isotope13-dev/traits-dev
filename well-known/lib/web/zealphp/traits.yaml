defaults:
  platforms: [windows, unix]
  for: [php, text, archives]
  crit: notable
  conf: 0.98
traits:
  - id: zealphp-source-tree
    desc: ZealPHP source tree
    if:
      type: path
      regex: '(?i)zealphp'
