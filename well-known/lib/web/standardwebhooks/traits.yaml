# Standard Webhooks — language-library signature verification marker.
defaults:
  platforms: [unix, windows]
  for: [ruby]
  crit: notable
  conf: 0.99

traits:
- id: webhook-verify
  desc: Standard Webhooks signature verification
  if:
    type: text
    substr: StandardWebhooks::Webhook.new
