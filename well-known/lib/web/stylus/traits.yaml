# stylus — CSS preprocessor.
defaults:
  platforms: [unix, windows]
  for: [javascript, package.json]
  crit: notable
  conf: 0.99

traits:
  - id: package-name
    desc: stylus npm package name
    for: [package.json]
    if:
      type: value
      path: name
      exact: stylus

  - id: vendored-path
    desc: vendored stylus package path
    for: [scripts, manifests, data, source]
    if:
      type: path
      regex: 'node_modules/stylus/'
