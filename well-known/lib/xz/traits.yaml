defaults:
  platforms: [windows]
  for: [dll]
  crit: notable
  conf: 0.99
  size_min: 100000
  size_max: 20000000

traits:
  - id: xz-utils-windows-library
    desc: XZ Utils liblzma Windows runtime library
    if:
      type: basename
      regex: '(?i)^liblzma\.dll$'
