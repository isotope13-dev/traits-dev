defaults:
  platforms: [unix]
  for: [elf, so]
  crit: notable
  conf: 0.99

traits:
  - id: libdbus-shared-library
    desc: libdbus shared library
    size_min: 100000
    size_max: 2000000
    if:
      type: basename
      regex: '^libdbus-1\.so(\.[0-9]+)*$'
