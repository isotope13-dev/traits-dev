defaults:
  platforms:
  - unix
  - windows
  for:
  - macho
  crit: baseline
  conf: 0.97
  size_max: 5000000
traits:
- id: sigmashake-governance-marker
  desc: SigmaShake Governance tool marker
  size_min: 65536
  if:
    type: text
    regex: "(?i)sigmashake|ssg-(eval|audit|hook)"
    section: const
