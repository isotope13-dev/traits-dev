defaults:
  platforms: [unix, windows]
  crit: notable
  conf: 0.99

traits:
  - id: source-tree
    desc: CIRISVerify framework source tree
    for: [source, rust, python, shell, text]
    if:
      type: path
      regex: '(?i)(^|[\\/])(cirisverify|ciris-verify)(@|[-_]v?[0-9]|[\\/]|$)'
