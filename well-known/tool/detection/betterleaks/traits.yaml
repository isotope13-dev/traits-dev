defaults:
  platforms: [unix, windows]
  for: [go]
  size_min: 1
  size_max: 20000000
traits:
  - id: betterleaks-rule-generator
    desc: betterleaks detection rule generator
    crit: notable
    conf: 0.97
    if:
      type: path
      regex: 'betterleaks-[0-9]'
