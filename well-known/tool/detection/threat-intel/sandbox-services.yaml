defaults:
  platforms: [unix, windows]
  for: [scripts, binaries, source]

traits:
  - id: any-run-service-reference
    desc: ANY.RUN sandbox service reference
    crit: notable
    conf: 0.9
    size_max: 104857600
    if:
      type: text
      regex: '(?i)\bany[.]run\b'
