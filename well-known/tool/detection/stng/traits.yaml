defaults:
  conf: 0.99
  for: [elf]
  platforms: [unix]
  crit: notable
  size_min: 512
  size_max: 20000000

traits:
  - id: stng-help-text
    desc: stng malware-string extractor
    if:
      type: text
      section: .rodata
      substr: stng extracts and classifies strings
