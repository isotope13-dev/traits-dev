defaults:
  platforms: [windows]
  for: [pe, dll]
  crit: notable
  conf: 0.96
  size_min: 1
  size_max: 5000000

traits:
  - id: inno-setup-compiler
    desc: Inno Setup compiler DLL identity
    if:
      type: basename
      exact: ISCmplr.dll
