defaults:
  platforms: [windows, unix]
  for: [python, pe, dll]
  crit: notable
  conf: 0.99

traits:
  - id: qt-android-runner-script
    desc: Qt for Android runner script
    for: [python]
    if:
      type: path
      basename: true
      exact: qt-android-runner.py
