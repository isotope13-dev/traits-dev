defaults:
  platforms:
  - unix
  - windows
  for:
  - shell
  - python
  - ruby
  - javascript
  crit: baseline
  size_max: 5000000
traits:
- id: jq-processing
  desc: Uses jq for JSON processing
  if:
    type: symbol
    kind: call
    exact: jq
- id: sed-processing
  desc: Uses sed for text processing
  if:
    type: symbol
    kind: call
    exact: sed
- id: awk-processing
  desc: Uses awk for text processing
  if:
    type: symbol
    kind: call
    exact: awk
