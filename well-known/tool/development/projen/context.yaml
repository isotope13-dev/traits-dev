defaults:
  platforms: [unix, windows]

composite_rules:
  - id: package-context
    desc: Recognized Projen project-generator package context
    crit: exception
    conf: 0.99
    scope: archive
    any:
      - id: pypi-package
      - id: npm-package
