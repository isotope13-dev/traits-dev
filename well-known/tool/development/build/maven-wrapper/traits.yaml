defaults:
  platforms: [unix, windows]
  for: [class]
  crit: notable
  conf: 0.98
traits:
  # Maven wrapper downloads a distribution, adds it to a URLClassLoader and
  # invokes its main. Same mechanism as the Gradle wrapper, vendored as
  # maven-wrapper.jar.
  - id: maven-wrapper-package-class
    desc: Maven wrapper package class
    if:
      type: text
      substr: org/apache/maven/wrapper/
