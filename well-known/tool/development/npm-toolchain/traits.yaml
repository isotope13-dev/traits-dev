defaults:
  platforms: [unix]
  for: [shell]
  crit: notable
  conf: 0.98
  size_max: 100000

traits:
  - id: npm-install-script-path
    desc: Bundled npm installer script path
    if:
      type: path
      regex: 'npm/scripts/install\.sh$'
