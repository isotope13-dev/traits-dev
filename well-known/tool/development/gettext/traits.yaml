defaults:
  platforms: [linux, windows, macos]
  for: [text, oledoc, ooxml, javascript, typescript, python]

traits:
  - id: po-catalog
    desc: GNU gettext PO/POT catalog file
    crit: notable
    conf: 0.95
    if:
      type: path
      basename: true
      regex: '(?i)\.pot?$'
