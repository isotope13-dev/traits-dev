defaults:
  platforms: [unix, windows]
  for: [lua, pe]
  size_max: 6000000

traits:
  - id: luarocks-source-path
    desc: LuaRocks source package path
    crit: baseline
    conf: 0.99
    if:
      type: path
      regex: 'luarocks'
