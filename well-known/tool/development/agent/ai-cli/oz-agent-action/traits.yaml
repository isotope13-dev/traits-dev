defaults:
  platforms: [unix, windows]
  crit: baseline
  conf: 0.98

traits:
  - id: oz-agent-action-source
    desc: Oz GitHub Action source
    crit: notable
    for: [github-actions, shell, javascript, typescript]
    if:
      type: path
      substr: oz-agent-action
