defaults:
  platforms: [unix, windows]
  for: [javascript, typescript]
  crit: baseline
  conf: 0.96
  size_max: 16777216

traits:
  - id: package-bundle
    desc: Prettier formatter package bundle
    crit: notable
    if:
      type: path
      regex: '(node_modules[\\/])?prettier[\\/]'
