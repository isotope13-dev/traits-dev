defaults:
  platforms: [unix, windows]
  for: [go, shell]
  crit: notable
  conf: 0.98

traits:
  - id: keploy-source-tree-path
    desc: Keploy source path
    if:
      type: path
      regex: '(?i)keploy-[^/]*'
