defaults:
  platforms: [unix]
traits:
- id: deadband-loop-detector-wheel
  desc: Deadband loop detector wheel
  crit: notable
  conf: 0.98
  for: [whl]
  if:
    type: basename
    regex: '^deadband_loop_detector-[0-9].*\.whl$'
