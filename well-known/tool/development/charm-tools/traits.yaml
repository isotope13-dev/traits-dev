defaults:
  platforms: [unix]
  for: [python, shell, text, archive]
  crit: baseline
  conf: 0.96

traits:
  - id: charm-tools-test-layer-path
    desc: Charm Tools test layer fixture path
    if:
      type: path
      regex: 'tests/layers'
