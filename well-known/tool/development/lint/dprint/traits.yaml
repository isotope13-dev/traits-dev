defaults:
  platforms: [unix, windows]
  for: [binaries]
  size_min: 1048576
  size_max: 104857600

traits:
  - id: dprint-cli
    desc: dprint code formatter CLI
    crit: notable
    conf: 0.99
    if:
      type: text
      regex: 'DPRINT_EDITOR[\s\S]{0,200}DPRINT_GLOBAL_GITIGNORE'
