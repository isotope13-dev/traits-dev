defaults:
  conf: 0.99
  platforms: [unix, windows]

traits:
  - id: parser-source-path
    desc: Biome JavaScript parser source path
    crit: notable
    for: [javascript]
    if:
      type: path
      regex: '(^|[\\/])biome_js_parser[\\/]'
  - id: empty-strings-depth-fixture
    desc: Biome empty-string depth fixture
    crit: notable
    for: [javascript]
    if:
      type: basename
      exact: 'many_empty_strings.js'
  - id: cli-binary
    desc: Biome toolchain CLI executable
    crit: notable
    for: [binaries]
    size_min: 1048576
    if:
      type: text
      substr: 'https://biomejs.dev'

composite_rules:
  - id: parser-depth-fixture-context
    desc: Recognized Biome parser depth fixture
    crit: exception
    all:
      - id: parser-source-path
      - id: empty-strings-depth-fixture
