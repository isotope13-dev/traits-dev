defaults:
  platforms: [windows]
  for: [batch]

traits:
  - id: windows-wrapper-script
    desc: Clang-format Windows wrapper script
    crit: notable
    conf: 0.97
    if:
      type: text
      regex: '(?i)SCRIPT_DIR=%~dp0.{0,120}clang-format-windows\.ps1'
