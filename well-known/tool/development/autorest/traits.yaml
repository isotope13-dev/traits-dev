defaults:
  platforms: [unix, windows]
  for: [javascript]
  crit: notable
  conf: 0.96
  size_max: 10000000

traits:
  - id: autorest-dist-path
    desc: AutoRest dist JS path
    if:
      type: path
      regex: 'package/dist/.*\.js$'
    crit: notable
