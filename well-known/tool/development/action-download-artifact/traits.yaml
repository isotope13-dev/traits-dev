defaults:
  platforms: [linux, windows, macos]
  crit: notable
  conf: 0.96

traits:
  - id: source-tree
    desc: action-download-artifact source tree path
    for: [source, scripts, javascript, typescript, package.json]
    if:
      type: path
      regex: 'action-download-artifact-[0-9][^\\/]*/'
