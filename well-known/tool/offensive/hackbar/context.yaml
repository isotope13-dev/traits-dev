defaults:
  platforms: [unix, windows]

composite_rules:
  - id: tool-context
    desc: Recognized HackBar pentesting extension context
    crit: exception
    conf: 0.98
    all:
      - id: manifest-name
      - id: homepage
