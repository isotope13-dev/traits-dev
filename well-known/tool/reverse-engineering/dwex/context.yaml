defaults:
  platforms: [unix, windows]

composite_rules:
  - id: packaging-context
    desc: Recognized DWARF Explorer packaging context
    crit: exception
    conf: 0.99
    for: [python]
    all:
      - id: project-identity
      - id: metadata/build/package/recipe::setuptools-run-override
