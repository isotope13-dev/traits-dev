defaults:
  platforms: [unix, windows]
  for: [macho, plist, archives, binaries]
  crit: notable
  conf: 0.98
  size_max: 300000000

traits:
  - id: cutter-release-member
    desc: Cutter reverse engineering suite release member
    if:
      type: path
      regex: 'Cutter-v[0-9][^\\/]*|Cutter\.app'
