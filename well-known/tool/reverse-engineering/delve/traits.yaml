defaults:
  platforms: [linux, macos, windows]
  for: [go]
  crit: notable
  conf: 0.99

traits:
  - id: source-tree
    desc: Go Delve debugger source tree
    if:
      type: path
      substr: github.com/go-delve/delve@
