defaults:
  platforms: [unix]
  for: [elf]
  crit: notable
  conf: 0.98
  size_min: 1024
  size_max: 4194304

traits:
  - id: gdb-module-basename
    desc: GNU GDB debugger module
    if:
      type: basename
      exact: gdb.mod
