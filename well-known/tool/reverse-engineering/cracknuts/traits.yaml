defaults:
  platforms: [unix, windows]
  for: [elf]
  size_max: 52428800

traits:
  - id: cracknuts-firmware-bundle-path
    desc: CrackNuts device firmware bundle path
    crit: notable
    conf: 0.95
    if:
      type: path
      regex: '(?i)cracknuts/firmware/.+\.bin$'
