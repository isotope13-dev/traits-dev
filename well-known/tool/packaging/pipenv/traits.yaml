defaults:
  platforms: [windows, unix]
  for: [python, conda, tar.zst]
  crit: notable
  conf: 0.98
traits:
  - id: pipenv-package-path
    desc: Pipenv package path
    if:
      type: path
      regex: '(?i)pipenv'
