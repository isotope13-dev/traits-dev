defaults:
  platforms: [unix, windows]
  for: [shell]
  crit: notable
  conf: 0.95

traits:
  - id: bun-shell-install
    desc: Bun installer shell profile setup
    if:
      type: text
      substr: 'BUN_INSTALL'
