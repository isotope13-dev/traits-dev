defaults:
  platforms: [windows]
  for: [pe]
  crit: notable
  conf: 0.96
  size_min: 50000
  size_max: 500000

traits:
  - id: bfi-helper
    crit: notable
    desc: wimlib BFI helper binary
    if:
      type: path
      substr: esd-decrypter-wimlib/bin/bfi.exe
