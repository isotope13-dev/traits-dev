defaults:
  platforms: [windows, unix]
  for: [kotlin, lua, python, shell, php]
  crit: baseline
  conf: 0.96
  size_max: 1000000

traits:
  - id: nmap-nse-script-path
    desc: Nmap NSE script path
    if:
      type: path
      regex: 'scripts[\\/][A-Za-z0-9_-]+\.nse$'
