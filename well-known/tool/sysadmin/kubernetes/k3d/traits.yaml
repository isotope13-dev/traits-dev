defaults:
  platforms: [unix]
  for: [shell]
  size_max: 5000000
traits:
  - id: k3d-install-sh-pipe
    desc: k3d official installer pipe
    crit: notable
    if:
      type: text
      regex: '(?i)k3d-io/k3d/(main|master)/install\.sh[^\n]{0,80}\|.{0,40}(bash|sh)\b'
