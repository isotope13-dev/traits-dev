defaults:
  platforms: [linux, macos, windows]
  for: [shell]
  size_max: 5000000

traits:
  - id: kubernetes-authors-shell
    desc: Kubernetes source copyright marker
    crit: notable
    conf: 0.95
    if:
      type: text
      substr: The Kubernetes Authors
