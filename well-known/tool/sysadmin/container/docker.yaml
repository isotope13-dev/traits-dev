defaults:
  platforms:
  - linux
  - macos
  - windows
  crit: baseline
  conf: 0.9
  size_max: 5000000
traits:
- id: docker-registry-login-status-python
  desc: Docker registry login status helper
  for:
  - python
  if:
    type: text
    regex: Read ~/.docker/config\.json and report registries|def registry_logins\(\)
