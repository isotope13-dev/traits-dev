defaults:
  size_max: 52428800
  for: [pe]
  platforms: [windows]
  crit: baseline
  conf: 0.99

traits:
  - id: unlocker-device
    desc: IObit Unlocker device name
    if:
      type: text
      exact: "\\Device\\IObitUnlockerDevice"
      section: rdata
