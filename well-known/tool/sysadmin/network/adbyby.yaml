defaults:
  platforms: [unix, openwrt]
  for: [elf]
  crit: baseline
  size_max: 52428800

traits:
  - id: adbyby-basename
    desc: adbyby executable basename
    conf: 0.98
    if:
      type: basename
      exact: adbyby

composite_rules:
  - id: adbyby-package-binary
    desc: adbyby executable with protocol markers
    crit: notable
    conf: 0.98
    all:
      - id: adbyby-basename
      - id: well-known/lib/native/uclibc::static-uclibc-string-tables
