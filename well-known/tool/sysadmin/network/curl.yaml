defaults:
  platforms:
  - unix
  - android
  - windows
  for:
  - elf
  - pe
  - macho
  size_min: 10000
  size_max: 50000000
traits:
- id: curl-basename
  desc: curl command-line client filename
  crit: notable
  conf: 0.98
  if:
    type: path
    basename: true
    regex: '^curl(\.exe)?$'
