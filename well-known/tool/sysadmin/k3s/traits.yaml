defaults:
  platforms: [linux]
  for: [data, shell]
  crit: baseline
  conf: 0.96
  size_max: 134217728

traits:
  - id: airgap-image-layer
    desc: k3s airgap image layer
    if:
      type: path
      regex: '(k3s-airgap-images|blobs[\\/]sha256[\\/])'
