defaults:
  for:
    - pe
  platforms:
    - windows
  size_max: 2000000
traits:
  - id: ollydbg-executable-marker
    desc: OllyDbg executable identity markers
    crit: baseline
    conf: 0.98
    if:
      type: string_value
      regex: (?i)(ollydbg|32-bit analysing debugger)
      section: .text
