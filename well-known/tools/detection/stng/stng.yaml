defaults:
  platforms: [unix, macos, windows]
  for: [binaries, rust]
  crit: baseline
  conf: 0.95
  size_max: 52428800
traits:
  - id: xor-classify
    desc: STNG XOR classification function
    if:
      type: symbol
      regex: "stng::xor::classify"
