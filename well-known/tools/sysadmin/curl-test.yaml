defaults:
  platforms:
    - unix
    - windows
traits:
  - id: curl-smb-server
    size_min: 512
    size_max: 500000000
    desc: curl SMB test server script
    crit: notable
    conf: 1.0
    for:
      - python
    if:
      type: string_literal
      substr: Server for testing SMB
