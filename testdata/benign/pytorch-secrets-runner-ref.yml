name: Linux job
on: workflow_call
jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - name: Run script
        env:
          ALL_SECRETS: ${{ toJSON(secrets) }}
        run: |
          python3 "${{ github.workspace }}/test-infra/.github/scripts/run_with_env_secrets.py" "MY_TOKEN"
