name: Unpinned image controls
description: Static test case; do not execute this example.
inputs:
  wait-token:
    description: Token used to poll the API
    default: ${{ github.token }}
runs:
  using: docker
  image: docker://ghcr.io/example-org/pinned-waiter
