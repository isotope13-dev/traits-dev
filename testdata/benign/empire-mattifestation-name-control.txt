# Static negative-control fixture for the Mattifestation module-name traits.
# Ordinary `name:` declarations without any AMSI content must not match.
name: deploy
description: Routine deployment helper
steps:
  - run: echo hello
  - run: Get-Service | Where-Object { $_.Status -eq 'Running' }
