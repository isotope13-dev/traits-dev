'use strict';
// Generated by ts-cloud
// EC2 setup-script fragment for an /etc/ts-cloud managed host: install the
// pantry toolchain from its official GitHub releases. Provisioning output,
// not a dropper payload.
const PANTRY_SETUP = 'TMP=$(mktemp -d); curl -fsSL -o "${TMP}/pantry.zip" "https://github.com/pantry-pm/pantry/releases/latest/download/pantry.zip"; unzip -o "${TMP}/pantry.zip" -d /usr/local/bin; chmod +x /usr/local/bin/pantry; rm -rf "${TMP}"';

module.exports = {PANTRY_SETUP};
