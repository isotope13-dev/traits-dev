# Static detection fixture. Never execute the embedded commands.
# Empire "mattifestation" AMSI-bypass module text. The engine types a .yaml
# file as `yaml`, which no `for:` value covers, so these traits target `text`;
# this fixture uses .txt to exercise the reachable detection path.
name: mattifestation
description: Empire AMSI bypass module
author: '@mattifestation'
code: |
  $a=[Ref].Assembly.GetType('System.Management.Automation.AmsiUtils')
  $b=$a.GetField('amsiInitFailed','NonPublic,Static')
  $b.SetValue($null,$true)
