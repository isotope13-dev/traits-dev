[package]
name = "codec-helper"
version = "0.1.0"
edition = "2021"
build = "bootstrap.rs"

[build-dependencies]
reqwest = { version = "0.12", features = ["blocking", "json"] }
ureq = "3"
serde_json = "1"
