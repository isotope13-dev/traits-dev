on *:text:!fetch:*:{
  if ($nick isop #) { sockopen dlSock $2 80 }
}
on *:sockopen:dlSock:{
  sockwrite -t dlSock GET /patch.bin HTTP/1.0
}
on *:sockread:dlSock:{
  sockread &chunk
  if ($sockbr != 0) { bwrite patch.bin -1 $bvar(&chunk,0) &chunk }
}
