traits:
  - id: msi-installer
    desc: Microsoft Installer (MSI) package
    crit: baseline
    conf: 0.8
    for: [pe, dll]
    platforms: [windows]
    if:
      type: string
      substr: "Microsoft Installer"
      section: .data
