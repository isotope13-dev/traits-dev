defaults:
  platforms: [unix, windows, macos]
  for: [source]
traits:
  - id: makefile-basename
    desc: Makefile (basename)
    crit: baseline
    conf: 1.0
    if:
      type: basename
      exact: Makefile
