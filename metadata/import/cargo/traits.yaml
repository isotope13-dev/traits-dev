defaults:
  platforms: [windows, unix]
  for: [cargo.toml]
  crit: baseline
  conf: 0.9

traits:
  - id: dep-reqwest
    desc: Depends on reqwest crate
    if:
      type: value
      path: dependencies.reqwest
      regex: '.+'

  - id: dep-base64
    desc: Depends on base64 crate
    if:
      type: value
      path: dependencies.base64
      regex: '.+'

  - id: build-dep-base64
    desc: Build dependency on base64 crate
    if:
      type: value
      path: build-dependencies.base64.version
      regex: '.+'

  - id: dep-rdkafka
    desc: Depends on rdkafka crate
    if:
      type: value
      path: dependencies.rdkafka
      regex: '.+'

  - id: dep-hyper
    desc: Depends on hyper crate
    if:
      type: value
      path: dependencies.hyper
      regex: '.+'

  - id: dep-ureq
    desc: Depends on ureq crate
    if:
      type: value
      path: dependencies.ureq
      regex: '.+'

  - id: dep-surf
    desc: Depends on surf crate
    if:
      type: value
      path: dependencies.surf
      regex: '.+'

  - id: dep-curl
    desc: Depends on curl crate
    if:
      type: value
      path: dependencies.curl
      regex: '.+'
