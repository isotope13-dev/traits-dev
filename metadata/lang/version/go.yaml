defaults:
  platforms: [windows, linux, macos]
  for: [go, elf, macho, pe]
traits:
  - id: version
    desc: Detects Go programming language version
    crit: baseline
    if:
      type: text
      regex: go(\d+\.\d+(\.\d+)?)
      section: .text
