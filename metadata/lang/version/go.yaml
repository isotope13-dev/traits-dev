defaults:
  platforms: [linux, unix, android, macos, ios, windows]
  for: [go, binaries]
traits:
  - id: version
    desc: "Detects Go programming language version"
    crit: baseline
    if:
      type: string_value
      regex: "go(\\d+\\.\\d+(\\.\\d+)?)"
      section: .text
