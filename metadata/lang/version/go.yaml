defaults:
  platforms: [linux, unix, android, macos, ios, windows]
  for: [go, elf, macho, pe]
traits:
  - id: version
    desc: "Detects Go programming language version"
    crit: baseline
    if:
      type: string
      regex: "go(\\d+\\.\\d+(\\.\\d+)?)"
      section: .text
