defaults:
  for: [elf, macho, pe, so, dylib, dll, shell]
traits:
  - id: version
    desc: "Detects Go programming language version"
    crit: baseline
    if:
      type: string
      regex: "go(\\d+\\.\\d+(\\.\\d+)?)"
      section: .text
