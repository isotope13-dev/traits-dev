defaults:
  platforms: [unix, windows]
  for: [source, -c]
traits:
  - id: stack-chk-fail
    desc: __stack_chk_fail function
    crit: baseline
    conf: 0.9
    for: [cpp, go, java, rust]
    size_min: 1
    if:
      id: metadata/hardening::stack-chk-fail
  - id: fortify-fail
    desc: __fortify_fail function
    crit: notable
    conf: 0.85
    for: [cpp, go, java, rust]
    size_min: 1
    if:
      id: metadata/hardening::fortify-fail
  - id: chk-fail
    desc: __chk_fail function
    crit: notable
    conf: 0.85
    for: [cpp, go, java, rust]
    size_min: 1
    if:
      id: metadata/hardening::chk-fail
  - id: chk-suffix
    desc: _chk@ function suffix
    crit: notable
    conf: 0.85
    for: [cpp, go, java, rust]
    size_min: 1
    if:
      id: metadata/hardening::chk-suffix
  - id: memcpy-chk
    desc: __memcpy_chk function
    crit: notable
    conf: 0.85
    for: [cpp, go, java, rust]
    size_min: 1
    if:
      id: metadata/hardening::memcpy-chk
  - id: strcpy-chk
    desc: __strcpy_chk function
    crit: notable
    conf: 0.85
    for: [cpp, go, java, rust]
    size_min: 1
    if:
      id: metadata/hardening::strcpy-chk
composite_rules:
  - id: pie
    desc: Position Independent Executable (PIE)
    crit: notable
    conf: 0.85
    any:
      - id: metadata/hardening::pie-macro
      - id: metadata/hardening::fpie-flag
      - id: metadata/hardening::got-plt-section
  - id: stack-protector
    desc: Stack protector/canary enabled
    crit: notable
    conf: 0.9
    any:
      - id: stack-chk-fail
      - id: metadata/hardening::stack-chk-guard
      - id: metadata/hardening::fstack-protector
  - id: fortify
    desc: FORTIFY_SOURCE hardening enabled
    crit: notable
    conf: 0.85
    any:
      - id: fortify-fail
      - id: chk-fail
      - id: chk-suffix
      - id: memcpy-chk
      - id: strcpy-chk
  - id: relro
    desc: RELRO hardening enabled
    crit: notable
    conf: 0.8
    any:
      - id: metadata/hardening::gnu-relro
      - id: metadata/hardening::got-section
