defaults:
  platforms: [windows, unix]
  for:
    - typescript
traits:
  - id: dts-declaration-file
    desc: TypeScript declaration file (.d.ts/.d.cts/.d.mts)
    crit: baseline
    conf: 0.99
    if:
      type: path
      basename: true
      regex: '\.d\.(ts|cts|mts)$'
