defaults:
  platforms: [unix, windows]
  for: [php]
  crit: component
  conf: 0.95
traits:
  - id: php-open-tag
    desc: PHP opening tag present
    if:
      type: text
      substr: "<?php"
      case_insensitive: true
