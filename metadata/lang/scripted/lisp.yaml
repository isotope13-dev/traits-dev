defaults:
  crit: notable
  platforms: [windows, unix]
  for: [binaries]
traits:
  - id: sbcl-runtime
    desc: "SBCL Common Lisp runtime artifact"
    conf: 0.99
    if:
      type: text
      substr: sbcl_runtime
      section: .text
  - id: sbcl-home
    desc: SBCL_HOME string
    conf: 0.95
    if:
      type: text
      substr: SBCL_HOME
      section: .text
composite_rules:
  - id: is-sbcl
    desc: Steel Bank Common Lisp (SBCL) binary
    conf: 0.99
    any:
      - id: sbcl-runtime
      - id: sbcl-home
