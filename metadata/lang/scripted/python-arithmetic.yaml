defaults:
  platforms: [unix, windows]
  for: [python]
  crit: component
  conf: 0.9
traits:
- id: python-cpu-count-integer-division
  desc: Python CPU count integer division
  if:
    type: text
    regex: 'os\.cpu_count\(\)\s*//\s*\d+'
