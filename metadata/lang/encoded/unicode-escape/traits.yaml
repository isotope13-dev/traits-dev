defaults:
  platforms:
    - linux
    - macos
    - windows
    - unix
    - android
    - ios
  for: [scripts, source, manifests, documents, data]
traits:
  - id: javascript
    desc: javascript code encoded in string
    crit: notable
    conf: 1.0
    size_min: 20000
    count_min: 5
    if:
      type: encoded
      encoding: unicode-escape
      regex: (function\s+\w+\s*\(|var\s+\w+\s*=)
    unless:
      - type: basename
        regex: \.js$
      - type: raw
        substr: dist/assets/
      - type: encoded
        encoding: unicode-escape
        substr: \u003c
      - type: encoded
        encoding: unicode-escape
        regex: \x1b\[[0-9;]{1,12}m
      - type: encoded
        encoding: unicode-escape
        regex: \\x1b\[[0-9;]{1,12}m
      - type: encoded
        encoding: unicode-escape
        regex: \\u001b\[[0-9;]{1,12}m
      - type: raw
        substr: \\u0026
  - id: shell-interpreter-cmd
    desc: cmd interpreter marker encoded
    crit: component
    conf: 0.95
    for: [binaries, documents]
    if:
      type: encoded
      encoding: unicode-escape
      regex: (?i)\bcmd(?:\.exe)?\b
  - id: shell-interpreter-powershell
    desc: powershell marker encoded
    crit: component
    conf: 0.95
    for: [binaries, documents]
    if:
      type: encoded
      encoding: unicode-escape
      regex: (?i)\b(?:powershell|pwsh)(?:\.exe)?\b
    unless:
      - type: raw
        substr: minikube completion powershell
      - type: raw
        substr: shell (bash, zsh, fish or powershell)
  - id: shell-interpreter-script-host
    desc: script host marker encoded
    crit: component
    conf: 0.95
    for: [binaries, documents]
    if:
      type: encoded
      encoding: unicode-escape
      regex: (?i)\b(?:wscript|cscript)(?:\.exe)?\b
  - id: shell-shebang
    desc: shell shebang encoded in string
    crit: suspicious
    conf: 0.98
    for: [binaries, documents]
    if:
      type: encoded
      encoding: unicode-escape
      regex: "#!/bin/(?:sh|bash|zsh)\\b"
  - id: shell-batch-prologue
    desc: batch prologue encoded in string
    crit: suspicious
    conf: 0.96
    for: [binaries, documents]
    if:
      type: encoded
      encoding: unicode-escape
      regex: (?i)(@echo off\b|\bsetlocal\b)
  - id: shell-completion-help
    desc: shell completion help text
    crit: component
    conf: 1.0
    for: [binaries, documents]
    if:
      type: encoded
      encoding: unicode-escape
      section: .rodata
      substr: Outputs minikube shell completion
  - id: powershell-completion-example
    desc: powershell completion example text
    crit: component
    conf: 1.0
    for: [binaries, documents]
    if:
      type: encoded
      encoding: unicode-escape
      section: .rodata
      substr: minikube completion powershell

composite_rules:
  - id: shell
    desc: Encoded shell content (unicode escapes)
    crit: notable
    conf: 0.8
    for: [binaries, documents]
    any:
      - id: shell-shebang
      - id: shell-batch-prologue
      - id: shell-interpreter-encoded
    unless:
      - id: shell-completion-help
      - id: powershell-completion-example
  - id: shell-interpreter-encoded
    desc: Encoded shell interpreter (unicode escapes)
    crit: notable
    conf: 0.9
    for: [binaries, documents]
    any:
      - id: shell-interpreter-cmd
      - id: shell-interpreter-powershell
      - id: shell-interpreter-script-host
