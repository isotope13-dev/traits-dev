defaults:
  platforms: [unix, windows]
  for: [source, text]
  crit: component
traits:
  - id: erlang-module-marker
    desc: Erlang module declaration marker
    conf: 1.0
    if:
      type: text
      substr: "-module("
