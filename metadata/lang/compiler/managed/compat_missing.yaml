defaults:
  platforms: [windows]
  for: [binaries]

traits:
  - id: versioned-vcl-app
    for: [text]
    desc: Compatibility placeholder
    crit: component
    conf: 1.0
    if:
      type: section
      exact: __cleave_missing_versioned_vcl_app__
