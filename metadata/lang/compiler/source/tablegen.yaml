defaults:
  platforms: [linux, macos, windows]
  for: [python]

traits:
  - id: llvm-tablegen-source-marker
    desc: LLVM TableGen source-language mode marker
    crit: component
    conf: 0.99
    if:
      type: text
      substr: '-*- tablegen -*-'
