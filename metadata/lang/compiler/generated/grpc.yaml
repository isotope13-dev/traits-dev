defaults:
  platforms: [unix, windows]
  for: [binaries]
  crit: component
  conf: 0.95

traits:
  - id: rpc-idl-placeholder-identifiers
    desc: RPC IDL generated-code identifier
    if:
      type: text
      regex: '\bstruct_[A-Z]\b|\belem_[0-9]+\b'
