---
defaults:
  platforms:
  - unix
  - windows
  for:
  - javascript
  - typescript
traits:
- id: extension-id-map
  desc: Hardcoded extension ID map or list
  crit: component
  conf: 0.86
  count_min: 3
  if:
    type: text
    regex: "\\b[a-z]{32}\\b"
  unless:
  - id: metadata/package/testing/
