defaults:
  platforms: [unix, windows]
  for: [chrome-manifest]
  crit: notable
  conf: 0.94

traits:
  - id: permission-desktop-capture
    desc: Extension can capture desktop media
    if:
      type: value
      path: permissions
      exact: desktopCapture
