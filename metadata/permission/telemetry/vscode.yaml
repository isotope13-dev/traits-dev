---
defaults:
  platforms:
  - unix
  - windows
  for:
  - javascript
  - typescript
traits:
- id: telemetry-event
  desc: VSCode telemetry event sending
  crit: notable
  conf: 0.7
  if:
    type: string_literal
    substr: sendTelemetryEvent
