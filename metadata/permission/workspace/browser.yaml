defaults:
  platforms: [unix, windows]
  for: [chrome-manifest]
  crit: notable
  conf: 0.95

traits:
  - id: extension-file-system-access
    desc: Extension requests filesystem API access
    if:
      type: value
      path: 'permissions[*].fileSystem'
      exists: true
