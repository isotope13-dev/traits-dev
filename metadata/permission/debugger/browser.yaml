defaults:
  platforms: [windows, unix]
  for: [chrome-manifest]
  crit: notable
  conf: 0.98

traits:
  - id: permission-debugger
    desc: Extension declares debugger attachment authority
    if:
      type: value
      path: permissions
      exact: debugger
