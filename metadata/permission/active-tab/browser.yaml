---
defaults:
  platforms:
  - unix
  - windows
  for:
  - chrome-manifest
  crit: notable
  conf: 0.9
traits:
- id: permission-active-tab
  desc: Extension requests activeTab permission
  if:
    type: value
    path: permissions
    exact: activeTab
