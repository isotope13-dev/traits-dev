defaults:
  platforms: [unix, windows]
  for: [chrome-manifest]
  crit: notable
  conf: 0.85

traits:
  - id: extension-file-handler
    desc: Extension registers a file-type handler
    if:
      type: value
      path: file_handlers
      exists: true

  - id: content-script-run-at-start
    desc: Content script runs at document_start
    if:
      type: value
      path: 'content_scripts[*].run_at'
      exact: 'document_start'

  - id: content-script-match-about-blank
    desc: Content script runs in about:blank frames
    if:
      type: value
      path: 'content_scripts[*].match_about_blank'
      exact: 'true'

  - id: content-script-main-world
    desc: Content script runs in page main world
    if:
      type: value
      path: 'content_scripts[*].world'
      exact: 'MAIN'
