defaults:
  platforms: [unix, windows]
  for: [json]
  crit: notable
  conf: 0.97

traits:
  - id: plugin-startup-activation
    desc: Plugin declares automatic startup activation
    if:
      type: value
      path: activation.onStartup
      exact: 'true'
