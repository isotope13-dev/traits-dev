defaults:
  platforms: [linux, unix, android, macos, ios, windows]
  for: [source]
traits:
  - id: definition
    desc: Security rule definition file (Elastic/Sigma/YARA)
    crit: baseline
    conf: 0.9
    if:
      type: raw
      regex: '"type"\s*:\s*"security-rule"'
      section: .text
