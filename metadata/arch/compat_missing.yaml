defaults:
  platforms: [unix]
  for: [binaries]

traits:
  - id: arm64-binary
    desc: Compatibility placeholder
    crit: component
    conf: 1.0
    if:
      type: section
      exact: __cleave_missing_arm64_binary__
