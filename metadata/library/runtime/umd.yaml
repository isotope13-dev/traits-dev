defaults:
  platforms: [windows, unix]
  for: [javascript, typescript]
traits:
  - id: umd-window-assignment
    desc: UMD pattern assigning to window
    crit: baseline
    if:
      type: text
      regex: 'typeof\s+window\s*!=\s*[''"]undefined[''"]\s*&&\s*\(\s*window\.[\w$]+\s*='
