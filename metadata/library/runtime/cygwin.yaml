defaults:
  platforms: [windows]
traits:
  - id: cygwin-runtime
    desc: Cygwin runtime version marker
    crit: baseline
    conf: 0.98
    for: [pe, dll]
    if:
      type: text
      substr: "%%% Cygwin dll identifier: cygwin1"
