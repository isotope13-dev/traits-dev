defaults:
  platforms: [windows]
traits:
  - id: cygwin-runtime
    desc: Cygwin runtime version marker
    crit: baseline
    conf: 0.98
    for: [pe]
    if:
      type: text
      substr: "%%% Cygwin dll identifier: cygwin1"
      section: rdata
