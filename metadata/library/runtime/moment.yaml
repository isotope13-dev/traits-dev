defaults:
  platforms: [windows, unix]
  for: [binaries]
traits:
  - id: moment-locale
    desc: Moment.js locale file
    crit: baseline
    if:
      type: raw
      regex: moment\.js language configuration
      section: .text
