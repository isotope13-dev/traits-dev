defaults:
  platforms: [linux, macos, windows, unix]
  for: [binaries]
traits:
  - id: openmp-runtime
    desc: OpenMP runtime marker string
    crit: component
    if:
      type: string_value
      substr: OpenMP Runtime
      section: .rdata
