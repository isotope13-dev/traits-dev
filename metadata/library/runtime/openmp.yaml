defaults:
  platforms: [windows, unix]
  for: [binaries]
traits:
  - id: openmp-runtime
    desc: OpenMP runtime marker string
    crit: component
    if:
      type: text
      substr: OpenMP Runtime
      section: .rdata
