defaults:
  platforms: [windows, unix]
  for: [javascript]

traits:
  - id: build-banner
    desc: Modernizr build banner
    crit: component
    conf: 0.95
    if:
      type: text
      substr: 'Build https://modernizr.com/download?'
