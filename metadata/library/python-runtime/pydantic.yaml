defaults:
  platforms: [linux, macos, windows]
  for: [python]
traits:
  - id: pydantic-import
    desc: Pydantic data-model library import
    crit: baseline
    conf: 0.95
    if:
      type: text
      regex: '(?m)^\s*(from\s+pydantic\s+import\b|import\s+pydantic\b)'
