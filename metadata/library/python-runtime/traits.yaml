defaults:
  platforms: [linux, macos, windows]
traits:
  - id: python-runtime
    desc: Python runtime environment
    crit: baseline
    conf: 0.95
    for: [pe, dll, so, dylib]
    if:
      type: symbol
      exact: "PyGILState_Ensure"
