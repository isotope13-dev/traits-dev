defaults:
  platforms: [windows, unix]
traits:
  - id: python-runtime
    desc: Python runtime environment
    crit: baseline
    conf: 0.95
    for: [pe, so, dylib]
    if:
      type: symbol
      exact: "PyGILState_Ensure"
