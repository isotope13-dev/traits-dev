defaults:
  platforms: [windows, unix]
traits:
  - id: client
    desc: Socket.IO client library
    crit: baseline
    conf: 1.0
    for: [javascript]
    if:
      type: text
      regex: 'Socket\.IO\s+v\d+\.\d+\.\d+'
