defaults:
  platforms: [linux, unix, android, macos, ios, windows]
  for: [source]
traits:
  - id: botocore-lib
    desc: botocore library detected
    crit: baseline
    if:
      type: string
      substr: "botocore"
      section: .text
