defaults:
  for: [elf, macho, pe, so, dylib, dll, shell]
traits:
  - id: botocore-lib
    desc: botocore library detected
    crit: baseline
    if:
      type: string
      substr: "botocore"
      section: .text
