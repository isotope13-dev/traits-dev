defaults:
  platforms: [linux, unix, android, macos, ios, windows]
  for: [source]
traits:
  - id: botocore-lib
    desc: botocore library detected
    crit: baseline
    if:
      type: string_value
      substr: "botocore"
      section: .text
