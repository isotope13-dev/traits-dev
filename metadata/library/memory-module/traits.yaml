defaults:
  platforms: [windows]
  for: [c, cpp]
traits:
  - id: memory-module-source
    desc: MemoryModule library source file
    crit: baseline
    conf: 0.99
    if:
      type: basename
      regex: MemoryModule\.(c|h)$
