defaults:
  platforms:
    - linux
    - unix
    - android
    - macos
    - ios
    - windows
  for: [binaries]
traits:
  - id: botocore-lib
    desc: botocore library detected
    crit: baseline
    if:
      type: string_value
      substr: botocore
      section: .text
