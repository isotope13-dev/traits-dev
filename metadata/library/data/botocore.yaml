defaults:
  platforms: [windows, unix]
  for: [binaries]
traits:
  - id: botocore-lib
    desc: botocore library detected
    crit: baseline
    if:
      type: text
      substr: botocore
      section: .text
