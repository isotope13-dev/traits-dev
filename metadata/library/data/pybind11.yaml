defaults:
  platforms:
    - windows
    - linux
    - macos
  for: [pe, elf, macho]
traits:
  - id: pybind11-marker
    desc: pybind11 C++ to Python library marker
    crit: baseline
    conf: 0.95
    if:
      type: text
      substr: pybind11
      section: .rdata
