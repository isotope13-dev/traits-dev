defaults:
  platforms:
    - windows
  for: [pe, dll]
traits:
  - id: aws-sdk-dotnet-core
    desc: AWS SDK for .NET Core assembly
    crit: baseline
    if:
      type: text
      substr: AWSSDK.Core
      section: .text
