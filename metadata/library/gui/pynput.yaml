defaults:
  platforms: [windows, unix]
  for: [python, pyc]

traits:
  - id: pynput-package-path
    desc: pynput package module path
    crit: baseline
    conf: 0.95
    if:
      type: path
      regex: '(^|[/\\])pynput[/\\]'
