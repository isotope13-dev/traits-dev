defaults:
  platforms: [linux, unix, android, macos, ios, windows]
  for: [source]
traits:
  - id: moment-locale
    desc: Moment.js locale file
    crit: baseline
    if:
      type: raw
      regex: "moment\\.js language configuration"
      section: .text
