defaults:
  for: [elf, macho, pe, so, dylib, dll, shell]
traits:
  - id: moment-locale
    desc: Moment.js locale file
    crit: baseline
    if:
      type: raw
      regex: "moment\\.js language configuration"
      section: .text
