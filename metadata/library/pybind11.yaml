defaults:
  platforms: [windows, linux, macos]
  for: [elf, macho, pe, dylib, dll]
traits:
  - id: pybind11-marker
    desc: pybind11 C++ to Python library marker
    crit: component
    conf: 0.95
    if:
      type: string
      substr: "pybind11"
      section: ".rdata"