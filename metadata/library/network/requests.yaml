# requests — Python HTTP library.
defaults:
  platforms: [windows, unix]
  for: [python, pyc]
  crit: baseline

traits:
  - id: requests-package-path
    desc: requests package path segment
    if:
      type: path
      regex: '(?i)(^|[\\/])requests([\\/]|$)'
