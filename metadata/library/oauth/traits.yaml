defaults:
  platforms: [unix, windows]
traits:
  - id: oauthlib-rfc6749-source
    desc: oauthlib RFC6749 OAuth client source
    crit: baseline
    conf: 0.98
    for: [python, pyc]
    if:
      type: text
      substr: oauthlib.oauth2.rfc6749
