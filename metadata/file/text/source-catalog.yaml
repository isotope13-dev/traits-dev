defaults:
  platforms: [unix, windows]
  for: [lua]
  crit: baseline
  conf: 0.95

traits:
  - id: neovim-filetype-registry
    desc: Neovim filetype mapping registry
    if:
      type: text
      regex: 'vim\.filetype\.mapping[\s\S]{0,4096}local extension = \{[\s\S]{0,4096}BEGIN EXTENSION'
