defaults:
  platforms: [windows]
  for: [pe, dll]
  crit: component
  conf: 0.8
traits:
  - id: is-windows-named
    desc: File name contains "Windows"
    if:
      type: basename
      substr: Windows
