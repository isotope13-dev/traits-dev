defaults:
  platforms: [unix, windows]
  for: [documents, scripts, binaries]
traits:
  - id: xmrig-product
    desc: XMRig product name
    crit: notable
    conf: 0.8
    if:
      type: text
      word: xmrig
      case_insensitive: true
