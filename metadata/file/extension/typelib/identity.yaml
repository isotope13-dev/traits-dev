defaults:
  platforms: [windows, unix]
  for: [pe]
  crit: baseline
  conf: 1.0

traits:
  - id: tlb
    desc: COM type library resource extension
    if:
      type: path
      basename: true
      regex: (?i)\.tlb$
