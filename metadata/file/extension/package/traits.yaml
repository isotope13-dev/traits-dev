defaults:
  platforms: [windows, unix]

traits:
  - id: rust-crate-basename
    desc: Rust crate package archive basename
    crit: baseline
    conf: 0.95
    for: [archive, crate]
    if:
      type: basename
      regex: '(?i)\.crate$'
