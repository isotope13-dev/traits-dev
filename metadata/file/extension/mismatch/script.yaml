defaults:
  conf: 0.9
  platforms: [windows]
  for: [batch]

traits:
  - id: batch-content-extension-mismatch
    desc: Batch content mismatches filename extension
    crit: notable
    if:
      type: metrics
      field: consistency.extension_content_mismatch
      min: 1
