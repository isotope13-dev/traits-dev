defaults:
  platforms: [windows, unix]
  for: [php, javascript, ruby, perl, lua]
  crit: baseline
  conf: 0.9

traits:
  - id: template-source-basename
    desc: Template source file basename
    if:
      type: path
      basename: true
      regex: '\.(blade\.php|tpl|twig)$'
