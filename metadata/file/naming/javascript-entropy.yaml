defaults:
  platforms: [windows, unix]
  for: [javascript, typescript]
  crit: notable
  conf: 0.9

traits:
- id: js-high-entropy-identifier-ratio-metric
  desc: High ratio of entropy-heavy identifiers
  crit: notable
  conf: 0.84
  size_min: 8000
  size_max: 30000
  if:
    type: metrics
    field: identifiers.high_entropy_ratio
    min: 0.35

