defaults:
  platforms: [unix, windows]
  for: [class, jar, zip]

traits:
  - id: jvm-dot-cmd-suffix
    for: [class, jar]
    desc: JVM dot-cmd string suffix
    crit: component
    conf: 0.94
    if:
      type: value
      path: class.strings[*]
      regex: '\.cmd$'

  - id: jvm-dot-out-suffix
    for: [class, jar]
    desc: JVM dot-out string suffix
    crit: component
    conf: 0.94
    if:
      type: value
      path: class.strings[*]
      regex: '\.out$'

  - id: jvm-active-server-log-name
    for: [class, jar]
    desc: JVM active server log constant
    crit: notable
    conf: 0.96
    if:
      type: value
      path: class.strings[*]
      exact: server.log

  - id: jvm-rotated-server-log-prefix
    for: [class, jar]
    desc: JVM rotated server log prefix
    crit: notable
    conf: 0.96
    if:
      type: value
      path: class.strings[*]
      exact: server.log.

  - id: jvm-derby-log-path
    for: [class, jar]
    desc: JVM Apache Derby log path
    crit: component
    conf: 0.96
    if:
      type: value
      path: class.strings[*]
      regex: '(^|[/\\])derby\.log$'
