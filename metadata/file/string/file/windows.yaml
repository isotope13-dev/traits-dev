defaults:
  platforms: [windows]
  for: [binaries, scripts]

traits:
  - id: desktop-ini-filename
    desc: desktop.ini filename reference
    crit: component
    conf: 0.86
    if:
      type: text
      exact: desktop.ini
