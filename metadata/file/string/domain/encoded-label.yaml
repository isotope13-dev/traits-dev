defaults:
  platforms: [windows, unix]
  for: [scripts, source]

traits:
  - id: long-alphanumeric-third-level-label
    desc: Long alphanumeric third-level domain label
    crit: component
    conf: 0.75
    if:
      type: text
      regex: '\b[a-z0-9]{30,}\.[a-z]{2,16}\.(com|net|org|io)\b'
