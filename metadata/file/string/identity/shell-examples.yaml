defaults:
  platforms: [unix, windows]
  for: [scripts, source, documents]

traits:
  - id: abbreviated-curl-bash-reference
    desc: Abbreviated curl-to-Bash command reference
    crit: notable
    conf: 0.9
    if:
      type: text
      substr: curl ... | bash
