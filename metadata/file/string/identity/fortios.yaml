defaults:
  platforms: [fortios, appliance, unix]
  for: [scripts, binaries]

traits:
  - id: fortios-automation-results-placeholder
    desc: FortiOS automation output placeholder
    crit: notable
    conf: 0.95
    if:
      type: text
      substr: "%%results%%"
