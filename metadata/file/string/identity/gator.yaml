defaults:
  platforms: [windows]
  for: [pe, dll]

traits:
  - id: gator-install-routine-marker
    desc: Gator installation routine marker
    crit: component
    conf: 0.99
    if:
      type: text
      exact: InstallGator
      section: .data
