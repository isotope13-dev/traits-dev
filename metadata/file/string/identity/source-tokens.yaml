defaults:
  platforms: [windows, unix]
  for: [scripts, source]
  crit: component
  conf: 0.95

traits:
  - id: chunk-token
    desc: Source chunk token
    if:
      type: text
      substr: chunk
    unless:
      - id: well-known/app/
      - id: well-known/lib/
      - id: well-known/tool/
