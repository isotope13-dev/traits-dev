defaults:
  platforms: [windows, unix]
  for: [python, javascript, powershell, pe, elf]

traits:
  - id: established-session-state
    desc: dnscat2 established-session state constant
    crit: notable
    conf: 0.9
    if:
      type: text
      substr: SESSION_STATE_ESTABLISHED
