---
defaults:
  platforms:
  - windows
  - unix
  crit: component
  conf: 0.2
  for:
  - binaries
  - scripts
  - source
traits:
- id: demo-prefix-keyword
  desc: DEMO bare word (temp/payload prefix)
  conf: 0.3
  platforms:
  - windows
  for:
  - pe
  if:
    type: text
    exact: DEMO
    section: rdata
- id: unknown-token
  desc: 'Bare token: UNKNOWN'
  crit: component
  conf: 0.2
  if:
    type: string_literal
    exact: UNKNOWN
