# Neutral credential-related identifiers. A label alone does not establish
# credential access; objectives combine it with reads, collection, or use.
defaults:
  platforms: [unix, windows]
  for: [scripts, source]
  crit: component

traits:
  - id: uppercase-password-identifier
    desc: Uppercase identifier contains PASSWORD or PASS
    conf: 0.45
    if:
      type: literal
      regex: '[A-Z][A-Z0-9_]{0,48}(PASSWORD|PASS)(_[A-Z0-9_]{1,48})?\b'
