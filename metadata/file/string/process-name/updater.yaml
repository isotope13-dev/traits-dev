defaults:
  platforms: [windows]
  for: [pe, dll]
  crit: baseline
  conf: 0.7

traits:
  - id: updater-exe-name
    desc: updater.exe process name reference
    if:
      type: text
      exact: updater.exe
      section: .rdata
