defaults:
  platforms:
  - windows
  for:
  - pe
  - dll
traits:
- id: calc-exe-name
  desc: calc.exe process name reference
  crit: baseline
  conf: 0.6
  if:
    type: text
    exact: calc.exe
    section: ".rdata"
- id: notepad-exe-name
  desc: notepad.exe process name reference
  crit: baseline
  conf: 0.6
  if:
    type: text
    exact: notepad.exe
    section: ".rdata"
- id: iexplore-exe-name
  desc: iexplore.exe process name reference
  crit: baseline
  conf: 0.6
  if:
    type: text
    exact: iexplore.exe
    section: ".rdata"
