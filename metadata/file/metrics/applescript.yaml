defaults:
  platforms: [macos]
  for: [applescript]

traits:
  - id: excess-blank-line-padding
    desc: Excess blank-line padding
    crit: baseline
    conf: 0.8
    count_min: 500
    if:
      type: text
      regex: '^\s*$'
