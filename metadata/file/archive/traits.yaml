# Member count of the archive itself.
defaults:
  platforms: [windows, unix]
  for: [php]
traits:
  - id: archive-few-members
    desc: Archive has at most five members
    crit: baseline
    conf: 0.85
    for: [whl]
    if:
      type: metrics
      field: archive.file_count
      max: 5
