defaults:
  platforms: [android]
  # Android manifests are also analyzed as extracted XML leaves.
  for: [apk, zip, xml]
  crit: notable
  conf: 0.86

traits:
  - id: android-browsable-intent-category
    desc: Android browsable intent category declaration
    ref: sha256:093c727149837d576d10f843edce933436efe648e74b9008ccd91badf197fc5c
    if:
      type: text
      substr: "android.intent.category.BROWSABLE"
