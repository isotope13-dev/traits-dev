defaults:
  platforms: [linux]
  for: [scripts, binaries]
  crit: notable
  conf: 0.82

traits:
  - id: dbus-system-policy-conf-path
    desc: D-Bus system policy path
    if:
      type: text
      regex: '/(etc|usr/share)/dbus-1/system\.d/[^\s]{1,120}\.conf\b'
