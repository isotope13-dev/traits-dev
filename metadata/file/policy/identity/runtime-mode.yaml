defaults:
  platforms: [windows, unix]
  for: [json, chrome-manifest]
  crit: baseline
  conf: 0.96

traits:
  - id: run-type-mode
    desc: JSON run_type execution mode field
    if:
      type: value
      path: run_type
      regex: '^(client|server|forward|nat)$'
