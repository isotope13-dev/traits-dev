defaults:
  platforms: [android]
  for: [xml]
  crit: baseline
  conf: 0.99

traits:
  - id: android-sdk-api-version-catalog
    desc: Android SDK API version catalog
    if:
      type: basename
      exact: api-versions.xml
