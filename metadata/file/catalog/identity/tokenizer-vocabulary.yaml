defaults:
  platforms: [unix, windows]
  for: [binaries]

traits:
  - id: tokenizer-vocab-json
    desc: Tokenizer vocabulary JSON blob
    crit: component
    conf: 0.9
    if:
      type: text
      substr: '{"!": 0, "\"": 1'
