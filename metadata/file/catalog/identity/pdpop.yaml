defaults:
  crit: notable
  conf: 0.99
  platforms: [windows]
  for: [pe]

traits:
  - id: webhard-registry-path
    desc: PDPOP WebHard registry path
    if:
      type: text
      exact: Software\pdpop\WebHard
      section: .rdata

  - id: sfpdpop-helper-dll
    desc: PDPOP smartfilter helper DLL
    if:
      type: text
      exact: sfpdpop.dll
      section: .rdata
