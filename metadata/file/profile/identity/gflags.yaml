defaults:
  platforms: [windows]
  for: [pe]

traits:
  - id: gflags-original-filename-claim
    desc: GFLAGS.EXE filename resource claim
    crit: notable
    conf: 0.98
    if:
      type: value
      path: pe.version.original_filename
      exact: GFLAGS.EXE

  - id: gflags-description-claim
    desc: GFlags description resource claim
    crit: notable
    conf: 0.98
    if:
      type: value
      path: pe.version.description
      regex: '(?i)\bGlobal Flags Manipulator\b'

  - id: gflags-version-claim
    desc: GFlags version resource claim
    crit: notable
    conf: 0.96
    if:
      type: value
      path: pe.version.file_version
      regex: '^6\.1\.[0-9]+\.[0-9]+'

composite_rules:
  - id: gflags-registry-management-context
    desc: Matching GFlags registry context
    crit: exception
    conf: 0.98
    all:
      - id: gflags-original-filename-claim
      - id: gflags-description-claim
      - id: gflags-version-claim
