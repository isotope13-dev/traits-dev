defaults:
  platforms: [windows, unix]
  for: [zip]

traits:
  - id: hex-hash-archive-member-basename
    desc: Archive member has a hexadecimal hash basename
    crit: notable
    conf: 0.92
    if:
      type: path
      basename: true
      regex: '^[0-9a-f]{32,64}(-[0-9]+)?(\.zip)?$'
