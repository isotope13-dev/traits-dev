defaults:
  platforms: [windows]
  for: [pe]
  crit: component
  conf: 0.99

traits:
  - id: iexpress-wextract-manifest
    desc: Microsoft IExpress extraction manifest
    if:
      type: text
      substr: '<description>IExpress extraction tool</description>'
      section: rsrc
