defaults:
  platforms: [windows, unix]
  for: [xml, javascript]

traits:
  - id: notepad-plus-plus-language-model
    desc: Notepad++ language model file
    crit: baseline
    conf: 0.9
    if:
      type: path
      basename: true
      exact: langs.model.xml
