defaults:
  platforms: [unix, windows]
  for: [go]

traits:
  - id: go-long-hex-byte-tables
    desc: Go has multiple long hex byte tables
    crit: component
    conf: 0.85
    count_min: 2
    if:
      type: text
      regex: '\[\]byte\{\s*0x[0-9A-Fa-f]{2}(\s*,\s*0x[0-9A-Fa-f]{2}){15,}'
