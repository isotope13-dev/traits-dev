defaults:
  platforms: [unix, windows]
  for: [javascript, typescript, zig, csharp]
traits:
  - id: jinja-template-source
    desc: Jinja template source file
    crit: baseline
    conf: 0.95
    if:
      type: path
      basename: true
      regex: '\.(jinja|jinja2|j2)$'
