defaults:
  platforms: [windows, unix]
  for: [binaries]
traits:
  - id: definition
    desc: "Security rule definition file"
    for: [elf, macho, pe]
    crit: baseline
    conf: 0.9
    if:
      type: raw
      regex: '"type"\s*:\s*"security-rule"'
      section: .text
