defaults:
  platforms: [linux, macos, windows]
  for: [xml]

traits:
  - id: xml-reference-attribute
    desc: XML reference attribute
    crit: component
    conf: 0.9
    if:
      type: text
      substr: ' reference='
