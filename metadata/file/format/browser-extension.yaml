defaults:
  platforms: [unix, windows]
  for: [zip, xpi, crx]

traits:
  - id: web-extension-root-manifest
    desc: Root web-extension manifest.json
    crit: component
    conf: 0.95
    if:
      type: value
      path: "archive.members[*].path"
      exact: "manifest.json"
