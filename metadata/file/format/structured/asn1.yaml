defaults:
  platforms: [unix, windows]
  for: [scripts, data]

traits:
  - id: asn1-module-definition
    desc: ASN.1 module definition
    crit: baseline
    conf: 0.99
    if:
      type: text
      regex: '\b[A-Z][A-Za-z0-9-]*\s+DEFINITIONS\s+::=\s+BEGIN\b'
