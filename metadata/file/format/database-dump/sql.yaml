defaults:
  platforms: [windows, unix]
  for: [text]

traits:
  - id: mysql-database-dump
    desc: MySQL database dump text
    crit: notable
    conf: 0.99
    if:
      type: text
      regex: '(?s)^-- MySQL dump [0-9]+\.[0-9]+.{0,900}/\*![0-9]{5} SET @OLD_UNIQUE_CHECKS='
