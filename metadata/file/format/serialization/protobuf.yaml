defaults:
  platforms: [windows, unix]
  for: [text]

traits:
  - id: protobuf-schema-source
    desc: Protocol Buffers schema source
    crit: baseline
    conf: 0.98
    if:
      type: text
      substr: 'syntax = "proto3";'
