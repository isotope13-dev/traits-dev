defaults:
  for: [source, binaries]
  platforms: [linux, macos, windows]

traits:
  - id: system-activity-detected-log
    desc: Log message simulating system activity
    crit: notable
    if:
      type: text
      substr: "[System Activity Detected at "
