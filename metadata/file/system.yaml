defaults:
  platforms: [unix]
  for: [binaries]
traits:
  - id: system-shell-basename
    desc: System shell basename
    crit: component
    conf: 1.0
    if:
      type: basename
      regex: (^|/)sh$
