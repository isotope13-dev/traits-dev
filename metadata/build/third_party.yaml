# metadata/build/third_party.yaml
defaults:
  platforms: [windows, unix]
  for: [any]
traits:
  - id: in-third-party-dir
    desc: File is in a third-party directory
    crit: baseline
    conf: 0.9
    if:
      type: path
      regex: /third_party/
