defaults:
  platforms: [windows, unix]
  for: [data, zip, tar]

traits:
  - id: go-archive-tar-malformed-header-fixture
    desc: Go archive/tar malformed-header test fixture
    crit: component
    conf: 0.99
    if:
      type: basename
      exact: pax-bad-hdr-large.tar.bz2
