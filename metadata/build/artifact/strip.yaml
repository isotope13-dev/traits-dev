defaults:
  platforms:
    - unix
    - android
  for:
    - elf
traits:
  - id: aggressive-strip
    desc: Toolchain banner stripped, build-id retained
    crit: notable
    conf: 1.0
    if:
      type: kv
      path: elf.stripped_metadata_sections
      exact: ".comment"
