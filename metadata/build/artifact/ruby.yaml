defaults:
  platforms: [unix, windows]
  for: [markdown, text]

traits:
  - id: documented-ruby-gem-obfuscation
    desc: Documents an obfuscated Ruby gem build
    crit: notable
    conf: 0.98
    if:
      type: text
      regex: 'rake build.{0,100}(obfuscation|obfuscated gem)'
