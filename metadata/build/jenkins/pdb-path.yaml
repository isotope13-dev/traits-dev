traits:
  - id: jenkins-pdb-path
    desc: PDB path with Jenkins CI workspace
    crit: notable
    conf: 0.85
    for: [pe]
    platforms: [windows]
    if:
      type: text
      regex: "(?i)Jenkins.WorkSpace"
