defaults:
  platforms: [windows]
  for: [pe]
traits:
  - id: jenkins-pdb-path
    desc: Jenkins CI PDB path
    crit: notable
    conf: 0.85
    if:
      type: kv
      path: debug.pdb_path
      regex: '(?i)jenkins.{0,80}workspace|workspace.{0,80}jenkins'
