defaults:
  platforms:
    - unix
  for:
    - shell

traits:
  - id: tensorflow-user-wrapper
    desc: TensorFlow CI user wrapper
    crit: component
    conf: 0.95
    if:
      type: text
      substr: with_the_same_user script
