defaults:
  platforms: [windows, unix]
  for: [javascript, typescript, json]
  crit: baseline
  conf: 0.94

traits:
  - id: cloudformation-env-substitution
    desc: CloudFormation environment substitution
    if:
      type: text
      regex: '"Fn::Sub"[\s\S]{0,80}"\$\{Environment\}"'
