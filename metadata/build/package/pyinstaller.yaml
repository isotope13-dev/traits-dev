defaults:
  platforms: [linux, macos, windows]
  for: [shell]
  crit: notable
  conf: 0.9

traits:
  - id: pyinstaller-build-command
    desc: PyInstaller packaging command
    if:
      type: symbol
      exact: pyinstaller
