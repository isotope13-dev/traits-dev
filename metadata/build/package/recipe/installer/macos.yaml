defaults:
  for: [shell]
  platforms: [macos]
  crit: notable
  conf: 0.95

traits:
  - id: macos-installer-hook-basename
    desc: macOS installer hook filename
    if:
      type: basename
      regex: '^(preinstall|postinstall|preflight|postflight)$'
