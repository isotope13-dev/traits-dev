# Ruby build system metadata
defaults:
  platforms: [windows, unix]
  for: [ruby]
  crit: notable
  conf: 1.0
traits:
  - id: filename-extconf
    desc: File is named extconf.rb
    if:
      type: path
      basename: true
      exact: extconf.rb
      case_insensitive: true
