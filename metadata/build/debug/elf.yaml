defaults:
  platforms:
    - unix
    - android
  for:
    - elf
traits:
  - id: external-debug-reference
    desc: ELF external debug artifact reference
    crit: baseline
    conf: 1.0
    if:
      type: trait
      id: metadata/build/debug::elf-debuglink
