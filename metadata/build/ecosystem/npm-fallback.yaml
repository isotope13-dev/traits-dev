defaults:
  platforms: [unix, windows]
  for: [package.json]

traits:
- id: npm-logical-or-fallback
  desc: npm script uses logical-OR fallback
  crit: component
  conf: 0.95
  if:
    type: value
    path: 'scripts[*]'
    regex: '\|\|\s*[A-Za-z][A-Za-z0-9_.-]*\b'
