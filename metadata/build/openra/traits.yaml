defaults:
  for: [shell]
  platforms: [linux]
traits:
  - id: openra-system-library-helper
    desc: OpenRA system library configuration helper
    crit: baseline
    if:
      type: text
      substr: "OpenRA expects to find a file matching"
