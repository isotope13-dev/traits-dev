defaults:
  platforms: [unix]
  for: [shell, go]
traits:
  - id: containerd-service-provisioning-marker
    desc: Containerd provisioning iptables rule
    crit: notable
    conf: 0.95
    if:
      type: text
      substr: 'containerd.service'
