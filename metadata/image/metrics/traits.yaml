defaults:
  platforms: [windows, unix]
  for: [png]
  crit: component
  conf: 0.65

traits:
  - id: low-edge-density
    desc: Low image edge density metric
    size_min: 8192
    if:
      type: metrics
      field: image.edge_density
      max: 0.02
