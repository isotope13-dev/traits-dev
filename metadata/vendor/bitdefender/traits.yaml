defaults:
  platforms: [windows]
  for: [pe, dll]
  crit: baseline
  conf: 0.9
traits:
  - id: version-resource-product
    desc: Bitdefender product version resource
    if:
      type: encoded
      substr: Bitdefender
      encoding: wide
