defaults:
  platforms:
    - windows
  for:
    - pe
    - dll
traits:
  - id: logapi-pipe-marker
    desc: LogAPI pipe marker
    crit: component
    conf: 0.95
    if:
      type: text
      substr: '\\.\pipe\LogAPI'
