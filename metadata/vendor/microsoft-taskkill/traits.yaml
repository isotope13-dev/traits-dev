defaults:
  platforms: [windows]
  for: [pe]
  crit: baseline
  conf: 0.95
traits:
  - id: taskkill-marker
    desc: Microsoft TaskKill utility marker
    if:
      type: text
      regex: "Microsoft\\.Windows\\.CmdLine\\.TaskKill"
      section: .rsrc
