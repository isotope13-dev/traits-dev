defaults:
  platforms: [windows]
  for: [pe]
  crit: baseline
  conf: 0.95
traits:
  - id: taskkill-marker
    desc: Microsoft TaskKill utility marker
    if:
      type: string_value
      regex: "Microsoft\\.Windows\\.CmdLine\\.TaskKill"
      section: .rsrc
