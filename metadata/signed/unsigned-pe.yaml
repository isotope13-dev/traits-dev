defaults:
  for: [pe, dll]
  platforms: [windows]
traits:
  - id: unsigned-pe-executable
    desc: PE executable is unsigned
    crit: notable
    conf: 0.8
    if:
      type: metrics
      field: pe.has_signature
      max: 0
