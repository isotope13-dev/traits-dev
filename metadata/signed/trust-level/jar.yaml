defaults:
  platforms: [unix, windows]
  for: [jar]
  crit: notable
  conf: 0.95

traits:
  - id: jar-signature-artifacts
    desc: JAR contains signature block artifacts
    if:
      type: metrics
      field: jar.signature_count
      min: 1
