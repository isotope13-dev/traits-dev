defaults:
  platforms: [windows]
  for: [pe, dll]

traits:
  - id: pe-signer-oracle-america
    desc: PE signer is Oracle America
    crit: notable
    conf: 0.99
    if:
      type: value
      path: pe.signatures[0].subject
      regex: '(?i)Oracle America'
