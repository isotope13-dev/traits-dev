defaults:
  platforms: [windows]
  for: [pe]

traits:
  - id: pe-signer-ly-corporation
    desc: LY Corporation Authenticode signer
    crit: notable
    conf: 0.99
    if:
      type: value
      path: pe.signatures[0].subject
      regex: '(?i)CN=LY Corporation,O=LY Corporation'
