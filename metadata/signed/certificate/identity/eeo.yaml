defaults:
  platforms: [windows]
  for: [pe, dll]
  crit: component
  conf: 0.98

traits:
  - id: eeo-signed-publisher
    desc: EEO verified code-signing publisher
    if:
      type: value
      path: pe.signatures[*].subject
      regex: '(?i)Beijing EEO Education Technology Co'
