defaults:
  platforms: [windows]
  for: [pe, dll]

traits:
  - id: symantec-timestamp-signer-overlay-text
    desc: Symantec timestamp signer text in PE overlay
    crit: notable
    conf: 0.84
    if:
      type: text
      substr: Symantec Time Stamping Services Signer
      section: overlay
