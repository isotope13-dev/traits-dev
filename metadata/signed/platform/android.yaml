defaults:
  for: [archives]
  platforms: [android]
  crit: component
  conf: 0.9

traits:
  - id: android-debug-testkey-signature
    desc: Android debug testkey signature
    if:
      type: text
      substr: "META-INF/TESTKEY.SF"
