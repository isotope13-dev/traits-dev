defaults:
  for: [package.json]
  platforms: [linux, macos, windows, unix, android, ios]
  crit: baseline
  conf: 0.99

traits:
  - id: serverless-sf-core-installer
    desc: Serverless sf installer package
    if:
      type: kv
      path: "$.name"
      exact: "@serverlessinc/sf-core-installer"
