defaults:
  platforms: [windows]
  for: [pe, dll]
  crit: component
  conf: 0.95
traits:
  - id: webview2loader-basename
    desc: Microsoft WebView2 loader DLL name
    if:
      type: path
      basename: true
      exact: WebView2Loader.dll
