defaults:
  size_max: 52428800
  platforms: [windows]
  for: [pe, dll]
  crit: baseline
  conf: 0.9
traits:
  - id: bitdefender-product-marker
    desc: Bitdefender product marker in version resource
    if:
      type: encoded
      substr: Bitdefender
      encoding: wide
