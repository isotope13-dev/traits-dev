defaults:
  platforms: [windows, unix]
  for: [pe, dll]
  crit: baseline
  conf: 0.97

traits:
  - id: yara-test-corpus-path
    desc: YARA test corpus binary path
    if:
      type: path
      regex: 'yara/tests/'
