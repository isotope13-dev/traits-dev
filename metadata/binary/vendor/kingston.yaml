defaults:
  platforms: [windows]
  for: [pe, dll]

traits:
  - id: kingston-versioninfo
    desc: Claims Kingston in PE version information
    crit: component
    conf: 0.98
    if:
      type: value
      path: pe.version.company
      regex: '(?i)^Kingston Technology Company,? Inc\.?$'
