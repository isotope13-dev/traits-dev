defaults:
  platforms: [windows]
  for: [pe, dll]

traits:
  - id: cisco-versioninfo
    desc: Claims Cisco Systems in PE version information
    crit: component
    conf: 0.98
    if:
      type: value
      path: pe.version.company
      regex: '(?i)^Cisco Systems,? Inc\.?$'
