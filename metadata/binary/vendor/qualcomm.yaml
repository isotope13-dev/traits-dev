defaults:
  platforms: [linux]
  for: [elf]
  crit: baseline

traits:
  - id: qcom-adsp-dtbs-basename
    desc: Qualcomm ADSP device-tree bundle basename
    crit: baseline
    conf: 0.99
    if:
      type: basename
      exact: adsp_dtbs.elf
