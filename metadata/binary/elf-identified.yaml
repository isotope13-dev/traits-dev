# Binary Format Checked
# Checks if binary format is identified
defaults:
  platforms: [unix, android, ios]
  crit: baseline
  conf: 1.0
traits:
  - id: binary-format-checked
    desc: Binary format is identified
    for: [elf]
    if:
      type: structure
      feature: "binary/format/elf"
