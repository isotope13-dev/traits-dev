defaults:
  platforms: [windows]
  for: [pe, dll]
traits:
  - id: imports-advapi32-dll
    desc: Binary imports ADVAPI32.dll
    crit: component
    conf: 0.95
    if:
      type: text
      substr: "ADVAPI32.dll"
      case_insensitive: true
      section: rdata
