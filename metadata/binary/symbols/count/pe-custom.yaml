# PE section measurements.
defaults:
  platforms: [windows]
  for: [pe, dll]
  crit: suspicious
traits:
- id: high-import-count-pe
  desc: High PE import count
  crit: baseline
  conf: 0.84
  if:
    type: metrics
    field: imports.count
    min: 150
