defaults:
  platforms: [windows]
  for: [pe]
traits:
  - id: shelloject-createpaint-export
    desc: Suspicious CreatePaint export
    crit: suspicious
    conf: 0.95
    if:
      type: symbol
      exact: "CreatePaint"
