defaults:
  platforms: [windows, unix]
  for: [pe, elf, macho]

traits:
  - id: bare-runtime-addon-export
    desc: Bare runtime native addon export
    crit: baseline
    conf: 0.98
    if:
      type: symbol
      regex: '^_?bare_(get_module_name|register_module)_v0$'
