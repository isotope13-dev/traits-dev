defaults:
  platforms: [linux, unix, android, macos, ios, windows]
  for: [source]
traits:
  - id: prismtool-pdb
    desc: Mentions PrismTool PDB path
    crit: notable
    if:
      type: string
      regex: '(?i)\\PrismTool\.pdb$'
      section: .text
