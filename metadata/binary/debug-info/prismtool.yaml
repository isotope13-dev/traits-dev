defaults:
  for: [elf, macho, pe, so, dylib, dll, shell]
traits:
  - id: prismtool-pdb
    desc: Mentions PrismTool PDB path
    crit: notable
    if:
      type: string
      regex: '(?i)\\PrismTool\.pdb$'
      section: .text
