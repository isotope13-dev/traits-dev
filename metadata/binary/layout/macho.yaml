defaults:
  platforms: [macos]
  for: [macho]
traits:
  - id: macho-object-file
    desc: Mach-O relocatable object file (MH_OBJECT)
    crit: baseline
    if:
      type: metrics
      field: macho.file_type
      min: 1
      max: 1
