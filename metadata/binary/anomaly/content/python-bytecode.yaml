# Python bytecode (.pyc) format anomalies
defaults:
  platforms: [windows, unix]
  for: [pyc]
traits:
  - id: pyc-zeroed-metadata
    desc: Python bytecode with zeroed timestamp and size
    crit: suspicious
    conf: 0.9
    if:
      type: hex
      pattern: "?? 0D 0D 0A 00 00 00 00 00 00 00 00 00 00 00 00"
      offset: 0
