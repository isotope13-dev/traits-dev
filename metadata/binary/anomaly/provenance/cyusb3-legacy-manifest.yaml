defaults:
  platforms: [windows]
  for: [pe]

traits:
  - id: cyusb3-legacy-manifest-identifier
    desc: CyUSB3 legacy-manifest identifier
    crit: notable
    conf: 0.99
    if:
      type: text
      exact: cyusb3.legacy.manifest.v1
      section: .rdata
