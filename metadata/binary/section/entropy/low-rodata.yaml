defaults:
  platforms: [linux, macos, windows]
  for: [elf, macho, pe]

traits:
  - id: large-low-entropy-rodata-section
    desc: Large low-entropy read-only data section
    crit: notable
    conf: 0.95
    if:
      type: section
      exact: .rodata
      length_min: 1048576
      entropy_max: 5.5
