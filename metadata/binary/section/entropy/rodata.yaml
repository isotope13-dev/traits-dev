defaults:
  platforms: [unix]
  for: [elf, macho]

traits:
  - id: elevated-rodata-entropy
    desc: Elevated-entropy read-only data section
    crit: baseline
    conf: 0.8
    if:
      type: section
      exact: rodata
      entropy_min: 5.5
      entropy_max: 7.5
