defaults:
  platforms: [windows]
  for: [pe, dll]

traits:
- id: high-entropy-rwx-data-section
  desc: High-entropy executable writable .data section
  crit: notable
  conf: 0.95
  if:
    type: section
    exact: '.data'
    length_min: 4096
    entropy_min: 7.5
    readable: true
    writable: true
    executable: true
