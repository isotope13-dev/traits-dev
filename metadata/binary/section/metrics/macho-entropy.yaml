defaults:
  platforms: [macos]
  for: [macho]
traits:
  - id: high-entropy-const-region
    desc: High entropy const payload region
    crit: component
    conf: 0.9
    if:
      type: section
      regex: __const$
      entropy_min: 7.5
