# Section-table size.
defaults:
  platforms: [windows, unix]
  for: [binaries]
traits:
  - id: many-sections-20
    desc: Binary has 20+ sections (installer/packer)
    crit: baseline
    if:
      type: metrics
      field: sections.count
      min: 20
