defaults:
  platforms: [windows]
  for: [pe, dll]

traits:
- id: inflated-reloc-section
  desc: Reloc virtual size exceeds file size
  crit: component
  conf: 0.9
  if:
    type: value
    path: pe.inflated_sections[*]
    exact: .reloc
