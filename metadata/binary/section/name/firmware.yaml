defaults:
  platforms: [unix, windows]
  for: [elf]
traits:
  - id: fwimage-section
    desc: Firmware image ELF section
    crit: baseline
    conf: 0.9
    if:
      type: section
      exact: .fwimage
