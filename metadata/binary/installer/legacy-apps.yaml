defaults:
  platforms: [windows]
  crit: baseline
  conf: 0.85
  for: [binaries]
traits:
  - id: registry-path-compuserve
    desc: Legacy Compuserve registry path
    if:
      type: text
      regex: (?i)Software\\Compuserve
      section: .text
