defaults:
  platforms: [windows]
  for: [pe]
  crit: baseline
  conf: 0.96

traits:
  - id: freearc-sfx
    desc: FreeArc self-extracting archive
    if:
      type: text
      substr: FreeArc SFX
      section: .rsrc
