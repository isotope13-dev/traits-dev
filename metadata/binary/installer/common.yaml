defaults:
  platforms: [windows]
  for: [pe]
composite_rules:
  - id: known-benign-installer
    desc: Known benign installer framework
    crit: component
    conf: 0.95
    any:
      - id: metadata/binary/installer::wix-burn-bootstrapper
      - id: metadata/binary/installer::nsis
      - id: metadata/binary/installer::inno-setup
      - id: metadata/binary/installer::7zip-sfx-installer
      - id: metadata/binary/installer::winrar-sfx
      - id: metadata/binary/installer::iexpress
