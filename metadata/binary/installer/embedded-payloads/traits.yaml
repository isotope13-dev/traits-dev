defaults:
  platforms: [windows]
  for: [oledoc]
  crit: notable
  conf: 0.96

traits:
  - id: embedded-native-payload-count
    desc: MSI embeds two native PE payloads
    if:
      type: metrics
      field: binary.embedded_binary_count
      min: 2
