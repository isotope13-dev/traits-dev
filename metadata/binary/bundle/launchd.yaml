defaults:
  crit: baseline
  platforms:
    - macos
  for:
    - macho
    - dylib
traits:
  - id: launchd-plist
    desc: launchd plist embedding
    conf: 0.85
    if:
      type: text
      substr: launchd.plist
      section: .text
