defaults:
  crit: baseline
  platforms:
    - macos
  for:
    - macho
traits:
  - id: xpc-service
    desc: Apple XPC service bundle path
    conf: 0.9
    if:
      type: text
      regex: \.xpc/Contents/MacOS/
      section: .text
