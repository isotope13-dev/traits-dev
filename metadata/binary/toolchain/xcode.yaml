defaults:
  crit: baseline
  platforms:
    - macos
  for:
    - macho
    - dylib
traits:
  - id: build-info-xcode
    desc: Apple Build Information (DTPlatformBuild/DTXcode)
    conf: 1.0
    if:
      type: raw
      regex: (DTPlatformBuild|DTXcode)
      section: .text
