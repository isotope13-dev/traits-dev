defaults:
  platforms: [windows, unix]
  for: [binaries]
traits:
  - id: prismtool-pdb
    desc: Mentions PrismTool PDB path
    crit: notable
    if:
      type: text
      regex: (?i)\\PrismTool\.pdb$
      section: .text
