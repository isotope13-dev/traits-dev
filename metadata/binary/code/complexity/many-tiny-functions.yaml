defaults:
  platforms: [linux, windows, macos]
  for: [elf, macho, pe]
traits:
  - id: many-tiny-functions
    desc: 500+ functions contain one basic block
    crit: notable
    conf: 0.4
    if:
      type: metrics
      field: binary.tiny_func_count
      min: 500
