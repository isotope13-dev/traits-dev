defaults:
  platforms: [unix, windows]
  for: [binaries]
traits:
  - id: statically-linked
    desc: statically linked string
    crit: notable
    conf: 0.8
    if:
      type: text
      substr: statically linked
      section: .text
