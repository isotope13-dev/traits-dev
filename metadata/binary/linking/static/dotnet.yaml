defaults:
  for: [pe, dll]
  platforms: [windows]

traits:
  - id: ilrepack-list-resource
    desc: ILRepack merged-assembly manifest
    crit: baseline
    conf: 0.99
    if:
      type: text
      exact: ILRepack.List
      section: .text
