defaults:
  platforms: [unix]
  for: [elf, macho]
  crit: baseline
  conf: 0.95

traits:
  - id: musl-loader-basename
    desc: musl dynamic loader basename
    if:
      type: path
      basename: true
      regex: '^ld-musl-.{1,32}\.so(\.[0-9]{1,3})?$'
