defaults:
  platforms:
    - windows
    - unix
  for:
    - binaries
traits:
  - id: cosmopolitan-ape-runtime
    desc: Cosmopolitan APE runtime marker
    crit: baseline
    conf: 0.98
    if:
      type: text
      substr: "actually portable executable loader version"
