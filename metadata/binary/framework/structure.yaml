defaults:
  platforms: [linux, macos, windows, unix]
  for: [binaries]
traits:
  - id: is-binary
    desc: File is a compiled binary
    crit: component
    if:
      type: structure
      feature: binary
