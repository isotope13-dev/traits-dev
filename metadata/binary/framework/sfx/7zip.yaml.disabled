
defaults:
  for: [pe]
  platforms: [windows]
traits:
  - id: 7zip-sfx-strings
    desc: 7-Zip SFX string cluster
    crit: baseline
    conf: 0.92
    if:
      type: string_value
      regex: (?i)(7z|lzma|self-extracting)
      section: .rdata
