defaults:
  for: [pe]
  platforms: [windows]
  crit: baseline
  conf: 0.95

traits:
  - id: windows-bits
    desc: Windows BITS service binary marker
    if:
      type: text
      substr: Background Intelligent Transfer Service
      section: rdata
