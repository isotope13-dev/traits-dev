defaults:
  for: [pe]
  platforms: [windows]
  crit: baseline
  conf: 0.95

traits:
  - id: windows-media-player
    desc: Windows Media Player binary marker
    if:
      type: text
      substr: Microsoft Windows Media Player
      section: rdata
