defaults:
  platforms: [windows]
  for: [pe, dll]
  crit: component
  conf: 0.7

traits:
  - id: uefi-driver-ntfs-debug-path
    desc: UEFI NTFS driver debug/source path
    if:
      type: text
      substr: "uefi-driver/uefi_driver/DEBUG/ntfs.dll"
      section: rdata
