defaults:
  platforms: [windows]
  for: [pe, dll]
traits:
  - id: html-content-in-resource
    desc: HTML content type in PE resource
    crit: notable
    conf: 0.7
    if:
      type: text
      substr: "Content-Type: text/html"
      section: .rsrc
      case_insensitive: true
