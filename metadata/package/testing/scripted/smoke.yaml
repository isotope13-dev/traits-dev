defaults:
  platforms: [windows, unix]
  for: [batch, shell]
  crit: notable
  conf: 0.9
traits:
  - id: endtoend-smoke-download-recipe
    desc: End-to-end download smoke test
    if:
      type: text
      substr: endtoend-smoke-download
