defaults:
  platforms: [unix, windows]

traits:
- id: webkit-test-runner-source
  desc: WebKit TestRunnerShared source
  crit: baseline
  conf: 0.98
  for: [c, cpp]
  if:
    type: path
    dirname: true
    regex: '(^|[\\/])TestRunnerShared([\\/]|$)'
    case_insensitive: true
