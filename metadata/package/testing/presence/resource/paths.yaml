defaults:
  platforms: [windows, unix]
  for: [pdf]

traits:
- id: pdf-test-resources-directory-path
  desc: PDF file in test resources tree
  crit: notable
  conf: 0.98
  if:
    type: path
    dirname: true
    regex: 'tests?[\\/]resources$'
    case_insensitive: true
