defaults:
  platforms: [unix, windows]
  for: [javascript, typescript]

traits:
  - id: jest-config-basename
    desc: Jest test configuration file
    crit: baseline
    conf: 0.99
    if:
      type: basename
      regex: '^jest\.config\.[cm]?[jt]s$'
