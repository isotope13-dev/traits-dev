defaults:
  platforms: [unix]
  for: [shell]
traits:
  - id: qemu-examples-tests
    desc: QEMU examples test script
    crit: baseline
    conf: 0.9
    if:
      type: text
      substr: QEMU examples & tests.
