defaults:
  platforms: [windows, unix]
  for: [rust]

traits:
  - id: boringssl-bogo-test-shim
    desc: BoringSSL bogo TLS test shim
    crit: notable
    conf: 0.99
    if:
      type: text
      substr: "This is a test shim for the BoringSSL-Go"
