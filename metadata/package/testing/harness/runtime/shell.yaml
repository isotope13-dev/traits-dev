defaults:
  platforms: [unix]
  for: [shell]

traits:
  - id: cross-os-dogfood-harness
    desc: Cross-platform dogfood validation harness
    crit: notable
    conf: 0.98
    if:
      type: text
      substr: "Deep cross-OS dogfooding"
