defaults:
  platforms: [windows, unix]
  for: [python]

traits:
  - id: adversarial-benchmark-corpus
    desc: Adversarial benchmark corpus marker
    crit: baseline
    conf: 0.9
    if:
      type: text
      substr: "Built-in adversarial benchmark suite"
