defaults:
  platforms: [unix, windows]
  for: [javascript]
  crit: baseline
  conf: 0.98

traits:
  - id: jsimplifier-benignjs-corpus-path
    desc: JSimplifier BenignJS corpus path
    if:
      type: path
      regex: '(?i)(^|[\\/])BenignJS[\\/]'
