defaults:
  platforms: [unix, windows]
  for: [pkginfo]

traits:
  - id: css-selector-parser-claim
    desc: Registry description claims CSS selector parser
    crit: baseline
    conf: 0.95
    if:
      type: text
      regex: '"description"\s*:\s*"[^"]{0,80}CSS selector parser[^"]{0,120}"'
      case_insensitive: true
