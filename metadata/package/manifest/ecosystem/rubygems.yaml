defaults:
  platforms: [unix, windows]
  for: [pkginfo]

traits:
  - id: rubygems-spec-yaml-marker
    desc: RubyGems specification YAML marker
    crit: baseline
    conf: 0.98
    if:
      type: text
      substr: '--- !ruby/object:Gem::Specification'
