defaults:
  platforms: [unix, windows]
  for: [pkginfo]

traits:
  - id: selparsecss-selector-name
    desc: Package is named selparsecss-selector
    crit: baseline
    conf: 1.0
    if:
      type: text
      regex: '"name"\s*:\s*"selparsecss-selector"'
