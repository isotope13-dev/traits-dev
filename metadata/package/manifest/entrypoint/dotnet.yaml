defaults:
  platforms: [windows, linux, macos]
  for: [xml]

traits:
  - id: dotnet-tool-command-manifest
    desc: NuGet .NET tool command manifest
    crit: notable
    conf: 0.95
    if:
      type: text
      regex: '<DotNetCliTool\b|<Command\b.{0,240}\bEntryPoint="[^"]{1,160}"'
