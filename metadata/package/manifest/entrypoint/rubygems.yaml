defaults:
  platforms: [unix, windows]
  for: [pkginfo]

traits:
  - id: rubygems-bindir-bin
    desc: RubyGems bindir is bin
    crit: component
    conf: 0.9
    if:
      type: value
      path: bindir
      exact: bin
