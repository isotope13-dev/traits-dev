defaults:
  platforms: [linux, macos, windows]
  for: [package.json]

traits:
  - id: publishes-all-files
    desc: npm files field includes every package file
    crit: notable
    conf: 0.95
    if:
      type: value
      path: files[*]
      exact: '*'
