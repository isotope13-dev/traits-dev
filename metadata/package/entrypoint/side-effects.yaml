---
defaults:
  platforms:
  - linux
  - macos
  - windows
  for:
  - package.json
traits:
- id: has-side-effects
  desc: Package declares sideEffects metadata
  crit: notable
  conf: 0.85
  if:
    type: text
    regex: '"sideEffects":\s*(false|\[)'
