defaults:
  platforms: [unix, windows]
  for: [groovy]

traits:
  - id: gradle-jnativehook-dependency
    desc: Gradle depends on jnativehook
    crit: notable
    conf: 0.9
    if:
      type: text
      regex: "['\"]com\\.github\\.kwhat:jnativehook:"
