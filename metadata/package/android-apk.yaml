defaults:
  for: [archives]
  platforms: [android]
  crit: component
  conf: 0.86

traits:
  - id: apk-secondary-dex
    desc: APK contains secondary DEX
    if:
      type: raw
      substr: "classes2.dex"
