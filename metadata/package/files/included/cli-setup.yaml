defaults:
  platforms: [unix, windows]
  for: [python]
  crit: notable
  conf: 0.95

traits:
  - id: nested-cli-setup-py
    desc: Nested CLI module named setup.py
    if:
      type: path
      regex: '(^|/)(commands|cli|interface|scripts|tools|bin)/setup\.py$'
