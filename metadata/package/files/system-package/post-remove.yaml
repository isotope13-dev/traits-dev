defaults:
  platforms: [linux]
  for: [shell]

traits:
  - id: package-post-remove-script
    desc: Package post-remove script filename
    crit: baseline
    conf: 0.95
    if:
      type: path
      basename: true
      regex: '^(post-remove|postrm)(\.sh)?$'
