defaults:
  platforms: [unix, windows]
  for: [text]

traits:
  - id: large-b64-file
    desc: Large .b64 file
    crit: notable
    conf: 0.95
    size_min: 4096
    if:
      type: path
      basename: true
      regex: '^[^/]{1,120}\.b64$'
