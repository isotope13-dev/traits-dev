defaults:
  platforms: [unix, windows]
  for: [manifests, scripts]

traits:
  - id: setup-py-file
    for: [text]
    desc: Compatibility alias
    crit: component
    conf: 1.0
    if:
      type: string_literal
      exact: __cleave_missing_setup_py_file__
