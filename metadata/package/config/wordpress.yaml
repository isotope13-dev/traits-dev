defaults:
  platforms: [unix, windows]
  for: [php]
  crit: baseline
  conf: 0.9

traits:
  - id: wordpress-development-config-file
    desc: WordPress development config file
    if:
      type: text
      regex: '(?i)(DEVELOP|local development|config-develop\.php|Allowed values:\s*develop)'
