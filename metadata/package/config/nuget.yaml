defaults:
  platforms: [windows]
  for: [xml, data]
  crit: component
  conf: 0.98

traits:
  - id: nuspec-file
    desc: NuGet nuspec manifest file
    if:
      type: path
      basename: true
      regex: '\.nuspec$'
      case_insensitive: true
