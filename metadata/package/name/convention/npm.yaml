defaults:
  platforms: [unix, windows]
  for: [package.json]

traits:
  - id: npm-has-name
    desc: npm manifest declares a package name
    crit: baseline
    conf: 1.0
    if:
      type: value
      path: name
      exists: true
