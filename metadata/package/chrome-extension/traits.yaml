defaults:
  platforms: [linux, macos, windows]
  for: [chrome-manifest]
traits:
  - id: host-all-urls
    desc: Extension has access to ALL websites
    crit: notable
    conf: 0.95
    if:
      type: value
      path: permissions
      substr: "<all_urls>"
