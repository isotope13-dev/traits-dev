---
defaults:
  platforms:
  - linux
  - macos
  - windows
  for:
  - package.json
traits:
- id: has-files-field
  desc: Package explicitly lists published files
  crit: notable
  conf: 0.8
  if:
    type: value
    path: files
  not:
  - substr: inflight.js
  - substr: '"tabby-builtin-plugin"'
  - substr: '"name": "inflight"'
  unless:
  - type: value
    path: devDependencies
    exists: true
  - id: metadata/package/fields/freeform::official-react-native-package
  - type: value
    path: name
    regex: "^(node-pty|native-process-working-directory)$"
  - type: value
    path: keywords
    regex: tabby-builtin-plugin
- id: files-dist-ref
  desc: Package files references dist directory
  crit: baseline
  conf: 0.85
  if:
    type: value
    path: "files[*]"
    regex: "^(\\./)?dist(/|$)"
