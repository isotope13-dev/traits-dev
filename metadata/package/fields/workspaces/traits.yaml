---
defaults:
  platforms:
  - linux
  - macos
  - windows
  for:
  - package.json
traits:
- id: has-workspaces
  desc: Package uses workspaces (monorepo pattern)
  crit: notable
  conf: 0.9
  if:
    type: kv
    path: workspaces
    exists: true
