defaults:
  platforms: [unix, windows]
  for: [package.json]

traits:
  - id: vscode-extension
    desc: Package is a VSCode extension
    crit: baseline
    conf: 0.95
    if:
      type: value
      path: engines.vscode
