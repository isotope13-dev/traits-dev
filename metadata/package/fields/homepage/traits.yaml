---
defaults:
  platforms:
  - linux
  - macos
  - windows
  for:
  - package.json
traits:
- id: has-homepage-field
  desc: Package has homepage URL
  crit: baseline
  conf: 0.8
  if:
    type: kv
    path: homepage
    regex: "^https?://"
