---
defaults:
  platforms:
  - linux
  - macos
  - windows
  for:
  - package.json
traits:
- id: has-publish-config
  desc: Has publish configuration
  crit: notable
  conf: 0.85
  unless:
  - type: kv
    path: devDependencies
    exists: true
  - type: kv
    path: repository.url
    regex: github\.com/
  if:
    type: kv
    path: publishConfig
    exists: true
