defaults:
  platforms: [unix, windows]
  for: [python]
traits:
  - id: metadata-packages
    desc: packages= metadata field
    crit: component
    conf: 0.5
    if:
      type: string_literal
      regex: packages\s*=
