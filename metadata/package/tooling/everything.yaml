traits:
  - id: everything-binary
    desc: Everything search tool binary
    crit: notable
    platforms: [windows]
    for: [pe]
    if:
      type: text
      substr: www.voidtools.com
      section: .rdata
