traits:
  - id: azure-agentbaker-tree
    desc: Azure agentbaker node image tree
    platforms: [unix, windows]
    for: [shell, powershell, json]
    if:
      type: path
      regex: '(?i)agentbaker@'
