traits:
  - id: vm-image-provisioner-script
    desc: VM image provisioner script
    platforms: [unix]
    for: [shell, ruby]
    if:
      type: path
      regex: '(?i)(vagrant|packer)[\\/]provisioners[\\/]'
