defaults:
  platforms: [windows]
  for: [pe]
  crit: baseline
  conf: 0.95

traits:
  - id: frida-carved-runtime-artifact
    desc: Frida carved runtime artifact
    platforms: [windows, unix]
    for: [elf, javascript]
    size_min: 1000
    size_max: 50000000
    if:
      type: path
      substr: frida-server-
