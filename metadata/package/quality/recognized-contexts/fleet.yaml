defaults:
  platforms: [macos]

traits:
- id: fleet-conditional-access-profile-marker
  desc: Fleet conditional-access profile marker
  crit: component
  conf: 0.95
  for: [plist, xml]
  if:
    type: text
    regex: 'Fleet conditional access|com\.fleetdm\.conditional-access'
