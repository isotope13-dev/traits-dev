defaults:
  platforms: [linux, windows, macos]
  for: [javascript, typescript]

traits:
  - id: python-runtime-bootstrap-source
    desc: Python standalone runtime bootstrap source
    crit: notable
    conf: 0.92
    if:
      type: text
      regex: 'python-build-standalone/releases/download[\s\S]{0,300}install_only\.tar\.gz'
