defaults:
  platforms: [unix, macos]
  for: [shell]

composite_rules:
  - id: recognized-shell-cleanup-context
    desc: Recognized benign shell cleanup context
    crit: exception
    conf: 0.98
    any:
      - id: metadata/package/quality/recognized-contexts-extra::macos-app-updater-context
      - id: metadata/package/quality/recognized-contexts-extra::user-setup-context
