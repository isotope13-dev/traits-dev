---
defaults:
  platforms:
  - linux
  - macos
  - windows
  for:
  - package.json
traits:
- id: has-keywords
  desc: Package has keywords array
  crit: baseline
  conf: 0.75
  if:
    type: value
    path: keywords
  unless:
  - id: metadata/package/manifest/freeform::official-react-native-package
