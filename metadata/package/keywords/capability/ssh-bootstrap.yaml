defaults:
  platforms: [unix, windows]
  for: [pkginfo]

traits:
  - id: pkginfo-rot13-ssh-key-doc
    desc: Package documents ROT13 SSH key bootstrap
    crit: notable
    conf: 0.94
    if:
      type: text
      regex: 'ROT13.{0,40}SSH key'
