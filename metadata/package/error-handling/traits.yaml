defaults:
  platforms: [windows, unix]
traits:
  - id: go-err-nil-check
    desc: Go error nil check pattern
    crit: baseline
    conf: 0.6
    for:
      - go
    if:
      type: string_literal
      substr: if err != nil
  - id: go-errors-new
    desc: Go errors.New function
    crit: baseline
    conf: 0.6
    for:
      - go
    if:
      type: string_literal
      substr: errors.New
  - id: go-fmt-errorf
    desc: Go fmt.Errorf function
    crit: baseline
    conf: 0.6
    for:
      - go
    if:
      type: string_literal
      substr: fmt.Errorf
  - id: rust-result-type
    desc: Rust Result<T,E> type
    crit: baseline
    conf: 0.6
    for:
      - rust
    if:
      type: string_literal
      substr: Result<
  - id: rust-unwrap-or
    desc: Rust unwrap_or method
    crit: baseline
    conf: 0.6
    for:
      - rust
    if:
      type: string_literal
      substr: unwrap_or
  - id: try-block-open
    desc: Contains the opening of a try block
    crit: baseline
    conf: 0.5
    for: [scripts, binaries]
    if:
      type: string_literal
      substr: try {
  - id: catch-keyword
    desc: Contains a catch error handler
    crit: baseline
    conf: 0.5
    for: [scripts, binaries]
    if:
      type: string_literal
      word: catch
  - id: except-keyword
    desc: Contains a Python except handler
    crit: baseline
    conf: 0.5
    for:
      - python
    if:
      type: string_literal
      word: except
  - id: finally-keyword
    desc: Contains a finally cleanup handler
    crit: baseline
    conf: 0.5
    for: [scripts, binaries]
    if:
      type: string_literal
      word: finally
