defaults:
  platforms: [windows, unix]
  for: [package.json]
  crit: baseline
  conf: 0.85

traits:
  - id: npm-version-2-0-0
    desc: NPM package has version 2.0.0
    crit: baseline
    if:
      type: value
      path: version
      exact: 2.0.0
