defaults:
  platforms: [windows]
  for: [powershell]

traits:
  - id: powershell-comment-help-installer-example
    desc: PowerShell comment-help installer example
    crit: notable
    conf: 0.9
    if:
      type: text
      regex: '(?is)\.SYNOPSIS\b.{0,999}\.(EXAMPLE|USAGE)\b'
