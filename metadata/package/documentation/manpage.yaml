defaults:
  platforms: [unix]
  for: [javascript, typescript]
traits:
  - id: unix-manpage
    desc: Unix manual page or bundled manpage example
    crit: baseline
    conf: 0.9
    if:
      type: path
      regex: '(^|[\\/])usr/share/man[\\/]|(?i)\.[1-9][a-z0-9]{0,8}(\.gz)?$'
