defaults:
  for: [elf, macho, pe, so, dylib, dll, shell]
traits:
  - id: codegen-script
    desc: Swagger Codegen git push script
    crit: baseline
    if:
      type: raw
      substr: "Generated by: https://github.com/swagger-api/swagger-codegen.git"
      section: .text
