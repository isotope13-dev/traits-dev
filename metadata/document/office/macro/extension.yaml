defaults:
  for: [ooxml]
  platforms: [windows]

traits:
  - id: macro-enabled-extension
    desc: Office macro-enabled filename extension
    crit: baseline
    conf: 0.99
    if:
      type: metrics
      field: office.is_macro_enabled_extension
      min: 1
