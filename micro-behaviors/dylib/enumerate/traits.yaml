defaults:
  platforms:
    - linux
    - unix
    - android
traits:
  - id: dl-iterate-phdr
    desc: Iterate shared object list
    crit: baseline
    conf: 0.5
    for:
      - elf
    if:
      type: string_value
      substr: dl_iterate_phdr
