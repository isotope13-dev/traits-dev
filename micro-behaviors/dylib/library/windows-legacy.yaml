defaults:
  for: [pe, dll]
  platforms: [windows]
traits:
  - id: mfc42-ref
    desc: Links legacy MFC42 runtime
    crit: baseline
    conf: 0.85
    if:
      type: string_value
      exact: "MFC42.DLL"
