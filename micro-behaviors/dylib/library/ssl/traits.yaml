defaults:
  crit: baseline
  platforms:
    - unix
    - windows
  for: [scripts, binaries]
traits:
  - id: libssl-ref
    desc: libssl library reference
    conf: 0.3
    if:
      type: text
      substr: libssl
  - id: libcrypto-ref
    desc: libcrypto library reference
    conf: 0.3
    if:
      type: text
      substr: libcrypto
  - id: nss3-word
    desc: nss3 library reference
    conf: 0.75
    if:
      type: text
      word: nss3
  - id: libnss3-ref
    desc: libnss3 library reference
    conf: 0.75
    if:
      type: text
      substr: libnss3
