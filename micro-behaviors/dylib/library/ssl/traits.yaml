defaults:
  platforms:
    - unix
    - windows
    - macos
  for: [scripts, binaries]
traits:
  - id: libssl-ref
    desc: libssl library reference
    crit: baseline
    conf: 0.3
    if:
      type: text
      substr: libssl
  - id: libcrypto-ref
    desc: libcrypto library reference
    crit: baseline
    conf: 0.3
    if:
      type: text
      substr: libcrypto
