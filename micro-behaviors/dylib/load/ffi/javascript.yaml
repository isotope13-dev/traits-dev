defaults:
  platforms: [windows, unix]
  for: [javascript, typescript]
  attack: T1106

traits:
  - id: node-ffi-rs-binding
    desc: Node.js loads the ffi-rs native binding
    crit: notable
    conf: 0.96
    if:
      type: text
      regex: '(?i)require\s*\(\s*[''"'']ffi-rs[''"'']\s*\)|from\s+[''"'']ffi-rs[''"'']'
