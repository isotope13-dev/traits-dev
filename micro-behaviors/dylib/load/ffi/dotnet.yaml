defaults:
  platforms: [windows]
  for: [javascript, typescript, csharp, powershell, batch]
  crit: notable
  conf: 0.96

traits:
  - id: dotnet-kernel32-pinvoke
    desc: .NET kernel32 P/Invoke declaration
    if:
      type: text
      regex: 'DllImport.{0,40}kernel32\.dll'
