defaults:
  for: [python]
  platforms: [linux, macos, windows]
traits:
  - id: python-dylib-plugin-api
    desc: Python dynamic-library plugin API
    crit: notable
    conf: 0.88
    if:
      type: symbol
      kind: call
      regex: '(^|\.)((load|register)_dylib)$'
