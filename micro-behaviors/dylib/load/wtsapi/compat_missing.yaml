defaults:
  platforms: [windows]
  for: [pe]
composite_rules:
  - id: wts-session-notification-lifecycle
    desc: WTS session notification lifecycle
    crit: notable
    conf: 0.88
    any:
      - id: wts-session-notification
      - id: wts-disconnect
