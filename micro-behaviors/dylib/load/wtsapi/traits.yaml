# Windows Terminal Services (wtsapi32.dll) Imports
# Detects WTS API usage for session manipulation.
defaults:
  platforms: [windows]
  for: [pe]
  crit: baseline
traits:
  - id: wts-disconnect
    desc: "Imports WTSDisconnectSession"
    conf: 0.8
    if:
      type: symbol
      exact: "WTSDisconnectSession"
  - id: wts-session-notification
    desc: "Imports WTS session notification API"
    conf: 0.8
    if:
      type: symbol
      exact: "WTSUnRegisterSessionNotification"
  - id: wtsapi32-dll-text
    desc: wtsapi32.dll module string reference
    conf: 0.7
    if:
      type: text
      regex: '(?i)wtsapi32\.dll'
