defaults:
  platforms: [unix, windows, macos]
  for: [shell, python, javascript, powershell, pe, elf]
traits:
  - id: actionscript-debugger-check
    desc: Detects ActionScript Capabilities.isDebugger check (evasion)
    crit: notable
    conf: 0.9
    if:
      type: raw
      regex: "Capabilities\\.isDebugger"
