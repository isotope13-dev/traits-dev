defaults:
  platforms: [windows]
  for: [pe, dll]
traits:
- id: vb6-process-injection-pattern-cond-1--inline-17571
  desc: Low-import VB6 legacy binaries
  crit: component
  conf: 0.92
  if:
    type: metrics
    field: imports.count
    max: 50
  unless:
  # go-buildinfo is PE-only; add the ELF/Mach-O Go marker so low-import Go
  # binaries on any platform are excluded from this VB6-injection fragment.
  - id: metadata/lang/compiler/systems::go-buildinfo
  - id: metadata/lang/compiled/

  count_max: 1017572
