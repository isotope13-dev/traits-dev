defaults:
  platforms: [windows]
  for: [oledoc, ole]
  crit: baseline
  conf: 0.9

traits:
  - id: msi-embedded-create-process-a
    desc: MSI embedded CreateProcessA reference
    attack: T1106
    if:
      type: raw
      substr: CreateProcessA
