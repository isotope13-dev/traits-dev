defaults:
  platforms: [windows]
  for: [oledoc, ole]
  crit: baseline
  conf: 0.9
  attack: T1106

traits:
  - id: msi-embedded-create-process-a
    desc: MSI embedded CreateProcessA reference
    if:
      type: raw
      substr: CreateProcessA
  - id: msi-embedded-create-process-w
    desc: MSI embedded CreateProcessW reference
    if:
      type: raw
      substr: CreateProcessW
