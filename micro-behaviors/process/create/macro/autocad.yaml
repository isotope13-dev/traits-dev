defaults:
  for: [binaries, scripts, source, documents, manifests, data, media]
  platforms: [linux, macos, windows, unix, android, ios]
  size_max: 16384
  crit: notable
  conf: 0.9

traits:
  - id: acad-document-deactivate-trigger
    desc: AutoCAD document deactivation trigger
    if:
      type: raw
      exact: "Private Sub AcadDocument_Deactivate()"
      section: .text
