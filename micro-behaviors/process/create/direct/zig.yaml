defaults:
  platforms: [unix, openwrt]
  for: [zig]
  attack: T1059

traits:
  - id: zig-child-init
    desc: Zig std.process.Child.init usage
    crit: notable
    conf: 0.86
    if:
      type: text
      word: Child.init
