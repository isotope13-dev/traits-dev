defaults:
  platforms: [unix]
  for: [javascript, typescript]
  crit: notable

traits:
  - id: glib-command-line-spawn-call
    desc: Spawns a command-line child process
    conf: 0.94
    if:
      type: symbol
      kind: call
      regex: '(^|\.)spawn_command_line_(a)?sync$'
