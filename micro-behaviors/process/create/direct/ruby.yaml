defaults:
  platforms: [windows, unix]
  for: [ruby]

traits:
  - id: ruby-spawn
    desc: Ruby creates a child process
    crit: notable
    conf: 0.95
    if:
      type: symbol
      kind: call
      exact: spawn
