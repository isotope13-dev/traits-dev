defaults:
  platforms: [linux]
  for: [shell]
  attack: T1195.002

traits:
  - id: pkgbuild-sudo-srcdir-entry
    desc: PKGBUILD sudo-executes a source entry
    crit: notable
    conf: 0.98
    if:
      type: text
      regex: '(?m)^\s*sudo\s+["'']?\$(srcdir|\{srcdir\})/[A-Za-z0-9_.${}-]+["'']?\s*$'
