defaults:
  platforms: [windows, unix]
  for: [elf, macho, pe]

traits:
  - id: go-binary-combined-output-text
    desc: Go binary embeds the CombinedOutput symbol text
    crit: baseline
    conf: 0.84
    attack: "T1059.003"
    if:
      type: text
      substr: CombinedOutput
