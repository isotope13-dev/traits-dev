defaults:
  crit: notable
  for: [perl]
  platforms: [unix, windows]
traits:
  - id: perl-open-interpolated-pipe
    desc: Opens a pipe from an interpolated command
    conf: 0.9
    if:
      type: text
      regex: 'open\s*\([^)\n]{0,40},[^)\n]{0,80}\$[A-Za-z_]\w*[^)\n]{0,40}\|'
