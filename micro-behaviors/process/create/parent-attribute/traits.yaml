defaults:
  for: [pe, dll]
  platforms: [windows]
  crit: notable
  conf: 0.94
  attack: "T1055.012"

traits:
  - id: parent-process-attribute
    desc: Parent-process startup attribute marker
    if:
      type: text
      exact: "PROC_THREAD_ATTRIBUTE_PARENT_PROCESS"
