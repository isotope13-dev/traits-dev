defaults:
  platforms: [unix]
  for: [elf, macho]
traits:
  - id: bash-binary-name
    desc: Native binary references Bash shell name
    crit: notable
    conf: 0.75
    if:
      type: text
      exact: bash
      section: rodata
