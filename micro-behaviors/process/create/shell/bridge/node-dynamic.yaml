defaults:
  platforms: [unix, windows]
  for: [javascript, typescript]
  attack: T1059.007

traits:
  - id: node-dynamic-execsync-resolver
    desc: Node dynamic execSync resolver
    crit: notable
    conf: 0.9
    if:
      type: text
      regex: 'execSync\s*:\s*execSync|ctx\s*\[\s*name\s*\].{0,80}execSync'
