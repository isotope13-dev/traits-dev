defaults:
  platforms: [windows]
  for: [batch]
  crit: component
  conf: 0.85

traits:
- id: normalizepath-label-call
  desc: Batch helper CALL to NORMALIZEPATH label
  if:
    type: text
    substr: CALL :NORMALIZEPATH
