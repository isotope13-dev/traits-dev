defaults:
  platforms: [windows]
  for: [batch]
  attack: T1059.003

traits:
  - id: call-self-with-args
    desc: Batch CALL invokes itself with arguments
    crit: notable
    conf: 0.9
    if:
      type: text
      regex: '(?i)\bcall\s+%+0\s+[^\r\n]{1,40}'
