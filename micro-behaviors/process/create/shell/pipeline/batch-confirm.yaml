defaults:
  platforms: [windows]
  for: [batch]
  crit: notable
  conf: 0.9

traits:
  - id: batch-echo-y-pipe
    desc: Batch pipes yes into next command
    if:
      type: text
      regex: '(?i)@?echo\s+y\s*\|'
