defaults:
  platforms: [linux]
  for: [shell]

traits:
  - id: busybox-command
    desc: Shell invokes a BusyBox command
    crit: notable
    conf: 0.9
    if:
      type: text
      regex: '\bbusybox\s+\w'
