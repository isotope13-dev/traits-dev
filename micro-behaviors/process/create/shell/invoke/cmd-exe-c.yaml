defaults:
  platforms: [windows]
  for: [pe, dll, scripts, rust]
traits:
  - id: cmd-exe-c-command
    desc: Execute commands via cmd.exe /c
    crit: notable
    conf: 0.85
    if:
      type: text
      regex: '(?is)(^|["''`\s(=,:])cmd(\.exe)?\s*/c\s+\S|Command::new\("cmd"\).{0,80}"/C"'
