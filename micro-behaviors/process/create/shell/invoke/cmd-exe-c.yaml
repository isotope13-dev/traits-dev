defaults:
  platforms: [windows]
  for: [pe, dll, scripts, rust]
traits:
  - id: cmd-exe-c-command
    desc: Execute commands via cmd.exe /c
    crit: notable
    conf: 0.85
    if:
      type: text
      regex: '(?i)cmd(\.exe)?\s*/c|Command::new\s*\(\s*"cmd"\s*\)[\s\S]{0,120}args\s*\(&\[\s*"/C"'
