defaults:
  platforms: [windows]
  for: [pe, dll, scripts]
traits:
  - id: cmd-exe-c-command
    desc: Execute commands via cmd.exe /c
    crit: notable
    conf: 0.85
    if:
      type: text
      substr: "cmd.exe /c"
      case_insensitive: true
