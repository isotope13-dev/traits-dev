defaults:
  platforms: [unix, windows]
  for: [ruby]

traits:
  - id: ruby-exec-local-variable
    desc: Ruby exec() launches a local variable
    crit: notable
    conf: 0.95
    if:
      type: symbol
      kind: call
      exact: exec
      arg:
        kind: identifier
        name: bin
