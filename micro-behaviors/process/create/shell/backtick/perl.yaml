defaults:
  for: [perl]
  platforms: [unix, windows]
  attack: "T1059.006"

traits:
  - id: backticks-variable-exec
    desc: Perl backticks execute a variable
    crit: notable
    conf: 0.9
    if:
      type: text
      regex: '`\$[A-Za-z_][A-Za-z0-9_]*`'
