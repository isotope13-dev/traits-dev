defaults:
  platforms: [windows]
  for: [batch]
  attack: T1059.005
traits:
  - id: cscript-nologo
    desc: cscript launched without logo banner
    crit: notable
    conf: 0.82
    if:
      type: text
      regex: (?i)\bcscript(\.exe)?\b[^\n]{0,80}/nologo\b
