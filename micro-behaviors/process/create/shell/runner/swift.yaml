defaults:
  platforms: [macos]
  for: [swift]
  attack: T1059

traits:
  - id: swift-shell-command-exec
    desc: Swift launches /bin/sh with -c
    crit: notable
    conf: 0.9
    if:
      type: text
      regex: 'Process\(\)[\s\S]{0,180}/bin/sh[\s\S]{0,80}-c'
