defaults:
  platforms: [unix]
  for: [csharp]

traits:
  - id: dotnet-unix-shell-command
    desc: C# prepares a /bin/sh -c command
    crit: notable
    conf: 0.9
    if:
      type: text
      regex: '"/bin/sh"[\s\S]{0,180}-c\b'
