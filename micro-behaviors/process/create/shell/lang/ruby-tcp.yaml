# Ruby TCPSocket constructor — direct client socket primitive.
defaults:
  for: [ruby]
  platforms: [linux, macos, windows]

traits:
  - id: tcpsocket-new
    desc: Ruby TCPSocket.new constructor
    crit: notable
    conf: 0.95
    if:
      type: text
      regex: '\bTCPSocket\.(new|open)\s*\('
