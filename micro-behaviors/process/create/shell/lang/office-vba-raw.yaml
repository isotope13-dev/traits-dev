defaults:
  platforms: [windows]
  for: [oledoc, ooxml, xlsx]
  crit: notable
  conf: 0.98

traits:
  - id: vba-windows-shell-run
    desc: VBA Windows shell execution surface
    if:
      type: raw
      regex: '(?i)(WScript\.Shell|wscript\.exe|\.Run\b|Shell\s*\()'
