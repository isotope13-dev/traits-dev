defaults:
  platforms: [unix, windows]
  for: [javascript, typescript]
  attack: T1059.004

traits:
  - id: node-spawn-bash-shell
    desc: Node spawns Bash shell process
    crit: notable
    conf: 0.94
    if:
      type: symbol
      kind: call
      exact: cp.spawn
      arg:
        kind: string
        exact: /bin/bash

  - id: node-spawn-sh-shell
    desc: Node spawns POSIX sh process
    crit: notable
    conf: 0.96
    if:
      type: symbol
      kind: call
      regex: '(?i)(^|\.)(child[_-]?process|childproc|cp)\.spawn$'
      arg:
        kind: string
        exact: sh

  - id: bash-interactive-fd-bridge
    desc: Bash interactive fd bridge
    crit: suspicious
    conf: 0.98
    if:
      type: text
      regex: 'bash\s+-i\s*<&[0-9]+\s*>&[0-9]+\s*2>&[0-9]+'
