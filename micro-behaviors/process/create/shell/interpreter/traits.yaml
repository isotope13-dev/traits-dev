defaults:
  platforms: [unix, windows]
  for: [pe, pyc, powershell, python, batch, javascript, shell, vbs]
  crit: notable
  conf: 0.98

traits:
  - id: powershell-executable-token
    desc: PowerShell executable name reference
    if:
      type: text
      regex: '(?i)\bpowershell\.exe\b'
