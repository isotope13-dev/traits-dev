defaults:
  for: [all]

traits:
  - id: ping-delay
    desc: Ping loop for execution delay
    crit: notable
    conf: 0.8
    if:
      type: string
      regex: '(?i)ping\s+(-[wn]\s+\d+\s*){1,2}(127\.0\.0\.1|localhost)(>nul)?'
