defaults:
  attack: T1059
  for:
    - scripts
  platforms:
    - unix
traits:
  - id: sh-interactive
    desc: Interactive shell invocation (sh -i)
    crit: notable
    conf: 0.5
    if:
      type: string_literal
      substr: sh -i
