defaults:
  platforms: [unix, windows]
  for: [javascript, typescript]

traits:
  - id: shell-executable-reference
    desc: References a shell executable
    crit: notable
    conf: 0.9
    if:
      type: text
      regex: 'cmd\.exe|/(usr/)?bin/(sh|bash)|powershell(\.exe)?'
