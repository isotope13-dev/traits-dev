defaults:
  platforms: [macos]
  for: [macho, objc, swift, shell, applescript]
traits:
  - id: macos-pkg-root-install
    desc: macOS package install to system root
    crit: notable
    conf: 0.9
    if:
      type: text
      regex: /usr/sbin/installer\s+-pkg\b.{0,160}-target\s+/
