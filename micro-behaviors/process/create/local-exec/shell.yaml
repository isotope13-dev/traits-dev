defaults:
  for:
    - shell
traits:
  - id: local-execution
    desc: "Executes a file from current directory"
    crit: notable
    if:
      type: raw
      regex: '(?m)(^|[\\n;&|])\s*\./["''\\w\$][^\s;&|]{0,80}'
