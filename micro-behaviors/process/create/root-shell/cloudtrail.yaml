defaults:
  platforms: [unix, windows]
  for: [json]
  crit: notable
  conf: 0.9

traits:
  - id: root-shell-script-field
    desc: Audit script field runs root shell
    if:
      type: text
      regex: '(?is)"script"\s*:\s*\[[^\]]{0,600}\b(sudo|/bin/(ba)?sh|powershell(\.exe)?)\b'
