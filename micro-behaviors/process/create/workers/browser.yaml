defaults:
  platforms: [unix, windows, ios]
  for: [javascript, typescript, html]

traits:
  - id: browser-worker-constructor
    desc: Creates a browser Web Worker
    crit: notable
    conf: 0.9
    if:
      # Use the parsed call surface so JSDoc examples do not become runtime
      # process-creation findings.
      type: symbol
      kind: call
      exact: Worker
