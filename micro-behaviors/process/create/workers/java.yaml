defaults:
  platforms: [android, unix, windows]
  for: [java, kotlin]

traits:
  - id: java-fixed-thread-pool-source
    desc: Java creates a fixed-size worker pool
    crit: notable
    conf: 0.94
    if:
      type: symbol
      kind: call
      exact: newFixedThreadPool
