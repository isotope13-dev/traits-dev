defaults:
  platforms: [windows]
  for: [c, cpp]
  attack: T1059

traits:
  - id: beacon-spawn-temporary-process-declaration
    desc: Beacon BOF temporary-process API declaration
    crit: notable
    conf: 0.92
    if:
      type: text
      regex: 'DECLSPEC_IMPORT\s+[^;\n]{0,180}\bBeaconSpawnTemporaryProcess\s*\('
