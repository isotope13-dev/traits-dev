defaults:
  platforms: [macos]
  for: [shell]

traits:
  - id: macos-updater-app-launch
    desc: macOS updater launches a replacement application
    crit: notable
    conf: 0.95
    if:
      type: text
      regex: '(?m)^\s*open\s+["''].{0,200}\$APP_NAME'
