# Linux Application Launch Detection
# ATT&CK: T1106 (Native API)

defaults:
  for: [python, shell]
  platforms: [linux]

traits:
  - id: xdg-open-command
    desc: xdg-open command execution
    crit: notable
    conf: 0.80
    attack: "T1106"
    if:
      type: string
      regex: "xdg-open"
