defaults:
  for:
    - python
    - shell
  platforms:
    - linux
traits:
  - id: xdg-open-command
    desc: xdg-open command execution
    crit: notable
    conf: 0.8
    attack: T1106
    if:
      type: text
      regex: '(?m)(^\s*|[;&({]\s*)xdg-open(\s|$)'
