defaults:
  for:
    - python
    - shell
  platforms:
    - linux
traits:
  - id: xdg-open-command
    desc: xdg-open command execution
    crit: baseline
    conf: 0.8
    attack: T1106
    if:
      type: string_value
      regex: xdg-open
