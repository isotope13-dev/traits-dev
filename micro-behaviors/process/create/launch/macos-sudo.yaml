defaults:
  platforms: [macos, linux]
  for: [elf, macho]
  crit: notable
  conf: 0.6

traits:
  - id: sudo-command-path-reference
    desc: References the sudo command path
    if:
      type: text
      regex: '/(usr/)?s?bin/sudo(\.real)?'
