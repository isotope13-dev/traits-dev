defaults:
  platforms: [unix, windows]
  for: [javascript, typescript]
  attack: T1059.007

traits:
  - id: javascript-git-read-query
    desc: Node exec runs Git repository query
    crit: notable
    conf: 0.94
    if:
      type: text
      regex: '(?i)\bexec(Sync)?\s*\(\s*["''`]git\s+(branch|ls-tree|show|config)\b'
