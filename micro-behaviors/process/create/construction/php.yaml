defaults:
  platforms: [unix, windows]
  for: [php]
  crit: baseline
  conf: 0.8

traits:
  - id: php-object-instantiation
    desc: Instantiates a PHP object
    if:
      type: text
      regex: '\bnew\s+[A-Z][A-Za-z0-9_\\]*\s*\('
