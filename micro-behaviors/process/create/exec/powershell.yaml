# Process Creation in PowerShell
defaults:
  platforms: [linux, macos, windows, unix, android, ios]
  for: [powershell]
  crit: notable
  conf: 0.9
traits:
  - id: start-process-dup
    desc: Uses Start-Process cmdlet
    if:
      type: raw
      regex: '(?i)\bStart-Process\b'
