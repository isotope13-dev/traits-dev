defaults:
  for: [elf]
  platforms: [android]

traits:
  - id: execlp-process-launch
    desc: Imports execlp process-launch API
    crit: notable
    conf: 0.99
    if:
      type: import
      exact: execlp
