defaults:
  platforms: [windows]
  for: [vbs, scripts]
  attack: T1218.007

traits:
  - id: vbs-msiexec-quiet-install
    desc: VBScript runs msiexec quietly
    crit: notable
    conf: 0.97
    if:
      type: text
      regex: '(?i)\bmsiexec(\.exe)?\b.{0,100}\s/i\s+.{1,80}\s/(q|qn|quiet)\b'
