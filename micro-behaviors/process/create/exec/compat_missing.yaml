defaults:
  platforms: [windows]
  for: [binaries, scripts]

traits:
  - id: shell-execute-ex-a
    desc: Compatibility alias
    crit: component
    conf: 1.0
    if:
      id: micro-behaviors/process/create/system::shell-execute-ex-a
  - id: comspec-reference
    for: [text]
    desc: Compatibility placeholder
    crit: baseline
    conf: 1.0
    if:
      type: text
      exact: __cleave_missing_comspec_reference__
