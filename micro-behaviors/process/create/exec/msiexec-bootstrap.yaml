defaults:
  platforms: [windows]
  for: [pe, dll]
  attack: T1218.007

traits:
  - id: msiexec-install-reboot-suppressed
    desc: Launches MSI install with reboot suppressed
    crit: notable
    conf: 0.95
    if:
      type: text
      exact: msiexec.exe /i"%s" %s REBOOT="ReallySuppress"

