defaults:
  for: [perl]
  platforms: [unix, windows]
  crit: notable
  conf: 0.95

traits:
  - id: perl-exec-call
    desc: Perl replaces process with exec
    if:
      type: symbol
      kind: call
      exact: exec
