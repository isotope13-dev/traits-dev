traits:
  - id: file-execution
    criticality: notable
    description: WSH Run execution.
    platforms: [windows]
    for: [javascript]
    if:
      type: text
      regex: 'WScript\.Shell[\s\S]{0,200}\.Run\('
