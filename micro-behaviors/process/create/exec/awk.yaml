defaults:
  platforms: [unix, windows]
  for: [shell]

traits:
  - id: awk-system-call
    desc: Awk program invokes an operating-system command
    crit: notable
    conf: 0.9
    if:
      type: literal
      regex: '\bsystem\s*\('
