defaults:
  platforms: [unix]
  for: [perl]

traits:
  - id: perl-systemcall-wrapper
    desc: Perl external command wrapper call
    crit: notable
    conf: 0.9
    attack: "T1059.006"
    if:
      type: text
      regex: '\bsystemcall(_silent(_error)?|_or_warn)?\s*\('
