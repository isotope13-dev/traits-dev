defaults:
  platforms: [unix, windows]
traits:
  - id: python-sudo-execution
    desc: Executes python via sudo
    crit: notable
    conf: 0.9
    for:
      - shell
      - python
    if:
      type: text
      regex: sudo python3?
