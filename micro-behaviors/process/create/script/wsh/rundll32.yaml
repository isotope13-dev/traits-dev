defaults:
  platforms: [windows]
  for: [javascript]
traits:
  - id: js-rundll32-execution-call
    desc: Executes rundll32 via WScript.Shell
    crit: suspicious
    conf: 0.85
    if:
      type: string_value
      regex: '\["Run"\]\("rundll32"'
