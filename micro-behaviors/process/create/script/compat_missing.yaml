traits:
  - id: shell-cmd
    desc: compat placeholder
    crit: component
    conf: 0.1
    if:
      type: string
      exact: __cleave_missing_d30a2998ebab__
defaults:
  crit: component
  conf: 0.1
  for:
    - all
