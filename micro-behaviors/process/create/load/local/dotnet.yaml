defaults:
  platforms: [windows, unix]
  for: [pe, dll]

traits:
  - id: dotnet-load-from
    desc: .NET LoadFrom assembly-loading reference
    crit: notable
    conf: 0.85
    if:
      type: text
      exact: LoadFrom
