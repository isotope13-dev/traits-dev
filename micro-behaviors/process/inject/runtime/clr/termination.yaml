defaults:
  platforms: [windows]
  for: [pe, dll]
traits:
  - id: corexitprocess-string
    desc: CorExitProcess CLR termination reference
    crit: baseline
    conf: 0.8
    if:
      type: text
      word: CorExitProcess
