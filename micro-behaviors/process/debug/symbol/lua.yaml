defaults:
  platforms: [windows, unix]
  for: [lua]

traits:
  - id: lua-debug-module-access
    desc: Accesses Lua debug module
    crit: notable
    conf: 0.9
    if:
      type: symbol
      regex: '^debug\.'
