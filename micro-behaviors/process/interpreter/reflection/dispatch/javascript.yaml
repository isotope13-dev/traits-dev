defaults:
  platforms: [windows, unix]
  for: [javascript, typescript]

traits:
  - id: js-prototype-direct-invocation
    desc: Invokes a prototype property directly
    crit: component
    conf: 0.9
    if:
      type: symbol
      kind: call
      regex: '^[A-Za-z_$][A-Za-z0-9_$]*\.prototype$'
