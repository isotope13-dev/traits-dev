defaults:
  platforms: [unix]
  for: [shell]

traits:
  - id: shell-embedded-python-heredoc
    desc: Shell embeds Python heredoc execution
    crit: notable
    conf: 0.9
    if:
      type: text
      regex: 'python3\s+-\s*<<-?\s*[A-Za-z_][A-Za-z0-9_]*'
