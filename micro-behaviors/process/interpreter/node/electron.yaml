defaults:
  platforms: [windows, unix]
  for: [javascript, typescript]
traits:
  - id: js-electron-main-require
    desc: Node requires Electron main process module
    crit: notable
    conf: 0.95
    if:
      type: text
      regex: 'require\(["'']electron["'']\)'
