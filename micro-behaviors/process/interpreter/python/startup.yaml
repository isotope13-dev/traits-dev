defaults:
  platforms: [windows, unix]
  for: [python]

traits:
  - id: usercustomize-startup-hook
    desc: Python usercustomize startup hook file
    crit: notable
    conf: 0.95
    if:
      type: path
      basename: true
      exact: usercustomize.py
