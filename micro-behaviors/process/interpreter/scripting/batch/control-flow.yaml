defaults:
  platforms: [windows]
  for: [batch]

traits:
- id: local-label-call
  desc: Batch script calls a local subroutine label
  crit: baseline
  conf: 0.98
  if:
    type: text
    regex: (?im)^\s*@?call\s+:[A-Za-z0-9_.$-]+
