defaults:
  platforms: [windows]
  for: [scripts]

traits:
  - id: jscript-cc-tail-eval
    desc: Compatibility placeholder
    crit: component
    conf: 1.0
    if:
      type: string_literal
      exact: __cleave_missing_jscript_cc_tail_eval__
