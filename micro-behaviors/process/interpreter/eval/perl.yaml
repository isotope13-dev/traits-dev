defaults:
  platforms: [unix, windows]
  for: [perl]
  attack: T1059.006

traits:
  - id: perl-eval-variable
    desc: Perl eval on variable
    crit: suspicious
    conf: 0.9
    if:
      type: text
      regex: '\beval\s+\$[A-Za-z_][A-Za-z0-9_]*\b'
