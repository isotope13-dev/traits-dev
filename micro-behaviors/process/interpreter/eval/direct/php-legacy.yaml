defaults:
  platforms: [unix, windows]
  for: [php]
  crit: notable
  conf: 0.95

traits:
  - id: php-create-function-call
    desc: Creates PHP function from source strings
    if:
      type: symbol
      kind: call
      regex: '(?i)^create_function$'
