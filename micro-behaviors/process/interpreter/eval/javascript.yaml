# JavaScript Eval Detection
# MBC: B0009.016 (Dynamic Execution)
# ATT&CK: T1059.007

traits:
  - id: js-eval-raw
    desc: eval() function call (raw)
    crit: notable
    conf: 0.7
    for: [javascript]
    if:
      type: raw
      regex: '(^|[^\w$.])eval\s*\('
