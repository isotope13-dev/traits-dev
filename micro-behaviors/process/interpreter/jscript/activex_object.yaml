defaults:
  platforms: [unix, windows, macos]
  for: [javascript]
traits:
  - id: activex-object
    desc: ActiveXObject string presence
    crit: notable
    if:
      type: raw
      substr: "ActiveXObject"
    unless:
      - type: raw
        substr: "@externs"
