defaults:
  platforms: [unix, appliance]
  for: [objc, c, elf]

traits:
  - id: objc-runtime-msgsend
    desc: ObjC runtime objc_msgSend marker
    crit: component
    conf: 0.94
    if:
      type: text
      substr: objc_msgSend
