defaults:
  platforms: [linux, macos, unix]
  for: [perl]

traits:
  - id: fork-call
    desc: Perl fork child process
    crit: notable
    conf: 0.95
    if:
      type: raw
      regex: 'if\s*\(\s*\$[A-Za-z_][A-Za-z0-9_]*\s*=\s*fork\s*\)'
