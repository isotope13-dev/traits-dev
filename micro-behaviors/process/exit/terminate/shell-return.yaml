defaults:
  platforms: [unix, windows]
  for: [shell, batch]

traits:
  - id: shell-return-status
    desc: Script returns a process status
    crit: baseline
    conf: 0.8
    if:
      type: text
      regex: '(?i)(return|exit)[[:space:]]+\b(0|1)\b'
