defaults:
  platforms: [windows, unix]
  for: [ruby]

traits:
  - id: ruby-exit-call
    desc: Terminates the Ruby process with exit
    crit: notable
    conf: 0.9
    if:
      type: symbol
      kind: call
      exact: exit
