defaults:
  platforms: [windows, unix]
  for: [class, java_class, jar, zip]

traits:
  - id: jvm-system-exit-bytecode
    desc: JVM invokes System.exit
    crit: notable
    conf: 0.93
    if:
      type: import
      exact: java/lang/System.exit
