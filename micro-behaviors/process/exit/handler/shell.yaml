defaults:
  platforms: [unix]
  for: [shell]

traits:
  - id: shell-exit-trap
    desc: Shell registers a process-exit trap
    crit: notable
    conf: 0.9
    if:
      type: text
      regex: '(?m)^\s*trap\s+[''\"]?[A-Za-z_][A-Za-z0-9_]*[''\"]?\s+(0|EXIT)\b'
