defaults:
  platforms: [windows, unix]
  for: [javascript, typescript]

traits:
  - id: readystatechange-handler
    desc: Assigns an onreadystatechange handler
    crit: notable
    conf: 0.7
    if:
      type: symbol
      kind: bind
      regex: '^[A-Za-z_$][A-Za-z0-9_$.]*\.onreadystatechange$'
