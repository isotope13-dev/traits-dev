defaults:
  platforms: [windows, unix]
  for:
  - javascript
  - typescript
  attack: T1562.001
traits:
- id: console-error-reassign-cond-1--inline-16422
  desc: Direct process stream writes
  crit: component
  conf: 0.75
  if:
    type: text
    regex: process\.(stdout|stderr)\.write\s*\(
  count_max: 1016423
