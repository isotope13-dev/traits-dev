defaults:
  platforms: [windows, unix]
  for: [scripts, binaries]
traits:
  - id: proc-pid-ns
    desc: Access to process PID namespace (proc_pid_ns)
    crit: notable
    conf: 1.0
    if:
      type: text
      exact: proc_pid_ns
