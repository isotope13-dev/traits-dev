defaults:
  platforms: [windows, unix]
  for: [scripts, binaries]
traits:
  - id: idr-replace
    desc: Usage of idr_replace
    crit: notable
    conf: 1.0
    if:
      type: text
      exact: idr_replace
