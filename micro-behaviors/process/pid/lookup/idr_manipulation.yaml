traits:
  - id: idr-replace
    desc: Usage of idr_replace
    crit: notable
    conf: 1.0
    for: [all]
    if:
      type: string
      exact: "idr_replace"

  - id: proc-pid-ns
    desc: Access to process PID namespace (proc_pid_ns)
    crit: notable
    conf: 1.0
    for: [all]
    if:
      type: string
      exact: "proc_pid_ns"
