defaults:
  platforms: [windows, unix]
  for: [c]

traits:
  - id: fixed-pid-comparison
    desc: Compares PID variable to fixed constant
    crit: component
    conf: 0.94
    if:
      type: text
      regex: '\bpid\s*==\s*(0x[0-9A-Fa-f]{3,8}|[1-9][0-9]{3,8})'
