defaults:
  crit: notable
  conf: 0.82
  platforms: [unix, windows]
  for: [pe, csharp]

traits:
  - id: run-process-output-helper
    desc: .NET process-output helper token
    if:
      type: text
      exact: RunProcessOutput

  - id: run-process-input-helper
    desc: .NET process-input helper token
    if:
      type: text
      exact: RunProcessInput
