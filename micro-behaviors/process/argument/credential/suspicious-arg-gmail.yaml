defaults:
  platforms: [windows, unix]
traits:
  - id: suspicious-arg-gmail
    desc: Suspicious --gmail argument usage
    crit: notable
    for:
      - python
    if:
      type: string_literal
      exact: --gmail
