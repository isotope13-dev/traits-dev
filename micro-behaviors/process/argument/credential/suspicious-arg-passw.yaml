defaults:
  platforms: [windows, unix]
traits:
  - id: suspicious-arg-passw
    desc: Suspicious --passw argument usage
    crit: notable
    for:
      - python
    if:
      type: string_literal
      exact: --passw
