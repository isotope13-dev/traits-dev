defaults:
  platforms: [unix, windows]
  for: [shell]
  crit: notable
  conf: 0.9

traits:
  - id: shell-positional-args-1-through-4
    desc: Shell expands four positional arguments
    if:
      type: string_literal
      substr: $1 $2 $3 $4
