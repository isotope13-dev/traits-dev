defaults:
  platforms: [unix]
  for: [php]

traits:
  - id: php-xdebug-extension-check
    desc: PHP Xdebug extension check
    crit: notable
    conf: 0.94
    if:
      type: text
      substr: extension_loaded('xdebug')
