defaults:
  platforms: [macos, ios]
  for: [macho]
  crit: notable
  conf: 0.98
  attack: T1055

traits:
  - id: task-for-pid
    desc: Obtains another process Mach task
    if:
      type: import
      exact: task_for_pid

  - id: task-memory-write
    desc: Writes another Mach task memory
    if:
      type: import
      exact: mach_vm_write
