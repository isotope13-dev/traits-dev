defaults:
  platforms: [linux, macos, windows, unix, android, ios]
traits:
  - id: respawn
    desc: Thread self-respawning logic in finally block
    crit: suspicious
    conf: 0.8
    for: [java]
    if:
      type: text
      regex: 'finally[\s\S]*new\s+Thread[\s\S]*\.start\(\)'
