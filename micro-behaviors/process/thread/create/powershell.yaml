defaults:
  platforms: [windows]
  for: [powershell]
  attack: T1055.001

traits:
  - id: powershell-create-thread-pinvoke
    desc: PowerShell P/Invokes CreateThread
    crit: notable
    conf: 0.96
    if:
      type: text
      regex: '(?is)DllImport\s*\(\s*["'']kernel32\.dll["''].{0,80}\)[\s\S]{0,180}\bCreateThread\b'
