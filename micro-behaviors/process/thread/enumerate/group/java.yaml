defaults:
  platforms: [windows, unix]
  for: [java]
traits:
  - id: get-group
    desc: Java accesses thread group enumeration
    crit: notable
    conf: 0.8
    if:
      type: text
      regex: '\.getThreadGroup\s*\(\s*\)'
    unless:
      - type: basename
        regex: '.*Test\.java$'
