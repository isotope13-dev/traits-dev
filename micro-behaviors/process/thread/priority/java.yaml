defaults:
  platforms: [linux, macos, windows, unix, android, ios]
traits:
  - id: set-max
    desc: Sets thread priority to maximum
    crit: notable
    conf: 0.9
    for: [java]
    if:
      type: text
      regex: 'setPriority\s*\(\s*(Thread\.)?MAX_PRIORITY\s*\)'
