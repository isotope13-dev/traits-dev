defaults:
  for: [pe, dll]
  platforms: [windows]
traits:
  - id: set-priority-class
    desc: Raise process priority class
    crit: notable
    conf: 0.85
    if:
      type: symbol
      exact: "SetPriorityClass"
  - id: set-priority-class-text
    desc: SetPriorityClass API string
    crit: baseline
    conf: 0.8
    if:
      type: text
      exact: "SetPriorityClass"
  - id: set-thread-priority
    desc: Raise thread execution priority
    crit: notable
    conf: 0.85
    if:
      type: symbol
      exact: "SetThreadPriority"
  - id: set-thread-priority-text
    desc: SetThreadPriority API string
    crit: baseline
    conf: 0.8
    if:
      type: text
      exact: "SetThreadPriority"
composite_rules:
  - id: priority-adjustment
    desc: Adjust process or thread priority
    crit: notable
    conf: 0.88
    any:
      - id: set-priority-class
      - id: set-priority-class-text
      - id: set-thread-priority
      - id: set-thread-priority-text
