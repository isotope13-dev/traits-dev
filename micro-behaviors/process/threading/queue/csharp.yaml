defaults:
  platforms: [windows]
  for: [csharp]

traits:
  - id: queue-user-work-item-call
    desc: ThreadPool.QueueUserWorkItem call
    crit: notable
    conf: 0.9
    if:
      type: symbol
      kind: call
      regex: '^ThreadPool\.QueueUserWorkItem$'
