defaults:
  platforms: [windows]
  for: [pe, dll]

traits:
  - id: queue-user-work-item
    desc: Queue a Windows thread-pool work item
    crit: notable
    conf: 0.9
    if:
      type: import
      exact: QueueUserWorkItem
