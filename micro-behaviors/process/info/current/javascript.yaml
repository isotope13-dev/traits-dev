defaults:
  platforms: [windows, unix]
  for: [javascript, typescript]
traits:
  - id: node-process-ppid-access
    desc: Node process.ppid access
    conf: 0.8
    crit: notable
    if:
      type: text
      regex: '\bprocess\.ppid\b'
