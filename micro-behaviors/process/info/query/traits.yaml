defaults:
  for: [binaries, c]
  platforms:
    - linux
    - unix
    - macos
traits:
  - id: get-process-for-pid
    desc: Lookup process by PID (Carbon)
    crit: notable
    conf: 0.8
    platforms:
      - macos
    for:
      - macho
      - c
    if:
      type: string_value
      substr: GetProcessForPID
