defaults:
  platforms: [windows, unix]
  for: [javascript]
traits:
  - id: check-argv
    desc: Reads Node.js arguments through process.argv
    conf: 0.8
    crit: baseline
    if:
      type: text
      substr: process.argv
