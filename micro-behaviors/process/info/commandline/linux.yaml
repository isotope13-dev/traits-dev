defaults:
  platforms: [linux]
  for: [elf]

traits:
  - id: daemon-helper-name-cluster
    desc: Cluster of daemon-like process names
    crit: notable
    conf: 0.9
    count_min: 5
    if:
      type: text
      regex: '^[a-z]{4,12}-(helper|monitor|svc)$'
