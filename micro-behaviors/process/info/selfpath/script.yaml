defaults:
  platforms: [unix, windows]
  for: [python, javascript, typescript, shell]

traits:
  - id: self-update-flow-marker
    desc: Source declares an explicit self-update flow
    crit: notable
    conf: 0.9
    if:
      type: text
      substr: '"self_update"'
