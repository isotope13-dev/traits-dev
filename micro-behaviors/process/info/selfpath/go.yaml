defaults:
  platforms: [windows, linux, macos]
  for: [go]

traits:
  - id: os-executable
    desc: Go queries own executable path
    crit: notable
    conf: 0.7
    if:
      type: symbol
      kind: call
      exact: os.Executable
