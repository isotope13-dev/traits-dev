defaults:
  platforms: [windows, unix]
  for: [csharp]

traits:
  - id: mainmodule-filename
    desc: C# reads executable path via MainModule.FileName
    crit: notable
    conf: 0.88
    if:
      type: text
      substr: MainModule.FileName
