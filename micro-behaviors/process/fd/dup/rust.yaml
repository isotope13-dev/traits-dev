# Rust file-descriptor duplication primitives.
defaults:
  platforms: [linux, macos]
  for: [rust]

traits:
  - id: rust-libc-dup-fd
    desc: Rust calls libc::dup on a file descriptor
    crit: notable
    conf: 0.9
    if:
      type: text
      regex: 'libc::dup\s*\(\s*fd\s*\)'
