# Python file-descriptor duplication primitives.
defaults:
  platforms: [unix, windows]
  for: [python]

traits:
  - id: python-dup2
    desc: Python file descriptor duplication (os.dup2)
    crit: notable
    conf: 0.85
    if:
      type: symbol
      exact: "os.dup2"
