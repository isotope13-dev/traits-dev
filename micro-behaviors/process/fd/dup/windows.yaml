defaults:
  platforms: [windows]
  for: [pe, dll, c, cpp]
traits:
  - id: duplicate-handle-windows
    desc: Duplicate handle
    crit: baseline
    conf: 0.80
    if:
      type: raw
      regex: "\\b([Dd]up(licate)?[Hh]andle)\\s*\\("
