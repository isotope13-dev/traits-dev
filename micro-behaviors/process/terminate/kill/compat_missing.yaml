defaults:
  platforms: [windows]
  for: [binaries]

traits:
  - id: zw-process-kill-chain
    desc: Compatibility placeholder
    crit: component
    conf: 1.0
    if:
      type: text
      exact: __cleave_missing_zw_process_kill_chain__
