defaults:
  platforms: [unix]
  for: [shell]
traits:
  - id: kill-abrt-pid-zero
    desc: kill ABRT signal to group
    crit: suspicious
    conf: 0.9
    if:
      type: text
      regex: 'kill\s+-ABRT\s+0\b'
