# Process Termination Capabilities
defaults:
  platforms: [windows, unix]
  for: [python, elf, pe, macho]
traits:
  - id: bsod-symbol
    desc: Reference to BSOD (symbol)
    crit: notable
    conf: 0.6
    if:
      type: symbol
      exact: "bsod"
