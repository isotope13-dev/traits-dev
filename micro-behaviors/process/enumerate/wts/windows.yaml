defaults:
  platforms: [windows]
  for: [pe, dll]

traits:
- id: wts-enumerate-processes
  desc: Enumerate processes through WTS
  crit: notable
  conf: 0.9
  attack: T1057
  if:
    type: symbol
    exact: WTSEnumerateProcessesW
  downgrade:
    any:
    - id: metadata/signed/platform/
    - id: metadata/binary/vendor::microsoft-corp-versioninfo
