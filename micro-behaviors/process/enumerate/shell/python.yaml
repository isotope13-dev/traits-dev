defaults:
  platforms: [unix]
  for: [python, pyc]
  attack: T1041

traits:
  - id: python-process-snapshot-command
    desc: Python process snapshot command
    crit: suspicious
    conf: 0.93
    if:
      type: text
      regex: '"ps"[\s\S]{0,80}"-eo"[\s\S]{0,140}"pid,ppid,user,stat,start,time,comm"'
