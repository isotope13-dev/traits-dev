defaults:
  platforms: [unix]
  for: [shell]
  attack: T1041

traits:
  - id: shell-macos-process-inventory
    desc: Shell macOS process inventory
    crit: suspicious
    conf: 0.94
    if:
      type: text
      regex: 'ps auxww[\s\S]{0,220}ps axjf[\s\S]{0,220}lsof -i'
