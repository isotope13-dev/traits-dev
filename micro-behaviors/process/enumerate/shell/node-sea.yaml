# Process enumeration used to gate a Node SEA payload
defaults:
  platforms: [windows]
  for: [pe]
  attack: T1057

traits:
  - id: node-sea-process-list-flags
    desc: Windows process-list flags
    crit: suspicious
    conf: 0.96
    if:
      type: text
      substr: '"/fo","csv","/nh"'
