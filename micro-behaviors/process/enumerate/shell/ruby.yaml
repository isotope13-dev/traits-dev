defaults:
  platforms: [windows]
  for: [ruby]
  attack: T1041

traits:
  - id: ruby-tasklist-command
    desc: Ruby tasklist command
    crit: suspicious
    conf: 0.92
    if:
      type: text
      regex: '(system\(|`|exec\()[^\n]{0,24}tasklist\.exe'
