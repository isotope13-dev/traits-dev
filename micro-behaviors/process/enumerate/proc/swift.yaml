defaults:
  platforms: [unix]
  for: [swift]
  attack: T1057

traits:
  - id: proc-status-path-reference
    desc: Swift reads proc status for process enumeration
    crit: notable
    conf: 0.92
    if:
      type: text
      regex: '/proc[\\s\\S]{0,180}/status'
