defaults:
  platforms: [unix]
  for: [shell]
traits:
- id: ps-grep-targeting
  desc: ps output filtered with grep
  crit: notable
  conf: 0.7
  if:
    type: text
    regex: (ps -eo|ps aux \| grep|ps\s+[a-z]*\s*\|\s*grep)
