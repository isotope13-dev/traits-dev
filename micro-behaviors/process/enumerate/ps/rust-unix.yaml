defaults:
  platforms: [unix]
  for: [rust]
  crit: notable
  conf: 0.9

traits:
  - id: rust-ps-command
    desc: Rust launches ps for process enumeration
    if:
      type: text
      regex: 'Command::new\("ps"\)'
