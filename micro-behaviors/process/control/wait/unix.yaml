defaults:
  for: [elf, macho, c]
  platforms:
    - linux
    - macos
traits:
  - id: waitpid
    desc: Wait for child process state
    crit: baseline
    conf: 0.66
    if:
      type: symbol
      exact: waitpid
  - id: waitid
    desc: Wait for process state changes
    crit: baseline
    conf: 0.66
    if:
      type: symbol
      exact: waitid
