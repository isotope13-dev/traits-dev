defaults:
  crit: baseline
  conf: 0.66
  for: [elf, macho, c]
  platforms:
    - linux
    - macos
traits:
  - id: waitpid
    desc: Wait for child process state
    if:
      type: symbol
      exact: waitpid
  - id: waitid
    desc: Wait for process state changes
    if:
      type: symbol
      exact: waitid
