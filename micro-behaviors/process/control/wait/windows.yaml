defaults:
  platforms: [windows]
  for: [pe, dll]
traits:
  - id: wait-for-single-object-api
    desc: WaitForSingleObject process synchronization API
    crit: baseline
    conf: 0.9
    if:
      type: symbol
      exact: WaitForSingleObject
