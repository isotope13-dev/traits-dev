defaults:
  platforms: [windows, unix, android]
  for: [class, jar, zip, android_apk]
traits:
  - id: java-process-waitfor-bytecode
    desc: Java bytecode process wait
    crit: notable
    conf: 0.9
    for: [class]
    if:
      type: import
      exact: java/lang/Process.waitFor
