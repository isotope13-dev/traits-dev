defaults:
  platforms: [unix, appliance]
  for: [shell, elf]
  crit: notable
  conf: 0.94
  attack: T1562.001

traits:
  - id: embedded-httpd-termination
    desc: Stops an embedded httpd management service
    if:
      type: text
      regex: '\b(killall|pkill)\s+(-9\s+)?httpd\b'
