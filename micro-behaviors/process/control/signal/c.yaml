defaults:
  platforms: [unix]
  for: [c, cpp, objc]

traits:
  - id: c-kill-sigstop
    desc: C sends SIGSTOP to a process
    crit: notable
    conf: 0.85
    if:
      type: text
      regex: 'kill\s*\([^;]{0,120}SIGSTOP'
