defaults:
  platforms: [unix]
  for: [ruby]

traits:
  - id: ruby-term-int-signal-handlers
    desc: Ruby handles TERM and INT signals
    crit: notable
    conf: 0.95
    if:
      type: text
      regex: "trap\\('TERM'\\)[\\s\\S]{0,160}trap\\('INT'\\)"
