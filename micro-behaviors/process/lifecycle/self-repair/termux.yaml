defaults:
  platforms: [android, unix]
  for: [javascript, typescript]

traits:
  - id: termux-utils-missing-core-reinstall
    desc: Reinstalls missing Termux utility core
    crit: notable
    conf: 0.95
    if:
      type: text
      substr: "Missing core, reinstalling"
