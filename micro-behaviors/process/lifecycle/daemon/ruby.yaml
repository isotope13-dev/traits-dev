# Process Lifecycle - Ruby
traits:
  - id: ruby-daemon
    desc: Daemonize process (background)
    crit: notable
    conf: 0.93
    platforms: [unix]
    for: [ruby]
    if:
      type: symbol
      regex: "Process\\.daemon"
