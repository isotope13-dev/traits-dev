defaults:
  platforms: [unix, windows]
  for: [c, cpp]

traits:
  - id: gcc-constructor-attr
    desc: GCC constructor runs before main
    crit: notable
    conf: 0.8
    if:
      type: text
      substr: "__attribute__((constructor))"

