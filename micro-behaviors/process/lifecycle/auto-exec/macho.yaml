defaults:
  platforms: [macos]
  for: [macho]

traits:
  - id: macho-module-initializer
    desc: Mach-O global module initializer table
    crit: baseline
    conf: 0.95
    if:
      type: section
      regex: 'mod_init_func'
