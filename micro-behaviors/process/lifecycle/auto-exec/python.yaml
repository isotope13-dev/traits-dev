defaults:
  platforms: [unix, windows]
  for: [python]
traits:
  - id: asyncio-run-call
    desc: Python asyncio.run call
    crit: notable
    conf: 0.85
    if:
      type: symbol
      kind: call
      exact: asyncio.run
    unless:
      - id: metadata/lang/source::linux-kernel-primary-tree-path
      - id: metadata/file/string/corpus::hashed-text-record
