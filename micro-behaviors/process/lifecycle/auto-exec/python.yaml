defaults:
  platforms: [unix, windows]
  for: [python]
traits:
  - id: asyncio-run-main
    desc: Python asyncio main entrypoint
    crit: notable
    conf: 0.85
    if:
      type: text
      substr: asyncio.run(main())
    unless:
      - id: metadata/lang/source::linux-kernel-primary-tree-path
