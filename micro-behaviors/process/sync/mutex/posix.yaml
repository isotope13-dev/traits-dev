defaults:
  platforms: [unix, ios, android]
  for: [c, cpp, macho, elf, javascript, typescript]
  mbc: C0042
traits:
  - id: posix-pthread-mutex-init
    desc: POSIX pthread_mutex_init call
    crit: baseline
    conf: 0.9
    if:
      type: string_literal
      exact: pthread_mutex_init
