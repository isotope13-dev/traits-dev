defaults:
  platforms: [unix, windows]
  for: [javascript, typescript]

traits:
  - id: javascript-mutex-call
    desc: Calls a JavaScript Mutex binding
    crit: baseline
    conf: 0.9
    if:
      type: symbol
      kind: call
      exact: Mutex
