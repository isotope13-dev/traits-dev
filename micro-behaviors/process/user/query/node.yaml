defaults:
  platforms: [unix, windows]
  for: [javascript, typescript]
  crit: notable
  attack: T1033

traits:
  - id: node-shell-id-command
    desc: Node passes id to a shell helper
    conf: 0.9
    if:
      type: tree-sitter
      kind: call
      regex: '\b[A-Za-z_$][A-Za-z0-9_$]*\s*\(\s*["'']id["'']\s*\)'
