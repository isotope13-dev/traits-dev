defaults:
  platforms: [unix, windows]
  for: [javascript, typescript]
  attack: T1041

traits:
  - id: node-runs-id
    desc: Node wrapper runs identity command
    crit: suspicious
    conf: 0.94
    if:
      type: text
      regex: "run\\s*\\(\\s*['\"]id['\"]\\s*\\)"
