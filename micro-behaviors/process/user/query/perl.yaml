defaults:
  for: [perl]
  platforms: [linux, macos, windows, unix, android, ios]
  crit: notable
  conf: 0.78
  mbc: E1033
  attack: T1033
traits:
  - id: perl-getpwuid
    desc: Get passwd entry by UID
    if:
      type: raw
      regex: '\bgetpwuid\s*\('
