defaults:
  platforms: [macos]
  for: [macho, swift, objc]

traits:
  - id: nsusername
    desc: Reads current macOS username
    crit: notable
    conf: 0.8
    if:
      type: symbol
      exact: NSUserName
