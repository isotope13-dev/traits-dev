defaults:
  platforms: [windows, unix]
  for: [javascript, typescript]
  crit: notable
  attack: T1033
traits:
  - id: os-userinfo-call
    desc: os.userInfo() user query
    conf: 0.8
    if:
      type: symbol
      exact: os.userInfo
