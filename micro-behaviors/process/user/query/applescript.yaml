defaults:
  platforms: [macos]
  for: [applescript]
  attack: T1033

traits:
  - id: applescript-shell-whoami
    desc: AppleScript invokes the whoami command
    crit: notable
    conf: 0.98
    if:
      type: text
      regex: 'do shell script\s+["'']whoami["'']'
