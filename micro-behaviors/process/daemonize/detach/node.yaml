# Node.js event-loop handle detachment
defaults:
  platforms: [windows, unix]
  for: [javascript, typescript]
  mbc: "F0011"

traits:
  - id: detached-obfuscated-true
    desc: Detached spawn uses obfuscated true
    crit: notable
    conf: 0.9
    if:
      type: text
      regex: "['\"]detached['\"]\\s*:\\s*!!\\s*\\[\\s*\\]"
  - id: node-handle-unref-call
    desc: Node handle detached from event loop
    crit: notable
    conf: 0.9
    # Call-site evidence only: text `.unref()` matches JSDoc in .d.ts files.
    # Vue's unref(value) is a different call shape (has an argument).
    if:
      type: symbol
      kind: call
      substr: .unref
    unless:
      - id: metadata/lang/scripted::dts-declaration-file
  # Large UMD bundles can lose call facts while retaining the expression.
  - id: node-handle-unref-text
    desc: Node handle unref call in bundled JavaScript
    crit: notable
    conf: 0.86
    if:
      type: text
      regex: '\b[A-Za-z_$][A-Za-z0-9_$]*\.unref\s*\(\s*\)'
    unless:
      - id: metadata/lang/scripted::dts-declaration-file
