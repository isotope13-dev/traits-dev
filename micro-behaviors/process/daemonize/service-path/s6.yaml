defaults:
  platforms: [unix]
  for: [scripts, binaries]

traits:
  - id: s6-service-run-path
    desc: References an s6 service run-script path
    crit: notable
    conf: 0.9
    if:
      type: text
      regex: '/etc/s6(-rc)?/[^\s]{1,160}/run\b'
