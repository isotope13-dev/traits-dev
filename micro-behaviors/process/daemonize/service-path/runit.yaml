defaults:
  platforms: [unix]
  for: [shell, scripts, binaries]

traits:
  - id: runit-service-path
    desc: runit service directory path
    crit: notable
    conf: 0.9
    if:
      type: text
      regex: '/etc/(sv|service)/[A-Za-z0-9_.@-]{1,80}/run\b'
