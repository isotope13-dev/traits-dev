defaults:
  platforms: [linux]
  for: [c, cpp, elf, shell]

traits:
  - id: rsyslogd-daemon-path
    desc: References the rsyslogd daemon path
    crit: notable
    conf: 0.9
    if:
      type: text
      exact: /usr/sbin/rsyslogd
