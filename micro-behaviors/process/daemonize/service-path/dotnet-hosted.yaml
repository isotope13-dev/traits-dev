defaults:
  platforms: [windows, linux, macos]
  for: [pe, dll]
  crit: notable

traits:
  - id: dotnet-hosted-startup-service
    desc: Registers background hosted startup service
    conf: 0.9
    if:
      type: text
      regex: '^(AddHostedService|HttpSinkStartupService)$'
    count_min: 2

