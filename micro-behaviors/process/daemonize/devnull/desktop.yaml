defaults:
  platforms: [linux]
  for: [desktop-entry]

traits:
  - id: desktop-exec-devnull-redirection
    desc: Desktop Exec suppresses process output
    crit: notable
    conf: 0.92
    if:
      type: value
      path: desktop_entry.exec
      substr: '>/dev/null 2>&1'
