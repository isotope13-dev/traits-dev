defaults:
  crit: baseline
  platforms:
    - macos
  for:
    - macho
traits:
  - id: system-daemon
    desc: Apple system daemon (libexec path)
    conf: 0.9
    if:
      type: text
      regex: /usr/libexec/[a-z][a-z0-9_-]+d\b
      section: .text
