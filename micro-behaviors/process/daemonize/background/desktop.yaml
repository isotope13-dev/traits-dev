defaults:
  platforms: [linux]
  for: [desktop-entry]

traits:
  - id: desktop-exec-background-command
    desc: Desktop Exec backgrounds a command
    crit: notable
    conf: 0.9
    if:
      type: value
      path: desktop_entry.exec
      regex: '&\s+(sleep|\()'
