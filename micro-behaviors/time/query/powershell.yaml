defaults:
  platforms: [windows]
  for: [powershell]
  crit: notable

traits:
  - id: powershell-past-date-cutoff
    desc: PowerShell computes a past date cutoff
    conf: 0.95
    if:
      type: text
      regex: '(?i)\(\s*Get-Date\s*\)\s*\.AddDays\(\s*-\d{1,4}\s*\)'
