defaults:
  platforms: [windows]
  for: [c, cpp]
traits:
  - id: sleep-source
    desc: Delay execution with Sleep in source
    crit: baseline
    conf: 0.82
    if:
      type: symbol
      exact: Sleep
