defaults:
  platforms: [windows, unix]
  for: [ruby]
  crit: baseline
  conf: 0.65
traits:
  - id: ruby-sleep-call
    desc: Ruby sleep delay call
    if:
      type: symbol
      kind: call
      regex: ^sleep$
    unless:
      - id: well-known/lib/jruby/
