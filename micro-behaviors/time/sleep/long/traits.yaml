defaults:
  platforms: [unix, windows]
traits:
  - id: process-sleep-60-seconds
    desc: Sleeps for 60 seconds
    crit: component
    conf: 0.7
    for:
      - powershell
      - python
    if:
      type: text
      regex: (Start-Sleep\s+-Seconds\s+60|time\.sleep\(60\))
