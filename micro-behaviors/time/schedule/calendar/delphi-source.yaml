defaults:
  crit: notable
  for: [pe]
  platforms: [windows]
traits:
  - id: day-of-month-trigger-source
    desc: Day of month payload trigger
    conf: 0.95
    if:
      type: text
      exact: "if Tiempo.wDay = 4 then"
  - id: october-trigger-source
    desc: October payload trigger
    conf: 0.94
    if:
      type: text
      exact: "iF Tiempo.wMonth = 10  Then"
