defaults:
  platforms: [unix, android, ios]
  for: [c]
traits:
  - id: sigalrm-handler
    desc: SIGALRM timeout handler
    crit: baseline
    conf: 0.9
    if:
      type: text
      regex: "signal\\s*\\(\\s*SIGALRM\\s*,"
