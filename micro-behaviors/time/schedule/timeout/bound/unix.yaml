defaults:
  platforms: [unix, android, ios]
  for: [c]
composite_rules:
  - id: alarm-timeout-handler
    desc: Alarm-based execution timeout
    crit: baseline
    conf: 0.92
    all:
      - id: micro-behaviors/time/schedule/timeout/alarm::alarm
      - id: micro-behaviors/time/schedule/timeout/handler::sigalrm-handler
