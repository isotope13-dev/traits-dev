# Timer Queue Callback APIs (Windows)
defaults:
  platforms: [windows]
  for: [pe, dll]
traits:
  - id: delete-timer-queue-timer-sym
    desc: DeleteTimerQueueTimer symbol
    crit: baseline
    conf: 0.9
    if:
      type: symbol
      exact: "DeleteTimerQueueTimer"
