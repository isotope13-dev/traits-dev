defaults:
  platforms: [windows]
  for: [pe, dll]
traits:
  - id: vb6-timer-control
    desc: VB6 Timer1 control marker
    crit: notable
    conf: 0.82
    if:
      type: string_value
      substr: "Timer1"
