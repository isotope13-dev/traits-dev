defaults:
  for: [scripts, source]
  platforms: [linux]
  crit: notable
  conf: 0.9

traits:
  - id: systemd-timer-schedule-literal
    desc: Embeds systemd timer schedule directive
    if:
      type: literal
      regex: '(?m)^\s*(OnCalendar|OnUnitActiveSec)\s*='
