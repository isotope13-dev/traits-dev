defaults:
  platforms: [windows, unix]
  for:
    - javascript
    - typescript
traits:
  - id: setInterval
    desc: setInterval timer usage
    crit: baseline
    conf: 0.5
    if:
      type: symbol
      kind: call
      exact: setInterval
  - id: setTimeout
    desc: setTimeout timer usage
    crit: baseline
    conf: 0.5
    if:
      type: symbol
      kind: call
      exact: setTimeout
