defaults:
  platforms:
    - linux
    - macos
    - windows
    - unix
    - android
    - ios
  for:
    - javascript
    - typescript
traits:
  - id: setInterval
    desc: setInterval timer usage
    crit: baseline
    conf: 0.5
    if:
      type: string_value
      regex: setInterval\s*\(
  - id: setTimeout
    desc: setTimeout timer usage
    crit: baseline
    conf: 0.5
    if:
      type: string_value
      regex: setTimeout\s*\(
