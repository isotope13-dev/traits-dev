defaults:
  platforms: [ios, macos]
  for: [swift]
  crit: notable
  conf: 0.97

traits:
  - id: swift-repeating-scheduled-timer
    desc: Swift source creates a repeating scheduled timer
    if:
      type: text
      regex: '\bTimer\.scheduledTimer\s*\([^)]{0,180}\brepeats\s*:\s*true\b'
