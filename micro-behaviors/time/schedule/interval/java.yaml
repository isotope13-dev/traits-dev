defaults:
  platforms: [unix, windows]
  for: [java, class, jar]
traits:
  - id: java-schedule-at-fixed-rate
    desc: Java fixed-rate scheduled task
    crit: notable
    conf: 0.88
    if:
      type: raw
      substr: scheduleAtFixedRate
