defaults:
  platforms: [unix, windows]
  for: [rust]
  attack: T1090

traits:
  - id: rust-socks5-client-connect
    desc: Rust SOCKS5 client opens proxy connection
    crit: notable
    conf: 0.96
    if:
      type: text
      regex: 'async fn socks5_connect[\s\S]{0,180}TcpStream::connect'
