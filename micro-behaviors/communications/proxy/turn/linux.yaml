defaults:
  platforms: [unix]
  for: [elf]

traits:
  - id: google-stun-host
    desc: Google STUN server hostname
    crit: notable
    conf: 0.88
    if:
      type: text
      regex: '\bstun[0-4]?\.l\.google\.com\b'
      section: rodata
