defaults:
  platforms: [windows, unix]
  for: [python]
  mbc: C0002

traits:
  - id: turn-credential-request
    desc: TURN credential request
    crit: notable
    conf: 0.84
    if:
      type: text
      regex: '(/turn/token|GetTurnCredential|TurnCredentialRequest)'
