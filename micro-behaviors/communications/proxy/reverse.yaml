defaults:
  platforms: [windows, unix]
  for: [elf, macho, pe]
  crit: notable
  conf: 0.75
  attack: T1090
traits:
  - id: reverse-proxy
    desc: Reverse proxy implementation reference
    if:
      type: text
      regex: (?i)reverse[-_]?(proxy)
    unless:
      - type: text
        substr: '"comment":"buildkit.dockerfile.v0"'
