defaults:
  platforms:
    - unix
    - windows
  for: [scripts, binaries]
  crit: notable
  conf: 0.9
traits:
  - id: iperf
    desc: iperf bandwidth test
    if:
      type: text
      regex: (?i)\biperf\b
  - id: iperf3
    desc: iperf3 bandwidth test
    if:
      type: text
      regex: (?i)\biperf3\b
