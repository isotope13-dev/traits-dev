defaults:
  platforms: [windows, unix]
  for: [binaries, scripts]

traits:
  - id: udp-flood-function
    desc: UDP flood function name
    crit: notable
    conf: 0.9
    if:
      type: text
      substr: udp_flood
