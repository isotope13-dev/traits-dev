defaults:
  platforms:
    - unix
    - windows
  for: [scripts, binaries]
  crit: notable
  conf: 0.9
traits:
  - id: speedtest
    desc: speedtest utility
    if:
      type: text
      substr: speedtest
