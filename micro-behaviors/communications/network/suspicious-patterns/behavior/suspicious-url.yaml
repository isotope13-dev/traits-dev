defaults:
  platforms: [unix, windows, macos]
  for: [shell, python, javascript, powershell, pe, elf]
traits:
  - id: suspicious-url
    desc: Suspicious C2 URL or email address.
    crit: suspicious
    conf: 0.9
    if:
      type: string
      exact: "0rUWe_@cVW.axS"
