defaults:
  for: [go]
  platforms: [windows, unix]

traits:
- id: go-named-pipe-path
  desc: Go source names a Windows named-pipe path
  crit: notable
  conf: 0.8
  if:
    type: text
    # Match the on-disk Go spelling `\\.\pipe\` (raw or quoted).
    substr: "\\\\.\\pipe\\"
