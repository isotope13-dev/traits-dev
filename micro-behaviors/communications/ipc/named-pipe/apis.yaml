defaults:
  for: [pe]
  platforms: [windows]
traits:
  - id: named-pipe-api-usage
    desc: Uses named pipe IPC APIs
    crit: notable
    conf: 1.0
    if:
      type: symbol
      substr: "NamedPipe"
