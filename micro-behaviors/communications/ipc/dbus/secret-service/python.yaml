defaults:
  platforms: [unix]
  for: [python]
  crit: notable
  conf: 0.9

traits:
  - id: freedesktop-secret-service-reference
    desc: Freedesktop Secret Service bus reference
    if:
      type: string_literal
      exact: org.freedesktop.secrets
