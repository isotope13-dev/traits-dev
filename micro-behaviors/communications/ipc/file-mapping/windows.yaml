defaults:
  platforms: [windows]
  for: [pe, dll]
traits:
  - id: open-file-mapping-a
    desc: Open named file mapping
    crit: notable
    conf: 0.85
    if:
      type: symbol
      exact: "OpenFileMappingA"
