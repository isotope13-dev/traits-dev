defaults:
  crit: notable
  conf: 0.85
  platforms: [windows]
  for: [pe, dll]
traits:
  - id: open-file-mapping-a
    desc: Open named file mapping
    if:
      type: symbol
      exact: "OpenFileMappingA"
  - id: open-file-mapping-w
    desc: Open named file mapping (Unicode)
    if:
      type: symbol
      exact: "OpenFileMappingW"
