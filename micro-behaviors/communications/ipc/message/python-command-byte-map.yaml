defaults:
  platforms: [windows, unix]
  for: [python]

traits:
  - id: python-command-byte-map
    desc: Python maps protocol commands to byte constants
    crit: notable
    conf: 0.85
    count_min: 4
    if:
      type: text
      regex: '(?i)[a-z_]+\s*=\s*b"\\x[0-9a-fA-F]{2}(\\x00){2,3}"'
