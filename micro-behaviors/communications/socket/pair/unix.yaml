defaults:
  platforms: [unix]
  for: [c, cpp]
traits:
  - id: pair
    desc: Create a pair of connected
    crit: notable
    conf: 0.66
    if:
      type: symbol
      kind: call
      exact: socketpair
