defaults:
  platforms: [unix]
  for: [c, cpp]
traits:
  - id: pair
    desc: Create connected socket pair
    crit: notable
    conf: 0.66
    if:
      type: symbol
      kind: call
      exact: socketpair
    unless:
      - id: micro-behaviors/os/syscall/invoke::linux-kernel-tree-path-any
      - id: micro-behaviors/os/syscall/invoke::linux-kernel-source-context
