defaults:
  for: [c, cpp]
  platforms: [windows]

traits:
  - id: windows-tcp-socket-source
    desc: C source creates TCP socket
    crit: notable
    conf: 0.9
    if:
      type: text
      regex: 'socket\s*\(\s*AF_INET\s*,\s*SOCK_STREAM\s*,\s*IPPROTO_TCP\s*\)'
