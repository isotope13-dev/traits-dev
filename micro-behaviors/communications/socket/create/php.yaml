defaults:
  platforms: [windows, unix]
  for: [php]
traits:
  - id: php-socket-create
    desc: PHP socket_create function
    crit: notable
    conf: 0.85
    if:
      type: symbol
      kind: call
      exact: socket_create
