# PHP stream-context configuration for socket and stream operations.
defaults:
  platforms: [unix, windows]
  for: [php]

traits:
  - id: php-stream-context-create
    desc: Creates a PHP stream context
    crit: notable
    conf: 0.9
    if:
      type: symbol
      kind: call
      exact: stream_context_create
