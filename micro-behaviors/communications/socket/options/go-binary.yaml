defaults:
  platforms: [unix, windows]
  for: [elf, macho, pe]
  mbc: C0001

traits:
  - id: go-set-read-deadline-text
    desc: Go SetReadDeadline text
    crit: baseline
    conf: 0.82
    if:
      type: text
      substr: SetReadDeadline
