# Telnet - Remote Terminal Protocol
# Unencrypted remote connection tool
defaults:
  for: [all]
  crit: notable
  conf: 0.9
traits:
  - id: telnet
    desc: Telnet protocol reference
    if:
      type: string
      word: "telnet"
