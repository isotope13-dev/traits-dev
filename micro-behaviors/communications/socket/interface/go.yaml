defaults:
  platforms: [linux]
  for: [go]
  crit: notable
  conf: 0.9

traits:
  - id: go-bind-socket-to-device
    desc: Binds a Go socket to a device
    if:
      type: symbol
      kind: call
      exact: syscall.BindToDevice
