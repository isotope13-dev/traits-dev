defaults:
  platforms: [windows, unix]
  for: [go]

traits:
  - id: ntrip-client
    desc: NTRIP GNSS caster client protocol
    crit: notable
    conf: 0.95
    if:
      type: text
      regex: '(?i)\bNTRIP\b'
