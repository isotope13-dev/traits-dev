defaults:
  platforms: [windows, unix]
  for: [python]
  attack: T1071
  mbc: C0001

traits:
  - id: asyncio-open-connection-call
    desc: asyncio opens TCP connection
    crit: notable
    conf: 0.9
    if:
      type: symbol
      kind: call
      exact: asyncio.open_connection
