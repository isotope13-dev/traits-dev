defaults:
  platforms: [linux]
  for: [elf]

traits:
  - id: redkill-client-marker
    desc: RedKill custom command-channel protocol marker
    crit: notable
    conf: 0.98
    if:
      type: text
      exact: RedKill
