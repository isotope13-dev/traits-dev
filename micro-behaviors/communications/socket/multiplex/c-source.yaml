defaults:
  platforms: [windows, linux, macos]
  for: [c, cpp]

traits:
  - id: c-accepted-socket-selected
    desc: C select monitors accepted socket
    crit: notable
    conf: 0.88
    if:
      type: text
      regex: 'FD_SET\s*\(\s*\w+\s*,\s*&rset\s*\)'
