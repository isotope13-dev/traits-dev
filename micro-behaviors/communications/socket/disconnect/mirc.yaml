defaults:
  platforms: [unix, windows]
  crit: notable
  for: [mirc, ircii]

traits:
  # Relocated from objectives/command-and-control/channel/irc/client/.
  - id: mirc-sockclose
    desc: mIRC script closes a socket
    conf: 0.88
    if:
      type: text
      regex: '(?i)\bsockclose\b'
