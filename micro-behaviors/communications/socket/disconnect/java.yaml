defaults:
  platforms: [windows, unix]
  for: [class]
traits:
  - id: disconnect-reference
    desc: Disconnect operation reference
    crit: notable
    conf: 0.8
    if:
      type: text
      regex: (?i)(^|[^A-Za-z0-9])disconnect($|[^A-Za-z0-9])
