defaults:
  platforms: [windows, unix]
  for: [powershell]
traits:
  - id: tcp-client-connect
    desc: PowerShell TcpClient connection
    crit: baseline
    conf: 0.8
    if:
      type: text
      substr: System.Net.Sockets.TcpClient
