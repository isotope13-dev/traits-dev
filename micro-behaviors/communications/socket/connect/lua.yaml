defaults:
  platforms: [unix, ios]
  for: [lua]
  attack: T1071
  mbc: C0001
traits:
  - id: lua-socket-tcp
    desc: LuaSocket TCP socket creation
    crit: notable
    conf: 0.86
    if:
      type: symbol
      exact: socket.tcp
  - id: lua-method-connect
    desc: Lua socket connect method
    crit: component
    conf: 0.7
    if:
      type: text
      substr: :connect(
composite_rules:
  - id: lua-tcp-connect
    desc: Lua TCP socket connection
    crit: notable
    conf: 0.88
    all:
      - id: lua-socket-tcp
      - id: lua-method-connect
