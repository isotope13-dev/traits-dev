# Perl socket dial primitives.
defaults:
  for: [perl]
  platforms: [linux, macos, windows]
traits:
  - id: perl-sockaddr-in
    desc: Perl sockaddr_in marshalling
    crit: notable
    conf: 0.9
    if:
      type: symbol
      exact: sockaddr_in
