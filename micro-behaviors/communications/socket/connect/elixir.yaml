# Erlang/Elixir socket dial primitives.
defaults:
  for: [elixir]
  platforms: [linux, macos, windows]

traits:
  - id: gen-tcp-connect
    desc: Erlang gen_tcp connect call
    crit: notable
    conf: 0.95
    if:
      type: text
      regex: 'gen_tcp[:\.]connect'
