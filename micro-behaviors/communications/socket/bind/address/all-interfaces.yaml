defaults:
  platforms: [windows, unix]
  for: [scripts, binaries]
traits:
  - id: bind-all-interfaces
    desc: Bind to all network interfaces (0.0.0.0)
    crit: notable
    conf: 0.8
    if:
      type: text
      regex: '(?i)\b(bind|listen|host|addr)\b.{0,80}0\.0\.0\.0'
    not:
      - substr: "0.0.0.0/0"
    unless:
      - id: metadata/package/testing/harness/runtime::is-test
      - id: metadata/lang/compiler/managed::dotnet-ipv4-like-version
      - type: text
        regex: 'Version=\d+\.0\.0\.0'
      - type: text
        regex: '(?i)(fallback|falls back|default|return)\s+.{0,40}0\.0\.0\.0'
