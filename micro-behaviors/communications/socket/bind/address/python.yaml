defaults:
  platforms: [unix, windows]
  for: [python]
  crit: notable
  conf: 0.94

traits:
  - id: python-bind-all-interfaces
    desc: Python socket binds all interfaces
    if:
      type: text
      regex: '\b[A-Za-z_]\w*\.bind\(\s*\(\s*["'']0\.0\.0\.0["'']\s*,'
