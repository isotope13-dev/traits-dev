# Rust socket bind primitives.
defaults:
  for: [rust]
  platforms: [unix, windows, openwrt]

traits:
  - id: udpsocket-bind
    desc: Rust UdpSocket::bind call
    crit: notable
    conf: 0.85
    if:
      type: text
      regex: '\bUdpSocket::bind\s*\('
