defaults:
  platforms: [unix, windows]
  for: [python]

traits:
  - id: qtcpserver-listen
    desc: Python Qt TCP server listens for connections
    crit: notable
    conf: 0.96
    mbc: C0001
    if:
      type: text
      regex: 'QTcpServer\s*\(\)[\s\S]{0,900}\.listen\s*\('
