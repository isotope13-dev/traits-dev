defaults:
  platforms: [windows, unix]
  for: [python]
  attack: T1071
  mbc: C0001

traits:
  - id: asyncio-start-server-call
    desc: asyncio starts TCP server
    crit: notable
    conf: 0.9
    if:
      type: symbol
      kind: call
      exact: asyncio.start_server
