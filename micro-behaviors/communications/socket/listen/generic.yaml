defaults:
  platforms: [unix, windows]
  for: [javascript, python, c, cpp, go]

traits:
  - id: socket-listen-api
    desc: SocketListen call
    crit: notable
    conf: 0.85
    if:
      type: symbol
      kind: call
      exact: SocketListen
