defaults:
  platforms: [windows, linux, unix, android, macos, ios]
  for: [binaries, source]
traits:
  - id: send-call-raw
    desc: Raw socket send call
    crit: baseline
    if:
      type: raw
      regex: '(send|sendto|sendmsg)\s*\('
