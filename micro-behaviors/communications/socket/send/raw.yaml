defaults:
  platforms: [unix, windows]
  for: [c, cpp, go, rust]
traits:
  - id: send-call-raw
    desc: Raw socket send call
    crit: baseline
    if:
      type: text
      regex: '(send|sendto|sendmsg)\s*\('
