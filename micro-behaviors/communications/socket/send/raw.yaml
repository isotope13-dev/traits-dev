defaults:
  platforms: [unix, windows]
  for: [c, cpp, go, rust]
traits:
  - id: send-call-raw
    desc: Raw socket send call
    crit: notable
    if:
      type: text
      regex: '(^|[^A-Za-z0-9_])(send|sendto|sendmsg)\s*\('
