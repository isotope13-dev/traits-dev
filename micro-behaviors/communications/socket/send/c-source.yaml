defaults:
  for: [c, cpp]
  platforms: [windows]
  mbc: C0001

traits:
  - id: source-send-call
    desc: C source sends socket data
    crit: notable
    conf: 0.9
    if:
      type: symbol
      kind: call
      exact: send
