# Swift Network.framework socket send patterns
defaults:
  platforms: [macos, ios]
  for: [macho]
traits:
  - id: nwconnection-send
    desc: Swift NWConnection send call
    crit: notable
    conf: 0.85
    mbc: "C0001"
    attack: "T1071"
    if:
      type: symbol
      regex: "NWConnection.{0,80}send"
