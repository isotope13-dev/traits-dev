defaults:
  platforms: [qnx, unix]
  for: [c, cpp, elf]
  attack: T1041

traits:
  - id: qnx-native-raw-socket-send
    desc: QNX native raw socket send
    crit: suspicious
    conf: 0.9
    if:
      type: text
      regex: 'gethostbyname[\s\S]{0,520}send\(sock'
