defaults:
  platforms: [unix, ios]
  for: [lua]
  attack: T1071
  mbc: C0001

traits:
  - id: lua-socket-send
    desc: Lua socket send method
    crit: notable
    conf: 0.86
    if:
      type: text
      substr: :send(
