defaults:
  platforms:
    - unix
    - windows
  for:
    - binaries
    - c
    - cpp
traits:
  - id: sendto-struct-usage
    desc: sendto usage with struct sockaddr (common in
    crit: notable
    if:
      type: text
      regex: sendto\s*\(.{0,220}sizeof\s*\(.{0,220}struct\s+sockaddr
