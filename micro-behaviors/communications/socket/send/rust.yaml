defaults:
  platforms: [unix, windows]
  for: [rust]
  attack: T1071
  mbc: C0001
traits:
  - id: rust-write-all
    desc: Rust writes all bytes to stream
    crit: notable
    conf: 0.9
    if:
      type: text
      regex: '\bstream\.write_all\s*\('
