defaults:
  platforms: [windows]
  for: [binaries]

traits:
  - id: kernel-wsk-network-stack
    desc: Compatibility placeholder
    crit: component
    conf: 1.0
    if:
      type: text
      exact: __cleave_missing_kernel_wsk_network_stack__
