defaults:
  platforms: [windows, unix]
  for: [go]
  crit: notable
  conf: 0.92

traits:
  - id: request-response-message-handler
    desc: Go message handler returns response message
    if:
      type: text
      substr: 'handler func(*Message) *Message'
