defaults:
  platforms: [windows, unix]
  for: [javascript, typescript]
  crit: notable
  conf: 0.86
  attack: T1071
  mbc: C0001

traits:
  - id: javascript-socket-message-handler
    desc: JavaScript socket message handler
    if:
      type: text
      regex: '\b(socket|websocket|ws)\.onmessage\s*='
