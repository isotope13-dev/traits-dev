defaults:
  platforms: [unix, windows]
  for: [perl]
  attack: T1071
  mbc: C0001

traits:
  - id: perl-socket-recv
    desc: Perl socket recv into buffer
    crit: notable
    conf: 0.86
    if:
      type: text
      substr: 'recv($sock,'
