defaults:
  platforms: [unix, windows]
  for: [php]
  crit: notable
  conf: 0.9

traits:
  - id: php-stream-socket-crypto
    desc: PHP negotiates stream socket encryption
    if:
      type: symbol
      kind: call
      exact: stream_socket_enable_crypto
