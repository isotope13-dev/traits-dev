defaults:
  platforms: [unix]
  for: [c, elf]

composite_rules:
  - id: tls-library-io-api-pair
    desc: Multiple TLS library I/O API markers
    crit: notable
    conf: 0.8
    needs: 2
    any:
      - id: openssl-ssl-session-function
      - id: gnutls-record-send-recv-source
      - id: nspr-io-functions
