defaults:
  platforms: [windows, unix]
  for: [go]
  crit: notable
  conf: 0.94

traits:
  - id: icmp-channel-constructor
    desc: Go constructs an ICMP transport channel
    if:
      type: symbol
      kind: call
      exact: newICMPChannel
