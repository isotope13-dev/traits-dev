defaults:
  platforms: [unix, windows]
  for: [c, cpp]
  crit: notable
  conf: 0.85
  mbc: C0014

traits:
  - id: icmp-echo-api-constant
    desc: ICMP echo API constant
    if:
      type: text
      substr: ICMP_ECHO
    unless:
      - id: micro-behaviors/os/syscall/invoke::linux-kernel-source-context
