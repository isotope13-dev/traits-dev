defaults:
  platforms:
    - unix
    - windows
    - macos
  for: [scripts, binaries]
  crit: notable
  conf: 0.9
traits:
  - id: ping
    desc: ping command
    not:
      - regex: \bping(\s+-\w+\s+\S+){1,3}\s+127\.
      - regex: \bping(\s+-\w+\s+\S+){1,3}\s+localhost\b
    if:
      type: raw
      regex: \bping(\s+-\w+\s+\S+){1,3}\s+\S+
  - id: ping6
    desc: IPv6 ping command
    if:
      type: text
      word: ping6
  - id: fping
    desc: fping fast ping
    if:
      type: text
      word: fping
