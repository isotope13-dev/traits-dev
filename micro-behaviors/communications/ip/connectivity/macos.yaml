defaults:
  for: [macho, objc, swift]
  platforms: [macos]

traits:
  - id: macos-reachability-api
    desc: macOS network reachability API
    crit: notable
    conf: 0.88
    if:
      type: text
      substr: SCNetworkReachability
