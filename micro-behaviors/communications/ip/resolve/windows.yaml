defaults:
  platforms: [windows]
  for: [pe, dll]
traits:
  - id: resolve-gethostbyname-import
    desc: gethostbyname import string
    crit: baseline
    conf: 0.7
    if:
      type: string
      exact: "gethostbyname"
