defaults:
  platforms: [unix, openwrt]
  for: [zig]

traits:
  - id: zig-getaddrinfo
    desc: Zig getaddrinfo DNS resolution
    crit: notable
    conf: 0.9
    if:
      type: text
      word: getaddrinfo
