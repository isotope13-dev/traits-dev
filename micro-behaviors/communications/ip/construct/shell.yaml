defaults:
  platforms: [unix]
  for: [shell]

traits:
  - id: shell-random-ipv4-octet
    desc: Repeated random IPv4 octet arithmetic
    crit: notable
    conf: 0.94
    count_min: 3
    if:
      type: text
      regex: 'RANDOM\s*%\s*[0-9]{1,3}'
