defaults:
  platforms: [android, unix, windows]
  for: [java, kotlin]

traits:
  - id: java-four-part-ipv4-concat
    desc: Java constructs IPv4 from four parts
    crit: baseline
    conf: 0.9
    if:
      type: text
      regex: '\w+\[0\].{0,60}\w+\[1\].{0,60}\+\s*\w+\s*\+\s*"\."\s*\+\s*\w+'
