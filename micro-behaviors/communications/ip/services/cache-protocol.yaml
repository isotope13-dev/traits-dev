defaults:
  platforms: [windows, unix]
  for: [scripts, binaries]
  crit: notable
  conf: 0.85
traits:
  - id: redis-resp-ping-frame
    desc: Redis RESP-framed PING command
    conf: 0.9
    if:
      type: text
      substr: "*1\r\n$4\r\nPING\r\n"
  - id: redis-require-call
    desc: require('redis') call
    if:
      type: text
      regex: 'require\(["'']redis["'']\)'
  - id: redis-new-constructor
    desc: new Redis() constructor
    if:
      type: text
      regex: '\bnew\s+Redis\b'
    unless:
      - id: metadata/package/quality/recognized-contexts-extra::hashed-text-record
      - id: metadata/package/quality/recognized-contexts-extra::scarno-defensive-catalog-source
      - id: metadata/package/quality/recognized-contexts-extra::scarno-defensive-scanner-context
  - id: redis-createclient-6379
    desc: Redis default port 6379 in createClient
    if:
      type: text
      regex: 'createClient\s*\(\s*\{[^}]{0,200}6379'
  # Keep the product name case-sensitive.  SNMP MIB objects such as
  # `memCached` are ordinary monitoring vocabulary, not Memcached protocol
  # evidence; the previous case-insensitive matcher confused the two.
  - id: memcached-name
    desc: Memcached protocol client name
    conf: 0.8
    if:
      type: text
      word: memcached
    unless:
      - type: trait
        id: metadata/file/extension/identity::linux-kernel-package-artifact-basename
      - id: metadata/package/quality/recognized-contexts-extra::scarno-defensive-catalog-source
  - id: pymemcache-module
    desc: pymemcache client module name
    conf: 0.8
    if:
      type: text
      word: pymemcache
    unless:
      - type: trait
        id: metadata/file/extension/identity::linux-kernel-package-artifact-basename
      - id: metadata/package/quality/recognized-contexts-extra::scarno-defensive-catalog-source

  # Net-SNMP's public session/PDU symbols identify an SNMP protocol client;
  # unlike a generic socket import, these are useful capability evidence for
  # future network-tool and implant triage.
  - id: snmp-session-open
    desc: Opens an SNMP client session
    crit: notable
    conf: 0.9
    platforms: [unix]
    for: [elf, macho]
    if:
      type: symbol
      exact: snmp_open
  - id: snmp-request-send
    desc: Sends an SNMP request
    crit: notable
    conf: 0.9
    platforms: [unix]
    for: [elf, macho]
    if:
      type: symbol
      exact: snmp_send
  - id: snmp-synchronous-response
    desc: Waits for an SNMP response
    crit: notable
    conf: 0.9
    platforms: [unix]
    for: [elf, macho]
    if:
      type: symbol
      exact: snmp_synch_response
  - id: snmp-pdu-create
    desc: Creates an SNMP protocol data unit
    crit: notable
    conf: 0.9
    platforms: [unix]
    for: [elf, macho]
    if:
      type: symbol
      exact: snmp_pdu_create
composite_rules:
  - id: redis-ping-protocol
    desc: Redis client/protocol marker
    any:
      - id: redis-resp-ping-frame
      - id: redis-require-call
      - id: redis-new-constructor
      - id: redis-createclient-6379
