defaults:
  platforms: [windows, unix]
  for: [javascript, typescript]
  crit: baseline
  conf: 0.9

traits:
  - id: node-is-ip
    desc: Node.js validates an IP address value
    if:
      type: symbol
      kind: call
      exact: net.isIP
