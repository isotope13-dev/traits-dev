defaults:
  platforms: [windows, linux, macos]
  for: [python, pyc]
traits:
  - id: socket-inet-pton
    desc: Parse IP with socket.inet_pton
    crit: notable
    conf: 0.8
    if:
      type: symbol
      exact: socket.inet_pton
