# Go IP Parsing
# Neutral Go net package IP operations
defaults:
  platforms: [windows, unix]
  for: [go]
  crit: baseline
traits:
  - id: net-parseip
    desc: net.ParseIP() call
    conf: 0.5
    if:
      type: ast
      kind: call
      exact: "net.ParseIP"
