# DNS Cache Flush - Windows
defaults:
  for: [c, cpp, pe]
  platforms: [windows]
traits:
  - id: dns-flush-resolver-cache
    desc: DnsFlushResolverCache API call
    crit: notable
    conf: 0.85
    if:
      type: text
      word: "DnsFlushResolverCache"
