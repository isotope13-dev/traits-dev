defaults:
  platforms: [windows, unix]
  for: [scripts, pe, go, rust, package.json]
  crit: notable
traits:
  - id: eyes-sh-callback-domain
    desc: References eyes.sh callback infrastructure
    conf: 0.95
    if:
      type: text
      regex: '\b([a-z0-9-]+\.)?eyes\.sh\b'
