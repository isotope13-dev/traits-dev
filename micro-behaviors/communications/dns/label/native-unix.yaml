defaults:
  platforms: [unix]
  for: [c, cpp, objc, elf]

traits:
  - id: native-byte-subdomain-format
    desc: Native byte subdomain format
    crit: notable
    conf: 0.86
    if:
      type: text
      substr: '%c%c.%s'

