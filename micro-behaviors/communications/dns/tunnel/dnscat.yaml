defaults:
  platforms: [windows, unix]
  for: [python, javascript, powershell, pe, elf]

traits:
  - id: dnscat-established-session-state
    desc: dnscat established-session state
    crit: notable
    conf: 0.9
    if:
      type: text
      substr: SESSION_STATE_ESTABLISHED
