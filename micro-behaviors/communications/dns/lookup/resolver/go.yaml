defaults:
  platforms: [windows, unix]
  for: [go]
  attack: T1071.004
  mbc: C0011
  crit: notable
traits:
  - id: net-lookup-host
    desc: net.LookupHost DNS lookup
    conf: 0.85
    if:
      type: symbol
      exact: net.LookupHost
  - id: net-lookup-ip
    desc: net.LookupIP DNS lookup
    conf: 0.85
    if:
      type: symbol
      exact: net.LookupIP
  - id: net-lookup-addr
    desc: net.LookupAddr reverse DNS
    conf: 0.85
    if:
      type: symbol
      exact: net.LookupAddr
  - id: go-dns-client-lookup-ip
    desc: Go DNS client LookupIP call
    conf: 0.9
    if:
      type: symbol
      kind: call
      regex: '(?i)(client|resolver|dnsClient)\.LookupIP$'
composite_rules:
  - id: dns-lookup
    desc: Go DNS lookup capability
    crit: notable
    conf: 0.85
    any:
      - id: net-lookup-host
      - id: net-lookup-ip
      - id: net-lookup-addr
      - id: go-dns-client-lookup-ip
