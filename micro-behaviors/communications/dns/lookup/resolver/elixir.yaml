defaults:
  for: [elixir]
  platforms: [linux, macos, windows]
  crit: notable
  conf: 0.95

traits:
  - id: elixir-inet-getaddr
    desc: Elixir resolves host address
    if:
      type: symbol
      kind: call
      exact: :inet.getaddr
