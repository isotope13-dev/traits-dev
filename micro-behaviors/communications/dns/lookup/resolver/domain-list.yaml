defaults:
  platforms: [unix, windows]
  for: [javascript, typescript]

traits:
  - id: domain-enumeration-cli-marker
    desc: Domain-list resolver marker
    crit: notable
    conf: 0.99
    if:
      type: text
      substr: resolveDomainListFile
