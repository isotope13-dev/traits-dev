defaults:
  platforms: [windows, unix]
  for: [mirc, ircii]
  crit: notable
  conf: 0.9
traits:
  - id: mirc-dns-command
    desc: mIRC dns command resolves a name
    if:
      type: text
      regex: '(?i)(^|[\s;{|=])/?\.?dns\s+\$[0-9]'
