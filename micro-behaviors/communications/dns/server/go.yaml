defaults:
  platforms: [windows, unix]
  for: [go]
  attack: T1071.004
  mbc: C0011

traits:
  - id: go-dns-server-listen
    desc: Go DNS server ListenAndServe call
    crit: notable
    conf: 0.9
    if:
      type: text
      regex: '\bdns\.Server\s*\{[\s\S]{0,240}\bListenAndServe\s*\('
