# Siemens S7comm / ISO-TSAP Protocol (TCP port 102)
# Neutral protocol references — intent inferred at objectives tier.
defaults:
  for: [binaries, scripts]
  platforms: [windows, linux]
  crit: notable
  conf: 0.85

traits:
  - id: keyword
    desc: S7comm/Siemens protocol keyword
    if:
      type: text
      regex: (?i)\b(s7comm|s7[-_]?protocol)\b

  - id: step7-keyword
    desc: Siemens Step 7 software reference
    if:
      type: text
      regex: (?i)\b(?:Siemens|SIMATIC|S7)\b.{0,40}\bStep\s?7\b|\bStep\s?7\b.{0,40}\b(?:Siemens|SIMATIC|S7)\b

  - id: library
    desc: S7 library or module reference
    conf: 0.88
    if:
      type: text
      regex: (?i)(snap7|python-snap7|libnodave|s7[-_]?client)

  - id: port-102
    desc: S7comm TCP port 102 context
    crit: component
    conf: 0.7
    if:
      type: text
      regex: (?i)(s7|siemens|iso|connect).{0,20}\b102\b

composite_rules:
  - id: protocol-usage
    desc: S7comm protocol usage indicators
    crit: notable
    conf: 0.88
    any:
      - id: keyword
      - id: step7-keyword
      - id: library
