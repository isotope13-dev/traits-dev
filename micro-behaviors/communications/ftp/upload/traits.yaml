defaults:
  platforms: [windows, unix]
  for: [php, c, cpp, pe]
traits:
  - id: ftp-upload-symbol
    desc: FTP upload symbol
    crit: notable
    conf: 0.7
    if:
      type: symbol
      regex: ftp_put|^STOR$|ftp_upload
