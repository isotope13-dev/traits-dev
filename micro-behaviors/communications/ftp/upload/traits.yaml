# FTP Upload Patterns
defaults:
  platforms: [linux, unix, android, macos, ios, windows]
  for: [scripts, binaries]
traits:
  - id: ftp-upload-symbol
    desc: FTP upload symbol
    crit: notable
    conf: 0.7
    if:
      type: symbol
      regex: ftp_put|^STOR$|ftp_upload
