defaults:
  platforms: [unix, appliance]
  for: [shell]
  crit: notable
  conf: 0.92
  attack: T1105

traits:
  - id: tftp-download-to-temp
    desc: TFTP downloads into temporary storage
    if:
      type: text
      regex: '\btftp\b[^\n]{0,160}\s-l\s+["'']?/(tmp|var/tmp)/'
