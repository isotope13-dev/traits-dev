defaults:
  platforms: [linux]
  for: [elf]
  attack: T1105

traits:
  - id: tftp-elf-reference
    desc: ELF embeds BusyBox tftp invocation
    crit: notable
    conf: 0.92
    if:
      type: text
      regex: '\bbusybox\s+tftp\b'
