defaults:
  platforms: [windows, unix]
  for: [go]
  crit: notable
  conf: 0.9

traits:
  - id: go-url-parse
    desc: Parses a URL with net/url
    crit: notable
    conf: 0.9
    if:
      type: symbol
      exact: url.Parse
