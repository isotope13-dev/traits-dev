defaults:
  platforms:
    - unix
    - windows
    - android
  for: [scripts, binaries]
traits:
  - id: ipfs-str
    desc: IPFS string reference
    crit: component
    if:
      type: text
      word: ipfs
