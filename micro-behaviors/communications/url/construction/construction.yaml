defaults:
  platforms:
    - unix
    - windows
    - macos
  for: [ruby]
traits:
  - id: ruby-iplogger-url
    desc: Detects 'iplogger.org' URL construction
    crit: suspicious
    if:
      type: string_literal
      substr: iplogger.org
