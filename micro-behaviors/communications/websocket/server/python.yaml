defaults:
  platforms: [windows, unix]
  for: [python]
  attack: T1071.001
  mbc: C0002

traits:
  - id: websockets-asyncio-serve-call
    desc: Python starts WebSocket server
    crit: notable
    conf: 0.94
    if:
      type: symbol
      kind: call
      exact: websockets.asyncio.server.serve
