defaults:
  platforms: [unix, windows]
  for: [javascript, typescript]
  crit: notable
  conf: 0.9
  attack: T1071.001

traits:
  - id: websocket-message-event-handler
    desc: WebSocket message event handler
    if:
      type: text
      regex: '\b(ws|websocket|wsserver|wss)\s*\.\s*on\s*\(\s*["'']message["'']'
