defaults:
  platforms: [windows, unix]
  for: [python]
  attack: T1071.001
  mbc: C0002

traits:
  - id: terminal-ws-path
    desc: Terminal WebSocket endpoint path
    crit: notable
    conf: 0.82
    if:
      type: text
      substr: "/terminal/ws"
