defaults:
  platforms: [android]
  for: [apk, java, kotlin, xml]

traits:
  - id: local-websocket-endpoint
    desc: Local WebSocket endpoint reference
    crit: notable
    conf: 0.9
    if:
      type: text
      regex: 'wss?://(127\.0\.0\.1|localhost)(:[0-9]{1,5})?'

  - id: android-wss-endpoint
    desc: Android encrypted WebSocket endpoint
    crit: notable
    conf: 0.9
    if:
      type: text
      substr: wss://
