defaults:
  platforms: [windows, unix]
  for: [javascript, typescript]
  crit: notable
  conf: 0.82
traits:
  - id: managesieve-availability-message
    desc: References a ManageSieve availability message
    if:
      type: text
      regex: SERVICE CURRENTLY NOT AVAILABLE|ManageSieve
