defaults:
  platforms: [unix, windows]
  for: [scripts, source]

traits:
  - id: smtp-recipient-address-field
    desc: SMTP recipient envelope or message field
    crit: notable
    conf: 0.98
    if:
      type: text
      regex: '(?i)(RCPT\s+TO\s*:\s*<|Cc\s*:)[^\r\n]{0,160}@[A-Za-z0-9.-]+'
