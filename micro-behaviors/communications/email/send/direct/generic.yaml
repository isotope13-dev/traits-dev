defaults:
  platforms: [unix, windows]
  for: [scripts, source]

traits:
  - id: smtp-recipient-address-field
    desc: SMTP RCPT TO recipient address
    crit: notable
    conf: 0.98
    if:
      type: text
      regex: '(?i)(^|[\r\n]|["''])RCPT\s+TO\s*:\s*<[^@\r\n]{1,160}@[\w.-]+\b'
