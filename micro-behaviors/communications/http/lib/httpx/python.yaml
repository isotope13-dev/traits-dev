defaults:
  platforms:
    - linux
    - macos
    - windows
    - unix
    - android
    - ios
  for:
    - python
traits:
  - id: httpx-get
    desc: httpx.get call
    crit: notable
    conf: 0.7
    if:
      type: string_literal
      substr: httpx.get
  - id: httpx-post
    desc: httpx.post call
    crit: notable
    conf: 0.7
    if:
      type: string_literal
      substr: httpx.post
