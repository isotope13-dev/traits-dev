# Nominatim (OpenStreetMap) reverse geocoding API
# Used by legitimate mapping apps and C2 panels for victim location enrichment
defaults:
  for: [php, javascript, python, ruby, shell]
  platforms: [windows, unix]
  crit: notable
  conf: 0.8
  attack: T1082
traits:
  - id: nominatim-reverse
    desc: Nominatim reverse geocoding API call
    if:
      type: text
      substr: nominatim.openstreetmap.org/reverse
