defaults:
  platforms: [unix, windows]
  for: [java, class, groovy, kotlin, scala]

traits:
  - id: jetty-server-package
    desc: Jetty server framework package reference
    crit: notable
    conf: 0.85
    if:
      type: text
      regex: '(?i)\borg[./](eclipse|mortbay)[./]jetty\b'
