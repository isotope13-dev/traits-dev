defaults:
  platforms: [windows, unix]
  for: [python]
  crit: notable
  conf: 0.92

traits:
  - id: permission-guard-reference
    desc: FastAPI permission guard reference
    if:
      type: text
      regex: '\bPermissionGuard\b|\brequire_permission\s*\('
