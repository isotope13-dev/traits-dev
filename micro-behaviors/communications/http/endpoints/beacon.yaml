defaults:
  platforms: [unix, windows, appliance]
  for: [rust, java, lua, perl, csharp, shell, javascript, pe]
  crit: component
  conf: 0.86

traits:
- id: beacon-path-token
  desc: HTTP beacon endpoint path token
  if:
    type: text
    substr: /beacon
