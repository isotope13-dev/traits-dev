# Stable services embedded in the malicious YanZhiTi PyPI repack. They remain
# network IOCs here; objective composites establish what data is sent to them.
defaults:
  platforms: [unix, windows]
  for: [python]
  crit: notable
  conf: 1.0

traits:
  - id: replacement-telemetry-endpoint
    desc: YanZhiTi replacement telemetry endpoint
    if:
      type: literal
      kind: string
      exact: https://yanzhitielemetry-sukzkbfzhc.ap-southeast-1.fcapp.run

  - id: plaintext-activation-endpoint
    desc: YanZhiTi plaintext activation endpoint
    if:
      type: literal
      kind: string
      exact: http://124.71.10.117:9090
