defaults:
  platforms:
    - unix
    - windows
  for: [scripts, binaries]
traits:
  - id: endpoint
    desc: "OpenAI Chat Completions API endpoint"
    crit: notable
    conf: 1.0
    if:
      type: text
      substr: api.openai.com/v1/chat/completions
