defaults:
  for: [all]

traits:
  - id: endpoint
    desc: Detects the OpenAI Chat Completions API endpoint
    crit: notable
    conf: 1.0
    if:
      type: string
      substr: "api.openai.com/v1/chat/completions"
