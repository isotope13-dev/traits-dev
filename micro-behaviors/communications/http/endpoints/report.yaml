defaults:
  platforms: [unix, windows]
  for: [scripts, binaries]

traits:
  - id: api-report-path
    desc: HTTP /api/report endpoint path
    crit: notable
    conf: 0.85
    if:
      type: text
      substr: /api/report
