defaults:
  platforms: [unix, windows]
  for: [scripts, binaries]
traits:
  - id: google-connectivity-probe-host
    desc: Google connectivity probe hostname
    crit: notable
    conf: 0.78
    if:
      type: text
      substr: www.google.com
