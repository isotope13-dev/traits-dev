defaults:
  platforms: [unix, windows]
  for: [scripts, binaries]
traits:
  - id: google-web-host
    desc: Google web hostname
    crit: notable
    conf: 0.78
    if:
      type: text
      substr: www.google.com
