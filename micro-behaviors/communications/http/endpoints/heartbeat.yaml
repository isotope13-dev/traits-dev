defaults:
  platforms: [unix, windows]
  for: [java, kotlin, class, jar]
  crit: notable
  conf: 0.9

traits:
  - id: http-heartbeat-endpoint
    desc: HTTP heartbeat endpoint string
    if:
      type: literal
      kind: string
      regex: '/api/(v[0-9]{1,2}/)?heartbeat'
