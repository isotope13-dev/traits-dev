defaults:
  platforms: [unix, windows]
  for: [python, javascript, php, ruby, go, java]
traits:
- id: health-check-path
  desc: HTTP health-check endpoint path
  crit: notable
  conf: 0.9
  if:
    type: text
    substr: /api/health
