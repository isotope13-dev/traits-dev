defaults:
  platforms: [unix, windows]
  for: [scripts, source]
traits:
- id: health-check-path
  desc: HTTP health-check endpoint path
  crit: notable
  conf: 0.9
  if:
    type: text
    substr: /api/health
