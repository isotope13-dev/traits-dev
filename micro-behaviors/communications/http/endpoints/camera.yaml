defaults:
  platforms: [unix, windows]
  for: [go]

traits:
  - id: camera-snapshot-cgi-endpoint
    desc: Camera snapshot CGI endpoint path
    crit: notable
    conf: 0.94
    if:
      type: literal
      kind: string
      substr: snapshot.cgi
