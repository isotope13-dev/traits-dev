defaults:
  platforms: [windows]
  for: [ruby]
  attack: T1041

traits:
  - id: ruby-http-chunk-headers
    desc: Ruby HTTP chunk headers
    crit: suspicious
    conf: 0.9
    if:
      type: text
      regex: 'X-Chunk-ID[\s\S]{0,180}X-Chunk-Index[\s\S]{0,180}X-Chunk-Total'
