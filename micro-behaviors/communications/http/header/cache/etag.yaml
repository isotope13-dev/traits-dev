defaults:
  platforms: [windows]
  for: [c, cpp, pe, dll]
traits:
  - id: etag-header-line
    desc: HTTP ETag cache-validator header
    crit: notable
    conf: 0.8
    if:
      type: text
      regex: '(?i)(^|[^A-Za-z0-9_])ETag\s*:'
