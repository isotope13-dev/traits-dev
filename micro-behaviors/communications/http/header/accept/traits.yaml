defaults:
  platforms: [windows, unix]
  for: [scripts, source]
  attack: T1071.001
  mbc: C0002

traits:
  - id: accept-json-header
    desc: Requests a JSON HTTP response
    crit: notable
    conf: 0.95
    if:
      type: text
      regex: '\bAccept["'']?\s*[:=]\s*["'']?application/json\b'
      case_insensitive: true
