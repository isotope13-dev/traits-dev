defaults:
  platforms: [unix, windows]
  for: [c, cpp, elf, rust, go]
  attack: T1071.001

traits:
  - id: native-json-content-type
    desc: Native HTTP JSON content type
    crit: notable
    conf: 0.9
    if:
      type: text
      regex: '(?i)Content-Type[^\r\n]{0,80}application/json'
