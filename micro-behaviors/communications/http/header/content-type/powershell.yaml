defaults:
  platforms: [windows, unix]
  for: [powershell]

traits:
  - id: zip-content-type
    desc: HTTP application/zip content type
    crit: notable
    conf: 0.86
    if:
      type: text
      substr: application/zip
