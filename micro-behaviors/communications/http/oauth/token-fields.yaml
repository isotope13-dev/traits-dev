defaults:
  platforms:
  - windows
  for:
  - pe
traits:
- id: access-token-field
  desc: access_token OAuth field name string
  crit: component
  conf: 0.9
  if:
    type: text
    exact: access_token
    section: rdata
- id: device-token-field
  desc: device_token OAuth field name string
  crit: component
  conf: 0.9
  if:
    type: text
    exact: device_token
    section: rdata
- id: bearer-token-field
  desc: bearer_token OAuth field name string
  crit: component
  conf: 0.9
  if:
    type: text
    exact: bearer_token
    section: rdata
- id: new-device-token-field
  desc: newdevicetoken OAuth field name string
  crit: component
  conf: 0.9
  if:
    type: text
    exact: newdevicetoken
    section: rdata
- id: device-token-variant
  desc: devicetoken OAuth field name string
  crit: component
  conf: 0.9
  if:
    type: text
    exact: devicetoken
    section: rdata
