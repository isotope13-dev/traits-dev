defaults:
  platforms: [windows, unix]
  for: [javascript, typescript]
  crit: notable
  conf: 0.95

traits:
  - id: url-authorization-code-read
    desc: Reads URL authorization code parameter
    if:
      type: text
      regex: '\.searchParams\.get\s*\(\s*[''"]code[''"]\s*\)'
