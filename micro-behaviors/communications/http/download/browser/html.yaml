defaults:
  platforms: [windows, unix]
  for: [html]
  crit: notable
  conf: 0.82

traits:
  - id: html-anchor-download-attribute
    desc: HTML anchor download attribute
    if:
      type: text
      regex: '<a[^>]{0,120}\bdownload\b'
      case_insensitive: true
