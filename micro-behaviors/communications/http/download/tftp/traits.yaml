defaults:
  platforms:
    - unix
    - windows
    - macos
  for: [scripts, binaries]
  attack: T1105
traits:
  - id: command
    desc: tftp command invocation
    crit: notable
    conf: 0.5
    if:
      type: raw
      regex: '\btftp\s+-[glr]\b'
    unless:
      - type: raw
        substr: tftp error msg
      - type: raw
        substr: tftp rrq file
