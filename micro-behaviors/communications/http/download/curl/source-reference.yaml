defaults:
  platforms: [linux, macos, windows]
  for: [scripts, source, class, java_class]
  crit: notable

traits:
  - id: curl-fail-silent-url-reference
    desc: References curl flags and HTTP URL
    if:
      type: text
      regex: 'curl\s+-fsSL\s+https?://'
