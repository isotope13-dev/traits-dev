defaults:
  platforms: [windows]
  for: [batch]
  attack: T1105

traits:
  - id: batch-curl-call
    desc: Batch calls curl
    crit: component
    conf: 0.9
    if:
      type: symbol
      kind: call
      exact: curl
