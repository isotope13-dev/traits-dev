defaults:
  platforms:
    - unix
    - windows
  for: [scripts, binaries]
traits:
  - id: generic-dup
    desc: Function to handle remote file
    crit: notable
    conf: 0.8
    if:
      type: text
      substr: handleFileDownload
