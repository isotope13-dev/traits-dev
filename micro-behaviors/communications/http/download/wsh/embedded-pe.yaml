defaults:
  platforms: [windows]

traits:
  - id: pe-embedded-downloadfile-helper
    desc: Embedded PE script defines a DownloadFile helper
    crit: notable
    conf: 0.9
    for: [pe]
    if:
      type: raw
      regex: '(?is)\bFunction\s+DownloadFile\s*\(\s*[^)]{0,120}\)'
