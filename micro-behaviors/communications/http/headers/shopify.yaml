defaults:
  platforms: [linux, macos, windows]
  for: [javascript, typescript]

traits:
  - id: shopify-access-token-header
    desc: Shopify access-token HTTP header
    crit: notable
    conf: 0.95
    if:
      type: literal
      regex: '^X-Shopify-(Access|Storefront-Access)-Token$'
