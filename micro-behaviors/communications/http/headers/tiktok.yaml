defaults:
  platforms: [windows, unix]
  for: [javascript, typescript, scripts, binaries]
traits:
- id: x-tt-params-header
  desc: Uses TikTok x-tt-params request header
  crit: notable
  conf: 0.9
  if:
    type: text
    substr: x-tt-params
