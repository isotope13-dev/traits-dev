defaults:
  platforms: [unix, windows, macos]
  crit: notable
  conf: 0.7
  for: [shell, python, javascript, powershell, pe, elf]
traits:
  - id: delete
    desc: HTTP DELETE method
    if:
      type: string
      regex: '^DELETE\\s+\\S+\\s+HTTP/1\\.[01]$'
      case_insensitive: false
