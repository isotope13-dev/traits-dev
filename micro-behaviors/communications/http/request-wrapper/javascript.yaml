defaults:
  platforms: [windows, unix]
  for: [javascript, typescript]
  attack: T1105
  mbc: C0002

traits:
  - id: forwarded-fetch-url-options
    desc: Fetch forwarded with URL and options
    crit: notable
    conf: 0.9
    if:
      type: text
      regex: '\(\s*fetch\s*,\s*\w+\s*,\s*\{\s*method\s*:'
