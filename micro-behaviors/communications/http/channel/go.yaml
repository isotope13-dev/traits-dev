defaults:
  platforms: [windows, unix]
  for: [go]
  crit: notable
  conf: 0.94

traits:
  - id: http-channel-constructor
    desc: Go constructs an HTTP transport channel
    if:
      type: symbol
      kind: call
      exact: newHTTPChannel
