defaults:
  platforms: [unix, windows]
  for: [javascript, typescript]
traits:
  - id: new-url-endpoint
    desc: Builds URL from endpoint
    crit: notable
    conf: 0.84
    if:
      type: text
      substr: new URL(endpoint)