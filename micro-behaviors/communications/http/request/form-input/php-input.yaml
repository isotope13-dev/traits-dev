defaults:
  platforms: [windows, unix]
  for:
    - php
  crit: notable
traits:
  - id: php-input
    desc: Accesses raw PHP request body
    conf: 0.95
    unless:
      - id: well-known/app/recognized-contexts/fp::php-empty-function-declarations
      - id: well-known/app/recognized-contexts/fp::php-manual-empty-function-stubs
    if:
      type: string_literal
      substr: php://input
