defaults:
  platforms: [unix]
  for: [elf]
  attack: T1071.001
  mbc: C0002

traits:
  - id: fixed-random-data-form-body
    desc: HTTP form body sends fixed random_data
    crit: notable
    conf: 0.95
    if:
      type: text
      exact: data=random_data
      section: .rodata
