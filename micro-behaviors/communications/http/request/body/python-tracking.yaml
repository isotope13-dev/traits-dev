defaults:
  crit: notable
  conf: 0.94
  platforms: [unix, windows]
  for: [python]

traits:
  - id: tracking-metrics-json-payload
    desc: Serializes collected metrics as tracking JSON
    if:
      type: text
      regex: 'tracking\.json[\s\S]{0,180}dumps\s*\(\s*get_track_metrics\s*\('
