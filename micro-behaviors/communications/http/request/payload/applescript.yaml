defaults:
  platforms: [macos]
  for: [applescript]
  attack: T1071.001

traits:
  - id: applescript-host-user-form-fields
    desc: AppleScript builds host and user form fields
    crit: notable
    conf: 0.98
    if:
      type: text
      regex: '["'']host=["'']\s*&\s*\w+\s*&\s*["'']&user=["'']\s*&\s*\w+'
  - id: applescript-env-form-field
    desc: AppleScript builds env form field
    crit: notable
    conf: 0.98
    if:
      type: text
      regex: '["'']&env=["'']\s*&\s*\w+'
