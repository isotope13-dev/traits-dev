defaults:
  platforms: [android]
  for: [java, kotlin]
  attack: T1071.001
  mbc: C0002

traits:
  - id: jvm-dynamic-url-construction
    desc: JVM URL concatenates runtime data
    crit: notable
    conf: 0.9
    if:
      type: text
      regex: '\b(new\s+)?URL\s*\([^\r\n)]{1,240}\+'
