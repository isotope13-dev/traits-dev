defaults:
  platforms: [unix, windows]
  for: [package.json]

traits:
  - id: npm-postinstall-https-get
    desc: npm postinstall calls Node HTTPS GET
    crit: notable
    conf: 0.92
    if:
      type: value
      path: scripts.postinstall
      regex: "require\\(['\"]https['\"]\\)\\.get\\s*\\("
