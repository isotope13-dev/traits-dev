defaults:
  platforms: [unix, android, ios]
  for: [shell]

traits:
  - id: shell-http-endpoint-variable
    desc: Shell HTTP endpoint variable
    crit: notable
    conf: 0.82
    if:
      type: text
      regex: "(?im)^\\s*(c2|server|callback|beacon)\\s*=\\s*[\"']?https?://"
