defaults:
  platforms: [unix, windows]
  for: [javascript, typescript]

traits:
  - id: node-http-client-get
    desc: Node HTTP client invokes get()
    crit: notable
    conf: 0.75
    if:
      type: symbol
      kind: call
      regex: '^(http|https|client)[.]get$'
