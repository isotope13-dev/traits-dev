defaults:
  crit: notable
  platforms: [unix, windows]
  for: [javascript, typescript]
  attack: T1071.001
  mbc: C0002

traits:
  - id: get-with-params
    desc: HTTP GET with query parameters
    conf: 0.8
    if:
      type: string_literal
      regex: path:\s*[`"'][^`"']{0,220}\$\{
  - id: fetch-token-query-parameter
    desc: Fetch call with token query parameter
    conf: 0.9
    if:
      type: text
      regex: '(?is)\bfetch\s*\([^)]{0,600}\?token='
