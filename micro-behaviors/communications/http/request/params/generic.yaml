defaults:
  platforms: [windows, unix]
  for: [scripts, binaries]
  mbc: C0002
traits:
- id: payload-param
  desc: payload request parameter
  crit: component
  conf: 0.7
  if:
    type: text
    regex: '\bpayload='
