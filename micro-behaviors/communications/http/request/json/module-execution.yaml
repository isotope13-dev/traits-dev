defaults:
  platforms: [unix, windows]
  for: [javascript, typescript]

traits:
  - id: module-execution-http-call
    desc: Module initialization calls an HTTP client
    crit: notable
    conf: 0.95
    if:
      type: value
      path: source.execution.module_http
      exact: 'true'
