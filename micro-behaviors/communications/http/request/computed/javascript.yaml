defaults:
  platforms: [unix, windows]
  for: [javascript, typescript]

traits:
  - id: js-computed-https-client-call
    desc: Calls HTTPS through a computed property
    crit: notable
    conf: 0.86
    if:
      type: text
      regex: '\bhttps\s*\[.{1,80}\]\s*\('
