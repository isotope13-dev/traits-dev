defaults:
  platforms: [windows, unix]
  for: [html]
  attack: T1071.001
  mbc: C0002

traits:
  - id: html-fetch-call
    desc: Calls fetch API from HTML script
    crit: notable
    conf: 0.8
    if:
      type: text
      regex: '(?i)\bfetch\s*\('
