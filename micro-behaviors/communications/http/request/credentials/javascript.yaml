defaults:
  platforms:
    - unix
    - windows
  for: [javascript, typescript]
  attack: T1071.001
  mbc: C0002
traits:
  - id: fetch-credentials-include
    desc: Browser fetch includes credentials
    crit: notable
    conf: 0.86
    if:
      type: text
      substr: 'credentials:"include"'
