defaults:
  platforms: [windows]
  for: [pe]
traits:
  - id: content-range-bytes-format
    desc: HTTP Content-Range bytes format string
    crit: notable
    conf: 0.88
    attack: T1071.001
    if:
      type: text
      exact: "Content-Range: bytes %lu-%lu/%lu"
