# Async I/O libraries and OS-level I/O multiplexing (libev, libuv, c-ares,
# libevent, boost::asio, tokio, epoll, kqueue) moved to:
#   micro-behaviors/communications/async-io/traits.yaml
defaults:
  platforms:
    - linux
    - unix
    - android
    - macos
    - ios
    - windows
  for:
    - elf
    - macho
traits: []
composite_rules: []
