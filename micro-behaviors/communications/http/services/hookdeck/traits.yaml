defaults:
  platforms: [windows, unix]
  for: [scripts, source]
  mbc: C0002
  attack: T1071.001

traits:
  - id: hookdeck-webhook-host
    desc: Hookdeck webhook relay host
    crit: notable
    conf: 0.92
    if:
      type: text
      regex: '(?i)(^|[^a-z0-9])(hookdeck\.com|hkdk\.events)([^a-z0-9]|$)'
