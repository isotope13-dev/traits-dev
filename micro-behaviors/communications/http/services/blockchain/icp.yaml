defaults:
  platforms: [unix, windows]
  for: [scripts, binaries]
  crit: notable
  conf: 0.9
  attack: T1071.001
  mbc: C0002

traits:
  - id: icp-canister-http-endpoint
    desc: Internet Computer canister HTTP endpoint
    if:
      type: text
      regex: '\b[a-z0-9-]{5,80}\.raw\.icp[0-9]+\.io\b'
