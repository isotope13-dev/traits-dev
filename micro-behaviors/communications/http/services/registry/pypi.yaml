defaults:
  platforms: [unix, windows]
  for: [scripts]

traits:
  - id: package-json-endpoint
    desc: PyPI package JSON endpoint reference
    crit: notable
    conf: 0.85
    if:
      type: string_literal
      regex: 'https?://(www\.)?pypi\.org/pypi/[^\s''"]{1,80}/json$'
