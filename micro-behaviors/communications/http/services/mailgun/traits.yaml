defaults:
  platforms: [unix, windows]
  for: [python, javascript, typescript, go, php, powershell]
  crit: notable
  conf: 0.9

traits:
  - id: mailgun-api-domain
    desc: References the Mailgun API hostname
    if:
      type: text
      substr: api.mailgun.net
