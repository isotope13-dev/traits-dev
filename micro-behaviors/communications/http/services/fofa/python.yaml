defaults:
  platforms: [windows, unix]
  for: [python]

traits:
  - id: search-api-endpoint
    desc: Connects to the FOFA asset-search API
    crit: notable
    conf: 0.98
    if:
      type: text
      substr: https://fofa.info/api/v1/search/all
