defaults:
  platforms: [linux, macos, windows]
  for: [javascript, typescript]

traits:
  - id: global-discovery-instances-route
    desc: Dataverse Global Discovery API route
    crit: notable
    conf: 0.96
    if:
      type: text
      substr: /api/discovery/v2.0/Instances
