defaults:
  platforms:
    - unix
    - windows
  for: [scripts, binaries]
traits:
  - id: gh-issue-mgmt
    desc: GitHub issue management
    crit: notable
    if:
      type: raw
      regex: gh (issue|label|repo|pr)\b
