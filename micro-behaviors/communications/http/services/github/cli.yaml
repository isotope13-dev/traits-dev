defaults:
  platforms:
    - unix
    - windows
  for: [scripts, binaries]
traits:
  - id: gh-issue-mgmt
    desc: GitHub issue management
    crit: notable
    unless:
      - type: basename
        regex: '\.typ$'
    if:
      type: text
      regex: gh (issue|label|repo|pr)\b
