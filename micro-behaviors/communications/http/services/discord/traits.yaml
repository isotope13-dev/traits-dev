defaults:
  platforms: [windows, macos, linux]
  for: [javascript, typescript, python, pe, elf, macho]
  crit: notable
  conf: 0.8

traits:
  - id: invites-api-endpoint
    desc: Discord invites API endpoint
    if:
      type: text
      substr: /api/v10/invites/
