defaults:
  platforms: [unix, windows]
  for: [scripts, source]
  crit: notable
  conf: 0.94

traits:
  - id: user-profile-endpoint
    desc: SendGrid authenticated user-profile API endpoint
    if:
      type: text
      regex: 'https?://api\.sendgrid\.com/v3/user/profile\b'
