defaults:
  platforms: [unix, windows]
  for: [python]
  crit: notable
  conf: 0.9
  mbc: C0002

traits:
  - id: ssm-with-decryption-parameter
    desc: AWS SSM decryption flag parameter
    crit: notable
    if:
      type: literal
      exact: WithDecryption
