defaults:
  platforms: [windows, unix]
  for: [javascript, typescript, python, powershell]
  crit: notable
  conf: 0.94

traits:
  - id: git-items-api-url
    desc: References the Azure DevOps Git Items API
    if:
      type: text
      regex: 'dev\.azure\.com/[^\s"'']{1,160}/_apis/git/repositories/'
