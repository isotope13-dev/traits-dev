defaults:
  platforms: [windows, unix]
  for: [markdown, text]

traits:
  - id: notion-create-comment-operation
    desc: References a Notion comment creation operation
    crit: notable
    conf: 0.95
    if:
      type: text
      substr: notion-create-comment
