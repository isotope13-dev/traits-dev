defaults:
  for: [pe, dll]
traits:
  - id: system-info-url-query
    desc: System info URL query (OS, MAC, DiskNum)
    crit: suspicious
    conf: 0.95
    if:
      type: string
      regex: "OS=.{0,50}&MAC=.{0,50}&DiskNum=.{0,50}"
