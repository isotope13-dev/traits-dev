defaults:
  platforms: [unix, windows]
  for: [ruby]
  crit: notable
  conf: 0.75

traits:
  - id: rack-env-http-cookie-key
    desc: Rack env HTTP_COOKIE key access
    if:
      type: text
      regex: (?i)\[\s*['"](HTTP_COOKIE|http_cookie)['"]\s*\]
