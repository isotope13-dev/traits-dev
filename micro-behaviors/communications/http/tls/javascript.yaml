defaults:
  platforms: [windows, unix]
  for: [javascript, typescript]
  mbc: C0005

traits:
  - id: tls-connect-servername
    desc: tls.connect specifies SNI servername
    crit: notable
    conf: 0.88
    if:
      type: text
      regex: 'tls\.connect\s*\(\s*\{[^}\n]{0,400}servername\s*:'
