defaults:
  platforms: [linux, macos, windows, unix, android, ios]
traits:
  - id: ws-dynamic-import
    desc: "Dynamically imports the 'ws' WebSocket module"
    conf: 0.8
    crit: notable
    for: [javascript, typescript]
    if:
      type: text
      regex: 'import\s*\(\s*("ws"|''ws'')\s*\)'
