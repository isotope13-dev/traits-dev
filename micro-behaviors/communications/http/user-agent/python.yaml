defaults:
  platforms: [unix, windows]
  for: [python]
  crit: suspicious
  conf: 0.96

traits:
  - id: curl-cffi-chrome-impersonation
    desc: Impersonates Chrome with curl_cffi
    if:
      type: text
      regex: 'impersonate\s*=\s*["'']chrome["'']'
