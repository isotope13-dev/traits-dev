defaults:
  platforms: [unix, windows]
  for: [elf, macho, pe, dll]
  crit: notable
  conf: 0.95

traits:
  - id: qnetworkaccessmanager-get
    desc: Qt QNetworkAccessManager GET request
    if:
      type: symbol
      substr: QNetworkAccessManager3get
