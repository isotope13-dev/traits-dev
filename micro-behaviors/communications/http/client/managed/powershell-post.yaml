defaults:
  platforms: [windows, unix, appliance]
  for: [powershell]
  attack: T1041

traits:
  - id: ps-httpwebrequest-post
    desc: PowerShell HttpWebRequest POST
    crit: suspicious
    conf: 0.92
    if:
      type: text
      regex: 'HttpWebRequest[\s\S]{0,180}\.Method = "POST"'
