defaults:
  platforms: [windows]
  for: [lua]
  attack: T1041

traits:
  - id: lua-webclient-upload-command
    desc: Lua WebClient upload command
    crit: suspicious
    conf: 0.93
    if:
      type: literal
      regex: 'Upload(Data|String)\("http://'
