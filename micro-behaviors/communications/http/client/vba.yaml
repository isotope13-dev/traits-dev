defaults:
  for:
    - vbs
  platforms:
    - windows
  crit: notable
  conf: 0.88
traits:
  - id: xmlhttp-object
    desc: XMLHTTP COM object instantiation
    if:
      type: string_value
      regex: (?i)MSXML2\.XMLHTTP|Microsoft\.XMLHTTP|ServerXMLHTTP
  - id: urldownloadtofile-api
    desc: URLDownloadToFile API call
    crit: suspicious
    conf: 0.93
    if:
      type: string_value
      regex: (?i)URLDownloadToFile
  - id: internetopen-api
    desc: WinInet InternetOpen API call
    if:
      type: string_value
      regex: (?i)InternetOpen
  - id: http-open-send
    desc: HTTP Open and Send method sequence
    conf: 0.85
    if:
      type: raw
      regex: (?is)\.Open\s+"GET".{0,200}\.Send
composite_rules:
  - id: vba-http-download-to-file
    desc: VBA HTTP download to local file
    crit: suspicious
    conf: 0.95
    all:
      - id: xmlhttp-object
    any:
      - id: metadata/lang::adodb-stream
      - id: micro-behaviors/fs/write/vba::vba-open-for-output
