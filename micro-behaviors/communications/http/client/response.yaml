defaults:
  platforms: [windows, unix]
traits:
  - id: response-text
    desc: Access to HTTP response body (.responseText)
    crit: notable
    conf: 0.7
    for: [powershell, javascript]
    if:
      type: text
      regex: '(?i)\.responseText'
    unless:
      - type: text
        substr: "@externs"
