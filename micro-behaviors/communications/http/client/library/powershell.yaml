defaults:
  platforms: [windows, unix]
  for: [powershell]
traits:
  - id: invoke-webrequest
    desc: PowerShell Invoke-WebRequest
    crit: baseline
    conf: 0.8
    if:
      type: text
      word: Invoke-WebRequest
