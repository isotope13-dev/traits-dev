defaults:
  platforms: [unix, windows]
  for: [java, class, kotlin]
traits:
  - id: java-http-url-connection
    desc: Java/Kotlin HttpURLConnection usage
    crit: notable
    conf: 0.8
    if:
      type: text
      regex: '\bHttps?URLConnection\b'
