defaults:
  platforms: [unix, windows]
  for: [java, class, kotlin]
traits:
  - id: java-http-url-connection
    desc: Java HttpURLConnection usage
    crit: baseline
    conf: 0.8
    if:
      type: text
      word: HttpURLConnection
