defaults:
  platforms: [windows]
  for: [java, class, java_class, jar]
  attack: T1041

traits:
  - id: jvm-archive-upload-call
    desc: JVM archive upload call
    crit: notable
    conf: 0.96
    for: [class]
    if:
      type: import
      exact: postFile
