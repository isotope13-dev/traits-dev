defaults:
  platforms: [windows, unix]
  for: [go, pe, elf, macho]
  mbc: C0002
  attack: T1071.001

traits:
  - id: go-http3-client-transport
    desc: Go configures an HTTP/3 client transport
    crit: notable
    conf: 0.98
    if:
      type: text
      regex: '(?m)\bhttp3\.(Transport|RoundTripper)\s*\{'
