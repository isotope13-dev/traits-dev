defaults:
  platforms: [unix, windows]
  for: [python]
  crit: notable
  conf: 0.96

traits:
  - id: curl-cffi-import
    desc: Imports the curl_cffi Python client
    if:
      type: import
      exact: curl_cffi
