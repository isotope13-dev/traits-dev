defaults:
  platforms: [unix, windows]
  for: [python]
traits:
  - id: urllib-request
    desc: Python urllib.request usage
    crit: baseline
    conf: 0.8
    if:
      type: text
      word: urllib.request
