defaults:
  platforms: [windows, unix]
  for: [javascript, typescript]
  crit: notable
  conf: 0.8

traits:
  - id: eventsource-client
    desc: JavaScript Server-Sent Events client
    if:
      type: text
      word: EventSource
