# Axios HTTP HEAD
defaults:
  for: [javascript, typescript]
  platforms: [unix, windows]
  crit: notable
  attack: T1071.001
traits:
  - id: axios-head-call
    desc: "axios.head() HTTP HEAD request"
    conf: 0.8
    if:
      type: tree-sitter
      kind: call
      exact: axios.head
