defaults:
  platforms:
    - windows
    - linux
    - macos
  for: [binaries, c, cpp, go, java]
  crit: notable
  conf: 0.75
traits:
  - id: http2-preface
    desc: HTTP/2 client connection preface
    if:
      type: text
      substr: PRI * HTTP/2.0
