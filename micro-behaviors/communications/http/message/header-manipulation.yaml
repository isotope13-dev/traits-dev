defaults:
  platforms: [windows, unix]
traits:
  - id: header-stringify-scan
    desc: "Converting HTTP headers to string for scanning"
    crit: notable
    conf: 0.8
    for: [javascript, typescript]
    if:
      type: text
      regex: 'JSON\.stringify\s*\(\s*headers\s*\)'
