defaults:
  platforms: [windows, unix]
  for: [go]
  mbc: C0002

traits:
  - id: go-webhook-fire-call
    desc: Go webhook notification dispatch
    crit: notable
    conf: 0.86
    if:
      type: symbol
      kind: call
      regex: '(^|\.)[A-Za-z_$][A-Za-z0-9_$]*\.Webhook\.Fire$'
