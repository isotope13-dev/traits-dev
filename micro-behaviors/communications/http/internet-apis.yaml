defaults:
  for: [pe]
  platforms: [windows]
traits:
  - id: has-internet-communication-apis
    desc: Uses internet communication API imports
    crit: baseline
    conf: 0.8
    if:
      type: symbol
      regex: "^(InternetOpen[AW]|HttpOpenRequest[AW]|WSAStartup)$"
