defaults:
  platforms: [unix, windows]
  for: [javascript, typescript]
  attack: T1090
  mbc: E1102

traits:
  - id: rotating-http-proxy-pool
    desc: Configures a rotating proxy pool
    crit: notable
    conf: 0.96
    count_min: 2
    if:
      type: text
      regex: '(?i)(getProxyPool|resolveExtractorProxy|pr[''"]\s*\+\s*[''"]ox[''"]\s*\+\s*[''"]y)'
