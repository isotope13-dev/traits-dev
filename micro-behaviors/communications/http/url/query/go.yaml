defaults:
  platforms: [unix]
  for: [elf]

traits:
  - id: go-cache-bust-symbol
    desc: Go cache-busting URL query routine symbol
    crit: notable
    conf: 0.95
    if:
      type: text
      regex: '\bmain\.[a-z]{0,8}[Cc]ache[Bb]ust\b'
      section: .gopclntab
