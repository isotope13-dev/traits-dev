defaults:
  platforms: [windows, unix]
  for: [php]

traits:
  - id: php-request-host-script-url
    desc: PHP request host and script path
    crit: notable
    conf: 0.9
    if:
      type: text
      regex: 'HTTP_HOST[\s\S]{0,160}PHP_SELF'
