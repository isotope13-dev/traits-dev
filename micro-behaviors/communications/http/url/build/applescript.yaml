defaults:
  for: [applescript]
  platforms: [macos]
  crit: component
  conf: 0.75

traits:
  - id: url-variable
    crit: notable
    desc: URL variable name
    if:
      type: symbol
      regex: "(?i)^url$"

  - id: server-variable
    crit: notable
    desc: Server variable name
    if:
      type: symbol
      regex: "(?i)^server$"
