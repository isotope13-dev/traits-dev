defaults:
  platforms: [windows]
  for: [vbs]
  crit: notable
  conf: 0.9

traits:
  - id: vbs-encoded-http-url
    desc: HTTP URL recovered from encoded VBScript content
    if:
      type: encoded
      encoding: base64
      regex: '(?i)https?://[A-Za-z0-9._:/?&=%+~-]{3,253}'
