defaults:
  platforms: [windows]
  for: [vbs]
  crit: notable
  conf: 0.9

traits:
  - id: vbs-encoded-http-url
    desc: VBScript encoded URL
    if:
      type: text
      regex: '(?i)https?://[A-Za-z0-9._:/?&=%+~-]{3,253}'
