defaults:
  platforms: [windows]
  for: [lua]
  attack: T1041

traits:
  - id: lua-creds-api-endpoint
    desc: Lua credentials API endpoint
    crit: notable
    conf: 0.9
    if:
      type: literal
      exact: /api/creds
