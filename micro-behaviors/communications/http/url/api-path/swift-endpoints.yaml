defaults:
  platforms: [windows, unix]
  for: [swift]
  attack: T1041

traits:
  - id: swift-publish-endpoint
    desc: Swift publish endpoint
    crit: notable
    conf: 0.74
    if:
      type: text
      substr: /publish
  - id: swift-messages-endpoint
    desc: Swift messages endpoint
    crit: notable
    conf: 0.74
    if:
      type: text
      substr: /messages
