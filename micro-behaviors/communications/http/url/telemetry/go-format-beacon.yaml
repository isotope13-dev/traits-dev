defaults:
  platforms: [panos, appliance, unix]
  for: [go, elf]
  attack: T1041

traits:
  - id: go-panos-http-beacon-post
    desc: Go PAN-OS HTTP beacon post
    crit: suspicious
    conf: 0.94
    if:
      type: text
      substr: http://%s:8080/beacon
