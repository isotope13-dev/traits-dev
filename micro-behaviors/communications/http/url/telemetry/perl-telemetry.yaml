defaults:
  platforms: [qnx, unix]
  for: [perl]
  attack: T1041

traits:
  - id: qnx-perl-telemetry-post
    desc: QNX Perl telemetry POST
    crit: suspicious
    conf: 0.94
    if:
      type: text
      substr: POST /api/v1/telemetry HTTP/1.1
