defaults:
  platforms: [windows, unix]
  for: [javascript, typescript]
  crit: notable
  conf: 0.92

traits:
  - id: config-domain-gates-current-host
    desc: Gates page action by configured domains
    if:
      type: text
      regex: '\w+\(window\.location\.hostname,\s*config\.\w*[Dd]omains\)'
