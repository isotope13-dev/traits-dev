defaults:
  platforms: [unix, windows]
  for: [scripts, binaries]
traits:
  - id: http-cookiejar
    desc: http.cookiejar module
    crit: notable
    conf: 0.5
    if:
      type: text
      substr: http.cookiejar
