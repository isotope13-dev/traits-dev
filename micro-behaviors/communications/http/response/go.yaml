defaults:
  platforms: [unix, windows]
  for: [go, source]
  crit: notable
  conf: 0.92

traits:
  - id: go-http-redirect-call
    desc: Go http.Redirect HTTP response call
    if:
      type: text
      regex: '(?i)\bhttp\.Redirect\s*\('
