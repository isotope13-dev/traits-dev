# Axios HTTP PATCH
defaults:
  for: [javascript, typescript]
  platforms: [unix, windows]
  crit: notable
  attack: T1071.001
traits:
  - id: axios-patch-call
    desc: "axios.patch() HTTP PATCH request"
    conf: 0.8
    if:
      type: symbol
      kind: call
      exact: axios.patch
