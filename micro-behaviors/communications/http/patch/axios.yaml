# Axios HTTP PATCH
defaults:
  for: [javascript, typescript]
  platforms: [unix, windows, macos]
  crit: notable
  attack: T1071.001
traits:
  - id: axios-patch-call
    desc: "axios.patch() HTTP PATCH request"
    conf: 0.8
    if:
      type: ast
      kind: call
      exact: axios.patch
