defaults:
  platforms: [linux, macos, windows]
  for: [scripts, source]
  crit: notable
  conf: 0.86

traits:
  - id: megabyte-buffer-allocation
    desc: Source allocates a megabyte-sized buffer
    if:
      type: text
      regex: '(1\s*(<<|<<<)\s*20|1\s*-shl\s*20|1048576|1024\s*[*]\s*1024)'
