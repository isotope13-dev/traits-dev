defaults:
  platforms: [windows]
  for: [python]
  attack: T1055

traits:
  - id: python-heapalloc-call
    desc: Python calls the Windows HeapAlloc API
    crit: notable
    conf: 0.95
    if:
      type: text
      regex: '(?i)\b[A-Za-z_]\w*\.HeapAlloc\s*\('
