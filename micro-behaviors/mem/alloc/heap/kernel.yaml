defaults:
  platforms: [linux]
  for: [elf, c, cpp]
  crit: notable
  conf: 0.9

traits:
  - id: kernel-heap-free
    desc: Linux kernel heap memory deallocation
    if:
      type: symbol
      kind: call
      exact: kfree
