defaults:
  platforms: [windows]
  for: [pe, dll, c, cpp, csharp, go, rust, powershell]

traits:
  - id: sec-image-mapping-flag
    desc: Uses Windows SEC_IMAGE mapping flag
    crit: notable
    conf: 0.9
    if:
      type: text
      word: SEC_IMAGE
    unless:
      - id: metadata/library/generated::winmd-metadata
