# Cross-process memory read - Windows native source
defaults:
  for: [c, cpp, rust]
  platforms: [windows]
traits:
  - id: read-process-memory-source
    desc: ReadProcessMemory cross-process read
    crit: notable
    conf: 0.9
    mbc: "C0008"
    if:
      type: symbol
      exact: "ReadProcessMemory"
