defaults:
  platforms: [windows]
  for: [oledoc, ole]
  crit: baseline
  conf: 0.9

traits:
  - id: msi-embedded-virtual-alloc-ex
    desc: MSI embedded VirtualAllocEx reference
    attack: T1055
    if:
      type: raw
      substr: VirtualAllocEx
