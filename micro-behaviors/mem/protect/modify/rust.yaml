defaults:
  platforms: [unix]
  for: [rust]
  mbc: C0021

traits:
  - id: rust-libc-mprotect-call
    desc: Rust calls libc mprotect
    crit: notable
    conf: 0.92
    if:
      type: text
      substr: libc::mprotect
