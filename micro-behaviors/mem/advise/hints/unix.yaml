# Memory Advisory - Unix
traits:
  - id: madvise
    desc: Memory advisory hints (madvise)
    crit: baseline
    conf: 1.0
    platforms: [linux, macos]
    for: [elf, macho, c]
    if:
      type: symbol
      regex: "^_?madvise$"
