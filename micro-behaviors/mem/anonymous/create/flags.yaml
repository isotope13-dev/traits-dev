defaults:
  platforms: [linux]
  for: [python, c, cpp, rust, go, elf]

traits:
  - id: memfd-cloexec-flag
    desc: References MFD_CLOEXEC memfd flag
    crit: notable
    conf: 0.85
    if:
      type: string_literal
      substr: MFD_CLOEXEC
