# Managed-to-unmanaged memory copies in C#.
defaults:
  platforms: [windows]
  for: [csharp]

traits:
  - id: marshal-copy-csharp
    desc: C# copies bytes through Marshal.Copy
    crit: notable
    conf: 0.95
    attack: T1620
    if:
      type: symbol
      kind: call
      exact: Marshal.Copy
