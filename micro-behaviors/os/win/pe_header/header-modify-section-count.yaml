defaults:
  for: [all]

traits:
  - id: header-modify-section-count
    conf: 0.9
    crit: suspicious
    desc: Modifies NumberOfSections in PE header (often for
    if:
      type: raw
      regex: '(inc|add|mov) word ptr \[(e[abcd]x|e[sd]i)\+6\],? ?([0-9a-fA-F]+h?)?'
