defaults:
  platforms: [unix, windows]
  for: [c, cpp, pe, elf, shell]
traits:
  - id: tunneler-keyword
    desc: Tunneling terminology in source
    crit: notable
    conf: 0.85
    count_min: 2
    if:
      type: text
      substr: tunneler
  - id: list-of-lists
    desc: DOS List of Lists via AH=52h
    crit: notable
    conf: 0.92
    if:
      type: text
      regex: '(?i)mov\s+ah,52'
  - id: trap-flag-hide
    desc: Hides trap flag from pushf
    crit: suspicious
    conf: 0.9
    count_min: 2
    if:
      type: text
      regex: '(?i)\bnot\s+100h\b'
  - id: mcb-boundary-var
    desc: DOS Memory Control Block variable
    crit: notable
    conf: 0.88
    if:
      type: text
      substr: firstMCB
  - id: tunneled-int21-result
    desc: Stores tunneled INT 21h address
    crit: suspicious
    conf: 0.92
    if:
      type: text
      regex: '(saved21_CS|\btunnel_cs\b)'
  - id: x86-opcode-table
    desc: x86 opcode disassembly jump table
    crit: suspicious
    conf: 0.9
    size_min: 5000
    count_min: 20
    if:
      type: text
      regex: '(?i)db\s+ModRM'
