defaults:
  platforms: [windows]
  for: [pe]
traits:
  - id: int-21h-inline-asm
    desc: Inline assembly call to DOS Interrupt 21h
    crit: suspicious
    conf: 0.95
    if:
      type: text
      regex: '(?i)\b(__asm|asm)\b[^\r\n;]{0,40}\bint\s+(0x21|21h)\b'
