defaults:
  platforms: [unix, windows]
  for: [java]

traits:
  - id: java-current-user-property
    desc: Java reads the current user name
    crit: notable
    conf: 0.97
    if:
      type: symbol
      kind: call
      exact: getProperty
      arg:
        kind: string
        exact: user.name
