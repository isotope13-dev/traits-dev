defaults:
  platforms: [macos]
  for: [scripts, binaries]

traits:
  - id: macos-list-local-accounts
    desc: Lists or reads macOS local user accounts
    crit: notable
    conf: 0.9
    attack: T1087.001
    if:
      type: text
      regex: '(?i)dscl\s+\.\s+-(list|read)\s+/Users\b'
