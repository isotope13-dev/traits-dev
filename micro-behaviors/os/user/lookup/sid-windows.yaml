defaults:
  platforms: [windows]
  for: [pe, dll]
  crit: notable
  conf: 0.85

traits:
  - id: sid-string-conversion
    desc: ConvertSidToStringSidW API symbol
    if:
      type: symbol
      exact: ConvertSidToStringSidW
