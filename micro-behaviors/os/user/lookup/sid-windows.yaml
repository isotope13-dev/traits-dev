defaults:
  platforms: [windows]
  for: [pe, dll]
  crit: notable
  conf: 0.85

traits:
  - id: sid-string-conversion
    desc: Convert Windows SID to string
    if:
      type: symbol
      exact: ConvertSidToStringSidW
