# Windows Account Lookup APIs
defaults:
  platforms: [windows]
  for: [pe]
  crit: notable
  conf: 0.8
traits:
  - id: lookup-account-name
    desc: Resolve account name to SID
    if:
      type: symbol
      regex: "^LookupAccountName[AW]$"
