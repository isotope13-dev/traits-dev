defaults:
  for:
    - shell
    - elf
  platforms:
    - linux
traits:
  - id: usermod
    desc: usermod command
    crit: notable
    conf: 0.8
    if:
      type: text
      regex: '(?m)(^|[\n;&|`$({])\s*(/usr/sbin/)?usermod\s+-[A-Za-z]'
