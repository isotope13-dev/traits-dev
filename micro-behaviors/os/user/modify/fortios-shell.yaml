defaults:
  platforms: [fortios, unix]
  for: [shell]
  crit: notable
  conf: 0.95

traits:
  - id: fortios-admin-config-mode
    desc: Enters FortiOS administrator configuration
    if:
      type: text
      substr: config system admin
