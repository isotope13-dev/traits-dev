defaults:
  platforms: [windows, unix]
  for: [javascript, typescript]

traits:
  - id: administrator-role-field
    desc: Sets an administrator role field
    crit: notable
    conf: 0.9
    if:
      type: text
      regex: '\broles?["''\s:=\[]{1,6}administrator\b'
