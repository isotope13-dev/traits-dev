defaults:
  platforms: [routeros, appliance, unix]
  for: [text, shell, python, javascript]
  attack: T1552

traits:
- id: routeros-user-database-read
  desc: RouterOS user database read
  crit: suspicious
  conf: 0.93
  if:
    type: text
    regex: '(?m)^[ \t\[]*/user[ \t]+(print|get|find|count)\b'
