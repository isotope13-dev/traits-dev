defaults:
  platforms: [unix, windows]
  for: [go]

traits:
  - id: go-panic-call
    desc: Invokes Go panic builtin
    crit: baseline
    conf: 0.98
    if:
      type: symbol
      kind: call
      exact: panic
