defaults:
  for: [go]
  platforms: [linux, macos, windows]

traits:
  - id: go-empty-recover
    desc: Suppresses Go runtime panics
    crit: suspicious
    conf: 0.95
    if:
      type: text
      regex: 'recover\(\) != nil \{\s*\}'