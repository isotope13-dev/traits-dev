defaults:
  platforms: [unix]
  for: [c, cpp]

traits:
  - id: errno-esrch-assignment
    desc: Sets errno to no-such-process
    crit: notable
    conf: 0.9
    if:
      type: text
      regex: '\berrno\s*=\s*ESRCH\s*;'
