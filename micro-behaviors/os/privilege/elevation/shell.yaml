# Neutral shell privilege-elevation command invocation. A bare `sudo`/`doas`
# call is a common, legitimate capability — intent is inferred by composites
# (e.g. sudo inside a PKGBUILD build() function, which runs in fakeroot and
# should never need real elevation).
defaults:
  platforms: [unix]
  for: [shell]

traits:
  - id: sudo-command-invoke
    desc: Invokes sudo or doas to elevate
    crit: notable
    conf: 0.7
    # A REQUIRED preceding command separator, deliberately with no `^`
    # alternative: the text corpus also serves parser string literals that
    # begin at `sudo`, so a `^` anchor matched inside quotes and made every
    # script that merely PRINTS an install hint (`echo 'sudo snap install …'`,
    # usage/README text) a privilege-elevation finding.  Requiring a real
    # newline/;/&/| in front cannot be satisfied by a fragment that starts at
    # the keyword, while every genuine invocation — line-leading, `&& sudo`,
    # `; sudo`, `| sudo` — still matches. Require a command-like first
    # argument so menu labels such as `sudo "Grant sudo access"` do not fire.
    if:
      type: text
      regex: '[\n;&|][ \t]*(sudo|doas)[ \t]+[-A-Za-z0-9_./$]'
    unless:
      - id: metadata/lang/source::linux-kernel-secondary-tree-path
      - id: micro-behaviors/os/syscall/invoke::linux-kernel-tree-path-scripts
      - id: micro-behaviors/os/syscall/invoke::linux-kernel-tree-path-extra
      - id: micro-behaviors/os/syscall/invoke::linux-kernel-tree-path-mmfs
      - id: metadata/lang/source::linux-kernel-primary-tree-path
  - id: sudo-curl-privileged-path
    desc: sudo curl writes into privileged path
    crit: notable
    conf: 0.88
    if:
      type: text
      regex: 'sudo\s+curl\s+.{0,160}(-o|--output)\s+/(etc|usr|var)/'
  - id: sudo-wget-privileged-path
    desc: sudo wget writes into privileged path
    crit: notable
    conf: 0.88
    if:
      type: text
      regex: 'sudo\s+wget\s+.{0,160}(-O|--output-document)[ =]+/(etc|usr|var)/'
  - id: sudo-chmod-executable
    desc: sudo chmod grants executable permissions
    crit: notable
    conf: 0.86
    if:
      type: text
      regex: 'sudo\s+chmod\s+(7[0-7]{2}|\+x)\s+'
    not:
      - substr: sudo chmod 777
