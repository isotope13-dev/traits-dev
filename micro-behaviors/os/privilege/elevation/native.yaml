defaults:
  platforms: [unix]
  for: [binaries]
  crit: notable

traits:
  - id: sudo-binary-path-reference
    desc: References the sudo binary path
    conf: 0.82
    if:
      type: text
      exact: /usr/bin/sudo
