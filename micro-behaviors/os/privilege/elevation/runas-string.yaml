defaults:
  platforms: [windows]
  for: [pe, dll, scripts]
traits:
  - id: runas-string
    desc: runas privilege elevation string
    crit: notable
    conf: 0.7
    if:
      type: text
      word: runas
      case_insensitive: true
