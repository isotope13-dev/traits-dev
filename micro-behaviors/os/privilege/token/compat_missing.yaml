defaults:
  platforms: [windows]
  for: [binaries]

traits:
  - id: adjust-token-privileges-text
    desc: Compatibility placeholder
    crit: component
    conf: 1.0
    if:
      type: text
      exact: __cleave_missing_adjust_token_privileges_text__
  - id: nsis7z-dll-basename
    desc: Compatibility placeholder
    crit: component
    conf: 1.0
    if:
      type: text
      exact: __cleave_missing_nsis7z_dll_basename__
  - id: process-token-privilege-setup
    desc: Compatibility placeholder
    crit: component
    conf: 1.0
    if:
      type: text
      exact: __cleave_missing_process_token_privilege_setup__
