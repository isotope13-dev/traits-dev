defaults:
  for: [pe, dll]
  platforms: [windows]
composite_rules:
  - id: token-privilege-api-usage
    desc: Token privilege API usage detected
    crit: baseline
    conf: 0.9
    any:
      - id: open-process-token
      - id: open-thread-token
      - id: get-token-information
