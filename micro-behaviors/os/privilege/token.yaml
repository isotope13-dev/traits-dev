# Token Query APIs — Windows
# Neutral token/privilege query APIs
defaults:
  platforms: [windows]
  for: [pe, dll]
  crit: notable
traits:
  - id: get-token-information
    desc: GetTokenInformation API
    conf: 0.8
    if:
      type: symbol
      exact: "GetTokenInformation"
