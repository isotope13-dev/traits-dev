defaults:
  platforms: [unix]
  for: [perl]

traits:
  - id: perl-effective-uid-root-check
    desc: Perl checks for effective root UID
    crit: notable
    conf: 0.9
    if:
      type: text
      regex: '\$>\s*(==|!=)\s*0'
