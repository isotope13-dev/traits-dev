defaults:
  platforms: [windows]
  for: [go]
  crit: notable
  conf: 0.9

traits:
  - id: physical-drive-admin-probe
    desc: Opens physical drive to test admin access
    if:
      type: string_literal
      regex: '\\\\.\\PHYSICALDRIVE[0-9]+'
