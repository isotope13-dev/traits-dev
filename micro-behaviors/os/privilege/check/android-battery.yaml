defaults:
  platforms: [android]
  for: [apk, zip]
  crit: component
  conf: 0.84

traits:
  - id: android-oppo-battery-settings-layout
    desc: Android OPPO battery settings prompt
    if:
      type: raw
      substr: "res/layout/oppobattery.xml"
      offset_range: [-262144, null]
