defaults:
  platforms: [windows]
  for: [java, class, java_class]
  crit: notable
  conf: 0.9

traits:
  - id: jvm-is-admin-method
    desc: JVM isAdmin method reference
    if:
      type: import
      exact: isAdmin
