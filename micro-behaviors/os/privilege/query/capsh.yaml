defaults:
  platforms: [unix]
  for: [shell]
  crit: notable
  conf: 0.9
  mbc: B0046

traits:
  - id: capsh-capability-inspection
    desc: Inspects Linux capabilities with capsh
    if:
      type: text
      regex: '(?m)^\s*([A-Za-z_][A-Za-z0-9_]*\s*=\s*\$\(\s*)?capsh\s+--print\b'
