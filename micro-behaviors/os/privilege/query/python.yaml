# Process Privilege Query (Python)
defaults:
  platforms: [windows, unix]
  for: [python]
  crit: baseline
traits:
  - id: os-getuid-dup
    desc: os.getuid attribute
    conf: 0.6
    if:
      type: text
      substr: "os.getuid"
