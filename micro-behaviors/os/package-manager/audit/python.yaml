defaults:
  platforms: [unix, windows]
  for: [python]

traits:
  - id: pip-audit-command
    desc: Invokes pip-audit vulnerability scanner
    crit: notable
    conf: 0.98
    if:
      type: string_literal
      exact: pip-audit
