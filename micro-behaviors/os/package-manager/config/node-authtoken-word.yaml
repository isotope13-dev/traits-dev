defaults:
  platforms: [unix, windows]
  for: [javascript, typescript]
  attack: T1041

traits:
  - id: node-auth-token-token
    desc: Node auth token field
    crit: notable
    conf: 0.9
    if:
      type: text
      word: _authToken
