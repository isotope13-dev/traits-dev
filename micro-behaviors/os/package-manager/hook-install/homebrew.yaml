defaults:
  platforms: [macos]
  for: [ruby]
  crit: notable
  conf: 0.84

traits:
  - id: cask-postflight-command
    desc: Homebrew Cask postflight command
    if:
      type: text
      regex: 'postflight\s+do[\s\S]{0,200}\b(system_command|system|exec)\b'
