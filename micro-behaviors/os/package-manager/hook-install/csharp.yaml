defaults:
  platforms: [windows]
  for: [csharp]
traits:
  - id: csharp-beforeinstall-hook
    desc: C# BeforeInstall lifecycle callback
    crit: notable
    conf: 0.96
    if:
      type: text
      substr: project.BeforeInstall +=
