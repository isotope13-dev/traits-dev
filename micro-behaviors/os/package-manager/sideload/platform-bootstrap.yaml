defaults:
  platforms: [unix, windows]
  for: [javascript]

traits:
  - id: platform-binary-bootstrap-reference
    desc: References platform binary bootstrap helper
    crit: notable
    conf: 0.94
    if:
      type: text
      regex: '(?is)\bplatform-bootstrap\.mjs\b[\s\S]{0,320}\bensure(Platform|Native)Binary\b'
