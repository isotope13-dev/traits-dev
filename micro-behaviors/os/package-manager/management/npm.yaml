defaults:
  platforms: [unix, windows]
  for: [javascript, typescript]
  crit: notable
  conf: 0.95

traits:
  - id: npm-maintainer-package-enumeration
    desc: Enumerates npm packages by maintainer
    if:
      type: text
      substr: getPackagesByMaintainer
