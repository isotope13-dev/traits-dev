defaults:
  platforms: [android]
  for: [dex]
  crit: component
  conf: 0.92

traits:
  - id: android-package-archive-inspection
    desc: Reads Android package archive metadata
    if:
      type: text
      exact: getPackageArchiveInfo
