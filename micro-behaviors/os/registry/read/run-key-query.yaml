defaults:
  platforms: [windows]
  for: [ruby]
  attack: T1041

traits:
  - id: ruby-registry-runkey-query
    desc: Ruby registry Run key query
    crit: suspicious
    conf: 0.9
    if:
      type: text
      regex: 'reg query[\s\S]{0,240}CurrentVersion\\\\Run'
