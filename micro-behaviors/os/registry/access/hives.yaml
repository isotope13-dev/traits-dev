defaults:
  crit: baseline
  conf: 0.4
  platforms: [windows]
  for: [scripts, binaries]
traits:
  - id: hklm-hive
    desc: HKLM registry hive reference
    if:
      type: text
      exact: HKLM
  - id: hkcu-hive
    desc: HKCU registry hive reference
    if:
      type: text
      exact: HKCU
