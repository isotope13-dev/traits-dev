defaults:
  platforms: [windows]
  for: [pe, dll]
  crit: notable
traits:
  - id: xor-registry-value-write-api
    desc: XOR-encoded registry value-write API
    conf: 0.96
    if:
      type: encoded
      encoding: xor
      regex: '^RegSet(ValueEx|KeyValue)[AW]$'
