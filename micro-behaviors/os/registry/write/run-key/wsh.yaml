defaults:
  platforms: [windows]
  for: [vbs]
  attack: T1112
  mbc: C0036
traits:
  - id: wsh-regwrite-run-key
    desc: WScript.Shell regwrite to Run key
    crit: suspicious
    conf: 0.88
    if:
      type: text
      word: CurrentVersion\Run
