defaults:
  platforms: [windows]
  for: [vbs]
  attack: T1112
  mbc: C0036
composite_rules:
  - id: wsh-persistence-run-key
    desc: WSH persistence via Run key
    crit: suspicious
    conf: 0.95
    all:
      - id: micro-behaviors/os/registry/keys::currentversion-run
      - id: micro-behaviors/os/registry/write/dword::wsh-regwrite-dword
