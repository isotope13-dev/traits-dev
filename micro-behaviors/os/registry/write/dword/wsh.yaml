defaults:
  platforms: [windows]
  for: [vbs]
  attack: T1112
  mbc: C0036
traits:
  - id: wsh-regwrite-dword
    desc: WScript.Shell RegWrite with REG_DWORD
    crit: notable
    conf: 0.9
    if:
      type: text
      substr: REG_DWORD
