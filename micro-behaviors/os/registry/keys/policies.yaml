defaults:
  platforms: [windows]
traits:
  - id: disable-taskmgr
    desc: Disable Task Manager registry policy
    crit: suspicious
    conf: 0.95
    if:
      type: text
      word: DisableTaskMgr
