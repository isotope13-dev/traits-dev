defaults:
  platforms: [windows]
  for: [pe, dll, batch, powershell, vbs, javascript, csharp]
traits:
  - id: disable-taskmgr
    desc: Disable Task Manager registry policy
    crit: component
    conf: 0.95
    if:
      type: text
      word: DisableTaskMgr
