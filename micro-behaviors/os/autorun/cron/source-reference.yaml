defaults:
  platforms: [linux, macos, windows]
  for: [scripts, source, class, java_class]
  crit: notable

traits:
  - id: crontab-option-source-reference
    desc: References crontab list or input option
    if:
      type: text
      regex: 'crontab.{0,50}(-l|-[''"]|-$)'
