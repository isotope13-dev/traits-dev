defaults:
  platforms: [unix, windows, macos]
  for: [elf, shell, python]
  crit: notable
  conf: 0.5
traits:
  - id: crontab-keyword
    desc: crontab keyword
    if:
      type: string_value
      word: "crontab"
