defaults:
  platforms: [unix]
  for: [c, ruby, elf, python]
  attack: T1547.013

traits:
  - id: source-xdg-autostart-path
    desc: Source references an XDG autostart path
    crit: component
    conf: 0.9
    if:
      type: text
      substr: .config/autostart

  - id: source-desktop-entry-exec
    desc: Source embeds an executable desktop entry
    crit: notable
    conf: 0.92
    if:
      type: text
      regex: '\[Desktop Entry\][\s\S]{0,260}Type=Application[\s\S]{0,260}Exec='

  - id: source-nodisplay-desktop-entry
    desc: Source embeds a hidden desktop entry
    crit: notable
    conf: 0.92
    if:
      type: text
      regex: 'NoDisplay=true[\s\S]{0,260}Exec='
