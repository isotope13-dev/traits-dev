defaults:
  platforms:
  - ios
  - unix
  for:
  - macho
traits:
- id: lssharedfilelist-insert-item
  desc: Add a macOS login item via LSSharedFileListInsertItemURL
  crit: notable
  conf: 0.9
  if:
    type: symbol
    exact: LSSharedFileListInsertItemURL
