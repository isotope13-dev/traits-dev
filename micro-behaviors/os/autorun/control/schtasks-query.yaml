defaults:
  platforms: [windows]
  for: [ruby]
  attack: T1041

traits:
  - id: ruby-schtasks-query-command
    desc: Ruby schtasks query command
    crit: suspicious
    conf: 0.9
    if:
      type: text
      substr: schtasks.exe /query
