defaults:
  platforms: [windows]
  for: [scripts, binaries, source]
  crit: notable
  attack: T1053.005

traits:
  - id: schtasks-disable-command
    desc: Disables a task with schtasks
    conf: 0.96
    if:
      type: text
      regex: '(?i)\bschtasks(\.exe)?\b[^\r\n]{0,160}/change\b[^\r\n]{0,160}/disable\b'

  - id: schtasks-delete-force
    desc: Force-deletes a Windows scheduled task
    conf: 0.93
    if:
      type: text
      regex: '(?i)\bschtasks(\.exe)?\b[^\r\n]{0,120}/delete\b[^\r\n]{0,120}/f\b'
