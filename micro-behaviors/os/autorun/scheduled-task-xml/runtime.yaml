defaults:
  crit: notable
  conf: 0.96
  for: [powershell, batch, pe, dll, html]
  platforms: [windows]

traits:
  - id: scheduled-task-unlimited-runtime-xml
    desc: Scheduled task XML disables runtime limit
    if:
      type: text
      substr: '<ExecutionTimeLimit>PT0S</ExecutionTimeLimit>'
