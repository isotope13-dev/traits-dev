defaults:
  platforms: [windows, unix]
  for: [python, javascript, typescript, shell, batch, powershell, vbs, html, binaries, ruby]
  crit: notable
  conf: 0.8

traits:
  - id: wmi-moniker-string
    desc: WMI moniker string reference
    if:
      type: text
      substr: 'winmgmts:'
