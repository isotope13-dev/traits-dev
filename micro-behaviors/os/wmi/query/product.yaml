defaults:
  platforms: [windows]
  for: [scripts, binaries]
traits:
  - id: wmic-product-get
    desc: Uses WMIC to list installed products
    crit: notable
    conf: 0.9
    if:
      type: text
      regex: (?i)wmic\s+product\s+(.*?\s+)?get\s+(.*?\s+)?name
