defaults:
  platforms: [unix]
  for: [shell]

traits:
  - id: incus-admin-init
    desc: Incus container manager initialization
    crit: notable
    conf: 0.95
    if:
      type: text
      regex: '\bincus\s+admin\s+init\b'
