defaults:
  platforms: [unix, windows, macos]
  for: [shell, python, javascript, powershell, pe, elf]
traits:
  - id: int-vector-calc-asm
    desc: DOS Interrupt Vector Calculation (Assembly)
    crit: notable
    if:
      type: raw
      regex: 'mov\s+si,4\*(13|21)'
