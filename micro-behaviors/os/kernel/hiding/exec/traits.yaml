defaults:
  platforms: [unix, windows, macos]
  for: [shell, python, javascript, powershell, pe, elf]
traits:
  - id: thread-manipulation-enc
    desc: Hidden (encoded) thread manipulation API
    crit: suspicious
    if:
      type: encoded
      exact: NtCreateThreadEx
