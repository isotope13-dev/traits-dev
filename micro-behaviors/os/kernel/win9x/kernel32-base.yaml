defaults:
  platforms:
    - unix
    - windows
  for: [scripts, binaries]
traits:
  - id: win9x-kernel32-base
    conf: 0.85
    crit: suspicious
    desc: 'Hardcoded Win9x kernel32.dll base address'
    if:
      type: text
      regex: (0BFF70000[hH]|0xBFF70000)
