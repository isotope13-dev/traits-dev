defaults:
  platforms: [windows]
  for: [binaries, c, cpp]

traits:
  - id: filesystem-minifilter-control
    desc: Filesystem filter registration
    crit: notable
    conf: 0.95
    if:
      type: text
      regex: \b(IoRegisterFsRegistrationChange|FltRegisterFilter|FltStartFiltering)\b
