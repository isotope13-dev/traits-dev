defaults:
  platforms: [windows]
  for: [binaries]

traits:
  - id: filesystem-minifilter-control
    desc: Compatibility placeholder
    crit: component
    conf: 1.0
    if:
      type: text
      exact: __cleave_missing_filesystem_minifilter_control__
