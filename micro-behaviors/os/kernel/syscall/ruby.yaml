# Ruby Syscall Detection
# Direct usage of kernel syscalls in Ruby
defaults:
  platforms: [windows, unix]
  for: [ruby]
traits:
  - id: ruby-syscall
    desc: Calls raw kernel syscalls
    crit: notable
    if:
      type: symbol
      exact: syscall
