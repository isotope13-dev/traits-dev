defaults:
  platforms: [unix]
  for: [shell, c, cpp, binaries]
traits:
  - id: kern-panic-write
    desc: BSD kern.panic sysctl write
    crit: suspicious
    conf: 0.95
    if:
      type: text
      regex: 'sysctl\s+-w\s+kern\.panic=1'
