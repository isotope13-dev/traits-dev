defaults:
  platforms: [linux, openwrt]
  for: [c, cpp, elf]

traits:
  - id: linux-register-chrdev
    desc: Registers Linux char device
    crit: notable
    conf: 0.9
    if:
      type: text
      word: register_chrdev
  - id: linux-major-device-number
    desc: Extracts Linux major device number
    crit: notable
    conf: 0.75
    for: [c]
    if:
      type: symbol
      kind: call
      exact: MAJOR
