defaults:
  platforms: [windows]
  for: [binaries]

traits:
  - id: userland-device-bridge
    for: [text]
    desc: Compatibility placeholder
    crit: component
    conf: 1.0
    if:
      type: text
      exact: __cleave_missing_userland_device_bridge__
