# Windows Admin Privilege Check
# Detects Windows APIs checking for admin/elevated privileges
# ATT&CK: T1033 (System Owner/User Discovery)
defaults:
  for: [pe, dll]
  attack: "T1033"
  platforms: [windows]
traits:
  - id: is-user-an-admin
    desc: IsUserAnAdmin API call
    crit: notable
    conf: 0.75
    if:
      type: string
      exact: "IsUserAnAdmin"
