defaults:
  platforms: [unix]
  for: [shell]
  attack: T1041

traits:
  - id: shell-env-temp-dump
    desc: Shell env temp dump
    crit: suspicious
    conf: 0.94
    if:
      type: text
      regex: 'env > /tmp/\.[^\n]{0,80}'
