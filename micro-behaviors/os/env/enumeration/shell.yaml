defaults:
  platforms: [unix, appliance]
  for: [shell]

traits:
  - id: shell-enumerates-env-names
    desc: Shell enumerates environment variable names
    crit: notable
    conf: 0.92
    if:
      type: text
      substr: env | cut -d= -f1
