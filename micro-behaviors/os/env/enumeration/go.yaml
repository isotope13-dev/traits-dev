defaults:
  platforms: [windows, linux, macos]
  for: [go, elf, macho, pe]
  crit: baseline
traits:
  - id: os-environ
    desc: os.Environ() enumerates the entire environment
    crit: notable
    conf: 0.85
    attack: T1082
    if:
      type: symbol
      exact: os.Environ
