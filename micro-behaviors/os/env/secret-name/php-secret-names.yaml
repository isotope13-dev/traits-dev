defaults:
  platforms: [unix]
  for: [php]
  attack: T1041

traits:
  - id: php-generic-secret-key-list
    desc: PHP generic secret key list
    crit: notable
    conf: 0.93
    if:
      type: text
      regex: 'TOKEN[\s\S]{0,180}API_KEY[\s\S]{0,180}SECRET'
