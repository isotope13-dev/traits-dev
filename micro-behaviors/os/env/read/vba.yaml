defaults:
  for:
    - vbs
    - oledoc
    - ooxml
  platforms:
    - windows
  crit: baseline
  conf: 0.85
traits:
  - id: environ-function
    desc: VBA Environ function call
    if:
      type: string_literal
      regex: (?i)\bEnviron\(
