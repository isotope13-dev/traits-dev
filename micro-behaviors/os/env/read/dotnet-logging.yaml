defaults:
  platforms: [windows, linux, macos]
  for: [pe, dll]

traits:
  - id: dotnet-environment-variable-log-template
    desc: Logs enumerated environment variables
    crit: suspicious
    conf: 0.98
    if:
      type: text
      regex: 'Environment variable: \{Key\}=(\{Value\}|\*\*\*)'

