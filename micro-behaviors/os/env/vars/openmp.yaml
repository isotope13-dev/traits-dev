defaults:
  platforms: [unix, windows, macos]
  for: [shell, python, javascript, powershell, pe, elf]
traits:
  - id: omp-num-threads
    desc: OpenMP thread count setting
    crit: notable
    conf: 0.8
    if:
      type: string
      word: OMP_NUM_THREADS
