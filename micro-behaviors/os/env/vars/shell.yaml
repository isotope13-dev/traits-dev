defaults:
  platforms:
    - linux
    - unix
    - android
    - macos
    - ios
    - windows
  for: [binaries]
  crit: notable
  conf: 0.8
traits:
  - id: shell-zsh
    desc: SHELL environment variable zsh marker
    if:
      type: text
      substr: SHELLzsh
