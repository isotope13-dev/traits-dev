defaults:
  platforms: [unix, windows]
  for: [python]
traits:
- id: python-api-key-env-read
  desc: Python reads API key env vars
  crit: notable
  conf: 0.85
  if:
    type: text
    regex: '(getenv|environ\.get)\(.{0,20}API.{0,20}KEY'
    case_insensitive: true
