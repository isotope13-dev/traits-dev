traits:
  - id: env-access
    desc: compat placeholder
    crit: baseline
    conf: 0.1
    if:
      type: string
      exact: __cleave_missing_c92dd800f517__
defaults:
  crit: component
  conf: 0.1
  for:
    - all
