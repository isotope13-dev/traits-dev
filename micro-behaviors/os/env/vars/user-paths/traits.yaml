defaults:
  for: [scripts, binaries]
  crit: baseline
  conf: 0.5
  platforms: [windows]
traits:
  - id: userprofile-var
    desc: USERPROFILE path environment variable
    crit: notable
    if:
      type: text
      word: USERPROFILE
