defaults:
  platforms: [unix, windows]
  for: [python, shell, javascript]
  crit: notable
  conf: 0.9

traits:
  - id: python-azure-tenant-id-env-read
    desc: Python reads Azure tenant ID env var
    for: [python]
    conf: 0.95
    if:
      type: symbol
      kind: member
      exact: os.environ.AZURE_TENANT_ID
