defaults:
  platforms: [windows, unix]
  for: [perl]

traits:
  - id: perl-env-map-all-vars
    desc: Perl iterates all env vars
    crit: notable
    conf: 0.9
    if:
      type: text
      substr: sort keys %ENV
