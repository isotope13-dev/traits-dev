traits:
  - id: env-access
    desc: compat placeholder
    crit: component
    conf: 0.1
    if:
      type: string
      exact: __cleave_missing_9443518f3533__
defaults:
  crit: component
  conf: 0.1
  for:
    - all
