traits:
  - id: env-access
    desc: compat placeholder
    crit: baseline
    conf: 0.1
    if:
      type: text
      exact: __cleave_missing_9443518f3533__
defaults:
  platforms:
    - unix
    - windows
  crit: baseline
  conf: 0.1
  for: [scripts, binaries]
