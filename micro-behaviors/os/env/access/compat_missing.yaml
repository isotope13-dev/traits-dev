traits:
  - id: env-access
    for: [text]
    desc: compat placeholder
    crit: baseline
    conf: 0.1
    if:
      type: text
      exact: __cleave_missing_9443518f3533__
  - id: set-environment-variable-wide
    for: [text]
    desc: Compatibility placeholder
    crit: component
    conf: 1.0
    if:
      type: text
      exact: __cleave_missing_set_environment_variable_wide__
defaults:
  platforms:
    - unix
    - windows
  crit: baseline
  conf: 0.1
  for: [scripts, binaries]
