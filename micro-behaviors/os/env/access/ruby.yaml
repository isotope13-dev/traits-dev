defaults:
  platforms: [windows, unix]
  for: [ruby]
  crit: notable
  conf: 0.75
traits:
  - id: sensitive-env-var
    desc: Ruby sensitive ENV reference
    if:
      type: text
      regex: 'ENV\[.{0,30}(TOKEN|SECRET|KEY|PASS)'
