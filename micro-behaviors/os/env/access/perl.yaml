defaults:
  platforms: [unix]
  for: [perl]

traits:
  - id: perl-path-env-reference
    desc: Perl references PATH environment variable
    crit: notable
    conf: 0.88
    if:
      type: text
      regex: '\$ENV\s*\{\s*["'']?PATH["'']?\s*\}'
