defaults:
  platforms: [unix]
  for: [perl]

traits:
  - id: perl-path-env-read
    desc: Perl reads PATH from environment
    crit: notable
    conf: 0.88
    if:
      type: text
      regex: '\$ENV\s*\{\s*["'']?PATH["'']?\s*\}'
