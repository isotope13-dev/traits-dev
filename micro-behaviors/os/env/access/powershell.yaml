defaults:
  platforms: [windows]
  for: [powershell]

traits:
  - id: powershell-get-environment-variable
    desc: PowerShell reads a named environment variable
    crit: notable
    conf: 0.86
    if:
      type: text
      substr: GetEnvironmentVariable
