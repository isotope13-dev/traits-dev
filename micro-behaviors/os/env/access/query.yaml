defaults:
  for: [binaries, c]
  crit: baseline
  platforms:
    - unix
    - windows
traits:
  - id: getenv-user
    desc: Query USER environment variable
    conf: 0.5
    attack: T1033
    mbc: E1033
    platforms:
      - unix
    if:
      type: text
      regex: getenv\("USER"\)
  - id: getenv-home
    desc: Query HOME environment variable
    conf: 0.5
    attack: T1082
    mbc: E1082
    platforms:
      - unix
    if:
      type: text
      regex: getenv\("HOME"\)
