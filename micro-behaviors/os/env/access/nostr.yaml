defaults:
  platforms: [windows, unix]
  for: [javascript, typescript]
  crit: notable
  conf: 0.9
traits:
  - id: nostr-private-key-env
    desc: Nostr private key env access
    if:
      type: text
      regex: process\.env\.NOSTR_PRIVATE_KEY
