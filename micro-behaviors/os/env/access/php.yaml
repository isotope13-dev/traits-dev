defaults:
  platforms: [unix, windows]
  for: [php]

traits:
  - id: php-getenv-call
    desc: Calls PHP getenv
    crit: notable
    conf: 0.85
    if:
      type: symbol
      kind: call
      exact: getenv
