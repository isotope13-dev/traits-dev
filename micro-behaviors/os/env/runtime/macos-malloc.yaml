defaults:
  platforms: [unix]
  for: [python, shell, macho]
  size_min: 16
  size_max: 50000000
traits:
  - id: malloc-stack-logging
    desc: "MallocStackLogging heap-debug env var"
    crit: notable
    conf: 0.6
    if:
      type: text
      substr: MallocStackLogging
