defaults:
  platforms: [openbsd, bsd, unix]
  for: [shell]
  attack: T1041
  mbc: E1041

traits:
  - id: shell-vpn-env-secret-filter
    desc: Shell filters VPN secret env vars
    crit: suspicious
    conf: 0.9
    if:
      type: text
      substr: "env | grep -iE 'vpn|tunnel|proxy|auth"
