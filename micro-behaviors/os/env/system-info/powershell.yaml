defaults:
  crit: notable
  conf: 0.8
  platforms: [windows]
  for: [powershell]
traits:
  - id: env-username
    desc: Accesses username environment variable
    for: [powershell, batch]
    if:
      type: text
      regex: '(?i)(\$env:USERNAME\b|%USERNAME%)'
  - id: env-computername
    desc: Accesses computername environment variable
    for: [powershell, batch]
    if:
      type: text
      regex: '(?i)(\$env:COMPUTERNAME\b|%COMPUTERNAME%)'
