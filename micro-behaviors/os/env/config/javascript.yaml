# Browser runtime configuration access.
defaults:
  platforms: [windows, unix]
  for: [javascript, typescript, html]
  crit: notable
  conf: 0.9

traits:
  - id: browser-app-config-global
    desc: Browser runtime configuration global reference
    if:
      type: text
      regex: 'window\.__[A-Z0-9_]*CONFIG__'
