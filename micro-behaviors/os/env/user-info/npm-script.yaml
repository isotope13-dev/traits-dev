defaults:
  platforms: [unix, windows]
  for: [package.json]
  crit: notable
  conf: 0.9
traits:
  - id: npm-postinstall-user-env
    desc: npm postinstall reads user identity environment
    if:
      type: value
      path: 'scripts.postinstall'
      regex: 'process\.env\.(USER|USERNAME)\b'
