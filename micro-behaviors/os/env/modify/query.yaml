defaults:
  for: [binaries, c]
  crit: baseline
  platforms:
    - unix
    - windows
traits:
  - id: native-setenv
    desc: Sets process environment through setenv
    conf: 0.8
    for: [elf, macho, c, cpp, objc]
    platforms: [unix]
    if:
      type: symbol
      exact: setenv
