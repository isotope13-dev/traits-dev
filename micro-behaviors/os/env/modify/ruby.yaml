defaults:
  platforms: [windows, unix]
  for: [ruby]
  crit: notable
  conf: 0.94

traits:
  - id: ruby-assigns-home-environment
    desc: Ruby assigns the HOME environment variable
    if:
      type: text
      regex: 'ENV\s*\[\s*["'']HOME["'']\s*\]\s*='

