defaults:
  platforms: [unix]
  for: [php]
  attack: T1041

traits:
  - id: php-cloud-secret-key-list
    desc: PHP cloud secret key list
    crit: suspicious
    conf: 0.93
    if:
      type: text
      regex: 'AWS_SECRET_ACCESS_KEY[\s\S]{0,260}GOOGLE_APPLICATION_CREDENTIALS'
