defaults:
  platforms: [linux, macos, windows, unix, android, ios]
traits:
  - id: rails-env-check-obfuscated
    desc: Obfuscated Rails environment check
    crit: suspicious
    conf: 0.9
    for: [ruby]
    mbc: "B0032"
    if:
      type: text
      regex: 'Rails\.env\[0\]\s*==\s*["'']p["'']'
