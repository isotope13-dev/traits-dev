defaults:
  platforms: [windows]
  for: [scripts, binaries]

traits:
  - id: pdf-corruption-error-message
    desc: Reports a corrupted PDF conversion error
    crit: notable
    conf: 0.8
    if:
      type: text
      regex: '(?i)\bpdf file is corrupted\b'
