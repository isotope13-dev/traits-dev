defaults:
  platforms: [unix]
  for: [shell]
  crit: notable
  conf: 0.95

traits:
  - id: sed-adds-wheel-group-members
    desc: Sed expression adds wheel group members
    if:
      type: literal
      regex: 's/wheel:x:[0-9]+:/wheel:x:[0-9]+:[^/\n]+/g'
