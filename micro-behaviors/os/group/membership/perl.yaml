defaults:
  platforms: [unix]
  for: [perl]
  crit: notable
  conf: 0.9

traits:
  - id: perl-gpasswd-membership-replacement
    desc: Perl replaces Unix group membership list
    if:
      type: text
      regex: '\bsystemcall\s*\(\s*["'']/usr/bin/gpasswd["'']\s*,\s*["'']-M["'']'
