defaults:
  for: [elf]
  platforms: [android]

traits:
  - id: direct-syscall-api
    desc: Imports direct syscall invocation API
    crit: notable
    conf: 0.99
    if:
      type: import
      exact: syscall
