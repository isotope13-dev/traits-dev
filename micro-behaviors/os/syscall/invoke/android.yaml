defaults:
  for: [elf]
  platforms: [unix]

traits:
  - id: direct-syscall-api
    desc: Imports the Unix syscall wrapper API
    crit: notable
    conf: 0.99
    if:
      type: import
      exact: syscall
