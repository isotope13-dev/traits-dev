defaults:
  platforms: [windows]
  for: [pe]
traits:
  - id: console-screen-buffer-info
    desc: GetConsoleScreenBufferInfo API
    crit: baseline
    conf: 0.7
    if:
      type: symbol
      exact: GetConsoleScreenBufferInfo
