defaults:
  platforms: [unix, windows]
  for: [javascript, typescript, python, shell]
  attack: T1082
  crit: notable
  conf: 0.9

traits:
  - id: nvidia-smi-query-gpu
    desc: Queries GPU inventory via nvidia-smi
    if:
      type: text
      regex: 'nvidia-smi[^\n]{0,16}(--query-gpu|["'']?-L\b)'