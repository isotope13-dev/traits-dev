defaults:
  platforms: [windows]
  for: [pe, dll, c, cpp]
  crit: notable
  conf: 0.86
traits:
  - id: set-computer-name-api
    desc: Windows API that changes the computer name
    if:
      type: symbol
      kind: import
      regex: '^SetComputerName[AW]?$'

  - id: set-system-time-api
    desc: Windows API that sets system time
    if:
      type: symbol
      kind: import
      regex: '^SetSystemTime(Adjustment)?$'
