defaults:
  platforms: [windows, unix]
  for: [php]
  crit: notable
  conf: 0.7

traits:
  - id: phpinfo-call
    desc: Calls PHP phpinfo()
    if:
      type: symbol
      kind: call
      exact: phpinfo
