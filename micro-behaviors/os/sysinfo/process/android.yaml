defaults:
  for: [elf]
  platforms: [android]

traits:
  - id: system-information-query
    desc: Imports Android system-information query API
    crit: notable
    conf: 0.99
    if:
      type: import
      exact: sysinfo
