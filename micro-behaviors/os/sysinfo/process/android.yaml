defaults:
  for: [elf]
  platforms: [unix]

traits:
  - id: system-information-query
    desc: Imports the Unix sysinfo system-information API
    crit: notable
    conf: 0.99
    if:
      type: import
      exact: sysinfo
