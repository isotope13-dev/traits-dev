defaults:
  platforms: [windows]
  for: [batch, powershell, shell, binaries]
  attack: T1082
traits:
  - id: windows-systeminfo-command
    desc: Windows systeminfo command
    crit: notable
    conf: 0.72
    if:
      type: text
      regex: (?i)(^|[\r\n&|({])\s*systeminfo(?:\.exe)?\b
