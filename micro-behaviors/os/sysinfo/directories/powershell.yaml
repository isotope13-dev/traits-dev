# PowerShell Directory Access
defaults:
  platforms: [windows]
  for: [powershell]
traits:
  - id: special-folder
    desc: Accesses special folders (e.g. Startup)
    crit: notable
    conf: 0.8
    if:
      type: text
      regex: '(?i)\[Environment\]::GetFolderPath'
