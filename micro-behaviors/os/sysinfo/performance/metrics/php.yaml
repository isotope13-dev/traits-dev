defaults:
  platforms: [unix, windows]
  for: [php]
  crit: notable
  conf: 0.5

traits:
  - id: memory-get-usage
    desc: Reads PHP process memory usage
    if:
      type: symbol
      kind: call
      exact: memory_get_usage
