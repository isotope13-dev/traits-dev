# Hostname Discovery - Shell/POSIX
# Retrieving system hostname via shell commands
defaults:
  platforms: [unix, windows]
  for: [shell, javascript, typescript, python, ruby, perl]
  attack: "T1082"
traits:
  - id: hostname-cmd
    desc: Invokes hostname shell command
    crit: notable
    conf: 0.5
    mbc: "E1082"
    if:
      type: string_literal
      regex: ^hostname(?:\s|$)
    unless:
      - type: text
        regex: verify_hostname|:hostname\b
      - type: text
        regex: '["'']hostname["'']\s*,\s*["'']port["'']'
      - type: basename
        regex: (_spec|_test)\.rb$
