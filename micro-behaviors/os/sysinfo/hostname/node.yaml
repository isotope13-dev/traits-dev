defaults:
  platforms: [windows, unix]
  for: [javascript, typescript]
  crit: notable
  attack: T1082
traits:
  - id: os-hostname-call
    desc: os.hostname() call
    conf: 0.75
    if:
      type: symbol
      exact: os.hostname
