defaults:
  platforms: [macos]
  for: [applescript]
  attack: T1082

traits:
  - id: applescript-shell-hostname
    desc: AppleScript invokes the hostname command
    crit: notable
    conf: 0.98
    if:
      type: text
      regex: 'do shell script\s+["'']hostname["'']'
