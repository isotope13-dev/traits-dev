traits:
  - id: gopsutil
    desc: Go disk info via gopsutil
    crit: notable
    conf: 0.6
    attack: T1082
    platforms:
      - linux
    for:
      - elf
    if:
      type: text
      substr: gopsutil/v3/disk
