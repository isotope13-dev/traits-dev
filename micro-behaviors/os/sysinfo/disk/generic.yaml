defaults:
  for: [scripts, binaries]
  platforms:
    - unix
    - windows
traits:
  - id: freespace
    desc: Disk free space query
    crit: baseline
    conf: 0.5
    attack: T1082
    if:
      type: text
      substr: DiskFreeSpace
