defaults:
  platforms: [windows]
  for: [scripts, binaries]

traits:
  - id: get-user-default-ui-language
    for: [text]
    desc: Compatibility placeholder
    crit: component
    conf: 1.0
    if:
      type: text
      exact: __cleave_missing_get_user_default_ui_language__
