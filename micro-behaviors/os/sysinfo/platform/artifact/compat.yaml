defaults:
  platforms: [windows]
  for: [scripts, binaries]

traits:
  - id: get-user-default-ui-language
    desc: Compatibility placeholder
    crit: component
    conf: 1.0
    if:
      type: text
      exact: __cleave_missing_get_user_default_ui_language__
  - id: libegl-basename
    desc: Compatibility placeholder
    crit: component
    conf: 1.0
    if:
      type: text
      exact: __cleave_missing_libegl_basename__
