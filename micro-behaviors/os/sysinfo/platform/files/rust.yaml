defaults:
  platforms: [unix]
  for: [rust, elf]
  crit: notable
  conf: 0.9

traits:
  - id: unix-release-and-proc-info-paths
    desc: Unix release and proc information paths
    if:
      type: text
      regex: '(/etc/os-release|/proc/version)[\s\S]{0,260}/proc/cpuinfo'
