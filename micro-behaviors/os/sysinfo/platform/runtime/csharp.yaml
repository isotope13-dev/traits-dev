defaults:
  platforms: [unix, windows]
  for: [csharp]

traits:
  - id: dotnet-runtime-info
    desc: .NET reads the runtime OS description
    crit: notable
    conf: 0.88
    if:
      type: text
      substr: RuntimeInformation.OSDescription
