defaults:
  platforms: [aix, unix, windows]
  for: [elf, c, cpp, powershell, python]
traits:
  - id: ptrace-debugger
    desc: Check for debugger attachment
    crit: notable
    conf: 0.8
    if:
      type: text
      substr: TracerPid
    unless:
      - id: metadata/lang/source::linux-kernel-primary-tree-path
      # systemd uses TracerPid to recognize proot/container virtualization;
      # this is not a debugger-attachment check in this known library.
      - id: well-known/lib/native/systemd::systemd-shared-library
  - id: python-uptime-check
    desc: Check system uptime
    crit: notable
    conf: 0.8
    if:
      type: text
      regex: 'open\(\s*["'']/proc/uptime["'']'
    unless:
      - id: metadata/lang/source::linux-kernel-secondary-tree-path
      - id: micro-behaviors/os/syscall/invoke::linux-kernel-tree-path-scripts
      - id: micro-behaviors/os/syscall/invoke::linux-kernel-tree-path-extra
      - id: micro-behaviors/os/syscall/invoke::linux-kernel-tree-path-mmfs
      - id: metadata/lang/source::linux-kernel-primary-tree-path
