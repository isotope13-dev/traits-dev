defaults:
  platforms: [unix]
  for: [swift]
  attack: T1041

traits:
  - id: swift-bin-uname-recon
    desc: Swift runs uname for host recon
    crit: notable
    conf: 0.9
    if:
      type: text
      substr: /bin/uname
