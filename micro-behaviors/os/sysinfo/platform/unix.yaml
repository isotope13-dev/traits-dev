defaults:
  for: [binaries, c]
  crit: baseline
  platforms:
    - unix
    - linux
    - macos
traits:
  - id: uname
    desc: Query system name and information
    conf: 0.6
    mbc: E1082
    attack: T1082
    if:
      type: symbol
      regex: uname
