defaults:
  platforms:
    - unix
    - windows
  for:
    - shell
  crit: component
traits:
  - id: sh-uname-fingerprint
    desc: Reads OS or CPU information with uname
    crit: notable
    conf: 0.8
    attack: T1082
    mbc: E1082
    unless:
      - id: micro-behaviors/os/syscall/invoke::linux-kernel-source-context
      - id: metadata/package/testing/presence/harness/
      - id: metadata/file/extension/identity::autotools-configure-basename
      - type: text
        substr: "case $(uname -s) in"
    if:
      type: text
      regex: "\\$\\(uname\\s+-[a-z]+\\)|`uname\\s+-[a-z]+`"
  - id: os-release-read
    desc: Reads Linux os-release file
    crit: notable
    conf: 0.82
    mbc: E1082
    attack: T1082
    if:
      type: text
      regex: '\bcat\s+/etc/os-release\b'
  - id: arch-mips
    desc: MIPS architecture string
    if:
      type: text
      word: mips
  - id: arch-arm
    desc: ARM architecture string
    if:
      type: text
      regex: \barm[0-9]?\b
  - id: arch-aarch64
    desc: AArch64 (ARM64) architecture string
    if:
      type: text
      word: aarch64
  - id: arch-arm64
    desc: arm64 architecture string
    if:
      type: text
      word: arm64
  - id: arch-x86
    desc: x86 architecture string
    if:
      type: text
      word: x86
  - id: arch-sh4
    desc: SH4 architecture string
    if:
      type: text
      word: sh4
  - id: arch-ppc
    desc: PPC architecture string
    if:
      type: text
      word: ppc
  - id: arch-arc
    desc: ARC architecture string
    if:
      type: text
      word: arc
  - id: architecture-selection-wordlist
    desc: Selects among multiple CPU architecture payloads
    crit: notable
    conf: 0.94
    count_min: 3
    if:
      type: text
      regex: '(?i)\b(x86_64|amd64|i[3-6]86|aarch64|arm[0-9]+|riscv|mips|ppc)\b'
    unless:
      - id: micro-behaviors/os/syscall/invoke::linux-kernel-source-context
      - id: metadata/build/autotools::autoconf-generated-script
      - id: metadata/package/testing/
composite_rules:
  - id: multi-arch-target
    desc: Script targets multiple CPU architectures
    crit: notable
    conf: 0.85
    mbc: Discovery
    attack: T1082
    needs: 3
    any:
      - id: arch-mips
      - id: arch-arm
      - id: arch-aarch64
      - id: arch-arm64
      - id: arch-x86
      - id: arch-sh4
      - id: arch-ppc
      - id: arch-arc
      - id: architecture-selection-wordlist
    unless:
      - id: micro-behaviors/os/syscall/invoke::linux-kernel-source-context
      - id: metadata/file/extension/identity::squashfs-container-filename
