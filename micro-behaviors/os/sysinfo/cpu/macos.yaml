defaults:
  for: [scripts, binaries]
  platforms:
    - unix
    - windows
traits:
  - id: macos-cpuinfo
    desc: macOS CPU information query
    crit: notable
    conf: 0.7
    attack: T1082
    platforms:
      - macos
    if:
      type: text
      substr: CpuInfoAndModel
