defaults:
  platforms: [windows, unix]
  for: [javascript, typescript]

traits:
  - id: node-readline-line-handler
    desc: Handles line events from Node.js readline
    crit: baseline
    conf: 0.95
    if:
      type: text
      regex: '\brl\.on\s*\(\s*["'']line["'']'
