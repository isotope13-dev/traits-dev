defaults:
  for: [vbs]
  platforms: [windows]
traits:
  - id: wsh-windows-interpreter-option
    desc: WSH selects the Windows script interpreter
    crit: notable
    conf: 0.95
    attack: T1059.007
    if:
      type: text
      regex: '(?im)^UseWindowsInterpreter\s*=\s*true\s*$'
