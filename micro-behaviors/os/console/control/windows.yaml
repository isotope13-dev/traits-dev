defaults:
  platforms: [windows]
  for: [pe, dll]
traits:
  - id: set-console-ctrl-handler
    desc: Register console control handler
    crit: notable
    conf: 0.86
    if:
      type: symbol
      exact: SetConsoleCtrlHandler
