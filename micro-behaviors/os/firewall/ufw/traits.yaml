# ufw - Uncomplicated Firewall
# Ubuntu/Debian frontend for iptables
defaults:
  platforms: [unix, windows, macos]
  for: [shell, python, javascript, powershell, pe, elf]
  crit: notable
  conf: 0.9
traits:
  - id: ufw
    desc: ufw firewall command
    if:
      type: string
      exact: "ufw "
