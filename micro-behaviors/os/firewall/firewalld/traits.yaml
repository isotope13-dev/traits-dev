defaults:
  platforms:
    - unix
    - windows
    - macos
  for: [scripts, binaries]
  crit: notable
  conf: 0.9
traits:
  - id: firewall-cmd
    desc: firewall-cmd utility
    if:
      type: text
      substr: firewall-cmd
  - id: firewalld
    desc: firewalld service
    if:
      type: text
      substr: firewalld
