defaults:
  platforms: [openbsd, bsd, unix]
  for: [go, elf, shell]
  crit: notable
  conf: 0.9

traits:
  - id: pf-conf-path
    desc: OpenBSD PF configuration path
    if:
      type: text
      substr: /etc/pf.conf
