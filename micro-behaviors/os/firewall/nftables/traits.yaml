defaults:
  platforms:
    - unix
    - windows
  for: [scripts, binaries]
  crit: notable
  conf: 0.9
traits:
  - id: nft
    desc: nft command
    if:
      type: text
      exact: 'nft '
  - id: nftables
    desc: nftables reference
    if:
      type: text
      substr: nftables
