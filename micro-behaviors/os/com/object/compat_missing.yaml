defaults:
  platforms: [windows]
  for: [scripts, binaries]

traits:
  - id: mapi-namespace-progid
    for: [text]
    desc: Compatibility placeholder
    crit: component
    conf: 1.0
    if:
      type: text
      exact: __cleave_missing_mapi_namespace_progid__
  - id: outlook-application-progid
    for: [text]
    desc: Compatibility placeholder
    crit: component
    conf: 1.0
    if:
      type: text
      exact: __cleave_missing_outlook_application_progid__
