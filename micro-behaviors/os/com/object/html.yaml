defaults:
  crit: notable
  conf: 0.9
  platforms: [windows]
  for: [html]

traits:
  - id: html-activex-classid-object
    desc: Embeds ActiveX object by CLSID
    if:
      type: text
      regex: '(?i)<object\b[^><]{0,64}\bclassid\s*=\s*["'']?CLSID\s*:'
