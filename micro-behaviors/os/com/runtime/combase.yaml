defaults:
  platforms: [windows]
  for: [pe]
traits:
  - id: combase-dll-reference
    desc: References the Windows COM runtime DLL
    crit: notable
    conf: 0.9
    if:
      type: text
      exact: combase.dll
      section: rdata
    unless:
      - type: import
        substr: combase
