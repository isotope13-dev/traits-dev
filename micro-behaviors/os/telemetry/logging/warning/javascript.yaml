defaults:
  platforms: [windows, unix]
  for: [javascript, typescript]
  crit: notable
  conf: 0.9

traits:
  - id: node-process-emit-warning
    desc: Node.js process emit-warning call
    if:
      type: tree-sitter
      kind: call
      regex: '\bemitWarning\s*\('
