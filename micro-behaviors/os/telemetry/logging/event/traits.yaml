defaults:
  platforms: [windows, unix]
  for: [macho, swift, objc, javascript, typescript]
  crit: notable

traits:
  - id: log-event-identifier
    desc: References a logEvent logging identifier
    conf: 0.82
    if:
      type: text
      word: logEvent
    unless:
      - id: metadata/lang/scripted::dts-declaration-file
