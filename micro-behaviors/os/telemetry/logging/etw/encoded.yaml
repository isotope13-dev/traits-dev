defaults:
  platforms: [windows]
  for: [pe, dll]
  crit: notable
traits:
  - id: etw-event-write-xor
    desc: XOR-encoded ETW event-write API reference
    conf: 0.98
    if:
      type: encoded
      encoding: xor
      substr: EtwEventWrite
