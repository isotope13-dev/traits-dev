defaults:
  platforms: [unix, windows]
  for: [php]
  crit: component
  conf: 0.82

traits:
- id: authenticated-user-field
  desc: Captures authenticated user ID
  if:
    type: text
    regex: "['\"]user_id['\"][\\s\\S]{0,120}->user\\s*\\(\\)\\?->id"
