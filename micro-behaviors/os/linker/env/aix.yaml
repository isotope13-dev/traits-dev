defaults:
  platforms: [aix, unix]
  for: [c, cpp, elf, shell, php]
traits:
  - id: libpath
    desc: LIBPATH environment variable (AIX)
    crit: notable
    conf: 0.7
    mbc: F0015
    attack: T1574.006
    if:
      type: text
      substr: LIBPATH
