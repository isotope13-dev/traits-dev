defaults:
  platforms: [windows, unix]
  for: [python]

traits:
  - id: dunder-import-getattr
    desc: Python dynamic import via __import__ and getattr
    crit: notable
    conf: 0.9
    if:
      type: text
      regex: 'getattr\s*\(\s*__import__\s*\('

