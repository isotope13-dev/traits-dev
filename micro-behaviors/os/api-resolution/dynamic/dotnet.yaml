defaults:
  platforms: [windows]
  for: [csharp]

traits:
  - id: assembly-load-getmethod-chain
    desc: .NET reflection loads type method
    crit: notable
    conf: 0.9
    if:
      type: text
      regex: 'Assembly\.Load[\s\S]{0,140}\.GetType[\s\S]{0,360}\.GetMethod'
