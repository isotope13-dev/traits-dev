defaults:
  platforms: [unix, windows]
  for: [clojure]
  crit: notable
  conf: 0.92

traits:
  - id: clojure-secure-random-constructor
    desc: Clojure constructs a secure random generator
    if:
      type: symbol
      kind: call
      regex: '^(java\.security\.)?SecureRandom\.$'
