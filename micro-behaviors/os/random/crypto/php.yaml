defaults:
  platforms: [unix, windows]
  for: [php]
  crit: notable
  conf: 0.82

traits:
  - id: php-random-bytes
    desc: PHP cryptographic random byte generation
    if:
      type: symbol
      kind: call
      exact: random_bytes
