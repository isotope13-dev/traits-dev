defaults:
  platforms: [windows, unix]
  for: [html, javascript, typescript]

traits:
  - id: webcrypto-get-random-values
    desc: Generates Web Crypto random bytes
    crit: notable
    conf: 0.96
    if:
      type: text
      regex: '\bcrypto\.getRandomValues\s*\('
