defaults:
  crit: notable
  conf: 0.85
  platforms: [windows]
  for: [pe, csharp]

traits:
  - id: dotnet-random-number-generator
    desc: .NET cryptographic random generator
    if:
      type: text
      exact: RandomNumberGenerator
