defaults:
  platforms: [windows, unix]
  for: [python]
traits:
  - id: os-urandom
    desc: Python os.urandom cryptographic random bytes
    crit: notable
    conf: 0.9
    if:
      type: text
      regex: '\bos\.urandom\s*\('
