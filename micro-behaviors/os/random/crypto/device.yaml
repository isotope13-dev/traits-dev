defaults:
  for: [scripts, binaries]
  platforms:
    - unix
    - windows
traits:
  - id: urandom
    desc: Access /dev/urandom
    crit: baseline
    conf: 1.0
    platforms:
      - unix
    if:
      type: text
      substr: /dev/urandom
