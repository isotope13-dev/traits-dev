defaults:
  platforms: [unix, windows]
  for: [python]

traits:
  - id: import-random
    desc: Python random module import
    crit: baseline
    conf: 0.8
    if:
      type: text
      regex: ^\s*import\s+random\b
