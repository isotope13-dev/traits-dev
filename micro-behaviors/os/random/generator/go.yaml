defaults:
  platforms: [windows, unix]
  for: [go]

traits:
  - id: go-rand-read-call
    desc: Go fills bytes from rand.Read
    crit: notable
    conf: 0.86
    if:
      type: symbol
      kind: call
      exact: rand.Read
