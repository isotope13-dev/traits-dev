defaults:
  platforms: [android, unix]
  for: [elf]

traits:
  - id: qemud-socket-path-reference
    desc: References Android qemud socket path
    crit: notable
    conf: 0.93
    if:
      type: text
      substr: "/dev/socket/qemud"
