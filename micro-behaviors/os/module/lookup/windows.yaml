defaults:
  for: [pe, dll]
  platforms: [windows]

traits:
  - id: known-dlls-object-path
    desc: Windows KnownDlls object directory
    crit: notable
    conf: 0.9
    if:
      type: text
      regex: '(?i)\\KnownDlls\\|KnownDlls'
