defaults:
  platforms: [unix, windows]
  for: [ruby]
  crit: notable
  conf: 0.9
traits:
  - id: ruby-require-relative
    desc: Ruby loads a relative source module
    if:
      type: symbol
      kind: call
      exact: require_relative
      arg:
        kind: string
