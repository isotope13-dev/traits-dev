defaults:
  platforms: [windows]
  for: [binaries]

traits:
  - id: disable-thread-library-calls
    desc: Compatibility placeholder
    crit: component
    conf: 1.0
    if:
      type: text
      exact: __cleave_missing_disable_thread_library_calls__
  - id: uninstall-exe-basename
    desc: Compatibility placeholder
    crit: component
    conf: 1.0
    if:
      type: text
      exact: __cleave_missing_uninstall_exe_basename__
