defaults:
  platforms: [macos]
  for: [macho]
  crit: notable
  conf: 0.9

traits:
  - id: mach-o-object-image-loader-export
    desc: Mach-O object-image loader export marker
    crit: component
    conf: 0.92
    if:
      type: export
      regex: '(^|_)[A-Za-z0-9_]{0,32}loader$'
