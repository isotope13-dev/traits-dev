defaults:
  platforms: [windows, unix]
  for: [python]
traits:
  - id: sys-path-mutate
    desc: sys.path mutation (alters module search path)
    crit: notable
    conf: 0.86
    if:
      type: symbol
      kind: call
      regex: '^sys\.path\.(insert|append|extend)$'
