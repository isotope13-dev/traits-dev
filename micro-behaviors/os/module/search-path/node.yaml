defaults:
  platforms: [windows, unix]
  for: [javascript]
traits:
  - id: node-module-paths-mutate
    desc: module.paths mutation (alters Node module search path)
    crit: notable
    conf: 0.86
    if:
      type: text
      regex: 'module\.paths\.(unshift|push|splice)\s*\('
