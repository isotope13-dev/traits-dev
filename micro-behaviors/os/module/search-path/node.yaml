defaults:
  platforms: [windows, unix]
  for: [javascript]
traits:
  - id: node-module-paths-mutate
    desc: 'module.paths mutation'
    crit: notable
    conf: 0.86
    if:
      type: symbol
      kind: call
      regex: '^module\.paths\.(unshift|push|splice)$'
