defaults:
  platforms: [windows]
  for: [pe, dll]
  crit: notable
traits:
  - id: scan-buffer-api-xor
    desc: XOR-encoded AMSI scan API reference
    conf: 0.98
    if:
      type: encoded
      encoding: xor
      substr: AmsiScanBuffer
