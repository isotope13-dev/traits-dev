defaults:
  platforms: [macos]
  for: [shell, applescript, macho]
  crit: notable
  conf: 0.85
  attack: T1087.001

traits:
  - id: dscl-cli
    desc: Runs macOS Directory Service CLI
    if:
      type: text
      regex: '\bdscl\s+\.'
