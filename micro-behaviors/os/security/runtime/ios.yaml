defaults:
  platforms: [ios]
  for: [c, cpp, objc, rust, swift, kotlin]
  crit: notable
  conf: 0.94

traits:
  - id: ios-jailbreak-artifact-path
    desc: References a known iOS jailbreak artifact path
    if:
      type: text
      regex: 'Cydia\.app|MobileSubstrate|/private/var/lib/apt'
