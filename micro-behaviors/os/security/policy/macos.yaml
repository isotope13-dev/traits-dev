defaults:
  platforms: [macos]
  for: [scripts, source, binaries]
  crit: notable
  conf: 0.9

traits:
  - id: authorizationdb-write
    desc: macOS AuthorizationDB policy write
    if:
      type: text
      regex: '(?i)(^|[^A-Za-z0-9_])security\s+authorizationdb\s+write\b'
