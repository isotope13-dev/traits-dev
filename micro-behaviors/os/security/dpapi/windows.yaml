defaults:
  platforms: [windows]
  for: [pe, dll]

traits:
  - id: crypt-unprotect-data-api
    desc: Imports Windows DPAPI unprotect API
    crit: notable
    conf: 0.9
    if:
      type: symbol
      kind: import
      exact: CryptUnprotectData
