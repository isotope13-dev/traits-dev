defaults:
  platforms: [linux]
traits:
  - id: enforcing-keyword
    desc: References an enforcing policy state
    crit: notable
    conf: 0.6
    for:
      - shell
    if:
      type: string_literal
      word: enforcing
