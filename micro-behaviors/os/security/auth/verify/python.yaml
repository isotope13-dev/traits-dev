defaults:
  platforms: [windows, unix]
  for: [python]
  crit: notable
  conf: 0.94

traits:
  - id: telegram-chat-authorization-gate
    desc: Telegram commands verify authorized chat ID
    if:
      type: text
      regex: 'allowed_chat_id.{0,80}!=.{0,40}chat_id'
