defaults:
  platforms: [unix, windows]
  for: [javascript, typescript]

traits:
  - id: optional-token-request-auth
    desc: Disables request authentication without token
    crit: suspicious
    conf: 0.99
    if:
      type: text
      regex: '\bauthed\s*=\s*!\w*TOKEN\w*\s*\|\|\s*req\.headers'
