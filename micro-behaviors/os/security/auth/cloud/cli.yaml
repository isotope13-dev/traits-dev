defaults:
  platforms: [unix, windows]
  for: [shell, python, javascript, source]
  crit: notable
  conf: 0.9

traits:
  - id: vercel-whoami
    desc: Vercel CLI authentication-state query
    if:
      type: text
      word: vercel whoami
