defaults:
  platforms: [windows, unix]
  for: [binaries, scripts]
  attack: T1550.002

traits:
  - id: ntlmssp-marker
    desc: NTLMSSP authentication marker
    crit: notable
    conf: 0.9
    count_min: 2
    if:
      type: text
      substr: NTLMSSP
