defaults:
  platforms: [unix, windows]
  for: [python]

traits:
  - id: python-select-call
    desc: Python select I/O multiplexing call
    crit: notable
    conf: 0.9
    if:
      type: symbol
      kind: call
      exact: select.select
