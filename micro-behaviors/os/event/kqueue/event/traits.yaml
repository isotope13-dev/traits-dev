defaults:
  platforms: [macos]
  for: [macho]
traits:
  - id: kevent-func
    desc: References macOS kevent API
    crit: baseline
    conf: 0.4
    if:
      type: symbol
      regex: kevent
    unless:
      - type: basename
        regex: \.node$
