defaults:
  platforms: [macos]
  for: [macho]
traits:
  - id: kevent-func
    desc: kevent function
    crit: baseline
    conf: 0.4
    if:
      type: symbol
      regex: kevent
    unless:
      - type: basename
        regex: \.node$
