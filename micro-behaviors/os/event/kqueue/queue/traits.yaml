defaults:
  platforms: [macos]
  for: [macho]
traits:
  - id: kqueue-func
    desc: kqueue function
    crit: baseline
    conf: 0.4
    if:
      type: symbol
      regex: kqueue
    unless:
      - type: basename
        regex: \.node$
