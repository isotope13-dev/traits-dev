defaults:
  platforms: [unix]
  for: [c, cpp, rust, go, elf, macho, shell]
  attack: T1529

traits:
  - id: unix-reboot-api
    desc: Invokes Unix reboot operation
    crit: notable
    conf: 0.9
    if:
      type: symbol
      kind: call
      exact: reboot
