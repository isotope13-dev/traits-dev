defaults:
  platforms: [unix]
  for: [c, cpp, rust, go, elf, macho]
  attack: T1529

traits:
  - id: unix-reboot-api
    desc: Calls the Unix reboot API
    crit: notable
    conf: 0.9
    if:
      type: symbol
      kind: call
      exact: reboot
