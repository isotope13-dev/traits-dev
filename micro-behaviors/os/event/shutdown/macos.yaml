defaults:
  platforms: [macos]
  for: [c, cpp]
  crit: notable
  conf: 0.9
  attack: T1529

traits:
  - id: c-applescript-restart-literal
    desc: C source embeds an AppleScript restart command
    if:
      type: text
      regex: '(?i)\bto\s+restart\b'
