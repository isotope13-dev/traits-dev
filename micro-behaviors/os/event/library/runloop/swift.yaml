defaults:
  for: [swift]
  platforms: [macos]

traits:
  - id: swift-main-runloop-run
    desc: Swift runs main run loop
    crit: notable
    conf: 0.82
    if:
      type: text
      substr: RunLoop.main.run()
