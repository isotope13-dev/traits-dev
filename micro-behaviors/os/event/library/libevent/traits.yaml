defaults:
  platforms: [unix, android, windows]
  for: [elf, macho, pe]
traits:
  - id: event-base-new
    desc: event_base_new function
    crit: baseline
    conf: 0.4
    if:
      type: symbol
      regex: event_base_new
  - id: event-base-dispatch
    desc: event_base_dispatch function
    crit: baseline
    conf: 0.4
    if:
      type: symbol
      regex: event_base_dispatch
  - id: event-add
    desc: event_add function
    crit: baseline
    conf: 0.4
    if:
      type: symbol
      regex: event_add
  - id: bufferevent-prefix
    desc: bufferevent_ function prefix
    crit: baseline
    conf: 0.4
    if:
      type: text
      regex: \bbufferevent_
  - id: evbuffer-prefix
    desc: evbuffer_ function prefix
    crit: baseline
    conf: 0.4
    if:
      type: text
      regex: \bevbuffer_
  - id: evdns-prefix
    desc: evdns_ function prefix
    crit: baseline
    conf: 0.4
    if:
      type: text
      regex: \bevdns_
  - id: evhttp-prefix
    desc: evhttp_ function prefix
    crit: baseline
    conf: 0.4
    if:
      type: text
      regex: \bevhttp_
composite_rules:
  - id: libevent
    desc: libevent event loop library
    crit: notable
    conf: 0.7
    needs: 3
    any:
      - id: micro-behaviors/os/event/library/libevent
