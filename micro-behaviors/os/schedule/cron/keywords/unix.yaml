defaults:
  platforms: [unix, windows, macos]
  for: [elf, shell, python]
  crit: baseline
  conf: 0.5
traits:
  - id: crontab-keyword
    desc: crontab keyword
    if:
      type: string_value
      word: "crontab"
