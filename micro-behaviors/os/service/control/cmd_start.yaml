defaults:
  platforms:
    - unix
    - windows
  for: [scripts, binaries]
traits:
  - id: sc-start-timeout
    desc: Uses timeout to delay sc.exe start
    attack: T1543.003
    crit: suspicious
    if:
      type: text
      regex: timeout[ \w/]+&& sc start
      case_insensitive: true
