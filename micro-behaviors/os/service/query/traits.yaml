defaults:
  for:
    - shell
    - elf
  platforms:
    - linux
traits:
  - id: systemctl-list-units
    desc: systemctl list-units command
    crit: notable
    conf: 0.75
    if:
      type: text
      substr: systemctl list-units
