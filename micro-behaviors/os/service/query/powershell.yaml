defaults:
  platforms: [windows]
  for: [powershell]
  attack: T1007

traits:
  - id: powershell-get-service
    desc: PowerShell queries Windows services
    crit: notable
    conf: 0.88
    if:
      type: symbol
      kind: call
      exact: Get-Service
