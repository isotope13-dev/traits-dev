defaults:
  platforms: [windows]
  for: [pe]
traits:
  - id: create-service-a
    desc: "Creates a new service (ANSI)"
    crit: notable
    conf: 1.0
    if:
      type: symbol
      exact: "CreateServiceA"
