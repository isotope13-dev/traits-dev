defaults:
  for: [pe]
  platforms: [windows]
composite_rules:
  - id: service-reconfig-api-name-cluster
    desc: Service reconfiguration API cluster
    crit: notable
    conf: 0.92
    any:
      - id: reconfigure-and-start-service
      - id: micro-behaviors/os/service/stop::service-stop-control
    unless:
      - id: well-known/app/data/xore-honeypot::benign-context
