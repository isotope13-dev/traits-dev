defaults:
  platforms: [macos]
  for: [shell]
  crit: notable
  conf: 0.9

traits:
  - id: mdutil-disable-indexing
    desc: Shell disables Spotlight indexing for a volume
    if:
      type: text
      regex: '\bmdutil\s+-i\s+off\s+\S+'
