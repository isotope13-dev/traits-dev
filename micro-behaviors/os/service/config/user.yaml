defaults:
  platforms: [linux, macos]
  for: [python]

traits:
  - id: python-user-systemd-unit
    desc: Python writes a user systemd unit
    crit: notable
    conf: 0.94
    if:
      type: text
      regex: '\.config/systemd/user[\s\S]{0,500}(ExecStart|systemctl\s+--user|daemon-reload|enable)'
