defaults:
  platforms: [macos]
  for: [go]
  crit: notable
  conf: 0.9

traits:
  - id: go-launchagent-plist-template
    desc: Go source embeds LaunchAgent plist template
    if:
      type: text
      regex: '<key>RunAtLoad</key>[\s\S]{0,220}<key>KeepAlive</key>'
