defaults:
  platforms: [macos]
  for: [c, objc, macho, shell]
  crit: notable
  conf: 0.85
  attack: T1543.004

traits:
  - id: launchdaemons-path
    desc: macOS LaunchDaemons path reference
    if:
      type: text
      substr: /Library/LaunchDaemons
    unless:
      - id: well-known/tool/development/ipsw
