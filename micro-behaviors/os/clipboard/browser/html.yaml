defaults:
  platforms: [windows, unix]
  for: [html]
  attack: T1115

traits:
  - id: browser-html-clipboard-write
    desc: HTML script writes text to browser clipboard
    crit: notable
    conf: 0.92
    if:
      type: text
      regex: '(?i)\b(navigator|browser)\.clipboard\.writeText\s*\('
