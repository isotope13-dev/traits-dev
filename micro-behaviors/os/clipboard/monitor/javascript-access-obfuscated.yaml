defaults:
  platforms: [windows, unix]
  for: [javascript, typescript]
  attack: T1115
traits:
  - id: browser-clipboard-state-tracking
    desc: Tracks previously read clipboard content
    crit: notable
    conf: 0.86
    if:
      type: text
      regex: '\b(last|previous|old)[A-Za-z_$]{0,40}[Cc]lipboard[A-Za-z_$]{0,40}\b'
