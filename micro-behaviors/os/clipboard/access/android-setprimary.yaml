defaults:
  platforms: [android]
  for: [java]
  crit: notable
  conf: 0.9
  attack: T1115

traits:
  - id: set-primary-clip
    desc: Android clipboard write via setPrimaryClip
    count_min: 1
    if:
      type: text
      substr: "setPrimaryClip"
