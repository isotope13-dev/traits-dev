defaults:
  platforms: [windows, unix, macos]
  for: [javascript, typescript]

traits:
  - id: vscode-platform-clipboard-helper-paths
    desc: VS Code clipboard helper path
    crit: component
    conf: 0.92
    platforms: [windows]
    if:
      type: text
      substr: lib/pc.ps1
