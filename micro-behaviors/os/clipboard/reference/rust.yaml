defaults:
  platforms: [windows, unix]
  for: [rust]
  attack: T1115
traits:
  - id: rust-xclip-command
    desc: Rust invokes xclip clipboard
    crit: notable
    conf: 0.85
    if:
      type: text
      substr: xclip
