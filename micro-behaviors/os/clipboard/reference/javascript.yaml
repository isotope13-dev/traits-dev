defaults:
  platforms: [windows, unix]
  for: [javascript, typescript, html]
  attack: T1115
traits:
  - id: browser-clipboard-api-marker
    desc: References a browser clipboard API
    crit: notable
    conf: 0.8
    if:
      type: text
      regex: '(?i)navigator\.clipboard|clipboardData'

