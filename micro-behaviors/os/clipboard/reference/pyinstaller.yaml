defaults:
  platforms: [windows]
  for: [pe]
traits:
  - id: clipboard-module
    desc: PyInstaller archive bundles clipboard module
    crit: notable
    conf: 0.96
    if:
      type: text
      exact: clipboard)
