defaults:
  platforms: [macos]
  for: [python]
  attack: T1115

traits:
  - id: python-pbcopy-subprocess-write
    desc: Python writes the macOS clipboard through pbcopy
    crit: notable
    conf: 0.94
    if:
      type: text
      regex: 'subprocess\.(run|Popen|check_call)\s*\(\s*\[\s*["'']pbcopy["'']'
