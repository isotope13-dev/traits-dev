defaults:
  platforms: [macos]
  for: [python]
  attack: T1115

traits:
  - id: python-pbpaste-subprocess-read
    desc: Python reads the macOS clipboard through pbpaste
    crit: notable
    conf: 0.94
    if:
      type: text
      regex: 'subprocess\.(run|Popen|check_output)\s*\(\s*\[\s*["'']pbpaste["'']'

