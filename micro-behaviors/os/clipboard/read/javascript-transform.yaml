defaults:
  platforms: [windows, unix]
  for: [javascript, typescript, html]
  attack: T1115
traits:
  - id: browser-execcommand-paste
    desc: Browser execCommand clipboard paste
    crit: notable
    conf: 0.9
    if:
      type: text
      regex: "document\\.execCommand\\s*\\(\\s*['\"]paste['\"]"
