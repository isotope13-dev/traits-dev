defaults:
  platforms: [windows]
  for: [powershell]
  attack: T1018

traits:
  - id: powershell-arp
    desc: PowerShell enumerates the ARP table
    crit: notable
    conf: 0.88
    if:
      type: text
      regex: '(?im)(^|[;&|({\s])arp\s+-a\b'
