defaults:
  platforms: [windows]
  for: [powershell]
traits:
  - id: resolve-dns-name
    desc: PowerShell Resolve-DnsName
    crit: baseline
    conf: 0.8
    if:
      type: text
      word: Resolve-DnsName
