defaults:
  platforms: [windows]
  for: [binaries, scripts]

traits:
  - id: setdns-identifier
    desc: SetDNS identifier token
    crit: component
    conf: 0.94
    if:
      type: text
      regex: '(?i)\bSetDNS\b'
