defaults:
  platforms: [unix]
  for: [swift]
  attack: T1041

traits:
  - id: swift-sbin-netstat-recon
    desc: Swift runs netstat for host recon
    crit: notable
    conf: 0.9
    if:
      type: text
      substr: /sbin/netstat
