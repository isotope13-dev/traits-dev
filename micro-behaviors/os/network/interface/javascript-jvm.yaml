defaults:
  platforms: [routeros, appliance, unix]
  for: [javascript]
  attack: T1016

traits:
  - id: jvm-network-interface-enumeration
    desc: Java network interface enumeration reference
    crit: notable
    conf: 0.9
    if:
      type: text
      substr: NetworkInterface.getNetworkInterfaces
