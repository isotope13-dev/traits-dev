defaults:
  platforms: [windows]
  for: [powershell]

traits:
  - id: powershell-writehost-foregroundcolor
    desc: PowerShell writes colored terminal output
    crit: notable
    conf: 0.85
    if:
      type: text
      regex: '(?i)Write-Host\b[^\r\n]{0,140}-ForegroundColor\b'
