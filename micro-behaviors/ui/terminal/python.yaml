defaults:
  platforms: [unix, windows]
  for: [python]

traits:
  - id: python-console-output-capture
    desc: Configures interpreter console output capture
    crit: notable
    conf: 0.9
    if:
      type: text
      regex: '\b(console_capture|capture_console)\b'
    count_min: 2
