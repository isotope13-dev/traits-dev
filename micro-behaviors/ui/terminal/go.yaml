defaults:
  platforms: [unix, windows]
  for: [go]
traits:
  - id: go-x-term-import
    desc: Go x/term terminal library import
    crit: notable
    conf: 0.8
    if:
      type: string_literal
      substr: golang.org/x/term
