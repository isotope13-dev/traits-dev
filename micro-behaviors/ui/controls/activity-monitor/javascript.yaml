defaults:
  platforms: [unix, windows]
  for: [javascript, typescript]

traits:
  - id: activity-monitor-helper
    desc: ActivityMonitor editor input helper
    crit: notable
    conf: 0.9
    if:
      type: text
      word: ActivityMonitor
