defaults:
  platforms: [unix, windows, macos]
  for: [python]
  crit: baseline
traits:
  - id: qtwidgets-import
    desc: QtWidgets framework import
    crit: baseline
    conf: 0.25
    if:
      type: raw
      substr: "QtWidgets"
