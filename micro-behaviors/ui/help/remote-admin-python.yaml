defaults:
  platforms: [windows, unix]
  for: [python]
  crit: notable
  conf: 0.94

traits:
  - id: documented-remote-admin-command-set
    desc: Documents screen command and file controls
    if:
      type: text
      regex: '(?s)/screen.{0,950}/cmd.{0,400}/getfile'
