defaults:
  for: [source]
  platforms: [windows]
traits:
  - id: messagebox-call-source
    desc: Delphi MessageBox prompt call
    crit: notable
    conf: 0.93
    if:
      type: raw
      regex: '(?i)\bMessageBox\s*\('
