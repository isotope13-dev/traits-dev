defaults:
  platforms: [windows]
  for: [binaries, source]

traits:
  - id: message-box-w
    desc: Compatibility placeholder
    crit: component
    conf: 1.0
    if:
      type: text
      exact: __cleave_missing_message_box_w__
