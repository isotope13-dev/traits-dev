defaults:
  platforms: [windows]
  for: [c, cpp, csharp, rust, go]

traits:
  - id: set-foreground-window-source
    desc: Brings a native window to foreground
    crit: notable
    conf: 0.9
    if:
      type: symbol
      kind: call
      regex: '(^|\.)SetForegroundWindow$'
