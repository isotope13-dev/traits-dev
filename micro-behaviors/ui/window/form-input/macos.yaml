defaults:
  platforms: [macos]
  for: [macho, swift, objc]
  crit: notable
  conf: 0.78

traits:
  - id: macos-secure-text-field-control
    desc: Uses the macOS secure text field control
    if:
      type: symbol
      exact: 'OBJC_CLASS_$_NSSecureTextField'
