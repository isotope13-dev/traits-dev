defaults:
  crit: notable
  conf: 0.94
  platforms: [ios]
  for: [objc, cpp]

traits:
  - id: ios-textfield-text-change-notification
    desc: iOS text field change notification
    if:
      type: text
      substr: UITextFieldTextDidChangeNotification

  - id: ios-textview-text-change-notification
    desc: iOS text view change notification
    if:
      type: text
      substr: UITextViewTextDidChangeNotification
