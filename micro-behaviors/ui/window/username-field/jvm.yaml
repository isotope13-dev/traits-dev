defaults:
  platforms: [android, unix, windows]
  for: [kotlin, java, class]
  crit: notable
  conf: 0.86
traits:
  - id: username-field-identifier
    desc: UI username field identifier
    if:
      type: text
      substr: "usernameField"
    unless:
      - id: metadata/package/files/included::jsii-metadata-basename
