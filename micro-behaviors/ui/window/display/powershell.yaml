defaults:
  platforms: [windows]
  for: [powershell]

traits:
  - id: user32-showwindow-pinvoke
    desc: PowerShell P/Invokes user32 ShowWindow
    crit: notable
    conf: 0.94
    if:
      type: text
      regex: '(?is)DllImport\s*\(\s*["'']user32\.dll["'']\s*\).{0,160}\bShowWindow\b'
