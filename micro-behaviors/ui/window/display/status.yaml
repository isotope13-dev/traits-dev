defaults:
  platforms: [windows, unix]
  for: [html, javascript, typescript]
  crit: baseline
  conf: 0.7

traits:
  - id: please-wait-status-text
    desc: Displays a connection wait status
    if:
      type: text
      regex: '(?i)\b(Please wait|Connecting to|Joining)\b'
