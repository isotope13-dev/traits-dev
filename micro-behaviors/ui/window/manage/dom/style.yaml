defaults:
  platforms: [unix, windows]
  for: [html, javascript, typescript]
  crit: component
traits:
- id: size-prop
  desc: DOM or CSS size property
  if:
    type: text
    regex: '\b(height|width)\b'
