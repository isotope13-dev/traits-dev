defaults:
  platforms: [windows]
  for: [binaries, source]

traits:
  - id: set-parent-api
    desc: Compatibility alias
    crit: component
    conf: 1.0
    if:
      id: micro-behaviors/os/clipboard/access::micro-behaviors/ui/window/manage::set-parent-api
