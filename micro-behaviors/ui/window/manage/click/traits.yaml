defaults:
  platforms: [windows, unix]
  for: [javascript, typescript, html]

traits:
  - id: browser-click-listener
    desc: Registers browser click event listener
    crit: notable
    conf: 0.88
    if:
      type: text
      regex: 'addEventListener\s*\(\s*["'']click["'']'

  - id: browser-computed-click-listener
    desc: Registers computed browser click listener
    crit: notable
    conf: 0.86
    if:
      type: text
      regex: '(document|window)\s*\[[^\]\n]{1,160}addEventLi[^\]\n]{0,160}\]\s*\('

  - id: dom-computed-click-call
    desc: Calls click through computed DOM property
    crit: notable
    conf: 0.86
    if:
      type: text
      regex: '\[[^\]\n]{1,80}click[^\]\n]{0,80}\]\s*\('
