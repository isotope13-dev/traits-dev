defaults:
  platforms: [windows]
  for: [vbs, javascript]
  crit: notable
  conf: 0.86

traits:
  - id: wsh-appactivate
    desc: WSH activates window by title
    if:
      type: text
      regex: '(?i)\.AppActivate\s*\('
