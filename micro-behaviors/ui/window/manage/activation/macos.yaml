defaults:
  for: [macho]
  platforms: [macos]

traits:
  - id: macos-set-activation-policy
    desc: Set macOS app activation policy
    crit: baseline
    conf: 0.75
    if:
      type: text
      exact: 'setActivationPolicy:'
