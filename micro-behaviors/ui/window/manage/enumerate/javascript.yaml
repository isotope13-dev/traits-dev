defaults:
  platforms: [windows, unix]
  for: [javascript, typescript]
  crit: notable
  conf: 0.9

traits:
- id: page-script-element-enumeration
  desc: Enumerates page script elements
  crit: notable
  conf: 0.92
  if:
    type: text
    regex: 'querySelectorAll\s*\(\s*[''\"]script'
