defaults:
  for: [pe]
  platforms: [windows]
traits:
  - id: hidden-main-form-source
    desc: Hides Delphi main form
    crit: suspicious
    conf: 0.97
    if:
      type: string
      exact: "Application.ShowMainForm := false;"
