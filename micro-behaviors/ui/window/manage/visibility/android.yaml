defaults:
  platforms: [android]
  for: [apk, zip, xml]

traits:
  - id: android-exclude-from-recents
    desc: Android activity excluded from recent-apps list
    crit: notable
    conf: 0.9
    if:
      type: text
      substr: 'android:excludeFromRecents="true"'
