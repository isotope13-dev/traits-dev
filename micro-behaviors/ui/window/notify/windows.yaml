defaults:
  for: [pe, dll]
  platforms: [windows]

traits:
  - id: shell-change-notify
    desc: Notify shell of file changes
    crit: baseline
    conf: 0.8
    if:
      type: symbol
      exact: "SHChangeNotify"
