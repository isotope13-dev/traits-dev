defaults:
  platforms: [windows]
  for: [pe, dll]
traits:
- id: set-layered-window-attributes
  desc: 'set layered window attributes'
  crit: notable
  conf: 0.9
  if:
    type: text
    regex: SetLayeredWindowAttribu(tes|ces)
    section: ".text"
