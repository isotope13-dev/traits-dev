defaults:
  platforms: [windows, unix]
  for: [javascript, typescript]
  crit: notable
  conf: 0.9
traits:
  - id: document-body-click-handler
    desc: Installs document body click handler
    if:
      type: symbol
      exact: document.body.onclick
