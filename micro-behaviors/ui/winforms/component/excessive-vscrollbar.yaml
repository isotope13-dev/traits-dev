defaults:
  for: [all]
traits:
  - id: excessive-vscrollbar
    desc: Excessive VScrollBar controls for UI deception.
    crit: suspicious
    count_min: 10
    if:
      type: string
      regex: "VScrollBar\\d+"
