defaults:
  platforms: [unix, windows, macos]
  for: [shell, python, javascript, powershell, pe, elf]
traits:
  - id: hide-dock-icon-action
    desc: "Action to hide application Dock icon"
    crit: suspicious
    if:
      type: raw
      substr: "HideAppIConDockAction"
