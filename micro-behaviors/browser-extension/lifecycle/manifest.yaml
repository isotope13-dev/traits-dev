defaults:
  platforms: [windows, unix]
  for: [chrome-manifest]
  crit: notable
  conf: 0.7

traits:
  - id: content-script-host-scoped
    desc: Content script targets URL hosts
    if:
      type: value
      path: 'content_scripts[*].matches'
      regex: '://'
