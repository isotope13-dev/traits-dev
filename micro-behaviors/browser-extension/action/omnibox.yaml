defaults:
  for: [javascript, typescript]
  platforms: [windows, unix]
  crit: notable
  conf: 0.95
traits:
  - id: omnibox-input-change-listener
    desc: Registers browser omnibox input listener
    if:
      type: symbol
      kind: call
      regex: '(^|\.)omnibox\.onInputChanged\.addListener$'
