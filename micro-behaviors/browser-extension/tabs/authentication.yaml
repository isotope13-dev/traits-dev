defaults:
  platforms: [windows, unix]
  for: [javascript, typescript]
  crit: notable
  conf: 0.95

traits:
  - id: tab-title-substring-check
    desc: Checks a tab title substring
    if:
      type: text
      regex: 'tabs\.onUpdated\.addListener[\s\S]{0,160}\.title\.includes\s*\('
