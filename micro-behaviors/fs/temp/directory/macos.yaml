defaults:
  platforms: [macos]
  for: [scripts, binaries]
traits:
  - id: directory-cocoa
    desc: Look up temp directory via NSTemporaryDirectory
    crit: baseline
    conf: 0.66
    if:
      type: text
      substr: NSTemporaryDirectory
