defaults:
  platforms: [unix]
  for: [perl]
  crit: baseline

traits:
  - id: perl-file-temp-new
    desc: Perl File::Temp temporary file creation
    if:
      type: text
      regex: '\bFile::Temp->new\s*\('
