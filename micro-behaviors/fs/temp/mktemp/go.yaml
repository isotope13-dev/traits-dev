defaults:
  platforms:
    - unix
    - windows
  for: [go, binaries]
traits:
  - id: mktemp-go
    desc: Create temporary files (Go)
    crit: baseline
    conf: 0.66
    if:
      type: text
      regex: (ioutil\.TempFile|os\.CreateTemp)
