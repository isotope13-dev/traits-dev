defaults:
  platforms: [unix]

traits:
  - id: sudo-nopasswd
    desc: NOPASSWD sudo directive
    crit: suspicious
    conf: 0.85
    for:
      - elf
      - shell
    if:
      type: text
      substr: NOPASSWD
    unless:
      - id: metadata/package/testing/harness/runtime::is-test
      - id: metadata/build/ci::tensorflow-user-wrapper
    downgrade:
      any:
        - type: text
          regex: '/etc/sudoers\.d/'
        - type: raw
          regex: '\b(CI_BUILD_[A-Z0-9_]+|GITHUB_ACTIONS|BUILDKITE|TF_BUILD)\b'
        - type: raw
          regex: '\b(JENKINS_HOME|Docker|cloud-init|cloud\.cfg)\b'
        - type: raw
          regex: '\b(container|ci_build\.sh|cloud_config)\b'
        - type: raw
          regex: 'Vagrant User[\s\S]{0,2048}vagrant insecure public key'
    attack: T1548.004