# SQLite Database Indicators
defaults:
  platforms: [linux, unix, android, macos, ios, windows]
  for: [elf, macho, pe]
  crit: notable
  conf: 0.8
traits:
  - id: sqlite-auth-user
    desc: SQLite auth user marker (QLITE_AUTH_USER)
    if:
      type: string_value
      substr: "QLITE_AUTH_USER"
