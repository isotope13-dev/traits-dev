defaults:
  platforms:
    - linux
    - unix
    - android
    - macos
    - ios
    - windows
  for: [binaries]
  crit: notable
  conf: 0.8
traits:
  - id: sqlite-auth-user
    desc: SQLite auth user marker (QLITE_AUTH_USER)
    if:
      type: text
      substr: QLITE_AUTH_USER
