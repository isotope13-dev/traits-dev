defaults:
  platforms: [macos]
  for: [macho, python, shell, applescript]

traits:
  - id: cfbundle-executable-key-reference
    desc: References macOS bundle executable key
    crit: notable
    conf: 0.8
    if:
      type: text
      substr: CFBundleExecutable
