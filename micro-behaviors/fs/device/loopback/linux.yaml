defaults:
  platforms:
    - unix
    - windows
  for: [scripts, binaries]
traits:
  - id: loopback
    desc: Access virtual block devices (loopback)
    crit: notable
    conf: 0.66
    if:
      type: text
      regex: /dev/loop[\$%\w\{\}]{0,16}
