defaults:
  platforms: [windows, unix]
  for: [pe, dll, csharp]
  crit: notable
  conf: 0.9

traits:
  - id: dotnet-serial-ports
    desc: .NET serial-port communication APIs
    if:
      type: text
      word: System.IO.Ports
  - id: dotnet-enumerate-ports
    desc: .NET serial-port enumeration API reference
    if:
      type: text
      word: GetPortNames
