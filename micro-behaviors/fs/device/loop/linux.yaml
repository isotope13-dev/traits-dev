defaults:
  platforms:
    - unix
    - windows
  for: [scripts, binaries]
traits:
  - id: loop-device-path
    desc: References loop block-device path prefix
    crit: notable
    conf: 0.66
    if:
      type: text
      regex: /dev/loop[\$%\w\{\}]{0,16}
