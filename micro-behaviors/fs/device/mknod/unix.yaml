defaults:
  platforms: [linux, macos]
  for: [elf, macho, c]
traits:
  - id: mknod-base
    desc: Create device files
    crit: notable
    conf: 0.66
    if:
      type: symbol
      exact: mknod
  - id: mknodat
    desc: Create device files
    crit: notable
    conf: 0.66
    if:
      type: symbol
      exact: mknodat
