defaults:
  platforms: [linux, macos]
  for: [elf, macho, c, cpp]
traits:
  - id: get-dup
    desc: Look up file permissions
    crit: baseline
    conf: 0.66
    if:
      type: symbol
      regex: ^_?(getmode|stat|lstat|fstat)$
