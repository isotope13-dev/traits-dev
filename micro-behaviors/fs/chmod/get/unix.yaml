defaults:
  platforms: [unix]
  for: [binaries, c]
traits:
  - id: get-dup
    desc: Look up file permissions
    crit: baseline
    conf: 0.66
    if:
      type: symbol
      regex: ^_?(getmode|stat|lstat|fstat)$
