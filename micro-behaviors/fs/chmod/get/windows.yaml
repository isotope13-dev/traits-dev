defaults:
  platforms:
    - unix
    - windows
  for: [scripts, binaries]
traits:
  - id: get-icacls
    desc: Look up file permissions via icacls
    crit: baseline
    conf: 0.66
    if:
      type: text
      substr: icacls
