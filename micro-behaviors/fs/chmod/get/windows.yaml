defaults:
  platforms:
    - unix
    - windows
    - macos
  for:
    - pe
    - powershell
    - batch
    - vbs
traits:
  - id: get-icacls
    desc: Look up file permissions via
    crit: baseline
    conf: 0.66
    if:
      type: string_value
      substr: icacls
