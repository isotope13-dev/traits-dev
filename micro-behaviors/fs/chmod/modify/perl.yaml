defaults:
  platforms: [unix]
  for: [perl]

traits:
  - id: perl-chmod-call
    desc: Perl changes file permissions
    crit: notable
    conf: 0.9
    if:
      type: text
      regex: '\bchmod\s+([0-7]{3,4}|[A-Za-z_$][^;\n]{0,80}),\s*[A-Za-z_$]'
