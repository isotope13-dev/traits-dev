defaults:
  platforms: [macos]
  for: [shell]

traits:
  - id: hdiutil-attach
    desc: Attaches a macOS disk image
    crit: notable
    conf: 0.95
    if:
      type: text
      regex: '\bhdiutil\s+attach\b'
