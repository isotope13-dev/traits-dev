defaults:
  platforms: [linux, macos]
  for: [elf, macho, c]
traits:
  - id: umount-fn
    desc: Unmount file system
    crit: notable
    conf: 0.66
    if:
      type: symbol
      exact: umount
  - id: umount2-fn
    desc: Unmount file system
    crit: notable
    conf: 0.66
    if:
      type: symbol
      exact: umount2
