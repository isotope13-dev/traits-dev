defaults:
  platforms: [windows]
  for: [powershell]
  crit: notable

traits:
  - id: powershell-lastwrite-lower-bound
    desc: PowerShell filters newer file timestamps
    conf: 0.95
    if:
      type: text
      regex: '(?i)\bLastWriteTime(Utc)?\s+-ge\s+\$\w+\b'
