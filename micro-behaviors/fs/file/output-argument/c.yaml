defaults:
  platforms: [unix, windows]
  for: [c, cpp]

traits:
  - id: c-fopen-argv2-binary-overwrite
    desc: C overwrites argv second output path
    crit: notable
    conf: 0.95
    if:
      type: text
      regex: 'fopen\s*\(\s*argv\s*\[\s*2\s*\]\s*,\s*"wb"\s*\)'
