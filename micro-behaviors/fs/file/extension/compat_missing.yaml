traits:
  - id: image-extension
    desc: compat placeholder
    crit: baseline
    conf: 0.1
    if:
      type: string
      exact: __cleave_missing_313dcd630a08__
defaults:
  crit: component
  conf: 0.1
  for:
    - all
