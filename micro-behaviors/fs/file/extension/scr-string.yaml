defaults:
  platforms: [windows, unix]
traits:
  - id: scr-string
    desc: Reference to .scr executable extension
    crit: notable
    conf: 0.7
    for: [scripts, binaries]
    if:
      type: string_literal
      regex: \.scr["']
