defaults:
  platforms: [windows]
  for: [go, pe]

traits:
  - id: go-chunked-file-encryption
    desc: Go chunked file-encryption routine
    crit: notable
    conf: 0.96
    if:
      type: text
      substr: EncryptFileInChunks
