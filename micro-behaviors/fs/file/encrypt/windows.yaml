defaults:
  platforms: [windows]
  for: [pe, dll]
  attack: T1486
traits:
  - id: encrypt-file-api
    desc: EFS file encryption API
    crit: notable
    conf: 0.9
    if:
      type: symbol
      regex: '^EncryptFile[AW]?$'
