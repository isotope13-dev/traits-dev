defaults:
  platforms: [unix, windows]
  for: [php]

traits:
  - id: php-upload-temp-file-access
    desc: PHP accesses uploaded temporary file path
    crit: notable
    conf: 0.9
    if:
      type: text
      substr: "['tmp_name']"
