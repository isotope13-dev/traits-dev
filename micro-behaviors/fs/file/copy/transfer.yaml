defaults:
  platforms: [windows, unix]
  for: [scripts]
  crit: notable
  conf: 0.88

traits:
  - id: skip-existing-option
    desc: Transfer skips existing destination files
    if:
      type: text
      regex: '(?i)--ignore-existing\b'
