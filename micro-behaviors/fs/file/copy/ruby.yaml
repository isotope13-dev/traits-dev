defaults:
  for: [ruby]
  platforms: [unix, windows]
traits:
  - id: ruby-fileutils-copy
    desc: Copies a file with Ruby FileUtils
    crit: notable
    conf: 0.9
    if:
      # FileUtils.cp/copy -> member chain
      type: symbol
      kind: member
      regex: '^FileUtils\.(cp|copy)$'
