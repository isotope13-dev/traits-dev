defaults:
  platforms: [windows, unix]
  for: [python]
traits:
  - id: shutil-copy
    desc: Copies a file (shutil.copy)
    crit: notable
    conf: 0.8
    if:
      type: symbol
      kind: call
      regex: ^shutil\.(copy|copy2|copyfile)$
