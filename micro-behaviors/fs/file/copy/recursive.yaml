defaults:
  platforms: [windows, unix]
  for: [pe, dll, csharp, powershell, xml]

traits:
  - id: copy-files-recursively-token
    desc: Recursive file-copy helper reference
    crit: notable
    conf: 0.85
    if:
      type: text
      word: CopyFilesRecursively
