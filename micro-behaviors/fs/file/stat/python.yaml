# Python file existence / stat checks
defaults:
  platforms: [windows, unix]
  for: [python]
traits:
  - id: path-exists
    desc: Checks file existence (os.path.exists)
    crit: baseline
    conf: 0.85
    if:
      type: symbol
      kind: call
      regex: ^os\.path\.(exists|isfile|isdir)$
