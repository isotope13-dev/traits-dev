defaults:
  platforms: [windows, unix]
  for: [go]

traits:
  - id: go-os-stat-missing-check
    desc: Go tests missing path with os.Stat
    crit: notable
    conf: 0.9
    if:
      type: text
      regex: '(?is)os\.Stat\s*\([^)]{1,240}\)\s*;\s*(err\s*!=\s*nil|os\.IsNotExist\s*\(\s*err\s*\))'
