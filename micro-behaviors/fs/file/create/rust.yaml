defaults:
  for: [rust]
  platforms: [windows, unix]

traits:
  - id: rust-file-create
    desc: Rust File::create call
    crit: notable
    conf: 0.86
    if:
      type: text
      substr: "File::create("
