defaults:
  platforms: [windows, unix]
  for: [go]
  attack: T1041
traits:
  - id: os-create
    desc: Create file via os.Create
    crit: notable
    conf: 0.8
    if:
      type: symbol
      exact: os.Create
