defaults:
  platforms: [windows, unix]
  for: [rust]

traits:
  - id: rust-write-file-identifier
    desc: Rust write_file identifier
    crit: component
    conf: 0.8
    if:
      type: text
      word: write_file
