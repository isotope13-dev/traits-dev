defaults:
  platforms: [windows, unix]
traits:
  - id: adodb-stream-charset-msdos
    desc: ADODB.Stream with MS-DOS charset 437
    crit: suspicious
    conf: 0.8
    for: [javascript, typescript]
    if:
      type: text
      regex: 'ADODB\.Stream[\s\S]{0,100}Charset[\s="''\]\[]+437[''"]'
