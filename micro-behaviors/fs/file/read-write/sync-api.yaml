defaults:
  platforms: [unix, windows]
  for: [javascript, typescript]
  crit: component
  conf: 0.75
traits:
  - id: filesync-api-suffix
    desc: Synchronous file API suffix
    if:
      type: text
      substr: FileSync
