defaults:
  platforms: [linux, macos, windows, unix, android, ios]
traits:
  - id: file-write
    criticality: notable
    description: Writes to a file
    for: [javascript]
    if:
      type: string
      substr: "SaveToFile"
