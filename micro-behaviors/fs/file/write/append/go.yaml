defaults:
  platforms: [unix]
  for: [go]

traits:
  - id: append-ssh-authorized-keys
    desc: Go appends to SSH authorized_keys
    crit: notable
    conf: 0.94
    if:
      type: text
      regex: 'authorized_keys[\s\S]{0,220}os\.O_APPEND'
