defaults:
  platforms: [windows, unix, openwrt]
  for: [csharp, pe, dll]

traits:
  - id: dotnet-filemode-append-token
    desc: .NET append file mode token
    crit: notable
    conf: 0.84
    if:
      type: text
      regex: 'FileMode\.Append|FileMode[^A-Za-z0-9_]{0,24}Append'
