defaults:
  platforms: [windows, unix]
  for: [go]

traits:
  - id: go-write-readonly-file
    desc: Go writes file with read-only mode
    crit: notable
    conf: 0.95
    if:
      type: text
      regex: 'os\.WriteFile\([^)]{0,120},\s*[^,]{0,80},\s*0444\)'
