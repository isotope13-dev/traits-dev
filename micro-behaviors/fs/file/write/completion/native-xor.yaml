defaults:
  platforms: [macos]
  for: [macho]

traits:
  - id: xor-decoded-write-failure-status
    desc: XOR-decoded path-write failure status
    crit: notable
    conf: 0.96
    if:
      type: encoded
      encoding: xor
      regex: '^Write failed to:'
