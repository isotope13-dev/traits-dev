defaults:
  platforms: [unix, windows]
  for: [c, cpp]

traits:
  - id: c-fopen-overwrite-mode
    desc: C opens a file in overwrite mode
    crit: notable
    conf: 0.94
    if:
      type: symbol
      kind: call
      exact: fopen
      arg:
        kind: string
        exact: w
