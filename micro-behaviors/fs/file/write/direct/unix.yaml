defaults:
  platforms: [unix]
  for: [elf, macho, c, cpp, objc]
traits:
  - id: write
    desc: Write files
    crit: baseline
    conf: 0.66
    if:
      type: symbol
      regex: ^(fwrite|write|pwrite|pwrite64)$
  - id: fputc
    desc: Write single bytes to file
    crit: baseline
    conf: 0.66
    if:
      type: symbol
      exact: fputc
