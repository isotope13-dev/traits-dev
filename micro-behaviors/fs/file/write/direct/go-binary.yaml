defaults:
  platforms: [windows, unix]
  for: [elf, macho, pe]

traits:
  - id: go-binary-write-file-text
    desc: Go binary WriteFile symbol text
    crit: baseline
    conf: 0.84
    if:
      type: text
      substr: WriteFile
