defaults:
  platforms: [windows, unix]
  for: [elf, macho, pe]

traits:
  - id: native-binary-write-file-text
    desc: Native binary WriteFile reference
    crit: baseline
    conf: 0.84
    if:
      type: text
      substr: WriteFile
