defaults:
  platforms: [windows, unix]
  for:
    - python
traits:
  - id: py-write
    desc: write() call
    crit: baseline
    conf: 0.5
    if:
      type: string_literal
      regex: \.write\(
  - id: py-write-call
    desc: Python write() call
    crit: notable
    conf: 0.78
    if:
      type: text
      regex: '\.write\s*\('
