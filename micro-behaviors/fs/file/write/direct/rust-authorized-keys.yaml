defaults:
  platforms: [unix]
  for: [rust]
traits:
  - id: rust-authorized-keys-write-flow
    desc: Writes a path derived from SSH authorized_keys
    crit: notable
    conf: 0.95
    if:
      type: value
      path: source.payload_flow.events[*].kind
      exact: ssh-authorized-keys-write
