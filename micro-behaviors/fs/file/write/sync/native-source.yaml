defaults:
  platforms: [unix]
  for: [source]
  crit: component
  conf: 0.92

traits:
  - id: native-write-file-call
    desc: Calls native write or sys_write
    if:
      type: text
      regex: '\b(write|sys_write)\s*\('
