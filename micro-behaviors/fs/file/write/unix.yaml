defaults:
  platforms: [unix]
  for: [binaries, c]
traits:
  - id: write
    desc: Write files
    crit: baseline
    conf: 0.66
    if:
      type: symbol
      regex: ^(fwrite|write|pwrite|pwrite64)$
