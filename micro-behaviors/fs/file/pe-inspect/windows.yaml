# PE File Inspection APIs
# APIs for parsing and inspecting PE file headers
defaults:
  platforms: [windows]
  for: [pe]
  crit: notable
  conf: 0.85
traits:
  - id: image-nt-header
    desc: Parse PE NT headers via dbghelp
    if:
      type: symbol
      exact: "ImageNtHeader"
