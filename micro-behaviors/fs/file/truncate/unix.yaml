defaults:
  platforms: [linux, macos]
  for: [elf, macho, c, cpp]
traits:
  - id: truncate
    desc: Truncate file
    crit: baseline
    conf: 0.5
    if:
      type: symbol
      regex: ^_?(truncate|ftruncate|truncate64|ftruncate64)$
