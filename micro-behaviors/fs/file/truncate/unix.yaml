defaults:
  platforms: [unix]
  for: [binaries, c]
traits:
  - id: truncate
    desc: Truncate file
    crit: baseline
    conf: 0.5
    if:
      type: symbol
      regex: ^_?(truncate|ftruncate|truncate64|ftruncate64)$
