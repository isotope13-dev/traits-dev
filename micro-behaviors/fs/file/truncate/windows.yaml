defaults:
  for: [pe, dll]
  platforms: [windows]
traits:
  - id: set-end-of-file-win
    desc: Truncate file via SetEndOfFile
    crit: baseline
    conf: 0.9
    if:
      type: symbol
      exact: "SetEndOfFile"
