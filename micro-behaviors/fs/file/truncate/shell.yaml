defaults:
  platforms: [unix]
  for: [scripts]
  attack: T1070.002

traits:
  - id: shell-truncate-log-redirection
    desc: Shell truncates log file
    crit: notable
    conf: 0.88
    if:
      type: text
      regex: 'echo\s+-n\s*>\s*/var/log/[A-Za-z0-9_.-]+'
