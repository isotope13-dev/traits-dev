traits:
  - id: delete
    desc: compat placeholder
    crit: baseline
    conf: 0.1
    if:
      type: text
      exact: __cleave_missing_a5dd1e4e2792__
defaults:
  platforms:
    - unix
    - windows
  crit: baseline
  conf: 0.1
  for: [scripts, binaries]
