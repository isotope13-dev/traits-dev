defaults:
  platforms: [linux, macos, windows]
  for: [elf, macho, pe, dll]
  mbc: C0047
  attack: T1070.004

traits:
  - id: delete-files-symbol
    desc: Native delete-files symbol
    crit: notable
    conf: 0.8
    if:
      type: symbol
      regex: deleteFiles?
