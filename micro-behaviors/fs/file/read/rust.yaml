defaults:
  platforms: [windows, unix]
  for: [rust]
  crit: baseline
  conf: 0.86

traits:
  - id: rust-fs-read
    desc: Rust reads a file into bytes
    crit: baseline
    conf: 0.9
    if:
      type: text
      regex: '\b(std::)?fs::read\s*\('

  - id: rust-read-to-string
    desc: Reads a file into a Rust string
    crit: notable
    conf: 0.88
    if:
      type: text
      substr: fs::read_to_string(
    unless:
      - id: metadata/package/testing/presence/harness/

  - id: rust-read-to-end
    desc: Rust read file to buffer
    if:
      type: text
      regex: '\.read_to_end\s*\('
    unless:
      - id: metadata/package/testing/presence/harness/
