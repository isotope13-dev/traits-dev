defaults:
  for: [pe, dll]
  platforms: [windows]
traits:
  - id: read-file
    desc: Read data from file handle
    crit: baseline
    conf: 0.9
    if:
      type: symbol
      regex: "^_?ReadFile(?:@\\d+)?$"
