defaults:
  platforms:
    - unix
    - windows
    - macos
  for: [go, binaries]
traits:
  - id: read-go
    desc: Read files (Go)
    crit: baseline
    conf: 0.66
    if:
      type: text
      regex: os\.\(\*File\)\.Read
