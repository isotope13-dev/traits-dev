defaults:
  platforms: [ios, openwrt, windows]
  for: [csharp, pe, dll]

traits:
  - id: dotnet-readallbytes-token
    desc: .NET File.ReadAllBytes token
    crit: notable
    conf: 0.86
    if:
      type: text
      regex: 'ReadAllBytes|File\.ReadAllBytes'
