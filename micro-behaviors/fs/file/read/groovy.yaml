defaults:
  platforms: [unix, windows]
  for: [groovy]
traits:
  - id: groovy-file-text-read
    desc: Groovy reads file text
    crit: notable
    conf: 0.82
    if:
      type: text
      regex: 'new File\s*\([^\n]{0,80}\)[\s\S]{0,160}\.text'
