defaults:
  platforms: [windows, unix]
  for: [php]

traits:
  - id: php-show-source
    desc: PHP displays source code
    crit: notable
    conf: 0.85
    if:
      type: symbol
      kind: call
      exact: show_source
