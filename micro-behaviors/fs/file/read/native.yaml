defaults:
  platforms: [linux, macos, windows]
  for: [elf, macho, pe]

traits:
  - id: fgets-api-import
    desc: Imports C fgets line-input API
    crit: baseline
    conf: 0.8
    if:
      type: import
      exact: fgets
