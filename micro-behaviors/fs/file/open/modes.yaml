defaults:
  platforms:
    - linux
    - macos
    - windows
    - unix
    - android
    - ios
  for: [c, cpp, binaries]
traits:
  - id: mode-read-write-binary
    desc: Open file in binary read/write mode (rb+,
    crit: notable
    conf: 0.9
    if:
      type: raw
      regex: '"[rw]b\+"'
