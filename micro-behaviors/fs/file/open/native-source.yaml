defaults:
  platforms: [unix]
  for: [source]
  crit: component
  conf: 0.92

traits:
  - id: native-open-file-call
    desc: Calls native open or sys_open
    if:
      type: text
      regex: '\b(open|sys_open)\s*\('
