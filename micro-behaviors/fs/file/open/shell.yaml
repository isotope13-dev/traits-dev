defaults:
  platforms: [unix]
  for: [shell]
  size_min: 512
  size_max: 5000000
traits:
  - id: fopen-reference
    desc: fopen libc call referenced by name
    crit: baseline
    conf: 0.6
    if:
      type: string_literal
      word: fopen
