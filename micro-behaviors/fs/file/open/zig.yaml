defaults:
  platforms: [linux, macos, windows]
  for: [zig]
  crit: notable
  conf: 0.96

traits:
  - id: zig-create-file
    desc: Zig creates a filesystem file
    if:
      type: symbol
      kind: call
      exact: std.fs.cwd.createFile
