defaults:
  platforms: [android]
  for: [java, kotlin]

traits:
  - id: android-asset-open-source
    desc: Android source opens a packaged asset
    crit: baseline
    conf: 0.94
    if:
      type: text
      regex: '\.getAssets\s*\(\s*\)\s*\.\s*open\s*\('
