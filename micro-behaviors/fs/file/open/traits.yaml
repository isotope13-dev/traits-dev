defaults:
  platforms: [unix, windows]
  for: [python, pyc, csharp, pe, dll, javascript, typescript, powershell, go, java]
# Filesystem file opening
traits:
  - id: exodus-wallet-path
    desc: Exodus cryptocurrency wallet
    crit: baseline
    conf: 0.7
    if:
      type: text
      substr: exodus.wallet

# Removed open-go duplicate - use micro-behaviors/fs/file/open/go::os-open (symbol is more precise)
