defaults:
  platforms:
    - unix
    - windows
  for: [binaries]
traits:
  - id: vdata
    conf: 0.8
    crit: suspicious
    desc: '.vdata section name'
    if:
      type: section
      exact: '.vdata'
