defaults:
  platforms:
    - unix
    - windows
  for: [scripts, binaries]
traits:
  - id: vdata
    conf: 0.8
    crit: suspicious
    desc: '.vdata section name'
    if:
      type: text
      regex: \.vdata
