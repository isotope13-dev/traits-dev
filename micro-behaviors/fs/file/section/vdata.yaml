defaults:
  platforms:
    - unix
    - windows
  for: [scripts, binaries]
traits:
  - id: vdata
    conf: 0.8
    crit: suspicious
    desc: Suspicious PE section name ".vdata" (often associated
    if:
      type: text
      regex: \.vdata
