defaults:
  for: [csharp]
  platforms: [windows]

traits:
  - id: file-move-source
    desc: File.Move call in C# source
    crit: notable
    conf: 0.88
    if:
      type: symbol
      kind: call
      exact: File.Move
