defaults:
  platforms: [windows, unix]
  for: [go]

traits:
  - id: go-os-rename
    desc: Go renames a file with os.Rename
    crit: notable
    conf: 0.94
    if:
      type: symbol
      kind: call
      exact: os.Rename
