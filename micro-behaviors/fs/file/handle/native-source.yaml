defaults:
  platforms: [unix]
  for: [c, cpp]
  crit: baseline
  conf: 0.92

traits:
  - id: native-close-file-call
    desc: Calls native close or sys_close
    if:
      type: symbol
      kind: call
      regex: '^(close|sys_close)$'
