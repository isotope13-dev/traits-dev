defaults:
  platforms: [unix]
  for: [source]
  crit: component
  conf: 0.92

traits:
  - id: native-close-file-call
    desc: Calls native close or sys_close
    if:
      type: text
      regex: '\b(close|sys_close)\s*\('
