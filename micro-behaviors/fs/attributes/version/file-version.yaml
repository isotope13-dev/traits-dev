defaults:
  platforms:
  - windows
  for:
  - pe
  - dll
traits:
- id: get-file-version-info-size
  desc: Queries Windows file-version metadata size
  crit: notable
  conf: 0.8
  if:
    type: import
    exact: GetFileVersionInfoSizeA
