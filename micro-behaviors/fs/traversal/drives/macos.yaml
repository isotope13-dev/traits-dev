defaults:
  platforms:
    - macos
  for:
    - applescript
    - scripts
    - macho
traits:
  - id: applescript-list-disks
    desc: AppleScript lists mounted disks
    crit: notable
    conf: 0.85
    if:
      type: text
      substr: list disks
