defaults:
  platforms: [unix, android]
  for: [rust]
  crit: notable
  conf: 0.92

traits:
  - id: rust-process-pid-metadata-path
    desc: Rust formats a /proc PID metadata path
    if:
      type: text
      regex: '/proc/\{\}/(cmdline|status)'
