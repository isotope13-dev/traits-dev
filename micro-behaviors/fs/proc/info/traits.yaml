defaults:
  platforms: [unix, windows, macos]
  for: [shell, python, javascript, powershell, pe, elf]
traits:
  - id: process-pid-exe
    desc: Access executable via /proc
    crit: notable
    conf: 0.9
    if:
      type: string
      regex: "/proc/(self|%d|all)/exe"
