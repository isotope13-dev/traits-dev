defaults:
  platforms:
    - unix
    - windows
  for: [scripts, binaries]
traits:
  - id: process-pid-exe
    desc: Access executable via /proc
    crit: baseline
    conf: 0.9
    if:
      type: text
      regex: /proc/(%d|all)/exe
    unless:
      - type: basename
        exact: tabby
