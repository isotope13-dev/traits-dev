defaults:
  platforms:
    - unix
    - windows
    - macos
  for: [scripts, binaries]
traits:
  - id: process-pid-exe
    desc: Access executable via /proc
    crit: notable
    conf: 0.9
    if:
      type: string_value
      regex: /proc/(self|%d|all)/exe
