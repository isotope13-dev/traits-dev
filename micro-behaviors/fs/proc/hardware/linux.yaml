defaults:
  platforms:
    - unix
    - windows
  for: [scripts, binaries]
traits:
  - id: hardware-nvidia
    desc: Access NVIDIA GPU info in procfs
    crit: notable
    conf: 0.66
    if:
      type: text
      substr: /proc/driver/nvidia
