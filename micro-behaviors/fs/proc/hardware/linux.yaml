# /proc Hardware Information (Linux)
# MBC: E1082 (System Information Discovery)
# ATT&CK: T1082

defaults:
  for: [all]

traits:
  - id: hardware-nvidia
    desc: Access NVIDIA GPU information via
    crit: baseline
    conf: 0.66
    if:
      type: string
      regex: "/proc/driver/nvidia"
