defaults:
  platforms:
    - unix
    - windows
    - macos
  for:
    - elf
    - shell
    - python
traits:
  - id: hardware-nvidia
    desc: Access NVIDIA GPU information via
    crit: notable
    conf: 0.66
    if:
      type: text
      substr: /proc/driver/nvidia
