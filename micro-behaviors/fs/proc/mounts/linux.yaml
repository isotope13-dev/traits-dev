defaults:
  platforms:
    - unix
    - windows
  for: [scripts, binaries]
traits:
  - id: proc-mounts-path
    desc: References /proc/mounts path
    crit: notable
    conf: 0.66
    if:
      type: text
      substr: /proc/mounts
    unless:
      - id: metadata/lang/source::linux-kernel-secondary-tree-path
      - id: micro-behaviors/os/syscall/invoke::linux-kernel-tree-path-scripts
      - id: micro-behaviors/os/syscall/invoke::linux-kernel-tree-path-extra
      - id: micro-behaviors/os/syscall/invoke::linux-kernel-tree-path-mmfs
      - id: micro-behaviors/os/syscall/invoke::linux-kernel-tree-path-any
  - id: mounts-mountinfo
    desc: Read mount info from /proc/self/mountinfo
    crit: notable
    conf: 0.66
    for: [scripts, binaries, c, cpp, go, rust]
    if:
      type: text
      substr: /proc/self/mountinfo
    downgrade:
      # Go's runtime/cgroup detector embeds "/proc/self/mountinfo" in
      # gopclntab regardless of program behavior.
      any:
        - id: metadata/lang/compiled/
        - id: metadata/lang/compiler/systems::go-binary
  - id: noexec-mount-enumeration
    desc: Enumerates filesystem mounts for noexec flags
    crit: notable
    conf: 0.9
    for: [shell]
    if:
      type: text
      regex: '\b(findmnt|cat\s+/proc/mounts)\b.{0,80}\bnoexec\b'
