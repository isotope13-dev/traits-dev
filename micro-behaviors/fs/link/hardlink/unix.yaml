defaults:
  platforms: [linux, macos]
  for: [elf, macho, c]
traits:
  - id: hardlink
    desc: Create hard file links
    crit: baseline
    conf: 0.5
    if:
      type: symbol
      regex: ^_?(link|linkat)$
