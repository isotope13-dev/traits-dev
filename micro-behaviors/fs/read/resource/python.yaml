defaults:
  platforms: [windows, unix]
  for: [python]
traits:
  - id: pkgutil-get-data
    desc: Python packaged resource read
    crit: notable
    conf: 0.9
    if:
      type: text
      substr: pkgutil.get_data
