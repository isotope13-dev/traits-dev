defaults:
  platforms: [windows, unix]
traits:
  - id: self-file-reference
    desc: Self-reference via __FILE__
    crit: baseline
    conf: 0.80
    for: [ruby, php]
    if:
      type: text
      substr: '__FILE__'
    unless:
      - type: text
        regex: namespace\s+Nette
