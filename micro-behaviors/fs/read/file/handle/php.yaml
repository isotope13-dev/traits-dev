defaults:
  crit: baseline
  platforms: [windows, unix]
  for: [php]
traits:
  - id: fread
    desc: File read via fread
    conf: 0.75
    mbc: C0051
    if:
      type: string_literal
      regex: \bfread\s*\(
  - id: fgets
    desc: Line read via fgets
    conf: 0.6
    if:
      type: string_literal
      regex: \bfgets\s*\(
  - id: fopen-dup
    desc: File open operation
    conf: 0.6
    if:
      type: string_literal
      regex: \bfopen\s*\(
