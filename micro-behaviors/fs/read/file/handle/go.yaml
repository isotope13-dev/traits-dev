defaults:
  platforms: [unix, windows]
  for: [go, binaries]
traits:
  - id: read-go
    desc: Go os.File Read method
    crit: baseline
    conf: 0.66
    if:
      type: text
      substr: 'os.(*File).Read'
