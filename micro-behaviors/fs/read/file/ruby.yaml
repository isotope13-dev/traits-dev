defaults:
  platforms: [windows, unix]
  for: [ruby]
  crit: notable
  conf: 0.94

traits:
  - id: ruby-file-binread-content
    desc: Reads binary content with Ruby File.binread
    if:
      type: symbol
      kind: call
      exact: binread

