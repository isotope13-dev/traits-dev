defaults:
  platforms: [linux, macos, windows]
  for: [elf, macho, pe, c]
traits:
  - id: read
    desc: Read file descriptor (read/pread)
    crit: baseline
    conf: 0.7
    if:
      type: symbol
      regex: ^_?(read|pread|pread64)$
