# .NET file-read APIs
defaults:
  platforms: [windows]
  for: [pe, dll]
traits:
  - id: dotnet-read-all-text
    desc: .NET ReadAllText file read
    crit: notable
    conf: 0.7
    if:
      type: text
      exact: ReadAllText
      section: rdata
