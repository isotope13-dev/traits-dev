defaults:
  for: [elixir]
  platforms: [linux, macos, windows]
  crit: notable
  conf: 0.95

traits:
  - id: elixir-file-read
    desc: Elixir reads complete file contents
    if:
      type: symbol
      kind: call
      regex: '^File[.]read!?$'
