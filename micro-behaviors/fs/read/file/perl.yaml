defaults:
  platforms: [unix, windows]
  for: [perl]

traits:
  - id: perl-open-read-mode
    desc: Perl opens a file for reading
    crit: baseline
    conf: 0.9
    if:
      type: text
      regex: '\bopen\s*(\(\s*)?(my\s+)?\$[A-Za-z_]\w*\s*,\s*["'']<'
