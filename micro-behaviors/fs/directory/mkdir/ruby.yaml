defaults:
  platforms: [unix, windows]
  for: [ruby]

traits:
  - id: ruby-fileutils-mkdir-p
    desc: Ruby creates a directory tree
    crit: notable
    conf: 0.9
    if:
      type: text
      regex: '\bFileUtils\.mkdir_p\s*\('
