defaults:
  platforms: [windows, unix]
  for:
    - java
traits:
  - id: mkdir-java
    desc: Create directories (Java)
    crit: baseline
    conf: 0.66
    if:
      type: string_literal
      regex: createDirector(y|ies)
