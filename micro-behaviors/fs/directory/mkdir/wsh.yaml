defaults:
  platforms: [windows]
  for: [javascript, vbs]
  crit: notable
  conf: 0.8
traits:
  - id: wsh-createfolder
    desc: FSO CreateFolder directory creation
    if:
      type: symbol
      regex: CreateFolder
