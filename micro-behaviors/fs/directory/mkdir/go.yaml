defaults:
  conf: 0.9
  platforms: [unix, windows]
  for: [go]
traits:
  - id: os-mkdirall
    desc: Creates a directory tree with Go os.MkdirAll
    crit: notable
    if:
      type: symbol
      exact: os.MkdirAll
  - id: os-mkdir
    desc: Create directory via os.Mkdir
    crit: baseline
    if:
      type: symbol
      exact: os.Mkdir
