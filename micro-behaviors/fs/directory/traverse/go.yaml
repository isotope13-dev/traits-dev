defaults:
  platforms: [windows, unix]
  for: [go, pe, elf, macho]
  attack: T1083
traits:
  - id: filepath-walk
    desc: Enumerate files via filepath.Walk
    crit: notable
    conf: 0.75
    if:
      type: text
      regex: '(^|/)filepath\.Walk$'
