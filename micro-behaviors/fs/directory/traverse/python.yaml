defaults:
  platforms:
    - unix
    - windows
    - macos
  for:
    - python
    - pyc
traits:
  - id: traverse-walk
    desc: Traverse filesystem hierarchy (os.walk)
    crit: notable
    conf: 0.66
    if:
      type: string_value
      substr: os.walk
