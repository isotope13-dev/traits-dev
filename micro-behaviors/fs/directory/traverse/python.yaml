defaults:
  platforms:
    - unix
    - windows
  for:
    - python
    - pyc
traits:
  - id: traverse-walk
    desc: Traverse filesystem hierarchy (os.walk)
    crit: baseline
    conf: 0.66
    if:
      type: text
      substr: os.walk
