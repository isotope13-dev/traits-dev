# Shell directory deletion operations.
defaults:
  platforms: [unix]
  for: [shell]
  crit: notable
  conf: 0.8
  mbc: C0047
  attack: T1070.004

traits:
  - id: sudo-rm-rf
    desc: Shell recursively deletes a directory with sudo
    if:
      type: text
      regex: '\bsudo\s+rm\s+-rf\s+'
