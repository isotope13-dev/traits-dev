defaults:
  for: [ruby]
  platforms: [unix, windows]
  crit: notable
  conf: 0.95
  mbc: C0047
  attack: T1070.004

traits:
  - id: ruby-fileutils-rm-rf
    desc: Ruby recursively deletes a directory tree
    if:
      type: symbol
      regex: '^(FileUtils\.)?rm_rf$'
