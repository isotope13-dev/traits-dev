defaults:
  platforms: [unix, windows]
  for: [ruby]

traits:
  - id: ruby-dir-chdir
    desc: Ruby changes the working directory
    crit: notable
    conf: 0.9
    if:
      type: text
      regex: '\bDir\.chdir\s*\('
