defaults:
  for: [pe]
  platforms: [windows]
traits:
  - id: remove-directory-a
    desc: Remove directory (ANSI)
    crit: baseline
    conf: 0.7
    if:
      type: symbol
      exact: RemoveDirectoryA
  - id: remove-directory-w
    desc: Remove directory (Unicode)
    crit: baseline
    conf: 0.7
    if:
      type: symbol
      exact: RemoveDirectoryW
