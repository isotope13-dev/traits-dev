defaults:
  platforms: [unix, windows]
  for: [php]
  crit: notable

traits:
  - id: php-opendir
    desc: Opens directory with PHP opendir
    if:
      type: symbol
      kind: call
      exact: opendir
