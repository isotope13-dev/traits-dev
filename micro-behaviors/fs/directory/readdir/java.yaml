defaults:
  platforms: [windows, unix]
  for:
    - java
    - class
traits:
  - id: readdir-java
    desc: List directory contents (Java)
    crit: baseline
    conf: 0.66
    if:
      type: raw
      word: listFiles

  - id: readdir-android-apk
    desc: Android APK listFiles reference
    crit: baseline
    conf: 0.78
    platforms: [android]
    for: [apk, zip]
    if:
      type: raw
      word: listFiles
