defaults:
  platforms:
    - unix
    - windows
  for:
    - shell
traits:
  - id: readdir-ls
    desc: List directory contents via /bin/ls
    crit: baseline
    conf: 0.66
    if:
      type: string_literal
      substr: /bin/ls
