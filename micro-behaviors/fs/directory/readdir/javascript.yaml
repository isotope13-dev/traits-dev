defaults:
  platforms:
    - unix
    - windows
    - macos
  for:
    - javascript
traits:
  - id: readdir-node
    desc: List directory contents (Node.js)
    crit: baseline
    conf: 0.66
    if:
      type: string_literal
      regex: \.readdir(Sync)?\(
