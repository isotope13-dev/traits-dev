defaults:
  platforms: [windows, unix]
  for: [rust]
  attack: T1083
traits:
  - id: rust-read-dir
    desc: Enumerates a directory with Rust std::fs
    crit: notable
    conf: 0.82
    if:
      type: text
      regex: std::fs::read_dir\s*\(
