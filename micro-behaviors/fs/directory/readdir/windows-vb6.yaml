defaults:
  for: [pe, dll]
  platforms: [windows]
traits:
  - id: vb6-rtcdir
    desc: VB6 directory enumeration helper
    crit: baseline
    conf: 0.9
    if:
      type: string
      exact: "rtcDir"
