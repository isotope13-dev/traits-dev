defaults:
  platforms:
    - unix
    - windows
    - macos
  for: [go, binaries]
traits:
  - id: readdir-go
    desc: List directory contents (Go)
    crit: baseline
    conf: 0.66
    if:
      type: string_value
      regex: \.(Open|Read)Dir
