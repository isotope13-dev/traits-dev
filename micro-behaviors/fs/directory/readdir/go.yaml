defaults:
  platforms:
    - unix
    - windows
  for: [go, binaries]
traits:
  - id: readdir-go
    desc: List directory contents (Go)
    crit: baseline
    conf: 0.66
    if:
      type: text
      regex: \.(Open|Read)Dir
