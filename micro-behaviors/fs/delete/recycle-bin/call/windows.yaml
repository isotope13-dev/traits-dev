defaults:
  platforms: [windows]
  for: [pe, dll, c, cpp, powershell, vbs]
traits:
  - id: sh-empty-recycle-bin
    desc: SHEmptyRecycleBin API call
    crit: notable
    conf: 0.9
    if:
      type: symbol
      regex: SHEmptyRecycleBin[AW]?$
