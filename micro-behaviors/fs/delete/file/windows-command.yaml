defaults:
  platforms: [windows]
  for: [vbs, batch, powershell, html]
  crit: notable

traits:
  - id: cmd-del-command
    desc: Runs file deletion through cmd.exe
    conf: 0.95
    if:
      type: text
      regex: '(?i)\bcmd(\.exe)?\s+/c\s+del\b'
