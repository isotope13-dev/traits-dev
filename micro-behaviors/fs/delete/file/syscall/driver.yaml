defaults:
  platforms: [windows]
  for: [pe]
traits:
  - id: irp-file-delete
    desc: Direct IRP file deletion (kernel driver)
    crit: suspicious
    conf: 0.9
    if:
      type: text
      regex: Irp(FileDelete|FileCreate|SetFileAttributes)
