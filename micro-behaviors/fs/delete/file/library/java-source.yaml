defaults:
  crit: notable
  conf: 0.9
  platforms: [unix, windows]
  for: [java]
  mbc: C0047
  attack: T1070.004
traits:
  - id: java-files-delete
    desc: Java deletes file through NIO
    if:
      type: text
      regex: '\bFiles\.delete\s*\('
  - id: java-delete-if-exists
    desc: Java deletes file if present
    if:
      type: text
      substr: Files.deleteIfExists
