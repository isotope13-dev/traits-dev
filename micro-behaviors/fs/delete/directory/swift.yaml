defaults:
  platforms: [windows, unix]
  for: [swift]
  crit: notable
  conf: 0.9
  attack: T1070.004

traits:
  - id: swift-directory-enumeration-delete
    desc: Swift directory enumeration and deletion
    if:
      type: text
      regex: 'contentsOfDirectory(AtPath|\s*\()[\s\S]{0,220}removeItem'
