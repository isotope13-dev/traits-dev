defaults:
  platforms: [unix, android, ios]
  for: [shell]

traits:
  - id: force-removes-variable-path
    desc: Force-removes a shell variable path
    crit: notable
    conf: 0.85
    if:
      type: text
      regex: '(?m)^\s*rm\s+-[rf]+\s+["'']?\$\{?[A-Za-z_][A-Za-z0-9_]*\}?["'']?'
