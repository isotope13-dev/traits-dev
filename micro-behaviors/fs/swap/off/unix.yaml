defaults:
  platforms: [unix]
  for: [shell]

traits:
  - id: shell-swapoff-all
    desc: Shell disables all swap
    crit: notable
    conf: 0.9
    if:
      type: text
      regex: '(?m)^[ \t]*((sudo[ \t]+)?swapoff)[ \t]+-a\b'
