defaults:
  platforms: [unix]
  for: [shell]

traits:
  - id: shell-swap-line-enumeration
    desc: Shell scans input for swap lines
    crit: notable
    conf: 0.9
    if:
      type: text
      regex: "awk\\s+['\"]/swap/"
