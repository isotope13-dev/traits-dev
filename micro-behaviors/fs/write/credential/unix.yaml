defaults:
  platforms: [unix]
  for: [scripts, binaries]
  crit: notable
  conf: 0.85

traits:
  - id: unix-shadow-file-append
    desc: Appends a line to /etc/shadow
    attack: T1098
    if:
      type: text
      regex: '>>\s*[''"]?/etc/shadow'
