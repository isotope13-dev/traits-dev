defaults:
  for: [pe, dll]
  platforms: [windows]
  crit: notable
  conf: 0.9

traits:
  - id: write-encrypted-file-raw
    desc: Writes EFS data via raw backup API
    if:
      type: symbol
      exact: WriteEncryptedFileRaw
