defaults:
  platforms: [windows, unix]
  for: [python]

traits:
  - id: python-pyc-output-write
    desc: Writes a Python bytecode file
    crit: notable
    conf: 0.95
    if:
      type: text
      regex: 'open\s*\([^)]{0,120}\.pyc[^)]{0,40}["'']wb["'']\s*\)'
