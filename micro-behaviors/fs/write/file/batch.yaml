defaults:
  for: [batch]
  platforms: [windows]
traits:
  - id: batch-append-redirect
    desc: Batch file append redirection operator
    crit: notable
    conf: 0.75
    mbc: C0016
    if:
      type: text
      regex: (?i)>>[a-z0-9_%~\\./]+
