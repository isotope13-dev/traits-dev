defaults:
  platforms: [windows]
  for: [powershell]
  mbc: C0016

traits:
  - id: powershell-all-stream-append-redirect
    desc: PowerShell appends all output streams
    crit: notable
    conf: 0.95
    if:
      type: text
      regex: '(?m)\*>>\s*\$[A-Za-z_]\w*'
