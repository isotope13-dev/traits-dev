# File Writing in PowerShell
defaults:
  platforms: [linux, macos, windows, unix, android, ios]
  for: [powershell]
  crit: notable
  conf: 0.9
traits:
  - id: write-all-bytes
    desc: Writes binary data
    if:
      type: text
      regex: '(?i)\[System\.IO\.File\]::\s*WriteAllBytes'
  - id: out-file
    desc: Uses Out-File
    if:
      type: text
      regex: '(?i)\bOut-File\b'
  - id: set-content
    desc: Uses Set-Content
    if:
      type: text
      regex: '(?i)\bSet-Content\b'
