defaults:
  for: [powershell]
  platforms: [windows]
traits:
- id: write-all-bytes-ungated
  desc: Writes raw bytes to file
  crit: notable
  conf: 0.9
  if:
    type: text
    substr: '[IO.File]::WriteAllBytes'
