defaults:
  platforms: [windows]
  for: [vbs]
traits:
  - id: vb6-filecopy-output
    desc: VB6 FileCopy output
    crit: notable
    conf: 0.86
    if:
      type: text
      substr: FileCopy Environ$("TEMP") & "\Crypted.exe"
