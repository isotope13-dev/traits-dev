defaults:
  platforms: [windows, unix]
  for:
    - python
  crit: notable
  mbc: C0016
traits:
  - id: python-shutil-copy
    desc: Copy file object (shutil.copyfileobj)
    conf: 0.75
    crit: baseline
    if:
      type: text
      substr: 'shutil.copyfileobj('
