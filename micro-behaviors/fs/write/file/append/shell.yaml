defaults:
  platforms: [unix]
  for: [shell]
  crit: notable
  conf: 0.95

traits:
  - id: tee-hidden-home-cache-append
    desc: tee appends into hidden home cache file
    if:
      type: text
      regex: '\btee\s+-a\s+(~|\$HOME)/\.cache/\.[A-Za-z0-9_.-]+'
