defaults:
  platforms: [windows, unix]
  for:
    - python
  crit: notable
  mbc: C0016
traits:
  - id: python-open-append
    desc: Append to text file (open with 'a')
    conf: 0.8
    if:
      type: text
      substr: open(logFile, "a")
  - id: python-open-append-binary
    desc: Python opens file for binary append
    conf: 0.8
    if:
      type: string_literal
      regex: open\(.{1,220}['"]ab['"]\)
