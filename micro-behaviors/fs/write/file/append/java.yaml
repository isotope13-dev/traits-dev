defaults:
  platforms:
    - unix
    - windows
  for: [scripts, binaries]
traits:
  - id: kotlin-append-text-source
    desc: Kotlin appends text to a file
    crit: notable
    conf: 0.88
    for: [kotlin]
    if:
      type: text
      regex: '\.appendText\s*\('
