defaults:
  for: [go]
traits:
  - id: go-append-file-method-call
    platforms: [windows, unix]
    desc: Go AppendFile method call
    crit: notable
    conf: 0.75
    if:
      type: text
      substr: .AppendFile(
  - id: append-ssh-authorized-keys
    platforms: [unix]
    desc: Go appends to SSH authorized_keys
    crit: notable
    conf: 0.94
    if:
      type: text
      regex: 'authorized_keys[\s\S]{0,220}os\.O_APPEND'
