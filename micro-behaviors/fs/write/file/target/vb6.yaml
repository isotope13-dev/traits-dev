defaults:
  platforms: [windows]
  for: [vbs]
traits:
  - id: vb6-temp-exe-output
    desc: VB6 writes TEMP executable
    crit: notable
    conf: 0.92
    if:
      type: text
      substr: Environ$("TEMP") & "\Crypted.exe"
