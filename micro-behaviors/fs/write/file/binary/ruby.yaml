defaults:
  platforms: [unix, windows]
  for: [ruby]

traits:
  - id: ruby-file-binwrite-content
    desc: Writes binary content with Ruby File.binwrite
    crit: notable
    conf: 0.9
    if:
      type: text
      regex: '\bFile\.binwrite\s*\('
