defaults:
  platforms: [unix, windows]
  for: [ruby]

traits:
  - id: ruby-file-binwrite-content
    desc: Ruby writes binary file content
    crit: notable
    conf: 0.9
    if:
      type: text
      regex: '\bFile\.binwrite\s*\('
