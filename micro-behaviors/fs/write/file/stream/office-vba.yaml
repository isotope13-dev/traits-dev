defaults:
  platforms: [windows]
  for: [oledoc, ooxml]
  crit: notable
  conf: 0.98

traits:
  - id: vba-adodb-stream-file-write
    desc: VBA ADODB stream file-write surface
    if:
      type: raw
      regex: '(?i)(ADODB\.Stream|SaveToFile)'
