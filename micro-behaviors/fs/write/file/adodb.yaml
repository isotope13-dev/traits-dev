defaults:
  platforms:
    - unix
    - windows
    - macos
  for: [scripts, binaries]
traits:
  - id: adodb-stream
    desc: ADODB.Stream object
    crit: notable
    if:
      type: text
      regex: ADODB\.Stream
