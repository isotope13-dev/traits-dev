defaults:
  platforms:
    - unix
    - windows
  for:
    - javascript
    - vbs
traits:
  - id: adodb-stream
    desc: ADODB.Stream object
    crit: notable
    if:
      type: text
      regex: (?i)(ActiveXObject|WScript\.CreateObject|CreateObject)\s*\(.{0,80}ADODB\.Stream
