defaults:
  for:
    - ruby
  platforms:
    - unix
    - windows
traits:
  - id: ruby-file-write-content
    desc: Writes file content with Ruby File.write
    crit: notable
    conf: 0.85
    if:
      type: text
      regex: '\bFile\.write\s*\('
