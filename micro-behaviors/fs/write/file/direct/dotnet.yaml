defaults:
  platforms: [windows, unix, openwrt]
  for: [pe, dll]
traits:
  - id: dotnet-writeallbytes-token
    desc: .NET File.WriteAllBytes token
    crit: notable
    conf: 0.86
    if:
      type: text
      exact: WriteAllBytes
