defaults:
  platforms: [linux, macos]
  for: [elf, macho, c, cpp]
traits:
  - id: fifo
    desc: Create FIFO named pipe via mkfifoat
    crit: notable
    conf: 0.66
    if:
      type: symbol
      exact: mkfifoat
