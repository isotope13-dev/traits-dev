defaults:
  for: [pe, dll, c, cpp]
  platforms: [windows]

traits:
  - id: read-directory-changes-w
    desc: 'Monitor filesystem changes via ReaddirChangesW'
    crit: notable
    conf: 0.9
    attack: T1083
    mbc: C0046
    if:
      type: symbol
      exact: ReadDirectoryChangesW
