# Disk Quota Manipulation (Linux)
# MBC: E1222 (File Permission Modification)
# ATT&CK: T1222
defaults:
  platforms: [linux]
traits:
  - id: quotactl
    desc: Manipulate disk quota
    crit: notable
    conf: 0.66
    for: [elf, c]
    if:
      type: symbol
      exact: "quotactl"
