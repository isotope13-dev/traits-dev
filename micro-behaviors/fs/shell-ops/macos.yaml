defaults:
  platforms: [macos]
  for: [shell]
  crit: notable
  conf: 0.9

traits:
  - id: cp-command-macos
    desc: macOS shell invokes cp file-copy command
    if:
      type: symbol
      exact: cp
