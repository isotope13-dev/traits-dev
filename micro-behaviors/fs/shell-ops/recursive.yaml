defaults:
  platforms: [unix, android, ios]
  for: [shell]
  crit: notable
  conf: 0.9

traits:
  - id: recursive-copy-variable-destination
    desc: Recursive copy to variable destination
    if:
      type: text
      regex: '\bcp\s+-r(f)?\s+.{0,80}\$[A-Za-z_][A-Za-z0-9_]*'
