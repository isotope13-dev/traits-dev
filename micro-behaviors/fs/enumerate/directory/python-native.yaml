defaults:
  platforms: [macos]
  for: [macho]

traits:
  - id: xor-decoded-site-packages-enumeration
    desc: XOR-decoded site-packages enumeration status
    crit: notable
    conf: 0.98
    if:
      type: encoded
      encoding: xor
      substr: site-packages directories
