defaults:
  platforms: [unix, windows]
  for: [rust]

traits:
  - id: rust-glob-file-enumeration
    desc: Rust glob path enumeration
    crit: notable
    conf: 0.92
    if:
      type: symbol
      kind: call
      exact: glob
