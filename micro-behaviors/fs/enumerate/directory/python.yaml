defaults:
  platforms: [windows, unix]
  for: [python]

traits:
  - id: python-directory-listing
    desc: Python lists directory entries
    crit: notable
    conf: 0.86
    attack: T1083
    if:
      type: symbol
      kind: call
      regex: '^os\.(listdir|scandir)$'
