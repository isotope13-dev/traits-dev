defaults:
  platforms: [windows]
  for: [pe, dll, c, cpp]

composite_rules:
  - id: windows-volume-enumeration
    desc: Enumerates Windows volumes
    crit: notable
    conf: 0.9
    attack: T1083
    any:
      - id: micro-behaviors/fs/enumerate/volume::find-first-volume
      - id: micro-behaviors/fs/enumerate/volume::find-first-volume-source
