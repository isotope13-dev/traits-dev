defaults:
  platforms: [windows]
  for: [powershell]
  attack: T1082

traits:
  - id: powershell-get-psdrive
    desc: PowerShell enumerates filesystem drives
    crit: notable
    conf: 0.9
    if:
      type: text
      regex: '(?i)\bGet-PSDrive\b[^\r\n]{0,80}-PSProvider\s+FileSystem\b'
