defaults:
  platforms: [windows, unix]
  for: [python]
traits:
  - id: search-image-extensions
    desc: Search for image file extensions
    crit: notable
    conf: 0.8
    if:
      type: text
      regex: '\*\.jpe?g|\*\.png'
      case_insensitive: true
