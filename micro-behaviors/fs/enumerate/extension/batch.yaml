defaults:
  platforms: [windows]
  for: [batch]

traits:
  - id: batch-recursive-executable-enumeration
    desc: Batch recursively enumerates executable files
    crit: notable
    conf: 0.94
    attack: T1083
    mbc: C0049
    if:
      type: text
      regex: '(?i)for\s+/r\s+"\."\s+%%[A-Za-z]\s+in\s+\(\*\.exe\)'
