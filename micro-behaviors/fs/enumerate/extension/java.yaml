defaults:
  platforms: [unix, windows]
  for: [java]
traits:
  - id: jar-file-listing
    desc: Java directory entry filtered by .jar
    crit: component
    conf: 0.8
    if:
      type: text
      regex: \.getName\(\)\.endsWith\(["']\.jar["']\)
