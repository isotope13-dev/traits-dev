defaults:
  platforms: [unix, windows, macos]
traits:
  - id: get-logical-drive-strings-w
    desc: "GetLogicalDriveStringsW drive enum import"
    crit: baseline
    conf: 0.9
    platforms: [windows]
    for: [pe, dll]
    if:
      type: import
      exact: GetLogicalDriveStringsW
