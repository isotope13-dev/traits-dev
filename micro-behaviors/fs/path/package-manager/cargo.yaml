defaults:
  platforms:
    - unix
    - windows
  for: [scripts, rust, c, cpp, cargo.toml, python]
  crit: notable
  conf: 0.9
  attack: T1552.001

traits:
  - id: cargo-credentials-file
    desc: Cargo credentials file path reference
    if:
      type: literal
      substr: .cargo/credentials
