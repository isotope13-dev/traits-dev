defaults:
  platforms: [android]
  for: [apk]

traits:
  - id: android-external-storage-directory-reference
    desc: Android external storage directory reference
    crit: notable
    conf: 0.86
    if:
      type: text
      substr: getExternalStorageDirectory
