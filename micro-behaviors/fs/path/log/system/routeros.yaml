defaults:
  platforms: [routeros, appliance]
  for: [c, cpp]

traits:
  - id: routeros-log-file-paths
    desc: RouterOS log file paths
    crit: notable
    conf: 0.9
    count_min: 2
    if:
      type: text
      regex: '/var/log/(flash/)?(userlog|newlog|disk\.log)'
