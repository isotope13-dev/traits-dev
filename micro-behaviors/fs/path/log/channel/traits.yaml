defaults:
  platforms: [unix, windows]
  for: [scripts, source, binaries]

traits:
  - id: numbered-session-log-path
    desc: Numbered session log channel path
    crit: notable
    conf: 0.85
    if:
      type: text
      regex: '(?i)\$?(session|sid|channel)\w*\s*[.+]\s*["''][12]\.log["'']'
