defaults:
  platforms:
    - unix
    - windows
    - macos
  for: [scripts, binaries]
  crit: notable
  conf: 0.7
traits:
  - id: multiple-log-files
    desc: Accesses multiple log files
    count_min: 2
    if:
      type: text
      regex: /var/log/(wtmp|btmp)
