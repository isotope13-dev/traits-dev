defaults:
  platforms: [windows]
  for: [kotlin, class, java_class, jar]
  attack: T1041

traits:
  - id: jvm-gcloud-credentials-bytes
    desc: JVM gcloud credential bytes
    crit: suspicious
    conf: 0.9
    # This is a bytecode marker.  Scanning an outer ZIP as raw bytes makes
    # vendored PHP/fixture paths look like JVM credential access.
    for: [class, java_class]
    if:
      type: hex
      pattern: 2E 63 6F 6E 66 69 67 2F 67 63 6C 6F 75 64
    unless:
      - id: metadata/package/files/system-package::container-image-tar-name
      - id: well-known/lib/data/snowflake-jdbc::jdbc-driver-context
