defaults:
  for: [pe, dll, documents]
  platforms: [windows]
composite_rules:
  - id: file-path-discovery-chain
    desc: File path discovery chain
    crit: notable
    conf: 0.75
    any:
      - id: micro-behaviors/fs/path/check::get-module-filename
      - id: micro-behaviors/process/info/current::get-module-filename
