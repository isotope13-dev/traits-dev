defaults:
  platforms: [windows]
  for: [batch, powershell]

traits:
  - id: sibling-powershell-script-path
    desc: References a sibling PowerShell script path
    crit: notable
    conf: 0.9
    if:
      type: text
      regex: '(?i)(%SCRIPT_DIR%|%~dp0)[^"\r\n]{1,48}\.ps1\b'

