defaults:
  platforms: [windows, unix]
  for: [php]

traits:
  - id: realpath-call
    desc: Resolves canonical filesystem path
    crit: notable
    conf: 0.9
    if:
      type: symbol
      kind: call
      exact: realpath
