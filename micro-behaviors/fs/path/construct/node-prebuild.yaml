defaults:
  platforms: [windows, unix]
  for: [javascript]

traits:
  - id: platform-prebuild-node-addon-path
    desc: Selects platform-specific Node addon path
    crit: notable
    conf: 0.96
    if:
      type: text
      regex: '["'']prebuilds["''][^\n]{0,120}["''][A-Za-z0-9_.-]+\.node["'']'
