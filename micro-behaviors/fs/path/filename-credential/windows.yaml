defaults:
  platforms: [unix, windows]
  for: [scripts, binaries, source]

traits:
  - id: windows-sam-hive-path
    desc: References Windows SAM hive path
    crit: notable
    conf: 0.97
    if:
      type: text
      regex: '(?i)Windows[/\\]System32[/\\]config[/\\]SAM\b'
