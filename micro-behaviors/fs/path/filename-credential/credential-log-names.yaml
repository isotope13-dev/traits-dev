defaults:
  platforms: [linux, macos]
  for: [elf, macho]
  attack: T1041

traits:
  - id: password-log-name
    desc: Password log filename
    crit: notable
    conf: 0.6
    if:
      type: text
      regex: '(?i)(^|[/\\])(passwd|shadow)[a-z0-9_-]*\.(log|txt)\b'
  - id: credential-log-name
    desc: Credential log filename
    crit: notable
    conf: 0.6
    if:
      type: text
      regex: '(?i)(^|[/\\])credentials?[a-z0-9_-]*\.(log|txt)\b'
