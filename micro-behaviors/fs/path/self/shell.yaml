defaults:
  platforms: [unix, android, ios]
  for: [shell]

traits:
  - id: realpath-current-script
    desc: Resolves current script path with realpath
    crit: notable
    conf: 0.85
    if:
      type: text
      regex: 'realpath\s+["'']?\$0'
