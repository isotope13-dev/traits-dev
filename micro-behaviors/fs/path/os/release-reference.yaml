defaults:
  platforms: [unix, windows]
  for: [scripts, source]
traits:
  - id: linux-release-file-reference
    desc: Linux OS release filename reference
    crit: notable
    conf: 0.85
    if:
      type: text
      regex: '/etc/os-re|os-release'
