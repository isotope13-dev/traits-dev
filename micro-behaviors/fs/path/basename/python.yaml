defaults:
  platforms: [unix, windows]
  for: [python]
traits:
- id: os-path-basename
  desc: Python os.path.basename call
  crit: component
  conf: 0.7
  if:
    type: text
    substr: os.path.basename
