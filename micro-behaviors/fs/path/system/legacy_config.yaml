defaults:
  platforms:
    - unix
    - windows
  for: [scripts, binaries, batch]
traits:
  - id: legacy-boot-config
    desc: References DOS boot configuration files
    crit: notable
    conf: 0.8
    mbc: E1083
    attack: T1083
    if:
      type: text
      regex: (?i)\b(config\.sys|fdconfig\.sys|autoexec\.bat|fdauto\.bat)\b
