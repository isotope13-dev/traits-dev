defaults:
  platforms: [windows]
  for: [pe, dll, scripts]
traits:
  - id: windows-setup-state-ini-path
    desc: Windows setup state path
    crit: notable
    conf: 0.86
    attack: T1083
    if:
      type: text
      regex: '(?i)C:\\Windows\\Setup\\State\\State\.ini'
