defaults:
  platforms: [unix, windows, macos]
  for: [scripts, binaries]
traits:
  - id: suspicious-dict-override
    desc: Suspicious system dictionary path override
    crit: suspicious
    conf: 1.0
    if:
      type: string_value
      regex: "/usr/share/dict/words/"
