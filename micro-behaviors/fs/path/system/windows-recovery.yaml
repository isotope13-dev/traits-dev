defaults:
  platforms: [windows]
  for: [pe, dll, batch, powershell]
  crit: notable
  conf: 0.92

traits:
  - id: boot-sdi-recovery-loader
    desc: References Windows recovery boot.sdi image
    if:
      type: text
      regex: 'boot\.sdi(\.[A-Za-z0-9]{4,14})?'
      case_insensitive: true
