defaults:
  platforms:
    - unix
    - windows
    - macos
  for: [scripts, binaries]
traits:
  - id: hardcoded-path-var-www-html
    desc: Detects a hardcoded /var/www/html path.
    crit: notable
    if:
      type: string_value
      exact: /var/www/html
