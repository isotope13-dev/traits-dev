defaults:
  platforms: [unix, netbsd]
  for: [perl]
  attack: T1552.001

traits:
  - id: perl-netbsd-pki-private-path
    desc: Perl NetBSD PKI private path
    crit: suspicious
    conf: 0.94
    if:
      type: text
      substr: /usr/pkg/etc/ssl/private
