defaults:
  platforms: [windows]
  for: [scripts, binaries]

traits:
  - id: default-downloads-folder
    for: [text]
    desc: Compatibility placeholder
    crit: component
    conf: 1.0
    if:
      type: text
      exact: __cleave_missing_default_downloads_folder__
  - id: p2p-client-registry-sweep
    for: [text]
    desc: Compatibility placeholder
    crit: component
    conf: 1.0
    if:
      type: text
      exact: __cleave_missing_p2p_client_registry_sweep__
