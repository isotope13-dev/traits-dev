defaults:
  platforms: [unix, windows]
  for: [python, javascript, typescript]

traits:
  - id: wine-drive-c-path
    desc: References Wine drive_c prefix
    crit: notable
    conf: 0.9
    if:
      type: literal
      regex: \.wine/drive_c
