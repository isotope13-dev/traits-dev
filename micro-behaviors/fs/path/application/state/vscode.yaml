defaults:
  platforms: [windows, unix]
  for: [python, shell, javascript]
  crit: notable
traits:
  - id: vscode-state-store
    desc: VS Code state store paths
    conf: 0.9
    if:
      type: text
      regex: state\.vscdb(\.backup)?|storage\.json
