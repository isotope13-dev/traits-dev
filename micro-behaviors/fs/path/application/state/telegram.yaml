defaults:
  platforms: [windows, unix]
  for: [scripts, binaries]
  crit: notable
  conf: 0.85

traits:
  - id: telegram-desktop-data-path
    desc: Telegram Desktop application-data path
    conf: 0.9
    if:
      type: text
      regex: 'Telegram Desktop[/\\]+(tdata|[A-Za-z0-9_.-]+[/\\]+tdata)'
