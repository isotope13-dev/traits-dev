defaults:
  platforms: [unix]
  for: [go, shell, javascript, python, rust, c, cpp]
  crit: notable
  conf: 0.9

traits:
  - id: system-ca-trust-anchor-directory-reference
    desc: Reference to a system CA trust-anchor directory
    if:
      type: text
      substr: /etc/pki/ca-trust/source/anchors/
