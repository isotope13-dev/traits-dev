defaults:
  platforms: [routeros, appliance]
  for: [c, cpp]

traits:
  - id: routeros-flash-backup-path
    desc: RouterOS flash backup path
    crit: notable
    conf: 0.9
    if:
      type: text
      substr: /var/log/flash/backup.rsc
