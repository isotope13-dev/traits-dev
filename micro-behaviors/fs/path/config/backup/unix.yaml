defaults:
  platforms: [unix]
  for: [shell]
  crit: notable
  conf: 0.85

traits:
  - id: database-backup-sql-path
    desc: Database backup SQL path
    if:
      type: text
      regex: '(?i)(^|[/\s"''=])[A-Za-z0-9_.-]*(backup|dump)\.sql\b'
