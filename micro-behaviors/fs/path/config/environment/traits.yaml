# References to dotenv-style application configuration files.
defaults:
  platforms: [unix, windows]
  for: [scripts]
  crit: notable
  conf: 0.9

traits:
  - id: env-file-path
    desc: References dotenv configuration file path
    if:
      type: text
      regex: '(^|/)\.env($|\.)'
