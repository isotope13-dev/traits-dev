defaults:
  platforms: [openbsd, bsd, unix]
  for: [shell]
  attack: T1041
  mbc: E1041

traits:
  - id: shell-xor-passwd-source
    desc: Shell XOR encodes passwd path
    crit: suspicious
    conf: 0.92
    if:
      type: text
      regex: '"/etc/passwd"\s+XOR'
