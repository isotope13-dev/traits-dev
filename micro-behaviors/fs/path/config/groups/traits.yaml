defaults:
  platforms:
    - unix
    - windows
  for: [scripts, binaries]
  crit: notable
  conf: 0.8
  attack: T1069
traits:
  - id: etc-group
    desc: /etc/group group database
    if:
      type: text
      substr: /etc/group
    unless:
      - id: metadata/file/format/
      - id: metadata/package/testing/harness/runtime/
      - id: metadata/file/catalog/identity/
      # Wiki/docs discuss the path; scripts/binaries that open it still match.
      - id: metadata/file/profile/identity::static-html-shell
      - id: metadata/file/profile/identity::generated-documentation-page
  - id: etc-gshadow
    desc: /etc/gshadow group passwords
    conf: 0.85
    if:
      type: text
      substr: /etc/gshadow
    unless:
      - id: metadata/file/format/
      - id: metadata/file/catalog/identity/
