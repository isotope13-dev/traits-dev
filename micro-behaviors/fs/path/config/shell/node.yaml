defaults:
  platforms: [unix]
  for: [javascript, typescript]

traits:
  - id: node-user-shell-rc-targets
    desc: Node references user shell rc files
    crit: notable
    conf: 0.9
    if:
      type: text
      regex: '\.bashrc[\s\S]{0,180}\.zshrc[\s\S]{0,180}\.profile'
