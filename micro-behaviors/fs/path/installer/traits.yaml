defaults:
  platforms: [windows]
  for: [all]

traits:
  - id: windows-run-key-executable-reference
    desc: Windows Run key references an executable
    crit: notable
    conf: 0.97
    attack: T1547.001
    if:
      type: text
      regex: '(?is)CurrentVersion\\Run.{0,180}(#|\\|\.exe)'
