defaults:
  platforms: [unix, windows]
  for: [scripts, source, binaries]

traits:
  - id: android-apk-reference
    desc: References Android APK path or suffix
    crit: notable
    conf: 0.94
    if:
      type: text
      regex: '(?i)["''][^"'']{0,160}\.apk\b'
