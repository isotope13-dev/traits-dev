defaults:
  platforms:
    - unix
    - windows
  for: [scripts, binaries]
traits:
  - id: hta-extension
    desc: References an HTA filename or path
    crit: notable
    conf: 0.8
    for: [scripts, binaries, html]
    if:
      type: text
      regex: '(?i)\.hta\b'
