defaults:
  platforms:
    - unix
    - windows
  for: [pe, elf, macho, zip]
traits:
  - id: scr-extension
    desc: '.scr Windows screensaver filename extension'
    crit: notable
    if:
      type: path
      regex: '(?i)(^|[\\/])[^\\/]{1,64}\.scr$'
