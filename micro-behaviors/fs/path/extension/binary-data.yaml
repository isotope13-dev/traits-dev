defaults:
  platforms: [windows]
  for: [pe, dll]
  crit: component
  conf: 0.82

traits:
  - id: short-bin-sidecar-reference
    desc: Short BIN sidecar reference
    if:
      type: text
      section: .rdata
      regex: '(?i)\b[a-z0-9_]{4,8}\.bin\b'
