defaults:
  platforms: [unix]
  for: [scripts, binaries]
  crit: notable
  conf: 0.7

traits:
  - id: dev-kmsg-path
    desc: References /dev/kmsg kernel message device
    if:
      type: text
      substr: /dev/kmsg
