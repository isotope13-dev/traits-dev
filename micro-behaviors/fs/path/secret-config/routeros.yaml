defaults:
  platforms: [routeros, appliance, unix]
  for: [java, kotlin, rust, elf]

traits:
  - id: routeros-config-backup-path
    desc: RouterOS hidden configuration backup path
    crit: suspicious
    conf: 0.92
    if:
      type: text
      substr: /.config/backup.rsc
