defaults:
  platforms: [unix]
  for: [swift]
  attack: T1041

traits:
  - id: swift-package-secret-paths
    desc: Swift targets package secrets
    crit: suspicious
    conf: 0.94
    if:
      type: text
      regex: '\.cargo/credentials\.toml|\.gem/credentials'
