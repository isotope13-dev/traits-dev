defaults:
  platforms: [windows]
  for: [batch]
  crit: component
traits:
  - id: batch-multiple-if-exist-gates
    desc: Batch multiple file gates
    conf: 0.82
    count_min: 3
    if:
      type: text
      regex: '(?i)\bif\s+exist\s+"?[^"\r\n]{1,80}"?\s*\('
