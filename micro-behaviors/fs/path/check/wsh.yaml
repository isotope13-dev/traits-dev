defaults:
  platforms: [windows]
  for: [javascript, vbs, html]

traits:
  - id: fso-file-exists
    desc: FileSystemObject checks whether a path exists
    crit: notable
    conf: 0.92
    if:
      type: text
      regex: '(?i)\b[A-Za-z_][A-Za-z0-9_]{0,40}\.\s*FileExists\s*\('
