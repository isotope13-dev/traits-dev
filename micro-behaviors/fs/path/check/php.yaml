defaults:
  platforms: [windows, unix]
  for: [php]
traits:
  - id: php-is-writable-call
    desc: PHP is_writable path check
    crit: notable
    conf: 0.9
    if:
      type: text
      substr: is_writable
