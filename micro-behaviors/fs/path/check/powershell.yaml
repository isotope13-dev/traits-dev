# Neutral PowerShell path checks.
defaults:
  platforms: [windows]
  for: [powershell]

traits:
  - id: test-path
    desc: PowerShell checks path existence
    crit: baseline
    conf: 0.7
    if:
      type: text
      word: Test-Path
