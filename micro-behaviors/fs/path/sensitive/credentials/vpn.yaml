# OpenVPN / TLS client config & credential file references. Neutral path
# capabilities — VPN clients (openvpn), NetworkManager (nmcli/nmtui), editors,
# and git-lfs reference these names benignly. Credential-theft intent comes from
# composites that pair them with the ipsec.secrets/chap-secrets cluster.
defaults:
  platforms: [unix]
  for: [go, elf, c, cpp, objc, python]
traits:
  - id: openvpn-path-or-config-ref
    desc: OpenVPN path or configuration reference
    crit: notable
    conf: 0.9
    if:
      type: text
      # Path/config shape only. A bare word had to be followed by whitespace,
      # which made every prose mention match — FreeBSD sys/priv.h's
      # `/* Administer OpenVPN DCO. */` comment is not a config reference.
      # A separator (`/` or `.`) keeps /etc/openvpn/, openvpn.conf, *.ovpn.
      regex: '(?i)\bopenvpn[/.]|\.ovpn\b'
    unless:
      - id: metadata/lang/source::linux-kernel-secondary-tree-path
      - id: micro-behaviors/os/syscall/invoke::linux-kernel-tree-path-scripts
      - id: micro-behaviors/os/syscall/invoke::linux-kernel-tree-path-extra
      - id: micro-behaviors/os/syscall/invoke::linux-kernel-tree-path-mmfs
      - id: metadata/lang/source::linux-kernel-primary-tree-path
      - id: micro-behaviors/os/syscall/invoke::linux-kernel-source-context
  - id: tls-client-key-path
    desc: TLS client.key file path
    crit: notable
    conf: 0.85
    if:
      type: text
      # Bare `client.key` is a common TLS KeyFile example (Consul, etc.).
      # Keep this to OpenVPN-shaped paths.
      regex: '(?i)openvpn.{0,40}client\.key'
