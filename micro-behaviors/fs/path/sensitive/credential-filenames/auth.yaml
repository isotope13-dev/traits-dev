defaults:
  platforms: [unix, windows]
  for: [javascript, typescript, scripts, binaries]

traits:
  - id: auth-json-join-argument
    desc: Auth JSON path construction
    crit: notable
    conf: 0.9
    if:
      type: text
      regex: '(join|resolve)[\s\S]{0,120}["'']auth\.json["'']'
