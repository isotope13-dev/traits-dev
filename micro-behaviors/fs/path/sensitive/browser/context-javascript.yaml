defaults:
  for: [javascript, typescript]
  platforms: [linux, macos, windows, unix, android, ios]
traits:
  - id: window-location-hostname
    desc: Reads browser location hostname
    crit: notable
    conf: 0.75
    if:
      type: text
      regex: '\b(window\.)?location\.hostname\b'
