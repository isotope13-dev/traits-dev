defaults:
  platforms: [unix, windows, macos]
  for: [macho, objc, swift, applescript]
traits:
  - id: login-data
    desc: Reference to browser Login Data (credentials)
    crit: suspicious
    conf: 0.8
    mbc: T1555.003
    attack: T1555.003
    if:
      type: string_value
      substr: "Login Data"
