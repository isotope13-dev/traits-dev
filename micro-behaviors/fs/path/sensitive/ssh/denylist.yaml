defaults:
  platforms: [unix, windows]
  for: [javascript, typescript]

traits:
  # Security guards commonly keep private-key filenames in a read-deny list.
  # This is defensive policy and must not be mistaken for a directory stealer.