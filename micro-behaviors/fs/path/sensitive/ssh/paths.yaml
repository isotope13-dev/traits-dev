defaults:
  platforms: [unix, windows]
  for: [shell, python]

traits:
  - id: ssh-dir-reference
    desc: SSH configuration directory reference
    crit: notable
    conf: 0.9
    if:
      type: text
      regex: '(\bls\b|\bcd\b)\s+(-[A-Za-z0-9-]+\s+)?~/?\.ssh\b'
