defaults:
  platforms: [unix, windows, macos]
  for: [macho, objc, swift, applescript]
traits:
  - id: keys
    desc: Reference to SSH keys directory
    crit: notable
    conf: 0.8
    mbc: T1552.004
    attack: T1552.004
    if:
      type: string_value
      substr: ".ssh"
