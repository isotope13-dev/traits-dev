defaults:
  platforms: [netbsd, unix]
  for: [c, cpp, objc, elf]
  attack: T1552.001

traits:
  - id: native-bsd-credential-paths
    desc: Native BSD credential paths
    crit: suspicious
    conf: 0.94
    if:
      type: text
      regex: '/etc/master\.passwd[\s\S]{0,700}(id_rsa|authorized_keys|\.pgpass|\.netrc)'
