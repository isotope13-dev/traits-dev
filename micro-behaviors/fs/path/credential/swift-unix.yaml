defaults:
  platforms: [unix]
  for: [swift]
  attack: T1041

traits:
  - id: swift-container-aws-paths
    desc: Swift targets cloud secrets
    crit: suspicious
    conf: 0.94
    if:
      type: text
      regex: '\.docker/config\.json[\s\S]{0,220}aws/credentials'
