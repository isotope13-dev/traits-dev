defaults:
  platforms: [netbsd, unix]
  for: [rust, elf]
  attack: T1552.001

traits:
  - id: rust-netbsd-secret-file-bundle
    desc: Rust NetBSD secret file bundle
    crit: suspicious
    conf: 0.94
    if:
      type: text
      regex: 'id_rsa[\s\S]{0,500}\.docker/config\.json[\s\S]{0,500}/etc/master\.passwd'
