defaults:
  platforms: [unix]
  for: [shell]
  attack: T1552.001

traits:
  - id: shell-cloud-browser-token-bundle
    desc: Shell bundles browser tokens
    crit: suspicious
    conf: 0.94
    if:
      type: text
      regex: 'Login Data[\s\S]{0,520}\.aws/credentials[\s\S]{0,520}\.docker/config\.json'
