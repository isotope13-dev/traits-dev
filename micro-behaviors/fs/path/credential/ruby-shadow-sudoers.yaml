defaults:
  platforms: [openbsd, bsd, unix]
  for: [ruby]
  attack: T1041

traits:
  - id: ruby-openbsd-credential-scan
    desc: Ruby OpenBSD credential scan
    crit: suspicious
    conf: 0.94
    if:
      type: text
      regex: /etc/shadow[\s\S]{0,220}/etc/sudoers
