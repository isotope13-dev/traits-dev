defaults:
  platforms: [panos, appliance, unix]
  for: [go, elf]
  attack: T1041

traits:
  - id: go-panos-secret-path-set
    desc: Go PAN-OS secret path set
    crit: suspicious
    conf: 0.94
    if:
      type: text
      regex: '/etc/shadow[\s\S]{0,420}/root/\.bash_history'
