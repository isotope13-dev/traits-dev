defaults:
  platforms: [unix]
  for: [c, cpp, objc, elf]
  attack: T1552.001

traits:
  - id: c-unix-ssh-token-files
    desc: C Unix SSH token files
    crit: suspicious
    conf: 0.94
    if:
      type: text
      regex: '\.ssh/id_rsa[\s\S]{0,260}\.ssh/known_hosts[\s\S]{0,260}\.netrc'
