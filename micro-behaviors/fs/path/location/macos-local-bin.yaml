defaults:
  platforms: [macos]
  for: [macho]

traits:
  - id: local-bin-path-reference
    desc: References a user-local executable bin directory
    crit: notable
    conf: 0.86
    if:
      type: text
      substr: /.local/bin/
