defaults:
  platforms: [unix]
  for: [python, pyc]

traits:
  - id: python-hidden-device-identifier-path
    desc: Hidden device identifier path
    crit: notable
    conf: 0.9
    if:
      type: text
      regex: 'expanduser.{0,120}~/\.[^"'']{0,80}(device|machine|hwid|host)[_-]?id'
