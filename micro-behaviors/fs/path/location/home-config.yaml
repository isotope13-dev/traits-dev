defaults:
  platforms: [unix]
  for: [python]
traits:
- id: home-config-documents-reference
  desc: References config or document paths under home
  crit: notable
  conf: 0.78
  if:
    type: text
    regex: (home_dir|Path\.home\(\)).{0,200}(Documents|Desktop|\.config)
