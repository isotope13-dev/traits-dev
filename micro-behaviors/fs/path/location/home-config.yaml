defaults:
  platforms: [unix]
  for: [python]
traits:
- id: home-config-documents-reference
  desc: Home config/documents path reference
  crit: notable
  conf: 0.78
  if:
    type: text
    regex: (home_dir|Path\.home\(\)).{0,200}(Documents|Desktop|\.config)
