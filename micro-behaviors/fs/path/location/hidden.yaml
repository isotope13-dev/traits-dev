defaults:
  platforms: [windows, unix]
  for: [scripts, text]

traits:
  - id: hidden-dir-literal
    desc: Hidden directory path literal
    crit: component
    conf: 0.84
    if:
      type: string_literal
      regex: '(^|/|~/)\.[A-Za-z_][\w-]{0,30}(/|$)'
