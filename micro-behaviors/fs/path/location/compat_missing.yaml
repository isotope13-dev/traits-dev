defaults:
  platforms: [windows]
  for: [scripts, binaries]

traits:
  - id: desktop-pb-payload-path
    desc: Compatibility placeholder
    crit: component
    conf: 1.0
    if:
      type: text
      exact: __cleave_missing_desktop_pb_payload_path__
