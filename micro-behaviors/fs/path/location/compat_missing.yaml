defaults:
  platforms: [windows]
  for: [scripts, binaries]

traits:
  - id: desktop-pb-payload-path
    desc: Desktop .pb payload path
    crit: component
    conf: 0.95
    if:
      type: text
      regex: '\\Desktop\\[A-Za-z0-9._ -]{1,64}\.pb'
