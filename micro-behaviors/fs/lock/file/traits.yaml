defaults:
  platforms: [linux, windows, macos]
  for: [pe, elf, macho, c, cpp, rust, go]

traits:
  - id: printf-lock-sidecar-format
    desc: Formats a .lock sidecar filename
    crit: component
    conf: 0.82
    if:
      type: text
      regex: '(?i)%s\.lock\b'

  - id: printf-journal-sidecar-format
    desc: Formats a .journal sidecar filename
    crit: component
    conf: 0.82
    if:
      type: text
      regex: '(?i)%s\.journal\b'
