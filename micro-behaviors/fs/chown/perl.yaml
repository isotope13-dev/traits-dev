defaults:
  platforms: [unix]
  for: [perl]

traits:
  - id: perl-chown-call
    desc: Perl changes file ownership
    crit: notable
    conf: 0.9
    if:
      type: text
      regex: '\bchown\s+[A-Za-z_$][^;\n]{0,160};'
