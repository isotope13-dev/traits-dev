# File System Read/Write Traits
# MBC: OB0009 (File System) → E1083 (File and Directory Discovery)
# ATT&CK: T1083 (File and Directory Discovery), T1005 (Data from Local System)
# NOTE: stat and access functions moved to micro-behaviors/fs/file/stat/unix

traits:
  # NOTE: flock moved to micro-behaviors/fs/lock/flock/unix

  - id: fcntl
    desc: File descriptor manipulation and control
    crit: baseline
    mbc: "E1083"
    attack: "T1083"
    conf: 0.4
    for: [elf, macho, so, dylib, c]
    if:
      type: symbol
      regex: "^_?fcntl$"
    platforms: [unix, linux, macos]
