# Console Logging Patterns
# Detects common logging patterns used in tools and exploits

defaults:
  for: [all]

traits:
  - id: hacker-tool-log-marker
    desc: "Hacker tool logging marker ([*], [+], ...)"
    crit: notable
    conf: 0.8
    if:
      type: string
      regex: '(?m)^\[[*+\-!]\]\s'

  - id: shellcode-execution-status
    desc: Shellcode execution status logging
    crit: suspicious
    conf: 0.92
    count_min: 2
    if:
      type: string
      regex: '(?i)(shellcode finished with a return value|Allocating %d bytes of memory)'
