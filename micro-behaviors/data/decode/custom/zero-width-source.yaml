defaults:
  platforms: [linux, macos, windows]
  for: [scripts, source, class, java_class]
  crit: notable
  conf: 0.9

composite_rules:
  - id: zero-width-radix-four-decoder
    desc: Source decodes radix-four zero-width symbols
    all:
      - id: metadata/file/invisible-unicode::zero-width-only-string
      - id: micro-behaviors/data/source/arithmetic::radix-four-left-shift
