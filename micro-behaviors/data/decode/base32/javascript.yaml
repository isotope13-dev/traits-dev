defaults:
  platforms: [windows]
  for: [javascript, typescript]
  crit: notable
  conf: 0.9

traits:
  - id: dotnet-base32-decoder-in-script
    desc: JavaScript embeds a .NET Base32 decoder
    if:
      type: text
      regex: '(?i)FromBase32String\s*\('
