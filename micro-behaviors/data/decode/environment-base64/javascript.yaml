defaults:
  platforms: [unix, windows]
  for: [javascript, typescript]

traits:
  - id: atob-process-env-value
    desc: Decodes a process environment value with atob
    crit: notable
    conf: 0.95
    if:
      type: text
      substr: atob(process.env.
