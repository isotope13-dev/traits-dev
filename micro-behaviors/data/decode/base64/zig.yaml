defaults:
  platforms: [linux, macos, windows]
  for: [zig]
  crit: notable
  conf: 0.94

traits:
  - id: zig-standard-base64-decode-call
    desc: Zig decodes Base64 data
    if:
      type: symbol
      kind: call
      exact: std.base64.standard.Decoder.decode
