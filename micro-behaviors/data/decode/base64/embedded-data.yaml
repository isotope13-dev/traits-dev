defaults:
  for: [data]
  platforms: [windows, linux, macos]
  crit: notable
  conf: 0.9

traits:
  - id: embedded-atob-call-text
    desc: Data contains JavaScript atob call
    if:
      type: text
      regex: '(?i)\batob\s*\('
