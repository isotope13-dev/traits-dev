defaults:
  for: [elixir]
  platforms: [linux, macos, windows]
  crit: notable
  conf: 0.95

traits:
  - id: elixir-base64-decode
    desc: Elixir decodes Base64 data
    if:
      type: symbol
      kind: call
      regex: '^Base[.]decode64!?$'
