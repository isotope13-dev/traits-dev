defaults:
  platforms: [unix, windows]
  for: [clojure]
  crit: notable
  conf: 0.95

traits:
  - id: clojure-jvm-base64-decoder-accessor
    desc: Clojure obtains JVM Base64 decoder
    if:
      type: symbol
      kind: call
      exact: Base64/getDecoder
