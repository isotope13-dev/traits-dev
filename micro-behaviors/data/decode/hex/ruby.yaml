defaults:
  platforms: [unix, windows]
  for: [ruby]
  crit: notable
  conf: 0.8

traits:
  - id: ruby-pack-hex-decode
    desc: Ruby pack decodes hexadecimal data
    if:
      type: text
      regex: \.pack\(['"]H\*['"]\)
