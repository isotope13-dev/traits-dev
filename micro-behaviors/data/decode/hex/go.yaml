defaults:
  platforms: [windows, unix]
  for: [go]
  crit: notable
  conf: 0.94

traits:
  - id: go-hex-decode-string
    desc: Go decodes a hexadecimal string
    if:
      type: symbol
      kind: call
      exact: hex.DecodeString
