defaults:
  platforms: [windows, unix]
  for: [scripts, binaries]

traits:
  - id: xxd-tool-reference
    desc: xxd hexadecimal dump utility reference
    crit: component
    conf: 0.6
    if:
      type: text
      word: xxd
