defaults:
  platforms: [unix, windows]
  for: [shell, applescript]
  crit: notable
  conf: 0.8

traits:
  - id: xxd-hex-decode
    desc: Shell hex decoding via xxd
    mbc: C0053
    if:
      type: text
      regex: '\bxxd\s+-r\s+-p\b'
