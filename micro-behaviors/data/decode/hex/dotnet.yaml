defaults:
  platforms: [windows, unix]
  for: [csharp, pe, dll]

traits:
  - id: dotnet-hexstring-conversion
    desc: .NET hex string decoding
    crit: notable
    conf: 0.86
    if:
      type: text
      regex: 'FromHexString|Convert\.ToByte'
