defaults:
  platforms: [unix]
  for: [elf, macho]
  crit: notable
  conf: 0.9

traits:
  - id: native-decode-cli-flag
    desc: Native --decode command-line option
    if:
      type: text
      regex: '(?i)(^|[\s"''=:,])--?decode([\s"''=:,]|$)'
