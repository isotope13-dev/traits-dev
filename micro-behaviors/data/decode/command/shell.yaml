defaults:
  platforms: [unix, windows]
  for: [shell]
  crit: notable
  conf: 0.6

traits:
  - id: od-decode-command
    desc: od command invoked with option flags
    if:
      type: text
      regex: '\bod\s+-'
