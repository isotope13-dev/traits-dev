defaults:
  platforms: [unix, windows]
  for: [c]

traits:
  - id: c-b64decode-reference
    desc: C source references a Base64 decode operation
    crit: notable
    conf: 0.9
    if:
      type: text
      word: b64decode
