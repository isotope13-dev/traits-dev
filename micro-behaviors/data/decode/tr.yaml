defaults:
  platforms: [windows, unix]
  for: [scripts, binaries]
  crit: notable
  conf: 0.7
traits:
  - id: tr-delete
    desc: tr command for character deletion
    if:
      type: text
      regex: \btr\s+-d\s+['"].*?['"]\b
