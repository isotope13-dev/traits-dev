defaults:
  platforms: [linux, macos, unix]
  for: [shell]
  crit: notable
  conf: 0.8
traits:
  - id: gunzip-tool
    desc: gunzip tool reference
    if:
      type: string_literal
      word: gunzip
  - id: bunzip2-tool
    desc: bunzip2 tool reference
    if:
      type: string_literal
      word: bunzip2
