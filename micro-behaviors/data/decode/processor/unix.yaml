defaults:
  platforms: [unix]
  for: [shell]
  crit: notable
  conf: 0.8
traits:
  - id: gunzip-tool
    desc: gunzip tool reference
    if:
      type: symbol
      exact: gunzip

  - id: bunzip2-tool
    desc: bunzip2 tool reference
    if:
      type: symbol
      exact: bunzip2
