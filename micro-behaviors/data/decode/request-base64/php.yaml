defaults:
  platforms: [unix, windows]
  for: [php]
  mbc: C0053

traits:
  - id: php-base64-request-param
    desc: Base64-decodes a PHP request parameter
    crit: notable
    conf: 0.9
    if:
      type: text
      regex: '(?i)base64_decode\s*\(\s*\$_(GET|POST|REQUEST)\s*\[[^]]{1,60}\]'
