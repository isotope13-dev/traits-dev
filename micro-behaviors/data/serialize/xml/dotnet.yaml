defaults:
  platforms: [unix, windows]
  for: [pe, csharp]

traits:
  - id: xml-serializer
    desc: .NET XML serialization API reference
    crit: notable
    conf: 0.75
    if:
      type: text
      regex: XmlSerializer|System\.Xml\.Serialization
    unless:
      - id: metadata/lang/compiler/generated/
      - id: metadata/library/generated::winmd-metadata
