defaults:
  for: [go]
  platforms: [linux, macos, windows]
traits:
  - id: map-string-interface-envelope
    desc: Parses generic map[string]interface{} envelope
    crit: suspicious
    conf: 0.8
    if:
      type: text
      regex: 'envelope\s+map\[string\]interface\{\}'
