defaults:
  platforms: [windows, unix]
  for: [php]
  crit: notable
  conf: 0.95

traits:
  - id: wordpress-json-encode
    desc: WordPress JSON serialization
    if:
      type: symbol
      kind: call
      exact: wp_json_encode
