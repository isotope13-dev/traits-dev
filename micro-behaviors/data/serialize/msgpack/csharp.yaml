defaults:
  platforms: [windows]
  for: [csharp, pe, dll]

traits:
  - id: dotnet-messagepacklib-namespace
    desc: .NET MessagePackLib serialization namespace
    crit: notable
    conf: 0.94
    if:
      type: text
      exact: MessagePackLib.MessagePack
