defaults:
  platforms: [windows, unix]
  for: [php]
  crit: notable
  conf: 0.9

traits:
  - id: php-json-decode
    desc: PHP JSON deserialization
    if:
      type: symbol
      kind: call
      exact: json_decode
