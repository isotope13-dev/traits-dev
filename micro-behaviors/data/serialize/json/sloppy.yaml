defaults:
  platforms: [linux, macos, windows, unix, android, ios]
  for: [python]
traits:
  - id: json-quote-replace
    desc: Replaces quotes before JSON parsing
    crit: notable
    if:
      type: text
      regex: \.replace\(['"]'['"],\s*['"]"['"]\)
