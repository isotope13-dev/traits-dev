defaults:
  platforms: [windows, unix]
  for: [python]
traits:
  - id: json-quote-replace
    desc: Replaces quotes before JSON parsing
    crit: notable
    if:
      type: text
      regex: \.replace\(['"]'['"],\s*['"]"['"]\)
