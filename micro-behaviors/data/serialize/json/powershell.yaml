defaults:
  platforms: [windows]
  for: [powershell]

traits:
  - id: convertto-json
    desc: PowerShell serializes objects as JSON
    crit: notable
    conf: 0.9
    if:
      type: text
      word: ConvertTo-Json
      case_insensitive: true
