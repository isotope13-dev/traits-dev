defaults:
  platforms: [unix, windows]
  for: [scripts, source, json]

traits:
  - id: ndjson-filename-reference
    desc: References an NDJSON filename
    crit: component
    conf: 0.85
    if:
      type: text
      substr: '.ndjson'
