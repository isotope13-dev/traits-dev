defaults:
  platforms: [unix, windows]
  for: [python]
traits:
  - id: json-dumps
    desc: Python json.dumps() serialization
    crit: baseline
    conf: 0.8
    if:
      type: text
      word: json.dumps
