defaults:
  platforms: [macos, ios]
  for: [macho, swift, objc]
  crit: notable

traits:
  - id: nspropertylistserialization-text
    desc: ObjC property-list serialization API reference
    conf: 0.9
    if:
      type: text
      substr: NSPropertyListSerialization
