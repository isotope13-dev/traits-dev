defaults:
  platforms: [windows, unix]
  for: [javascript, typescript]

traits:
  - id: transfer-list-identifier-option
    desc: Transfer-list option references an identifier
    crit: component
    conf: 0.96
    if:
      type: text
      regex: '\btransferList\s*:\s*[A-Za-z_$][A-Za-z0-9_$]*'
