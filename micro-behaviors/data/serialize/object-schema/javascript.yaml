defaults:
  platforms: [windows, unix]
traits:
  - id: json-stringify-env-field
    for: [javascript, typescript]
    desc: JSON serialization includes an env field
    crit: notable
    conf: 0.88
    if:
      type: text
      regex: 'JSON\.stringify\s*\(\s*\{[^}]{0,500}\benv\b'
  - id: username-password-json-body
    for: [javascript, typescript]
    desc: Serializes username and password fields
    crit: notable
    conf: 0.94
    if:
      type: text
      regex: 'JSON\.stringify\s*\(\s*\{\s*username\s*,\s*password\s*\}'
  - id: email-password-json-body
    for: [javascript, typescript]
    desc: Serializes email and password fields
    crit: notable
    conf: 0.95
    if:
      type: text
      regex: 'JSON\.stringify\s*\(\s*\{[^}]{0,320}\bemail\s*:[^}]{0,180}\bpassword\s*:'
  - id: payload-user-field
    for: [javascript, typescript]
    desc: Payload contains a user field
    crit: notable
    conf: 0.9
    if:
      type: text
      regex: '\bpayload\s*=\s*\{[\s\S]{0,700}\buser\s*:'
  - id: payload-quiz-content-field
    for: [javascript, typescript]
    desc: Payload contains quiz content field
    crit: notable
    conf: 0.9
    if:
      type: text
      regex: '\bpayload\s*=\s*\{[\s\S]{0,700}\b(quiz|question|pageContent)\s*:'
  - id: payload-timestamp-field
    for: [javascript, typescript]
    desc: Payload contains a timestamp field
    crit: notable
    conf: 0.9
    if:
      type: text
      regex: '\bpayload\s*=\s*\{[\s\S]{0,700}\btimestamp\s*:'
  - id: json-object-action-field
    for: [javascript, typescript]
    desc: JSON payload includes an action field
    crit: notable
    conf: 0.82
    if:
      type: text
      regex: 'JSON\.stringify\(\s*\{[^}]{0,180}\baction\s*[:,}]'
  - id: json-object-variable-field
    for: [javascript, typescript]
    desc: JSON object includes a variable-valued field
    crit: notable
    conf: 0.84
    if:
      type: text
      regex: 'JSON[.]stringify.{0,160}:[[:space:]]*[A-Za-z_$][A-Za-z0-9_$]*[[:space:]]*[}]'
  - id: identity-platform-json-payload
    for: [javascript, typescript]
    desc: JSON payload has an ID and platform
    crit: notable
    conf: 0.9
    if:
      type: text
      regex: '(?is)JSON\.stringify\s*\(\s*\{.{0,120}(id|installId|clientId):.{0,100}platform\s*:'
  - id: refresh-token-field-stringify
    for: [javascript, typescript]
    desc: JSON serialization includes refresh-token field
    crit: notable
    conf: 0.96
    if:
      type: text
      regex: '(?i)JSON\.stringify\([\s\S]{0,120}refreshToken'
  - id: json-stringify-title-content-fields
    for: [javascript, typescript]
    desc: Serializes title and content fields as JSON
    crit: notable
    conf: 0.88
    if:
      type: text
      regex: 'JSON\.stringify\(\s*\{[^}]{0,240}(title|subject)\s*:[^}]{0,160}(content|message)\s*:'
  - id: json-object-body-field
    for: [html, javascript, typescript, php]
    desc: Serializes an object into a body field
    crit: notable
    conf: 0.9
    if:
      type: text
      regex: '\bbody\s*:\s*JSON\.stringify\(\s*\{'
  - id: json-stringified-text-field
    for: [html, javascript, typescript, php]
    desc: Serializes a text or caption field
    crit: notable
    conf: 0.9
    if:
      type: text
      regex: '\b(text|caption)\s*:\s*JSON\.stringify\('
composite_rules:
  - id: json-user-content-timestamp-payload
    for: [javascript, typescript]
    desc: Serializes user content with timestamp payload
    crit: notable
    conf: 0.94
    near_bytes: 1800
    all:
      - id: payload-user-field
      - id: payload-quiz-content-field
      - id: payload-timestamp-field
      - id: micro-behaviors/data/serialize/json::json-stringify-payload-call
  - id: nested-json-text-body
    for: [html, javascript, typescript, php]
    desc: Nests serialized text inside a JSON body
    crit: notable
    conf: 0.95
    near_bytes: 400
    all:
      - id: json-object-body-field
      - id: json-stringified-text-field
