defaults:
  platforms: [linux, macos, windows, unix, android, ios]
  for: [python]
  crit: notable
traits:
  - id: protobuf-or-sign-doc
    desc: Protobuf or SignDoc serialization
    conf: 0.85
    if:
      type: text
      regex: protobuf|SerializeToString|sign_doc|SignDoc
