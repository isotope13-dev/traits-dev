defaults:
  platforms: [unix, windows]
  for: [c, cpp, elf, shell, source]
  crit: component
  conf: 0.5

traits:
  - id: network-address-probing-status
    desc: Network address-probing status token
    if:
      type: literal
      kind: string
      exact: PROBING
    count_min: 2
