defaults:
  platforms: [windows, unix]
  for: [python]

traits:
  - id: python-shlex-command-split
    desc: Python parses shell-like command words
    crit: notable
    conf: 0.9
    if:
      type: symbol
      kind: call
      exact: shlex.split
