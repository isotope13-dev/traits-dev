defaults:
  platforms: [unix, windows]
  for: [go]

traits:
  - id: persistence-keyword-literal
    desc: Persistence-related string literal
    crit: component
    conf: 0.92
    if:
      type: literal
      regex: '(?i)\bpersist(en|ence)\b'
