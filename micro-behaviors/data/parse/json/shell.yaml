defaults:
  platforms: [unix]
  for: [shell]

traits:
  - id: jq-json-command-field-extract
    desc: jq extracts a command field from JSON
    crit: component
    conf: 0.88
    if:
      type: text
      regex: '\bjq\b[^\n]{0,100}\.command\b'
