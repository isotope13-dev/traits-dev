defaults:
  platforms: [unix, windows]
  for: [c, cpp]
  crit: notable
  conf: 0.9

traits:
  - id: list-add-call
    desc: Adds an entry to a linked list
    if:
      type: symbol
      kind: call
      exact: list_add
