defaults:
  platforms: [unix, windows]
  for: [python, javascript, typescript, powershell, shell]

traits:
  - id: string-replace-call
    desc: Calls a string replacement method
    crit: baseline
    conf: 0.8
    if:
      type: text
      regex: '\.replace\s*\('
