defaults:
  platforms: [windows]
  for: [batch]
  crit: baseline
  conf: 0.95

traits:
  - id: batch-for-f-file-iteration
    desc: Batch iterates file lines with FOR /F
    if:
      type: text
      regex: '(?i)\bfor\s+/f\b[^\r\n]{0,120}\bin\s*\(\s*"?[^''"\r\n]{1,120}"?\s*\)\s+do\b'
