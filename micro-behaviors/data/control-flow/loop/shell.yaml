defaults:
  platforms: [unix]
  for: [shell]
  crit: baseline

traits:
  - id: shell-for-loop-over-var
    desc: shell for loop over variable
    crit: component
    conf: 0.86
    if:
      type: text
      regex: '\bfor\s+[A-Za-z_]\w*\s+in\s+\$[A-Za-z_]\w*'
