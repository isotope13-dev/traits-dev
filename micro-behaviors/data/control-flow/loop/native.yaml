defaults:
  platforms: [windows, linux, macos]
  for: [c, cpp, go]
  crit: notable
  conf: 0.85

traits:
  - id: native-range-loop-large-bound
    desc: Uses a large-bound native loop
    if:
      type: text
      regex: '\bfor\s*\([^;\n]{0,40};[^;\n]{0,40}<\s*([5-9]|[1-9][0-9])\s*;'
