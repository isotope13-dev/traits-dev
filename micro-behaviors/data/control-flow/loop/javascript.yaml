# Generic JavaScript loop header (while/for)
defaults:
  platforms: [unix, windows]
  for: [javascript]
traits:
  - id: js-loop
    desc: Loop structure
    crit: baseline
    if:
      type: string_literal
      regex: \b(while|for)\s*\(
