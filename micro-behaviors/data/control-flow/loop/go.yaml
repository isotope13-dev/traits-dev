defaults:
  platforms: [unix, windows]
  for: [go]
  crit: notable
  conf: 0.9

traits:
  - id: go-unbounded-for-loop
    desc: Go source contains an unbounded loop
    if:
      type: text
      regex: '\bfor\s*(\{|[^{}\n]{0,40};\s*;)'
