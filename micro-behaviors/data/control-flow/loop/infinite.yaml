defaults:
  for: [all]
traits:
  - id: infinite-loop-header
    desc: Infinite loop structure (e.g. for(;;) or while(1))
    crit: baseline
    if:
      type: raw
      regex: 'for\s*\(\s*;\s*;\s*\)|while\s*\(\s*(1|true)\s*\)'
