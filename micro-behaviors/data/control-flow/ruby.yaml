# Control flow patterns - Ruby
defaults:
  platforms: [linux, macos, windows, unix, android, ios]
  for: [ruby]
  crit: notable
traits:
  - id: depth-limit-param
    desc: Depth-limited recursion parameter
    conf: 0.70
    if:
      type: text
      regex: 'max_depth|depth\s*[=-]'
