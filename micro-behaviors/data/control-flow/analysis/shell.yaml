defaults:
  platforms: [unix]
  for: [shell]

traits:
  - id: shell-arithmetic-half-value
    desc: Shell arithmetic computes half of a variable
    crit: baseline
    conf: 0.95
    if:
      type: text
      regex: '\(\(\s*\$?[A-Za-z_]\w*\s*/\s*2\s*\)\)'
