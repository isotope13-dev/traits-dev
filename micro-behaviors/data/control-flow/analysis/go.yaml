defaults:
  platforms: [unix, windows]
  for: [go]
  crit: notable
  conf: 0.84

traits:
  - id: go-range-over-credential-map
    desc: Go ranges over named credential map
    crit: notable
    conf: 0.84
    if:
      type: text
      regex: 'for\s+[^:=]{1,30}:=\s*range\s+credentials'
