defaults:
  for: [binaries, scripts, source, documents, manifests, data, media]
  platforms: [linux, macos, windows, unix, android, ios]
  size_max: 16384
  attack: "T1027"
  mbc: "C0027"
  crit: notable
  conf: 0.88

traits:
  - id: on-error-goto-runtonext
    desc: Silent On Error branch skip
    crit: suspicious
    if:
      type: raw
      exact: "On Error GoTo runtonext"
      section: .text
