defaults:
  platforms: [windows, unix]
  for: [php]

traits:
  - id: php-assert-call
    desc: PHP assertion function call
    crit: notable
    conf: 0.85
    if:
      type: symbol
      kind: call
      exact: assert
