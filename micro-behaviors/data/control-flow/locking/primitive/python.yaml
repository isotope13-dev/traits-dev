defaults:
  platforms: [windows, unix]
  for: [python]
  crit: baseline
traits:
  - id: python-thread-lock
    desc: Uses a Python threading lock
    conf: 0.7
    if:
      type: text
      regex: '\.Lock\(\)'
