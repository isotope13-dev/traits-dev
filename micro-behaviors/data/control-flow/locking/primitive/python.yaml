defaults:
  platforms: [windows, unix]
  for: [python]
  crit: baseline
traits:
  - id: python-thread-lock
    desc: Python threading lock usage
    conf: 0.7
    if:
      type: text
      regex: '\.Lock\(\)'
