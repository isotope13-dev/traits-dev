defaults:
  platforms: [windows, unix]
  for: [python]
  crit: baseline
traits:
  - id: python-with-lock
    desc: Python lock guarded block
    conf: 0.75
    if:
      type: text
      regex: 'with\s+[A-Za-z_][A-Za-z0-9_]*:\s*\n\s+with\s+open\('
