defaults:
  platforms: [windows]
  for: [csharp]

traits:
  - id: completed-job-status-reference
    desc: References a completed job status enum
    crit: component
    conf: 0.84
    if:
      type: text
      substr: "JobStatus.Completed"
