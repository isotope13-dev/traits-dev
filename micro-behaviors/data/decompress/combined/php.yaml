---
defaults:
  platforms:
  - windows
  - unix
  for:
  - php
  crit: notable
  conf: 0.9
traits:
- id: php-compressed-base64-decode
  desc: PHP decompresses base64-decoded data
  crit: notable
  conf: 0.95
  if:
    type: text
    regex: "(gzinflate|gzuncompress|gzdecode)\\s*\\(\\s*@?\\s*base64_decode\\s*\\("
