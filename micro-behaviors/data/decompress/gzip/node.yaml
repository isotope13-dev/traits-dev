---
defaults:
  crit: notable
  platforms:
  - windows
  - unix
  for:
  - javascript
  - typescript
traits:
- id: node-gunzip-function
  desc: Node gunzip decompression call
  conf: 0.8
  if:
    type: text
    regex: "\\bzlib\\.gunzip(Sync)?\\s*\\("
composite_rules:
- id: node-gzip-functions
  desc: Node gzip or gunzip calls
  conf: 0.8
  any:
  - id: micro-behaviors/data/compress/gzip::node-gzip-function
  - id: node-gunzip-function
