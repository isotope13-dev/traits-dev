---
defaults:
  crit: notable
  platforms:
  - windows
  - unix
  for:
  - go
traits:
- id: go-gzip-reader
  desc: Go gzip.NewReader decompression
  conf: 0.85
  if:
    type: symbol
    exact: gzip.NewReader
