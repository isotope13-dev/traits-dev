defaults:
  platforms: [windows]
  for: [pe, dll]
  crit: component
  conf: 0.95
traits:
  - id: native-ai-agent-identity
    desc: Native AI coding-agent reference
    if:
      type: text
      regex: '(?i)\b(cursor-agent|claude(code)?|kiro)\b'
    unless:
      - id: metadata/binary/section/name::bun-section-name
