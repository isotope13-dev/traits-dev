defaults:
  platforms: [windows, unix]
  for: [scripts]
  crit: notable
  conf: 0.95

traits:
  - id: answer-or-ground-truth-path
    desc: References an answer or ground-truth path
    if:
      type: literal
      regex: '(?i)\b(answers?|solutions?|ground[-_]?truth)\b[/\\.]'
