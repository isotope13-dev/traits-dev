defaults:
  platforms: [unix]
  for: [shell]
  crit: notable
  conf: 0.94

traits:
  - id: shell-json-control-character-escape
    desc: Shell escapes JSON control characters
    count_min: 2
    if:
      type: text
      regex: "\\$'\\\\[0-7]{3}'/\\\\\\\\u00[0-9a-fA-F]{2}"
