defaults:
  platforms: [windows, unix]
  for: [python]

traits:
  - id: python-image-getpixel-reference
    desc: References Python image pixel getter
    crit: baseline
    conf: 0.8
    if:
      type: symbol
      substr: .getpixel
