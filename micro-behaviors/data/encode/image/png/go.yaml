defaults:
  platforms: [windows, unix]
  for: [go]

traits:
  - id: png-encode-call
    desc: Go PNG image encoding
    crit: notable
    conf: 0.75
    if:
      type: symbol
      kind: call
      exact: png.Encode
