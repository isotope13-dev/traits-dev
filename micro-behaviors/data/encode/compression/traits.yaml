defaults:
  for: [elf, macho, pe]
  platforms:
    - linux
    - macos
    - windows
traits:
  - id: brotli-signature
    desc: Brotli compression library
    crit: baseline
    conf: 0.8
    for: [binaries, scripts]
    if:
      type: text
      regex: (?i)(^|[^a-z])brotli([^a-z]|$)
