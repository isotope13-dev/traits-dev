defaults:
  platforms: [windows, unix]
  # Stub placeholders for removed traits — match a magic token that never
  # appears in real files, so file-type scope is purely nominal.
  for: [text]

traits:
  - id: callbyname-call-raw
    desc: Compatibility placeholder
    crit: component
    conf: 1.0
    if:
      type: text
      word: __cleave_missing_callbyname_call_raw__
  - id: xor-array-payload-reconstruction
    desc: Compatibility placeholder
    crit: component
    conf: 1.0
    if:
      type: text
      word: __cleave_missing_xor_array_payload_reconstruction__
