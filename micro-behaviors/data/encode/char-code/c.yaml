defaults:
  platforms: [unix, windows]
  for: [c, cpp]
  crit: notable
  conf: 0.9

traits:
  - id: c-octal-escape-sequence
    desc: C source contains repeated octal escapes
    if:
      type: text
      regex: '(\\[0-7]{3}){20,}'
