defaults:
  platforms:
    - unix
    - windows
    - macos
  for: [source, scripts]
traits:
  - id: uuencode-header
    desc: UUEncode begin header marker
    crit: notable
    conf: 0.9
    if:
      type: text
      regex: 'begin 6[0-9][0-9] '
