defaults:
  platforms: [windows]
  for: [pe, dll]

traits:
  - id: loader-error-caption
    desc: RL unpacker loader-error caption
    crit: notable
    conf: 0.97
    if:
      type: text
      exact: LOADER ERROR
      section: .vr
