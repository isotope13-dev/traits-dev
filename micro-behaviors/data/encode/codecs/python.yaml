defaults:
  platforms: [linux, macos, windows, unix, android, ios]
  for: [python]
  crit: notable
traits:
  - id: codecs-decode-call
    desc: "Calls codecs.decode for data transformation"
    conf: 0.8
    if:
      type: symbol
      regex: 'codecs\.decode'
