defaults:
  platforms: [unix, windows]
  for: [svg]

traits:
  - id: svg-base64-string
    desc: Base64-like string literal in SVG markup
    crit: notable
    conf: 0.8
    if:
      type: text
      regex: '[''"]\$[A-Za-z0-9+/]{32,80}={1,2}[''"]'
