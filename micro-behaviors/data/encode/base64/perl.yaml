defaults:
  for: [perl]
  platforms: [unix, windows]

traits:
  - id: perl-return-encoded-hash-value
    desc: Returns base64-encoded selected hash value
    crit: notable
    conf: 0.99
    if:
      type: text
      regex: 'return\s+encode_base64\(\$\w+\s*->\s*\{\$\w+\}\)'
