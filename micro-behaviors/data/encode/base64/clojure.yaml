defaults:
  platforms: [unix, windows]
  for: [clojure]
  crit: notable
  conf: 0.95

traits:
  - id: clojure-jvm-base64-encoder-accessor
    desc: Clojure obtains JVM Base64 encoder
    if:
      type: symbol
      kind: call
      regex: '^Base64/get(Url)?Encoder$'
