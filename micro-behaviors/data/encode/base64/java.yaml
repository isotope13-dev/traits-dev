defaults:
  platforms: [unix, windows]
  for: [java, class, kotlin]
traits:
  - id: java-base64-encode
    desc: Java Base64 encoding
    crit: baseline
    conf: 0.8
    if:
      type: text
      word: java.util.Base64.getEncoder
