defaults:
  platforms: [unix, windows]
  for: [shell]
  crit: notable
  conf: 0.75

traits:
  - id: base64-encode-command
    desc: Shell Base64 encoding command
    mbc: C0025
    if:
      type: text
      regex: '\|\s*base64\b'
