defaults:
  platforms: [unix, windows]
  for: [scripts, source]
traits:
  - id: base64-json-encoding-proximity
    desc: Base64 encoding near JSON serialization
    crit: notable
    conf: 0.9
    if:
      type: text
      regex: '(b64encode|ToBase64String).{0,400}(json\.dumps|ConvertTo-Json)'
