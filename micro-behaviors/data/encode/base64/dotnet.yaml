defaults:
  platforms: [windows]
  for: [pe, dll, csharp]
  crit: component
  conf: 0.78
traits:
  - id: dotnet-to-base64string
    desc: .NET Base64 encoding method reference
    crit: notable
    if:
      type: symbol
      kind: call
      regex: '(^|\.)ToBase64String$'
