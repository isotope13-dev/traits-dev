defaults:
  platforms: [windows]
  for: [pe, dll, csharp]
  crit: component
  conf: 0.78
traits:
  - id: dotnet-to-base64string
    desc: .NET base64 conversion API
    if:
      type: text
      word: ToBase64String
