defaults:
  platforms: [windows]
  for: [pe, dll, csharp]
  crit: component
  conf: 0.78
traits:
  - id: dotnet-to-base64string
    desc: .NET ToBase64String method reference
    if:
      type: text
      word: ToBase64String
