defaults:
  platforms:
    - unix
    - windows
  for: [scripts, binaries]
traits:
  - id: hex-escaped-control-char
    desc: 'String containing escaped hex control character'
    crit: baseline
    conf: 0.8
    if:
      type: text
      regex: \\x0[67]
