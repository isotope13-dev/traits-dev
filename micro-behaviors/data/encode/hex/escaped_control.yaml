defaults:
  platforms: [unix, windows, macos]
  for: [scripts, binaries]
traits:
  - id: hex-escaped-control-char
    desc: String containing escaped hex control character (06-07)
    crit: baseline
    conf: 0.8
    if:
      type: raw
      regex: '\\x0[67]'
