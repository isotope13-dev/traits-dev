defaults:
  platforms: [windows, linux, macos]
  for: [data]
  crit: notable
  conf: 0.95
  mbc: C0026

traits:
  - id: standalone-hex-data-file
    desc: Standalone hexadecimal data file
    if:
      type: text
      regex: '^0x[0-9A-Fa-f]{1024,}$'
