defaults:
  platforms: [windows, unix, appliance]
  for: [lua]

traits:
  - id: lua-long-hex-string-table
    desc: Lua long hex string literals
    crit: notable
    conf: 0.94
    count_min: 6
    if:
      type: literal
      regex: '^[0-9a-f]{8,}$'
