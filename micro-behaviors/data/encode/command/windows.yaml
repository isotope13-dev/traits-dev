defaults:
  platforms: [windows]
  for: [batch, powershell]
  crit: notable
  conf: 0.94
  attack: T1132.001

traits:
  - id: certutil-base64-encode-command
    desc: certutil Base64 file-encoding command
    if:
      type: text
      regex: '(?i)\bcertutil(\.exe)?\s+-encode\b'
