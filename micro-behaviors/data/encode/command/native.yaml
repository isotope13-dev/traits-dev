defaults:
  platforms: [unix]
  for: [elf, macho]
  crit: notable
  conf: 0.9

traits:
  - id: native-encode-cli-flag
    desc: Native --encode command-line option
    if:
      type: text
      regex: '(?i)(^|[\s"''=:,])--?encode([\s"''=:,]|$)'
      
