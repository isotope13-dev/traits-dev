defaults:
  platforms: [windows, unix]
  for: [javascript, typescript]
traits:
  - id: js-permutation-loop
    desc: Nested for loops (control-flow building block)
    crit: baseline
    conf: 0.9
    if:
      type: text
      regex: \bfor\s*\([^{]{0,120}\{[^{]{0,120}\bfor\s*\([^{]{0,120}\{
