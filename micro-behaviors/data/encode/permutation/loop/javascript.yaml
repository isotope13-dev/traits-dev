defaults:
  platforms: [windows, unix]
  for: [javascript, typescript]
traits:
  - id: js-permutation-loop
    desc: Nested loops with multiple charAt calls
    crit: baseline
    conf: 0.9
    if:
      type: text
      regex: \bfor\s*\([^{]{0,120}\{[^{]{0,120}\bfor\s*\([^{]{0,120}\{
