defaults:
  platforms:
    - unix
    - windows
  for: [scripts, binaries]
traits:
  - id: gzip-header-identifier
    desc: GZIP header magic bytes at file start
    crit: notable
    conf: 1.0
    if:
      type: hex
      pattern: 1F 8B
      offset: 0
