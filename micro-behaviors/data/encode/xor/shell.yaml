defaults:
  platforms: [unix, windows]
  for: [shell]
  crit: notable
  conf: 0.82

traits:
  - id: xor-hex-encoding-loop
    desc: Shell XOR-to-hex encoding loop
    mbc: C0026
    if:
      type: text
      regex: '(?s)for\s*\(\(.*?\^.*?printf\s+-v\s+hex[_-]?val.*?%02x'
