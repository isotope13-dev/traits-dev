defaults:
  platforms: [unix, windows]
  for: [javascript, typescript]
traits:
  - id: tostring-call
    desc: .toString() method call
    crit: component
    if:
      type: text
      substr: .toString()