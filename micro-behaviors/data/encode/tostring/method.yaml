defaults:
  platforms: [unix, windows]
  for: [class, java, javascript, shell, typescript]
  crit: component
traits:
  - id: tostring-token
    desc: References the toString method
    if:
      type: text
      word: toString
