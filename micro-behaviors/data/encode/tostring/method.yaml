defaults:
  platforms: [unix, windows]
  for: [class, java, javascript, shell, typescript]
  crit: component
traits:
  - id: tostring-token
    desc: toString method token
    if:
      type: text
      word: toString
