defaults:
  platforms: [windows]
  for: [pe, dll]

traits:
  - id: char-to-oem-a-import
    desc: CharToOemA code-page conversion import
    crit: notable
    conf: 0.85
    if:
      type: import
      exact: CharToOemA
