defaults:
  platforms: [unix, windows]
  for: [python]

traits:
  - id: python-latin1-encode-call
    desc: Python encodes a string as latin-1 bytes
    crit: notable
    conf: 0.9
    if:
      type: text
      regex: '\.encode\s*\(\s*["\x27]latin[-_]?1["\x27]\s*\)'
