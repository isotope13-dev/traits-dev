defaults:
  platforms:
    - unix
    - windows
  for: [scripts, binaries]
  crit: baseline
  conf: 0.8
traits:
  - id: windows-1251-charset
    desc: windows-1251 charset reference
    unless:
      - id: metadata/library/runtime/
    if:
      type: text
      exact: windows-1251
