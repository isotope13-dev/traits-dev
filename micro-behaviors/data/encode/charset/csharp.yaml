defaults:
  for: [csharp]
  platforms: [windows, unix]

traits:
  - id: dotnet-utf8-getbytes
    desc: C# encodes text as UTF-8 bytes
    crit: notable
    conf: 0.9
    if:
      type: symbol
      kind: call
      regex: '(^|\.)Encoding\.UTF8\.GetBytes$'
