# URL encoding - PHP
defaults:
  for: [php]
  platforms: [unix, windows]

traits:
  - id: urlencode-call
    desc: URL-encodes data via PHP urlencode
    crit: notable
    conf: 0.85
    if:
      type: symbol
      kind: call
      exact: urlencode
