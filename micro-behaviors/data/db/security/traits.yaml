defaults:
  platforms: [unix, windows, macos]
  for: [shell, python, javascript, powershell, pe, elf]
traits:
  - id: commented-out-unique
    desc: Commented out unique constraint (often disables security)
    crit: notable
    if:
      type: raw
      regex: '//\s*unique\s*:\s*true'
