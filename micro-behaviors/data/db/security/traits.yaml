defaults:
  platforms:
    - unix
    - windows
  for: [scripts, binaries]
traits:
  - id: commented-out-unique
    desc: 'Commented out unique constraint'
    crit: notable
    if:
      type: text
      regex: //\s*unique\s*:\s*true
