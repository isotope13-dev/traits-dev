defaults:
  platforms: [android]
  for: [java, kotlin]

traits:
  - id: jvm-query-method-call
    desc: Calls a JVM object query method
    crit: component
    conf: 0.9
    if:
      type: symbol
      kind: call
      regex: '(^|\.)query$'
