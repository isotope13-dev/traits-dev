defaults:
  platforms: [unix, windows]
  for: [javascript, typescript]

traits:
  - id: github-db-backup-helper
    desc: GitHub database backup helper
    crit: notable
    conf: 0.9
    if:
      type: text
      substr: GithubDB

  - id: gitlab-db-backup-helper
    desc: GitLab database backup helper
    crit: notable
    conf: 0.9
    if:
      type: text
      substr: GitlabDB

  - id: supabase-db-backup-helper
    desc: Supabase database backup helper
    crit: notable
    conf: 0.9
    if:
      type: text
      substr: SupabaseDB

  - id: neon-db-backup-helper
    desc: Neon database backup helper
    crit: notable
    conf: 0.9
    if:
      type: text
      substr: NeonDB
