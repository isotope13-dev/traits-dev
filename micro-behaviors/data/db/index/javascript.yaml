defaults:
  platforms: [unix, windows]
  for: [javascript, typescript]
  crit: notable
  conf: 0.9

traits:
  - id: database-index-create-call
    desc: Creates a database index
    if:
      type: symbol
      kind: call
      regex: '(^|\.)createIndex$'
