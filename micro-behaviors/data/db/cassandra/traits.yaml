defaults:
  platforms:
    - unix
    - windows
  for: [scripts, binaries]
traits:
  - id: cassandra-string
    desc: "Apache Cassandra database reference"
    crit: notable
    conf: 0.9
    if:
      type: text
      regex: (?i)cassandra
    unless:
      - id: metadata/file/text/catalog::frequency-list-catalog
  - id: gocql
    desc: gocql Go driver
    crit: notable
    conf: 0.9
    if:
      type: text
      substr: gocql
  - id: datastax
    desc: datastax driver
    crit: notable
    conf: 0.9
    if:
      type: text
      substr: datastax
  - id: cql-url
    desc: cql:// URL scheme
    crit: notable
    conf: 0.9
    if:
      type: text
      substr: cql://
