defaults:
  platforms:
    - unix
    - windows
  for: [scripts, binaries]
traits:
  - id: dynamodb-string
    desc: dynamodb string
    crit: baseline
    conf: 0.9
    if:
      type: text
      regex: (?i)dynamodb
