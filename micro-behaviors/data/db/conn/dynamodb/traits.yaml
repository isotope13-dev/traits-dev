defaults:
  platforms:
    - unix
    - windows
    - macos
  for: [scripts, binaries]
traits:
  - id: dynamodb-string
    desc: dynamodb string
    crit: notable
    conf: 0.9
    if:
      type: text
      regex: (?i)dynamodb
