defaults:
  platforms: [linux, macos, windows]
  for: [elixir]
  crit: notable
  conf: 0.95

traits:
  - id: elixir-list-to-binary
    desc: Converts byte list into binary
    if:
      type: symbol
      kind: call
      exact: :erlang.list_to_binary
