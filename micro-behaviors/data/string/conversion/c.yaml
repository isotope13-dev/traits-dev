defaults:
  crit: baseline
  conf: 0.75
  for: [elf, macho, pe, dll]
  platforms: [unix, windows]
traits:
  - id: strtol
    desc: String to long integer conversion (strtol)
    if:
      type: symbol
      exact: strtol
  - id: atoi
    desc: String to integer conversion (atoi)
    if:
      type: symbol
      exact: atoi
