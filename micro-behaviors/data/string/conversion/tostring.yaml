defaults:
  platforms: [unix, windows]
  for: [class, java, javascript, shell, typescript]
  crit: component
traits:
  - id: tostring-token
    crit: notable
    desc: References the toString conversion method
    if:
      type: text
      word: toString
