defaults:
  platforms: [windows]
  for: [vbs]

traits:
  - id: vb6-unicode-string-to-bytes
    desc: VB6 Unicode string to bytes
    crit: notable
    conf: 0.92
    if:
      type: text
      regex: 'StrConv\([^)]{1,48},\s*vbFromUnicode\)'
