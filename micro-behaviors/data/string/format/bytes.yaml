defaults:
  platforms: [windows, unix]
  for: [scripts, source]

traits:
  - id: source-bytes-equals-literal
    desc: Source contains a bytes= string literal
    crit: component
    conf: 0.94
    if:
      type: literal
      regex: '(?i)^bytes\s*='
