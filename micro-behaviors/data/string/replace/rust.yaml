defaults:
  platforms: [windows, unix]
  for: [rust]

traits:
  - id: rust-string-replacement-call
    desc: Rust replaces matched string content
    crit: notable
    conf: 0.9
    if:
      type: symbol
      kind: call
      regex: '(^|\.)(replace|replace_all)$'
