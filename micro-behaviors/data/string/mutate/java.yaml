defaults:
  platforms: [unix, windows]
  for: [java, class]
  crit: baseline
  conf: 0.82

traits:
  - id: java-append-calls
    desc: Repeated Java append method calls
    count_min: 4
    if:
      type: symbol
      kind: call
      exact: append
