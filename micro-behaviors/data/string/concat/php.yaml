defaults:
  platforms: [windows, unix]
  for: [php]
  crit: notable
  conf: 0.9

traits:
  - id: php-literal-string-concatenation
    desc: PHP concatenates adjacent literal strings
    if:
      type: text
      regex: "(?m)^\\s*['\"][^'\"<>]{4,24}['\"]\\s*\\.\\s*['\"][^'\"<>]{4,24}['\"]\\s*;"
