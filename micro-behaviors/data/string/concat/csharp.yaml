defaults:
  platforms: [windows]
  for: [csharp]
  crit: component
  conf: 0.8

traits:
  - id: csharp-append-text-call
    desc: C# source contains an AppendText method call
    if:
      type: text
      substr: ".AppendText("
