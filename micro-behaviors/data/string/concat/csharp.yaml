defaults:
  platforms: [windows]
  for: [csharp]
  crit: component
  conf: 0.8

traits:
  - id: csharp-append-text-call
    crit: notable
    desc: C# source contains an AppendText method call
    if:
      type: text
      substr: ".AppendText("
