defaults:
  platforms: [unix]
  for: [shell]

traits:
  - id: shell-grep-env-ssh-names
    desc: Shell filters env and SSH names
    crit: notable
    conf: 0.82
    if:
      type: text
      regex: 'grep.{0,30}env.{0,5}ssh'
