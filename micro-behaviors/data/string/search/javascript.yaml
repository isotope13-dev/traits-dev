defaults:
  platforms: [unix, windows]
  for: [javascript, typescript]

traits:
  - id: js-includes-indexof-call
    desc: JavaScript substring search call
    crit: baseline
    conf: 0.82
    if:
      type: text
      regex: '\b(includes|indexOf)\s*\('
