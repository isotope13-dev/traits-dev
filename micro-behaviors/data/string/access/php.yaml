---
defaults:
  platforms: [windows, unix]
  for: [php]

traits:
  - id: php-substring-call
    desc: PHP substring extraction call
    crit: baseline
    conf: 0.8
    if:
      type: symbol
      kind: call
      exact: substr
