defaults:
  platforms: [windows, unix]
  for: [python]
  crit: baseline
  conf: 0.8

traits:
  - id: python-content-field-split
    desc: Python splits a content field
    if:
      type: symbol
      kind: call
      regex: '\.(text|content)\.(splitlines|split)$'
