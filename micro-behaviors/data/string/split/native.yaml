defaults:
  platforms: [unix, windows]
  for: [elf, macho, pe, dll]

traits:
  - id: strtok-import
    desc: Native strtok delimiter parser import
    crit: notable
    conf: 0.85
    if:
      type: import
      exact: strtok
