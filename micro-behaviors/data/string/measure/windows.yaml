defaults:
  platforms:
  - windows
  for:
  - pe
  - dll
traits:
- id: sys-string-len
  desc: SysStringLen OLE BSTR length import
  crit: notable
  conf: 0.85
  if:
    type: import
    exact: SysStringLen
