defaults:
  platforms: [linux, macos, windows]
  for: [javascript]
  crit: notable
  conf: 0.9

traits:
  - id: indexed-property-string-assembly
    desc: Indexed-value string assembly
    if:
      type: text
      regex: for\s*\([^)]{1,120}\)\s*\{[^{}]{0,120}\w+\s*\+=\s*this\[
