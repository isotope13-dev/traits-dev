traits:
- id: literal-string-comparison
  desc: Compares an input against a string literal
  crit: baseline
  conf: 0.85
  if:
    type: text
    regex: \bstr(n|case)?cmp\s*\(\s*[A-Za-z_][\w>.\[\]-]{0,40}\s*,\s*"[^"\n]{2,40}"
  for:
  - c
  - cpp
  - text
  platforms:
  - unix
