---
defaults:
  crit: notable
  platforms:
  - windows
  - unix
  for:
  - go
traits:
- id: go-zstd-package
  desc: Declares a Go Zstandard compression package
  conf: 0.98
  if:
    type: text
    regex: "(?m)^package\\s+zstd$"
