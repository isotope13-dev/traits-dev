---
defaults:
  crit: notable
  platforms:
  - windows
  - unix
  for:
  - javascript
  - typescript
traits:
- id: node-zlib-import
  desc: Imports the Node.js zlib module
  conf: 0.9
  if:
    type: import
    regex: "^(node:)?zlib$"
