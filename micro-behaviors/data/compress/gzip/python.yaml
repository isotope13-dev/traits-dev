---
defaults:
  crit: notable
  platforms:
  - windows
  - unix
  for:
  - python
traits:
- id: gzip-open-call
  desc: Python gzip.open call
  conf: 0.9
  if:
    type: symbol
    kind: call
    exact: gzip.open
- id: gzip-compress-call
  desc: Python gzip.compress call
  conf: 0.9
  if:
    type: symbol
    exact: gzip.compress
