---
defaults:
  crit: notable
  platforms:
  - windows
  - unix
  for:
  - go
traits:
- id: go-gzip-writer
  desc: Go gzip.NewWriter compression
  conf: 0.85
  if:
    type: symbol
    exact: gzip.NewWriter
