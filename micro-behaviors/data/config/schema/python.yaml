defaults:
  platforms: [unix, windows]
  for: [python]

traits:
  - id: connection-pool-field
    desc: Connection-pool configuration field
    crit: baseline
    conf: 0.85
    if:
      type: text
      substr: connection_pool
