defaults:
  platforms: [unix, windows]
  for: [javascript, typescript]

traits:
  - id: vscode-workspace-configuration-update
    desc: Updates VS Code workspace configuration
    crit: notable
    conf: 0.96
    if:
      type: symbol
      kind: call
      regex: '^vscode(_[0-9]+)?\.workspace\.getConfiguration\(\)\.update$'
