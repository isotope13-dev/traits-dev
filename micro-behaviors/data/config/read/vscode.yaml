defaults:
  platforms: [unix, windows]
  for: [javascript, typescript]

traits:
  - id: vscode-workspace-configuration-read
    desc: Reads VS Code workspace configuration
    crit: notable
    conf: 0.96
    if:
      type: symbol
      kind: call
      regex: '^vscode(_[0-9]+)?\.workspace\.getConfiguration$'
