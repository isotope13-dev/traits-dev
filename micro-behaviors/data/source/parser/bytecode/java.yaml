defaults:
  platforms:
    - unix
    - windows
    - macos
  for:
    - java
    - class
traits:
  - id: java-bytecode-magic
    desc: Checks for "CAFEBABE" magic bytes manually
    crit: suspicious
    if:
      type: string_value
      exact: CAFEBABE
