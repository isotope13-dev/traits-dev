defaults:
  platforms: [unix, windows, macos]
  for: [shell, python, javascript, powershell, pe, elf]
traits:
  - id: java-bytecode-magic
    desc: Checks for "CAFEBABE" magic bytes manually
    crit: suspicious
    if:
      type: string
      exact: "CAFEBABE"
