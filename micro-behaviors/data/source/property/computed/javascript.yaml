defaults:
  platforms: [windows, unix]
  for: [javascript, typescript]

traits:
  - id: js-computed-property-call
    desc: Calls a computed JavaScript property
    crit: notable
    conf: 0.82
    if:
      type: text
      regex: '\[[^\]\n]{1,120}\]\s*\('
