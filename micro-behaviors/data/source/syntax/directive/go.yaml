defaults:
  platforms: [windows, unix]
  for: [go]
  crit: notable
  conf: 0.85

traits:
  - id: go-linkname-directive
    desc: Go linkname compiler directive
    if:
      type: text
      substr: "//go:linkname"

  - id: go-noescape-directive
    desc: Go noescape compiler directive
    conf: 0.75
    if:
      type: text
      substr: "//go:noescape"

  - id: go-cgo-directive-comment
    desc: Go cgo compiler directive
    if:
      type: text
      regex: '(?m)^//\s*#cgo\b'
