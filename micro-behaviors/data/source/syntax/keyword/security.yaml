defaults:
  platforms: [unix, windows]
  for: [go, javascript, typescript, perl, php, ruby, python]
  crit: component
  conf: 0.8

traits:
  - id: webshell-term-reference
    desc: Web-shell term in source text
    if:
      type: text
      regex: w[3e]b[_\s]*sh[e3]ll
      case_insensitive: true
