# AppleScript language keywords. Bare source-syntax tokens; baseline only.
defaults:
  platforms: [macos]
  for: [macho, applescript]
  crit: baseline
  conf: 0.4
traits:
  - id: applescript-repeat-with
    desc: "AppleScript loop keyword: repeat with"
    if:
      type: text
      substr: 'repeat with '
