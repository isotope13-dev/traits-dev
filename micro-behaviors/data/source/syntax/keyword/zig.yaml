defaults:
  platforms: [aix, unix]
  for: [zig]
  crit: component
  conf: 0.85

traits:
  - id: zig-parser-exec-token-fragment
    crit: notable
    desc: Zig parser exec token fragment
    if:
      type: text
      regex: 'TokenType[\s\S]{0,260}\bexec\b'
