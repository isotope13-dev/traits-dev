defaults:
  platforms: [windows]
  for: [powershell]
  crit: component
  conf: 0.8

traits:
  - id: char-array-cast
    crit: notable
    desc: PowerShell char array cast
    if:
      type: text
      regex: '(?i)\[char\[\]\]'
    unless:
