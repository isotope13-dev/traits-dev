defaults:
  platforms:
    - unix
    - windows
    - macos
  for: [scripts, binaries]
traits:
  - id: actionscript-drm-listener
    desc: Implements DRMOperationCompleteListener interface
    crit: notable
    if:
      type: raw
      regex: implements\s+DRMOperationCompleteListener
