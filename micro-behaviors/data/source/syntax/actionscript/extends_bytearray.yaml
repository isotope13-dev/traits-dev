defaults:
  platforms:
    - unix
    - windows
    - macos
  for: [scripts, binaries]
traits:
  - id: actionscript-extends-bytearray
    desc: ActionScript class extending ByteArray (often used for
    crit: suspicious
    conf: 0.8
    if:
      type: string_value
      regex: (ByteArrayAsset|flash\.utils:ByteArray)
