defaults:
  platforms: [unix, windows]
  for: [go]

traits:
  - id: operator-typed-struct-field
    desc: Go struct field uses an Operator type
    crit: notable
    conf: 0.85
    if:
      type: text
      regex: '(?m)^\s*Operators?\s+\*?(operator\.)?Operator\b'
