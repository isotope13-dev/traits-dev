defaults:
  platforms: [unix, windows]
  for: [javascript, typescript]

traits:
- id: node-package-version-field
  desc: Node package version field
  crit: component
  conf: 0.8
  if:
    type: text
    word: packageVersion
