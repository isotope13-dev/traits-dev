defaults:
  for: [csharp]
  platforms: [windows, unix]

traits:
  - id: csharp-static-data-field-cluster
    desc: C# declares multiple static data fields
    crit: baseline
    conf: 0.9
    if:
      type: text
      regex: '(?im)\bstatic\s+(readonly\s+)?\w+[\[\]]*\s+\w+\s*[=;]'
    count_min: 4
