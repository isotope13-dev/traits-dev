defaults:
  platforms: [windows, unix]
  for: [javascript, typescript]

traits:
  - id: dependency-injection-call
    desc: Calls a dependency injection function
    crit: notable
    conf: 0.9
    if:
      type: symbol
      kind: call
      exact: injectDependency
