defaults:
  platforms: [windows, linux, macos]
  for: [python]

traits:
  - id: python-bytearray-constructor
    desc: Constructs a Python bytearray buffer
    crit: baseline
    conf: 0.95
    if:
      type: symbol
      kind: call
      exact: bytearray
