defaults:
  platforms: [windows]
  for: [powershell]
  crit: notable

traits:
  - id: powershell-fullname-timestamp-map
    desc: Maps file paths to modification times
    conf: 0.96
    if:
      type: text
      regex: '(?i)\$\w+\s*\[\s*\$\w+\.FullName\s*\]\s*=\s*\$\w+\.LastWriteTime(Utc)?\b'
