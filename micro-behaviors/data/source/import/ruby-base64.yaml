defaults:
  conf: 1.0
  platforms: [unix, windows]
  for: [ruby]
traits:
  - id: require-base64
    desc: Requires base64
    crit: baseline
    if:
      type: text
      substr: "require 'base64'"
  - id: usage-base64
    desc: Uses Base64 module
    crit: notable
    if:
      type: text
      substr: "Base64"
