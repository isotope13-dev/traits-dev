defaults:
  platforms: [unix, windows]
  for: [java, class, jar, zip]
traits:
  - id: java-defineclass
    desc: Java defineClass reflective load
    crit: component
    conf: 0.88
    if:
      type: text
      substr: defineClass
