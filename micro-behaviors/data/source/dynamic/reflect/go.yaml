defaults:
  platforms: [windows, unix]
  for: [go]
traits:
  - id: go-unsafe-package-import
    desc: Imports Go unsafe package
    crit: notable
    conf: 0.95
    if:
      type: import
      exact: unsafe
  - id: go-reflect-heavy
    desc: Repeated Go reflection API usage
    crit: notable
    conf: 0.8
    count_min: 3
    if:
      type: text
      regex: '\breflect\.'
  - id: go-unsafe-pointer-conversion
    desc: Converts a Go pointer with unsafe.Pointer
    crit: notable
    conf: 0.92
    if:
      type: symbol
      kind: call
      exact: unsafe.Pointer
composite_rules:
  - id: go-reflect-unsafe-combo
    desc: Go reflection with unsafe pointer conversion
    crit: notable
    conf: 0.85
    all:
      - id: go-unsafe-package-import
      - id: go-reflect-heavy
      - id: go-unsafe-pointer-conversion
