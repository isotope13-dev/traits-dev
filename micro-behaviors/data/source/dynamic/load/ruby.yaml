defaults:
  platforms: [unix, windows]
  for: [ruby]

traits:
  - id: ruby-load-source-call
    desc: Ruby loads source code with Kernel.load
    crit: notable
    conf: 0.85
    if:
      type: text
      regex: '(?m)^[ \t]*(Kernel[.])?load[ \t]*\('
