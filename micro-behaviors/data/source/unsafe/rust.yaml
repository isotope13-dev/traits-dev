defaults:
  crit: notable
  platforms: [windows, unix]
  for: [rust]

traits:
  - id: rust-mem-transmute-call
    desc: Rust calls mem::transmute
    conf: 0.85
    if:
      type: text
      regex: '\btransmute\s*(::<[^>]{0,60}>)?\s*\('
