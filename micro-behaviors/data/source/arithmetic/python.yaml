defaults:
  platforms: [windows, unix]
  for: [python]

traits:
  - id: bitshift-array
    desc: Python bitshift in a list comprehension
    crit: notable
    conf: 0.85
    if:
      type: text
      regex: '\[\s*\w+\s*(<<|>>)\s*\d+\s+for\s+\w+\s+in'

  - id: large-complex-number-expression
    desc: Python large complex-number expression
    crit: notable
    conf: 0.85
    if:
      type: tree-sitter
      language: python
      kind: binary_op
      regex: '\d{3,}\.\d+\s*[+-]\s*\d{3,}\.\d+j'
