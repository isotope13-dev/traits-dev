defaults:
  platforms: [windows]
  for: [pe]

traits:
  - id: go-claude-agent-watcher-function
    desc: Go function watches Claude agent sessions
    crit: notable
    conf: 0.98
    if:
      type: text
      substr: github.com/pa-arth/promptster-teams-cli/internal/capture.RunClaudeWatcher

  - id: go-codex-agent-watcher-function
    desc: Go function watches Codex agent sessions
    crit: notable
    conf: 0.98
    if:
      type: text
      substr: github.com/pa-arth/promptster-teams-cli/internal/capture.RunCodexWatcher

  - id: go-cursor-agent-watcher-function
    desc: Go function watches Cursor agent sessions
    crit: notable
    conf: 0.98
    if:
      type: text
      substr: github.com/pa-arth/promptster-teams-cli/internal/capture.RunCursorWatcher
