defaults:
  for: [swift]
  platforms: [unix]
  crit: component
traits:
  - id: swift-system-info-string-function
    desc: Swift systemInfo function returning a String
    conf: 0.9
    if:
      type: text
      substr: func systemInfo() -> String
