defaults:
  platforms: [windows, linux, macos]
  for: [elf, macho, pe, c, cpp, csharp, go, rust]
  crit: component
  conf: 0.85

traits:
  - id: get-session-id-accessor
    desc: GetSessionID function name
    if:
      type: text
      word: GetSessionID
