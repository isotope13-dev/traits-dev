traits:
  - id: go-agent-config-census-function
    desc: Go function inventories AI-agent configuration
    crit: notable
    conf: 0.98
    platforms: [windows]
    for: [pe]
    if:
      type: text
      regex: '\b(StartConfigCensus|emitConfigCensus|buildConfigCensus)\b'
