# ZIP Creation
defaults:
  for: [all]

traits:
  - id: generic
    desc: "Function to handle ZIP creation"
    crit: notable
    conf: 0.8
    if:
      type: string
      substr: "handleZipCreation"
