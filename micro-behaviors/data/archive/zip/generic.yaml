defaults:
  platforms:
    - unix
    - windows
    - macos
  for: [scripts, binaries]
traits:
  - id: generic-dup
    desc: Function to handle ZIP creation
    crit: notable
    conf: 0.8
    if:
      type: string_value
      substr: handleZipCreation
