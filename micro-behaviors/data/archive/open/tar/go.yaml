defaults:
  platforms: [windows, unix]
  for: [go]

traits:
  - id: go-tar-reader
    desc: Opens a tar archive reader
    crit: notable
    conf: 0.9
    if:
      type: symbol
      kind: call
      exact: tar.NewReader
