defaults:
  platforms: [macos]
  for: [macho, shell]
traits:
  - id: app-archive-name
    desc: macOS app bundle archive filename
    crit: component
    conf: 0.85
    if:
      type: text
      regex: '\b[A-Za-z0-9._ -]{1,60}App\.zip\b'
