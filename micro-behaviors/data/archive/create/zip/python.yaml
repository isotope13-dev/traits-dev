defaults:
  platforms: [unix, windows]
  for: [python, pyc]
  attack: T1560.001

traits:
  - id: python-zipfile-writer
    desc: Python writes a ZIP archive
    crit: notable
    conf: 0.9
    if:
      type: text
      regex: '\bzipfile\.ZipFile\s*\('
