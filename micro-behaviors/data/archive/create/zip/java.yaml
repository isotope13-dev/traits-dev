defaults:
  platforms: [unix, windows]
  for: [class]

traits:
  - id: jvm-zip-output-stream-class-ref
    desc: JVM bytecode references ZIP archive creation
    crit: notable
    conf: 0.95
    if:
      type: value
      path: class.class_refs[*]
      exact: java/util/zip/ZipOutputStream
