defaults:
  platforms: [unix, windows]
  for: [php]
  crit: notable
  conf: 0.9

traits:
  - id: php-ziparchive-add-from-string
    desc: PHP ZipArchive adds in-memory member
    if:
      type: text
      substr: addFromString
