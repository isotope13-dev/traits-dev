defaults:
  platforms: [windows, unix]
  for: [pe, dll, csharp, powershell, xml]

traits:
  - id: dotnet-create-from-directory
    desc: .NET CreateFromDirectory archive API
    crit: notable
    conf: 0.85
    if:
      type: text
      word: CreateFromDirectory
