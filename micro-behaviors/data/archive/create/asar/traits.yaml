defaults:
  platforms: [unix, windows]
  for: [scripts, binaries]

traits:
  - id: asar-pack
    desc: Creates an Electron ASAR archive
    crit: notable
    conf: 0.85
    if:
      type: text
      regex: '(?i)\basar(\.(cmd|exe))?["''\s:=-]{0,20}(pack|create)\b'
