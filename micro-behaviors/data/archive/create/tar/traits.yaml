defaults:
  platforms: [unix, windows]
  for: [shell, python, javascript, elf]
  crit: notable
  conf: 0.6
  attack: T1560

traits:
  - id: tar-create
    desc: Creates a tar archive
    if:
      type: text
      regex: '(^|[\n;&|({\[=:])\s*tar\s+-[A-Za-z]*c'
