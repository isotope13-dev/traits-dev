defaults:
  platforms: [unix, windows]
  for: [scripts, binaries]
traits:
  - id: unzip-overwrite
    desc: Unzip overwrites existing files
    crit: notable
    conf: 0.55
    if:
      type: string_literal
      regex: unzip\s+-o
