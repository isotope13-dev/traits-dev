defaults:
  platforms: [unix, windows]
  for: [ruby]
  crit: notable
  conf: 0.95

traits:
  - id: ruby-tar-archive-extract
    desc: Ruby extracts a tar archive via system()
    if:
      type: symbol
      kind: call
      exact: system
      arg:
        kind: string
        exact: tar
