defaults:
  platforms: [unix]
  for: [scripts, source, binaries]

traits:
  - id: external-compression-program
    desc: Tar extraction invokes an external filter
    crit: notable
    conf: 0.96
    if:
      type: text
      regex: '(?i)--use-compress-program(=|\s+)[^\s"'';&|]{1,300}'
