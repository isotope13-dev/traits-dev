defaults:
  platforms: [unix, windows]
  for: [python]
  attack: T1027.009
traits:
  - id: python-base64-zip-literal
    desc: Python embeds base64 ZIP payload
    crit: notable
    conf: 0.9
    if:
      type: literal
      kind: string
      regex: '^UEsDB[A-Za-z0-9+/=]{200,}$'
