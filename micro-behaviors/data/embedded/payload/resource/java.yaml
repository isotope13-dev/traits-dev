defaults:
  platforms: [unix, windows]
  for: [java, class, jar, zip]

traits:
  - id: java-getresourceasstream
    desc: Java classpath resource stream lookup
    crit: notable
    conf: 0.9
    if:
      type: text
      substr: getResourceAsStream
