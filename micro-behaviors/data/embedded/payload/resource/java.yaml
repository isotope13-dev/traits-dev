defaults:
  platforms: [unix, windows]
  for: [java, class, java_class, jar, zip]

traits:
  - id: java-getresourceasstream
    desc: Java classpath resource stream lookup
    crit: notable
    conf: 0.9
    if:
      type: import
      exact: java/lang/ClassLoader.getResourceAsStream
