# PHP Input Stream Access
# ATT&CK: T1059 (Command and Scripting Interpreter)

defaults:
  for: [php]
  crit: suspicious

traits:
  - id: php-input
    desc: Access to php://input (raw POST
    conf: 0.95
    if:
      type: string
      regex: "php://input"
