defaults:
  platforms: [unix, windows]
  for: [binaries, scripts, source]
  conf: 0.7
traits:
  - id: base64-token
    desc: base64 encoding keyword
    crit: notable
    if:
      type: text
      word: base64
      case_insensitive: true
