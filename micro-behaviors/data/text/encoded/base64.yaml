defaults:
  platforms: [unix, windows]
  for: [binaries, scripts, source]
  crit: component
  conf: 0.7
traits:
  - id: base64-token
    desc: Base64 text token
    if:
      type: text
      substr: base64
      case_insensitive: true
