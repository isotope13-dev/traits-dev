# Netdata Identification Traits
defaults:
  platforms: [unix, windows]
  for: [binaries, scripts]
traits:
  - id: netdata-path
    desc: File is part of netdata
    crit: baseline
    conf: 0.95
    count_min: 5
    if:
      type: text
      regex: netdata|NETDATA
      case_insensitive: true
