defaults:
  platforms: [unix, windows]
  for: [none]
  crit: baseline
  conf: 1.0
traits:
  - id: git-lfs-pointer
    desc: Git LFS pointer file
    if:
      type: raw
      substr: "version https://git-lfs.github.com/spec/v1"
