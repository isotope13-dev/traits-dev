defaults:
  platforms: [unix, windows]
  for: [text]
  crit: baseline
  conf: 1.0
traits:
  - id: git-lfs-pointer
    desc: Git LFS pointer file
    if:
      type: text
      substr: "version https://git-lfs.github.com/spec/v1"
