defaults:
  platforms: [unix, windows]
  for: [documents, scripts, source]
  crit: component
  conf: 0.7
traits:
  - id: webp-token
    desc: WebP image format token
    if:
      type: text
      substr: webp
