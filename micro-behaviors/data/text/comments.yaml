defaults:
  for: [scripts, source]
traits:
  - id: exploit-code-discussion
    desc: Discussion of exploit code in comments
    crit: component
    conf: 0.2
    if:
      type: text
      regex: '#.{0,50}exploit code|//.{0,50}exploit payload'