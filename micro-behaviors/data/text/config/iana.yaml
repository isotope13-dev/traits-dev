defaults:
  for: [c, cpp]
traits:
  - id: iana-port-definition
    desc: "IANA port definition structure"
    crit: baseline
    conf: 1.0
    if:
      type: raw
      regex: '\{ ".{0,220}", \{ NULL \}, \d+, ".{0,220}" \}'
