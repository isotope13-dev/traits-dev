defaults:
  platforms: [unix, windows, macos]
  for: [shell]
traits:
  - id: fake-shell-error
    desc: Fake shell error message (spoofed)
    criticality: suspicious
    if:
      type: string
      regex: 'shell:\s+[^:]{1,120}:\s+No such file or directory'
