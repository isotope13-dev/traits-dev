defaults:
  platforms:
    - unix
    - windows
  for:
    - shell
traits:
  - id: fake-shell-error
    desc: Fake shell error message (spoofed)
    crit: suspicious
    if:
      type: string_literal
      regex: shell:\s+[^:]{1,120}:\s+No such file or directory
