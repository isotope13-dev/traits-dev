defaults:
  platforms: [unix, windows, macos]
  for: [scripts, binaries]
traits:
  - id: telegram-bypass-tool-string
    crit: notable
    desc: "Contains Telegram bypass tool string"
    if:
      type: raw
      substr: "Telegram Bypass Tool"
  - id: telegram-fix-string
    crit: notable
    desc: "Contains Telegram Fix tool string"
    if:
      type: raw
      substr: "Telegram Fix"
