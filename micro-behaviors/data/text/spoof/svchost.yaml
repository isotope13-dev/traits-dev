defaults:
  platforms: [unix, windows, macos]
  for: [shell, python, javascript, powershell, pe, elf]
traits:
  - id: double-extension-svchost
    crit: suspicious
    desc: "Double extension svchost evasion string"
    if:
      type: raw
      substr: "svchost.svchost.exe"
