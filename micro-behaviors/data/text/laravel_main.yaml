traits:
  - id: micro-behaviors.data.text::laravel-main
    criticality: suspicious
    desc: "References known malicious GitHub user `laravel-main`."
    platforms: [linux, macos, windows]
    for: [scripts, manifests]
    if:
      type: text
      substr: "laravel-main"
