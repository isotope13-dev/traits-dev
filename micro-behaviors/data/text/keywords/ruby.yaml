defaults:
  for:
    - ruby
  platforms:
    - unix
    - windows
traits:
  - id: split-string
    desc: String split operation
    crit: baseline
    conf: 0.7
    if:
      type: string_value
      regex: split.{0,50}\.|file_name.{0,50}split
