defaults:
  platforms: [unix, windows]
traits:
  - id: github-actions-targeting-shell
    desc: Targets GitHub Actions (Shell)
    crit: component
    conf: 0.9
    for:
      - shell
    if:
      type: string_literal
      substr: isSecret":true
