defaults:
  platforms:
    - unix
    - windows
  for: [scripts, binaries]
traits:
  - id: hacker-tool-log-marker
    desc: Bracket-star log marker
    crit: baseline
    conf: 0.8
    if:
      type: text
      substr: '[*] '
