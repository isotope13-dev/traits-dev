defaults:
  platforms:
    - unix
    - windows
    - macos
  for: [scripts, binaries]
traits:
  - id: hacker-tool-log-marker
    desc: Hacker tool logging marker
    crit: notable
    conf: 0.8
    if:
      type: string_value
      substr: '[*] '
