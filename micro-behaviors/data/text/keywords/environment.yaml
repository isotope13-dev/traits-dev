defaults:
  platforms: [unix, windows, macos]
  for: [shell, batch, python, javascript, powershell, vbs]
traits:
  - id: environment-keyword
    desc: '"Environment" keyword'
    crit: component
    if:
      type: string
      word: environment
      case_insensitive: true
