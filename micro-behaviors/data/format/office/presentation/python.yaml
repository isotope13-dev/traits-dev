defaults:
  platforms: [unix, windows]
  for: [python]
  crit: notable
  conf: 0.9

traits:
  - id: python-presentation-parser-import
    desc: Imports Python PowerPoint parser
    if:
      type: import
      regex: '^pptx(\.|$)'
