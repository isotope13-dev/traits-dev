defaults:
  platforms: [unix, windows]
  for: [python]
  crit: notable
  conf: 0.9

traits:
  - id: python-docx-writer-import
    desc: Imports Python DOCX writer
    if:
      type: import
      regex: '^docx(\.|$)'
