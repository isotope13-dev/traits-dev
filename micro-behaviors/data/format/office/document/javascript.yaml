defaults:
  platforms: [windows, unix]
  for: [javascript, typescript, scripts, binaries]
  conf: 0.8

traits:
  - id: document-extension-literal
    desc: References a document filename suffix
    crit: component
    if:
      type: text
      regex: "['\"`]\\.(docx?|xlsx?|txt)['\"`]"
