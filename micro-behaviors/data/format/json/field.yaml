defaults:
  platforms: [unix, windows, android]
  for: [binaries, scripts]
traits:
  - id: status-json-field
    desc: JSON status field key
    crit: baseline
    conf: 0.4
    if:
      type: text
      regex: '",\s*"status"\s*:'
      section: .text
