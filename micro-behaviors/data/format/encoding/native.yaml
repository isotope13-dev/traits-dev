defaults:
  platforms: [unix]
  for: [elf, macho]
  crit: notable
  conf: 0.9

traits:
  - id: native-encoded-decoded-format
    desc: Native encoded and decoded format labels
    if:
      type: text
      regex: 'encoded\[%s\][\s\S]{0,120}decoded\[%s\]'
