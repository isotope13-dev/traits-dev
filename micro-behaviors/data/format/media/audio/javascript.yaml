defaults:
  platforms: [unix, windows]
  for: [javascript, typescript]

traits:
  - id: buffer-from-base64-audio
    desc: Decodes Base64 audio into a Buffer
    crit: notable
    conf: 0.96
    if:
      type: text
      regex: 'Buffer\.from\(\s*(audio|audioData|audioBase64)\s*,\s*["'']base64["'']\s*\)'
