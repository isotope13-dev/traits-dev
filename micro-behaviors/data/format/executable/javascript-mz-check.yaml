defaults:
  platforms: [linux, macos, windows, unix, android, ios]
  for: [javascript]
traits:
  - id: js-mz-header-check-array
    desc: Checks for MZ header in array
    crit: notable
    conf: 0.9
    if:
      type: string_value
      regex: '\[0\]\s*!=\s*77\s*\|\|\s*\w+\[1\]\s*!=\s*90'
