defaults:
  platforms: [linux]
  for: [shell]
  crit: suspicious
  conf: 0.95
traits:
  - id: elf-header-probing-dd
    desc: Probes ELF header using dd
    if:
      type: text
      regex: 'dd\s+bs=52\s+count=1\s+if='
