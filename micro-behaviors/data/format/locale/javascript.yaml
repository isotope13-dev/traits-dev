defaults:
  platforms: [windows, unix]
  for: [javascript, typescript]

traits:
  - id: js-locale-formatting-call
    desc: JavaScript locale-aware formatting call
    crit: notable
    conf: 0.9
    if:
      type: text
      regex: '\b(toLocale(Date|String)|Intl\.)'
