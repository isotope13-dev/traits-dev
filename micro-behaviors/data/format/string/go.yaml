defaults:
  platforms: [unix, windows]
  for: [go]

traits:
  - id: guid-formatted-literal
    desc: GUID-formatted string literal
    crit: component
    conf: 0.85
    if:
      type: literal
      regex: '^[0-9a-fA-F]{8}(-[0-9a-fA-F]{4}){3}-[0-9a-fA-F]{12}$'
