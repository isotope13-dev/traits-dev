defaults:
  platforms: [windows]
  for: [pe, dll, c, cpp]
  crit: notable
  conf: 0.9

traits:
  - id: uuid-from-string-api
    desc: Convert UUID text via Windows RPC API
    if:
      type: import
      regex: '^UuidFromString[AW]$'
