defaults:
  for:
    - ruby
  platforms:
    - unix
    - windows
traits:
  - id: signature-verify
    desc: Digital signature verification
    crit: notable
    conf: 0.85
    attack: T1027
    if:
      type: string_literal
      regex: .verify\(|OpenSSL::Digest|sha256
