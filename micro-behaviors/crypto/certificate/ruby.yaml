defaults:
  for:
    - ruby
  platforms:
    - unix
    - windows
traits:
  - id: signature-verify
    desc: Digital signature verification
    crit: baseline
    conf: 0.85
    attack: T1027
    if:
      type: string_value
      regex: .verify\(|OpenSSL::Digest|sha256
