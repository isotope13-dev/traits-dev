defaults:
  platforms: [unix, windows]
  for: [scripts, source, plist, html, xml]

traits:
  - id: apple-root-certificate-profile-payload
    desc: Apple profile installs a root certificate
    crit: notable
    conf: 0.98
    if:
      type: text
      substr: com.apple.security.root
