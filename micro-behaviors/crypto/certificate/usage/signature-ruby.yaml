defaults:
  for:
    - ruby
  platforms:
    - unix
    - windows
traits:
  - id: signature-verify
    desc: Ruby calls a verify or verify_pss method
    crit: notable
    conf: 0.85
    if:
      type: symbol
      kind: call
      regex: '^verify(_pss)?$'
