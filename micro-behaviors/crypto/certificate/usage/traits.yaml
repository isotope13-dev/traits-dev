defaults:
  platforms: [unix, windows, macos]
composite_rules:
  - id: has-pem-certificate
    desc: File contains a PEM-encoded certificate.
    crit: baseline
    conf: 1.0
    all:
      - id: pem-begin
      - id: pem-end
