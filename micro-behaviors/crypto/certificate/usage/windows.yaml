defaults:
  platforms: [windows]
  for: [powershell, batch]
  crit: notable
  conf: 0.9

traits:
  - id: certutil-enrollment-refresh
    desc: certutil refreshes enrollment policy
    if:
      type: text
      regex: '(?i)\bcertutil(\.exe)?\s+-pulse\b'
