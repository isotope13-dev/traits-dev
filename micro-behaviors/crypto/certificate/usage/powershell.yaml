defaults:
  platforms: [windows]
  for: [powershell]

traits:
  - id: powershell-self-signed-cert
    desc: Creates self-signed certificate
    crit: notable
    conf: 0.95
    if:
      type: symbol
      kind: call
      exact: New-SelfSignedCertificate
