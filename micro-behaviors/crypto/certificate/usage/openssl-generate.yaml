defaults:
  platforms: [windows, unix]
  for: [scripts]
  attack: T1587.003

traits:
  - id: openssl-self-signed-cert
    desc: Generates self-signed TLS certificate
    crit: notable
    conf: 0.85
    if:
      type: text
      regex: (\$OPENSSL|openssl)\s+req.{0,60}-x509
