defaults:
  platforms: [linux, macos, windows]
  for: [binaries]

composite_rules:
  - id: crypto-library-context
    desc: Native cryptographic library context
    crit: notable
    conf: 0.99
    any:
      - id: micro-behaviors/dylib/library/ssl::libcrypto-ref
      - id: metadata/library/data::cryptography-binding
