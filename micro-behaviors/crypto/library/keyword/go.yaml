defaults:
  platforms: [windows, unix]
  for: [binaries]

traits:
  - id: go-key-import-export-method
    desc: Go key import/export method
    crit: notable
    conf: 0.85
    if:
      type: text
      regex: \)\.(Export|Import)(Key|PublicKey)(\.|$)
