defaults:
  platforms: [windows]
  for: [c, cpp, csharp, go, rust]

traits:
  - id: bcrypt-open-provider-source
    desc: Source opens a Windows BCrypt provider
    crit: notable
    conf: 0.94
    if:
      type: symbol
      kind: call
      exact: BCryptOpenAlgorithmProvider
