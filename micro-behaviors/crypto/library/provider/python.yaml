defaults:
  platforms: [windows, unix]
  for: [python]

traits:
  - id: imports-cryptography
    desc: Imports Python cryptography library
    crit: notable
    conf: 0.95
    size_min: 16
    size_max: 50000000
    if:
      type: import
      regex: '^cryptography\.'
