defaults:
  platforms: [unix]
  for: [shell, elf]
traits:
  - id: openssl-passwd
    desc: OpenSSL passwd hash generation
    crit: notable
    conf: 0.75
    if:
      type: text
      regex: \bopenssl\s+passwd\b
