defaults:
  platforms: [windows, unix]
  for: [pe, dll, elf]

traits:
  - id: go-main-data-crypt-symbol
    desc: Go main data cryptography symbol
    crit: notable
    conf: 0.95
    if:
      type: text
      regex: 'main\.(encrypt|decrypt)Data(\.|$)'
