defaults:
  platforms: [windows, unix]
  for: [pe, dll, csharp, powershell]
  crit: notable
  conf: 0.9

traits:
  # SymmetricAlgorithm.CreateEncryptor serves DES, AES, and other algorithms.
  # Only a separate algorithm-specific reference establishes AES use.
  - id: dotnet-create-encryptor
    desc: .NET symmetric encryption API reference
    if:
      type: text
      substr: CreateEncryptor
