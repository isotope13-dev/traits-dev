defaults:
  platforms: [windows, unix]
  for: [go, binaries]
traits:
- id: go-xor-encoding--xorbytes
  desc: Go XOR stream operations (xorBytes)
  crit: notable
  conf: 0.85
  if:
    type: text
    word: xorBytes
- id: go-xor-encoding--xorkeystream
  desc: Go XOR stream operations (XORKeyStream)
  crit: notable
  conf: 0.85
  if:
    type: text
    word: XORKeyStream
composite_rules:
- id: go-xor-encoding
  desc: "Go XOR stream operations"
  crit: notable
  conf: 0.85
  any:
  - id: go-xor-encoding--xorbytes
  - id: go-xor-encoding--xorkeystream
