defaults:
  platforms: [windows, unix]
  for: [go, binaries]
traits:
  - id: go-xor-encoding
    desc: "Go XOR stream operations"
    crit: notable
    conf: 0.85
    if:
      type: text
      regex: (xorBytes|XORKeyStream)
