defaults:
  platforms: [windows, unix]
  for: [go, binaries, wasm]
traits:
- id: go-xor-encoding--xorbytes
  desc: Go byte-slice XOR helper (xorBytes)
  crit: notable
  conf: 0.85
  if:
    type: text
    word: xorBytes
- id: go-xor-encoding--xorkeystream
  desc: Go XOR stream operations (XORKeyStream)
  crit: notable
  conf: 0.85
  if:
    type: text
    word: XORKeyStream
