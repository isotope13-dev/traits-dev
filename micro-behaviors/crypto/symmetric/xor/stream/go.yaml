defaults:
  platforms: [windows, unix]
  for: [go, binaries, wasm]
traits:
- id: go-xor-encoding--xorkeystream
  desc: Go x/crypto ChaCha20 stream operation
  crit: notable
  conf: 0.85
  if:
    type: text
    regex: 'golang\.org/x/crypto/chacha20\.\(\*Cipher\)\.XORKeyStream'
