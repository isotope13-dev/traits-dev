defaults:
  platforms:
    - unix
    - windows
  for: [binaries]
traits:
  - id: go-xordecrypt-func
    desc: Payload XOR decryption routine
    crit: notable
    conf: 0.9
    if:
      type: text
      exact: main.xorDecrypt
