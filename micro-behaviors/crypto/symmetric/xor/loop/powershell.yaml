defaults:
  platforms: [windows]
  for: [powershell]
  mbc: B0032.002

traits:
  - id: indexed-bxor-loop
    desc: PowerShell indexed XOR loop
    crit: suspicious
    conf: 0.92
    if:
      type: text
      regex: '\$\w+\s*\[\s*\$_\s*\]\s*-bxor\s*\$\w+\s*\[\s*\$_\s*%\s*\$\w+\.Length\s*\]'
