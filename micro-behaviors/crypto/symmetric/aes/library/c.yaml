defaults:
  platforms: [windows, unix]
  for: [elf, macho, pe, c, cpp]
  crit: notable
  mbc: C0068

traits:
  - id: mbedtls-aes-crypt
    desc: mbedTLS AES cipher routine
    conf: 0.9
    if:
      type: symbol
      regex: '^mbedtls_aes_crypt_'
