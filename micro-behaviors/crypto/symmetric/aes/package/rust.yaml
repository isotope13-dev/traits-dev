defaults:
  platforms: [unix, windows]
  for: [cargo.toml]

traits:
  - id: cargo-dep-aes
    desc: Cargo manifest declares the AES crate
    crit: notable
    conf: 0.82
    if:
      type: value
      path: dependencies.aes
      regex: '.+'
