defaults:
  platforms: [windows]
  for: [powershell]
  crit: notable
  conf: 0.9

traits:
  - id: powershell-crypto-stream-write
    desc: PowerShell writes through a .NET CryptoStream
    if:
      type: text
      regex: '(?i)CryptoStreamMode\s*\]::\s*Write'
