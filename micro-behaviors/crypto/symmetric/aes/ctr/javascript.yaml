defaults:
  platforms: [windows, unix]
  for: [javascript, typescript]
traits:
  - id: aes-ctr-algorithm-marker
    desc: JavaScript AES-CTR algorithm marker
    crit: notable
    conf: 0.94
    if:
      type: text
      regex: '(?i)\baes-(128|192|256)-ctr\b'
