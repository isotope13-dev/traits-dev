---
defaults:
  platforms: [windows]
  for: [pe, csharp]

traits:
  - id: rc2-provider
    desc: .NET RC2 cryptography provider reference
    crit: notable
    conf: 0.9
    if:
      type: text
      exact: RC2CryptoServiceProvider
