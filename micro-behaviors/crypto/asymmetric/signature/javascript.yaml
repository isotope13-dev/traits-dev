defaults:
  platforms: [windows, unix]
  for: [javascript, typescript]
  crit: notable
  conf: 0.88

traits:
  - id: signature-import-helper
    desc: Uses a signature import helper
    if:
      type: symbol
      kind: call
      regex: '(^|\.)signatureImport$'
