defaults:
  platforms: [windows]
  for: [go, pe]

traits:
  - id: go-public-key-file-wrap
    desc: Go public-key file-key wrapping routine
    crit: notable
    conf: 0.96
    if:
      type: text
      substr: EncryptWithPublicKey
