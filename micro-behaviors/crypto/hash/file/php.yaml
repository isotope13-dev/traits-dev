defaults:
  platforms: [windows, unix]
  for: [php]

traits:
  - id: hash-file-contents
    desc: PHP hashes the contents of a file
    crit: notable
    conf: 0.9
    mbc: C0029
    if:
      type: symbol
      kind: call
      exact: hash_file
