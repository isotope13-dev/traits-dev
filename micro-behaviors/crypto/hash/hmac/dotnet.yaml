defaults:
  platforms: [windows]
  for: [pe, dll, csharp]

traits:
  - id: dotnet-hmacsha256
    desc: .NET HMAC-SHA256 operation
    crit: notable
    conf: 0.93
    if:
      type: text
      substr: HMACSHA256
