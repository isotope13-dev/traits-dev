defaults:
  platforms:
    - unix
    - windows
  for:
    - ruby
traits:
  - id: openssl-hmac
    desc: Ruby OpenSSL HMAC operation
    crit: notable
    conf: 0.9
    if:
      type: text
      regex: 'OpenSSL::HMAC\.(hexdigest|digest)\s*\('
