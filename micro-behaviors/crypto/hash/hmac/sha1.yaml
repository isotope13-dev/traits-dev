defaults:
  platforms: [unix, windows]
  for: [scripts, binaries, go]

traits:
  - id: hmac-sha1-operation
    desc: HMAC-SHA1 operation marker
    crit: notable
    conf: 0.93
    if:
      type: text
      regex: '(?i)hmac[_-]?sha1'
