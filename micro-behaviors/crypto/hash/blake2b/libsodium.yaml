defaults:
  for: [elf, macho, pe, so, dylib, dll]
  platforms: [linux, unix, macos, windows]
traits:
  - id: libsodium-blake2b
    desc: Libsodium Blake2b hash routine
    crit: notable
    conf: 0.98
    if:
      type: string
      exact: "crypto_generichash_blake2b_final"
