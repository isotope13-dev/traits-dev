defaults:
  platforms: [windows]
  for: [batch, powershell]
  crit: notable
  conf: 0.94

traits:
  - id: certutil-hashfile
    desc: Windows certutil file hash calculation
    if:
      type: text
      regex: '(?im)^\s*(&\s*)?["'']?(\.\\)?certutil(\.exe)?["'']?\s+-hashfile\b'
