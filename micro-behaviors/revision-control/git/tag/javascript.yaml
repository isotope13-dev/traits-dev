defaults:
  platforms:
    - linux
    - macos
    - windows
    - unix
    - android
    - ios
  for:
    - javascript
traits:
  - id: git-tag-annotate
    desc: Creates annotated git tag
    crit: notable
    conf: 0.82
    if:
      type: text
      regex: "git\\s+tag\\s+-a\\b"
