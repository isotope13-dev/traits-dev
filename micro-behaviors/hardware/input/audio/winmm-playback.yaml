defaults:
  platforms:
  - windows
  for:
  - pe
  - dll
traits:
- id: time-set-event
  desc: timeSetEvent multimedia timer import
  crit: notable
  conf: 0.9
  if:
    type: import
    exact: timeSetEvent
- id: wave-out-open
  desc: waveOutOpen audio playback import
  crit: notable
  conf: 0.9
  if:
    type: import
    exact: waveOutOpen
