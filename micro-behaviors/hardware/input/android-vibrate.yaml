defaults:
  platforms: [android]
  for: [java]
  crit: baseline
  conf: 0.86

traits:
  - id: vibrate-harassment
    desc: Android vibration API use
    crit: baseline
    conf: 0.95
    count_min: 1
    if:
      type: text
      substr: "vibrate("
