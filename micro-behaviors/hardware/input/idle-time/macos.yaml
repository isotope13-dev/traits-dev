defaults:
  platforms: [macos]
  for: [macho]

traits:
  - id: macos-hid-idle-time
    desc: macOS HID idle time API
    crit: notable
    conf: 0.8
    if:
      type: text
      substr: HIDIdleTime
