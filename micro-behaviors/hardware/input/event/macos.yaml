defaults:
  platforms: [macos]
  for: [macho, c, cpp]

traits:
  - id: macos-cgevent-tap-create
    desc: macOS CGEvent tap creation
    crit: notable
    conf: 0.85
    if:
      type: symbol
      substr: CGEventTapCreate
