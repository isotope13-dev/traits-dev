defaults:
  platforms: [unix, windows]
  for: [javascript, typescript]

traits:
  - id: browser-deviceorientation-listener
    desc: Browser device-orientation event listener
    crit: notable
    conf: 0.82
    if:
      type: text
      regex: 'addEventListener\(["'']deviceorientation["'']\)'
