defaults:
  platforms: [unix]
  for: [binaries, scripts]

traits:
  - id: linux-input-device-path
    desc: Linux input device path
    crit: notable
    conf: 0.84
    if:
      type: text
      substr: /dev/input
