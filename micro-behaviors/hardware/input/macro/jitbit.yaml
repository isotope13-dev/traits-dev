defaults:
  platforms: [windows]
  for: [pe]
  crit: notable
  conf: 0.98

traits:
  - id: play-saved-macro-file
    desc: Jitbit macro invokes saved macro
    if:
      type: raw
      regex: 'PLAY MACRO : [A-Za-z]:\\[^\r\n]{1,240}\.mcr'
