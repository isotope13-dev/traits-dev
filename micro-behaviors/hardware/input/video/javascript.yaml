defaults:
  platforms: [windows, unix]
  for: [javascript, typescript]
  crit: notable

traits:
  - id: browser-video-loop-enabled
    desc: Enables looping on browser video playback
    conf: 0.9
    if:
      type: text
      regex: 'createElement\(["'']video["'']\)[\s\S]{0,240}\.loop\s*=\s*(!0|true)'
