defaults:
  platforms: [windows]
  for: [scripts, binaries]

traits:
  - id: public-input-hook-library-api
    desc: Compatibility placeholder
    crit: component
    conf: 1.0
    if:
      type: text
      exact: __cleave_missing_public_input_hook_library_api__
