defaults:
  platforms: [windows]
  for: [vbs, javascript]
  crit: notable
  conf: 0.88

traits:
  - id: wsh-sendkeys
    desc: WSH SendKeys simulated keystrokes
    if:
      type: text
      regex: '(?i)\.SendKeys\s+'
