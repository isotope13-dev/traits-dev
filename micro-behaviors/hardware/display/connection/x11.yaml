defaults:
  platforms: [linux, macos]
  for: [elf, macho, so, c, cpp]

traits:
  - id: x11-open-display
    desc: References XOpenDisplay X11 connection API
    crit: notable
    conf: 0.95
    if:
      type: import
      exact: XOpenDisplay
