defaults:
  platforms: [windows, unix]
  for: [javascript, typescript]
  attack: T1113
  mbc: C0014

traits:
  - id: javascript-page-screenshot
    desc: JavaScript page screenshot call
    crit: notable
    conf: 0.9
    if:
      type: text
      regex: '\b[A-Za-z_$][A-Za-z0-9_$]*\.screenshot\s*\('
