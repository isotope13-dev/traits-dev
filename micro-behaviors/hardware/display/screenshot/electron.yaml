defaults:
  platforms: [windows, unix]
  for: [javascript, typescript]
  crit: component
  conf: 0.9

traits:
  - id: take-screenshot-string
    desc: Electron takeScreenshot string
    if:
      type: literal
      kind: string
      exact: takeScreenshot
