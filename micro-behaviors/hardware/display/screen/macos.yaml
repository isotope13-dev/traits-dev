defaults:
  for: [macho]
  platforms: [macos]
  attack: T1113
  mbc: C0014

traits:
  - id: cgwindow-image-capture-import
    desc: macOS CGWindow image capture import
    crit: notable
    conf: 0.95
    if:
      type: symbol
      exact: CGWindowListCreateImageFromArray
