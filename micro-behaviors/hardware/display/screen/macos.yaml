defaults:
  for: [macho]
  platforms: [macos]
  attack: T1113
  mbc: C0014

traits:
  - id: macos-screen-capture-api-import
    desc: macOS screen capture API import
    crit: notable
    conf: 0.95
    if:
      type: import
      regex: '^(CGDisplay(CreateImage|StreamCreate)|CGWindowListCreateImage|SCShareableContent)'
