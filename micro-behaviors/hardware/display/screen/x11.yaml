defaults:
  platforms: [unix]
  for: [binaries, source]

traits:
  - id: x11-reset-symbol
    desc: X11 screen capture reset function
    crit: component
    conf: 0.8
    if:
      type: text
      word: x11Reset