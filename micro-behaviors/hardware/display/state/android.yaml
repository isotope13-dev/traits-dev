defaults:
  platforms: [android]
  for: [apk, zip, dex]

traits:
  - id: android-screen-interactive-check
    desc: Checks whether Android screen is interactive
    crit: notable
    conf: 0.9
    if:
      type: text
      exact: isInteractive
