defaults:
  platforms: [android]
  for: [apk]

traits:
  - id: android-nfc-card-reader-reference
    desc: Android NFC card-reader reference
    crit: notable
    conf: 0.9
    if:
      type: text
      regex: 'Landroid/nfc/(NfcAdapter|tech/IsoDep);'
