# Random Device - Unix

defaults:
  for: [all]

traits:
  - id: urandom
    desc: Access /dev/urandom
    crit: baseline
    conf: 1.0
    platforms: [linux, macos, unix]
    if:
      type: string
      substr: "/dev/urandom"
