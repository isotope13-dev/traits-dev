# Wireless Network - Linux
# ATT&CK: T1016 (System Network Configuration Discovery)

defaults:
  for: [all]

traits:
  - id: wpa-supplicant
    desc: WPA supplicant wireless access
    crit: notable
    conf: 0.6
    platforms: [linux]
    if:
      type: string
      substr: "wpa_supplicant"

  - id: wps-supplicant
    desc: WPS supplicant wireless access
    crit: notable
    conf: 0.6
    platforms: [linux]
    if:
      type: string
      substr: "wps_supplicant"
