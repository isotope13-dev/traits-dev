defaults:
  platforms:
    - unix
    - windows
  for:
    - shell
traits:
  - id: dd-background
    desc: dd command running in background
    crit: suspicious
    conf: 0.66
    mbc: B0040
    attack: T1561
    if:
      type: string_literal
      regex: dd if=[\/\w\.\-\" \=]{0,64} &[^&]
