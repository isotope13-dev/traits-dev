defaults:
  platforms: [unix]
  for: [swift]
  attack: T1499

traits:
  - id: swift-socket-connect-loop
    desc: Swift socket connect loop
    crit: suspicious
    conf: 0.92
    if:
      type: text
      regex: 'while\s+true[\s\S]{0,320}socket\s*\(\s*AF_INET[\s\S]{0,520}connect\s*\('
