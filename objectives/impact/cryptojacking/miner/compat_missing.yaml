defaults:
  platforms: [windows]
  for: [binaries]

traits:
  - id: set-thread-affinity
    desc: Compatibility alias
    crit: component
    conf: 1.0
    if:
      id: objectives/anti-analysis/debugger-detect/check::objectives/impact/cryptojacking/miner::set-thread-affinity
