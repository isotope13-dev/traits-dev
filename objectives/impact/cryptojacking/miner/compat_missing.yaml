traits:
  - id: monero-wallets
    desc: compat placeholder
    crit: component
    conf: 0.1
    if:
      type: string
      exact: __cleave_missing_33c46a836879__
defaults:
  crit: component
  conf: 0.1
  for:
    - all
