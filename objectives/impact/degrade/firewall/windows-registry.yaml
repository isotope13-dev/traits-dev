# Firewall disable via registry - Windows
# Disabling Windows firewall by modifying SharedAccess service
defaults:
  for: [c, cpp, pe, dll]
  platforms: [windows]
  attack: T1562.004
traits: []
composite_rules:
