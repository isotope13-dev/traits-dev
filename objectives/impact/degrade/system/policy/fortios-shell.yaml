defaults:
  platforms: [fortios, unix]
  for: [shell]
  attack: T1562.001

traits:
  - id: fortios-admin-trusted-host-unset
    desc: Removes FortiOS admin trusted-host limit
    crit: suspicious
    conf: 0.97
    if:
      type: text
      regex: '(?m)^\s*unset\s+trusthost([1-9]|10)\s*$'
