defaults:
  platforms: [fortios, unix]
  for: [shell]
  attack: T1562.001

traits:
  - id: fortios-admin-two-factor-unset
    desc: Disables FortiOS admin two-factor auth
    crit: suspicious
    conf: 0.97
    if:
      type: text
      regex: '(?m)^\s*unset\s+two-factor\s*$'
