defaults:
  platforms: [windows]
  for: [go]
traits:
  - id: go-netsh-wifi-disable
    desc: Disables wireless network connection via netsh
    crit: suspicious
    conf: 0.9
    if:
      type: string_value
      exact: 'netsh interface set interface name="Wireless Network Connection" admin=DISABLED'
