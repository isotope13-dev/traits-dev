defaults:
  for: [all]

traits:
  - id: delete-critical-system-dll
    desc: "Deletes critical Windows system DLLs"
    crit: suspicious
    conf: 0.9
    mbc: E1485
    attack: T1485
    if:
      type: string
      regex: "(?i)unlink\\s*\\([\"']C:\\\\Windows(\\\\System)?\\\\[a-zA-Z0-9]+\\.dll[\"']\\)"