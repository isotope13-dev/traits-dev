defaults:
  platforms:
    - windows
  for:
    - pe
    - dll
    - vbs
traits:
  - id: sfc-avoidance-comment
    desc: Skips files protected by SFC
    crit: suspicious
    conf: 0.95
    for:
      - c
    if:
      type: string_literal
      substr: no infect files protected by SFC
