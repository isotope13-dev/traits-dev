defaults:
  platforms:
    - unix
    - windows
  for: [scripts, binaries]
traits:
  - id: unprofessional-error-marker
    desc: Unprofessional or aggressive error message
    crit: notable
    conf: 0.6
    if:
      type: text
      regex: (?i)(motherfucking|bastard|shit|fuck(ing)?).?error
