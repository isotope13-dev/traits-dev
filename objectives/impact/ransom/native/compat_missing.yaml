traits:
  - id: file-targeting
    desc: compat placeholder
    crit: component
    conf: 0.1
    if:
      type: string
      exact: __cleave_missing_cd1b472cae84__
defaults:
  crit: component
  conf: 0.1
  for:
    - all
