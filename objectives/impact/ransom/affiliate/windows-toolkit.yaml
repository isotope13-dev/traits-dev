# SoftPerfect Network Scanner recon paired with Remotely RMM install and
# s5cmd S3 archive upload — the post-compromise toolkit cluster reused by
# ransomware affiliates across RaaS brands.
defaults:
  platforms: [windows]
  for: [powershell, batch, scripts]
  attack: T1046,T1219,T1567.002

composite_rules:
  - id: softperfect-remotely-s5cmd-playbook
    desc: SoftPerfect recon with Remotely RMM and s5cmd S3 upload
    crit: hostile
    conf: 0.98
    all:
      - id: objectives/discovery/network/scan/utility::softperfect-network-scanner-product
      - id: objectives/collection/archive/upload::s5cmd-s3-upload-command
    any:
      - id: objectives/command-and-control/backdoor/control/rmm::remotely-irm-clientdownloads-bootstrap
      - id: objectives/command-and-control/backdoor/control/rmm::remotely-agent-zip-service-install
    needs: 1
