defaults:
  platforms: [unix, windows]
  for: [documents, scripts, source, manifests]
  attack: T1485

composite_rules:
  - id: llm-override-destructive-file-deletion
    desc: LLM prompt override requests destructive file deletion
    crit: hostile
    conf: 0.97
    near_bytes: 320
    all:
      - id: micro-behaviors/data/text/llm/override::llm-prior-instruction-override
    any:
      - id: objectives/execution/lure/shell::paste-run-destructive-shell
      - id: micro-behaviors/data/text/keywords/destructive::destructive-code-instruction
    unless:
      - id: micro-behaviors/data/text/llm/scanner::llm-scanner-context
      - id: micro-behaviors/data/text/security/scanner::defensive-threat-signatures

  - id: concealed-llm-override-destructive-file-deletion
    desc: Concealed LLM prompt override requests destructive file deletion
    crit: hostile
    conf: 0.98
    near_bytes: 420
    all:
      - id: llm-override-destructive-file-deletion
      - id: micro-behaviors/ui/terminal::ansi-output-erase
