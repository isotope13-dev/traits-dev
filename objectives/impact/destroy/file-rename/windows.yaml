# Destruction-by-rename: targets are retargeted to a dead extension so they no
# longer launch, while their bytes stay in place. The embedded `cmd /c rename`
# command supplies the technique; a shell-creation capability in the same
# binary supplies the execution. Seen in VB6 worms renaming .exe files to .die.
defaults:
  platforms: [windows]
  for: [pe, dll]
  attack: T1565.001

traits:
  - id: dos-comspec-name
    desc: Names the COMSPEC command interpreter
    crit: suspicious
    conf: 0.9
    for: [dos_com, static-lib]
    if:
      type: hex
      pattern: "43 4F 4D 53 50 45 43"

  - id: dos-cmd-rename-switch
    desc: Command switch renames a file
    crit: suspicious
    conf: 0.93
    for: [dos_com, static-lib]
    if:
      type: hex
      pattern: "2F 43 20 52 45 4E 41 4D 45"

composite_rules:
  - id: dos-comspec-rename-files
    desc: COMSPEC command renames files
    crit: hostile
    conf: 0.94
    for: [dos_com, static-lib]
    all:
      - id: dos-comspec-name
      - id: dos-cmd-rename-switch

  - id: shell-rename-to-die-extension
    desc: Executes shell commands renaming files to .die
    crit: hostile
    conf: 0.97
    all:
      - id: micro-behaviors/fs/file/rename::cmd-target-die-extension-rename
      - id: micro-behaviors/process/create/shell
