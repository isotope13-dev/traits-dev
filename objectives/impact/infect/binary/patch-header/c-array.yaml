defaults:
  platforms:
  - unix
  for:
  - c
  - cpp
traits:
- id: original-entry-patch-offset
  desc: Declares a parasite original-entry patch offset
  crit: notable
  conf: 0.95
  if:
    type: text
    word: PARASITE_ORIG_ENTRY_QUAD_OFFSET
composite_rules:
- id: embedded-entry-return-parasite
  desc: C array carries an original-entry return payload
  crit: suspicious
  conf: 0.96
  all:
  - id: original-entry-patch-offset
  - id: micro-behaviors/data/control-flow/transfer::c-array-rebased-entry-jump
