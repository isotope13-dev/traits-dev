defaults:
  platforms: [windows, unix]
  for: [ruby]
traits:
  - id: ocra-executable-const
    desc: Detects artifacts of OCRA (One-Click Ruby Application)
    crit: notable
    if:
      type: text
      word: OCRA_EXECUTABLE
  - id: ocra-stub-marker
    desc: Detects artifacts of OCRA (One-Click Ruby Application)
    crit: notable
    if:
      type: text
      word: ocrastub
