defaults:
  platforms: [windows]
  for: [binaries]

traits:
  - id: many-functions-dotnet-profile
    desc: Compatibility placeholder
    crit: component
    conf: 1.0
    if:
      type: text
      exact: __cleave_missing_many_functions_dotnet_profile__
