defaults:
  platforms: [netscaler, appliance, unix]
  for: [typescript, javascript]
  attack: T1027

traits:
  - id: node-decode-payload-import
    desc: Node decodePayload import
    crit: suspicious
    conf: 0.92
    if:
      type: text
      substr: decodePayload

  - id: node-packed-payload-log
    desc: Node packed payload log
    crit: suspicious
    conf: 0.92
    if:
      type: text
      substr: "[pack] payload decoded"

  - id: node-unpacked-size-use
    desc: Node unpacked size use
    crit: suspicious
    conf: 0.9
    if:
      type: text
      substr: unpacked.length

composite_rules:
  - id: node-packed-payload-execution
    desc: Node packed payload execution
    crit: hostile
    conf: 0.98
    all:
      - id: node-decode-payload-import
      - id: node-packed-payload-log
    any:
      - id: node-unpacked-size-use
