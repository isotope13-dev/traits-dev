defaults:
  platforms: [windows, unix]
  for: [javascript, typescript]
traits:
  - id: hex-command-regex
    desc: Extract hex commands from headers
    crit: suspicious
    conf: 0.9
    if:
      type: text
      regex: 'g\(\[a-f0-9\]\{4\}\)h\(\(\?:\[a-f0-9\]\{2\}\)\+\)i'
