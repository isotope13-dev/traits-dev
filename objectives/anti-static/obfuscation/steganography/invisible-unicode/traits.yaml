# Override: require at least 2 invisible chars for suspicious
# A single invisible char from a UTF-8 BOM is normal, not steganography.
defaults:
  for: [scripts, source]
  platforms: [linux, macos, windows]

traits:
  - id: multiple
    desc: Multiple invisible Unicode characters in source
    crit: suspicious
    conf: 0.95
    attack: "T1027"
    if:
      type: metrics
      field: "text.invisible_chars"
      min: 2
