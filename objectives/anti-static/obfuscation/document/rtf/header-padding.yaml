defaults:
  platforms: [windows, unix]
  for: [scripts, binaries]
traits:
  - id: header-padding
    desc: RTF excessive header padding.
    crit: suspicious
    conf: 0.9
    mbc: B0032.001
    if:
      type: raw
      regex: \{\\rtf[\s]{500,}
