defaults:
  platforms: [linux, macos, windows, unix, android, ios]
  for: [rtf]
traits:
  - id: control-symbol-injection
    desc: RTF control symbol injection within hex.
    crit: suspicious
    conf: 0.9
    if:
      type: raw
      regex: "((\\\\\\+){1,2}[0-9a-fA-F]\\s*){4,}"
