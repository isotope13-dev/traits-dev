defaults:
  platforms: [windows, unix]
  for: [scripts, binaries]
traits:
  - id: nested-objdata-in-data
    desc: Nested \objdata tag inside hex stream.
    crit: suspicious
    conf: 0.9
    mbc: B0032.001
    if:
      type: text
      regex: (?i)[0-9a-fA-F]\s*\{\\\*\\objdata\s+
