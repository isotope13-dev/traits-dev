defaults:
  platforms: [android]
  for: [zip]
  crit: suspicious
  conf: 0.92

traits:
  - id: android-raw-audio-com-resource
    desc: Android raw audio masked COM
    if:
      type: raw
      regex: 'res/raw/[A-Za-z0-9_]{3,32}\.com[\s\S]{0,256}ID3'
