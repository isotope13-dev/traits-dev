defaults:
  crit: notable
  conf: 0.8
  for: [pe, dll, csharp]
  platforms: [windows]
traits:
  - id: vscrollbar-spam
    desc: "A large number of VScrollBar strings"
    count_min: 10
    if:
      type: text
      regex: "VScrollBar[0-9]+"
