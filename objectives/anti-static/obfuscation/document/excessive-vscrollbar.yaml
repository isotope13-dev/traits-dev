defaults:
  platforms:
    - unix
    - windows
    - macos
  for: [scripts, binaries]
traits:
  - id: excessive-vscrollbar
    desc: Excessive VScrollBar controls for UI deception.
    crit: suspicious
    count_min: 10
    if:
      type: string_value
      regex: VScrollBar\d+
