
defaults:
  crit: notable
  conf: 0.8
  for: [pe, dll, csharp]
  platforms: [windows]

traits:
  - id: vscrollbar-spam
    desc: "A large number of VScrollBar strings"
    if:
      type: string_count
      regex: "VScrollBar[0-9]+"
      min: 10
