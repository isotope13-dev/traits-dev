defaults:
  platforms:
    - linux
    - macos
    - windows
    - unix
    - android
    - ios
  for: [scripts, binaries]
traits:
  - id: nested-math-pow
    desc: Detects excessive Math.pow() calls (often used in
    crit: notable
    count_min: 6
    if:
      type: raw
      substr: Math.pow
