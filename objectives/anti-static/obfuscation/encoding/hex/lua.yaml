defaults:
  platforms: [windows, unix, appliance]
  for: [lua]
  attack: T1027

traits:
  - id: lua-long-hex-string-table
    desc: Lua long hex string table
    crit: notable
    conf: 0.94
    count_min: 6
    if:
      type: literal
      regex: '^[0-9a-f]{8,}$'
