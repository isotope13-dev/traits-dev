defaults:
  for:
  - all
traits:
- id: return-this-prototype
  desc: Detects heavy prototype function manipulation
  crit: notable
  conf: 0.7
  count_min: 20
  if:
    type: raw
    regex: \.prototype\.[a-zA-Z0-9_$]+
- id: return-this-body
  desc: Detects function returning this
  crit: component
  conf: 0.7
  if:
    type: raw
    regex: function\s*\(\s*\)\s*\{[^}]{0,80}return\s+this\s*[;}]
composite_rules:
- id: return-this
  desc: Detects prototype function that returns this
  crit: notable
  conf: 0.75
  all:
  - id: return-this-prototype
  - id: return-this-body
