defaults:
  platforms: [linux, macos, windows, unix, android, ios]
  for: [javascript, typescript]
traits:
  - id: comment-fragment-array
    desc: "Nemucod FE-delimited comment fragment array"
    crit: suspicious
    conf: 0.95
    size_min: 1000
    size_max: 500000
    count_min: 100
    if:
      type: raw
      substr: "/* */'\\xfe'"
  - id: delimiter-split
    desc: "Nemucod FE delimiter split stage"
    crit: suspicious
    conf: 0.95
    size_min: 1000
    size_max: 500000
    if:
      type: raw
      substr: 'b = b.split("\xfe");'
  - id: length-gated-eval
    desc: "Nemucod length gated eval trigger"
    crit: suspicious
    conf: 0.97
    size_min: 1000
    size_max: 500000
    if:
      type: raw
      substr: 'if (c["length"] >= 12) f(c);'
composite_rules:
  - id: array-rebuild
    desc: "Nemucod FE delimiter rebuild and eval"
    crit: suspicious
    conf: 0.98
    all:
      - id: micro-behaviors/process/interpreter/scripting/jscript::jscript-cc-eval
      - id: comment-fragment-array
      - id: delimiter-split
      - id: length-gated-eval
