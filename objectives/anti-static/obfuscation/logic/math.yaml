traits:
  - id: complex-number-obfuscation
    desc: "Uses large complex number logic"
    crit: suspicious
    conf: 0.8
    for: [python]
    if:
      type: raw
      # Matches (1234.56 + 7890.12j) pattern with large numbers
      regex: '\(\s*\d{3,}\.\d+\s*\+\s*\d{3,}\.\d+j\s*\)'
