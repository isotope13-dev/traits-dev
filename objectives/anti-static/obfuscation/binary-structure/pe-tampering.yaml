defaults:
  for: [pe, dll]
  platforms: [windows]
traits:
  - id: corrupted-header
    desc: Malformed PE header structure
    crit: suspicious
    conf: 0.9
    if:
      type: structure
      feature: binary/format/pe
    downgrade:
      any:
        - id: metadata/package::versioned
