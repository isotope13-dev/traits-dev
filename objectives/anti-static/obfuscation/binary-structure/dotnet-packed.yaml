defaults:
  platforms: [windows]
  for: [pe]
composite_rules:
  - id: dotnet-oversized-packed-text
    desc: Packed .NET high-entropy text
    crit: suspicious
    conf: 0.9
    all:
      - id: metadata/binary/framework::dotnet-metadata
      - id: metadata/binary/section/metrics::oversized-high-entropy-text-section
