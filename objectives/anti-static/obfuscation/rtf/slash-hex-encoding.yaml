defaults:
  platforms: [linux, macos, windows, unix, android, ios]
  for: [rtf]
traits:
  - id: slash-hex-encoding
    desc: RTF slash-hex encoding pattern.
    crit: suspicious
    conf: 0.9
    if:
      type: raw
      regex: "(\\\\'2F[0-9a-fA-F]{2}\\s*){4,}"
