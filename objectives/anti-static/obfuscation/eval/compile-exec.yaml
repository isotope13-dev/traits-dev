# Python compile() + exec() Obfuscation Patterns
# Detects dynamic code compilation with synthetic filenames and immediate execution.
# Used by Python packers (PyOBFUSCATE, ShadowCoil) to hide payload origin.
# ATT&CK: T1027 (Obfuscated Files or Information)
defaults:
  platforms: [unix, windows]
  for: [python]
  attack: T1027

traits:
  - id: compile-synthetic-filename
    desc: compile() with synthetic filename argument
    crit: suspicious
    conf: 0.94
    mbc: B0032
    if:
      type: text
      regex: '(^|[^.a-zA-Z0-9_])compile\(.{0,80}''<[A-Za-z0-9_]{4,}'
    unless:
      - id: metadata/package/testing/presence::python-test-file
      - id: metadata/package/testing/presence::test-directory
      - type: text
        substr: "PythonParserGenerator"
      - type: text
        regex: 'compile\(.{0,80}''<(string|stdin|unknown|docs|code|doctest [^''">]{0,80})>'''

  - id: compile-exec-chain
    desc: compile() followed by exec() in script
    crit: suspicious
    conf: 0.92
    if:
      type: text
      regex: '(^|[^A-Za-z0-9_])_code\s*=\s*compile\('
    unless:
      - type: text
        substr: "template"
      - type: text
        substr: "TempliteCompiler"
      - type: text
        substr: "cpython.run_startup"

composite_rules:
  - id: compile-exec-obfuscation
    desc: Dynamic compile-then-exec obfuscation
    crit: suspicious
    conf: 0.94
    all:
      - id: micro-behaviors/process/interpreter/python::compile-builtin
      - id: micro-behaviors/process/interpreter/python::exec-builtin
    any:
      - id: compile-synthetic-filename
      - id: compile-exec-chain
    unless:
      - id: metadata/package/testing/presence::python-test-file
      - id: metadata/package/testing/presence::test-directory
