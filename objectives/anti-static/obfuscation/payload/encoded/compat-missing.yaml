defaults:
  platforms: [unix, windows]
  for: [binaries, scripts]

traits:
  - id: giant-string-blob
    desc: Compatibility alias
    crit: component
    conf: 1.0
    if:
      type: trait
      id: objectives/impact/wipe/disk/overwrite::giant-string-blob
