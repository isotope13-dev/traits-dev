defaults:
  for: [data]
  platforms: [windows, linux, macos]

traits:
  - id: base64-test-vector-filename
    desc: Base64 parser test vector filename
    crit: component
    conf: 0.95
    if:
      type: basename
      regex: '^def_.*b64.*\.txt$'
