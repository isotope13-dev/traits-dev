defaults:
  for:
    - pe
  platforms:
    - windows
  attack: T1027
  mbc: B0032
traits:
  # Generic PE sparsity atom: the matcher counts strings, it does not establish
  # a VB6 toolchain. VB6 context comes from the vb6-application-framework leg
  # of sparse-vb6-loader-profile below, not from this count.
  - id: low-string-count
    desc: PE binary with very few strings
    crit: notable
    conf: 0.9
    if:
      type: metrics
      field: binary.string_count
      max: 40
    unless:
      - id: metadata/lang/compiler/systems::go-pe-binary-context
  - id: dominant-text-layout-vb6
    desc: Dominant section for VB6 loader profile
    crit: component
    conf: 0.86
    if:
      id: metadata/binary/section/ratio::dominant-section
    unless:
      - id: metadata/binary/framework/
      - type: metrics
        field: pe.has_rich_header
        min: 1
      - type: metrics
        field: binary.largest_section_ratio
        max: 0
      - type: basename
        regex: (^vmlinuz$|\.(ko(\.zst)?|winmd|mun|rll)$)
      - id: metadata/binary/resource/
      - id: metadata/binary/symbols/plugin::ollydbg-plugin-export
composite_rules:
  - id: sparse-vb6-loader-profile
    desc: String-sparse VB6 loader profile
    crit: suspicious
    conf: 0.95
    all:
      - id: metadata/binary/framework::vb6-application-framework
      - id: low-string-count
      - id: dominant-text-layout-vb6
      - id: metadata/binary/section/header::pe-three-sections
