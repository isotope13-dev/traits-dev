defaults:
  platforms: [windows]
  for: [pe]
  crit: suspicious

traits:
  - id: large-high-entropy-rsrc
    desc: Large PE resource section with high entropy
    crit: baseline
    if:
      type: section
      exact: ".rsrc"
      length_min: 140000
      entropy_min: 6.5

composite_rules:
  - id: pe-rsrc-dropper-heuristic
    desc: Obfuscated high-entropy resource payload extracted dynamically
    all:
      - id: large-high-entropy-rsrc
      - id: metadata/binary/layout::has-overlay
      - id: micro-behaviors/data/embedded/payload/resource::pe-resource-full-extraction
      - id: objectives/anti-analysis/debugger-detect/check::debugger-check-with-guards
