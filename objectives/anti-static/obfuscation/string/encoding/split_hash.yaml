defaults:
  platforms: [windows, unix]
traits:
  - id: js-split-hash-join
    desc: 'String deobfuscation using split and join'
    crit: suspicious
    conf: 0.8
    for: [javascript]
    if:
      type: text
      regex: 'split\(\s*["'']##["'']\s*\).{0,50}join\(\s*["'']{2}\s*\)'
