traits:
  - id: string-concealment
    desc: compat placeholder
    crit: baseline
    conf: 0.1
    if:
      type: string_value
      exact: __cleave_missing_74d3ca65a278__
defaults:
  platforms: [unix, windows, macos]
  crit: component
  conf: 0.1
  for: [scripts, binaries]
