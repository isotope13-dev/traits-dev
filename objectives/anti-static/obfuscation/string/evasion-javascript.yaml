defaults:
  platforms: [windows, unix]
  for: [javascript, typescript]
traits:
  - id: js-replace-gibberish
    desc: "Detects the specific string replacement pattern used"
    crit: suspicious
    if:
      type: text
      regex: 'replace(/[a-zA-Z0-9]{10,}/g, "")'
