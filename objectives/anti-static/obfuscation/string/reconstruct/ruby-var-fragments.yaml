defaults:
  platforms: [windows, unix]
traits:
  - id: ruby-var-fragmented-strings
    desc: Fragmented URL protocol variables (d="ht", aa="tps:/")
    crit: suspicious
    conf: 0.9
    for: [ruby]
    if:
      type: text
      regex: '(d\s*=\s*"ht"|aa\s*=\s*"tps:/")'
