defaults:
  platforms: [windows, unix]
  for: [scripts, binaries]
traits:
  - id: weird-function-syntax
    desc: "weird non-standard func decl syntax (func"
    crit: suspicious
    conf: 0.95
    attack: T1027
    if:
      type: text
      regex: function\s+[a-zA-Z0-9_$]+\.prototype\.[a-zA-Z0-9_$]+\s*\(
