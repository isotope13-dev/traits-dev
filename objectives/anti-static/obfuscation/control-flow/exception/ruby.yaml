defaults:
  platforms: [windows, unix]
traits:
  - id: exception-swallower
    desc: Swallows all exceptions silently
    crit: suspicious
    conf: 0.8
    for: [ruby]
    mbc: "B0032"
    if:
      type: text
      regex: 'rescue\s+Exception(\s*end|;)'
