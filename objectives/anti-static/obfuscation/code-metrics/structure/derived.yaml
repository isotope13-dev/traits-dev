defaults:
  platforms: [unix, windows, macos]
  crit: notable
  conf: 1.0
  for: [scripts, binaries]
traits:
  - id: minified-code__unless_1
    desc: Internal html basename guard
    crit: component
    if:
      type: basename
      regex: \.html?$
