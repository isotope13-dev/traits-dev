defaults:
  platforms: [unix, windows]

composite_rules:
  - id: excessive-line-length-protobuf-text
    desc: Downgrade protobuf text line length
    crit: baseline
    conf: 0.45
    all:
      - id: objectives/anti-static/obfuscation/code-metrics/line-length/
    downgrade:
      any:
        - type: basename
          regex: \.pbtxt(\.gz)?$
