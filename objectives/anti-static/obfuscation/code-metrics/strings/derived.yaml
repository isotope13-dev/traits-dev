defaults:
  platforms:
    - unix
    - windows
    - macos
  crit: notable
  conf: 1.0
  for: [scripts, binaries]
traits:
  - id: string-hiding__unless_1
    desc: Derived raw condition
    if:
      type: raw
      regex: \bsw(eave|ig)\b
