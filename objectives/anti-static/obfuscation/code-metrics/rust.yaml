# Rust-Specific Metrics-Based Obfuscation Detection
# Detects obfuscation patterns in Rust source code via metrics analysis
defaults:
  platforms: [windows, unix]
  for: [rust]
  attack: "T1027"
  mbc: "B0032"
traits:
  - id: rust-many-high-entropy-identifiers
    desc: "Many high-entropy identifiers in Rust"
    crit: notable
    conf: 0.9
    # Small binaries with high entropy identifiers are more suspicious
    size_min: 4096
    size_max: 500000
    if:
      type: metrics
      field: identifiers.high_entropy_ratio
      min: 0.35
    unless:
      # SIMD intrinsic headers often contain architectural feature names that trigger high entropy
      - type: text
        substr: "relaxed_simd"
      # Rust benchmark files
      - type: text
        substr: "#[bench]"
      # Runtime CPU feature detection macros
      - type: text
        substr: "_feature_detected!"
      # Large generated Unicode tables use dense symbolic names without indicating obfuscation
      - type: text
        regex: generated by "scripts/unicode\.py"|Autogenerated\. .*sub-table
