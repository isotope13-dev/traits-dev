# Evasive Module Aliasing Detection
# Multiple sensitive stdlib modules aliased together is a deliberate-obscuring
# signal. The neutral per-module "imported under an alias" observations are
# generic capabilities and live in micro-behaviors/data/source/import/
# (a single aliased import is normal Python); this composite combines them.
defaults:
  crit: notable
  conf: 0.7
  platforms: [windows, unix]
  for: [python]
composite_rules:
  - id: evasive-module-aliasing
    desc: Multiple sensitive stdlib imports aliased
    needs: 3
    any:
      - id: micro-behaviors/data/source/import::alias-os
      - id: micro-behaviors/data/source/import::alias-subprocess
      - id: micro-behaviors/data/source/import::alias-base64
  - id: evasive-module-aliasing-recon
    desc: Multiple recon stdlib imports aliased
    needs: 3
    any:
      - id: micro-behaviors/data/source/import::alias-platform
      - id: micro-behaviors/data/source/import::alias-getpass
      - id: micro-behaviors/data/source/import::alias-urllib
