defaults:
  platforms: [unix, windows]
  for: [python]
  attack: T1027

traits:
  - id: environment-slice-hash-key
    desc: Hashes an env slice into key material
    crit: suspicious
    conf: 0.92
    if:
      type: text
      regex: 'hashlib\.(sha[0-9]+|sha3_[0-9]+)\s*\(\s*os\.environ\[[^]]{1,32}\]\[[^]]{1,16}\]'
