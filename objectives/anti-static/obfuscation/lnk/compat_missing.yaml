traits:
  - id: whitespace-hiding
    desc: compat placeholder
    crit: component
    conf: 0.1
    if:
      type: string
      exact: __cleave_missing_0e6f250da43f__
defaults:
  crit: component
  conf: 0.1
  for:
    - all
