defaults:
  platforms: [openbsd, bsd, unix]
  for: [objc, elf]
  attack: T1110.001

traits:
  - id: objc-ssh-brute-force-call
    desc: ssh_brute_force routine symbol
    crit: suspicious
    conf: 0.94
    if:
      type: text
      substr: ssh_brute_force

  - id: objc-ssh-password-auth-line
    desc: SSH userauth-password format string
    crit: notable
    conf: 0.9
    if:
      type: text
      substr: userauth-password %s %s

  - id: objc-openbsd-ssh-client-banner
    desc: OpenSSH 9.4 client version banner
    crit: notable
    conf: 0.88
    if:
      type: text
      substr: SSH-2.0-OpenSSH_9.4

  - id: objc-openbsd-default-passwords
    desc: admin/root/letmein password wordlist
    crit: suspicious
    conf: 0.92
    if:
      type: raw
      regex: 'password[\s\S]{0,120}admin[\s\S]{0,120}root[\s\S]{0,120}letmein'

composite_rules:
  - id: objc-openbsd-ssh-brute-force
    desc: ObjC SSH brute force
    crit: hostile
    conf: 0.98
    all:
      - id: objc-ssh-brute-force-call
      - id: objc-ssh-password-auth-line
      - id: objc-openbsd-ssh-client-banner

  - id: objc-openbsd-ssh-password-spray
    desc: ObjC SSH password spray
    crit: hostile
    conf: 0.98
    all:
      - id: objc-openbsd-ssh-client-banner
      - id: objc-ssh-password-auth-line
      - id: objc-openbsd-default-passwords
