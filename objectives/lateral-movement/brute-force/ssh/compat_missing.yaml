traits:
  - id: scanner
    desc: compat placeholder
    crit: component
    conf: 0.1
    if:
      type: string
      exact: __cleave_missing_80f20e1469d9__
defaults:
  crit: component
  conf: 0.1
  for:
    - all
