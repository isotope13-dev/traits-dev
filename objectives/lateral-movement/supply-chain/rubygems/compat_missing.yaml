traits:
  - id: http-client-imports
    desc: compat placeholder
    crit: component
    conf: 0.1
    if:
      type: string
      exact: __cleave_missing_503667c8cb49__
defaults:
  platforms: [unix, windows, macos]
  crit: component
  conf: 0.1
  for: [ruby]
