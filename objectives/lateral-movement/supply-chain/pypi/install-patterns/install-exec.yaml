defaults:
  crit: component
  conf: 1.0
  for:
    - all
traits:
  - id: setup-py-exec-yara-unless-scipy
    desc: Derived raw condition for scipy
    if:
      type: raw
      word: scipy
  - id: setup-py-exec-yara-unless-numpy
    desc: Derived raw condition for numpy
    if:
      type: raw
      word: numpy
  - id: setup-py-exec-yara-unless-docutils
    desc: Derived raw condition for docutils
    if:
      type: raw
      word: docutils
composite_rules:
  - id: setup-py-exec-yara__unless_1
    desc: Derived raw condition
    crit: notable
    conf: 1.0
    for: [all]
    any:
      - id: setup-py-exec-yara-unless-scipy
      - id: setup-py-exec-yara-unless-numpy
      - id: setup-py-exec-yara-unless-docutils
