defaults:
  platforms: [windows]
  for: [go]
  attack: T1542.001

composite_rules:
  - id: go-uefi-variable-persistence
    desc: Go UEFI variable persistence
    crit: hostile
    conf: 0.98
    all:
      - id: micro-behaviors/os/kernel/routines::go-set-firmware-env-var
      - id: micro-behaviors/data/format/string::guid-formatted-literal
      - id: micro-behaviors/data/parse/keywords::persistence-keyword-literal
