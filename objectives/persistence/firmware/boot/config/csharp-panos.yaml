defaults:
  platforms: [panos, appliance, windows]
  for: [csharp, pe]
  attack: T1542

traits:
  - id: csharp-panos-boot-config-path
    desc: PAN-OS rc.boot.local boot persistence file
    crit: suspicious
    conf: 0.94
    if:
      type: text
      substr: rc.boot.local



