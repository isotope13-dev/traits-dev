defaults:
  platforms: [unix]
  for: [scripts]
  attack: T1053.003

traits:
  - id: unix-root-crontab-path
    desc: Unix root crontab path
    crit: suspicious
    conf: 0.86
    if:
      type: text
      substr: /var/spool/cron/crontabs/root
