defaults:
  platforms: [unix]
  for: [lua]
  attack: T1053.003
composite_rules:
  - id: lua-crontab-persistence
    desc: Lua script installs a cron job for persistence
    crit: hostile
    conf: 0.95
    all:
      - id: micro-behaviors/process/create/shell/invoke::lua-os-execute
      - id: micro-behaviors/data/text/keywords/lexicon::cron-dup-aix
