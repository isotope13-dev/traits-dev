defaults:
  platforms: [unix]
  for: [lua]
  attack: T1053.003
composite_rules:
  - id: lua-crontab-persistence
    desc: Lua script installs a cron job for persistence
    crit: hostile
    conf: 0.95
    all:
      - id: micro-behaviors/process/create/shell/spawn::lua-os-execute
      - id: micro-behaviors/time/schedule/keywords::cron-keyword
