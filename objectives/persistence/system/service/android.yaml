defaults:
  platforms: [android]
  for: [apk, zip]

composite_rules:
  - id: android-device-admin-accessibility
    desc: Android device admin accessibility control
    crit: suspicious
    conf: 0.94
    all:
      - id: micro-behaviors/os/privilege/manifest::device-admin-policy-resource
      - id: micro-behaviors/hardware/input/accessibility::android-accessibility-service-config-resource
