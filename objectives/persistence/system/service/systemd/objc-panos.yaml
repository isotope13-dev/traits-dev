defaults:
  platforms: [panos, appliance, unix]
  for: [objc, c, elf]
  attack: T1543.002

traits:
  - id: objc-panos-cron-payload-install
    desc: ObjC PAN-OS cron payload install
    crit: suspicious
    conf: 0.94
    if:
      type: text
      substr: crontab /tmp/.cron_payload


