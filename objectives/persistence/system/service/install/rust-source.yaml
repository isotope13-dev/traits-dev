defaults:
  for: [pe, elf, macho]
  platforms: [windows, linux, macos]
  attack: T1543
  mbc: B0011

traits:
  - id: rust-persistence-source-module
    desc: Rust source path for persistence module
    crit: notable
    conf: 0.82
    if:
      type: text
      regex: src[\\/]persist(ence)?\.rs
