defaults:
  platforms: [windows]
  for: [binaries]

traits:
  - id: adobe-arm-helper-name
    desc: Compatibility placeholder
    crit: component
    conf: 1.0
    if:
      type: text
      exact: __cleave_missing_adobe_arm_helper_name__
  - id: lanmanserver-service-dll-hijack
    desc: Compatibility placeholder
    crit: component
    conf: 1.0
    if:
      type: text
      exact: __cleave_missing_lanmanserver_service_dll_hijack__
