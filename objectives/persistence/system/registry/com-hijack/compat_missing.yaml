defaults:
  platforms: [windows]
  for: [binaries, scripts]

traits:
  - id: inprocserver32-path
    desc: Compatibility alias
    crit: component
    conf: 1.0
    if:
      id: objectives/command-and-control/dropper/execution/inject::objectives/persistence/system/registry/com-hijack::inprocserver32-path
