defaults:
  platforms: [linux]
  for: [elf, rust, shell]
  crit: suspicious
  conf: 0.9
traits:
  - id: elf-backup-dir
    desc: Reference to ELF backup directory
    if:
      type: text
      substr: "elf_backup"
  - id: elf-work-dir
    desc: Reference to ELF working directory
    if:
      type: text
      substr: "elf_workdir"
composite_rules:
  - id: linux-binary-infection-orchestrator
    desc: Orchestrates ELF binary infection for persistence
    crit: hostile
    conf: 0.9
    attack: T1574.001
    all:
      - id: micro-behaviors/process/create/tool::cp-invocation
      - id: micro-behaviors/process/create/tool::mkdir-invocation
    any:
      - id: elf-backup-dir
      - id: elf-work-dir
      - id: micro-behaviors/process/create/tool::readelf-invocation
      - id: micro-behaviors/process/create/tool::md5sum-invocation
