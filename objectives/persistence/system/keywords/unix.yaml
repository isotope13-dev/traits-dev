# Unix/Linux/macOS Persistence Location Keywords
# Atomic traits moved to micro-behaviors/os/autorun/keywords.yaml
defaults:
  platforms: [unix, windows, macos]
  for: [elf, shell, python]
composite_rules:
  - id: unix-persistence-keywords
    desc: Unix persistence location keywords
    crit: notable
    conf: 0.7
    any:
      - id: micro-behaviors/os/autorun::startup-keyword
      - id: micro-behaviors/os/autorun::launchd-keyword
