# EnvironmentVariables and WorkingDirectory.
defaults:
  platforms: [macos]
  for: [plist]
traits:
  - id: env-vars-dup
    desc: EnvironmentVariables key
    crit: baseline
    conf: 0.8
    if:
      type: value
      path: EnvironmentVariables
  - id: working-directory
    desc: WorkingDirectory key
    crit: baseline
    conf: 0.8
    if:
      type: value
      path: WorkingDirectory
