# LaunchEvents, WatchPaths and other triggers.
defaults:
  platforms: [macos]
  for: [plist]
traits:
  - id: launch-events
    desc: LaunchEvents key (event-based activation)
    crit: baseline
    conf: 0.8
    if:
      type: value
      path: LaunchEvents
