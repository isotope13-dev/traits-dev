defaults:
  platforms: [netscaler, appliance, unix]
  for: [ruby]
  attack: T1543.002

traits:
  - id: ruby-netscaler-agent-service
    desc: Ruby NetScaler agent service
    crit: suspicious
    conf: 0.92
    if:
      type: text
      substr: netscaler-agent.service

  - id: ruby-systemd-service-write
    desc: Ruby systemd service write
    crit: suspicious
    conf: 0.92
    if:
      type: text
      substr: /etc/systemd/system/netscaler-agent.service

  - id: ruby-systemctl-enable-start
    desc: Ruby systemctl enable start
    crit: suspicious
    conf: 0.92
    if:
      type: text
      regex: 'systemctl enable[\s\S]{0,160}systemctl start'

composite_rules:
  - id: ruby-netscaler-systemd-persistence
    desc: Ruby NetScaler systemd persistence
    crit: hostile
    conf: 0.98
    all:
      - id: ruby-netscaler-agent-service
      - id: ruby-systemd-service-write
    any:
      - id: ruby-systemctl-enable-start
