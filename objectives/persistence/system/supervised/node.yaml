defaults:
  platforms: [windows, unix]
  for:
    - javascript
    - typescript
traits:
  - id: pm2-keyword
    desc: PM2 process manager keyword
    crit: component
    conf: 0.6
    if:
      type: string_literal
      exact: pm2
  - id: detached-option
    desc: Detached process option
    crit: component
    conf: 0.6
    if:
      type: string_literal
      substr: 'detached:'
  - id: pm2-spawn
    desc: Spawns PM2 process manager
    crit: notable
    conf: 0.85
    if:
      type: string_literal
      regex: spawn\(.{0,220}pm2
  - id: pm2-start
    desc: Starts process via PM2
    crit: notable
    conf: 0.85
    if:
      type: string_literal
      regex: pm2\s+start|exec\(.{0,220}pm2\s+start
  - id: pm2-startup-command
    desc: PM2 boot-startup configuration command
    crit: suspicious
    conf: 0.98
    if:
      type: text
      regex: 'run2\s*\(\s*pm2\s*,\s*\[\s*["'']startup["'']\s*\]'
  - id: pm2-autonomous-node-start
    desc: Starts PM2 autonomous agent process
    crit: suspicious
    conf: 0.99
    if:
      type: text
      regex: 'run2\s*\(\s*pm2\s*,\s*\[\s*["'']start["''][\s\S]{0,320}AGENT_HUB_AUTONODE_PM2'
  - id: forever-spawn
    desc: Uses forever process manager
    crit: notable
    conf: 0.8
    if:
      type: string_literal
      regex: forever\s+start|spawn\(.{0,220}forever
  - id: nodemon-spawn
    desc: Uses nodemon process manager
    crit: notable
    conf: 0.7
    if:
      type: string_literal
      regex: spawn\(.{0,220}nodemon
composite_rules:
  - id: pm2-detached
    desc: PM2 spawned in detached mode
    crit: suspicious
    conf: 0.9
    all:
      - id: pm2-keyword
      - id: micro-behaviors/process/create/shell/bridge::spawn-call
      - id: detached-option
