# One artifact that installs autostart on Windows (scheduled task / Run key),
# macOS (launchd LaunchAgent), and Linux (systemd user unit) in the same file
# is a cross-platform implant claiming persistence on every host it lands on.
# Legitimate software ships per-OS installers; nothing benign registers itself
# through all three low-level autostart surfaces from a single script. Each OS
# leg is an `any:` group so a variant that swaps schtasks for a Run key, or
# launchctl for a bare plist write, still matches. Same-file scope required.
defaults:
  platforms: [unix, windows]
  for: [javascript, typescript]
  attack: T1547,T1053,T1543
  mbc: F0012

composite_rules:
  - id: windows-autostart-surface
    desc: Registers a Windows autostart entry
    crit: notable
    conf: 0.85
    any:
      - id: micro-behaviors/os/autorun/scheduled::schtasks-utility-token
      - id: micro-behaviors/os/registry/keys::currentversion-run
      - id: micro-behaviors/os/registry/keys::run-value-name

  - id: macos-autostart-surface
    desc: Registers a macOS launchd autostart
    crit: notable
    conf: 0.85
    any:
      - id: micro-behaviors/os/service/start::launchctl-load-command-text
      - id: objectives/persistence/system/launchd/source::source-launchagent-load-via-node
      - id: micro-behaviors/os/service/launchagent::launchagent-run-at-load

  - id: linux-autostart-surface
    desc: Registers a Linux systemd autostart
    crit: notable
    conf: 0.85
    any:
      - id: objectives/persistence/system/service/systemd::source-systemctl-user-enable
      - id: objectives/persistence/system/service/systemd::source-systemctl-string

  - id: tri-os-autostart-installer
    desc: One file installs autostart on three OSes
    crit: hostile
    conf: 0.97
    scope: file
    all:
      - id: windows-autostart-surface
      - id: macos-autostart-surface
      - id: linux-autostart-surface
    unless:
      # Filename-convention test guards: the corpus itself sits under
      # testdata/, which the broader harness/runtime is-test context matches.
      - id: metadata/package/testing/presence/harness::test-directory
      - id: metadata/package/testing/presence/harness::test-directory-path
