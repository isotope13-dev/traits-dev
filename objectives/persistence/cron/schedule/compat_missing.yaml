traits:
  - id: base64-decode
    desc: compat placeholder
    crit: component
    conf: 0.1
    if:
      type: string
      exact: __cleave_missing_756e3a144230__
defaults:
  crit: component
  conf: 0.1
  for:
    - all
