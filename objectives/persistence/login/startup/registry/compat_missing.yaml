defaults:
  platforms: [windows]
  for: [scripts, binaries]

traits:
  - id: active-setup-stubpath
    desc: Compatibility placeholder
    crit: component
    conf: 1.0
    if:
      type: text
      exact: __cleave_missing_active_setup_stubpath__
  - id: active-setup-stubpath-persistence
    desc: Compatibility placeholder
    crit: component
    conf: 1.0
    if:
      type: text
      exact: __cleave_missing_active_setup_stubpath_persistence__
