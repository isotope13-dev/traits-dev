defaults:
  platforms:
    - unix
    - windows
  for:
    - java
    - class
traits:
  - id: java-startup-enum
    desc: Presence of STARTUP string in Java
    crit: suspicious
    if:
      type: text
      word: STARTUP
  - id: java-common-startup-enum
    desc: Presence of COMMON_STARTUP string in Java
    crit: suspicious
    if:
      type: text
      word: COMMON_STARTUP
