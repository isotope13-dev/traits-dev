defaults:
  platforms: [windows]
  for: [scripts, binaries]

traits:
  - id: vscode-dual-login-persistence
    desc: Compatibility placeholder
    crit: component
    conf: 1.0
    if:
      type: text
      exact: __cleave_missing_vscode_dual_login_persistence__
