defaults:
  platforms: [windows]
  for: [java, class, java_class, jar]
  attack: T1547.001

traits:
  - id: java-currentversion-run-path
    desc: Java CurrentVersion Run path
    crit: suspicious
    conf: 0.93
    if:
      type: text
      regex: 'CurrentVersion\\\\Run(Once)?'

  - id: java-reg-add-run-command
    desc: Java reg add Run command
    crit: suspicious
    conf: 0.93
    if:
      type: text
      regex: 'reg add[\s\S]{0,260}/t REG_SZ[\s\S]{0,260}/f'

  - id: jvm-currentversion-run-bytes
    desc: JVM CurrentVersion Run bytes
    crit: suspicious
    conf: 0.93
    for: [class, java_class, jar]
    if:
      type: hex
      pattern: 43 75 72 72 65 6E 74 56 65 72 73 69 6F 6E 5C 52 75 6E

  - id: jvm-reg-sz-bytes
    desc: JVM REG_SZ bytes
    crit: suspicious
    conf: 0.9
    for: [class, java_class, jar]
    if:
      type: hex
      pattern: 52 45 47 5F 53 5A

composite_rules:
  - id: java-run-key-persistence
    desc: Java Run key persistence
    crit: hostile
    conf: 0.98
    all:
      - id: java-currentversion-run-path
      - id: java-reg-add-run-command

  - id: jvm-run-key-persistence
    desc: JVM Run key persistence
    crit: hostile
    conf: 0.98
    for: [class, java_class, jar]
    all:
      - id: jvm-currentversion-run-bytes
      - id: jvm-reg-sz-bytes
      - id: micro-behaviors/process/create/spawn::java-processbuilder-init-bytecode-symbol
      - id: micro-behaviors/process/create/spawn::java-processbuilder-start-bytecode-symbol
