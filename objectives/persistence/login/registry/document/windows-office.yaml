defaults:
  platforms: [windows]
  for: [scripts, binaries]
  attack: T1137
traits:
  - id: office-addin-registry
    desc: Office add-in persistence
    crit: notable
    conf: 0.84
    if:
      type: text
      regex: '(?i)\\Microsoft\\Office\\[0-9.]+\\(Word|Excel|PowerPoint|Outlook)\\Addins\\|\\Office\\[^\\\n]{1,80}\\Addins\\|\\Outlook\\Addins\\'

  - id: outlook-form-region-registry
    desc: Outlook form region persistence
    crit: notable
    conf: 0.84
    if:
      type: text
      regex: '(?i)\\Outlook\\FormRegions\\|\\Office\\Outlook\\FormRegions\\'

composite_rules:
  - id: office-addin-registry-write
    desc: Office add-in registry write
    crit: suspicious
    conf: 0.88
    all:
      - id: micro-behaviors/os/registry/access::registry-write-api-chain
    any:
      - id: office-addin-registry
      - id: outlook-form-region-registry
    needs: 1
    downgrade:
      any:
        - id: metadata/signed/platform::microsoft
        - id: metadata/signed/platform::microsoft-corporation
