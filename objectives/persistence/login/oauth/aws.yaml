defaults:
  platforms: [unix, windows]
  for: [json]

composite_rules:
  - id: aws-identity-provider-persistence
    desc: AWS OIDC provider persistence changes
    crit: suspicious
    conf: 0.96
    all:
      - id: micro-behaviors/communications/http/services/aws::iam-create-openid-provider-operation
      - id: micro-behaviors/communications/http/services/aws::iam-add-openid-client-operation
