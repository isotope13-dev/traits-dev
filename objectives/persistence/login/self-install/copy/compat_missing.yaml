defaults:
  platforms: [windows]
  for: [scripts, binaries]

traits:
  - id: system32-messenger-self-install
    desc: Compatibility placeholder
    crit: component
    conf: 1.0
    if:
      type: text
      exact: __cleave_missing_system32_messenger_self_install__
