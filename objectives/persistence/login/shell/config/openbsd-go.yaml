defaults:
  platforms: [openbsd, bsd, unix]
  for: [go, elf]
  attack: T1546.004

traits:

  - id: go-shrc-persistence
    desc: Go shrc persistence path
    crit: suspicious
    conf: 0.88
    if:
      type: text
      regex: '(^|[/"''\s])\.shrc($|[/"''\s])'

