defaults:
  platforms: [netscaler, appliance, unix]
  for: [shell]
  attack: T1546.004

traits:

  - id: shell-login-rcfile-list
    desc: Shell login rcfile list
    crit: notable
    conf: 0.9
    if:
      type: text
      regex: '(\.bashrc|\.bash_profile|\.profile)[\s\S]{0,160}/root/\.profile'
    unless:
      - id: metadata/package/files/system-package::dpkg-maintainer-script

  - id: shell-zshrc-root-path
    desc: Shell zshrc root path
    crit: suspicious
    conf: 0.9
    if:
      type: text
      substr: /root/.zshrc

