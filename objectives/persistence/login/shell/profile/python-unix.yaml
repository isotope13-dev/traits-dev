defaults:
  platforms: [unix, appliance]
  for: [python, pyc]
  attack: T1037

traits:
  - id: python-login-profile-targets
    desc: References bashrc and profile paths
    crit: notable
    conf: 0.92
    if:
      type: text
      regex: '~/.bashrc[\s\S]{0,220}~/.profile'
    unless:
      - id: well-known/app/calvyn-code
      - type: text
        substr: "To complete setup, add this to"
      - type: text
        substr: "MANPATH does not include"

  - id: python-appends-path-to-login-profile
    desc: Python appends PATH to login profile
    crit: suspicious
    conf: 0.96
    if:
      type: text
      regex: 'open\(rc_file,\s*["'']a["'']\)[\s\S]{0,220}\.write\(export_line\)'

  - id: python-pam-login-hook
    desc: Python PAM login hook
    crit: suspicious
    conf: 0.93
    if:
      type: text
      substr: pam_exec.so expose_authtok

  - id: python-profile-backdoor-script
    desc: Python profile backdoor script
    crit: suspicious
    conf: 0.92
    if:
      type: text
      regex: 'backdoor\.sh|system health check daemon'


composite_rules:
  - id: python-login-profile-persistence
    desc: Python login profile persistence
    crit: hostile
    conf: 0.98
    all:
      - id: python-login-profile-targets
    any:
      - id: python-pam-login-hook
      - id: objectives/persistence/system/cron/job::python-crontab-beacon-callback
      - id: python-profile-backdoor-script

  - id: python-pyc-login-persistence
    desc: Python bytecode login persistence
    crit: hostile
    conf: 0.98
    for: [pyc]
    all:
      - id: python-profile-backdoor-script
    any:
      - id: python-pam-login-hook
      - id: objectives/persistence/system/cron/job::python-pyc-self-cron-entry
