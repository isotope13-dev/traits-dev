defaults:
  platforms: [windows]
  for: [go]
traits:
  - id: go-net-user-add
    desc: Uses net user to add an account
    crit: notable
    conf: 0.8
    if:
      type: string_value
      regex: '(?i)net\s+user\s+.*?\s+/ADD'
