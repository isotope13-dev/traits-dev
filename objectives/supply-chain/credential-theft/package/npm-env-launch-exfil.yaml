# sckit-class plugin implant: a detached payload inherits the full host
# environment (plus the victim's prompt text) while the plugin harvests
# registry tokens and posts JSON loot over HTTPS. No legitimate plugin
# respawns itself detached with cloned secrets and a POST channel open.
defaults:
  platforms: [unix, windows]
  for: [javascript, typescript]
composite_rules:
  - id: npm-detached-env-launch-token-exfil
    desc: Detached plugin payload inherits env and exfiltrates tokens
    crit: hostile
    conf: 0.94
    attack: T1195.002,T1552.001,T1041
    near_bytes: 4000
    all:
      - id: micro-behaviors/process/create/spawn::detached-ignored-unref-spawn
      - id: micro-behaviors/process/create/self-respawn::node-copied-process-env-option
      - id: micro-behaviors/communications/http/post::node-https-request-post
    any:
      - id: micro-behaviors/os/env/package-manager::npm-token-name-ref
      - id: objectives/supply-chain/recon-exfil/ai-cli::prompt-npm-token
      - id: objectives/credential-access/env/secrets/npm-registry::npmrc-access
