defaults:
  platforms: [unix, windows]
  for: [package.json]
  attack: T1528
traits:
  - id: gh-auth-token
    desc: gh auth token command
    crit: suspicious
    conf: 0.98
    if:
      type: text
      substr: gh auth token
