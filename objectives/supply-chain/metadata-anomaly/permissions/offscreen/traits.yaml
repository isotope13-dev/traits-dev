defaults:
  platforms: [unix, windows]
  for: [chrome-manifest]
  crit: notable
  conf: 0.9

traits:
  - id: permission-offscreen
    desc: Extension uses offscreen documents
    if:
      type: value
      path: permissions
      exact: offscreen
