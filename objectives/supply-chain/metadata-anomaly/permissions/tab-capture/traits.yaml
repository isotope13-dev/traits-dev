defaults:
  platforms: [unix, windows]
  for: [chrome-manifest]
  crit: notable
  conf: 0.94

traits:
  - id: permission-tab-capture
    desc: Extension can capture tab media
    if:
      type: value
      path: permissions
      exact: tabCapture
