defaults:
  platforms: [unix, windows]
  for: [chrome-manifest]
  crit: notable
  conf: 0.9

traits:
  - id: permission-system-display
    desc: Extension reads display information
    if:
      type: value
      path: permissions
      exact: system.display
