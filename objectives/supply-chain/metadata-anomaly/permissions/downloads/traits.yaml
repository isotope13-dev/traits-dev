defaults:
  platforms: [unix, windows]
  for: [chrome-manifest]
  crit: notable
  conf: 0.9

traits:
  - id: permission-downloads
    desc: Extension requests downloads permission
    if:
      type: value
      path: permissions
      exact: downloads
