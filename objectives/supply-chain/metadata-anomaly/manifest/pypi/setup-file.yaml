defaults:
  platforms: [unix, windows]
  for: [manifests, scripts]

traits:
  - id: setup-py-file
    desc: Compatibility alias
    crit: component
    conf: 1.0
    if:
      id: objectives/supply-chain/metadata-anomaly/scoring::objectives/supply-chain/metadata-anomaly/manifest/pypi::setup-py-file
