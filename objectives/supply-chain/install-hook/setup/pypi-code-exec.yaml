# Setup.py Code Execution During Installation
# Detects setup.py hooks combined with code execution — the primary
# mechanism for PyPI supply-chain attacks.
# ATT&CK: T1195.002 (Compromise Software Supply Chain)
# MBC: E1059.006 (Command and Scripting Interpreter)
defaults:
  platforms: [linux, macos, windows, unix, android, ios]
  for: [python]
composite_rules:
  # Top-level setup.py is itself an install/build hook: pip imports it while
  # resolving metadata and building an sdist, before a consumer imports the
  # package.  Pair that execution boundary with the already-narrow dataflow
  # shape `exec(requests.get(...).text)` (or its urllib/eval equivalents).
  # This detects mutable, memory-only second stages without assuming a package
  # name, registry, endpoint, or operating system.
  - id: setup-py-remote-code-loader
    desc: setup.py directly executes code fetched over HTTP
    crit: hostile
    conf: 0.99
    attack: "T1195.002,T1105,T1059.006"
    mbc: "B0024,E1059.006"
    scope: leaf
    all:
      - id: metadata/package/files/included::setup-py-basename
      - id: objectives/command-and-control/dropper/execution/eval::remote-exec-text

  - id: pypi-setup-root-home-file-read
    desc: Python setup.py reads a root-home file when loaded
    crit: hostile
    conf: 0.995
    attack: T1195.002,T1005
    scope: outer
    all:
      - id: metadata/build/pip::root-setup-py
      - id: objectives/collection/file-targeting/command::python-import-time-root-home-file-read

  - id: pypi-security-research-claim-with-install-execution
    desc: PyPI package claims security research and executes at install
    crit: hostile
    conf: 0.94
    attack: "T1195.002"
    mbc: "E1059.006"
    scope: outer
    all:
      - id: metadata/package/manifest/description::pkginfo-security-research-claim
      - id: setup-code-execution
  - id: setup-py-getenv
    desc: Environment variable access in setup.py
    crit: notable
    conf: 0.9
    all:
      - id: metadata/build/pip::root-setup-py
      - id: micro-behaviors/os/env/vars/access::python-os-getenv-call
  - id: setup-code-execution
    desc: Code execution during package installation
    crit: hostile
    conf: 0.95
    attack: "T1195.002"
    mbc: "E1059.006"
    near_lines: 10
    all:
      - id: metadata/build/package/recipe::setuptools-run-override
    any:
      # Direct execution
      - id: micro-behaviors/process/interpreter/python::exec-builtin
      - id: micro-behaviors/process/interpreter/python::eval-builtin
      - id: micro-behaviors/process/interpreter/python::compile-builtin
      # Process creation
      - id: micro-behaviors/process/create/subprocess::process-execution
      # Obfuscated execution
      - id: objectives/anti-static/obfuscation/eval/scripting::builtins-obfuscation
    downgrade:
      # Legitimate setup.py may check versions with eval
      any:
        - id: metadata/package/testing/
        - id: metadata/signed/platform/
  - id: pypi-encoded-install-subprocess-dropper
    desc: PyPI install executes base64-encoded subprocess hook
    crit: hostile
    conf: 0.97
    attack: T1195.002,T1059.006,T1105
    scope: outer
    all:
      - id: setup-code-execution
      - id: objectives/anti-static/obfuscation/encoding/content::encoded-python-subprocess-call

  - id: pypi-install-decode-dynamic-exec-dropper
    desc: PyPI install decodes and dynamically executes hook
    crit: hostile
    conf: 0.97
    attack: T1195.002,T1059.006,T1105
    scope: outer
    all:
      - id: setup-code-execution
      - id: objectives/anti-static/obfuscation/eval/scripting::python-decode-decrypt-dynamic-exec
