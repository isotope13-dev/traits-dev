defaults:
  platforms: [unix, windows]
  for: [package.json]
  attack: "T1195.002"
traits:
  - id: remote-http-dependency
    desc: Package depends on remote HTTP tarball
    crit: suspicious
    conf: 0.95
    if:
      type: value
      path: "dependencies"
      substr: 'http://'
  - id: preinstall-node-index-hook
    desc: preinstall runs Node index script
    crit: suspicious
    conf: 0.95
    if:
      type: value
      path: "scripts.preinstall"
      regex: "^node\\s+(\\./)?index\\.(c?js|mjs)$"
  - id: postinstall-node-script-hook
    desc: postinstall runs a local Node script
    crit: suspicious
    conf: 0.95
    if:
      type: value
      path: "scripts.postinstall"
      regex: "^node\\s+(\\./)?[A-Za-z0-9_.-]{1,80}\\.(c?js|mjs)$"
composite_rules:
  - id: remote-dependency-install-loader
    desc: Remote dependency triggers install loader
    crit: hostile
    conf: 0.98
    all:
      - id: remote-http-dependency
      - id: preinstall-node-index-hook
