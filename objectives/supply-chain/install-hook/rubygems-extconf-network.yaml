defaults:
  platforms: [unix, windows]
  crit: suspicious
  conf: 0.8
composite_rules:
  - id: extconf-network-request
    desc: Network request in extconf.rb (suspicious in build scripts)
    all:
      - id: metadata/build::filename-extconf
      - id: objectives/supply-chain/install-hook/scripts::http-client-imports
