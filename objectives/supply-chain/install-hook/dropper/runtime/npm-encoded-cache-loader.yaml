# An encoded downloader that is executed by npm's lifecycle hook is an
# install-time supply-chain dropper.  The delivery composite is intentionally
# separate so the same loader shape remains useful outside npm archives.
defaults:
  platforms: [unix, windows]
  for: [javascript, package.json, npm]
  attack: T1195.002,T1027,T1059.007,T1105
composite_rules:
  - id: npm-postinstall-obfuscated-self-cleaning-loader
    desc: npm postinstall executes an obfuscated self-cleaning loader
    crit: hostile
    conf: 0.99
    for: [npm]
    scope: archive
    all:
      - id: objectives/supply-chain/install-hook/package/manifest::postinstall-runs-node-script
      - id: micro-behaviors/data/decode/custom::fromcharcode-apply-array
      - id: micro-behaviors/process/create/shell/bridge::execSync-dynamic-command
      - id: micro-behaviors/data/source/dynamic/load::node-variable-require-call
      - id: micro-behaviors/fs/file/delete::fs-unlinkSync
