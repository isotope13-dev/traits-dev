defaults:
  platforms: [unix, windows]
  for: [go]
composite_rules:
  - id: uuid-library-masquerade
    desc: UUID library masquerade (standard UUID types + HTTP/Crypto)
    crit: notable
    conf: 0.8
    all:
      - id: micro-behaviors/crypto/library::aes-new-cipher
      - id: objectives/command-and-control/beacon::net-http-import
      - id: objectives/command-and-control/beacon::encoding-json-import
