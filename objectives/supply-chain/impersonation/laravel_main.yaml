defaults:
  platforms: [unix, windows]
  for: [scripts]
traits:
  - id: laravel-main
    desc: "References laravel-main malicious actor"
    crit: suspicious
    conf: 0.9
    if:
      type: text
      substr: 'laravel-main'
