defaults:
  platforms:
    - unix
    - windows
  crit: suspicious
  for:
    - python
    - pyproject.toml
traits:
  - id: pypihosted-domain
    desc: 'the typosquatted domain files.pypihosted.org'
    conf: 1.0
    if:
      type: text
      regex: (?i)[a-z0-9.-]*pypihosted\.org
