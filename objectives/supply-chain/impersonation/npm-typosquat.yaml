defaults:
  platforms: [unix, windows]
  for: [package.json]
composite_rules:
  - id: typosquat-eslint
    desc: Package 'tslint' may be typosquatting 'eslint'
    crit: notable
    conf: 0.5
    all:
      - id: metadata/import/npm::tslint
    unless:
      - id: metadata/import/npm::eslint
