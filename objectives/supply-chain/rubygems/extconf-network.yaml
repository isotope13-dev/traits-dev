defaults:
  platforms: [unix, windows, macos]
  crit: suspicious
  conf: 0.8
composite_rules:
  - id: extconf-network-request
    desc: Network request in extconf.rb (suspicious in build scripts)
    all:
      - id: objectives/supply-chain/rubygems::extconf-file
      - id: objectives/supply-chain/npm/install::http-client-imports
