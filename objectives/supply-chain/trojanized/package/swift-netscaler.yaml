defaults:
  platforms: [netscaler, appliance, unix]
  for: [swift]
  attack: T1195.002

traits:
  - id: swift-spm-cache-manifest
    desc: Swift SPM cache manifest
    crit: suspicious
    conf: 0.92
    if:
      type: text
      substr: .swiftpm

  - id: swift-malicious-manifest-write
    desc: Swift malicious manifest write
    crit: suspicious
    conf: 0.92
    if:
      type: text
      substr: Package.swift.bak


