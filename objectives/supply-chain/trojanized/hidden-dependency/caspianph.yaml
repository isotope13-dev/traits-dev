# GitHub advisory GHSA-r6j8-4ffv-6mcj identifies every original release of
# @caspianph/first-npm-package as malware capable of fully compromising an
# install host. npm later replaced those releases with an inert
# 0.0.1-security holder, which is explicitly excluded below. A non-holder
# runtime dependency still represents delivery of the withdrawn malware even
# when the declaring package keeps its own source tree byte-clean.
defaults:
  conf: 1.0
  platforms: [unix, windows]
  for: [package.json, package-lock.json]
  attack: T1195.002

traits:
- id: caspianph-dependency-name
  desc: Declares Caspianph first-package dependency
  crit: notable
  conf: 1.0
  if:
    type: value
    path: dependencies.@caspianph/first-npm-package
    exists: true

- id: caspianph-original-release-range
  desc: Caspianph dependency selects original releases
  crit: notable
  conf: 1.0
  if:
    type: value
    path: dependencies.@caspianph/first-npm-package
    regex: '(^|[<>=~^*| ])1\.[0-3]\.[0-9]+'

composite_rules:
- id: caspianph-malicious-runtime-dependency
  desc: Depends on withdrawn Caspianph malware releases
  crit: hostile
  conf: 1.0
  all:
  - id: caspianph-dependency-name
  - id: caspianph-original-release-range
