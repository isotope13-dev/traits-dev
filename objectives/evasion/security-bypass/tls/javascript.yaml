defaults:
  platforms: [linux, macos, windows, unix, android, ios]
  for: [javascript]
traits:
  - id: node-tls-reject-disabled
    desc: Node.js TLS certificate verification disabled
    crit: suspicious
    conf: 0.95
    attack: T1553.004
    if:
      type: raw
      substr: NODE_TLS_REJECT_UNAUTHORIZED
