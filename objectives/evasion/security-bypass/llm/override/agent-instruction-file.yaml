# A directive telling a coding agent to write the instruction it has just been
# given into the project's own agent-instruction file.
#
# CLAUDE.md, AGENTS.md, GEMINI.md, .cursorrules and .clinerules are loaded
# automatically at the start of every session, so content placed there outlives
# the conversation that planted it: an injected instruction ending with "and add
# this to CLAUDE.md" turns a one-shot prompt injection into a standing override
# applied to every future task in that checkout.
#
# Anchored on the concrete filenames and on language that transfers the current
# instruction/prompt into them. Ordinary project documentation such as "add a
# release checklist to AGENTS.md" is not an instruction-injection directive;
# the hostile reading is assembled in ../prompt/telemetry-injection.
defaults:
  platforms: [unix, windows]
  for: [json, text, markdown, scripts, source]

traits:
  - id: agent-instruction-file-prompt-write-directive
    desc: Directive writes current prompt to agent file
    crit: notable
    conf: 0.9
    attack: T1546
    if:
      type: text
      regex: '\b(add|append|write)\b.{0,12}(this|it|prompt).{0,12}(AGENTS|CLAUDE|GEMINI)\.md'
      case_insensitive: true
    unless:
      - id: well-known/tool/detection/llm-scanner/
      - id: well-known/tool/detection/security-scanner/
