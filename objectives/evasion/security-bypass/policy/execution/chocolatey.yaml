defaults:
  platforms: [windows]
  for: [powershell]
traits:
  - id: chocolatey-package-installer-marker
    desc: Chocolatey package installer header
    crit: notable
    conf: 0.97
    if:
      type: text
      substr: 'ChocolateyPackageInstaller'
