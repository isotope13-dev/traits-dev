defaults:
  platforms: [unix, windows]
  for: [python, pkginfo, binaries]

traits:
  - id: python-captcha-image-reader
    desc: Runs local CAPTCHA image recognition
    crit: notable
    conf: 0.9
    for: [python, pe, dll]
    if:
      type: text
      regex: '\bread_captcha\s*\('
  - id: browserforge-fingerprint-generation
    desc: Generates browser fingerprints with BrowserForge
    crit: notable
    conf: 0.98
    for: [python]
    if:
      type: symbol
      kind: call
      exact: FingerprintGenerator
  - id: humanized-browser-scroll
    desc: Simulates human scrolling in browser automation
    crit: notable
    conf: 0.94
    for: [python]
    if:
      type: symbol
      kind: call
      exact: human_scroll

  - id: cloudflare-bypasser-api
    desc: Cloudflare challenge bypass API
    crit: notable
    conf: 0.9
    for: [python, javascript, typescript]
    if:
      type: text
      substr: CloudflareBypasser
  - id: cloudflare-challenge-frame
    desc: Cloudflare challenge frame automation
    crit: notable
    conf: 0.95
    for: [python, javascript, typescript]
    if:
      type: text
      regex: '(?is)(frame_locator|locator)\s*\([^)]{0,240}challenges\.cloudflare\.com'
  - id: recaptcha-challenge-automation
    desc: Automates reCAPTCHA challenge controls
    crit: suspicious
    conf: 0.95
    for: [python, javascript, typescript]
    if:
      type: text
      regex: '(?is)(recaptcha|g-recaptcha).{0,240}(frame_locator|locator)'
    unless:
      - id: well-known/app/ai/mcp-stealth-chrome::benign-automation-context
      - id: well-known/app/ai/mcp-stealth-chrome::source-tree-path
  - id: hcaptcha-challenge-automation
    desc: Automates hCaptcha challenge controls
    crit: suspicious
    conf: 0.95
    for: [python, javascript, typescript]
    if:
      # Require Playwright/automation locators — bare "hCaptcha" near
      # addEventListener("click") is ordinary captcha UI (e.g. PayPal ngrlCaptcha).
      type: text
      regex: '(?is)hcaptcha.{0,240}(frame_locator|(page|frame|this)\.locator\s*\()'
  - id: turnstile-coordinate-solver
    desc: Turnstile challenge coordinate solver
    crit: suspicious
    conf: 0.95
    if:
      type: text
      substr: get_turnstile_coordinates
  - id: stealth-browser-api
    desc: Stealth browser automation API
    crit: notable
    conf: 0.85
    if:
      type: text
      substr: StealthBrowser
  - id: javascript-native-code-function-cloak
    desc: JS masks wrapped functions as native code
    crit: notable
    conf: 0.9
    for: [javascript]
    if:
      type: text
      regex: 'Function\.prototype\.toString\s*=\s*function[\s\S]{0,240}\[native code\]'
  - id: browser-fingerprint-platform-flag
    desc: Browser fingerprint platform launch flag
    crit: notable
    conf: 0.9
    if:
      type: text
      substr: --fingerprint-platform=
  - id: browser-fingerprint-locale-flag
    desc: Browser fingerprint locale launch flag
    crit: notable
    conf: 0.9
    if:
      type: text
      substr: --fingerprint-locale=
  - id: browser-fingerprint-seed-flag
    desc: Browser fingerprint seed launch flag
    crit: notable
    conf: 0.9
    for: [python]
    if:
      type: text
      substr: --fingerprint=
  - id: browser-fingerprint-noise-disabled
    desc: Disables browser fingerprint noise
    crit: notable
    conf: 0.9
    for: [python]
    if:
      type: literal
      kind: string
      exact: --fingerprint-noise=false
  - id: cloakbrowser-stealth-engine-import
    desc: Imports CloakBrowser stealth engine
    crit: notable
    conf: 0.96
    for: [python]
    if:
      type: import
      exact: cloakbrowser
  - id: cloudflare-turnstile-solver-api
    desc: Exposes Cloudflare challenge solver API
    crit: notable
    conf: 0.94
    for: [python]
    if:
      type: function
      exact: solve_cloudflare
  - id: antibot-evasion-description
    desc: Package description claims anti-bot evasion
    crit: notable
    conf: 0.9
    for: [pkginfo]
    if:
      type: text
      substr: anti-bot evasion
      case_insensitive: true
  - id: anti-detect-description
    desc: Package description claims anti-detect
    crit: notable
    conf: 0.92
    for: [pkginfo]
    if:
      type: text
      regex: '\banti[- ]?detect(ion)?\b|antidetect(ion)?'
      case_insensitive: true
  - id: anti-detection-fingerprint-claim
    desc: Anti-detection fingerprint browser claim
    crit: notable
    conf: 0.94
    for: [pkginfo]
    if:
      type: text
      regex: '(?i)\banti[- ]?detect(ion)?\b[\s\S]{0,100}\bfingerprint\b'
      case_insensitive: true
  - id: antibot-description
    desc: Package metadata claims anti-bot capability
    crit: notable
    conf: 0.9
    for: [pkginfo, package.json]
    if:
      type: text
      regex: '\b(antibot|anti[- ]bot|bot-detection)\b'
      case_insensitive: true
  - id: multi-vendor-antibot-solvers
    desc: Package lists multiple anti-bot solver vendors
    crit: notable
    conf: 0.95
    for: [pkginfo, package.json, python, javascript, go]
    if:
      type: text
      regex: 'PerimeterX.{0,180}hCaptcha.{0,180}DataDome.{0,180}Incapsula'
      case_insensitive: true
  - id: captcha-bypass-description
    desc: Package description claims captcha bypass
    crit: notable
    conf: 0.9
    for: [pkginfo]
    if:
      type: text
      regex: 'captcha[- ]?bypass'
      case_insensitive: true
  - id: browser-tls-impersonation-description
    desc: Browser TLS impersonation claim
    crit: notable
    conf: 0.9
    for: [pkginfo]
    if:
      type: text
      substr: browser-grade TLS impersonation
      case_insensitive: true
  - id: captcha-solver-description
    desc: Package describes captcha solver
    crit: notable
    conf: 0.92
    for: [pkginfo, python]
    if:
      type: text
      substr: captcha solver
      case_insensitive: true
  - id: automatic-captcha-solving
    desc: Package describes automatic captcha solving
    crit: notable
    conf: 0.92
    for: [pkginfo, python]
    if:
      type: text
      substr: automatic captcha solving
      case_insensitive: true
  - id: stealth-browser-session-description
    desc: Package describes stealth browser sessions
    crit: notable
    conf: 0.9
    for: [pkginfo]
    if:
      type: text
      regex: '(?i)\bstealth\s+browser\s+sessions?\b'
  - id: browser-proxy-automation-description
    desc: Package describes browser proxy automation
    crit: notable
    conf: 0.88
    for: [pkginfo]
    if:
      type: text
      regex: '(?i)\bbrowser\s+sessions?\b.{0,120}\bproxies\b|\bproxies\b.{0,120}\bbrowser\s+sessions?\b'
  - id: captcha-provider-list
    desc: Package lists multiple captcha providers
    crit: notable
    conf: 0.9
    for: [pkginfo, python]
    if:
      type: text
      regex: 'reCAPTCHA v2[\s\S]{0,180}hCaptcha[\s\S]{0,180}FunCaptcha'
    unless:
      - id: metadata/package/quality/benign-context::crawlo-benign-automation-context
  # Keep browser context and the evasion verb as separate atoms. This avoids
  # treating a cryptographic fingerprint described as preventing address
  # spoofing as browser evasion.
  - id: browser-fingerprint-marker
    desc: Browser fingerprint context
    crit: component
    conf: 0.95
    for: [pkginfo, python, javascript]
    if:
      type: text
      regex: '\bbrowser\b.{0,24}\bfingerprint\b'
      case_insensitive: true
  - id: fingerprint-evasion-verb
    desc: Fingerprint evasion verb
    crit: component
    conf: 0.95
    for: [pkginfo, python, javascript]
    if:
      type: text
      regex: '\bfingerprint\w*.{0,40}(spoof|patch|fake|forge|mask|noise)\w*\b'
  - id: custom-browser-fingerprints
    desc: Custom browser fingerprints claim
    crit: suspicious
    conf: 0.92
    for: [pkginfo, python, javascript]
    if:
      type: text
      substr: customisable browser fingerprints
      case_insensitive: true
    unless:
      - id: metadata/package/quality/benign-context::crawlo-benign-automation-context
      - id: metadata/package/quality/benign-context::mithwire-benign-automation-context
  - id: navigator-webdriver-code
    desc: Navigator webdriver masking code
    crit: suspicious
    conf: 0.9
    for: [javascript, typescript]
    if:
      # Match an executable Object.defineProperty call.  Text matching also
      # sees commented-out anti-bot examples in scraper source files.
      type: tree-sitter
      language: typescript
      query: |
        (call_expression
          function: (member_expression
            object: (identifier) @obj
            property: (property_identifier) @method)
          arguments: (arguments
            (identifier) @target
            (string (string_fragment) @property)
            (_))
          (#eq? @obj "Object")
          (#eq? @method "defineProperty")
          (#eq? @target "navigator")
          (#eq? @property "webdriver"))
    unless:
      # Directory refs keep known-good scraper/automation packages without
      # growing per-package leaves; DocSlicer is explicit (exc-unref).
      - id: well-known/app/ai/quantomate/
      - id: well-known/app/productivity/docslicer::playwright-stealth-context
      - id: metadata/package/testing/
      - id: well-known/app/ai/ethan-agent::ethan-agent-xiaohongshu-stealth
      - id: well-known/lib/runtime/stagehand::stagehand-browser-automation-context
      - id: well-known/lib/ai/openagent/
      - id: well-known/tool/offensive/bingo/
      - id: well-known/lib/web/crawl4ai::crawl4ai-package-context
      - id: well-known/lib/web/crawl4ai::package-path
  - id: python-navigator-webdriver-code
    desc: Python embeds navigator webdriver masking
    crit: suspicious
    conf: 0.95
    for: [python]
    if:
      type: literal
      kind: string
      regex: 'Object\.defineProperty\(navigator.{0,80}webdriver'
    unless:
      - id: metadata/package/testing/
      - id: metadata/package/quality/benign-context::selenium-browser-recording-context
      - id: well-known/app/ai/ethan-agent::ethan-agent-xiaohongshu-stealth
      - id: well-known/lib/core/blues-lib::webdriver-source-path
      - id: well-known/app/ai/lean-ai::lean-ai-browser-search-selenium
      - id: well-known/tool/offensive/bingo/
  - id: navigator-webdriver-prototype-delete
    desc: Deletes navigator prototype webdriver
    crit: suspicious
    conf: 0.9
    for: [python]
    if:
      type: text
      substr: delete Object.getPrototypeOf(navigator).webdriver
    not:
      - regex: '(?is)(?=.*vidsrc)(?=.*m3u8)(?=.*playwright)'
    unless:
      - id: well-known/app/system/cinnamon::vidsrc-playwright-scraper-context
      - id: well-known/app/system/cinnamon::vidsrc-scraper-source
      - id: metadata/library/automation::google-flow-browser-auth-client
  - id: navigator-webdriver-getter-override
    desc: Navigator webdriver getter masking
    crit: suspicious
    conf: 0.9
    for: [pkginfo, python, javascript]
    if:
      type: text
      regex: 'navigator\.__defineGetter__\s*\(\s*["\x27]webdriver["\x27]'
    unless:
      - id: metadata/library/automation::google-flow-browser-auth-client
      - id: metadata/package/quality/benign-context::crawlo-benign-automation-context
  - id: navigator-webdriver-delete
    desc: Navigator webdriver property deletion
    crit: suspicious
    conf: 0.88
    for: [pkginfo, python, javascript]
    if:
      type: text
      substr: delete navigator.webdriver
    unless:
      - id: metadata/library/automation::google-flow-browser-auth-client
      - id: metadata/package/quality/benign-context::crawlo-benign-automation-context
  - id: webrtc-ip-leak-blocked
    desc: WebRTC IP leak blocking
    crit: suspicious
    conf: 0.88
    for: [pkginfo, python, javascript]
    if:
      type: text
      substr: WebRTC IP leak blocked
    unless:
      - id: metadata/package/quality/benign-context::crawlo-benign-automation-context

  - id: captcha-solver-api-endpoint
    desc: CAPTCHA solver service endpoint
    crit: notable
    conf: 0.96
    for: [javascript, typescript]
    if:
      type: text
      # Solver farms, not CAPTCHA providers (hcaptcha.com, recaptcha.net).
      regex: '(?i)https://[A-Za-z0-9.-]*(2captcha|anti-captcha|capsolver|capmonster)'

  - id: captcha-task-creation-call
    desc: Creates external CAPTCHA solver task
    crit: notable
    conf: 0.96
    for: [javascript, typescript]
    if:
      type: text
      regex: '(?i)captcha.{0,160}createTask|createTask.{0,160}captcha'

  - id: captcha-task-result-poll
    desc: Polls external CAPTCHA solver result
    crit: notable
    conf: 0.96
    for: [javascript, typescript]
    if:
      type: text
      regex: '(?i)captcha.{0,160}getTaskResult|getTaskResult.{0,160}captcha'

  - id: image-captcha-task-marker
    desc: Image CAPTCHA solver task marker
    crit: component
    conf: 0.96
    for: [javascript, typescript]
    if:
      type: text
      substr: ImageToTextTask

  - id: captcha-client-key-marker
    desc: CAPTCHA solver client key field
    crit: component
    conf: 0.9
    for: [javascript, typescript]
    if:
      type: text
      regex: '(?i)captcha.{0,160}clientKey|clientKey.{0,160}captcha'

  - id: captcha-task-status-marker
    desc: CAPTCHA solver processing status check
    crit: component
    conf: 0.96
    for: [javascript, typescript]
    if:
      type: text
      regex: '(?i)captcha.{0,120}status.{0,40}(processing|ready)'

  - id: captcha-task-id-marker
    desc: CAPTCHA solver task identifier field
    crit: component
    conf: 0.9
    for: [javascript, typescript]
    if:
      type: text
      regex: '(?i)captcha.{0,160}taskId|taskId.{0,160}captcha'

  - id: captcha-image-submission-message
    desc: Sends CAPTCHA images to extension worker
    crit: notable
    conf: 0.94
    for: [javascript, typescript]
    if:
      type: text
      regex: '(?i)sendChrome\.X\s*\(\s*["'']imgs["'']|runtime\.sendMessage\s*\(\s*\[\s*["'']imgs["'']'

  - id: traceless-captcha-verification
    desc: Browser traceless CAPTCHA verification
    crit: suspicious
    conf: 0.98
    for: [python]
    if:
      type: text
      substr: startTracelessVerification

  - id: chromium-automation-controlled-suppression
    desc: Chromium hides AutomationControlled marker
    crit: notable
    conf: 0.95
    # The flag is a launch-argument string, not a Python idiom; Node/TS
    # puppeteer-style launchers pass the identical literal.
    #
    # Matched as text rather than as a parsed string literal: multi-megabyte
    # minified bundles (the shape a shipped VS Code extension or webpack build
    # arrives in) exceed the tree-sitter budget and expose no literals at all,
    # so the literal form went blind on exactly the files that matter. The
    # flag spelling is unambiguous, so a text match adds no comment/prose risk.
    for: [python, javascript, typescript]
    if:
      type: text
      substr: --disable-blink-features=AutomationControlled
    unless:
      - id: metadata/package/quality/benign-context::crawlo-benign-automation-context
      - id: metadata/package/quality/benign-context::wet-mcp-browser-automation-context
      - id: objectives/evasion/security-bypass/anti-bot::wet-mcp-interact-browser-context
      - id: well-known/app/infrastructure/harbor::harbor-framework-context
      - id: metadata/package/quality/benign-context::warn-scraper-benign-automation-context

  - id: browser-automation-artifact-cleanup
    desc: Deletes browser automation globals
    crit: suspicious
    conf: 0.95
    for: [python, javascript, typescript]
    if:
      type: text
      regex: '(?i)delete\s+window\.(callPhantom|_phantom|__nightmare|_selenium)'
    unless:
      - id: well-known/lib/core/yt-dlp/
      - id: well-known/lib/core/yt-dlp::yt-dlp-member-path
      - id: well-known/lib/core/yt-dlp::yt-dlp-wheel-path

  - id: browser-automation-marker-cleanup
    desc: Removes browser automation marker globals
    crit: suspicious
    conf: 0.95
    for: [python, javascript, typescript]
    if:
      type: text
      regex: '(?i)startsWith\(["''](__webdriver_|__selenium_|__fxdriver_|cdc_)'

  - id: navigator-webdriver-alias-mask
    desc: Masks webdriver property through navigator alias
    crit: suspicious
    conf: 0.95
    for: [javascript, typescript]
    if:
      type: text
      regex: '(?is)defineProperty\(\s*\w+\s*,\s*["'']webdriver["''][\s\S]{0,120}undefined'
    # Only the ALIASED form (webdriver masked on a stored navigator reference).
    # The direct `defineProperty(navigator, "webdriver", …)` form is already
    # owned by navigator-webdriver-code; excluding it here prevents both atoms
    # double-counting the single, ordinary anti-detect line every captcha solver
    # and stealth-automation library ships.
    not:
      - regex: 'defineProperty\(\s*navigator\s*,'
    unless:
      - id: well-known/app/ai/quantomate::quantomate-browser-extension-context
      - id: well-known/app/ai/quantomate::quantomate-cdp-runtime-context

  - id: browser-screen-dimensions-spoof
    desc: Overrides browser screen dimensions
    crit: notable
    conf: 0.9
    for: [python]
    if:
      type: literal
      kind: string
      regex: '(?is)\bscreen\s*,\s*["'']height["'']'

  - id: browser-canvas-export-patch
    desc: Patches browser canvas export APIs
    crit: notable
    conf: 0.9
    for: [python, javascript, typescript]
    if:
      type: text
      regex: 'HTMLCanvasElement\.prototype\.toDataURL\s*='

  - id: browser-performance-timing-delete
    desc: Deletes browser performance timing data
    crit: suspicious
    conf: 0.92
    for: [python, javascript, typescript]
    if:
      type: text
      regex: 'delete\s+window\.performance\.timing'
    unless:
      - id: well-known/lib/core/blues-lib::webdriver-source-path
  - id: browser-canvas-export-define-property
    desc: Defines browser canvas export API
    crit: notable
    conf: 0.9
    for: [python, javascript, typescript]
    if:
      type: text
      regex: 'defineProperty\(HTMLCanvasElement\.prototype,"toDataURL"'

  - id: browser-geolocation-api-suppression
    desc: Suppresses browser geolocation API
    crit: notable
    conf: 0.9
    for: [python, javascript, typescript]
    if:
      type: text
      regex: '(?is)Object\.defineProperty\(navigator\s*,\s*["'']geolocation["'']'

  - id: browser-global-webrtc-constructor-override
    desc: Defines global RTCPeerConnection property
    crit: notable
    conf: 0.9
    for: [python, javascript, typescript]
    if:
      type: text
      regex: 'Object\.defineProperty\s*\(\s*window\s*,\s*["'']RTCPeerConnection["'']'

  - id: browser-self-webrtc-constructor-override
    desc: Defines self RTCPeerConnection property
    crit: notable
    conf: 0.9
    for: [python, javascript, typescript]
    if:
      type: text
      regex: 'Object\.defineProperty\s*\(\s*(self|globalThis)\s*,\s*["'']RTCPeerConnection["'']'

  - id: navigator-hardware-concurrency-spoof
    desc: Spoofs navigator hardware concurrency
    crit: notable
    conf: 0.94
    for: [python, javascript, typescript]
    if:
      type: text
      regex: '(?is)Object\.defineProperty\(navigator.{0,80}hardwareConcurrency'

  - id: navigator-device-memory-spoof
    desc: Spoofs navigator device memory
    crit: notable
    conf: 0.94
    for: [python, javascript, typescript]
    if:
      type: text
      regex: '(?is)Object\.defineProperty\(navigator.{0,80}deviceMemory'

  # Align with sibling navigator spoofs (hardwareConcurrency, deviceMemory):
  # mobile-UI unlockers and preview tools set maxTouchPoints to open mobile
  # site layouts. Hostile anti-bot composites still require webdriver /
  # automation-mask legs before escalating.
  - id: navigator-touch-points-spoof
    desc: Spoofs navigator touch points
    crit: notable
    conf: 0.94
    for: [python, javascript, typescript]
    if:
      type: text
      regex: '(?is)Object\.defineProperty\(navigator.{0,80}maxTouchPoints'

  - id: browser-webgl-parameter-spoof
    desc: Spoofs WebGL vendor or renderer parameters
    crit: notable
    conf: 0.94
    for: [python, javascript, typescript]
    if:
      type: text
      regex: '(?is)getParameter\s*=\s*function[\s\S]{0,180}(37445|37446)'

  - id: browser-session-aging-script
    desc: Simulates aged browser session history
    crit: suspicious
    conf: 0.95
    for: [python, javascript, typescript]
    if:
      type: text
      regex: '(?is)SESSION_AGE[\s\S]{0,900}history\.pushState[\s\S]{0,900}localStorage\.setItem'

  - id: login-gated-browser-claim
    desc: Package mentions a login-gated browser flow
    # A login-gated viewer or service is not anti-bot behavior by itself;
    # anti-detect composites add fingerprinting and evasion evidence.
    crit: notable
    conf: 0.96
    for: [pkginfo, python]
    if:
      type: text
      regex: '(?i)\blogin-gated\b'
    unless:
      - id: well-known/app/ai/mcp-stealth-chrome::benign-automation-context
      - id: well-known/app/ai/mcp-stealth-chrome::package-name
      - id: well-known/app/ai/mcp-stealth-chrome::source-tree-path
  - id: humanized-browser-input-claim
    desc: Package claims humanized browser input
    crit: notable
    conf: 0.92
    for: [pkginfo, python]
    if:
      type: text
      regex: '(?i)humanize=True|human-like (mouse|keyboard|scroll)'
  - id: account-hopping-session-claim
    desc: Package claims account hopping
    crit: suspicious
    conf: 0.96
    for: [pkginfo, python]
    if:
      type: text
      regex: '(?i)\baccount[- ]hopping\b'

  - id: multi-account-matrix-claim
    desc: Package claims multi-account operation
    crit: notable
    conf: 0.94
    for: [pkginfo]
    if:
      type: text
      regex: '(?i)\b(multi[- ]account|account matrix|many accounts|multiple accounts)\b'


# ── Search-engine bot-block interstitial evasion ────────────────────────────
# A scraper that drives real Google searches eventually trips the "unusual
# traffic from your computer network" interstitial (google.com/sorry/...).
# Tooling that registers a content script *on that block page* to capture the
# interrupted `continue=` URL, reload until the block lifts, or wipe the
# origin's stored bot-tracking state is deliberately engineering around an
# anti-abuse control rather than backing off.
  - id: search-bot-interstitial-content-script
    desc: Content script targets Google bot-block page
    crit: notable
    conf: 0.92
    for: [chrome-manifest]
    if:
      type: value
      path: 'content_scripts[*].matches'
      regex: 'google\.[a-z.]{2,7}/sorry'

composite_rules:
  - id: wet-mcp-interact-browser-context
    desc: WET MCP interactive browser source context
    crit: exception
    conf: 0.99
    for: [python]
    all:
      - id: metadata/package/quality/benign-context::wet-mcp-source-tree-path
      - id: metadata/library/automation::patchright-python-import
      - id: micro-behaviors/process/create/script/automation::playwright-python-chromium-launch

  - id: browser-webrtc-api-neutralization
    desc: Overrides global WebRTC constructor
    crit: notable
    conf: 0.9
    for: [python, javascript, typescript]
    any:
      - id: browser-global-webrtc-constructor-override
      - id: browser-self-webrtc-constructor-override

  - id: anti-detection-fingerprint-browser-claim
    desc: Package claims anti-detection fingerprinting
    crit: notable
    conf: 0.93
    for: [pkginfo]
    all:
      - id: anti-detect-description
      - id: anti-detection-fingerprint-claim

  - id: browser-fingerprint-spoofing
    desc: Browser fingerprint spoofing claim
    crit: notable
    conf: 0.92
    for: [pkginfo, python, javascript]
    near_bytes: 256
    all:
      - id: browser-fingerprint-marker
      - id: fingerprint-evasion-verb
    unless:
      - id: metadata/package/quality/benign-context::crawlo-benign-automation-context
      - id: metadata/package/quality/benign-context::mithwire-package-identity-context
      - id: metadata/package/quality/benign-context::mithwire-benign-automation-context

  - id: covert-proctoring-evasion-overlay
    desc: Hidden overlay masks browser automation and screen capture
    crit: hostile
    conf: 0.99
    for: [javascript, typescript]
    platforms: [windows]
    attack: T1622
    all:
      - id: navigator-webdriver-override
      - id: micro-behaviors/ui/window/manage/control::window-anti-capture-affinity
      - id: micro-behaviors/ui/window/manage/visibility::document-hidden-state-override
    any:
      - id: navigator-device-memory-spoof
      - id: micro-behaviors/ui/window/manage/visibility::document-visibility-state-override
  - id: camoufox-fingerprint-humanization-bypass
    desc: Anti-detect browser fingerprint and humanization workflow
    crit: hostile
    conf: 0.98
    for: [python]
    scope: outer
    all:
      - id: well-known/lib/web/camoufox::python-async-api-import
      - id: browserforge-fingerprint-generation
      - id: humanized-browser-scroll

  - id: navigator-hardware-and-memory-spoof
    desc: Spoofs navigator hardware and device memory
    # Fingerprint randomization is dual-use in browser automation and privacy
    # tooling; hostile intent is carried by the composites below.
    crit: notable
    conf: 0.94
    all:
      - id: navigator-hardware-concurrency-spoof
      - id: navigator-device-memory-spoof
    unless:
      - id: well-known/app/ai/ethan-agent::ethan-agent-package
      - id: well-known/app/ai/ethan-agent::ethan-agent-xiaohongshu-stealth
  - id: navigator-webdriver-override
    desc: Navigator webdriver property masking
    # The masking operation is a useful analyst signal but is dual-use in
    # browser-login automation; hostile intent comes from the composites that
    # combine it with challenge solving or broader fingerprint spoofing.
    crit: notable
    conf: 0.9
    for: [python, javascript, typescript]
    any:
      - id: navigator-webdriver-code
      - id: python-navigator-webdriver-code
      - id: navigator-webdriver-prototype-delete
    unless:
      - id: well-known/app/system/cinnamon/
      - id: metadata/library/automation::google-flow-browser-auth-client
      - id: well-known/app/ai/ethan-agent::ethan-agent-package
      # The Ethan Xiaohongshu skill intentionally uses a known browser
      # automation stealth helper.  Keep the precise webdriver atom visible,
      # but do not duplicate it with this umbrella composite.
      - id: well-known/app/ai/ethan-agent::ethan-agent-xiaohongshu-stealth
      - id: well-known/lib/runtime/stagehand::stagehand-browser-automation-context
  - id: document-visibility-state-spoof
    desc: Suppresses browser page visibility signals
    crit: suspicious
    conf: 0.96
    for: [javascript, typescript]
    all:
      - id: micro-behaviors/ui/window/manage/visibility::document-visibility-state-override
      - id: micro-behaviors/ui/window/manage/visibility::document-hidden-state-override
      - id: micro-behaviors/ui/window/manage/visibility::visibilitychange-propagation-suppression
  - id: antibot-evasion-claim
    desc: Package claims anti-bot evasion
    crit: notable
    conf: 0.9
    for: [pkginfo, package.json, python, javascript, go]
    any:
      - id: antibot-evasion-description
      - id: anti-detect-description
      - id: antibot-description
      - id: captcha-bypass-description
      - id: browser-tls-impersonation-description
  - id: browser-captcha-solving-capability
    desc: Browser captcha solving capability
    crit: notable
    conf: 0.92
    any:
      - id: captcha-solver-description
      - id: automatic-captcha-solving
      - id: captcha-provider-list
  - id: stealth-fingerprint-spoofing-browser
    desc: Stealth browser fingerprint spoofing
    crit: suspicious
    conf: 0.92
    any:
      - id: chromium-automation-controlled-suppression
      - id: navigator-webdriver-override
      - id: navigator-webdriver-getter-override
      - id: navigator-webdriver-delete
      - id: webrtc-ip-leak-blocked
    unless:
      - id: metadata/library/automation::google-flow-browser-auth-client
      - id: well-known/app/ai/mcp-stealth-chrome/
      - id: well-known/app/ai/infiagent::infiagent-browser-automation-context
      - id: well-known/tool/offensive/bingo::bingo-pentest-module-context
      - id: well-known/app/ai/lightclaw::feishu-browser-automation-context
      - id: well-known/lib/core/blues-lib::webdriver-source-path
      - id: well-known/app/development/seleniumbase::seleniumbase-browser-automation-framework
  - id: stealth-humanized-browser-input
    desc: Browser automation masks detection while simulating human input
    crit: suspicious
    conf: 0.96
    for: [python, javascript, typescript]
    all:
      - id: navigator-webdriver-override
      - id: micro-behaviors/ui/window/manage/dom::playwright-humanized-input-helper
    unless:
      - id: metadata/library/automation::google-flow-browser-auth-client
      - id: metadata/package/quality/benign-context::crawlo-benign-automation-context
      - id: metadata/package/quality/benign-context::warn-scraper-benign-automation-context
      - id: well-known/app/ai/infiagent::infiagent-browser-automation-context

  - id: stealth-browser-antibot-bypass
    desc: Stealth browser anti-bot bypass
    crit: hostile
    conf: 0.98
    for: [python]
    all:
      - id: chromium-automation-controlled-suppression
      - id: navigator-webdriver-override
      - id: micro-behaviors/ui/window/manage/dom::playwright-humanized-input-helper
    unless:
      - id: well-known/app/system/cinnamon::vidsrc-playwright-scraper-context
      - id: well-known/app/system/cinnamon::vidsrc-scraper-source
      - id: metadata/library/automation::google-flow-browser-auth-client
      - id: metadata/package/quality/benign-context::crawlo-benign-automation-context
      - id: metadata/package/quality/benign-context::warn-scraper-benign-automation-context
      - id: well-known/app/ai/infiagent::infiagent-browser-automation-context

  - id: stealth-playwright-antibot-bypass
    desc: Stealth Playwright anti-bot bypass
    crit: hostile
    conf: 0.98
    for: [python]
    all:
      - id: chromium-automation-controlled-suppression
      - id: navigator-webdriver-override
      - id: micro-behaviors/process/create/script/automation::playwright-python-chromium-launch
    unless:
      - id: well-known/app/system/cinnamon::vidsrc-playwright-scraper-context
      - id: well-known/app/system/cinnamon::vidsrc-scraper-source
      - id: metadata/library/automation::google-flow-browser-auth-client
      - id: metadata/package/quality/benign-context::crawlo-benign-automation-context
      - id: metadata/package/quality/benign-context::warn-scraper-benign-automation-context
      - id: well-known/app/ai/infiagent::infiagent-browser-automation-context
      - id: well-known/tool/offensive/bingo::bingo-pentest-module-context

  - id: cloudflare-challenge-bypass-automation
    desc: Cloudflare challenge bypass automation
    crit: suspicious
    conf: 0.96
    all:
      - id: cloudflare-bypasser-api
    any:
      - id: cloudflare-challenge-frame
      - id: turnstile-coordinate-solver
  - id: stealth-browser-antibot-automation
    desc: Stealth browser anti-bot automation
    crit: suspicious
    conf: 0.94
    all:
      - id: antibot-evasion-claim
    any:
      - id: browser-fingerprint-platform-flag
      - id: browser-fingerprint-locale-flag
    unless:
      - id: metadata/library/automation::google-flow-browser-auth-client
      - id: metadata/package/quality/benign-context::browser-use-automation-context
      - id: metadata/package/quality/benign-context::browser-use-library-source-context
      - id: metadata/package/quality/benign-context::crawlo-benign-automation-context
      - id: metadata/package/quality/benign-context::mithwire-package-identity-context
      - id: metadata/package/quality/benign-context::mithwire-benign-automation-context
      - id: metadata/package/quality/benign-context::warn-scraper-benign-automation-context

  - id: anti-detect-credentialed-browser-automation
    desc: Anti-detect credentialed sessions
    crit: hostile
    conf: 0.98
    attack: T1185,T1539
    for: [pkginfo, python]
    all:
      - id: antibot-evasion-claim
      - id: browser-fingerprint-spoofing
    any:
      - id: micro-behaviors/communications/http/cookies::package-session-cookie-handling-claim
      - id: micro-behaviors/data/parse/vocabulary::stored-credentials-vocabulary
      - id: login-gated-browser-claim
      # A persistent profile is a normal Playwright/browser-SDK feature.  It
      # does not establish that the package carries credentials or sessions.
    unless:
      - id: metadata/package/quality/benign-context::mithwire-package-identity-context

  - id: anti-detect-humanized-fingerprint-browser
    desc: Anti-detect browser humanizes fingerprinted sessions
    crit: hostile
    conf: 0.98
    attack: T1185,T1497.001
    for: [pkginfo, python]
    scope: archive
    all:
      - id: antibot-evasion-claim
      - id: browser-fingerprint-spoofing
      - id: humanized-browser-input-claim
    any:
      - id: micro-behaviors/communications/http/cookies::package-session-cookie-handling-claim
      # Profile persistence alone is not evidence of authenticated evasion;
      # require the explicit session-cookie claim above.

  - id: anti-detect-account-hopping-automation
    desc: Anti-detect account hopping
    crit: hostile
    conf: 0.98
    attack: T1185,T1539
    for: [pkginfo, python]
    all:
      - id: antibot-evasion-claim
      - id: browser-fingerprint-spoofing
      - id: account-hopping-session-claim
    any:
      - id: micro-behaviors/communications/http/cookies::package-session-cookie-handling-claim
      - id: micro-behaviors/data/parse/vocabulary::stored-credentials-vocabulary
      - id: metadata/package/keywords/capability::persistent-browser-profile-claim

  - id: anti-detection-authenticated-browser-automation
    desc: Anti-detection automation reads browser sessions
    crit: notable
    conf: 0.98
    attack: T1185,T1539
    for: [pkginfo, python]
    scope: archive
    all:
      - id: anti-detection-fingerprint-browser-claim
      - id: humanized-browser-input-claim
    any:
      - id: micro-behaviors/communications/http/cookies::browser-cookie3-import
      - id: micro-behaviors/communications/http/cookies::playwright-browser-context-cookie-read

  # REMOVED: `anti-detect-copied-chrome-session` (hostile). Its comment claimed
  # the legs "identify session hijacking", but no leg checked a hijack: there
  # was no exfiltration destination, no foreign account, and no credential
  # decryption. What it actually matched was every documented step of an
  # anti-detect scraper reusing the operator's own login -- copy the cookie DB,
  # launch Chrome with a debug port, attach over CDP, read the context cookies
  # -- anchored by `anti-detect-description`, which only reads a marketing
  # phrase out of the package description. A README adjective cannot supply the
  # intent that the mechanical legs do not prove.
  #
  # Four of its six legs also duplicated
  # `objectives/credential-access/browser/chromium::python-chromium-cookie-store-copy-to-profile`,
  # which now carries that observation at suspicious, in the directory that
  # owns the cookie store. The residue ("anti-detect claim" + "attaches over
  # CDP") stays visible as `anti-detect-description` and
  # `micro-behaviors/communications/http/devtools/client::playwright-connect-over-cdp`,
  # both notable. Genuine cookie theft is covered by the chromium/ composites
  # that require a decryption leg.
  - id: anti-detection-multi-account-automation
    desc: Anti-detection multi-account automation
    # Multi-account browser automation is dual-use: social-media account
    # managers and test/QA harnesses can document the same controls.  Keep it
    # as a review signal, but reserve suspicious for concrete evasion mechanics
    # or an abuse chain.
    crit: notable
    conf: 0.98
    attack: T1185,T1497.001
    for: [pkginfo, python]
    scope: archive
    all:
      - id: anti-detection-fingerprint-browser-claim
      - id: humanized-browser-input-claim
      - id: multi-account-matrix-claim

  - id: js-stealth-browser-antibot-bypass
    desc: JavaScript browser stealth masks automation and fingerprints
    crit: hostile
    conf: 0.98
    attack: T1497.001
    for: [javascript, typescript]
    all:
      - id: navigator-webdriver-alias-mask
      - id: browser-automation-marker-cleanup
    any:
      - id: browser-canvas-export-patch
      - id: browser-canvas-export-define-property
      - id: navigator-touch-points-spoof
      - id: browser-webgl-parameter-spoof
    unless:
      - id: well-known/app/ai/quantomate::quantomate-browser-extension-context
      - id: well-known/app/ai/quantomate::quantomate-cdp-runtime-context

  - id: javascript-captcha-bypass-flow
    desc: JavaScript automates external CAPTCHA bypass
    crit: hostile
    conf: 0.98
    for: [javascript, typescript]
    all:
      - id: captcha-solver-api-endpoint
      - id: captcha-task-creation
      - id: captcha-task-result

  - id: captcha-task-creation
    desc: Creates an external CAPTCHA solver task
    crit: notable
    conf: 0.96
    for: [javascript, typescript]
    any:
      - id: captcha-task-creation-call
      - id: image-captcha-task-creation

  - id: captcha-task-result
    desc: Retrieves an external CAPTCHA solver result
    crit: notable
    conf: 0.96
    for: [javascript, typescript]
    any:
      - id: captcha-task-result-poll
      - id: image-captcha-result-poll

  - id: image-captcha-task-creation
    desc: Creates an image CAPTCHA solver task
    crit: notable
    conf: 0.96
    for: [javascript, typescript]
    all:
      - id: image-captcha-task-marker
      - id: captcha-client-key-marker

  - id: image-captcha-result-poll
    desc: Polls image CAPTCHA solver task status
    crit: notable
    conf: 0.96
    for: [javascript, typescript]
    all:
      - id: captcha-task-id-marker
      - id: captcha-task-status-marker
  - id: multi-vendor-antibot-solving
    desc: Multi-vendor anti-bot solving capability
    crit: suspicious
    conf: 0.96
    for: [pkginfo, package.json, python, javascript, go]
    all:
      - id: antibot-evasion-claim
      - id: multi-vendor-antibot-solvers
    unless:
      - id: metadata/package/quality/benign-context::crawlo-benign-automation-context

  - id: remote-captcha-solving-extension
    desc: Extension submits CAPTCHA images to remote solver
    crit: hostile
    conf: 0.98
    for: [javascript, typescript, chrome-manifest, crx]
    scope: archive
    all:
      - id: captcha-solver-api-endpoint
      - id: captcha-image-submission-message
      - id: navigator-webdriver-override

  - id: extension-remote-captcha-solver-api
    desc: Extension sends CAPTCHA tasks to remote solver API
    crit: hostile
    conf: 0.97
    for: [javascript, typescript, crx]
    scope: archive
    all:
      - id: captcha-solver-api-endpoint
      - id: captcha-task-creation-call
      - id: micro-behaviors/process/inject/webextension::broad-content-script-injection

  - id: stealth-captcha-verification
    desc: Stealth browser CAPTCHA token generation
    crit: hostile
    conf: 0.99
    for: [python]
    all:
      - id: navigator-webdriver-override
      - id: traceless-captcha-verification
      - id: chromium-automation-controlled-suppression

  - id: python-stealth-fingerprint-antibot-bypass
    desc: Python stealth browser masks automation fingerprints
    crit: hostile
    conf: 0.98
    for: [python]
    all:
      - id: navigator-webdriver-override
      - id: browser-canvas-export-patch
      - id: browser-webgl-parameter-spoof
    unless:
      - id: well-known/lib/core/blues-lib::webdriver-source-path

  - id: python-cdp-stealth-fingerprint-bypass
    desc: Python CDP stealth fingerprint bypass
    crit: hostile
    conf: 0.98
    attack: T1497.001
    for: [python]
    all:
      - id: navigator-webdriver-override
      - id: navigator-hardware-and-memory-spoof
      - id: browser-canvas-export-patch
      - id: micro-behaviors/communications/http/devtools/client::cdp-method-page-add-script
    unless:
      - id: well-known/lib/core/blues-lib::webdriver-source-path

  - id: python-cdp-performance-stealth-bypass
    desc: Python CDP performance fingerprint bypass
    crit: hostile
    conf: 0.98
    attack: T1497.001
    for: [python]
    all:
      - id: navigator-webdriver-override
      - id: browser-performance-timing-delete
      - id: micro-behaviors/communications/http/devtools/client::cdp-method-performance-disable
    unless:
      - id: well-known/lib/core/blues-lib::webdriver-source-path

  - id: python-webdriver-fingerprint-webrtc-bypass
    desc: Python masks WebDriver and browser fingerprints
    crit: hostile
    conf: 0.98
    attack: T1497.001
    for: [python]
    all:
      - id: navigator-webdriver-override
      - id: navigator-hardware-and-memory-spoof
      - id: browser-webgl-parameter-spoof
      - id: browser-webrtc-api-neutralization
    unless:
      - id: well-known/lib/core/blues-lib::webdriver-source-path

  - id: python-multi-provider-captcha-bypass
    desc: Python automates multiple anti-bot challenge providers
    crit: hostile
    conf: 0.98
    for: [python]
    scope: archive
    all:
      - id: recaptcha-challenge-automation
      - id: hcaptcha-challenge-automation
      - id: cloudflare-challenge-frame
      - id: navigator-webdriver-override
    unless:
      - id: well-known/app/media/forge::mediaforge-captcha-context

  - id: search-engine-bot-interstitial-evasion
    desc: Automates around a search engine bot block
    crit: suspicious
    conf: 0.94
    attack: T1497
    for: [javascript, chrome-manifest, crx, xpi]
    scope: archive
    all:
      - id: search-bot-interstitial-content-script
    any:
      - id: micro-behaviors/communications/http/url/query::continue-redirect-param-read
      - id: micro-behaviors/ui/window/manage/control::timed-page-reload
      - id: micro-behaviors/data/db/web-storage::browsing-data-origin-scoped-clear

  # A registration/login bot that pipes the site's own CAPTCHA image to a paid
  # solving service, then types credentials and clicks submit, has automated
  # away the exact control the site uses to require a human. Solver endpoint
  # alone stays notable (research and accessibility tooling calls these APIs);
  # the hostile verdict needs the solved challenge wired into an autofill and
  # submit chain.
  - id: captcha-solver-credential-autofill-bot
    desc: Solver-backed credential autofill and submit
    crit: hostile
    conf: 0.96
    attack: T1497
    for: [javascript, chrome-manifest, crx, xpi]
    scope: archive
    all:
      # Solver-host URL plus task creation separates outsourcing a challenge
      # to a paid solver from merely displaying a provider widget.
      - id: captcha-solver-api-endpoint
      - id: captcha-task-creation
      - id: micro-behaviors/ui/window/form-input::browser-sensitive-field-automation
      - id: micro-behaviors/ui/window/form-input::browser-submit-button-click
