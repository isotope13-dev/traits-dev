defaults:
  for: [c, macho]
  platforms: [macos]
composite_rules:
  - id: macos-kext-binary
    desc: macOS legacy kext rootkit structure
    crit: suspicious
    conf: 0.92
    any:
      - id: macos-kext-getdirentries-hook
      - id: macos-kext-write-nocancel-hook
      - id: macos-kext-ipf-filter
      - id: macos-kextstat-ref
    needs: 2
