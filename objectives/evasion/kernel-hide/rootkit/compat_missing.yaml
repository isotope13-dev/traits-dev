defaults:
  for: [c, macho]
  platforms: [macos]
composite_rules:
  - id: macos-kext-binary
    desc: macOS legacy kext rootkit structure
    crit: suspicious
    conf: 0.92
    any:
      - id: objectives/evasion/kernel-hide/module
    needs: 2
