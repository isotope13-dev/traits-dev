defaults:
  platforms: [solaris, unix]
  for: [go, elf]
  attack: T1564

traits:
  - id: go-solaris-pr-hidename
    desc: Go Solaris PR_HIDENAME
    crit: suspicious
    conf: 0.94
    if:
      type: text
      regex: 'prHidename\s*=\s*16[\s\S]{0,160}syscall\.Syscall'

  - id: go-solaris-pr-hidename-value
    desc: Go Solaris PR_HIDENAME value
    crit: suspicious
    conf: 0.9
    if:
      type: text
      regex: 'prHidename\s*=\s*16'

  - id: go-solaris-prctl-syscall
    desc: Go Solaris prctl syscall
    crit: suspicious
    conf: 0.9
    if:
      type: text
      regex: 'syscall\.Syscall\s*\(\s*sysPrctl'

  - id: go-solaris-svcd-name
    desc: Go Solaris svcd name
    crit: suspicious
    conf: 0.9
    if:
      type: text
      regex: 'name\s*:=\s*"svcd"'

  - id: go-elf-pr-hidename-error
    desc: Go ELF PR_HIDENAME error string
    crit: suspicious
    conf: 0.93
    for: [elf]
    if:
      type: text
      substr: 'prctl PR_HIDENAME'

  - id: go-elf-hidefromkernel-func
    desc: Go ELF hideFromKernel function
    crit: suspicious
    conf: 0.88
    for: [elf]
    if:
      type: text
      substr: main.hideFromKernel

composite_rules:
  - id: go-solaris-process-hide
    desc: Go Solaris process hide
    crit: hostile
    conf: 0.98
    all:
      - id: go-solaris-pr-hidename
      - id: go-solaris-svcd-name

  - id: go-solaris-prctl-hide
    desc: Go Solaris prctl hide
    crit: hostile
    conf: 0.98
    all:
      - id: go-solaris-pr-hidename-value
      - id: go-solaris-prctl-syscall
      - id: go-solaris-svcd-name

  - id: go-elf-solaris-process-hide
    desc: Go ELF Solaris process hide
    crit: hostile
    conf: 0.98
    all:
      - id: go-elf-pr-hidename-error
      - id: go-elf-hidefromkernel-func
