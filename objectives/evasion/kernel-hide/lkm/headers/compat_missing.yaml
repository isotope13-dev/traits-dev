defaults:
  platforms: [unix]
  for: [binaries, source]

traits:
  - id: kallsyms-lookup-name-str
    desc: kallsyms_lookup_name string reference
    crit: notable
    conf: 0.9
    attack: T1014
    if:
      type: text
      substr: kallsyms_lookup_name
