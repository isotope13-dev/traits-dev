defaults:
  platforms: [unix, appliance]
  for: [c, cpp, objc, elf]
  attack: T1055

traits:
  - id: native-sigaction-flow-hijack
    desc: Native sigaction flow hijack
    crit: suspicious
    conf: 0.93
    if:
      type: text
      regex: 'sigaction\(SIG[A-Z0-9]+[\s\S]{0,260}SA_SIGINFO'

  - id: native-sigaction-call
    desc: Native sigaction call
    crit: suspicious
    conf: 0.9
    if:
      type: text
      regex: 'sigaction\(SIG[A-Z0-9]+\s*,\s*&'

  - id: native-sa-siginfo-flag
    desc: Native SA_SIGINFO flag
    crit: suspicious
    conf: 0.9
    if:
      type: text
      substr: SA_SIGINFO

  - id: native-mprotect-code-page-rwx
    desc: Native mprotect code page RWX
    crit: suspicious
    conf: 0.93
    if:
      type: text
      regex: 'mprotect\(page[\s\S]{0,180}PROT_READ \| PROT_WRITE \| PROT_EXEC'

  - id: native-tcb-context-hijack
    desc: Native TCB context hijack
    crit: suspicious
    conf: 0.92
    if:
      type: text
      regex: 'TCBL[\s\S]{0,360}context\.pc'

composite_rules:
  - id: native-inline-flow-hijack
    desc: Native inline flow hijack
    crit: hostile
    conf: 0.98
    all:
      - id: native-sigaction-call
      - id: native-sa-siginfo-flag
      - id: native-mprotect-code-page-rwx
      - id: native-tcb-context-hijack
