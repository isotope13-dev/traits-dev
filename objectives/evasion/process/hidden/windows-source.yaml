# Hidden process creation - Windows C/C++ source
defaults:
  for: [c, cpp]
  platforms: [windows]
  attack: T1564
traits:
  - id: sw-hide-flag
    desc: SW_HIDE window show flag
    crit: component
    conf: 0.85
    if:
      type: raw
      word: "SW_HIDE"
composite_rules:
  - id: hidden-process-create
    desc: Create process with hidden window
    crit: notable
    conf: 0.9
    all:
      - id: sw-hide-flag
      - id: micro-behaviors/process/create/spawn::create-process-source
