defaults:
  platforms:
    - unix
    - windows
  for:
    - shell
traits:
  - id: hex-xor-loop-stub
    desc: "hex-encoded x64 shellcode stub (xor rcx,"
    crit: suspicious
    conf: 0.9
    mbc: C0043
    attack: T1027
    if:
      type: string_literal
      substr: 4831c94881e9
