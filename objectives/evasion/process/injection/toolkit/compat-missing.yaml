defaults:
  platforms: [windows]
  for: [binaries, scripts]

traits:
  - id: notepad-target
    for: [text]
    desc: Compatibility alias
    crit: component
    conf: 1.0
    if:
      type: string_literal
      exact: __cleave_missing_notepad_target__
