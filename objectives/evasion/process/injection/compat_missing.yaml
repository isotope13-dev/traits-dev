defaults:
  platforms: [windows]
  for: [binaries, scripts]

traits:
  - id: notepad-target
    desc: Compatibility alias
    crit: component
    conf: 1.0
    if:
      id: objectives/command-and-control/dropper/execution/inject::objectives/evasion/process/injection::notepad-target
  - id: decoy-process-hollowing-targets
    desc: Compatibility placeholder
    crit: component
    conf: 1.0
    if:
      type: text
      exact: __cleave_missing_decoy_process_hollowing_targets__
