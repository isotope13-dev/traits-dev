# Resource-backed process-hollowing capability on compiled Windows binaries.
# File-wide API observations do not prove that extracted resource bytes flow
# into the spawned process, so this remains suspicious until call/data-flow
# evidence establishes that handoff.
defaults:
  platforms: [windows]
  for: [pe, dll]
  attack: T1055.012
  mbc: E1055.012.014

composite_rules:
  - id: resource-and-thread-control-injection-capabilities
    desc: Resource extraction with spawned-process injection capabilities
    crit: suspicious
    conf: 0.98
    all:
      - id: micro-behaviors/process/create/spawn::create-process-a
      - id: objectives/evasion/process/injection/memory::remote-thread-injection-api-chain
      - id: micro-behaviors/data/embedded/payload/resource::pe-resource-full-extraction
    any:
      - id: micro-behaviors/process/create/flags::create-suspended-flag-reference
      - id: micro-behaviors/process/inject/runtime/call::set-thread-context-dup
      - id: micro-behaviors/process/inject/runtime/call::set-thread-context-text
    unless:
      - id: metadata/signed/platform/
      - id: well-known/tool/reverse-engineering/
      - id: well-known/tool/sysadmin/
      - id: well-known/game/
