defaults:
  platforms: [unix, appliance]
  for: [c, cpp, objc, elf]
  attack: T1546

traits:
  - id: native-forged-task-control
    desc: Native forged task control
    crit: suspicious
    conf: 0.92
    if:
      type: text
      regex: 'fakeTCB[\s\S]{0,260}entryPt'

composite_rules:
  - id: native-signal-task-flow-hijack
    desc: Native signal task flow hijack
    crit: hostile
    conf: 0.98
    all:
      - id: micro-behaviors/os/signal/handler::sigaction-handler-inspection
      - id: native-forged-task-control
