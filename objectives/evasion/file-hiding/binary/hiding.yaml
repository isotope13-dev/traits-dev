defaults:
  crit: suspicious
  attack: T1564.001
  for:
    - elf
    - shell
  platforms:
    - unix
traits:
  - id: binary-in-include
    desc: Binary or suspicious file in /usr/include
    crit: suspicious
    conf: 0.95
    if:
      type: text
      regex: /usr/include/.{1,50}\.(sh|py|bin)
  - id: header-like-in-include
    desc: Suspicious .h file path (not a header)
    crit: suspicious
    conf: 0.85
    if:
      type: text
      regex: ^/usr/include/[a-z]{5,}\.h$
    not:
      - regex: (stdio|stdlib|string|time|math|signal|errno|fcntl|unistd|stdint|inttypes)
  - id: sdfwex-path
    desc: Known malicious sdfwex hidden path
    crit: suspicious
    conf: 1.0
    if:
      type: text
      substr: sdfwex
  - id: hidden-tmp-binary
    desc: Hidden executable in /tmp
    crit: suspicious
    conf: 0.85
    if:
      type: text
      regex: /tmp/\.[a-z]{2,10}$
    not:
      - substr: /tmp/.superset
  - id: mid-tmp-path
    desc: /tmp/mid path (common dropper location)
    crit: suspicious
    conf: 0.85
    if:
      type: text
      regex: /tmp/midd?$
  - id: hidden-run-file
    desc: Hidden file in /run or /var/run
    crit: suspicious
    conf: 0.8
    if:
      type: text
      regex: /(var/)?run/\..{1,30}
    not:
      - /var/run/.heim_org.h5l.kcm-socket
      - /run/.containerenv
  - id: hidden-dev-file
    desc: Hidden file in /dev
    crit: suspicious
    conf: 0.85
    if:
      type: text
      regex: /dev/\..{1,30}
    unless:
      - type: text
        regex: /dev/\.null\b
      - type: text
        substr: /dev/./
      - type: text
        regex: /dev/\.tmp\.md
      - type: text
        substr: /dev/.udev
      - type: text
        substr: /dev/.devfsd
      - type: text
        regex: /dev/\.buildkit_.*
      - id: micro-behaviors/data/text/app
composite_rules:
  - id: binary-hiding
    desc: Binary hidden in unusual system location
    crit: hostile
    conf: 0.9
    any:
      - id: binary-in-include
      - id: sdfwex-path
      - id: hidden-dev-file
  - id: dropper-temp-paths
    desc: Common dropper temporary paths
    crit: suspicious
    conf: 0.85
    any:
      - id: hidden-tmp-binary
      - id: hidden-tmp-binary
      - id: mid-tmp-path
  - id: hidden-persistence
    desc: Hidden persistence indicators
    crit: suspicious
    conf: 0.85
    needs: 2
    any:
      - id: binary-in-include
      - id: sdfwex-path
      - id: hidden-tmp-binary
      - id: hidden-run-file
