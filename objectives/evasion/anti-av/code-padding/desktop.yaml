defaults:
  platforms: [linux]
  for: [desktop-entry]

traits:
  - id: megabyte-comment-fill
    desc: Megabyte comment fill pads Desktop Entry
    crit: suspicious
    conf: 0.99
    size_min: 1000000
    count_min: 1000
    per_kb_min: 0.75
    if:
      type: text
      regex: '#{128}'
