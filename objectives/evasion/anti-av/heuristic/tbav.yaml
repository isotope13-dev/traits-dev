defaults:
  for: [text, shell, ruby, python, javascript, php, powershell, vbs, applescript]
traits:
  - id: tbav-evasion-asm
    desc: TBAV Heuristic Evasion (Assembly)
    crit: suspicious
    if:
      type: raw
      regex: 'PUSH\s+AX,\s+DEC\s+BX,\s+NOP'
