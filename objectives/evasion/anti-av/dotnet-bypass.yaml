defaults:
  platforms: [windows]
  for: [pe, dll]
  attack: T1562.001
traits:
  - id: skip-verification
    desc: .NET SecurityPermission SkipVerification request
    crit: suspicious
    conf: 0.9
    if:
      type: text
      exact: SkipVerification
