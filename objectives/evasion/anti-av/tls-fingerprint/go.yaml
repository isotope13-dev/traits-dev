defaults:
  platforms: [unix, windows]
  for:
    - elf
    - pe
    - macho
    - dll
    - go
  attack: T1071.001
traits:
  - id: go-utls-fingerprint-spoofing
    desc: Go binary uses uTLS for TLS evasion
    crit: suspicious
    conf: 0.95
    if:
      id: micro-behaviors/communications/http/tls::go-utls-fingerprint-customization
