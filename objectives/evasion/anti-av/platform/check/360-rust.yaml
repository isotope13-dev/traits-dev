defaults:
  platforms: [windows, unix]
traits:
  - id: rust-360-check
    desc: Checks for 360 Safe Tray
    crit: suspicious
    conf: 1.0
    attack: T1036
    for:
      - rust
    if:
      type: string_literal
      substr: qhsafetray.exe
