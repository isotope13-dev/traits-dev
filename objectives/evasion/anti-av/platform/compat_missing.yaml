traits:
  - id: bypass
    desc: compat placeholder
    crit: component
    conf: 0.1
    if:
      type: string
      exact: __cleave_missing_631cfe502dd4__
  - id: uac-bypass
    desc: compat placeholder
    crit: component
    conf: 0.1
    if:
      type: string
      exact: __cleave_missing_935754e70d7f__
defaults:
  crit: component
  conf: 0.1
  for:
    - all
