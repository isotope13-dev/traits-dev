traits:
  - id: bypass
    desc: compat placeholder
    crit: baseline
    conf: 0.1
    if:
      type: text
      exact: __cleave_missing_631cfe502dd4__
defaults:
  platforms:
    - unix
    - windows
    - macos
  crit: baseline
  conf: 0.1
  for: [scripts, binaries]
