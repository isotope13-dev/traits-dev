defaults:
  platforms: [linux]
  for: [shell]
  attack: T1070.001
  mbc: F0005

traits:
  - id: proc-control-journal-flush
    desc: Proc control requests journal flush
    crit: suspicious
    conf: 0.995
    if:
      type: text
      regex: 'echo\s+journal-flush\s*>\s*/proc/[A-Za-z0-9_.-]+'
