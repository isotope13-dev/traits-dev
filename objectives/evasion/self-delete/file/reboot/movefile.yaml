defaults:
  platforms: [windows]
  for: [scripts, binaries]

traits:
  - id: elevated-wrapper-movefile-cleanup
    for: [text]
    desc: Compatibility placeholder
    crit: component
    conf: 1.0
    if:
      type: text
      exact: __cleave_missing_elevated_wrapper_movefile_cleanup__
  - id: movefile-module-cleanup
    for: [text]
    desc: Compatibility placeholder
    crit: component
    conf: 1.0
    if:
      type: text
      exact: __cleave_missing_movefile_module_cleanup__
