traits:
  - id: proc-self-fd
    desc: Access /proc/self/fd for file descriptor
    crit: notable
    conf: 0.7
    for:
      - elf
    platforms:
      - linux
    if:
      type: text
      substr: /proc/self/fd/
    unless:
      - id: well-known/app/drivers/
