traits:
  - id: proc-self-fd
    desc: Access /proc/self/fd for file descriptor
    crit: notable
    conf: 0.7
    for:
      - elf
    platforms:
      - linux
    if:
      type: string_value
      regex: /proc/self/fd/
