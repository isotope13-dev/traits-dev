traits:
  - id: proc-self-fd
    desc: Access /proc/self/fd for file descriptor
    crit: suspicious
    conf: 0.7
    for:
      - elf
    platforms:
      - linux
    if:
      type: text
      substr: /proc/self/fd/
