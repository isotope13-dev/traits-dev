defaults:
  platforms: [unix]
  for: [python, ruby, binaries, scripts]
  crit: notable
  conf: 0.8
  attack: T1620

traits:
- id: memfd_create-syscall-number--319
  desc: memfd_create Linux syscall number (319)
  crit: component
  for: [python, ruby, shell]
  if:
    type: text
    word: '319'
- id: memfd_create-syscall-number--279
  desc: memfd_create Linux syscall number (279)
  crit: component
  for: [python, ruby, shell]
  if:
    type: text
    word: '279'
composite_rules:
- id: memfd_create-syscall-number
  desc: memfd_create Linux syscall number (319 / 279)
  crit: component
  for: [python, ruby, shell]
  unless:
  - type: trait
    id: metadata/file/extension/identity::linux-kernel-package-artifact-basename
  any:
  - id: memfd_create-syscall-number--319
  - id: memfd_create-syscall-number--279
- id: script-memfd-create-hostile
  desc: Script-based memfd_create fileless execution
  crit: hostile
  conf: 0.95
  all:
  - id: micro-behaviors/os/syscall/invoke::libc-syscall
  any:
  - id: micro-behaviors/dylib/load/runtime::ctypes-cdll
  - id: micro-behaviors/dylib/load/runtime::ruby-dlopen
  - id: memfd_create-syscall-number
