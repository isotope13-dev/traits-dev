defaults:
  platforms: [windows]
  for: [binaries]

traits:
  - id: transacted-self-read-loader
    for: [text]
    desc: Compatibility alias
    crit: component
    conf: 1.0
    if:
      type: string_literal
      exact: __cleave_missing_transacted_self_read_loader__
  - id: exec-function
    for: [text]
    desc: Compatibility placeholder
    crit: component
    conf: 1.0
    if:
      type: text
      exact: __cleave_missing_exec_function__
  - id: memfd-create
    for: [text]
    desc: Compatibility placeholder
    crit: component
    conf: 1.0
    if:
      type: text
      exact: __cleave_missing_memfd_create__
