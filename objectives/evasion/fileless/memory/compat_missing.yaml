defaults:
  crit: component
  conf: 1.0
  platforms: [windows]
  for: [binaries]

traits:
  - id: exec-function
    for: [text]
    desc: Compatibility placeholder
    if:
      type: text
      exact: __cleave_missing_exec_function__
  - id: memfd-create
    for: [text]
    desc: Compatibility placeholder
    if:
      type: text
      exact: __cleave_missing_memfd_create__
