traits:
  - id: exec-function
    desc: compat placeholder
    crit: component
    conf: 0.1
    if:
      type: string_value
      exact: __cleave_missing_4cddd7c2bb5a__
  - id: memfd-create
    desc: compat placeholder
    crit: component
    conf: 0.1
    if:
      type: string_value
      exact: __cleave_missing_1cd416495bb6__
defaults:
  platforms: [unix, windows, macos]
  crit: component
  conf: 0.1
  for: [scripts, binaries]
