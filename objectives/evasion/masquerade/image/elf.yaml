defaults:
  platforms: [unix, windows]
  crit: hostile
  conf: 1.0
composite_rules:
  - id: elf-masquerading-as-image
    desc: ELF executable masquerading as an image file
    all:
      - id: metadata/file/magic::elf-header
      - id: metadata/file/extension::image-extension
