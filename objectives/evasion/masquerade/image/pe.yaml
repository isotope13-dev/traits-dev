defaults:
  platforms: [unix, windows]
  for: [binaries]
  crit: notable
  conf: 0.1
traits:
  - id: pe-masquerading-as-image
    crit: notable
    for: [text]
    desc: compatibility placeholder
    if:
      type: text
      exact: __cleave_missing_5d9e836d78fb__

# Image metadata embedded in an executable does not establish image-name
# masquerading. Filename/content mismatches are detected by the canonical
# extension-mismatch traits; the former composites here conflated the two.
