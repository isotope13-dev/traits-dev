defaults:
  platforms: [windows]
  for: [vbs]
  attack: T1036
traits:
  - id: wsh-public-rtf-decoy-write
    desc: WSH writes public RTF decoy
    crit: suspicious
    conf: 0.93
    if:
      type: text
      regex: (?is)CreateTextFile\s*\([^)]{0,140}C:\\Users\\Public\\[^)]{0,100}\.rtf
