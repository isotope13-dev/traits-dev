defaults:
  platforms: [windows]
  for: [pe]

traits:
  - id: microsoft-corporation-string
    desc: String claiming to be Microsoft Corporation
    crit: component
    if:
      type: text
      word: "Microsoft Corporation"

  - id: zero-import-count
    desc: Import count is exactly zero
    crit: baseline
    conf: 0.9
    if:
      type: metrics
      field: binary.import_count
      max: 0.0

composite_rules:
  - id: importless-microsoft-masquerade
    desc: Importless PE claiming to be Microsoft Corporation
    crit: hostile
    conf: 0.95
    all:
      - id: microsoft-corporation-string
      - id: zero-import-count
    unless:
      - id: metadata/signed::signer-evidence-present
      - id: metadata/signed/platform::microsoft
      - id: metadata/signed/leaf::microsoft-corporation
      - id: metadata/file/extension::dotnet-native-image
      - id: metadata/file/extension::msstyles
