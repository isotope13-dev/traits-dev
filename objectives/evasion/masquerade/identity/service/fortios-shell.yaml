defaults:
  platforms: [fortios, unix]
  for: [shell]
  attack: T1036

traits:
  - id: fortios-fake-update-agent
    desc: Fake FortiOS update agent
    crit: suspicious
    conf: 0.92
    if:
      type: text
      regex: '/tmp/\.sysupdate/fortios-update-agent|verify_signature=no'
