defaults:
  platforms: [windows]
  for: [pe]

traits:
  - id: microsoft-hearts-masquerade
    desc: Masquerading as Microsoft Hearts game
    crit: suspicious
    conf: 0.9
    if:
      type: text
      word: InboxGames.Hearts
