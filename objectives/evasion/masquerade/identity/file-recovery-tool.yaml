defaults:
  platforms: [linux, unix, android, macos, ios, windows]
  for: [binaries]
  crit: suspicious
traits:
  - id: file-recovery-tool
    desc: Mentions FileRecoveryTool
    if:
      type: string_value
      substr: "FileRecoveryTool"
      case_insensitive: true
