defaults:
  platforms: [windows]
  for: [pe]
traits:
  - id: ahnlab-masquerade
    desc: "Masquerades as AhnLab security product"
    crit: suspicious
    conf: 1.0
    if:
      type: text
      regex: 'AhnSvc\.exe|V3 Service'
