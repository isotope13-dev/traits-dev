defaults:
  platforms: [windows]
  for: [all]
  attack: T1036.005,T1547.001

composite_rules:
  - id: msi-run-key-system-host-masquerade
    desc: MSI persists a payload as a Windows host executable
    crit: hostile
    conf: 0.98
    all:
      - id: metadata/package/files/msi::msi-installation-database-title
      - id: metadata/package/files/msi::msi-run-key-table-entry
      - id: micro-behaviors/fs/path/host-process::windows-application-frame-host-name
