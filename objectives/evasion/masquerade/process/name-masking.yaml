defaults:
  platforms:
    - unix
    - windows
    - macos
  for: [scripts, binaries]
traits:
  - id: nodejs-title
    desc: Modify process title in NodeJS
    crit: notable
    conf: 0.9
    if:
      type: string_value
      regex: 'process\.title\s*=

'
