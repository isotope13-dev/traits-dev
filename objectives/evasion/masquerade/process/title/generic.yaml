defaults:
  platforms:
    - unix
    - windows
  for: [scripts, binaries]
traits:
  - id: nodejs-title
    desc: Modify process title in NodeJS
    crit: suspicious
    conf: 0.9
    if:
      type: text
      regex: 'process\.title\s*=

'
