defaults:
  platforms: [windows]
  for: [pe, dll]
traits:
  - id: invalid-console-api-import
    desc: Invalid console API import name
    crit: suspicious
    conf: 0.96
    if:
      type: text
      exact: SetConsoleCursorMode
      section: .text
