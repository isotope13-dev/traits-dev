defaults:
  platforms: [windows, linux, macos]
  for: [javascript, pe, elf, macho, batch, powershell]
  attack: T1614
  mbc: B0007.003
composite_rules:
  - id: locale-env-access
    desc: Locale environment variable access
    crit: component
    conf: 0.6
    needs: 2
    any:
      - id: micro-behaviors/os/env/access
    unless:
      - id: metadata/lang/compiler/systems::go-binary-comp
      - id: metadata/lang/compiled::go-runtime-marker
