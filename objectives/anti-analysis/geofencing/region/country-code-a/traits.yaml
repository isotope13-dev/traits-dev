defaults:
  platforms:
  - windows
  - linux
  - macos
  for:
  - javascript
  - pe
  - elf
  - macho
  - batch
  - powershell
  attack: T1614
  mbc: B0007.003
traits:
- id: country-code-am
  desc: Country code AM token
  crit: component
  conf: 0.6
  if:
    type: text
    regex: "(^|[^A-Za-z0-9])AM([^A-Za-z0-9]|$)"
- id: country-code-az
  desc: Country code AZ token
  crit: component
  conf: 0.6
  if:
    type: text
    regex: "(^|[^A-Za-z0-9])AZ([^A-Za-z0-9]|$)"
- id: country-code-by
  desc: Country code BY token
  crit: component
  conf: 0.6
  if:
    type: text
    regex: "(^|[^A-Za-z0-9])BY([^A-Za-z0-9]|$)"
