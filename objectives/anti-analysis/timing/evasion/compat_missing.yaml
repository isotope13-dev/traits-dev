defaults:
  platforms: [windows]
  for: [binaries, scripts]

traits:
  - id: filetime-systemtime-delta-check
    for: [text]
    desc: Compatibility alias
    crit: component
    conf: 1.0
    if:
      type: string_literal
      exact: __cleave_missing_filetime_systemtime_delta_check__
