traits:
  - id: lkm-rootkit-behavior
    desc: compat placeholder
    crit: component
    conf: 0.1
    if:
      type: string
      exact: __cleave_missing_b714c21f6aba__
defaults:
  crit: component
  conf: 0.1
  for:
    - all
