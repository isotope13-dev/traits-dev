defaults:
  platforms: [android, unix]
  for: [c, cpp, elf]
  attack: T1497.001

composite_rules:
  - id: android-qemu-emulator-check
    desc: Android QEMU emulator check
    crit: suspicious
    conf: 0.94
    all:
      - id: micro-behaviors/os/virtualization/qemu::qemud-socket-path-reference
      - id: qemu-lower
