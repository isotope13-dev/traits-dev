defaults:
  platforms: [windows, unix]
traits:
- id: js-date-setter-getter-check
  desc: Checks if Date setter works (anti-emulation)
  crit: suspicious
  conf: 0.8
  for: [javascript]
  if:
    type: text
    substr: FullYear
composite_rules: []
