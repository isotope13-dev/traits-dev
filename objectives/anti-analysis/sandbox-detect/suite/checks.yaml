defaults:
  platforms: [linux, macos, windows, unix, android, ios]
composite_rules:
  - id: evasion-checks
    desc: Evasion checks detected (usernames/hostnames/paths)
    crit: suspicious
    for: [javascript, typescript, shell, python]
    any:
      - id: objectives/anti-analysis/sandbox-detect/inline/
    unless:
      - id: metadata/file/text/help::libxslt-configure-js
      - id: micro-behaviors/process/create/shell/hg::hg-operations
