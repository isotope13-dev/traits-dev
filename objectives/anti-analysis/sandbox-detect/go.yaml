defaults:
  platforms: [linux, macos, windows]
  attack: T1497.001
  mbc: B0007
  for: [go]

composite_rules:
  - id: go-panic-crash-evasion
    desc: Go runtime panic suppression (Crash Evasion)
    crit: suspicious
    conf: 0.9
    all:
      - id: micro-behaviors/os/exception/error-handling::go-empty-recover
