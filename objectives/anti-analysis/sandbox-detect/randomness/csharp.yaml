# C# Randomness Traits (often used for evasion)
# MBC: B0009 - Probabilistic Execution
defaults:
  platforms: [windows, unix]
traits:
  - id: next
    desc: "Probabilistic RNG branch"
    crit: suspicious
    mbc: "B0009"
    conf: 0.7
    for: [csharp]
    if:
      type: symbol
      kind: call
      exact: ".Next("
