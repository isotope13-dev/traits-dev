defaults:
  platforms:
    - unix
    - windows
  for: [binaries]
traits:
  - id: go-issandbox-func
    desc: Sandbox evasion check function name
    crit: suspicious
    conf: 0.9
    if:
      type: text
      exact: main.isSandbox
