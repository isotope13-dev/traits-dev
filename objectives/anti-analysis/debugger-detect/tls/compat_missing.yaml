defaults:
  platforms: [windows]
  for: [binaries, scripts]

traits:
  - id: tls-callbacks-multiple
    desc: Compatibility alias
    crit: component
    conf: 1.0
    if:
      id: objectives/anti-analysis/debugger-detect/tls::objectives/anti-analysis/debugger-detect/tls::tls-callbacks-multiple
