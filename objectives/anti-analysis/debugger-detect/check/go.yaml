defaults:
  attack: T1622
  platforms: [unix]
  for: [go]
traits:
  - id: go-ptrace-traceme
    desc: Go ptrace traceme anti-debug
    crit: suspicious
    conf: 0.9
    mbc: B0001.m01
    if:
      type: text
      regex: '\b(syscall|unix)\.PtraceTraceme\s*\('
