defaults:
  platforms: [unix, netbsd]
  for: [shell]
  attack: T1027

traits:
  - id: shell-self-rewrite-sed-inject
    desc: Shell rewrites self with sed
    crit: suspicious
    conf: 0.94
    if:
      type: text
      regex: 'cp "\$SCRIPT_PATH" "\$tmpfile"[\s\S]{0,260}sed .{0,80}\$tmpfile'



