# Sandbox detection via timing checks - Windows C/C++ source
defaults:
  for: [c, cpp]
  platforms: [windows]
  attack: T1497.003
  mbc: B0007
traits:
  - id: gettickcount-timing
    desc: GetTickCount timing measurement
    crit: component
    conf: 0.85
    if:
      type: symbol
      exact: "GetTickCount"
  - id: sleep-api-source
    desc: Sleep API delay in source
    crit: component
    conf: 0.8
    if:
      type: symbol
      exact: "Sleep"
composite_rules:
  - id: timing-sandbox-detect
    desc: Timing-based sandbox detection
    crit: notable
    conf: 0.88
    all:
      - id: gettickcount-timing
      - id: sleep-api-source
      - id: micro-behaviors/process/terminate/kill::exit-process-source
