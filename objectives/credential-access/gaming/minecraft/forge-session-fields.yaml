defaults:
  platforms: [windows, unix]
  for: [class, jar, zip]
  attack: T1041

traits:
  - id: forge-session-token-fields
    desc: Forge mod session token fields
    crit: suspicious
    conf: 0.98
    attack: T1528
    for: [class]
    if:
      type: text
      regex: acceptedMinecraftVersions.{0,240}session.{0,40}token
