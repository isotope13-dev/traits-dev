# Hardcoded credential planting via environment variable assignment
# ATT&CK: T1552.001 (Credentials In Files)
defaults:
  for: [javascript, typescript]
  platforms: [unix, windows]
  attack: T1552.001
traits:
  - id: recognized-process-env-assignment
    desc: Recognized process.env assignment
    crit: baseline
    conf: 0.9
    if:
      type: text
      regex: 'process\.env\.(NODE_ENV|OM_INIT_FLOW|DOTENV_CONFIG_QUIET|PW_CAPTURE_SCREENSHOTS)\s*='
  - id: hardcoded-credential-env-assignment
    desc: Hardcoded credential environment assignment
    crit: notable
    conf: 0.92
    if:
      type: text
      regex: 'process\.env\.[A-Z0-9_]*(TOKEN|SECRET|PASSWORD|KEY)\s*=\s*["''][^"''\r\n]{8,}["'']'
composite_rules:
  - id: env-credential-plant
    desc: Hardcoded secret token planted in process.env
    crit: notable
    conf: 0.9
    near_lines: 20
    near_bytes: 500
    all:
      - id: micro-behaviors/os/env/vars/access::js-process-env-write
      - id: hardcoded-credential-env-assignment
