defaults:
  platforms: [linux, macos, windows]
  for: [javascript, typescript]
  attack: T1552.001

traits:
  - id: shopify-api-key-environment-variable
    desc: Shopify API-key environment variable
    crit: notable
    conf: 0.95
    if:
      type: literal
      regex: '^(METAOBJECTS_)?SHOPIFY_API_KEY$'
