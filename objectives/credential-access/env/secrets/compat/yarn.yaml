defaults:
  platforms: [windows, unix]
  for:
    - javascript
    - typescript

traits:
  - id: yarn-debug-env-filter
    desc: Yarn debug env filter
    crit: component
    conf: 0.9
    if:
      type: text
      regex: 'Object\.keys\(process\.env\)\.filter\(.{0,160}=>\s*/\^debug_/'
