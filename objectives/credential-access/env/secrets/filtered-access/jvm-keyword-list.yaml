defaults:
  platforms: [unix]
  for: [java, kotlin, class, java_class, jar]
  attack: T1041

traits:
  - id: jvm-env-secret-key-filter
    desc: JVM env secret key filter
    crit: suspicious
    conf: 0.91
    for: [java, kotlin, class, java_class]
    if:
      type: text
      regex: 'PASSWORD[\s\S]{0,180}TOKEN[\s\S]{0,180}API_KEY'
