defaults:
  platforms: [unix]
  for: [swift]
  attack: T1041

traits:
  - id: swift-filtered-env-secrets
    desc: Swift filters secret env vars
    crit: suspicious
    conf: 0.92
    if:
      type: text
      regex: 'key\.contains\("key"\)[\s\S]{0,220}token[\s\S]{0,220}secret'
