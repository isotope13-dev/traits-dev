 # Private-key environment-variable access is a neutral input capability and
 # is defined under micro-behaviors/os/env/vars/credentials.  Wallet-drain
 # objectives reference that capability together with transfer behavior.
defaults:
  platforms: [windows, unix]
  for: [javascript, typescript]
traits: []
