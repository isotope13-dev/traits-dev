# Database connection settings are a neutral application-configuration
# capability. Credential-access composites consume the capability from
# micro-behaviors/os/env/config/database instead of classifying one read as
# credential theft.
