# Cross-Platform Information Stealer Detection
# Platform-agnostic composites that detect stealer behavior regardless of binary format.
# These fire when wallet/browser/credential targeting combines with exfiltration signals,
# without depending on platform-specific APIs (CSIdentity, DPAPI, GDI, etc.).
# ATT&CK: T1555 (Credentials from Password Stores), T1005 (Data from Local System)
defaults:
  attack: "T1555"
  mbc: "B0028"
  platforms: [linux, unix, macos, windows, android, ios]
  for: [scripts, binaries]
composite_rules:
  # Browser credential harvest + wallet targeting = comprehensive stealer
  - id: browser-wallet-stealer
    desc: Browser credentials with wallet targeting
    crit: hostile
    conf: 0.95
    attack: "T1555.003"
    all:
      - id: objectives/discovery/host/browser::browser-data-files
    any:
      - id: objectives/credential-access/wallet/desktop
      - id: objectives/impact/crypto-manipulation/clipboard::wallet-dat-access
      - id: objectives/credential-access/wallet/extension
  # Multi-browser + multi-app targeting = broad credential stealer
  # Targeting 3+ different applications (browsers, messengers, FTP clients, etc.)
  - id: multi-app-credential-stealer
    desc: Multi-application credential harvesting
    crit: hostile
    conf: 0.95
    needs: 3
    any:
      - id: objectives/discovery/host/browser::browser-data-files
      - id: objectives/credential-access/wallet/desktop
      - id: objectives/credential-access/discord/token
      - id: objectives/credential-access/messaging/telegram
      - id: objectives/credential-access/ftp/filezilla
      - id: objectives/credential-access/gaming/steam
      - id: objectives/credential-access/email/thunderbird
      - id: objectives/credential-access/shell/history
      - id: objectives/credential-access/browser/safari
      - id: objectives/credential-access/keychain
      - id: objectives/credential-access/ssh/key
      - id: objectives/credential-access/vpn/config
  # CIS geofencing + ANY credential targeting = CIS-origin stealer
  # This is platform-agnostic because CIS locale strings can appear in any format
  - id: cis-geofenced-credential-stealer
    desc: CIS-geofenced credential stealer
    crit: hostile
    conf: 0.95
    attack: "T1614.001"
    all:
      - id: objectives/anti-analysis/geofencing/location::cis-locale
    any:
      - id: objectives/credential-access/wallet/desktop
      - id: objectives/discovery/host/browser::browser-data-files
      - id: objectives/collection/file-targeting
      - id: objectives/credential-access/discord/token
