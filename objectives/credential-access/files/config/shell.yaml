defaults:
  platforms: [linux, unix, macos]
  for: [shell]
  attack: T1552.001
traits:
  - id: grep-password-etc
    desc: Grep for passwords in config directories
    crit: suspicious
    conf: 0.9
    if:
      type: raw
      regex: grep\s+(-r\s+)?["']?password["']?\s+/etc
