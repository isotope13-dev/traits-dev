defaults:
  platforms: [routeros, appliance]
  for: [javascript]
  attack: T1041

traits:
  - id: routeros-js-certificate-list-call
    desc: RouterOS JS certificate list call
    crit: suspicious
    conf: 0.92
    if:
      type: text
      substr: getCertificateList
  - id: routeros-js-flash-ssh-path
    desc: RouterOS JS flash SSH path
    crit: suspicious
    conf: 0.92
    if:
      type: text
      substr: /flash/ssh/
