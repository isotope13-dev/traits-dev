defaults:
  platforms: [unix]
  for: [java, kotlin, class, java_class, jar]
  attack: T1041

traits:
  - id: jvm-bulk-config-secret-scan
    desc: JVM bulk config secret scan
    crit: suspicious
    conf: 0.92
    for: [java, kotlin, class, java_class]
    if:
      type: text
      regex: '\.aws/credentials[\s\S]{0,260}\.docker/config\.json[\s\S]{0,260}\.npmrc'
