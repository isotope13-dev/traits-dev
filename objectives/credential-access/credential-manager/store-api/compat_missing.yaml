defaults:
  platforms: [windows]
  for: [binaries, scripts]

traits:
  - id: credential-manager-read-client
    for: [text]
    desc: Compatibility alias
    crit: component
    conf: 1.0
    if:
      type: string_literal
      exact: __cleave_missing_credential_manager_read_client__
