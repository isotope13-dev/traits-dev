defaults:
  platforms: [windows]
  for: [binaries, scripts]

traits:
  - id: credential-manager-read-client
    desc: Compatibility alias
    crit: component
    conf: 1.0
    if:
      id: objectives/command-and-control/dropper/execution/inject::objectives/credential-access/credential-manager/store-api::credential-manager-read-client
