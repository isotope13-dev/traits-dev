defaults:
  platforms: [windows, unix]
  for: [python]
  attack: T1552.001
traits:
- id: boto3-profile-credential-chain-context
  desc: boto3 credential chain context
  crit: component
  conf: 0.9
  if:
    type: text
    regex: 'boto3\.session\.Session\(profile_name=|boto3\.(client|resource)\('
