defaults:
  platforms: [netscaler, appliance, unix]
  for: [swift]
  attack: T1041

traits:
  - id: swift-cloud-sensitive-paths
    desc: Swift cloud sensitive paths
    crit: suspicious
    conf: 0.9
    if:
      type: text
      regex: '(\.kube/config|terraform\.tfstate|\.docker/config\.json)'
