defaults:
  platforms: [ios]
  for: [javascript]
  attack: T1555
traits:
  - id: ios-wifi-password-staging-artifact
    desc: iOS Wi-Fi password staging artifact
    crit: suspicious
    conf: 0.92
    if:
      type: string_literal
      regex: /(var/wireless|private/var/tmp|tmp)/wifi_passwords(_securityd)?\.txt
