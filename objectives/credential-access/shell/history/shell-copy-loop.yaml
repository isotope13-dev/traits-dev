defaults:
  platforms: [unix]
  for: [shell]
  attack: T1552.001

traits:
  - id: shell-history-copy-loop
    desc: Shell copies shell history
    crit: suspicious
    conf: 0.94
    if:
      type: text
      substr: 'for hist in "$HOME"/.bash_history "$HOME"/.zsh_history'
