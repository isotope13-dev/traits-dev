defaults:
  platforms: [windows]
  for: [binaries, scripts]

traits:
  - id: embedded-browser-stealer-residue
    desc: Compatibility alias
    crit: component
    conf: 1.0
    if:
      id: objectives/command-and-control/dropper/staging/signed-bundle::objectives/credential-access/browser/multi-target::embedded-browser-stealer-residue
