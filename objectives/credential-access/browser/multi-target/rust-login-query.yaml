defaults:
  platforms: [unix]
  for: [rust, macho, elf]
  attack: T1041

traits:
  - id: rust-browser-login-query
    desc: Rust browser login query
    crit: suspicious
    conf: 0.94
    if:
      type: text
      substr: SELECT origin_url, username_value
