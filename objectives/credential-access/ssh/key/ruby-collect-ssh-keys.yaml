defaults:
  platforms: [unix]
  for: [ruby]
  attack: T1041

traits:
  - id: ruby-ssh-key-collection
    desc: Ruby SSH key collection
    crit: suspicious
    conf: 0.94
    if:
      type: text
      substr: collect_ssh_keys
