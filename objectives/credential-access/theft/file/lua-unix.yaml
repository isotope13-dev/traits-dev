defaults:
  platforms: [unix, appliance]
  for: [lua]
  attack: T1552.001

traits:

  - id: lua-inetd-conf-path
    desc: Lua inetd config path
    crit: suspicious
    conf: 0.88
    if:
      type: text
      substr: /etc/inetd.conf

  - id: lua-ssh-key-filenames
    desc: Lua SSH key filenames
    crit: suspicious
    conf: 0.88
    if:
      type: text
      regex: 'id_rsa[\s\S]{0,120}id_ed25519'

