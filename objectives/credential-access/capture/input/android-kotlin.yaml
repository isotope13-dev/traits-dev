defaults:
  platforms: [android]
  for: [kotlin, class]
  attack: T1056

traits:
  - id: kotlin-password-edit-id
    desc: Kotlin password edit ID
    crit: suspicious
    conf: 0.9
    if:
      type: text
      substr: "password_edit"
    unless:
      - id: metadata/package/files/included::jsii-metadata-basename

  - id: kotlin-store-credential-call
    desc: Kotlin store credential call
    crit: suspicious
    conf: 0.93
    for: [kotlin]
    if:
      type: symbol
      kind: call
      regex: '(^|[.$])storeCredentials?$'
    unless:
      - id: metadata/package/files/included::jsii-metadata-basename

  - id: kotlin-captured-creds-field
    desc: Kotlin captured creds field
    crit: suspicious
    conf: 0.93
    if:
      type: text
      substr: "captured_credentials"
    unless:
      - id: metadata/package/files/included::jsii-metadata-basename

composite_rules:
  - id: kotlin-login-credential-capture
    desc: Kotlin login credential capture
    crit: hostile
    conf: 0.98
    all:
      - id: kotlin-store-credential-call
    any:
      - id: micro-behaviors/ui/window/form-input::username-field-identifier
      - id: micro-behaviors/ui/window/form-input::password-field-identifier
      - id: kotlin-password-edit-id
      - id: micro-behaviors/ui/keywords::kotlin-login-success-status
