traits:
  - id: dscl-authonly
    desc: compat placeholder
    crit: component
    conf: 0.1
    if:
      type: string
      exact: __cleave_missing_fbfcba7848e8__
defaults:
  crit: component
  conf: 0.1
  for:
    - all
