---
defaults:
  platforms: [windows, unix]
  for: [javascript, typescript]
  crit: component
  conf: 0.6

traits:
  - id: amazon-provider-word
    desc: Amazon provider term
    if:
      type: string_literal
      word: amazon
      case_insensitive: true
