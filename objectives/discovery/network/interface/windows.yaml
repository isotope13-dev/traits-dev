defaults:
  platforms: [windows]
  for: [pe, dll]
  attack: T1016
composite_rules:
  - id: wlan-profile-bssid-discovery
    desc: WLAN profile and BSSID discovery
    crit: suspicious
    conf: 0.95
    all:
      - id: micro-behaviors/os/network/interface::wlan-bssid-profile-harvest
    unless:
      - id: metadata/signed/platform::microsoft
