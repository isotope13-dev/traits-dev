defaults:
  platforms:
  - unix
  - windows
  for:
  - go
  size_max: 500000000
traits:
- id: syn-scan-trigger-string
  desc: Triggers a network SYN scan string
  crit: notable
  conf: 1.0
  if:
    type: text
    substr: "triggers a network SYN scan"

composite_rules:
- id: syn-scan-trigger
  desc: Triggers a network SYN scan
  crit: hostile
  conf: 1.0
  all:
  - id: syn-scan-trigger-string
  unless:
  - id: metadata/file/text/catalog::frequency-list-catalog