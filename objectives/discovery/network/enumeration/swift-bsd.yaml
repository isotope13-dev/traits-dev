defaults:
  platforms: [freebsd, netbsd, unix]
  for: [swift]
  attack: T1041

traits:
  - id: swift-network-recon-payload
    desc: Swift network recon payload
    crit: suspicious
    conf: 0.92
    if:
      type: text
      regex: 'interfaces[\s\S]{0,500}routes[\s\S]{0,500}arp[\s\S]{0,500}dns'
