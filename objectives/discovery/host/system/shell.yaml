defaults:
  platforms: [linux, macos, windows, unix, android, ios]
traits:
  - id: hostname-dup
    desc: Shell hostname command execution
    crit: notable
    conf: 0.8
    attack: T1082
    for:
      - javascript
      - typescript
    if:
      type: string
      regex: exec(Sync)?\(['"]hostname['"]
  - id: uname-dup
    desc: Shell uname command execution
    crit: notable
    conf: 0.75
    attack: T1082
    for:
      - javascript
      - typescript
    if:
      type: string
      regex: exec(Sync)?\(['"]uname['"]
  - id: pwd-dup
    desc: Shell pwd command execution
    crit: notable
    conf: 0.7
    attack: T1083
    for:
      - javascript
      - typescript
    if:
      type: string
      regex: exec(Sync)?\(['"]pwd['"]
composite_rules:
  - id: system-profiling
    desc: System profiling command combination
    crit: suspicious
    conf: 0.9
    attack: T1082
    for:
      - javascript
      - typescript
    all:
      - id: objectives/discovery/host/system::hostname-dup
      - id: objectives/discovery/host/system::uname-dup
