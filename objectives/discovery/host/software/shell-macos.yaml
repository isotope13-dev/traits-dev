defaults:
  platforms: [unix]
  for: [shell]
  attack: T1041

traits:
  - id: shell-macos-kext-launch-list
    desc: Shell macOS kext launch list
    crit: suspicious
    conf: 0.94
    if:
      type: text
      regex: 'kextstat[\s\S]{0,220}LaunchAgents'
