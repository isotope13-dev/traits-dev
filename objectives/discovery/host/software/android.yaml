defaults:
  platforms: [android]
  for: [java, kotlin]
  crit: notable
  conf: 0.95
  attack: T1418

traits:
  - id: android-installed-package-enumeration
    desc: Enumerates installed Android packages
    if:
      type: symbol
      kind: call
      exact: getInstalledPackages
