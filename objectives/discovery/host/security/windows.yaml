defaults:
  for:
    - pe
    - dll
  platforms:
    - windows
traits:
  - id: uac-policy-query
    desc: UAC EnableLUA policy registry query
    crit: baseline
    conf: 0.85
    attack: T1082
    if:
      type: text
      substr: EnableLUA
