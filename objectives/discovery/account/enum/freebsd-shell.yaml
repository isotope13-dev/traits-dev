defaults:
  platforms: [freebsd, unix]
  for: [shell]
  attack: T1087.001

composite_rules:
  - id: shell-passwd-group-enum
    desc: Shell enumerates passwd and group files
    crit: suspicious
    conf: 0.92
    all:
      - id: micro-behaviors/fs/path/config/accounts::etc-passwd
      - id: micro-behaviors/fs/path/config/groups::etc-group
