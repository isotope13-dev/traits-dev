defaults:
  platforms: [windows]
  for: [php]
  attack: T1041

traits:
  - id: php-windows-profile-commands
    desc: PHP Windows profile commands
    crit: suspicious
    conf: 0.92
    if:
      type: text
      regex: 'ipconfig /all[\s\S]{0,260}tasklist /FO CSV[\s\S]{0,260}netstat -an'
