defaults:
  platforms: [windows]
  for: [scripts, binaries]

traits:
  - id: host-identity-fingerprint-cluster
    desc: Compatibility placeholder
    crit: component
    conf: 1.0
    if:
      type: text
      exact: __cleave_missing_host_identity_fingerprint_cluster__
