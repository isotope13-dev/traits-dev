defaults:
  platforms: [windows]
  for: [pe, dll]
  attack: T1082

traits:
  - id: native-host-profile-record
    desc: Native hardware identity profile record
    crit: suspicious
    conf: 0.99
    if:
      type: text
      substr: 'OS:%s;User:%s;Host:%s;CPU:%s;HWID:%s;RAM:%s;GUID:%s;GPU:%s'
