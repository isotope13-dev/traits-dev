defaults:
  platforms: [unix]
  for: [php]
  attack: T1041

traits:
  - id: php-system-info-commands
    desc: PHP system info commands
    crit: suspicious
    conf: 0.94
    if:
      type: text
      regex: 'hostname[\s\S]{0,120}sw_vers[\s\S]{0,120}uname -m'
