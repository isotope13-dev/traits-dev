defaults:
  platforms: [unix, android, ios]
  for: [shell]
traits:
  - id: config-guess-basename
    desc: GNU config.guess helper script
    crit: baseline
    if:
      type: basename
      exact: config.guess
