defaults:
  platforms: [linux, unix, macos, android, ios]
  for: [shell]
traits:
  - id: go-toolchain-download
    desc: Go toolchain download command
    crit: baseline
    conf: 0.7
    if:
      type: text
      substr: https://go.dev/dl/go${GO_VERSION}.linux-
composite_rules:
  - id: shell-multi-arch-array
    desc: "Multiple architecture strings (botnet array)"
    crit: suspicious
    conf: 0.85
    mbc: "Discovery"
    attack: "T1082"
    all:
      - id: micro-behaviors/os/sysinfo/platform::multi-arch-target
      - id: micro-behaviors/os/env/vars/cicd::sh-uname-fingerprint
    unless:
      - id: objectives/discovery/system/architecture::config-guess-basename
      - id: objectives/supply-chain/install-hook/dropper/shell::autoconf-generated
      - id: go-toolchain-download
