defaults:
  platforms: [unix, android, ios]
  for: [shell]
traits:
- id: buildarch-targetarch
  desc: Regex component marker
  crit: component
  conf: 0.9
  if:
    type: text
    regex: '\b(BUILDARCH|TARGETARCH)\b'
- id: build-arm
  desc: Regex component marker
  crit: component
  conf: 0.9
  if:
    type: text
    regex: '\b(BUILD_ARM|BUILD_ARM64|BUILD_AARCH64)\b'
- id: build-x86
  desc: Regex component marker
  crit: component
  conf: 0.9
  if:
    type: text
    regex: '\b(BUILD_X86|BUILD_X86_64)\b'
- id: build-ppc
  desc: Regex component marker
  crit: component
  conf: 0.9
  if:
    type: text
    regex: '\b(BUILD_PPC|BUILD_PPC64)\b'
- id: build-other-arch
  desc: Regex component marker
  crit: component
  conf: 0.9
  if:
    type: text
    regex: '\b(BUILD_MIPS|BUILD_SH4|BUILD_ARC)\b'
- id: multi-arch-build-helper-context--rx-2
  desc: Regex component marker
  crit: component
  conf: 0.9
  if:
    type: text
    regex: '\b(CROSS_COMPILE|QEMU_ARCH)\b'
  count_max: 1016493
- id: multi-arch-build-helper-context--rx-3
  desc: Regex component marker
  crit: component
  conf: 0.9
  if:
    type: text
    regex: '\b(qemu-(user|system)-static)\b'
  count_max: 1016494
- id: multi-arch-appimage-build-context
  desc: Multi-arch AppImage build helper
  crit: component
  conf: 0.9
  if:
    type: text
    substr: AppImage
- id: multi-arch-release-marker
  desc: Release package installer marker
  crit: component
  conf: 0.9
  if:
    type: text
    regex: '\b(RELEASE_PKG|release package)\b'
- id: multi-arch-release-archive
  desc: Release archive format marker
  crit: component
  conf: 0.9
  if:
    type: text
    regex: '\b(tar\.gz|\.zip)\b'
composite_rules:
- id: shell-multi-arch-array
  desc: "Multiple architecture strings in runtime shell logic"
  crit: suspicious
  conf: 0.85
  mbc: "Discovery"
  attack: "T1082"
  all:
  - id: micro-behaviors/os/sysinfo/platform/arch::multi-arch-target
  - id: micro-behaviors/os/sysinfo/platform/arch::sh-uname-fingerprint
  unless:
  - id: metadata/build/openra::openra-system-library-helper
  - id: objectives/discovery/system/architecture::config-guess-basename
  - id: objectives/discovery/system/architecture::config-sub-basename
  - id: multi-arch-build-helper-context
  - id: multi-arch-release-installer-context
- id: multi-arch-build-helper-context
  desc: Multi-arch build/cross-compile helper marker
  crit: component
  conf: 0.9
  any:
  - id: buildarch-targetarch
  - id: build-arm
  - id: build-x86
  - id: build-ppc
  - id: build-other-arch
  - id: multi-arch-build-helper-context--rx-2
  - id: multi-arch-build-helper-context--rx-3
  - id: multi-arch-appimage-build-context
- id: multi-arch-release-installer-context
  desc: Multi-arch release installer marker
  crit: component
  conf: 0.9
  all:
  - id: multi-arch-release-marker
  - id: multi-arch-release-archive
