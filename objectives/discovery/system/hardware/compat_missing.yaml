traits:
  - id: system-profiling
    desc: compat placeholder
    crit: component
    conf: 0.1
    if:
      type: string
      exact: __cleave_missing_d32ab5015177__
defaults:
  crit: component
  conf: 0.1
  for:
    - all
