defaults:
  platforms: [windows]
  for: [go]
traits:
  - id: go-physical-drive-access
    desc: Checks for root/admin by opening physical drive
    crit: suspicious
    conf: 0.8
    if:
      type: string_value
      regex: '(?i)\\\\\.\\\\PHYSICALDRIVE0'
