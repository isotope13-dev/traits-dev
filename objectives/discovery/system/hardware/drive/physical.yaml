defaults:
  platforms: [windows]
  for: [go]
traits:
  - id: go-physical-drive-access
    desc: "Checks root/admin by opening physical drive"
    crit: suspicious
    conf: 0.8
    if:
      type: string_literal
      regex: '(?i)\\\\\.\\\\PHYSICALDRIVE0'
