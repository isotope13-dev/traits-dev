defaults:
  platforms: [windows]
  for: [rust]
  attack: T1041

traits:
  - id: rust-windows-host-profile-fields
    desc: Rust Windows host profile fields
    crit: suspicious
    conf: 0.92
    if:
      type: text
      regex: 'hostname[\s\S]{0,220}username[\s\S]{0,220}process_count'
