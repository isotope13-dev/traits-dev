defaults:
  platforms: [windows]
  for: [php]
  attack: T1041

traits:
  - id: php-env-profile-fields
    desc: PHP Windows env profile fields
    crit: suspicious
    conf: 0.88
    if:
      type: text
      regex: 'USERPROFILE[\s\S]{0,180}SYSTEMDRIVE[\s\S]{0,180}PROGRAMFILES'
  - id: php-system-info-report
    desc: PHP system info report
    crit: notable
    conf: 0.88
    if:
      type: literal
      exact: system_info
