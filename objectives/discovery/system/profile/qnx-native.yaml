defaults:
  platforms: [qnx, unix]
  for: [c, cpp, elf]
  attack: T1041

traits:
  - id: qnx-native-host-profile-lines
    desc: QNX native host profile lines
    crit: suspicious
    conf: 0.94
    if:
      type: text
      regex: 'HOST=%s\\n[\s\S]{0,180}UID=%d EUID=%d\\n'
