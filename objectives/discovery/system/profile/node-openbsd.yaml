defaults:
  platforms: [openbsd, bsd, unix]
  for: [javascript]
  attack: T1041

traits:
  - id: node-process-snapshot
    desc: Node creates process snapshot
    crit: suspicious
    conf: 0.88
    if:
      type: text
      substr: /tmp/.ps_snapshot.json
