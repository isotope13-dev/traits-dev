defaults:
  platforms: [unix]
  for: [swift]
  attack: T1041

traits:
  - id: swift-unix-profile-fields
    desc: Swift Unix profile fields
    crit: suspicious
    conf: 0.92
    if:
      type: text
      regex: 'HOSTNAME=[\s\S]{0,160}KERNEL=[\s\S]{0,160}ENV='
