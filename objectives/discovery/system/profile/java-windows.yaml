defaults:
  platforms: [windows]
  for: [java, class, java_class, jar]
  attack: T1041

traits:
  - id: java-windows-host-profile-fields
    desc: Java Windows host profile fields
    crit: suspicious
    conf: 0.92
    for: [java, class]
    if:
      type: text
      regex: '--- ENV ---[\s\S]{0,220}--- DRIVES ---[\s\S]{0,220}--- PROCESSES ---'
