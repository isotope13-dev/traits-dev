defaults:
  platforms: [unix, windows]
  for: [javascript, typescript]

traits:
- id: node-system-profile-json
  desc: Node system profile JSON fields
  crit: suspicious
  conf: 0.9
  if:
    type: text
    regex: "operatingSystem.{0,80}ipAddress.{0,80}username"
