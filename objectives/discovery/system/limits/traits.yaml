# Resource Limit Modification
# MBC: F0004 (Defense Evasion)
# ATT&CK: T1562.001 (Impair Defenses: Disable or Modify Tools)
# NOTE: getrlimit moved to micro-behaviors/process/resources/limits/ (pure query, no suspicion)

defaults:
  for: [elf, macho, so, dylib, c]

traits:
  - id: setrlimit
    desc: Set resource limits for process
    crit: notable
    mbc: "F0004"
    attack: "T1562.001"
    conf: 0.6
    if:
      type: symbol
      regex: setrlimit
    platforms: [unix, linux, macos]
