traits:
  - id: python
    desc: compat placeholder
    crit: baseline
    conf: 0.1
    if:
      type: string
      exact: __cleave_missing_2443b996f8f5__
defaults:
  platforms: [unix, windows, macos]
  crit: component
  conf: 0.1
  for: [shell, python, javascript, powershell, pe, elf]
