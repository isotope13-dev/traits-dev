# IDE Triggers
# Detects automatic execution triggers in IDE configurations.
defaults:
  platforms: [windows, unix]
  for: [javascript, shell]
traits:
  - id: ide-auto-run-task
    desc: VS Code task runs on open
    crit: suspicious
    conf: 0.95
    if:
      type: text
      regex: '["'']runOn["'']\s*:\s*["'']folderOpen["'']'
