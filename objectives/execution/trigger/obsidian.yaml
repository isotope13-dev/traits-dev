# Obsidian Plugin Execution
# MBC: B0025 (Remote Command Execution)
# ATT&CK: T1204 (User Execution)
defaults:
  platforms: [macos, windows, linux]
  for: [package.json, manifests, binaries, scripts]
traits:
  - id: shell-commands-config
    desc: Obsidian Shell Commands plugin configuration
    crit: notable
    conf: 0.95
    if:
      type: text
      substr: ".obsidian/plugins/shell-commands/data.json"

  - id: shell-commands-event-trigger
    desc: Obsidian Shell Commands event trigger (opening vault/file)
    crit: notable
    conf: 0.9
    if:
      type: text
      regex: "(?i)(on-open-vault|on-open-file|on-save-file)"

composite_rules:
  - id: obsidian-plugin-abuse
    desc: Potential Obsidian plugin-based execution abuse
    crit: suspicious
    conf: 0.85
    all:
      - id: shell-commands-config
    any:
      - id: micro-behaviors/process/create/shell
      - id: micro-behaviors/process/create/shell/lang::powershell-keyword
      - id: micro-behaviors/process/create/script::osascript-exec
      - id: shell-commands-event-trigger
