# Python memory runners
defaults:
  platforms: [linux, macos, windows, unix, android, ios]
composite_rules:
  - id: python-shellcode-runner
    desc: Python script executes shellcode in memory via ctypes
    crit: hostile
    conf: 0.95
    for: [python]
    attack: "T1055"
    all:
      - id: micro-behaviors/mem/execute/fileless::python-virtualprotect-rwx
      - id: micro-behaviors/mem/execute/fileless::python-cfunctype-cast
