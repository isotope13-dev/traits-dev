traits:
  - id: ruby-shell-exec
    desc: compat placeholder
    crit: component
    conf: 0.1
    if:
      type: string
      exact: __cleave_missing_a52071743c90__
defaults:
  crit: component
  conf: 0.1
  for:
    - all
