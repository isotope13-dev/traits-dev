defaults:
  for: [csharp]
  platforms: [windows]
composite_rules:
  - id: runtime-compiled-module-loader
    desc: Runtime-compiled C# module loader
    crit: suspicious
    conf: 0.94
    all:
      - id: runtime-code-compilation
    any:
      - id: generate-executable
      - id: compile-assembly-from-source
    needs: 1
