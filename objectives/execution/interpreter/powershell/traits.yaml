defaults:
  platforms:
    - unix
    - windows
  for:
    - powershell
traits:
  - id: pwsh-downloader
    crit: suspicious
    conf: 0.9
    desc: 'PS downloader using.DownloadFile'
    if:
      type: string_literal
      regex: /new-object system.net.webclient\)\.downloadfile/i
