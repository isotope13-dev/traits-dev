defaults:
  for: [text, shell, ruby, python, javascript, php, powershell, vbs, applescript]
traits:
  - id: pwsh-downloader
    crit: suspicious
    conf: 0.9
    description: "Detects PowerShell downloader using (New-Object System.Net.WebClient).DownloadFile"
    if:
      type: string
      regex: /new-object system.net.webclient\)\.downloadfile/i
