defaults:
  platforms: [linux, macos, windows, unix, android, ios]
  for: [scripts, binaries]
traits:
  - id: action-events-exec
    desc: Executes script via ActionEvents engine
    crit: notable
    conf: 0.9
    if:
      type: string_value
      regex: "ActionEvents\\.executeScript|execute\\.script"
