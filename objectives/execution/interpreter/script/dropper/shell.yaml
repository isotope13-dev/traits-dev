defaults:
  platforms: [android, unix]
  for: [shell]
  attack: T1059.004
traits:
  - id: prefix-temp-exe-run-path
    desc: PREFIX temp executable runner path
    crit: component
    conf: 0.92
    if:
      type: text
      substr: "$PREFIX/tmp/exe_run_$$"
  # Copying a file to a temp path is the canonical self-extractor/installer idiom
  # (gzexe does exactly this). Building block for the shell-temp-executable-runner
  # composite, which adds chmod +x and execute-temp for the actual dropper flow.
  - id: copy-arg-to-temp-file
    desc: Copies argument file to temp
    crit: component
    conf: 0.88
    if:
      type: text
      regex: 'cp\s+"\$file"\s+"\$tmp"'
  - id: execute-temp-with-args
    desc: Executes temp file with arguments
    crit: suspicious
    conf: 0.9
    if:
      type: text
      regex: '"\$tmp"\s+"\$@"'
composite_rules:
  - id: shell-temp-executable-runner
    desc: Shell copies file to temp and executes
    crit: suspicious
    conf: 0.95
    all:
      - id: prefix-temp-exe-run-path
      - id: copy-arg-to-temp-file
      - id: micro-behaviors/fs/chmod/executable/mode::shell-plus-x
      - id: execute-temp-with-args
