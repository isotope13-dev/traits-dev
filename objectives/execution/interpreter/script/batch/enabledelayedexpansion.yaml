defaults:
  platforms: [windows, linux, unix, macos, android, ios]
  for: [batch, shell]
traits:
  - id: enabledelayedexpansion
    crit: notable
    desc: 'Batch uses "setlocal enabledelayedexpansion".'
    if:
      type: string_value
      exact: 'setlocal enabledelayedexpansion'
