defaults:
  platforms:
    - unix
    - windows
  for:
    - batch
    - shell
traits:
  - id: enabledelayedexpansion
    crit: notable
    desc: Batch delayed variable expansion
    if:
      type: text
      exact: setlocal enabledelayedexpansion
      case_insensitive: true
