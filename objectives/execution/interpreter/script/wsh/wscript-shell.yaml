defaults:
  platforms: [windows]
composite_rules:
  - id: wscript-shell-comp
    desc: Creates a WScript.Shell object
    crit: notable
    conf: 0.8
    for: [javascript]
    any:
      - id: micro-behaviors/os/com/object::wscript-shell-literal
      - id: objectives/execution/interpreter/script/wsh::wscript-shell
