defaults:
  platforms: [windows]
  crit: notable
  conf: 0.8
  for: [javascript, vbs]
traits:
  - id: wsh-adodb-stream-binary
    desc: "Sets stream type to binary (ADODB.Stream)"
    if:
      type: text
      regex: '(?i)\b(adodb(\.stream)?|stream|ado\w*)\.type\s*=\s*1\b'
