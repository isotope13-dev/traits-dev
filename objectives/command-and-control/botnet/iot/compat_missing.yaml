traits:
  - id: pipe-sh-or-bash
    desc: compat placeholder
    crit: component
    conf: 0.1
    if:
      type: string
      exact: __cleave_missing_d578f5da0c0b__
defaults:
  crit: component
  conf: 0.1
  for:
    - all
