defaults:
  platforms: [windows]
  for: [pe, dll]
  attack: T1082

traits:
- id: botversion-field
  desc: Bot version identifier string
  crit: component
  conf: 0.92
  if:
    type: text
    substr: botversion=
- id: taskid-field
  desc: C2 task identifier string
  crit: notable
  conf: 0.85
  if:
    type: text
    exact: taskid
