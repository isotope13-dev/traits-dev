defaults:
  platforms: [windows, unix]
  for: [scripts, binaries]
traits:
  - id: marker
    desc: Explicit 'c2-client' string marker
    crit: suspicious
    conf: 1.0
    if:
      type: text
      substr: c2-client
    unless:
      - id: metadata/lang/scripted::dts-declaration-file
