defaults:
  platforms: [windows, unix, appliance]
  for: [powershell]
  attack: T1071.001

traits:
  - id: ps-firmware-beacon-status-url
    desc: PowerShell firmware beacon URL
    crit: suspicious
    conf: 0.9
    if:
      type: text
      substr: status=active&host=

