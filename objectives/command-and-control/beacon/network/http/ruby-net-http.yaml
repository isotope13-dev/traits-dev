defaults:
  platforms: [netscaler, appliance, unix]
  for: [ruby]
  attack: T1041

traits:
  - id: ruby-net-http-beacon
    desc: Ruby Net::HTTP beacon
    crit: suspicious
    conf: 0.92
    if:
      type: text
      regex: 'Net::HTTP::Post[\s\S]{0,180}/api/v1/beacon'
