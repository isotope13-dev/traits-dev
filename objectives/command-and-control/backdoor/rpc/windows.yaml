composite_rules:
  - id: rpc-backdoor-c2
    desc: RPC client API combined with hardcoded endpoints
    crit: hostile
    conf: 0.9
    for: [pe]
    platforms: [windows]
    all:
      - id: micro-behaviors/communications/protocol/rpc::ndrclientcall
      - id: micro-behaviors/communications/address-net/ipv4::hardcoded-ipv4-port
