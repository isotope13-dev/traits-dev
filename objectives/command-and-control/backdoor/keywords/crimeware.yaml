# Crimeware/stealer lexicon words. Single English/jargon words that appear in
# panel source, READMEs, and ordinary code alike (e.g. npm `needle`, the word
# "stealer"). Component-only building blocks; a family verdict requires several
# co-occurring alongside a real behavior indicator.
defaults:
  platforms: [windows, unix]
  for: [javascript, typescript]
  crit: component
  conf: 0.3
traits:
  - id: crimeware-word
    desc: "Bare word: crimeware"
    if:
      type: text
      word: crimeware
      case_insensitive: true
  - id: drainer-word
    desc: "Bare word: drainer"
    if:
      type: text
      word: drainer
      case_insensitive: true
  - id: needle-word
    desc: "Bare word: needle"
    if:
      type: text
      word: needle
      case_insensitive: true
