defaults:
  platforms: [windows, unix]
  for: [php]
  attack: T1505.003
  mbc: B0030

traits:
  - id: php-upload-url-publication
    desc: PHP publishes uploaded file URL
    crit: suspicious
    conf: 0.96
    if:
      type: text
      regex: '\$_SERVER.{0,120}HTTP_HOST.{0,120}PHP_SELF'

composite_rules:
  - id: php-host-profiling-upload-shell
    desc: PHP upload shell profiles host and republishes files
    crit: hostile
    conf: 0.99
    all:
      - id: objectives/command-and-control/backdoor/webshell/file-manager::php-upload-copy-from-files
      - id: micro-behaviors/os/sysinfo/platform/runtime::php-uname-call
      - id: php-upload-url-publication
    near_bytes: 4096
