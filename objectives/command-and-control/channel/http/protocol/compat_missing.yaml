defaults:
  for: [binaries]
  platforms: [windows, linux, macos, unix, android, ios]
composite_rules:
  - id: remote-client-session-protocol
    desc: Interactive remote client HTTP session protocol
    crit: notable
    conf: 0.86
    all:
      - id: custom-client
    any:
      - id: http-method-post
      - id: http-beacon-counter
      - id: http-content-type
    needs: 1
