defaults:
  for: [binaries]
  platforms: [unix, windows]
composite_rules:
- id: remote-client-session-protocol
  desc: Countered HTTP client session protocol
  crit: notable
  conf: 0.92
  all:
  - id: custom-client
  - id: http-beacon-counter
