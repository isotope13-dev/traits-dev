defaults:
  platforms: [linux]
  for: [elf]
  attack: T1090.001
  mbc: B0022

traits:
  - id: vision-proxy-list-fetch-symbol
    desc: Vision proxy-list fetcher symbol
    crit: notable
    conf: 0.98
    if:
      type: text
      substr: main.fetchProxyList
      section: .gopclntab
composite_rules:
  - id: vision-socks-proxy-channel
    desc: Vision remotely controlled SOCKS proxy channel
    crit: hostile
    conf: 0.99
    all:
      - id: micro-behaviors/communications/proxy/tunnel::go-socks-dispatch-symbol
      - id: vision-proxy-list-fetch-symbol
