defaults:
  platforms: [windows]
  for: [c, cpp]
  attack: T1095
  crit: suspicious
  conf: 0.94

composite_rules:
  - id: vmci-guest-host-channel
    desc: VMware VMCI guest-host socket channel
    all:
      - id: micro-behaviors/communications/socket/vmci::vmci-device-name
      - id: micro-behaviors/communications/socket/vmci::vmci-host-prefix
