defaults:
  platforms:
    - linux
    - macos
    - windows
    - unix
    - android
    - ios
  crit: notable
  conf: 0.8
  for:
    - binaries
    - scripts
    - source
    - manifests
    - documents
    - media
    - data
traits:
  - id: ngrok
    desc: ngrok tunneling service
    if:
      type: text
      regex: '\b[a-z0-9-]+\.ngrok(-free)?\.(io|app)\b'
    unless:
      - id: metadata/package::has-tests
      - type: basename
        regex: (^|[\\/]).*_test\.go$
  - id: localhost-run-dup
    desc: localhost.run tunneling service
    if:
      type: text
      regex: localhost\.run\b
  - id: trycloudflare
    desc: Cloudflare Tunnel (TryCloudflare)
    if:
      type: text
      regex: \b[a-z0-9-]+\.trycloudflare\.com\b
    unless:
      - type: basename
        regex: (^|[\\/]).*_test\.go$
  - id: pipe-run
    desc: pipe.run tunneling service
    if:
      type: text
      regex: pipe\.run\b
  - id: serveo
    desc: serveo.net tunneling service
    if:
      type: text
      regex: serveo\.net\b
  - id: telebit
    desc: telebit.cloud tunneling service
    if:
      type: text
      regex: telebit\.cloud\b
  - id: pinggy
    desc: Pinggy tunneling service
    if:
      type: text
      regex: pinggy\.(io|link)\b
