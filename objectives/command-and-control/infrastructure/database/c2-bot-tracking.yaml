# C2 bot tracking database access
# Detects server-side scripts that query databases to track/manage
# compromised endpoints (bots/miners) in a C2 infrastructure.
# ATT&CK: T1071.001 (Application Layer Protocol: Web Protocols)
# ATT&CK: T1082 (System Information Discovery)
defaults:
  for: [php]
  platforms: [windows, unix]
  crit: suspicious
  attack: T1071.001

composite_rules:
  - id: c2-bot-data-api
    desc: C2 backend API serving bot status data
    crit: hostile
    conf: 0.95
    mbc: B0030
    all:
      - id: micro-behaviors/data/db/mysql::php-mysqli-connect
    needs: 2
    any:
      - id: micro-behaviors/data/db/sql::sql-select
      - id: micro-behaviors/communications/http/geocoding::nominatim-reverse
      - id: micro-behaviors/communications/http/request/params::get-dup
      - id: micro-behaviors/data/serialize/object::json-encode
    unless:
      - id: well-known/app/wordpress-plugins::wordpress-maintenance-plugin
