defaults:
  platforms:
    - unix
    - windows
    - macos
  for: [scripts, binaries]
traits:
  - id: suspicious-url
    desc: Suspicious C2 URL or email address.
    crit: suspicious
    conf: 0.9
    if:
      type: text
      exact: 0rUWe_@cVW.axS
