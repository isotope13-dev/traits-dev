defaults:
  platforms:
    - unix
    - windows
  for: [scripts, binaries]
traits:
  - id: suspicious-numeric-ip
    desc: Suspicious numeric string or obfuscated IP.
    crit: suspicious
    conf: 0.8
    if:
      type: text
      exact: '9087654356'
