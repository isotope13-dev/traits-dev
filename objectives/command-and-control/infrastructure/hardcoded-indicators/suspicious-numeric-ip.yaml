defaults:
  platforms:
    - unix
    - windows
    - macos
  for: [scripts, binaries]
traits:
  - id: suspicious-numeric-ip
    desc: Suspicious numeric string or obfuscated IP.
    crit: suspicious
    conf: 0.8
    if:
      type: string_value
      exact: '9087654356'
