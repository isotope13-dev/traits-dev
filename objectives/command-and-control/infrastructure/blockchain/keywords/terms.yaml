---
defaults:
  platforms:
  - windows
  - unix
  crit: component
  conf: 0.2
  for:
  - binaries
  - scripts
  - source
traits:
- id: contract-keyword
  desc: contract keyword
  conf: 0.3
  for:
  - javascript
  - typescript
  unless:
  - id: metadata/file/catalog/identity/
  if:
    type: literal
    kind: string
    regex: "(?i)contract"
