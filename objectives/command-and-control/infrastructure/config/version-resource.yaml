defaults:
  for: [pe, dll]
  platforms: [windows]
composite_rules:
  - id: encoded-version-resource-config
    desc: C2-like config hidden in version resource
    crit: suspicious
    conf: 0.92
    mbc: "C0002"
    attack: "T1071.001"
    all:
      - id: metadata/quality/versioning::anomalous-version-resource
    any:
      - id: objectives/anti-static/obfuscation/payload/encrypted::high-entropy-data
      - id: objectives/execution/reflection/class::dynamic-load-pattern
    downgrade:
      any:
        - id: metadata/format/installer::nsis
        - id: metadata/format/installer::inno-setup
        - id: metadata/sign/certificate::bitdefender-brand-text
        - id: metadata/format/pe::resource-only-dll
        - id: micro-behaviors/data/embedded/payload::pe-magic-in-rsrc
        - id: metadata/format::many-imports
