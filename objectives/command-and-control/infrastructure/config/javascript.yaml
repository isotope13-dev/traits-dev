defaults:
  platforms: [windows, unix, macos]
  for: [javascript]
  attack: T1071.001
  mbc: C0002
traits:
  - id: js-domain-array
    desc: Array of multiple domain names (C2 fallback)
    crit: notable
    conf: 0.8
    if:
      type: raw
      regex: '\["[a-zA-Z0-9][\w\.\-]*\.\w{2,6}"\s*,'
