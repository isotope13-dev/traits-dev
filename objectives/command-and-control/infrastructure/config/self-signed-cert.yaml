defaults:
  platforms: [linux, macos, windows, unix, android, ios]
  for: [scripts]
  attack: T1587.003
traits:
  - id: openssl-self-signed-cert
    desc: Generates self-signed TLS certificate
    crit: notable
    conf: 0.85
    if:
      type: raw
      regex: openssl req.{0,60}-x509
