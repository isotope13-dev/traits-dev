defaults:
  platforms: [windows]
  for: [pe]
composite_rules:
  - id: ocra-ipv4-port-stub
    desc: OCRA stub with IPv4 endpoint
    crit: suspicious
    conf: 0.95
    all:
      - id: objectives/anti-static/pack/ocra::ocra-packed-executable
      - id: objectives/command-and-control/infrastructure/network::ipv4-port-string
