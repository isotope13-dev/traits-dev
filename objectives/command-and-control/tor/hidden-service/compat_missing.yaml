traits:
  - id: hidden-service-setup
    desc: compat placeholder
    crit: component
    conf: 0.1
    if:
      type: string_value
      exact: __cleave_missing_2849675a0cfe__
defaults:
  platforms: [unix, windows, macos]
  crit: component
  conf: 0.1
  for: [scripts, binaries]
