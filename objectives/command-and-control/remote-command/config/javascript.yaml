defaults:
  platforms:
    - unix
    - windows
  for:
    - javascript
traits:
  - id: remote-config-service
    desc: Remote configuration service marker
    crit: component
    conf: 0.7
    if:
      type: string_literal
      regex: shepherd\.|config\.service|remote-config
    not:
      - exact: "config.service"
