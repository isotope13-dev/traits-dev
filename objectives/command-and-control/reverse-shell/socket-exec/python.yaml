# Remote Shell Detection
# MBC: OB0004 (Command & Control) → B0030 (C2 Communication)
# ATT&CK: T1059 (Command and Scripting Interpreter)
# NOTE: Socket connect/bind/listen are defined in communications/socket/*/unix.yaml
defaults:
  platforms: [unix, windows]
  for: [python]
traits:
  - id: inline-python-subprocess-shell
    desc: Inline Python interactive shell launch
    crit: suspicious
    conf: 0.95
    unless:
      - id: well-known/lib/ai/unsloth::unsloth-package-context
      # Grouped rather than N named leaves (RULES.md suppression-budget
      # guidance): any catalogued dual-use offensive tool/framework is a
      # directory reference, which counts once instead of summing.
      - id: well-known/tool/offensive/
      - id: well-known/tool/detection/agent-glovebox::staticscan-reverse-shell-fixture-context
    size_max: 50000
    if:
      type: text
      regex: 'subprocess\.(call|Popen)\s*\(.{0,120}/bin/(sh|bash).{0,80}-i'
  - id: python-bash-executable-string
    desc: Bash executable passed to process API
    crit: notable
    conf: 0.75
    if:
      type: tree-sitter
      kind: call
      regex: '(?s)\b(subprocess\.(call|Popen)|os\.(system|popen))\s*\([^)]{0,240}["''](/bin/)?bash["'']'
