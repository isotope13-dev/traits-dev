# Remote Shell Detection
# MBC: OB0004 (Command & Control) → B0030 (C2 Communication)
# ATT&CK: T1059 (Command and Scripting Interpreter)
# NOTE: Socket connect/bind/listen are defined in communications/socket/*/unix.yaml
defaults:
  platforms: [unix, windows]
  for: [python]
traits:
  # Python-specific reverse shell patterns
  - id: python-dup2
    desc: Python file descriptor duplication (os.dup2)
    crit: component
    conf: 0.85
    if:
      type: symbol
      exact: "os.dup2"
  - id: python-socket-fileno-common
    desc: Python named socket fileno
    crit: component
    conf: 0.7
    if:
      type: text
      regex: '\b(sock|socket|conn|client)\.fileno\(\)'
  - id: python-socket-fileno-short
    desc: Python short socket fileno
    crit: component
    conf: 0.65
    if:
      type: text
      regex: '\b(s|c)\.fileno\(\)'
  - id: inline-python-socket-dup2
    desc: Inline Python socket dup2 redirection
    crit: suspicious
    conf: 0.95
    unless:
      - id: well-known/tool/offensive/payload-corpus::payloadsallthethings-release-member
      - id: well-known/lib/ai/unsloth::unsloth-package-context
      - id: well-known/tool/detection/agent-glovebox::archive-context
      - id: well-known/tool/offensive/commix::commix-source-file
      - id: well-known/tool/offensive/pentestkit::local-storage-panel
      - id: well-known/tool/offensive/decepticon::package-tree-member
      - id: well-known/tool/offensive/keris::package-tree-member
      - id: well-known/tool/offensive/reconprobe/
      - id: well-known/tool/offensive/bingo/
    size_max: 50000
    if:
      type: text
      regex: 'socket\.socket[\s\S]{0,600}\.connect\s*\([\s\S]{0,600}os\.dup2\s*\(.{0,120}\.fileno\(\)'
  - id: inline-python-subprocess-shell
    desc: Inline Python interactive shell launch
    crit: suspicious
    conf: 0.95
    unless:
      - id: well-known/lib/ai/unsloth::unsloth-package-context
      - id: well-known/tool/offensive/commix::commix-source-file
      - id: well-known/tool/offensive/pentestkit::local-storage-panel
      - id: well-known/tool/offensive/decepticon::package-tree-member
      - id: well-known/tool/detection/agent-glovebox::staticscan-reverse-shell-fixture-context
      - id: well-known/tool/offensive/keris::package-tree-member
      - id: well-known/tool/offensive/reconprobe/
      - id: well-known/tool/offensive/bingo/
    size_max: 50000
    if:
      type: text
      regex: 'subprocess\.(call|Popen)\s*\(.{0,120}/bin/(sh|bash).{0,80}-i'
  - id: python-bash-executable-string
    desc: Bash executable passed to process API
    crit: component
    conf: 0.75
    if:
      type: tree-sitter
      kind: call
      regex: '(?s)\b(subprocess\.(call|Popen)|os\.(system|popen))\s*\([^)]{0,240}["''](/bin/)?bash["'']'
  - id: python-socket-io-forwarders
    desc: Python socket IO forwarders
    crit: suspicious
    conf: 0.9
    if:
      type: text
      regex: 'sock\.recv\s*\(\s*8192\s*\)[\s\S]{0,520}sock\.send\s*\('
    unless:
      - id: well-known/app/infrastructure/ansible::ansible-module-source-context
      - id: well-known/app/infrastructure/ansible::ansible-collections-wheel-context

composite_rules:
  - id: python-fileno
    desc: Python socket fileno() FD extraction
    crit: component
    conf: 0.7
    any:
      - id: python-socket-fileno-common
      - id: python-socket-fileno-short
  - id: python-dup2-io-redirect
    desc: Python os.dup2 socket FD redirect (stdin/stdout/stderr)
    crit: suspicious
    conf: 0.9
    attack: T1059.006
    near_lines: 80
    unless:
      - id: well-known/tool/offensive/pentestkit::local-storage-panel
      - id: metadata/package/testing/harness/runtime/
      - id: metadata/package/testing/presence/harness::python-test-file
      - id: metadata/package/testing/presence/harness/
      - id: metadata/import/python::unittest
      - id: metadata/import/python::test
      - id: well-known/tool/offensive/bingo/
    all:
      - id: python-dup2
      - id: python-fileno
      - id: micro-behaviors/communications/socket/connect::socket-connect-method
  - id: python-dup2-shell
    desc: Python reverse shell via os.dup2 FD hijack
    crit: suspicious
    unless:
    - id: well-known/tool/offensive/pentestkit::local-storage-panel
    - id: well-known/tool/offensive/bingo::bingo-pentest-module-context
    conf: 0.95
    attack: T1059.006
    all:
      - id: python-dup2-io-redirect
      - id: micro-behaviors/process/create/subprocess::call
    any:
      - id: micro-behaviors/process/create/shell/spawn::python-bash-path
      - id: micro-behaviors/process/interpreter/shell::bin-sh
      - id: micro-behaviors/process/create/shell/spawn::bin-shell
  - id: python-dup2-c2
    desc: Python PTY reverse shell connects to a hardcoded IP and port
    crit: hostile
    conf: 0.99
    attack: T1059.006
    mbc: "OB0004"
    all:
      - id: python-socket-dup2-pty-revshell
      - id: micro-behaviors/communications/socket/dial::python-connect-ip-port
  - id: inline-python-reverse-shell
    desc: Inline Python socket dup2 reverse shell
    crit: hostile
    conf: 0.99
    attack: T1059.006
    mbc: "B0030.002"
    all:
      - id: inline-python-socket-dup2
      - id: inline-python-subprocess-shell
    any:
      - id: micro-behaviors/process/create/shell/lang::python-c-flag
      - id: micro-behaviors/process/interpreter/shell::bin-sh
      - id: micro-behaviors/process/create/shell/spawn::bin-shell
    # A recognized offensive-security tool legitimately ships reverse-shell
    # payloads in its corpus — dual-use, not "no legitimate use." The underlying
    # socket-dup2 / shell-spawn atoms remain at their own (suspicious) tier, so
    # the capability still surfaces; only the hostile verdict is withheld.
    # Defensive scanner harnesses also keep reverse-shell bytes as grade fixtures.
    unless:
      - id: well-known/lib/ai/unsloth::unsloth-package-context
      - id: well-known/tool/offensive/
      - id: well-known/tool/detection/agent-glovebox::staticscan-reverse-shell-fixture-context
  - id: inline-python-reverse-shell-c2
    desc: Inline Python reverse shell with C2
    crit: hostile
    conf: 0.995
    attack: T1059.006
    mbc: "B0030.002"
    all:
      - id: inline-python-reverse-shell
      - id: micro-behaviors/communications/socket/dial::python-connect-ip-port
  - id: python-socket-popen-stream-revshell
    desc: Python socket bridged to piped shell process
    crit: hostile
    conf: 0.98
    attack: T1059.006
    mbc: "B0030.002"
    all:
      - id: micro-behaviors/communications/socket/connect::socket-connect-method
      - id: micro-behaviors/process/fd/stdio::py-stdin-pipe
      - id: micro-behaviors/process/fd/stdio::py-stdout-pipe
      - id: micro-behaviors/process/fd/stdio::py-stderr-stdout
      - id: python-bash-executable-string
      - id: micro-behaviors/communications/socket/receive::socket-recv-call
    any:
      - id: micro-behaviors/communications/socket/send::python-sendall
      - id: micro-behaviors/communications/socket/send::socket-send-call
    unless:
      - id: metadata/package/testing/harness/runtime/
      - id: metadata/package/testing/presence/harness::verified-python-test-file
      - id: metadata/package/testing/presence/harness/
      - id: well-known/tool/detection/llm-scanner/
  - id: python-generic-socket-popen-revshell
    desc: Python socket-to-process reverse shell
    crit: hostile
    conf: 0.97
    attack: T1059.006
    mbc: "B0030.002"
    all:
      - id: micro-behaviors/process/fd/stdio::py-stdin-pipe
      - id: micro-behaviors/process/fd/stdio::py-stdout-pipe
      - id: micro-behaviors/process/fd/stdio::py-stderr-stdout
      - id: python-socket-io-forwarders
    any:
      - id: micro-behaviors/process/create/thread::python-thread-create
      - id: micro-behaviors/communications/socket/create::python-socket
      - id: micro-behaviors/communications/socket/receive::socket-recv-call
  - id: python-bash-socket-threaded-revshell
    desc: Python threaded socket-to-bash reverse shell
    crit: hostile
    conf: 0.96
    attack: T1059.006
    mbc: "B0030.002"
    all:
      - id: python-socket-popen-stream-revshell
      - id: micro-behaviors/process/create/thread::python-thread-create
      - id: python-bash-executable-string
    any:
      - id: micro-behaviors/communications/socket/send::python-sendall
      - id: micro-behaviors/communications/socket/receive::socket-recv-call
  - id: combo-reverse-shell
    desc: Classic reverse shell pattern (socket
    crit: hostile
    mbc: B0030.002
    attack: T1059
    conf: 0.95
    platforms:
      - unix
    all:
      - id: micro-behaviors/communications/socket/connect::connect
      - id: python-dup2
    any:
      - id: micro-behaviors/process/interpreter/shell::bin-sh
      - id: micro-behaviors/process/create/shell/spawn::bin-shell
    downgrade:
      any:
        - id: micro-behaviors/dylib/library/family/
        - id: micro-behaviors/dylib/library/family/
        - id: micro-behaviors/dylib/library/family/
  - id: python-fd-redirect-shell
    desc: Python socket shell FD redirection
    crit: hostile
    mbc: B0030
    attack: T1059.006
    conf: 0.98
    platforms: [unix]
    near_lines: 80
    all:
      - id: micro-behaviors/process/create/system::python-os-system
      - id: micro-behaviors/process/create/shell/spawn::bin-sh-interactive
      - id: micro-behaviors/process/create/shell/fd::shell-stdin-fd-redirect
      - id: python-fileno
    any:
      - id: objectives/command-and-control/backdoor/shell/socket::python-connect-call
      - id: micro-behaviors/communications/socket/connect::socket-bind-call

  - id: python-socket-dup2-pty-revshell
    desc: Python socket + os.dup2 + pty.spawn reverse shell
    crit: hostile
    conf: 0.99
    mbc: B0030.002
    attack: T1059.006
    all:
      - id: inline-python-socket-dup2
    any:
      - id: micro-behaviors/process/tty/pty::pty-spawn
      - id: micro-behaviors/process/tty/pty::pty-spawn-inline-shell-text
    unless:
      - id: well-known/tool/detection/llm-scanner/

  - id: python-socket-fd-bridge-pty-revshell
    desc: Python connects a socket FD to stdio and spawns a PTY shell
    crit: hostile
    conf: 0.98
    mbc: B0030.002
    attack: T1059.006
    all:
      - id: micro-behaviors/communications/socket/connect::socket-connect-method
      - id: python-dup2
      - id: python-fileno
      - id: micro-behaviors/process/tty/pty::pty-spawn
    near_lines: 20
    unless:
      - id: well-known/tool/detection/llm-scanner/

  # The canonical Python reverse shell, wired through Popen's stdio keywords
  # rather than `os.dup2`.
  #
  # The rules above all require `os.dup2`, which is one of the two ways to
  # attach a shell to a socket. The other -- and the one the widely copied
  # one-liner uses -- passes the descriptor straight to the child:
  # `Popen(['/bin/bash','-i'], stdout=s.fileno(), stdin=s.fileno(),
  # stderr=s.fileno())`. No `dup2` appears anywhere, so `python-dup2-io-redirect`
  # and everything built on it stays silent on the commonest spelling.
  #
  # Hostile on the triple: a socket connected outward, an interactive shell
  # spawned, and that socket's descriptor handed to it as stdio. Each is
  # ordinary alone -- servers connect, tools spawn shells, supervisors pass
  # descriptors -- and no legitimate program does all three at once, because
  # the result is a shell driven by whoever is on the other end.
  #
  # Reached through decoded content as well as source: the payload is often a
  # base64 blob handed to `exec`, and the decoded text is searched like any
  # other string.
  - id: python-socket-fileno-shell
    desc: Shell spawned on a connected socket descriptor
    crit: hostile
    conf: 0.97
    attack: T1059.006,T1571
    mbc: B0022
    size_max: 50000
    all:
      - id: python-fileno
      - id: micro-behaviors/communications/socket/connect::socket-connect-method
    any:
      - id: micro-behaviors/process/create/shell/spawn::python-bash-path
      - id: micro-behaviors/process/interpreter/shell::bin-sh
      - id: micro-behaviors/process/create/subprocess::call
    # Suppressions are the offensive-tool contexts and an *imported* test
    # framework -- deliberately not the path-based `is-test` /
    # `presence/harness/` families the sibling rules use. Those match on a file
    # or directory being named like a test, which is a name the author
    # chooses: this payload ships as `src/test.py` and would excuse itself.
    # A real test suite imports unittest or pytest; a reverse shell does not
    # stop being one because of where it sits.
    #
    # The agent-glovebox staticscan fixture is the deliberate exception: it is
    # identified by content (the fixture-only `attacker.test` C2 host) as well
    # as location, and the sibling `inline-python-reverse-shell` already trusts
    # it -- so this spelling of the same payload trusts it too.
    #
    # Unsloth's consent-gate tests quote the canonical reverse-shell one-liner
    # as a string literal to assert their gate blocks it -- quoted fixture,
    # not executed payload. The sibling socket-dup2 and inline-shell rules in
    # this file already trust the Unsloth package context.
    unless:
      - id: well-known/tool/offensive/pentestkit::local-storage-panel
      - id: well-known/tool/offensive/bingo/
      - id: metadata/import/python::unittest
      - id: metadata/import/python::pytest
      - id: well-known/tool/detection/agent-glovebox::staticscan-reverse-shell-fixture-context
      - id: well-known/lib/ai/unsloth::unsloth-package-context
