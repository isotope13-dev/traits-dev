# Groovy reverse shell: Socket + ["/bin/sh","-i"].execute() + stream wiring.
defaults:
  for: [groovy]
  platforms: [linux, macos, windows]
  attack: T1059
  mbc: B0030.002
traits:
  - id: groovy-python-dup2-stdio
    desc: Python dup2 stdio redirect
    crit: suspicious
    conf: 0.92
    if:
      type: text
      substr: os.dup2(s.fileno()
