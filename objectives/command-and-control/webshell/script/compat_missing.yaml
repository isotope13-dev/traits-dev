traits:
  - id: php-shell
    desc: compat placeholder
    crit: component
    conf: 0.1
    if:
      type: string
      exact: __cleave_missing_99a0099316cb__
defaults:
  crit: component
  conf: 0.1
  for:
    - all
