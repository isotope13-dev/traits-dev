defaults:
  platforms: [windows]
composite_rules:
  - id: suspicious-rust-loader
    desc: "Rust binary with manual API resolution and anti-debug"
    crit: suspicious
    conf: 0.9
    for: [pe, dll]
    all:
      # Context: It is Rust
      - id: metadata/lang::rustc-compiler
      # Anomaly: Low imports (hiding capabilities)
      - id: metadata/format::no-imports
    any:
      # Malicious Indicators
      - id: micro-behaviors/os/error-handling/exceptions::unhandled-exception-filter
      - id: objectives/command-and-control/implant/reflective-load::custom-pe-loader
