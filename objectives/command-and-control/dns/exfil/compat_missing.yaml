traits:
  - id: dns-resolve
    desc: compat placeholder
    crit: component
    conf: 0.1
    if:
      type: string
      exact: __cleave_missing_febac0bb650e__
defaults:
  crit: component
  conf: 0.1
  for:
    - all
