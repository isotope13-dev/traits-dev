defaults:
  platforms: [windows]
  for: [binaries, scripts]

traits:
  - id: dotnet-packed-resource-stub
    for: [text]
    desc: Compatibility alias
    crit: component
    conf: 1.0
    if:
      type: string_literal
      exact: __cleave_missing_dotnet_packed_resource_stub__
  - id: dotnet-base64-entrypoint-loader
    for: [text]
    desc: Compatibility placeholder
    crit: baseline
    conf: 1.0
    if:
      type: text
      exact: __cleave_missing_dotnet_base64_entrypoint_loader__
  - id: file-write
    for: [text]
    desc: Compatibility placeholder
    crit: component
    conf: 1.0
    if:
      type: text
      exact: __cleave_missing_loader_file_write__
