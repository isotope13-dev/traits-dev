defaults:
  attack: T1059.005
  platforms: [windows]
  for: [vbs]
composite_rules:
  # Numeric string-table reconstruction: the delimiter is sliced out of a
  # carrier with Mid, the table splits on it, each cell decodes through
  # arithmetic, and the rebuilt stage feeds Execute. The three legs are
  # each generic alone; together they are the table-reconstruction loader.
  - id: split-decoded-vbs-execute-stage
    desc: Split string table decoded and executed
    crit: hostile
    conf: 0.95
    all:
      - id: objectives/execution/interpreter/vbscript/loader::execute-decoder-call
      - id: objectives/execution/interpreter/vbscript/interpreter::vbs-mid-power-slice
      - id: micro-behaviors/data/encode/char-code::chr-division-decoder
  - id: obfuscated-vbscript-ipfs-dropper
    desc: Obfuscated VBScript IPFS dropper
    crit: hostile
    conf: 0.99
    all:
      - id: objectives/anti-static/obfuscation/string/delimiter::delimiter-packed-vbscript-stager
      - id: objectives/execution/interpreter/vbscript/wmi::hidden-wmi-powershell-stager
    any:
      - id: objectives/persistence/login/startup/folder::powershell-copyitem-self-to-startup
      - id: objectives/command-and-control/infrastructure/blockchain/ipfs::ipfs-gateway-hex-stager
