defaults:
  platforms: [windows]
  for: [scripts, binaries]

traits:
  - id: cross-platform-git-bash-exe
    for: [text]
    desc: Compatibility alias
    crit: component
    conf: 1.0
    if:
      type: string_literal
      exact: __cleave_missing_cross_platform_git_bash_exe__
