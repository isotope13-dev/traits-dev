# Unsigned PE that talks to a PHP gate and also creates a thread or writes
# registry persistence is a downloader, not a leftover URL in a document.
defaults:
  platforms: [windows]
  for: [pe]
  attack: T1105,T1071.001
  crit: hostile
  conf: 0.94

composite_rules:
  - id: php-gate-thread-downloader
    desc: PHP gate downloader creates a thread
    all:
      - id: objectives/command-and-control/infrastructure/domain/malicious::php-gate
      - id: micro-behaviors/process/create/thread::create-thread
    unless:
      - id: metadata/signed/platform/
      - id: metadata/binary/installer/family/
      - id: well-known/lib/

  - id: php-gate-registry-downloader
    desc: PHP gate downloader writes registry keys
    all:
      - id: objectives/command-and-control/infrastructure/domain/malicious::php-gate
      - id: micro-behaviors/os/registry/manipulate::registry-write-api-chain
    unless:
      - id: metadata/signed/platform/
      - id: metadata/binary/installer/family/
      - id: well-known/lib/

  - id: php-gate-explorer-run-dropper
    desc: PHP gate dropper uses Explorer Run policy
    all:
      - id: objectives/command-and-control/infrastructure/domain/malicious::php-gate
      - id: objectives/persistence/login/multi-technique::policies-explorer-run-key-path
    unless:
      - id: metadata/signed/platform/
      - id: metadata/binary/installer/family/
      - id: well-known/lib/

  # Clickers and compact downloaders talk to a PHP URL over WinInet without
  # also writing a Run key. The PHP endpoint plus the fetch APIs is the dropper.
  - id: php-url-wininet-openurl
    desc: PHP URL fetched through WinInet OpenUrl
    all:
      - id: micro-behaviors/communications/http/url/php-endpoint::url-php-endpoint-binary
      - id: micro-behaviors/communications/http/client/wininet::internet-open-url
    unless:
      - id: metadata/signed/platform/
      - id: metadata/binary/installer/family/
      - id: well-known/lib/

  - id: php-url-wininet-read
    desc: PHP URL read through WinInet
    all:
      - id: micro-behaviors/communications/http/url/php-endpoint::url-php-endpoint-binary
      - id: micro-behaviors/communications/http/client/wininet::internet-read-file
    unless:
      - id: metadata/signed/platform/
      - id: metadata/binary/installer/family/
      - id: well-known/lib/

  - id: php-url-wininet-session
    desc: PHP URL fetched in a WinInet session
    all:
      - id: micro-behaviors/communications/http/url/php-endpoint::url-php-endpoint-binary
      - id: micro-behaviors/communications/http/client/wininet::internet-open
    unless:
      - id: metadata/signed/platform/
      - id: metadata/binary/installer/family/
      - id: well-known/lib/
