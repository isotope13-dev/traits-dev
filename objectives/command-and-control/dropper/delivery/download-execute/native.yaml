# Native downloader flow: a raw-IP HTTP GET followed by a child-process launch.
defaults:
  platforms: [linux, unix]
  for: [elf]
  attack: T1105,T1059.004
  mbc: B0024,E1105

composite_rules:
  # This is a generic implant installer, not a cryptojacking-specific behavior:
  # fetch from a pinned address, grant execute permission, launch, and persist.
  - id: fetched-payload-executed-with-boot-persistence
    desc: Fetched payload executes with boot persistence
    crit: hostile
    conf: 0.93
    attack: T1105,T1543
    all:
      - id: objectives/execution/exploit/network-service/command-execution::download-permission-execute-chain
      - id: micro-behaviors/communications/http/url/external-ip::url-external-ip-binary
      - id: objectives/persistence/system/supervised::c-generic-systemd-unit-persistence

  - id: raw-ip-http-download-fork-execute
    desc: Native raw-IP HTTP downloader forks and executes payload
    crit: hostile
    conf: 0.99
    all:
      - id: micro-behaviors/communications/http/get::manual-http10-dynamic-get
      - id: micro-behaviors/communications/ip/literal::external-ipv4-binary
      - id: micro-behaviors/process/fork/clone::fork
      - id: micro-behaviors/process/create/execv::c-exec-variants
