defaults:
  platforms: [windows]
  conf: 0.9
traits:
  - id: msi-callfunctionfromdll
    desc: MSI CallFunctionFromDLL custom action
    crit: notable
    for: [pe, dll]
    if:
      type: string_value
      substr: "CallFunctionFromDLL"
