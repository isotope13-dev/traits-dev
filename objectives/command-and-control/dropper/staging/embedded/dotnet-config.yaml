defaults:
  platforms:
    - windows
  for:
    - pe
  attack: T1027
traits:
  - id: arrow-delimited-config
    desc: Arrow-delimited dropper config pattern
    crit: notable
    conf: 0.9
    if:
      type: string_value
      regex: \.exe-=>(?:True|False)
