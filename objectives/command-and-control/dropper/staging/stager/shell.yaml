# Shell Stager Patterns
# Detects command substitution and inline execution for download-execute
# ATT&CK: T1059.004 (Unix Shell)
defaults:
  platforms: [unix, android, ios]
  for: [shell]
  attack: T1059.004
traits:
  - id: shell-c-curl-subshell
    desc: Download-execute via shell -c with command substitution
    crit: suspicious
    conf: 0.85
    if:
      type: text
      regex: 'shell\s+-c\s+.{0,10}\$\(curl'
  - id: sh-c-curl-subshell
    desc: Download-execute via sh -c with command substitution
    crit: suspicious
    conf: 0.85
    if:
      type: text
      regex: 'sh\s+-c\s+.{0,10}\$\(curl'
  - id: shell-c-wget-subshell
    desc: Download-execute via shell -c with wget substitution
    crit: suspicious
    conf: 0.85
    if:
      type: text
      regex: 'shell\s+-c\s+.{0,10}\$\(wget'
composite_rules:
  - id: shell-subshell-stager
    desc: Shell stager via command substitution
    crit: suspicious
    conf: 0.9
    any:
      - id: shell-c-curl-subshell
      - id: sh-c-curl-subshell
      - id: shell-c-wget-subshell
  - id: shell-ip-curl-subshell-loader
    desc: Shell executes IP curl payload
    crit: hostile
    conf: 0.98
    all:
      - id: sh-c-curl-subshell
      - id: micro-behaviors/communications/ip/literal::http-ip-url
      - id: micro-behaviors/communications/http/download/curl::curl-silent
  - id: bash-c-curl-ip-loader
    desc: Bash command loads IP payload
    crit: hostile
    conf: 0.98
    all:
      - id: sh-c-curl-subshell
      - id: objectives/execution/lolbin/os::bash-c-execution
      - id: objectives/command-and-control/infrastructure/domain::ip-address-url
