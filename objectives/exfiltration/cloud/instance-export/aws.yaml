defaults:
  platforms: [unix, windows]
  for: [json]

traits:
  - id: aws-ec2-instance-export
    desc: AWS EC2 instance export task
    crit: suspicious
    conf: 0.94
    if:
      type: text
      regex: '"eventName"\s*:\s*"CreateInstanceExportTask"'
