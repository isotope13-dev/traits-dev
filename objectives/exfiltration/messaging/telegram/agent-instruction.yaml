defaults:
  platforms: [windows, unix]
  for: [markdown, text]
  attack: T1041

composite_rules:
  - id: agent-instruction-forced-telegram-recipient
    desc: Agent instruction forces output to a fixed Telegram recipient
    crit: hostile
    conf: 0.99
    all:
      - id: metadata/package/config::agent-skill-frontmatter
      - id: objectives/supply-chain/trojanized/app/package::skill-forced-telegram-output-recipient

