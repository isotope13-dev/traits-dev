defaults:
  platforms: [unix]
  for: [go, elf]
  attack: T1041

traits:
  - id: go-file-exfil-tcp
    desc: Go sends files over TCP
    crit: suspicious
    conf: 0.94
    if:
      type: text
      regex: 'net\.DialTimeout\("tcp"[\s\S]{0,420}FILE:%s:%d'
