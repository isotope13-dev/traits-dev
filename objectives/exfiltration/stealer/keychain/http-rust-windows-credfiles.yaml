defaults:
  platforms: [windows]
  for: [rust]
  attack: T1041

composite_rules:
  - id: rust-windows-beacon-credential-exfil
    desc: Rust Windows beacon credential exfil
    crit: hostile
    conf: 0.98
    all:
      - id: micro-behaviors/fs/path/credential::rust-windows-sensitive-file-list
      - id: objectives/command-and-control/beacon/poll::rust-agent-http-beacon
