defaults:
  platforms: [macos]
  for: [binaries, scripts]
  attack: T1537
composite_rules:
  - id: macos-comm-stealer
    desc: Multi-service macOS communication stealer
    crit: hostile
    conf: 0.98
    needs: 2
    any:
      - id: micro-behaviors/fs/path/sensitive/apple::keychain-data
      - id: micro-behaviors/fs/path/sensitive/apple::imessage-chat-db
      - id: micro-behaviors/fs/path/sensitive/apple::safari-binary-cookies
