defaults:
  platforms: [routeros, appliance, unix]
  for: [text, shell, python, javascript]
  attack: T1041

# NOTE: the *-read / *-inventory atoms below match the standard RouterOS CLI
# read commands (/interface print, /ip address print, /export, …). Those are
# exactly what a legitimate device-management/monitoring/backup tool runs —
# Ansible's community.routeros `facts` module issues all of them — so on their
# own they are neutral network-management reads (notable), NOT stealer signal.
# The stealer verdict comes from pairing a read with a `/tool fetch … post`
# upload leg, which the hostile composites at the bottom require.
composite_rules:
  - id: routeros-config-http-exfil
    desc: RouterOS config HTTP exfil
    crit: hostile
    conf: 0.98
    all:
      - id: micro-behaviors/os/sysinfo/config::routeros-config-export-read
      - id: objectives/exfiltration/http/collect::routeros-http-data-upload
    any:
      - id: micro-behaviors/os/sysinfo/config::routeros-firewall-inventory
      - id: objectives/exfiltration/http/collect::routeros-fetch-collect-url
