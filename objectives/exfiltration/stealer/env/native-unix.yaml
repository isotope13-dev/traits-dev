defaults:
  crit: hostile
  conf: 0.98
  platforms: [unix]
  for: [c, cpp, objc, elf]
  attack: T1041

composite_rules:
  - id: native-http-host-profile-exfil
    for: [elf, c]
    desc: Native HTTP host profile exfil
    all:
      - id: micro-behaviors/os/env/read::native-environ-reference
      - id: micro-behaviors/communications/http/upload::native-octet-stream-post
