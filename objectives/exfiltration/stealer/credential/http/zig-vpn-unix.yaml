defaults:
  platforms: [unix]
  for: [zig, elf]
  attack: T1041

composite_rules:
  - id: zig-vpn-credential-http-exfil
    desc: Zig VPN credential HTTP exfil
    crit: hostile
    conf: 0.98
    all:
      - id: objectives/credential-access/vpn/config::zig-vpn-credential-harvest
      - id: micro-behaviors/communications/http/endpoints::collect-post-request
      - id: micro-behaviors/communications/http/upload::octet-stream-upload-header
