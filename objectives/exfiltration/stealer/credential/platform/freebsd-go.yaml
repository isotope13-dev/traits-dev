defaults:
  platforms: [freebsd, unix]
  for: [go, elf]
  attack: T1041

composite_rules:
  - id: go-freebsd-credential-http-exfil
    desc: Go exfiltrates FreeBSD credentials
    crit: hostile
    conf: 0.98
    all:
      - id: objectives/credential-access/files/config::go-freebsd-credential-sweep
      - id: objectives/exfiltration/sensitive-data::go-sysinfo-http-exfiltration
