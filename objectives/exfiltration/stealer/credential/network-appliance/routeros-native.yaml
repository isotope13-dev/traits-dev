defaults:
  platforms: [routeros, appliance, unix]
  for: [c, cpp, objc, rust, go, elf]
  attack: T1041

composite_rules:
  - id: routeros-runtime-secret-http-exfil
    desc: RouterOS secret HTTP exfil
    crit: hostile
    conf: 0.98
    all:
      - id: objectives/credential-access/files/config::routeros-runtime-secret-paths
      - id: objectives/exfiltration/http/collect::native-collect-http-post
