defaults:
  for: [tar, zip]
  platforms: [unix, windows]
  attack: T1552.001,T1041

composite_rules:
  - id: go-package-credential-upload
    desc: Go helpers upload developer credentials
    crit: hostile
    conf: 0.95
    all:
      - id: developer-secret-source
      - id: micro-behaviors/communications/http/request/credentials::go-package-secret-body
