defaults:
  platforms: [unix]
  for: [java, kotlin, class, java_class, jar]
  attack: T1041

composite_rules:
  - id: jvm-config-secret-http-exfil
    desc: JVM config secret HTTP exfil
    crit: hostile
    conf: 0.98
    all:
      - id: objectives/credential-access/files/secret-sweep::jvm-bulk-config-secret-scan
      - id: objectives/exfiltration/http/collect::jvm-collect-octet-post
