defaults:
  platforms: [aix, unix]
  for: [rust]
  attack: T1041
composite_rules:
  - id: rust-sensitive-tcp-exfil
    desc: Rust sensitive TCP exfil
    crit: hostile
    conf: 0.97
    all:
      - id: micro-behaviors/communications/socket/connect::tcpstream-connect-timeout
      - id: micro-behaviors/communications/socket/send::rust-write-all
    any:
      - id: objectives/exfiltration/serialization::rust-file-exfil-record
      - id: objectives/exfiltration/serialization::rust-cred-exfil-record
    needs: 1
