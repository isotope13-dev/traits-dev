defaults:
  platforms: [linux, unix, macos]
  for: [shell]
composite_rules:
  - id: shell-recon-exfil
    desc: System fingerprinting with HTTP exfiltration
    crit: suspicious
    conf: 0.9
    attack: T1082
    mbc: E1082
    all:
      - id: micro-behaviors/communications/ip::http-ip-url
      - id: micro-behaviors/os/env/vars/cicd::shell-system-fingerprinting
