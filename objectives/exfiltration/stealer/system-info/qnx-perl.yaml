defaults:
  platforms: [qnx, unix]
  for: [perl]
  attack: T1041

traits:


  - id: qnx-perl-exfil-beacon-call
    desc: QNX Perl exfil beacon call
    crit: suspicious
    conf: 0.94
    if:
      type: text
      substr: beacon('exfil'

