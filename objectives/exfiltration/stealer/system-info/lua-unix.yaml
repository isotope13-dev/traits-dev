defaults:
  platforms: [unix]
  for: [lua]
  attack: T1041
  mbc: E1020

traits:
  - id: lua-hex-post-body
    desc: Lua hex-encodes POST body
    crit: suspicious
    conf: 0.94
    if:
      type: text
      regex: 'local encoded = _hex_encode\(data\)[\s\S]{0,80}blob='

  - id: lua-curl-post-file-cleanup
    desc: Lua curl POST then removes file
    crit: suspicious
    conf: 0.94
    if:
      type: text
      regex: 'curl -s[\s\S]{0,80}-X POST -d @%s[\s\S]{0,80}rm -f %s'

composite_rules:
  - id: lua-encoded-system-http-exfil
    desc: Lua encoded system HTTP exfil
    crit: hostile
    conf: 0.98
    all:
      - id: objectives/anti-static/obfuscation/encoding/hex::lua-long-hex-string-table
      - id: lua-hex-post-body
      - id: lua-curl-post-file-cleanup
      - id: micro-behaviors/process/create/shell/spawn::lua-io-popen
