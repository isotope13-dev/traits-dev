defaults:
  platforms: [unix]
  for: [c, cpp, objc, elf]
  attack: T1056

composite_rules:
  - id: unix-terminal-http-exfil
    desc: Unix terminal HTTP exfil
    crit: hostile
    conf: 0.98
    all:
      - id: objectives/collection/keylog/terminal::native-terminal-raw-capture
      - id: objectives/exfiltration/http/collect::native-collect-http-post
