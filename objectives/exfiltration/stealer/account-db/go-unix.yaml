defaults:
  crit: hostile
  conf: 0.98
  platforms: [unix]
  for: [go, elf, macho]
  attack: T1041

composite_rules:
  - id: go-sensitive-profile-exfil
    desc: Go sensitive profile exfil
    all:
      - id: micro-behaviors/fs/path/account-db::unix-shadow-root-ssh-paths
    any:
      - id: micro-behaviors/communications/http/url/telemetry::collect-post-request
      - id: micro-behaviors/communications/http/url/telemetry::go-format-collect-url
