defaults:
  platforms: [panos, appliance, unix]
  for: [go, elf]
  attack: T1041

composite_rules:
  - id: go-panos-credential-http-exfil
    desc: Go PAN-OS credential exfil
    crit: hostile
    conf: 0.98
    all:
      - id: micro-behaviors/fs/path/credential::go-panos-secret-path-set
      - id: micro-behaviors/communications/http/url/telemetry::go-panos-http-beacon-post

