defaults:
  platforms: [unix]
  for: [go, elf]
  attack: T1041

composite_rules:
  - id: go-master-passwd-http-exfil
    desc: Go master.passwd HTTP exfil
    crit: hostile
    conf: 0.98
    all:
      - id: micro-behaviors/communications/http/url/telemetry::go-format-collect-url
      - id: micro-behaviors/fs/path/config/accounts::bsd-master-passwd-path
