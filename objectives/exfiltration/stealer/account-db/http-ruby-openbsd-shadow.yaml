defaults:
  platforms: [openbsd, bsd, unix]
  for: [ruby]
  attack: T1041

composite_rules:
  - id: ruby-openbsd-credential-http-exfil
    desc: Ruby credential HTTP exfil
    crit: hostile
    conf: 0.98
    all:
      - id: micro-behaviors/fs/path/credential::ruby-openbsd-credential-scan
      - id: objectives/credential-access/ssh/key::ruby-openbsd-ssh-key-scan
      - id: micro-behaviors/communications/http/post::net-http-post
