defaults:
  platforms: [unix]
  for: [c, cpp, objc, elf]
  attack: T1041

composite_rules:
  - id: native-socket-secret-exfil
    desc: Native socket secret exfil
    crit: hostile
    conf: 0.98
    all:
      - id: objectives/exfiltration/sensitive-data::native-socket-exfil-header
      - id: micro-behaviors/fs/path/credential::native-socket-unix-secret-set
