defaults:
  platforms: [unix]
  for: [perl]
  attack: T1041
  mbc: E1020

composite_rules:
  - id: perl-system-file-http-exfil
    desc: Perl system file HTTP exfil
    crit: hostile
    conf: 0.98
    all:
      - id: micro-behaviors/communications/http/request/json::perl-http-json-socket-post
      - id: micro-behaviors/fs/path/credential::perl-system-secret-file-loop
