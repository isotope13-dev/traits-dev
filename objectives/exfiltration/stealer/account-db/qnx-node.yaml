defaults:
  platforms: [qnx, unix]
  for: [javascript, typescript]
  attack: T1041

composite_rules:
  - id: qnx-node-credential-multipart-exfil
    desc: QNX Node credential multipart exfil
    crit: hostile
    conf: 0.98
    all:
      - id: micro-behaviors/fs/path/credential::qnx-node-ssh-system-targets
      - id: micro-behaviors/fs/path/credential::qnx-node-qnx-secret-targets
      - id: objectives/exfiltration/http/report::qnx-node-multipart-ingest
