defaults:
  platforms: [netscaler, appliance, unix]
  for: [c, elf]
  attack: T1041

traits:
  - id: native-socket-send-func
    desc: Native socket send function
    crit: notable
    conf: 0.92
    if:
      type: text
      regex: '(^|[^A-Za-z0-9_])send_data'
    unless:
      - type: basename
        regex: '\.ko$'
      - id: objectives/evasion/kernel-hide/module::vermagic
      - type: section
        exact: .modinfo


