defaults:
  platforms: [windows, unix, appliance]
  for: [powershell]
  attack: T1041

traits:
  - id: ps-exfil-state-path
    desc: PowerShell exfil state path
    crit: suspicious
    conf: 0.93
    if:
      type: text
      substr: /exfil/state
