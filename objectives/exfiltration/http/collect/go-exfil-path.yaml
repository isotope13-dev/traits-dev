defaults:
  platforms: [windows]
  for: [go]
  attack: T1041

traits:
  - id: go-exfil-endpoint-path
    desc: Go HTTP exfil endpoint path
    crit: component
    conf: 0.9
    if:
      type: literal
      exact: /exfil
