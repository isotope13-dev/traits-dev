defaults:
  platforms: [unix]
  for: [shell]
  attack: T1041

traits:
  - id: shell-curl-stdin-collect
    desc: Shell curl stdin collect
    crit: suspicious
    conf: 0.94
    if:
      type: text
      regex: 'curl -s -X POST [^;\n]{0,120}/collect[\s\S]{0,120}--data-binary @-'
