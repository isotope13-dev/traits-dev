defaults:
  platforms: [qnx, unix]
  for: [go, elf]
  attack: T1041

traits:
  - id: qnx-go-collect-endpoint-path
    desc: QNX-style HTTP /collect endpoint path
    crit: suspicious
    conf: 0.94
    if:
      type: text
      regex: ':8080/collect([/?]|$)'
