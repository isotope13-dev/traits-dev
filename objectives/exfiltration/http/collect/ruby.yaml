defaults:
  for:
    - ruby
  platforms:
    - unix
    - windows

traits:
  - id: ruby-exfil-upload-post
    desc: Ruby exfil upload POST
    crit: suspicious
    conf: 0.94
    if:
      type: text
      regex: '/exfil/upload[\s\S]{0,260}Net::HTTP::Post'
