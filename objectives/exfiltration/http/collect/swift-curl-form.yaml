defaults:
  platforms: [unix]
  for: [swift]
  attack: T1041

traits:
  - id: swift-curl-form-collect-post
    desc: Swift curl form POST collect
    crit: suspicious
    conf: 0.94
    if:
      type: text
      regex: 'curl[^\n]{0,180}-X[^\n]{0,40}POST[^\n]{0,180}--form[^\n]{0,180}/collect'
