defaults:
  platforms: [freebsd, netbsd, unix]
  for: [swift]
  attack: T1041

traits:
  - id: swift-urlsession-collect-post
    desc: Swift URLSession POST collect
    crit: suspicious
    conf: 0.92
    if:
      type: text
      regex: 'URLComponents\(string:[\s\S]{0,160}/collect[\s\S]{0,260}request\.httpMethod = "POST"'
