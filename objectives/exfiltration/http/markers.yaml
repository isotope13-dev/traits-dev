defaults:
  platforms:
    - linux
    - macos
    - windows
    - unix
    - android
    - ios
  for:
    - binaries
    - scripts
    - source
    - manifests
    - documents
    - media
    - data
traits:
  - id: secret-key
    desc: Secret marker string (key/token/api)
    crit: notable
    conf: 0.6
    if:
      type: string_value
      regex: secret[_-]?(key|token|api)
      case_insensitive: true
    not:
      - regex: ^SecretKey$
      - regex: (?i)SecretKey(Selector|Ref|Volume|Source|Spec|Factory|Ring)
      - regex: (?i)NSS.{0,10}SecretKey
    downgrade:
      any:
        - id: metadata/signed/platform::apple
  - id: secret-other
    desc: Secret marker string (exfil/data/steal/grab)
    crit: notable
    conf: 0.6
    if:
      type: string_value
      regex: secret[_-]?(exfil|data|steal|grab)
      case_insensitive: true
    not:
      - regex: (?i)SecretData(Source|Provider|Wrapper)
    downgrade:
      any:
        - id: metadata/signed/platform::apple
composite_rules:
  - id: secret-comp
    desc: Secret marker string
    crit: notable
    conf: 0.6
    any:
      - id: secret-key
      - id: secret-other
