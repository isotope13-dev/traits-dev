defaults:
  platforms: [windows, unix]
  for: [rust]
  attack: T1041

traits:
  - id: rust-env-content-field
    desc: Rust dotenv content field
    crit: notable
    conf: 0.9
    if:
      type: text
      substr: envContent
