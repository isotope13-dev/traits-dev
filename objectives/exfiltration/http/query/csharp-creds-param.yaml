defaults:
  platforms: [unix, windows]
  for: [csharp, pe]
  attack: T1552.001

traits:
  - id: csharp-beacon-creds-param
    desc: C# beacon creds parameter
    crit: suspicious
    conf: 0.93
    if:
      type: text
      substr: "&creds="
