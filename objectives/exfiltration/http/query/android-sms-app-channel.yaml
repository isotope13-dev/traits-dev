defaults:
  platforms: [android]
  for: [android_apk, binaries]
  crit: notable
  conf: 0.86
  attack: "T1636.004"

traits:
  - id: sms-app-channel-flag
    desc: SMS payload app-channel marker
    crit: suspicious
    conf: 0.95
    if:
      type: text
      substr: "&from=app"
