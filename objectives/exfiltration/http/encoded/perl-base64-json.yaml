defaults:
  platforms: [unix, netbsd]
  for: [perl]
  attack: T1552.001

traits:
  - id: perl-base64-json-exfil
    desc: Perl base64 JSON exfil
    crit: suspicious
    conf: 0.94
    if:
      type: text
      regex: 'JSON::PP[\s\S]{0,260}encode_base64'
