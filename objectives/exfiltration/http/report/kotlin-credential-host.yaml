defaults:
  platforms: [android]
  for: [kotlin, class]
  attack: T1041

traits:
  - id: kotlin-credential-exfil-host
    desc: Kotlin credential exfil host
    crit: suspicious
    conf: 0.92
    if:
      type: text
      regex: 'EXFILTRATION_HOST|/api/v1/credentials'
