defaults:
  platforms: [unix, netbsd, windows]
  for: [powershell]
  attack: T1041

traits:
  - id: ps-collect-post-exfil
    desc: PS collect POST exfil
    crit: suspicious
    conf: 0.94
    if:
      type: text
      regex: 'Invoke-RestMethod[\s\S]{0,180}/collect[\s\S]{0,120}POST'
