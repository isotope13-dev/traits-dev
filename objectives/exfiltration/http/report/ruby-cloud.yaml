defaults:
  platforms: [unix, windows]
  for: [ruby]
  attack: T1041

traits:
  - id: ruby-json-exfil-post
    desc: Ruby JSON exfil POST
    crit: suspicious
    conf: 0.9
    if:
      type: text
      regex: '/api/v1/exfil[\s\S]{0,500}Net::HTTP::Post'
