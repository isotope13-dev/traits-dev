defaults:
  platforms: [windows]
  for: [pe]
  attack: T1041

traits:
- id: ahk-process-post-open
  desc: AutoHotkey process list POST
  crit: suspicious
  conf: 0.98
  attack: T1071.001
  if:
    type: text
    exact: 'http.Open("POST", "http://ise.ms/b7.php", false)'
