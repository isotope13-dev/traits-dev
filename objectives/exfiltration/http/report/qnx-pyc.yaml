defaults:
  platforms: [qnx, unix]
  for: [python, pyc]
  attack: T1041

traits:
  - id: qnx-pyc-sha256-exfil-header
    desc: QNX pyc sha256 exfil header
    crit: suspicious
    conf: 0.94
    for: [pyc]
    if:
      type: text
      substr: 'X-Checksum:'
