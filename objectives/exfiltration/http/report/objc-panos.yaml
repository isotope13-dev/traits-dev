defaults:
  platforms: [panos, appliance, unix]
  for: [objc, c, elf, macho]
  attack: T1041

traits:
  - id: objc-panos-credential-exfil-path
    desc: ObjC PAN-OS credential exfil path
    crit: suspicious
    conf: 0.94
    if:
      type: text
      substr: POST /api/credentials/exfil
