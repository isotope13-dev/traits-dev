defaults:
  platforms: [unix]
  for: [shell]
  attack: T1555.003

traits:
  - id: shell-browser-creds-post
    desc: Shell browser creds POST
    crit: suspicious
    conf: 0.94
    if:
      type: text
      regex: '--data-binary @-[\s\S]{0,160}/api/creds'
