defaults:
  platforms: [unix]
  for: [go, elf]
  attack: T1552.001

traits:
  - id: go-postdata-credential
    desc: Go posts credential data
    crit: suspicious
    conf: 0.94
    if:
      type: text
      substr: 'postData(client, target, "credential"'
