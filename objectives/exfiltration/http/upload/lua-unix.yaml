defaults:
  platforms: [unix, fortios]
  for: [lua]
  attack: T1041

traits:
  - id: lua-curl-post-temp-file
    desc: Lua curl POST temp file
    crit: suspicious
    conf: 0.98
    if:
      type: text
      regex: 'curl -s -X POST -d @.{0,80}/tmp/\.[A-Za-z0-9_./-]+'
