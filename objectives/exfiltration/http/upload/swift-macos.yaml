defaults:
  platforms: [unix]
  for: [swift]
  attack: T1041

traits:
  - id: swift-urlrequest-collect-post
    desc: Swift URLRequest collect POST
    crit: suspicious
    conf: 0.94
    if:
      type: text
      substr: /collect

composite_rules:
  - id: swift-http-collect-upload
    desc: Swift HTTP collect upload
    crit: hostile
    conf: 0.98
    all:
      - id: swift-urlrequest-collect-post
      - id: micro-behaviors/communications/http/post::swift-urlrequest-post-method
