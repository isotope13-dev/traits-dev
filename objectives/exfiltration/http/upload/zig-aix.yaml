defaults:
  platforms: [aix, unix]
  for: [zig, elf]
  attack: T1041
traits:
  - id: zig-http-post-exfil-request
    desc: Zig HTTP POST exfil request
    crit: suspicious
    conf: 0.93
    if:
      type: text
      substr: 'POST /exfil HTTP/1.1'
