defaults:
  platforms: [ios, unix]
  for: [zig]
  attack: T1041

traits:

  - id: zig-ios-log-payload-exfil
    desc: Zig iOS log payload exfil
    crit: suspicious
    conf: 0.92
    if:
      type: text
      regex: 'const payload = log\.items|exfiltrateData\(payload\)'

