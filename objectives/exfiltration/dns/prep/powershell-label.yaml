defaults:
  platforms: [netscaler, appliance, windows]
  for: [powershell]
  attack: T1555.003

traits:
  - id: ps-dns-exfil-query
    desc: PowerShell DNS exfil query
    crit: suspicious
    conf: 0.93
    if:
      type: text
      substr: '.exfil.${Script:C2Host}'
