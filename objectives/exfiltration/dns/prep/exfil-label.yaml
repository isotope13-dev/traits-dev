defaults:
  platforms: [netscaler, appliance, unix]
  for: [scripts, binaries, go, java, kotlin, csharp, swift, zig, elf]
  attack: T1041

traits:
  - id: dns-exfil-label
    desc: DNS exfil label
    crit: notable
    conf: 0.78
    if:
      type: text
      substr: .exfil.
