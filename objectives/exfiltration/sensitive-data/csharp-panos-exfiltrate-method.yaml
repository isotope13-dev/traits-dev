defaults:
  platforms: [panos, appliance, windows]
  for: [csharp, pe]
  attack: T1041

traits:

  - id: csharp-byte-array-exfil-method
    desc: C# byte array exfil method
    crit: suspicious
    conf: 0.94
    if:
      type: text
      substr: 'Exfiltrate(byte[] data)'

