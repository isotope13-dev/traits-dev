defaults:
  platforms: [unix]
  for: [c, cpp, objc, elf]
  attack: T1041

traits:
  - id: native-socket-exfil-header
    desc: Native socket exfil header
    crit: suspicious
    conf: 0.92
    if:
      type: text
      substr: EXFIL|file=%s
