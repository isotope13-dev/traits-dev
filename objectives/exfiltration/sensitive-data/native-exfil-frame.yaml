defaults:
  platforms: [unix, appliance]
  for: [c, cpp, objc, elf]
  attack: T1041

traits:
  - id: native-credential-exfil-frame
    desc: Native credential exfil frame
    crit: suspicious
    conf: 0.93
    if:
      type: text
      substr: 'EXFIL|%s|%zu|'
