defaults:
  platforms: [unix]
  for: [javascript]

traits:
- id: shell-find-cat-all-files
  desc: Shell reads every file via find cat
  crit: suspicious
  conf: 0.97
  attack: T1005
  if:
    type: text
    regex: 'find\s+/[^\n"''`]{1,80}\s+-type\s+f\s+-exec\s+cat\s+\{\}\s+\\\\;'
