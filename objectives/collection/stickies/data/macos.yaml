defaults:
  platforms: [linux, unix, macos, android, ios]
  crit: suspicious
  conf: 0.9
  attack: T1005
  for: [shell, python]
composite_rules:
  - id: stickies-collection
    desc: Apple Stickies data collection
    all:
      - id: micro-behaviors/fs/path/sensitive/apple::stickies-data
      - id: micro-behaviors/fs/directory/traverse::traverse-walk
