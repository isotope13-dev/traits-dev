defaults:
  platforms: [panos, appliance, unix]
  for: [c, elf]
  attack: T1041

traits:
  - id: c-panos-tar-remove-archive
    desc: C PAN-OS tar remove archive
    crit: suspicious
    conf: 0.94
    if:
      type: text
      substr: tar czf '%s' --remove-files
