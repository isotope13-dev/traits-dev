traits:
  - id: screen-capture
    desc: compat placeholder
    crit: component
    conf: 0.1
    if:
      type: string
      exact: __cleave_missing_c345c01dbb5e__
defaults:
  crit: component
  conf: 0.1
  for:
    - all
