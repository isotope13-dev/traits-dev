defaults:
  platforms: [windows, unix]
traits:
  - id: telemetry-logging
    desc: Extensive telemetry/event logging
    crit: notable
    conf: 0.7
    for:
      - javascript
      - typescript
    if:
      type: string_literal
      regex: name:\s*"tracker"
