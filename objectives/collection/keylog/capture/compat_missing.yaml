traits:
  - id: osascript-exec-compat
    desc: compat placeholder
    crit: baseline
    conf: 0.1
    if:
      type: text
      exact: __cleave_missing_20af68ad0983__
  - id: python-network-exfil
    desc: compat placeholder
    crit: baseline
    conf: 0.1
    if:
      type: text
      exact: __cleave_missing_f1f9808bf5ef__
defaults:
  platforms:
    - unix
    - windows
  crit: baseline
  conf: 0.1
  for: [scripts, binaries]
