defaults:
  platforms: [unix, fortios]
  for: [lua]
  attack: T1056.001

traits:
  - id: lua-terminal-raw-mode-keylog
    desc: Lua terminal raw-mode keylog
    crit: suspicious
    conf: 0.98
    if:
      type: text
      regex: 'stty -echo -icanon[\s\S]{0,260}io\.read\s*\(\s*1\s*\)'
