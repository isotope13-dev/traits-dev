# DarkGate Stealer Behavioral Fingerprint
defaults:
  for: [pe, dll]
  platforms: [windows]
  attack: T1119
composite_rules:
  - id: darkgate-harvester-pattern
    desc: Behavioral fingerprint of DarkGate credential harvester
    crit: suspicious
    conf: 0.95
    all:
      - id: micro-behaviors/data/db/conn/sqlite::sqlite
      - id: micro-behaviors/fs/enumerate/directory::find-file-walker-wide
      - id: micro-behaviors/os/registry/access::registry-transacted-api-chain
    downgrade:
      any:
        - id: metadata/format::many-imports
