defaults:
  platforms: [unix, windows]
  for: [javascript, typescript]
  attack: T1114
  mbc: E1114

traits:
  - id: provider-email-extractor-entrypoint
    desc: Runs automated provider email extraction
    crit: notable
    conf: 0.97
    if:
      type: function
      exact: runEmailExtractor

  - id: provider-label-table
    desc: Defines multiple lead-data providers
    crit: notable
    conf: 0.95
    if:
      type: symbol
      kind: identifier
      exact: PROVIDER_LABELS

  - id: email-provider-api-key-fragment
    desc: Email or lead API-key fragment
    crit: component
    conf: 0.9
    if:
      type: text
      regex: '(?i)(email|lead|contact|prospect).{0,160}api[_-]?key'

  - id: api-key-email-provider-fragment
    desc: API key near email-provider fragment
    crit: component
    conf: 0.9
    if:
      type: text
      regex: '(?i)api[_-]?key.{0,160}(email|lead|contact|prospect)'

  - id: email-result-writer-reference
    desc: Uses an email-result writer
    crit: notable
    conf: 0.96
    if:
      type: text
      regex: '(?i)\bcreateEmailWriter\b'

composite_rules:
  - id: provider-api-key-templates
    desc: Email-provider API-key templates
    crit: component
    conf: 0.9
    any:
      - id: email-provider-api-key-fragment
      - id: api-key-email-provider-fragment

  - id: provider-email-harvest
    desc: Automates multi-provider email-address harvesting
    crit: suspicious
    conf: 0.96
    all:
      - id: provider-email-extractor-entrypoint
      - id: provider-label-table
      - id: provider-api-key-templates
      - id: email-result-writer-reference

  - id: obfuscated-provider-email-harvester
    desc: Obfuscated multi-provider email harvester with persisted results
    crit: hostile
    conf: 0.98
    all:
      - id: provider-email-harvest
      - id: objectives/supply-chain/hidden-payload/runtime::obfuscator-io-toolkit-bundle
