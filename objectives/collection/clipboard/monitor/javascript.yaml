defaults:
  platforms: [windows, unix]
  attack: T1115
  for:
    - javascript
composite_rules:
  - id: js-cli-clipboard-readwrite
    desc: Reads and rewrites the system clipboard via CLI helpers
    crit: suspicious
    conf: 0.9
    all:
      - id: micro-behaviors/os/clipboard/access::node-cli-clipboard-read
      - id: micro-behaviors/os/clipboard/access::node-cli-clipboard-write
