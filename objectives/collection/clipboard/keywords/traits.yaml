defaults:
  platforms: [unix, windows]
  for: [binaries, documents, manifests, scripts, source]
  crit: component
  conf: 0.6
traits:
  - id: clipboard-token
    desc: Clipboard keyword
    if:
      type: text
      substr: clipboard
      case_insensitive: true
