defaults:
  for: [pe]
  platforms: [windows]
composite_rules:
- id: directx-desktop-duplication-capture
  desc: DirectX desktop duplication style capture
  crit: notable
  conf: 0.88
  all:
  - id: micro-behaviors/hardware/display/directx/calls::gpu-graphics-access
  any:
  - id: gdi-window-capture
  - id: gdi-gdiplus-capture
  - id: gdi-dib-capture
